# Product

## Register

product

## Users

财务人员、运营管理人员和系统管理员在日常办公环境中使用本平台。核心场景是导入票据和流水、核对 OA 与银行流水、追踪发票使用和收款状态、处理异常、导出台账，并保留可审计的业务事实。

## Product Purpose

`fin-ops-platform` 是以银企核销为核心的财务运营平台。产品通过结构化导入、关联工作台、读模型页面、后台任务和 OA 集成，帮助财务团队更快确认支付、收款、发票和台账之间的关系，并让处理结果可搜索、可导出、可追溯。

## Brand Personality

克制、清晰、可靠。界面应服务于扫描、比较和处理，不制造营销感或装饰性负担。用户需要在密集数据中快速判断状态、金额、对象和下一步动作。

## Anti-references

不要做营销型 hero、装饰性卡片堆叠、低密度信息流、过度圆角、强装饰阴影、单一大面积色系或不符合财务工具习惯的实验式控件。表格、分栏、抽屉、状态提示和导出入口应保持稳定、熟悉、可预测。

## Design Principles

- 密集但可读：表格和工作台优先显示完整业务字段，字号、间距和列宽为扫描效率服务。
- 事实优先：影响业务状态的标签和结果必须来自结构化字段、后端契约和审计记录。
- 入口克制：按钮和筛选入口只在有明确任务价值时出现，避免挤压关键字段。
- 状态明确：刷新、空态、错误、权限不足和后台任务状态必须给出清楚反馈。
- 架构一致：前端只展示和触发业务操作，业务逻辑在 service，持久化在 repository，后台任务在 worker/service。

## Accessibility & Inclusion

默认满足 WCAG AA 的对比度和键盘可达性。图标按钮必须有可访问名称，状态颜色不能作为唯一信息来源。动画只用于状态反馈，并遵守减少动态效果偏好。
