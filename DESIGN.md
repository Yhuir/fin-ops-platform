# 产品与界面设计原则

本文档记录长期设计约束。具体业务需求放在 `docs/product-specs/`，具体前端实现放在 `docs/dev/frontend.md`。

## 产品判断

- 平台面向财务人员的日常核对、追溯和处理，不是营销型页面。
- 页面优先支持扫描、比较、批量处理、导出和审计追踪。
- 所有看似“标签”的状态，如果影响业务事实，必须有后端结构化字段和审计记录。
- 前端可以做局部乐观更新，但不能成为业务事实源。

## 工作台界面

- 关联工作台以 OA、银行流水、发票三栏为核心。
- 三栏展示应接近表格/工作表体验，避免卡片堆叠造成信息密度不足。
- 搜索、筛选、排序按当前栏驱动，但整组联动，不能打散同一候选组的上下文。
- 已配对、未配对、异常处理后的状态必须能被搜索、导出和追溯。
- 折叠摘要必须由后端 group 契约表达，不能只靠前端隐藏行。

## 表单和操作

- 导入、数据重置、批量提交等高影响操作必须有预览、确认和结果反馈。
- 阻塞式操作需要明确进度；长任务进入后台任务体系。
- 权限不足时不只隐藏按钮，后端也必须返回明确的 401/403。

## 视觉约束

- 业务工具页面保持密集、清晰、稳定，不做大面积装饰性 hero。
- 表格、分栏、抽屉和弹窗优先使用既有组件体系。
- 控件尺寸和列宽要稳定，避免动态文案导致布局跳动。
- 状态颜色必须服务于风险识别，不能让页面被单一色系支配。
