# 测试闭环状态

本文是测试闭环 master goal 的控制状态。每轮只处理一个模块或共享边界；结束时必须更新本文件，再根据状态选择下一轮。

## 2026-07-12 - 三组可逆 relation checkpoint 闭环

- 架构：复用唯一 `write_operation_e2e_smoke`，legacy 输入只在 load 阶段映射为单 checkpoint；可执行 shape/consumer 合同位于部署包内的 runner 常量，impact matrix 仅是测试约束的文档镜像，生产运行不读取 `docs/`。新链路不新增 relation/queue/freshness/Audit owner，也不改变业务 HTTP DTO。
- 证据：首次写前先通过固定 admin-only System Audit；每个 checkpoint 用唯一 idempotency key 从 durable committed record 取得 exact event IDs，再独立验证 fan-out/worker/freshness、runtime contract 全量 affected 页面 fixture identity、non-consumer 写前/写后 equality 和新 17/16 页 System Audit。consumer assertion 只能落在各正式 API 的业务数据根；成本统计 explorer 使用当前 view-specific `rows` 根，禁止恢复已退出 HTTP 合同的 `time_rows`/`bank_flow_time_rows`/`project_rows`/`expense_type_rows`，也不能用 freshness 元数据冒充业务证明。confirm 后 gate 失败走正式 recovery withdraw；withdraw 已提交但后置 gate 失败不会再次撤回；ambiguous network 只输出人工恢复证据，不盲写。
- 三 shape：bank+invoice、bank+turnover closure、bank+OA+invoice。bank+invoice profile 已按真实 Workbench planner 补 `cost_statistics`；bank+turnover 改走正式 turnover closure confirm/withdraw profile；full 三方 profile补 required `oa_pending_payment`；税金抵扣继续是 Workbench relation non-consumer isolation。
- 旧链路：普通 turnover/Workbench/no-OA production candidates 不再自动生成可执行 mutation；对应旧 builders/probe 已删除，候选只读。bank-flow 的独立正式 owner 保留。
- Browser：`workbench-withdraw-flow.spec.ts` 在撤回后同时验证银行明细候选标签、待找发票待开票、进项候选、OA 未支付候选、成本项目移除；confirm fan-out 与 tax-offset isolation 继续由现有定向 specs 覆盖。
- 真实环境边界：opt-in PostgreSQL 证据测试必须使用 visibly disposable `FIN_OPS_TEST_DATABASE_URL`；未提供时明确 skip。没有 test-owned 生产 fixture 时不得把 deterministic/contract 测试描述为生产业务数据已执行闭环。

## 2026-07-10 - Operations Audit 模块边界与响应合同闭环

- 架构边界：三个只读审计入口统一为 `server route -> OperationsAuditService -> PostgresOperationsAuditRepository`，route 不再读取 `LocalStateStore._connection` 或 import CLI tool；CLI 只负责参数、连接和输出，SQL/规则归 PostgreSQL repository。进项/销项原两套近重复审计已收敛为单一 `InvoiceReadModelAuditContract` 核心和两个方向薄适配。
- I/O 合同：审计响应明确拆分 `audit_status.integrity/freshness/queue`，问题字段统一为有上限的 `*_sample_count`、`issue_sample_limit_per_code` 和 `issue_samples_truncated`，不再把样本数伪装为精确问题总数；页面 durable dirty/outbox 查询按 tenant 隔离。前端只把显式 `fresh` 当作 fresh，并保留 Workbench 500 响应的 `requestId`。
- 旧链路：当前代码和长期文档已移除 automatic-candidate 拆分 Browser 成功流；automatic candidate 无 active relation 时继续 fail closed，负面测试保留 `split_candidate` 作为必须拒绝的旧 operation 输入，不作为兼容路径。
- 本轮定向验证：Operations Audit/App Health/CLI/架构守卫组合 `277 tests` 全部通过；前端全量 `71 files / 823 tests` 全部通过，生产 build 通过；`web/e2e/app-shell.spec.ts` Chromium `4 tests` 全部通过并覆盖 admin 只读 Audit GET/结构化 sample 文案/零 mutation；`bash scripts/verify.sh lint`、`bash scripts/verify.sh docs`、`git diff --check` 均通过。
- 全量后端基线：`bash scripts/verify.sh backend` 运行 `4231 tests`，本轮唯一相关的架构守卫截取错误已修复并复测通过；仍有 `15 failures / 25 skipped`，集中在成本统计、迁移 pin、权限写入口清单、read-model fresh allowlist、旧 E2E 证据、settings reset 和 Workbench characterization 等未触碰基线，不能归为本轮 Operations Audit 回归，也不能宣称全仓后端全绿。
- 生产只读证据：用本机 secret wrapper 对 2 个发票审计和 7 个 page audit 做 9 次只读 GET，全部 HTTP 200；当前线上仍返回旧响应合同，`pending_invoices=pass`，其余 8 个域为 `issues_found`。本轮未获生产部署或数据修复写权限，因此本地边界修复完成，但生产合同切换和线上数据问题仍是显式 external gate。

## 2026-06-20 - 销项收款导出 XLSX workbook 解析闭环

- 本轮把 `output-invoice-collections` 导出从 CSV 文本伪装 `.xlsx` 升级为 deterministic XLSX workbook；Browser E2E 下载后解析 workbook，再断言普通筛选导出字段和红蓝票 relation 字段。
- 测试覆盖：`web/e2e/output-invoice-collections-flow.spec.ts` 验证当前筛选导出、请求不带分页、文件名、业务字段、row-limit 零下载错误和无隐藏浏览器错误；`web/e2e/output-invoice-red-relation-fanout.spec.ts` 验证红蓝票确认后导出 workbook 包含 `红蓝票关系`、`红蓝票来源`、`红蓝票依据`、红字发票号、`manual` 和确认依据，并继续验证税金抵扣/成本统计 fresh downstream fan-out。
- 产品侧决策：本轮只加固 deterministic mock 和 Playwright 断言，不改产品逻辑、不改后端导出实现。
- 验证：`cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts e2e/output-invoice-red-relation-fanout.spec.ts --project=chromium` 通过 12 tests。
- 当前边界：销项收款本地 Browser 已能防止“下载事件成功但文件不是 workbook”的假阳性；进项发票使用、成本统计等其它页面导出仍需要逐页迁移到 workbook 解析，生产 Nginx/OA 代理 header、Excel/Numbers 实际打开结果、真实大数据导出和真实 worker/read model drain 仍归后续页面级 E2E 或 staging/runtime smoke。

## 2026-06-20 - 银行明细和待找发票导出 XLSX workbook 解析闭环

- 本轮推进全局 `DOWNLOAD-EXPORT` partial 中最明确的一条假阳性风险：银行明细和待找发票 deterministic export mock 不再把 CSV 文本伪装成 `.xlsx`，而是返回最小真实 XLSX workbook；Browser E2E 下载后会解析 workbook 并断言业务字段。
- 测试覆盖：`web/e2e/bank-details-export-download.spec.ts` 继续验证 Workbench confirm 后银行明细导出的 linked relation 字段；`web/e2e/bank-details-filtered-export-permissions.spec.ts` 的当前账户 + 关键字 + 分类、日期/分页不限制导出、`read_export_only` 下载权限三条路径也改为解析 XLSX 后断言字段；`web/e2e/pending-invoices-export-download.spec.ts` 验证 Workbench confirm 后待找发票当前筛选/排序导出、OA/发票/relation 字段和 row-limit 零下载错误。
- 产品侧决策：本轮只加固 deterministic mock 和 Playwright 断言，不改产品逻辑、不改后端导出实现。
- 验证：`cd web && npx playwright test e2e/bank-details-export-download.spec.ts e2e/bank-details-filtered-export-permissions.spec.ts --project=chromium` 通过 7 tests；`cd web && npx playwright test e2e/pending-invoices-export-download.spec.ts --project=chromium` 通过 2 tests。
- 当前边界：银行明细和待找发票本地 Browser 已能防止“下载事件成功但文件不是 workbook”的假阳性；其它页面导出仍需要逐页迁移到 workbook 解析，生产 Nginx/OA 代理 header、Excel/Numbers 实际打开结果、真实大数据导出和真实 worker/read model drain 仍归后续页面级 E2E 或 staging/runtime smoke。

## 2026-06-20 - 后端 full regression 失败收敛

- 本轮把前一轮后端 full regression 中剩余的 7 failures / 1 error 收敛为全绿：`PYTHONPATH=backend/src python3 -m unittest discover -s tests -v` 通过 3279 tests、25 skipped。
- 修复边界：Workbench confirm-link 相关 characterization / idempotency / stale-write 测试补入已文档化的金额不一致确认备注，避免把“必须写备注”的业务校验误判为 UoW/idempotency 失败；`test_workbench_v2_api` 中 legacy mark/oa-bank exception 断言收敛为 lifecycle invalidation 覆盖 `all`、operation-level persist target 只包含月份，和 Browser barrier 不等待 `all` 的现行规格一致。
- OA/read model 边界：`MongoOAAdapter` 普通 projection 继续尊重 settings 的 status filter，默认只投影 completed；`list_application_records_by_row_ids` 明确绕过 status settings 读取 completed + in_progress，用于 OA 待付款 `PaymentAdmittedOAProjectionAdapter` 按 `t_payment_simple.flow_id` 准入，避免设置页 completed-only 导入口径误伤进行中 OA 待付款 read model。
- Bootstrap 边界：生产初始化时 `OaPendingPaymentQueryService` 复用已构造的 OA adapter 创建 payment-admitted projection，不再在生产 API bootstrap 中间接构造 direct OA Mongo adapter；对应 runtime bootstrap 回归通过。
- 迁移/test pin 边界：PostgreSQL Stage 06 migration pin 更新到 `0072_oa_pending_payment_workflow_status.sql`；read model architecture guard 为销项收款 export preview 的 fresh 状态补显式 allowlist，原因限定为它来自已 fresh-gated 的 all-rows read model 路径。
- 定点验证：`tests.test_workbench_write_characterization` 47 tests 通过；`tests.test_postgres_test_utils` 5 tests 通过；`tests.test_workbench_idempotency_contract` 目标用例通过；`tests.test_workbench_stale_write_contract` 3 tests 通过；`tests.test_read_model_architecture_guards` 4 tests 通过；`tests.test_runtime_bootstrap.RuntimeBootstrapTests.test_production_bootstrap_does_not_construct_direct_oa_mongo_adapter` 通过；OA 纯行组装规则由 `tests.test_oa_pending_payment_projection_rows` 覆盖；Settings/Mongo/OA pending/payment status/API 组合测试通过；Workbench exception scope 组合 4 tests 通过。
- 当前边界：本地后端 full regression 已全绿，但 25 项 PostgreSQL/RabbitMQ/真实外部依赖 integration 因缺少 `FIN_OPS_TEST_DATABASE_URL`、`RABBITMQ_TEST_URL` 等环境被跳过；这仍不证明真实 staging/生产 worker drain、RabbitMQ transport、PostgreSQL durable queue、OA Mongo/MySQL 和浏览器端全页面闭环已完成。下一轮应继续从逐页面 Spec-first Browser E2E 或真实 infra smoke 中选择最高风险缺口。

## 2026-06-20 - 默认 infra-smoke gate 复核

- 本轮复核 read model / worker / runtime closure 的默认安全 gate：`bash scripts/verify.sh infra-smoke` 通过 77 tests、18 skipped。
- 覆盖范围：read model SLO smoke 工具合同、runtime sync closure gate 合同、write-operation SLO audit profiles、production external gate preflight、RabbitMQ staging preflight 工具合同均通过；secret 值保持 redacted。
- 外部输入边界：当前环境未设置 `FIN_OPS_TEST_DATABASE_URL`，PostgreSQL runtime integration 18 项被跳过；未设置 `RABBITMQ_TEST_URL`，RabbitMQ integration 被跳过；未设置 authenticated HTTP token/cookie、admin/OA Browser token、write scenario、approval ticket 和 Postgres URL，`production_route_shell_browser`、`production_admin_app_health_browser`、authenticated HTTP SLO 和 `write_operation_apply` 均报告 `external_input_required`。
- 当前判断：这证明本地 gate 和 preflight 合同可用，不证明真实 worker drain、真实业务写入 fan-out、真实 OA/生产 Browser 或 staging RabbitMQ 已闭环。若要把 read model / worker 最新状态闭成生产级证据，下一轮必须提供 staging/生产 env 并显式运行 dry-run 或已批准的 `FIN_OPS_INFRA_SMOKE_APPLY=1` / write-operation audit。

## 2026-06-20 - 销项收款 collection-reminder mutation 暂时失败重试恢复

- 本轮补齐 `output-invoice-collections` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/output-invoice-collections-flow.spec.ts` 新增 `PUT /api/output-invoice-collections/rows/{id}/collection-reminder` 暂时返回 503 后用户重试成功的场景，覆盖 status 已经保存成功但 reminder 保存失败的分步半成功风险。
- 产品侧修复：`CollectionStatusReminderDrawer` 不再让 `onSaveStatus` 自己刷新 rows；只有 status/reminder 整体保存成功后才通过 `onChanged` 触发 rows refresh。status 已成功但 reminder 失败时，drawer 保留错误和提醒草稿，并记录已成功保存的 status fingerprint；重试且 status payload 未改变时只重新提交 reminder，避免用旧 `expectedVersion` 重复保存 status。
- 测试断言第一次保存中 status 200、reminder 503 后错误提示可见、drawer/提醒时间/提醒备注保持、原 rows 不提前刷新且仍显示 `待收款，已收部分款`、rows GET count 不变、status PUT count 为 1、reminder PUT count 为 1；第二次保存只等待 reminder 200，status PUT count 仍为 1、reminder PUT count 为 2，随后才关闭 drawer、刷新 rows 到 `待冲红`，且成功节点无可见错误残留和无隐藏浏览器错误。
- 验证：`cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts --project=chromium` 通过 11 tests；`cd web && npm test -- --run src/test/OutputInvoiceCollectionsPage.test.tsx` 通过 7 tests；`cd web && npx tsc --noEmit --pretty false` 通过；`PYTHONPATH=backend/src python3 -m unittest tests.test_spec_first_e2e_docs -v` 通过 5 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 8 tests；`bash scripts/verify.sh docs` 和目标文件 `git diff --check` 通过；完整 `cd web && npm run e2e:smoke` 通过 174 tests / 174 passed。
- 当前边界：本轮只覆盖本地 deterministic Browser 下的 status/reminder 分步暂时失败；真实多用户同时修改 expectedVersion、真实 PostgreSQL/RabbitMQ/Redis/systemd `invoice-lifecycle`/`output_invoice_collection` worker drain、生产历史样本和真实网络长时间中断仍按后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 销项收款 receipt void/reissue mutation 暂时失败重试恢复

- 本轮补齐 `output-invoice-collections` 的两条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/output-invoice-collections-flow.spec.ts` 新增 `POST /api/output-invoice-collections/receipts/{id}/void` 和 `/reissue` 暂时返回 503 后用户重试成功的场景。
- 产品侧修复：`ReceiptHistoryDrawer` 的作废/重开失败不再无条件关闭原因弹窗；`handleVoid` / `handleReissue` 返回成功布尔值，`handleConfirmAction` 只在成功后关闭弹窗和清空原因。失败时保留原因输入、弹窗和 drawer 内错误，便于直接重试。
- 测试断言第一次作废 503 后原因弹窗和输入值保持、错误可见、history 仍为 issued、history/rows 不提前刷新；第二次作废 200 后才刷新 history/rows 并展示作废事实。随后第一次重开 503 后原因弹窗和输入值保持、错误可见、不显示新收据号、history/rows 不提前刷新；第二次重开 200 后才刷新 history/rows 并展示 `SK2026050003`，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 receipt void/reissue 本地 transient failure；collection-status、collection-reminder 分步失败、receipt create 和 rows 首屏 transient failure 已由 Browser 覆盖。真实 PostgreSQL 锁等待/唯一约束冲突恢复、真实 RabbitMQ/Redis/systemd `invoice-lifecycle`/`output_invoice_collection` worker drain、生产历史样本和真实网络中断仍按后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 销项收款 receipt create mutation 暂时失败重试恢复

- 本轮补齐 `output-invoice-collections` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/output-invoice-collections-flow.spec.ts` 新增 `POST /api/output-invoice-collections/rows/{id}/receipts` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；`ReceiptPreviewDrawer` 已有本地错误恢复行为。本轮只加固 deterministic mock 和真实 Chromium 断言。
- 测试断言第一次创建 503 时仍发送 `Idempotency-Key`，预览 drawer 和收据内容保持，后端错误提示可见，创建按钮恢复，rows 不提前刷新、不显示 `已出收据`、history endpoint 零调用；第二次创建 200 后才关闭 drawer、刷新 rows 到 `已出收据`，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 receipt create 本地 transient failure；collection-status transient failure、receipt void/reissue transient failure、rows 首屏失败恢复、成功状态/提醒、正式收据 void/reissue 成功流和红蓝票 relation 主流程已有 Browser 覆盖。真实 PostgreSQL 锁等待/唯一约束冲突恢复、真实 RabbitMQ/Redis/systemd `invoice-lifecycle`/`output_invoice_collection` worker drain、生产历史样本和真实网络中断仍按后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 销项收款 collection-status mutation 暂时失败重试恢复

- 本轮补齐 `output-invoice-collections` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/output-invoice-collections-flow.spec.ts` 新增 `PUT /api/output-invoice-collections/rows/{id}/collection-status` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有 drawer 本地错误恢复行为。本轮只加固 deterministic mock 和真实 Chromium 断言。
- 测试断言第一次保存 503 后错误提示可见、`状态/提醒` drawer 保持、手动状态/状态备注/提醒备注草稿保持、保存按钮恢复、`collection-reminder` endpoint 零调用、rows 不提前刷新且不伪装 `待冲红`；第二次保存 200 后才提交 reminder、关闭 drawer、刷新 rows 到 `待冲红`，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 collection-status 本地 transient failure；rows 首屏失败恢复、成功状态/提醒、collection-reminder 分步失败、receipt create/void/reissue transient failure、正式收据 create/void/reissue 成功流和红蓝票 relation 主流程已有 Browser 覆盖。真实 PostgreSQL/RabbitMQ/Redis/systemd `invoice-lifecycle`/`output_invoice_collection` worker drain、真实多用户 expectedVersion 冲突和生产网络中断仍按后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 待找发票 rules save mutation 暂时失败草稿重试恢复

- 本轮补齐 `pending-invoices` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/pending-invoices-rules-save-flow.spec.ts` 新增 `PUT /api/pending-invoices/rules` 暂时返回 503 后用户重试成功的场景。
- 产品侧修复：`GlobalOperationOverlayProvider.runOperation` 新增 `blockOnError=false` 选项；待找发票规则保存使用该选项，让规则抽屉保存失败回到抽屉内联错误和草稿状态，不留下被 drawer top-layer 拦截的不可点击全局“操作失败”弹窗。默认全局失败阻塞行为不变。
- 测试断言第一次保存 503 后抽屉内错误提示可见、`设备款` 草稿勾选保持、全局操作弹窗不存在、`operation-barrier/status` 和 rows 不触发；第二次保存 200 后才等待 `pending_invoice:expense:requires_invoice` barrier、rows refetch、刷新中反馈，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 rules save 本地 transient failure；attach existing confirm、income status 和 rows 首屏失败恢复已由既有 Browser 覆盖，真实 PostgreSQL/RabbitMQ/Redis/systemd `pending-invoice`/`search`/`invoice-lifecycle` worker drain、withdraw 等其它 mutation 真实网络中断、真实 XLSX workbook 和生产大数据仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 待找发票 income status mutation 暂时失败重试恢复

- 本轮补齐 `pending-invoices` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/pending-invoices-income-status-flow.spec.ts` 新增 `PUT /api/pending-invoices/income-statuses` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有保存失败后保留收入流水选择、显示后端错误并恢复按钮的行为，本轮只加固 deterministic mock 和真实 Chromium 断言。
- 测试断言第一次保存 503 后错误提示可见、`已选 2 条流水` 保持、收入流水仍保持 `未开票`、rows 不重读、逐行 fallback API 零调用；第二次点击 `标记现金收入` 返回 200 后 rows refetch，两条流水更新为 `现金收入`，选择清空，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 income status 本地 transient failure；attach existing confirm、rules save 和 rows 首屏失败恢复已由既有 Browser 覆盖，真实 PostgreSQL/RabbitMQ/Redis/systemd `pending-invoice`/`search`/`invoice-lifecycle` worker drain、withdraw 等其它 mutation 真实网络中断、真实 XLSX workbook 和生产大数据仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 待找发票 attach existing confirm 暂时失败重试恢复

- 本轮补齐 `pending-invoices` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/pending-invoices-attach-existing-flow.spec.ts` 新增 `POST /api/pending-invoices/attach-existing-invoices` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有 confirm 失败后保留“选择已有进项发票”抽屉、preview、已选流水/发票并展示后端错误的行为，本轮只加固 deterministic mock 和真实 Chromium 断言。
- 测试断言第一次 confirm 503 后错误提示可见、确认按钮恢复可用、抽屉不关闭、rows 不重读、原流水仍保持 `已支付待开票` 且不出现发票号；第二次 confirm 200 后抽屉关闭、rows refetch，两条流水更新为 `已支付已开票` 并显示发票号，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 attach existing confirm 本地 transient failure；rows 首屏失败恢复、income status 和 rules save 暂时失败重试已由后续 Browser 覆盖，真实 PostgreSQL/RabbitMQ/Redis/systemd `pending-invoice`/`search`/`invoice-lifecycle` worker drain、withdraw 等其它 mutation 真实网络中断、真实 XLSX workbook 和生产大数据仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC submitted reset/delete mutation 暂时失败重试恢复

- 本轮补齐 `etc-tickets` 已提交 bucket 下的 destructive mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增 `DELETE /api/etc/business-batches/{id}` 暂时返回 503 后用户重试成功的 submitted reset/delete 场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有失败后保留删除确认弹窗和已提交批次行的行为，本轮只加固 deterministic mock 初始状态、submitted expectedVersion/reason 断言和真实 Chromium 负面流。
- 测试断言第一次删除已提交批次 503 后错误提示可见、请求体携带 submitted 版本和“释放发票”原因、确认弹窗保持、已提交批次行和 `已提交 1` 计数保持；第二次点击返回 200 后确认弹窗关闭，已提交列表刷新为空，失败文案清除，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 submitted reset/delete endpoint 本地 transient failure；真实 relation command service 内部异常仍由后端测试覆盖，真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、真实 OA、对象存储/Nginx、大 ZIP 和 import confirm 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC ticket-root source upload mutation 暂时失败重试恢复

- 本轮补齐 `etc-tickets` 的一条 source upload mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增 `POST /api/etc/reconciliation-tasks/{taskId}/ticket-root-files` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有上传失败后保留当前 task、显示错误、清空 file input 并允许再次选择同一文件的行为，本轮只加固 deterministic mock、Vitest retry 交互和真实 Chromium 断言。
- 测试断言第一次上传票根网 TXT 503 后错误提示可见、文件列表不追加 `ticket-root-upload.txt`、上传入口保持可用；第二次选择同一文件返回 200 后文件列表追加 TXT source file，失败文案清除，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 ticket-root source upload mutation 的本地 transient failure；submitted reset/delete transient failure 已由后续本地 Browser 覆盖，真实对象存储写入失败/权限、Nginx 上传中断、大 ZIP、import confirm、真实 OA 页面和真实 worker drain 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC source file delete mutation 暂时失败重试恢复

- 本轮补齐 `etc-tickets` 的一条 destructive mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增 `DELETE /api/etc/reconciliation-tasks/{taskId}/source-files/{fileId}` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有失败后保留删除确认弹窗和原文件行的行为，本轮只加固 deterministic mock、Vitest retry 交互和真实 Chromium 断言。
- 测试断言第一次删除源文件 503 后错误提示可见、确认弹窗保持、原文件行保持；第二次点击返回 200 后删除确认弹窗关闭，文件列表刷新为空，失败文案清除，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 source file delete mutation 的本地 transient failure；ticket-root source upload 和 submitted reset/delete transient failure 已由后续本地 Browser 覆盖，真实对象存储写入失败/权限、import confirm、真实 OA 页面、对象存储/Nginx、大 ZIP 和真实 worker drain 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC business batch delete mutation 暂时失败重试恢复

- 本轮补齐 `etc-tickets` 的一条 destructive mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增 `DELETE /api/etc/business-batches/{id}` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有失败后保留删除确认弹窗和原批次行的行为，本轮只加固 deterministic mock、Vitest retry 交互和真实 Chromium 断言。
- 测试断言第一次删除 503 后错误提示可见、确认弹窗保持、原批次行保持、`未提交 1` 不变；第二次点击返回 200 后删除确认弹窗关闭，列表刷新到 `未提交 0`/`已提交 0`，失败文案清除，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖未提交 business batch delete mutation 的本地 transient failure；submitted reset/delete、source file delete 和 ticket-root source upload 已由后续本地 Browser retry 覆盖，import confirm、真实对象存储/Nginx 上传中断、真实 OA 页面、大 ZIP 和真实 worker drain 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC manual OA status mutation 暂时失败重试恢复

- 本轮补齐 `etc-tickets` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增 `POST /api/etc/business-batches/{id}/manual-oa-status` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有失败后保留 `OA提交确认` 的行为，本轮只加固 deterministic mock、Vitest retry 交互和真实 Chromium 断言。
- 测试断言第一次人工确认已提交 503 后错误提示可见、不切到 `已提交` bucket、`OA提交确认` 保持、确认按钮恢复可用；第二次点击返回 200 后进入 submitted bucket，失败文案清除，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 ETC manual OA status mutation 的本地 transient failure；delete、submitted reset/delete、source file delete 和 ticket-root source upload 已由后续本地 Browser retry 覆盖，import confirm、真实对象存储/Nginx 上传中断、真实 OA 页面、大 ZIP 和真实 worker drain 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC OA draft mutation 暂时失败重试恢复

- 本轮补齐 `etc-tickets` 的一条 mutation 级 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增 `POST /api/etc/business-batches/{id}/oa-draft` 暂时返回 503 后用户重试成功的场景。
- 产品侧决策：不改产品逻辑和后端 API；页面已有失败后保留创建草稿 dialog 的行为，本轮只加固 deterministic mock、Vitest retry 交互和真实 Chromium 断言。
- 测试断言第一次创建草稿 503 后错误提示可见、不进入 `OA提交确认` 伪成功、创建草稿 dialog 保持、提交 OA 入口不被错误推进；第二次点击返回 200 后进入提交确认，失败文案清除，且成功节点无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 ETC OA draft mutation 的本地 transient failure；manual status、delete、submitted reset/delete、source file delete 和 ticket-root source upload 已由后续本地 Browser retry 覆盖，import confirm、真实对象存储/Nginx 上传中断、真实 OA 页面、大 ZIP 和真实 worker drain 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - ETC business-batches GET 加载失败刷新恢复

- 本轮补齐 `etc-tickets` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/etc-tickets-flow.spec.ts` 新增首屏 `/api/etc/business-batches` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：新增显式 `刷新` 入口；错误态不显示“无匹配批次。”；刷新复用 `loadBatches`，未提交 tab 同步刷新 reconciliation tasks；不改变 OA 草稿、manual status、delete/reset、source file、import 或 Workbench relation 语义。
- 测试断言错误提示、普通空态消失、刷新后列表恢复、未提交业务批次/ETC 发票明细/提交 OA 入口恢复、失败文案清除、成功恢复后无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 `GET /api/etc/business-batches` 首屏加载失败恢复；OA 草稿/manual/delete/submitted reset/source file delete/ticket-root source upload mutation 级网络恢复已由后续本地 Browser 覆盖，import confirm、真实对象存储/Nginx 上传中断、真实 OA、真实 worker drain 和大 ZIP 仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 免 OA list GET 加载失败刷新恢复

- 本轮补齐 `no-oa-bank-batches` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/bank-flow-rule-batches-flow.spec.ts` 新增首屏 `/api/no-oa-bank-batches` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：错误态不再同时显示普通“当前标签下暂无流水”空态；刷新复用现有 `刷新` / `loadBatches` 路径，不改变 tag-selection、submit/withdraw、read model freshness 或 operation barrier 业务语义。
- 测试断言错误提示、普通空态消失、刷新后列表 200/fresh 恢复、主/子标签和建设银行流水表恢复、未选择时提交按钮仍禁用、失败文案清除、成功恢复后无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 `GET /api/no-oa-bank-batches` 首屏加载失败恢复；submit/withdraw/tag-selection mutation 级网络失败、真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、生产历史 no-OA relation 和大数据月份仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 外部往来 grouped ledger 加载失败刷新恢复

- 本轮补齐 `turnover-ledger` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/turnover-ledger-flow.spec.ts` 新增首屏 `/api/turnover-ledger` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：页面新增显式 `刷新台账` 入口；错误态不再同时显示普通“暂无往来款台账”空态；刷新复用现有 `loadLedger`，不改变 tag-selection、manual closure、withdraw、relation freshness 或 operation barrier 业务语义。
- 测试断言错误提示、普通空态消失、刷新后列表 200/fresh 恢复、grouped table 和业务行恢复、未选择时确认闭环仍禁用、失败文案清除、成功恢复后无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 `GET /api/turnover-ledger` 首屏加载失败恢复；confirm/withdraw/extra/tag-selection mutation 级网络失败、真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、生产历史外部往来关系和大数据 grouped table 性能仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 批量账务 GET 加载失败刷新恢复

- 本轮补齐 `batch-accounting` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/batch-accounting-flow.spec.ts` 新增首屏 `/api/batch-accounting` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：错误态不再同时显示普通“当前年份暂无批量账务流水”空态；刷新复用现有 `loadData`，不改变 submit/withdraw、relation freshness 或 operation barrier 业务语义。
- 测试断言错误提示、普通空态消失、刷新后列表 200/fresh 恢复、银行流水行/可关联 OA 表恢复、未选择时提交按钮仍禁用、失败文案清除、成功恢复后无可见错误残留和无隐藏浏览器错误。
- 当前边界：本轮只覆盖 `GET /api/batch-accounting` 首屏加载失败恢复；submit/withdraw mutation 级网络失败、真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、生产历史批量账务 relation 和大年份性能仍按既有测试或后续 Browser/staging/runtime smoke 管理。

## 2026-06-20 - 成本统计 explorer 加载失败刷新恢复

- 本轮补齐 `cost-statistics` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/cost-statistics-flow.spec.ts` 新增首屏 `/api/cost-statistics/explorer` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：页面新增显式 `刷新` 入口；根 explorer 加载失败且没有可用 explorer 数据时禁用 `导出中心`，但流水详情加载失败不禁用导出中心；错误态不显示普通空态、不渲染按时间表。
- 测试断言错误提示、正常空态消失、表格消失、导出禁用、刷新后 explorer 200/fresh 恢复、错误文案消失、业务成本行和导出入口恢复。
- 当前边界：本轮只覆盖 explorer 首屏加载失败恢复；detail/export non-fresh、大数据窄屏、relation/import/no-OA/turnover/settings fan-out 仍按既有测试覆盖。真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、真实网络中断恢复、真实 XLSX workbook 和生产大数据仍需后续 Browser/staging/runtime smoke。

## 2026-06-20 - OA 待付款 rows 加载失败刷新恢复

- 本轮补齐 `oa-pending-payments` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/oa-pending-payments-flow.spec.ts` 新增首屏 `/api/oa-pending-payments/rows` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：页面新增显式 `刷新` 入口；错误态表格空行不再显示正常“暂无 OA 待付款核对数据”，而是显示“OA 待付款核对加载失败，请点击刷新重试。”；普通空态“当前条件下暂无记录。”不在错误态出现。
- 测试断言错误提示、错误态空行文案、正常空态消失、刷新后 rows 200/fresh 恢复、错误文案消失、业务行和分页恢复。
- 当前边界：本轮只覆盖 rows 首屏加载失败恢复；confirm-paid、link-bank、rules drawer 和 detail non-fresh 仍按既有测试覆盖。真实 OA Mongo/MySQL、PostgreSQL/RabbitMQ/Redis/systemd worker drain、真实网络中断恢复和生产大数据仍需后续 Browser/staging/runtime smoke。

## 2026-06-20 - 进项发票使用 rows 加载失败刷新恢复

- 本轮补齐 `input-invoice-usage` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/input-invoice-usage-flow.spec.ts` 新增首屏 `/api/input-invoice-usage/rows` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：页面新增显式 `刷新` 入口；错误态表格空行不再显示正常“当前条件下没有进项发票使用记录。”，而是显示“进项发票使用情况加载失败，请点击刷新重试。”；同时加载错误或 read model refreshing 时禁用 `筛选内容导出`，避免失败态触发伪成功导出。
- 测试断言错误提示、错误态空行文案、正常空态消失、导出禁用、刷新后 rows 200/fresh 恢复、错误文案消失、业务行和分页恢复、导出重新可用。
- 当前边界：本轮只覆盖 rows 首屏加载失败恢复；支付规则、OA reverse、relation detail/export 等既有链路仍按各自测试覆盖。真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、真实网络中断恢复和真实 XLSX workbook 仍需后续 Browser/staging/runtime smoke。

## 2026-06-20 - 销项收款 rows 加载失败刷新恢复

- 本轮补齐 `output-invoice-collections` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/output-invoice-collections-flow.spec.ts` 新增首屏 `/api/output-invoice-collections/rows` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：页面新增显式 `刷新` 入口；错误态表格空行不再显示正常“当前条件下没有销项发票收款记录。”，而是显示“销项发票收款情况加载失败，请点击刷新重试。”；同时加载错误存在时禁用 `筛选内容导出`，避免失败态触发伪成功导出。
- 测试断言错误提示、错误态空行文案、正常空态消失、导出禁用、刷新后 rows 200/fresh 恢复、错误文案消失、业务行和分页恢复、导出重新可用。
- 验证：`cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts --project=chromium` 通过 7 tests。
- 当前边界：本轮只覆盖 rows 首屏加载失败恢复；状态/提醒、正式收据、红蓝票等 mutation 的真实网络中断恢复、真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain 和真实 XLSX workbook 仍需后续 Browser/staging/runtime smoke。

## 2026-06-20 - 待找发票 rows 加载失败刷新恢复

- 本轮补齐 `pending-invoices` 的一条本地 `NETWORK-RECOVERY` Browser 负面链路：`web/e2e/pending-invoices-filter-sort-flow.spec.ts` 新增首屏 `/api/pending-invoices/rows` 暂时返回 503 后用户手动刷新恢复的场景。
- 产品侧最小修正：错误态表格空行不再显示正常“当前条件下没有待找发票流水。”，而是显示“待找发票加载失败，请点击刷新重试。”；同时加载错误存在时禁用 `筛选内容导出`，避免失败态触发伪成功导出。
- 测试断言错误提示、错误态空行文案、正常空态消失、导出禁用、刷新后 rows 200/fresh 恢复、错误文案消失、业务行和分页恢复、导出重新可用。
- 当前边界：本轮只覆盖 rows 首屏加载失败恢复；attach existing confirm 暂时失败重试恢复已由后续 Browser 覆盖，income status、rules save、withdraw 等其它 mutation 的真实网络中断恢复、真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain 和真实 XLSX workbook 仍需后续 Browser/staging/runtime smoke。

## 2026-06-20 - admin 数据重置确认弹窗权限 smoke

- 本轮继续收敛 `permissions-and-audit` 的 `PERM-E2E-005`：`web/e2e/permissions-role-matrix.spec.ts` 的 admin 场景在 settings 中打开数据重置影响确认和 OA 密码复核弹窗。
- 测试断言 impact dialog 可见、密码复核弹窗中 `确认清理` 在未填密码时禁用、填入密码后启用，随后取消并确认没有新增 `POST /api/workbench/settings/data-reset/jobs`。这补的是 admin-only 高风险入口权限 smoke，不是真正执行 destructive reset。
- 文档影响：更新 `docs/modules/permissions-and-audit/e2e-coverage.md`、`write-entry-inventory.md`、`tests.md` 和 `implementation-notes.md`；真正 data reset job 提交、polling、settings reload 和下游 fresh 仍由 `web/e2e/settings-data-reset-flow.spec.ts` 负责。
- 当前边界：真实 admin 凭据、真实 OA 密码复核、生产备份/restore、worker drain 和全页面最终 fresh 仍需 data-safety-reset staging/production smoke。

## 2026-06-20 - 关联台现金特殊处理 full-access Browser 闭环

- 本轮补齐 `reconciliation-workbench` 的 `RECON-WB-E2E-013`：`web/e2e/workbench-cash-special-flow.spec.ts` 覆盖 full-access 用户在已配对银行流水更多菜单中执行 `确认为过账`、`确认为买票` 和 `取消现金处理`。
- 测试断言三个 mutation 请求体携带完整 group row ids，买票弹窗校验买票成本和项目名称，操作后等待 `workbench_relation` operation barrier，页面通过 operation projection/background refresh 仍可继续操作，并由 strict fixture 捕获成功后的隐藏 UI/browser 错误。
- 本轮只加固 deterministic mock、Browser E2E、`e2e:smoke` 和文档；不改产品页面逻辑或后端业务逻辑。read-export 下同入口零 mutation 已由上一轮权限矩阵覆盖。
- 当前边界：本地 Browser 证明 UI 消费现金特殊处理 contract；真实 PostgreSQL command 持久化、审计、成本统计下游、worker drain 和真实生产数据仍需 staging/production write-operation audit。

## 2026-06-20 - 权限矩阵关联台现金处理行级菜单

- 本轮继续收敛 `permissions-and-audit` 的 `PERM-E2E-003`：`web/e2e/fixtures/apiMocks.ts` 新增 `workbenchCashSpecialActions` deterministic 开关，让已配对银行行可显式暴露 `confirm_cash_pass_through`、`confirm_cash_ticket_purchase`、`cancel_cash_special` 三个现金处理 action。
- `web/e2e/permissions-role-matrix.spec.ts` 新增 `reconciliation-workbench:cash-special-actions` opener，在 `read_export_only` 下证明更多菜单、`确认为过账`、`确认为买票`、`取消现金处理` menuitem 和 `确认买票成本` 弹窗都不可见，并显式断言三个现金处理 durable mutation endpoint 零调用。
- 文档影响：更新 `docs/modules/permissions-and-audit/write-entry-inventory.md`、`e2e-coverage.md`、`tests.md`、`implementation-notes.md` 和全局 testing 文档。
- 当前边界：这不是 full-access 现金买票成本主流程覆盖；`PERM-E2E-003` 仍为 partial，因为尚未自动打开所有页面特定抽屉/弹窗，真实 OA/代理/生产审计仍需 staging/production gate。

## 2026-06-20 - 权限矩阵关联台列拖拽隐式写入口

- 本轮继续收敛 `permissions-and-audit` 的 `PERM-E2E-003`：`web/e2e/permissions-role-matrix.spec.ts` 在 `reconciliation-workbench:open-candidate-actions` opener 中新增列顺序拖拽断言，read-export 下所有 `拖动 ... 列` handle 必须 disabled，模拟 mouse drag 后 body 不进入 `column-layout-dragging`，且高风险写入口测试显式断言 `POST /api/workbench/settings` 零调用。
- 本轮只加固 Browser E2E 和权限 inventory/docs，不改产品逻辑；这个入口对应 full-access 用户列布局保存到 `/api/workbench/settings`，属于没有普通“保存”按钮文案的隐式设置写入口。
- 文档影响：更新 `docs/modules/permissions-and-audit/e2e-coverage.md`、`write-entry-inventory.md`、`tests.md`、`implementation-notes.md` 和本文件。
- 当前边界：`PERM-E2E-003` 仍为 partial，因为尚未自动打开所有页面特定抽屉/弹窗；真实 OA role sync、代理下载 header、生产审计和真实环境权限 smoke 仍需 staging/production gate。

## 2026-06-20 - 数据重置后多页面 fresh Browser contract

- 本轮补齐 `data-safety-reset` 的本地 Browser 缺口：`settings-data-reset-flow` 在 data reset job 202、polling、settings reload 和成功反馈后，继续进入银行明细并等待 `/api/bank-details/transactions` 返回 `read_model_status=fresh`，断言 reset bank transactions 后旧银行流水为空；随后进入待找发票并等待 `/api/pending-invoices/rows` 返回 `read_model_status=fresh` 且业务行可见。
- 本轮只加固 deterministic mock 和 Browser E2E，不改产品逻辑；mock 在 data reset job 完成后记录 completed action，使 `reset_bank_transactions` 对银行明细交易列表表现为 fresh empty。
- 文档影响：更新 `docs/modules/data-safety-reset/e2e-coverage.md`、`tests.md`、`docs/dev/testing.md`、`docs/dev/spec-first-e2e-inventory.md` 和本文件。
- 验证：`cd web && npx playwright test e2e/settings-data-reset-flow.spec.ts --project=chromium` 通过 2 tests。
- 当前边界：本地 Browser 只能证明 UI 消费 fresh contract；真实 PostgreSQL PITR/对象存储恢复、Redis/RabbitMQ/systemd worker drain、大库 reset 后全页面最终 fresh 和真实 OA Mongo/附件仍需 staging/production gate。

## 2026-06-20 - 销项收款正式收据 void/reissue Browser 闭环

- 本轮补齐 `output-invoice-collections` 的正式收据 Browser 写流：`output-invoice-collections-flow` 在状态/提醒保存、正式收据 create、history 展示后，继续打开作废确认弹窗、填写作废原因、确认 `POST /api/output-invoice-collections/receipts/receipt-output-e2e-001/void` body，等待 history/rows refresh，再打开重开确认弹窗、填写重开原因、确认 `POST /api/output-invoice-collections/receipts/receipt-output-e2e-001/reissue` body，并验证新收据号 `SK2026050003`。
- 本轮只加固 deterministic mock 和 Browser E2E，不改产品逻辑；mock 的收据状态从 boolean 扩成 `none -> issued -> voided -> reissued` 小状态机，用来表达正式收据生命周期。
- 文档影响：更新 `docs/modules/output-invoice-collections/e2e-spec.md`、`e2e-coverage.md`、`tests.md`、`implementation-notes.md`、`docs/dev/testing.md` 和本文件。
- 验证：先用 `cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts --project=chromium -g "saves collection status and creates a formal receipt"` 定位通过 1 test；随后复跑 `cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts --project=chromium` 通过 6 tests，覆盖同文件筛选/排序/page-size、收据 lifecycle、stale 诊断、导出、row-limit 和 read-export 权限场景；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 8 tests；`bash scripts/verify.sh docs` 和目标文件 `git diff --check` 通过。
- 当前边界：真实 PostgreSQL 锁等待、唯一约束冲突恢复、生产历史样本和真实 worker drain 仍需 staging/production gate。

## 2026-06-19 - 权限矩阵银行明细人工待分类 opener

- 本轮补齐权限矩阵中银行明细 `unmatched` 人工待分类动态入口：`web/e2e/permissions-role-matrix.spec.ts` 新增 `bank-details:manual-category-assignment` opener，在 `read_export_only` session 下进入 `/bank-details`，定位未匹配流水，断言 `待分类` 按钮禁用、`待分类主标签` 菜单不打开、`保存` 不出现，并复扫 visible enabled 写控件候选。
- 该场景断言 `POST /api/bank-details/transactions/bk-o-202603-001/category-assignment` 零调用、durable mutation 零调用、browser error 零残留；这是测试/mock 加固，不改产品逻辑。
- 文档影响：更新 `docs/modules/permissions-and-audit/e2e-coverage.md`、`docs/modules/permissions-and-audit/write-entry-inventory.md`、`docs/modules/permissions-and-audit/tests.md`、`docs/dev/testing.md` 和 `docs/dev/spec-first-e2e-inventory.md`。
- 验证：`cd web && npx playwright test e2e/permissions-role-matrix.spec.ts --project=chromium -g "read-export users cannot trigger bank detail manual assignment controls"` 通过 1 test；`cd web && npx playwright test e2e/permissions-role-matrix.spec.ts --project=chromium` 通过 7 tests；`cd web && npm run e2e:smoke` 通过 151 tests / 151 passed；`PYTHONPATH=backend/src python3 -m unittest tests.test_permissions_write_entry_inventory -v` 通过 14 tests。
- 当前边界：`PERM-E2E-003` 仍为 partial，因为尚未自动打开所有页面特定抽屉/弹窗；真实 OA role sync、代理下载 header 和生产审计 smoke 仍需 staging/生产 gate。

## 2026-06-19 - 批量账务 relation stale Browser 防 false-empty

- 本轮补齐批量账务 Browser 层 `workbench_relation` non-fresh 保护：`web/e2e/batch-accounting-flow.spec.ts` 新增 `read_model_status=stale` 用例，断言真实 Chromium 中显示 `关联台关系读模型 stale，正在刷新。`、stale reason 和 scope，当前银行/OA rows 仍可见，不显示普通 `当前年份暂无批量账务流水` 空态，选择 OA 后 submit 仍按 canonical write safety 语义保持可用，且该只读诊断场景零 submit/withdraw mutation。
- 本轮只加固 deterministic mock 和测试，不改产品逻辑。保留既有业务口径：普通 relation read model non-fresh 不全局禁用具备 canonical write safety 的操作；写入是否允许仍由权限/session、canonical relation、idempotency、owner 状态和后端写安全决定。
- 文档影响：更新 `docs/modules/batch-accounting/e2e-coverage.md`、`docs/modules/batch-accounting/tests.md`、`docs/modules/batch-accounting/implementation-notes.md`、`docs/modules/read-models/e2e-coverage.md`、`docs/modules/read-models/tests.md` 和 `docs/dev/testing.md`。
- 验证：`cd web && npx playwright test e2e/batch-accounting-flow.spec.ts --project=chromium` 通过 3 tests；`cd web && npm run e2e:smoke` 通过 150 tests / 150 passed；`PYTHONPATH=backend/src python3 -m unittest tests.test_spec_first_e2e_docs -v` 通过 5 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 8 tests；`bash scripts/verify.sh docs` 和目标文件 `git diff --check` 通过。
- 当前边界：这不是真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain，也不是生产历史 relation distribution 回放；真实基础设施仍走 `infra-smoke` / staging 或生产批准 smoke。

## 2026-06-19 - 生产外部 gate 输入预检

- 本轮新增 `fin_ops_platform.tools.production_external_gate_preflight`，用于在生产 admin Browser smoke、authenticated HTTP/SSE SLO 和 controlled write-operation apply 前统一判断外部输入是否具备。工具只输出 gate 状态和缺失 env 名称，不输出 token、cookie、数据库 URL 或 scenario 内容；`--require-ready` 在缺输入时返回退出码 `2`。
- 覆盖的 gate：`production_route_shell_browser` 需要 `FIN_OPS_E2E_OA_TOKEN`；`production_admin_app_health_browser` 需要 `FIN_OPS_E2E_ADMIN_TOKEN`；user-scope HTTP SLO 需要 bearer/admin token/cookie 之一；admin-scope HTTP SLO 需要 admin token 或 cookie 且后续必须证明 `can_admin_access`；`write_operation_apply` 需要真实认证、PostgreSQL URL、`FIN_OPS_WRITE_E2E_SCENARIO` 和 `FIN_OPS_WRITE_E2E_APPROVAL_TICKET`。
- 当前生产只读 env 证据：PostgreSQL URL 已配置；admin token、HTTP bearer/cookie、write approval、目标 OA credentials path 未配置；admin username 配置 1 个且只记录脱敏形态 `YN***05`；allowed username 配置 1 个。结论是 admin AppHealth Browser 和真实写操作 apply 仍为 `external_input_required`，不是 deterministic E2E 失败或业务代码失败。
- 文档影响：更新 `scripts/verify.sh`、`docs/dev/testing.md`、`docs/dev/nightly-ci.md` 和 `docs/operations/monitoring.md`，把该预检作为 `infra-smoke` 默认只读输出和真实生产 gate 前置步骤。
- 测试覆盖：新增 `tests/test_production_external_gate_preflight.py`，覆盖缺输入不泄露 secret、全输入 pass 且不泄露 secret、`--require-ready` 缺输入返回 `2`。

## 2026-06-19 - 生产 admin AppHealth Browser gate 已编码

- 本轮补齐 admin-only AppHealth 的生产 Browser smoke 入口：`web/e2e/production-admin-app-health.spec.ts` 和 `cd web && npm run e2e:production-admin`。该 spec 默认跳过，只有显式 `FIN_OPS_E2E_PRODUCTION_ADMIN_SMOKE=1` 且提供 `FIN_OPS_E2E_ADMIN_TOKEN` 时才向 `https://www.yn-sourcing.com` 注入 `Admin-Token` cookie。
- 该 gate 使用 strict Browser fixture，关闭 screenshot/trace/video，监听并拒绝 `POST`/`PUT`/`PATCH`/`DELETE`，打开 `/fin-ops/operations/app-health` 后断言 `AppHealth 运维状态`、`app-health-data`、`app-health-requests`、`app-health-runtime` 可见，且 `/api/operations/app-health-dashboard` 返回 200；页面不能停在 session/admin 权限拦截或 loading-only 状态。
- 当前状态：生产 admin Browser gate 已具备可复跑入口和静态 guard，但本轮没有真实 admin token/cookie，因此只能把 admin-only dashboard 从“无可执行 Browser gate”推进到“待 admin 凭据实跑”；不得把它记为生产 admin smoke passed。
- 验证入口：默认 skip 验证、`tests.test_playwright_e2e_strict_diagnostics`、`cd web && npx tsc --noEmit --pretty false`、`bash scripts/verify.sh docs`。

## 2026-06-19 - 成功写流 UI 错误残留 guard

- 本轮补齐“写操作实际成功，但页面仍弹操作失败/同步失败/read model 失败”的 Browser 检测基础设施：新增 `web/e2e/fixtures/successAssertions.ts` 的 `expectNoUnexpectedSuccessUiErrors`，检查成功写流后没有 `操作失败` dialog，也没有保存/刷新/导入/关联/撤回/校验/同步失败、导入任务创建失败、后台导入失败、read model 失败、`关系已写入，关联台刷新未完成` 或 `操作同步等待超时` 等可见错误残留。
- 已接入当前高价值成功写流样板：`confirmWorkbenchRelation` 共享 flow 和 relation fan-out specs 检查成功后无 UI 错误残留；关联台自身由 `workbench-withdraw-flow.spec.ts`、`workbench-exception-flow.spec.ts`、`workbench-network-recovery-flow.spec.ts` 覆盖撤回、异常处理、网络恢复和重复提交防护。未正式化 automatic candidate 不再有拆分成功流，其撤回拒绝和“不 suppress”由后端/API 负面测试保护。
- 静态 guard：`tests.test_playwright_e2e_strict_diagnostics` 防止从当前成功写流移除无错误残留断言；automatic candidate 仅保留撤回拒绝的负面语义，不登记为 Browser 成功写流。
- 本轮导入批次验证：`cd web && npx playwright test e2e/imports-bank-transactions-flow.spec.ts e2e/imports-invoices-flow.spec.ts e2e/imports-etc-invoices-flow.spec.ts --project=chromium` 通过 18 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 7 tests；`python3 -m py_compile tests/test_playwright_e2e_strict_diagnostics.py`、`bash scripts/verify.sh docs` 和目标文件 `git diff --check` 均通过。ETC preview 中合法的 `XML 解析失败` 文件级错误不会被误判为后台导入失败。
- 本轮进项/税金/待找规则批次验证：`cd web && npx playwright test e2e/input-invoice-usage-flow.spec.ts e2e/tax-offset-flow.spec.ts e2e/pending-invoices-rules-save-flow.spec.ts --project=chromium` 通过 22 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 7 tests；`python3 -m py_compile tests/test_playwright_e2e_strict_diagnostics.py` 通过。
- 本轮 Workbench relation fan-out 下游批次验证：`cd web && npx playwright test e2e/workbench-relation-fanout.spec.ts e2e/pending-invoices-fanout.spec.ts e2e/input-invoice-relation-fanout.spec.ts e2e/cost-statistics-relation-fanout.spec.ts e2e/workbench-relations-oa-pending-fanout.spec.ts e2e/workbench-relations-tax-offset-fanout.spec.ts --project=chromium` 通过 7 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 7 tests；`python3 -m py_compile tests/test_playwright_e2e_strict_diagnostics.py`、`bash scripts/verify.sh docs` 和目标文件 `git diff --check` 均通过。
- 当前 Workbench 自身写流批次入口：`cd web && npx playwright test e2e/workbench-withdraw-flow.spec.ts e2e/workbench-exception-flow.spec.ts e2e/workbench-network-recovery-flow.spec.ts --project=chromium`；automatic candidate 撤回拒绝由 `tests/test_workbench_auth_context_idempotency.py` 与 `tests/test_workbench_v2_api.py` 覆盖。
- 本轮银行明细/待找发票导出分类批次验证：`cd web && npx playwright test e2e/bank-details-category-flow.spec.ts e2e/bank-details-export-download.spec.ts e2e/bank-details-filtered-export-permissions.spec.ts e2e/pending-invoices-export-download.spec.ts --project=chromium` 通过 11 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 8 tests；`python3 -m py_compile tests/test_playwright_e2e_strict_diagnostics.py`、`bash scripts/verify.sh docs` 和目标文件 `git diff --check` 均通过。
- 本轮完整 deterministic Browser smoke 复核：`cd web && npm run e2e:smoke` 通过 149 tests / 149 passed，证明上述成功错误残留 guard 在全量 Playwright 顺序中没有破坏权限、负面、下载、read model non-fresh、导入、跨页面 fan-out 或大表格流程。
- 本轮 read model / worker gate 本地复核：`bash scripts/verify.sh infra-smoke` 通过 77 tests、18 skipped；真实 PostgreSQL/RabbitMQ integration 因未设置 `FIN_OPS_TEST_DATABASE_URL` / `RABBITMQ_TEST_URL` 被跳过；preflight 报告 authenticated HTTP/SSE、production Browser admin/user smoke 和 write-operation apply 仍为 `external_input_required`，缺少 bearer/admin token/cookie、write scenario、approval ticket 和 PostgreSQL URL，因此不能把本轮 dry-run 解释为真实生产写操作闭环。
- 当前边界：这不是生产真实写操作 apply；它先把本地 deterministic Browser E2E 的“假成功 UI 错误残留”检测能力固化。生产 write-operation closure 仍需要业务审批 ticket、真实认证和隔离/可回滚 scenario。

## 全局阶段状态

- 本轮权限矩阵动态区域校准：`web/e2e/permissions-role-matrix.spec.ts` 已用 role matrix opener registry 在 read-export 角色下打开关联台列顺序拖拽 settings 保存入口、关联台 open candidate 确认/异常/撤回、关联台已配对撤回、关联台现金处理行级菜单、关联台已处理异常/已忽略恢复弹窗、银行明细分类确认、银行明细人工待分类、银行明细自动标签、pending invoice 支出/收入规则、收入批量、进项支付规则、进项 OA reverse、销项收款状态规则/收据历史、OA pending 进行中/规则、ETC 对账流程、batch accounting 选择、batch accounting 已提交撤回、turnover 标签/明细等代表性动态区域并复跑 visible enabled 写控件候选扫描；写控件关键词已补拖动列、确认关联、异常处理、取消异常处理、撤回忽略、确认拆分/撤回、确认买票、确认为买票、确认为过账、取消现金处理、保存补充信息、保存凭据、清空密码、新增账户、提交/继续异常等深层动作；full-access 角色已补 settings 普通保存 POST/200/成功反馈/隐藏错误捕获 smoke，admin 角色已补访问账户新增只读导出用户并保存权限数组、保存后持久化账户仍显示、OA 申请人凭据保存 PUT/200、清空密码 DELETE/200、密码不回显且不进入普通 settings 保存 body，并补销项收款收据编号设置 drawer 保存 PUT/200；`tests/test_permissions_write_entry_inventory.py` 会双向校验 pageRegistry 与 inventory row、pageRegistry 与 role matrix readable route、opener id 与 Playwright、`covered-browser` row 必须有 dynamic opener 或页面级静态覆盖证明、dynamic/static registry 只能引用当前 `covered-browser` 模块且不能重复归类、Browser E2E 证据路径必须解析到当前文件或匹配真实 glob、写控件关键词 registry 与 role matrix DOM 扫描 pattern、源码高风险写控件文案 sentinel，且扫描 mutating feature API client 是否映射到 write-entry inventory；OA reverse preview 是 read-like POST 例外，但 OA draft、batch 和 manual status durable write endpoints 仍断言零调用；对应权限模块 `e2e-coverage.md`、`write-entry-inventory.md`、`tests.md`、`implementation-notes.md` 和全局 Spec-first/testing 文档已同步。`PERM-E2E-003` 仍保持 partial，剩余风险是尚未自动打开的所有页面特定抽屉/弹窗以及真实 OA/代理/生产审计 smoke。

| Phase | 内容 | 状态 | 最近验证 | 说明 |
| --- | --- | --- | --- | --- |
| 0 | 全局 dependency / impact map | documented-risk | `bash scripts/verify.sh docs` 通过 | `docs/dev/testing-closure-dependency-map.md` 已建立初版；具体 endpoint 到 service/repository trace 在模块轮次补充。 |
| 1 | Nightly CI 与统一验证入口 | documented-risk | 本轮 `PYTHONPATH=backend/src python3 -m unittest tests.test_nightly_ci tests.test_playwright_e2e_strict_diagnostics -v` 通过 12 tests；最新完整 `cd web && npm run e2e:smoke` 通过 174 tests / 174 passed；最新 `cd web && npx playwright test e2e/permissions-role-matrix.spec.ts --project=chromium` 通过 7 tests；`bash scripts/verify.sh docs` 通过；`bash scripts/verify.sh infra-smoke` 通过 77 tests、18 skipped；生产服务器只读 runtime smoke 显示 release `main-3ac07068-20260619013223` ready、20 个 worker unit active/running、最新 heartbeat lag 0-2s、`job.outbox_events`/`job.read_model_dirty_scopes` 只有 `done`、`read_model.app_status_readiness` 只有 `fresh`、scope-contract 审计 ok | `docs/dev/nightly-ci.md`、`scripts/verify.sh`、GitHub Actions workflow 已包含 backend/frontend/browser e2e/docs；`tests/test_nightly_ci.py` 会校验每个非生产 `web/e2e/*.spec.ts` 都列入 `npm run e2e:smoke`，且生产 opt-in specs 不混入默认 deterministic smoke；`web/playwright.config.ts` 在 CI 下启用 `forbidOnly`，`tests/test_playwright_e2e_strict_diagnostics.py` 会防止提交 `test.only`/`describe.only`。Browser smoke 覆盖清单以 `docs/dev/testing.md` 为准，本轮新增 `workbench-relations -> tax-offset` relation fan-out、tax-offset read model 非 fresh gate、search API/runtime fan-out、pending invoice relation 字段导出、pending invoice attach existing Browser 流、pending invoice income status Browser 流、pending invoice row-limit export Browser 流、pending invoice filter/sort Browser 流、pending invoice rules save Browser 流、OA pending linked fan-out、OA pending 进行中 confirm-paid Browser 流、OA pending 进行中 bank-link Browser 流、OA pending rows/detail non-fresh Browser 流、银行流水导入 preview stale/confirm 失败 Browser 流、发票导入 preview stale/confirm 失败 Browser 流、ETC 导入 preview stale/stale task/confirm 失败 Browser 流、no-OA 标签准入保存、no-OA submit 到成本统计 fresh read model Browser 流、turnover 标签准入保存、turnover manual closure 到成本统计 fresh read model Browser 流、settings project scope 到成本统计 active/all fresh read model Browser 流、成本统计按银行/按费用类型 baseline、成本统计 detail/export non-fresh 防伪成功、成本统计大数据窄屏 fresh 宽表/下钻滚动 Browser 流、销项收款 stale read model Browser false-empty 防护、列表 search/filter/sort/page-size、红蓝票 relation 字段 export-preview/download、进项发票使用 fresh rows/filter/sort/page-size、read-export 权限零 durable write、full-access settings 普通保存实际 POST/200/成功反馈、rows/relation detail/export read model Browser false-empty/长期 loading/禁用下载防护、fresh `+N` relation detail 展开、当前筛选导出/download/row-limit、红蓝票到税金/成本下游 fresh read model 展示，并完成 stale/status 断言、候选付款语义测试和权限矩阵 turnover extra / 银行人工待分类入口校准；Finance Table System 新增共享宽表窄屏滚动、右侧列可见和 AppHealth read model/worker 表格可读 Browser 覆盖。`infra-smoke` 是显式入口，默认不进入 `verify.sh all`；无 staging URL 时验证 read model SLO、runtime sync closure gate、write-operation SLO 和 RabbitMQ staging preflight 工具合同；有 `FIN_OPS_TEST_DATABASE_URL` 时追加真实 PostgreSQL critical scope dry-run discovery，但不写 queue；只有同时设置 `FIN_OPS_INFRA_SMOKE_APPLY=1` 才会真正 enqueue refresh events 并等待 read model worker drain；设置 `FIN_OPS_WRITE_OPERATION_AUDIT_OPERATIONS` 时追加真实业务写入 outbox SLO audit；有 `RABBITMQ_TEST_URL` 时追加 RabbitMQ staging preflight。生产服务器本轮先做只读当前状态验证，随后执行受控 critical apply：15 个 critical scope 均达到 done/fresh；5 秒 SLO 初次 3 个超时，重试后仅 invoice_lifecycle 仍约 5.76 秒。已发布 release `main-99ea9b35-invoice-lifecycle-batch-20260619145710`，发布后 critical apply 15/15 pass，summary p95/max 约 3.52 秒，invoice_lifecycle 约 1.29 秒，outbox/dirty 全 done、readiness 全 fresh。远端 Actions 是否启用需在 GitHub 侧确认。 |
| 2 | 统一模块 `tests.md` 模板 | documented-risk | `bash scripts/verify.sh docs` 通过 | 已按模块增量归并；所有 `docs/modules/*/tests.md` 均包含七类测试适用性、Nightly CI 覆盖和未测风险；`read-models` / `runtime-workers` 已登记 `infra-smoke` 作为真实基础设施 gate 入口。 |
| 3 | 模块闭环迭代 | documented-risk | read-models、runtime-workers、domain-events-lifecycle、reconciliation-workbench、bank-details、input-invoice-usage、cost-statistics、tax-offset、pending-invoices、oa-pending-payments、turnover-ledger、no-oa-bank-batches、batch-accounting、imports-bank-transactions、imports-invoices、imports-etc-invoices、output-invoice-collections、etc-tickets、settings、app-health-operations、permissions-and-audit、app-shell-navigation、finance-table-system、oa-integration、data-safety-reset、deploy 模块验证通过 | 旧测试闭环已完成首轮，状态为 `documented-risk`；这不等于 Spec-first E2E 全覆盖。 |
| 4 | 完成审计 | documented-risk | `bash scripts/verify.sh docs` 通过 | 模块四件套齐全；旧测试闭环剩余风险统一归入真实环境、外部系统、大数据、worker drain、浏览器视觉和生产 runbook smoke。Spec-first 审计从 Phase 5 重新逐模块推进。 |
| 5 | Spec-first E2E Audit | in-progress | 本轮新增 `cd web && npx playwright test e2e/bank-flow-rule-batches-flow.spec.ts --project=chromium` 通过 3 tests；`cd web && npx playwright test e2e/turnover-ledger-flow.spec.ts --project=chromium` 通过 3 tests；`cd web && npx playwright test e2e/batch-accounting-flow.spec.ts --project=chromium` 通过 3 tests；最新完整 `cd web && npm run e2e:smoke` 通过 174 tests / 174 passed；`PYTHONPATH=backend/src python3 -m unittest tests.test_spec_first_e2e_docs -v` 通过 5 tests；`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 8 tests；`tests.test_nightly_ci` 通过 5 tests；`bash scripts/verify.sh docs` 通过；最新 `permissions-role-matrix` Chromium 目标命令通过 7 tests | 已建立 `docs/dev/spec-first-e2e-audit.md` 和 `docs/dev/spec-first-e2e-inventory.md`；17 个页面模块当前均已达到页面级 `spec-first-covered`，27 个登记模块均已有 `e2e-spec.md` / `e2e-coverage.md` 入口，并由 `tests/test_spec_first_e2e_docs.py` 和 `verify.sh docs` 防止后续漏建、Spec ID 未映射或 Browser E2E 证据路径指向已删除/改名的 Playwright 文件；本轮补 no-OA Browser `read_model_status=stale -> fresh` 期间保持可见 rows、不显示普通空态并自动重读，补 batch accounting Browser `read_model_status=stale` 时保留当前银行/OA rows、显示 warning/reason/scope、防普通空态且零 mutation，补 turnover Browser `read_model_status=stale` 时保留当前 rows、显示 warning 且禁用确认闭环，也补 permissions Browser 银行人工待分类 read-export 零写入 opener。详细覆盖以各模块 `e2e-coverage.md` 和下方模块状态表为准；`read-models`、`runtime-workers`、`domain-events-lifecycle`、`oa-integration`、`data-safety-reset`、`deploy` 仍按 shared/runtime gate 管理，真实 PostgreSQL/RabbitMQ/Redis/systemd worker drain、真实 OA/对象存储、真实大文件、真实 XLSX/ZIP、Nginx/OA iframe SSE、大库 metrics 和生产数据继续作为 staging/runtime smoke 风险，不能写成本地 CI covered。 |

## 模块状态

2026-07-05 校准：流水规则批量处理页面的当前 Browser 入口、selected-row submit、成本统计 fan-out、withdraw/history 证据归 `bank-flow-rule-batches`；测试 fixture 使用 `bank-flow-rule-e2e-*` transaction id、`bank-flow-rule-batch-e2e-*` batch id 和 `流水规则手续费成本项目`。下方历史行中出现的 no-OA submit/免 OA 成本行仅代表旧阶段上下文，不能作为当前 bank-flow 页面 I/O 或 legacy no-OA 页面入口依据。

| Priority | Module | 状态 | 已有测试入口概览 | 当前缺口 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| 1 | `read-models` | spec-first-partial | `docs/modules/read-models/e2e-spec.md`、`docs/modules/read-models/e2e-coverage.md`、`tests/test_read_model_freshness.py`、`tests/test_read_model_query_gateway.py`、`tests/test_read_model_refresh_gateway.py`、`tests/test_read_model_readiness_reporter.py`、`tests/test_runtime_worker_read_model_refresh_scopes.py`、`tests/test_read_model_scope_contract.py`、`tests/test_runtime_queue.py`、`tests/test_platform_runtime_boundary_guards.py`、`web/e2e/bank-flow-rule-batches-flow.spec.ts`、`web/e2e/batch-accounting-flow.spec.ts`、`web/e2e/turnover-ledger-flow.spec.ts` | Spec ID 已建立；fresh/non-fresh/barrier/direct apply/write-operation audit 分层清楚。当前 release direct critical `--apply` 已复验 15/15 pass；本轮补 no-OA Browser `stale -> fresh` false-empty 防护、batch accounting relation stale 时保留当前银行/OA rows 且零 mutation、turnover Browser stale grouped ledger 禁写保护。剩余风险为真实业务写入口样本、认证态 HTTP/SSE、真实 Redis/RabbitMQ 外部 broker 采样和逐页面消费态全组合 | 下一步提供真实认证和审批 ticket 后执行一条低风险 write-operation apply |
| 2 | `runtime-workers` | spec-first-external-risk | `docs/modules/runtime-workers/e2e-spec.md`、`docs/modules/runtime-workers/e2e-coverage.md`、`tests/test_runtime_worker.py`、`tests/test_runtime_worker_registry.py`、`tests/test_runtime_queue.py`、`tests/test_runtime_monitoring.py`、`tests/test_runtime_worker_read_model_refresh_scopes.py`、`tests/test_read_model_scope_contract.py`、`tests/test_read_model_readiness_reporter.py`、`tests/test_rabbitmq_runtime.py`、`tests/test_runtime_queue_ops.py`、`tests/test_runtime_state_policy.py`、`tests/test_deploy_runtime_examples.py`、`tests/test_platform_runtime_boundary_guards.py`、`tests/test_app_status_readiness_backfill.py` | Spec ID 已建立；本地合同覆盖 worker/queue/registry/RabbitMQ fake。剩余风险为真实 Postgres/RabbitMQ/Redis/systemd worker drain 和长期运行 | 真实基础设施 smoke 由 staging/生产 gate 补 |
| 3 | `domain-events-lifecycle` | spec-first-partial | `docs/modules/domain-events-lifecycle/e2e-spec.md`、`docs/modules/domain-events-lifecycle/e2e-coverage.md`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_runtime_worker_read_model_refresh_scopes.py`、`web/src/test/domainEvents.test.ts`、`web/src/test/useActiveFinanceDomainEvent.test.tsx` | Spec ID 已建立；event contract 和主要 fan-out covered。剩余风险为全部 event x 页面 UI 状态组合和真实 write-operation durable 样本 | 页面级回归和 write-operation audit 渐进补 |
| 4 | `reconciliation-workbench` | spec-first-covered | `docs/modules/reconciliation-workbench/e2e-spec.md`、`docs/modules/reconciliation-workbench/e2e-coverage.md`、`tests/test_workbench_matching_rules.py`、`tests/test_workbench_auth_context_idempotency.py`、`tests/test_workbench_v2_api.py`、`web/src/test/Workbench*.test.tsx`、`web/e2e/workbench-relation-fanout.spec.ts`、`web/e2e/workbench-withdraw-flow.spec.ts`、`web/e2e/workbench-stale-error-flow.spec.ts`、`web/e2e/workbench-network-recovery-flow.spec.ts`、`web/e2e/workbench-exception-flow.spec.ts`、`web/e2e/workbench-cash-special-flow.spec.ts`、`web/e2e/workbench-large-scroll-flow.spec.ts`、`web/e2e/workbench-permissions-flow.spec.ts` | `RECON-WB-E2E-001..013` 已有 Browser/单元/API/integration 映射；confirm、active relation withdraw、automatic candidate 撤回拒绝且不 suppress、non-fresh/error、网络恢复、confirm/withdraw 重复提交、exception、权限和大数据交互均有当前证据。 |
| 4.1 | `workbench-relations` | spec-first-covered | `docs/modules/workbench-relations/e2e-spec.md`、`docs/modules/workbench-relations/e2e-coverage.md`、`web/e2e/workbench-relation-fanout.spec.ts`、`web/e2e/workbench-relations-candidate-semantics.spec.ts`、`web/e2e/workbench-relations-nonfresh-diagnostics.spec.ts`、`web/e2e/workbench-withdraw-flow.spec.ts`、`web/e2e/workbench-network-recovery-flow.spec.ts`、`tests/test_workbench_relation_repository.py`、`tests/test_workbench_auth_context_idempotency.py` | relation fan-out、candidate/linked 负面语义、active relation withdraw、automatic candidate 撤回拒绝、non-fresh 和网络恢复均有当前证据。 |
| 5 | `bank-details` | spec-first-covered | `docs/modules/bank-details/e2e-spec.md`、`docs/modules/bank-details/e2e-coverage.md`、`tests/test_bank_details_service.py`、`tests/test_bank_transaction_auto_category_service.py`、`tests/test_bank_transaction_category_service.py`、`tests/test_bank_auto_tag_rules_api.py`、`tests/test_bank_details_routes.py`、`tests/test_bankdetail_write_uow_contract.py`、`tests/test_bank_details_sql_runtime.py`、`tests/test_bank_account_balance_read_model.py`、`tests/test_bankdetail_backfill_cli.py`、`tests/test_bank_details_export_service.py`、`tests/test_bank_transaction_identity_service.py`、`web/src/test/BankDetails*.test.tsx`、`web/e2e/bank-details-initial-state.spec.ts`、`web/e2e/workbench-relation-fanout.spec.ts`、`web/e2e/bank-details-export-download.spec.ts`、`web/e2e/bank-details-stale-refreshing.spec.ts`、`web/e2e/bank-details-filtered-export-permissions.spec.ts`、`web/e2e/bank-details-large-scroll-flow.spec.ts`、`web/e2e/bank-details-category-flow.spec.ts`、`web/e2e/bank-details-auto-tag-rules-flow.spec.ts`、`web/e2e/imports-bank-transactions-flow.spec.ts` | `BANK-E2E-001..010` 已有 Browser/单元/API/integration 映射；真实 Chromium 覆盖默认当前年首屏、账户余额、默认列、fresh 空态、未正式化 decision / 历史 candidate 兼容值不显示 linked 标签、关联台确认后银行明细显示 `有oa`/`有发票`、confirm 后银行明细导出触发真实 download event 且文件包含 linked relation 字段、账户/自定义日期/关键字/分类筛选导出、分页状态不限制导出、长列表/宽字段/窄屏/菜单遮挡、`read_export_only` 可导出但分类/自动标签写入口禁用且零 mutation、forbidden/expired session gate 零 protected API、admin 分类写入、自动标签规则保存/reapply 等待 freshness 且 blocked 后置同步只显示 warning、候选确认/撤销、外部往来三层人工补分类/清除、transaction/account read model refreshing/stale/schema_mismatch/missing 诊断、false-empty、account retry、network recovery、导出业务错误，以及银行流水导入确认后进入银行明细显示导入行；剩余风险为真实历史多账户组合、真实 Postgres/RabbitMQ/Redis worker drain、真实导入到下游多页面 smoke、真实生产超大数据性能、真实 XLSX 完整解析和每个 account/transaction status 的笛卡尔组合 | 本模块 Spec-first E2E ID 已覆盖；下一轮推进 `workbench-relations` 导出权限/筛选组合、OA pending linked fan-out 或 `imports-bank-transactions` 账户冲突/失败链路 |
| 6 | `input-invoice-usage` | spec-first-covered | `docs/modules/input-invoice-usage/e2e-spec.md`、`docs/modules/input-invoice-usage/e2e-coverage.md`、`tests/test_invoice_usage_collection_sql_runtime.py`、`tests/test_input_invoice_usage_api.py`、`tests/test_read_model_freshness.py`、`tests/test_input_invoice_usage_oa_reverse_service.py`、`tests/test_input_invoice_usage_payment_rules.py`、`tests/test_search_pending_sql_runtime.py`、`tests/test_workbench_relation_repository.py`、`web/src/test/InputInvoiceUsage*.test.tsx`、`web/src/test/SettingsPage.test.tsx`、`web/e2e/input-invoice-relation-fanout.spec.ts`、`web/e2e/input-invoice-usage-flow.spec.ts`、`web/e2e/tax-offset-flow.spec.ts` | 已建立 Spec-first E2E 基线；Browser 覆盖 fresh rows/filter/sort/page-size、rows 暂时 503 错误态/导出禁用/刷新恢复、read-export 用户列表/导出/规则只读/OA reverse 不可创建草稿且 durable write endpoint 零调用、rows read model stale/refreshing false-empty 防护、relation detail stale/refreshing 详情不可用诊断、fresh `+N` relation detail 展开、当前筛选导出/download/row-limit/export non-fresh 禁用下载、OA reverse 子集草稿/已提交历史、未正式化 decision / 历史 candidate 兼容值不进入 linked 口径、Workbench confirm 后 linked rows evidence，并覆盖 OA pending/tax/cost downstream fresh read model 和支付规则当前页 refresh；`/api/search` 无独立前端 route，由 API/runtime 覆盖 relation 写入到 search fresh group context；剩余风险为真实 XLSX 完整解析/性能、真实 OA、真实 worker drain、未来 search/OA 状态刷新 UI 和真实 infra 收敛 | 本模块 Spec-first E2E ID 已覆盖；后续转真实 worker drain/staging smoke 或继续推进其他 `spec-first-partial` 页面 |
| 7 | `cost-statistics` | spec-first-covered | `docs/modules/cost-statistics/e2e-spec.md`、`docs/modules/cost-statistics/e2e-coverage.md`、`tests/test_cost_statistics_sql_projection_rules.py`、`tests/test_project_costing_service.py`、`tests/test_project_costing_api.py`、`tests/test_cost_statistics_api.py`、`tests/test_cost_statistics_runtime_service.py`、`tests/test_cost_statistics_sql_runtime.py`、`tests/test_cost_statistics_derived_lifecycle_executor.py`、`tests/test_read_model_refresh_gateway.py`、`tests/test_runtime_worker_read_model_refresh_scopes.py`、`tests/test_read_model_scope_contract.py`、`tests/test_read_model_architecture_guards.py`、`tests/test_app_status_overview_service.py`、`tests/test_runtime_monitoring.py`、`web/src/test/CostStatisticsApi.test.ts`、`web/src/test/CostStatisticsPage.test.tsx`、`web/e2e/cost-statistics-flow.spec.ts`、`web/e2e/cost-statistics-relation-fanout.spec.ts`、`web/e2e/imports-bank-transactions-flow.spec.ts`、`web/e2e/imports-invoices-flow.spec.ts`、`web/e2e/imports-etc-invoices-flow.spec.ts`、`web/e2e/bank-flow-rule-batches-flow.spec.ts`、`web/e2e/turnover-ledger-flow.spec.ts`、`web/e2e/settings-data-reset-flow.spec.ts` | `COST-E2E-001..010` 已有 Browser/单元/API/integration 映射；真实 Chromium 覆盖按时间首屏、explorer 暂时 503 错误态/普通空态防伪成功/导出禁用/手动刷新恢复、按项目、按银行选择银行账户/项目/流水详情、按费用类型选择费用类型/流水详情、`project_scope=all`、项目/费用类型/流水详情下钻、导出 preview、download event、row-limit 错误反馈、`read_export_only` read/export 权限、read model `refreshing`/`stale`/`failed` 非 fresh 防 false-empty/防旧项目行/防 0 条 summary/禁用导出入口、detail/export non-fresh 不打开旧详情/旧预览/下载文件、120+ 行大数据窄屏 fresh 宽表/下钻滚动，并覆盖 Workbench open candidate 不计入成本、confirmed 成本关系进入项目/流水详情、银行/发票/ETC import confirm 后成本统计 fresh read model 或导入影响行、no-OA submit 后成本统计 fresh read model 和免 OA 成本行展示、turnover manual closure 后成本统计 fresh read model 和外部往来闭环成本行展示、settings project completed save 后 active 排除/all 保留且 explorer fresh；service/architecture guards 保护 durable invalidation、queue 不可用不伪报成功以及进程内 read model owner 不回归；search 当前无独立前端 route，由 API/runtime 证据覆盖；剩余风险为真实 XLSX workbook 打开、真实生产超大数据性能、真实生产 scope cleanup `--apply`、真实 RabbitMQ/Redis/cost-statistics worker drain、真实导入样本和未来 search Browser UI | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke 或继续推进其他 `spec-first-partial` 页面 |
| 8 | `tax-offset` | spec-first-covered | `docs/modules/tax-offset/e2e-spec.md`、`docs/modules/tax-offset/e2e-coverage.md`、`tests/test_tax_offset_service.py`、`tests/test_tax_certified_import_service.py`、`tests/test_tax_offset_read_model_service.py`、`tests/test_tax_offset_api.py`、`tests/test_import_job_queue.py`、`tests/test_tax_offset_sql_runtime.py`、`tests/test_read_model_refresh_gateway.py`、`tests/test_runtime_worker_read_model_refresh_scopes.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_app_status_overview_service.py`、`tests/test_postgres_state_store.py`、`tests/test_postgres_migrations.py`、`web/src/test/TaxOffsetPage.test.tsx`、`web/src/test/TaxApi.test.ts`、`web/src/test/AppStatusIndicator.test.tsx`、`web/e2e/tax-offset-flow.spec.ts`、`web/e2e/workbench-relations-tax-offset-fanout.spec.ts` | 模块入口、测试矩阵、状态机、实施记录和 Spec-first E2E 基线已补齐；真实 Chromium 覆盖 read-export 可读无保存/导入入口、forbidden/expired 零 tax protected API、admin 写入口可见，也覆盖税金抵扣 `refreshing`/`stale`/`missing`/`failed` read model 非 fresh 防 false-empty、不泄露 stale reason、禁用保存计划、stale 自动恢复、取消勾选试算、保存计划、plan save 409 conflict 错误可见且不伪成功、页内已认证 Excel preview/confirm、刷新已认证 drawer、390px 窄屏 81 张销项/92 张进项大表搜索/排序/筛选/共享横向滚动/按钮无遮挡/筛选弹层 viewport clamp，以及 Workbench confirm 后重新读取 fresh tax offset read model 并展示 relation 影响后的进项计划行；剩余风险为真实税局认证 XLSX 大样本、真实 OA 附件/ETC 数据、真实 RabbitMQ/Redis/systemd tax-offset worker drain、生产超大数据性能和真实网络中断 smoke | 本模块 Spec-first E2E ID 已覆盖；后续转真实 worker drain/staging smoke 或继续推进其他 `spec-first-partial` 页面 |
| 9 | `pending-invoices` | spec-first-covered | `docs/modules/pending-invoices/e2e-spec.md`、`docs/modules/pending-invoices/e2e-coverage.md`、`tests/test_pending_invoice_service.py`、`tests/test_pending_invoice_api.py`、`tests/test_search_pending_sql_runtime.py`、`tests/test_invoice_lifecycle_page_integration.py`、`tests/test_pending_invoice_relation_identity.py`、`tests/test_pending_invoice_oa_identity_backfill.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_app_status_overview_service.py`、`tests/test_runtime_worker_registry.py`、`web/src/test/PendingInvoicesApi.test.ts`、`web/src/test/PendingInvoicesPage.test.tsx`、`web/src/test/GlobalOperationOverlayContext.test.tsx`、`web/e2e/pending-invoices-fanout.spec.ts`、`web/e2e/pending-invoices-filter-sort-flow.spec.ts`、`web/e2e/pending-invoices-attach-existing-flow.spec.ts`、`web/e2e/pending-invoices-income-status-flow.spec.ts`、`web/e2e/pending-invoices-export-download.spec.ts`、`web/e2e/pending-invoices-rules-save-flow.spec.ts`、`web/e2e/workbench-relations-candidate-semantics.spec.ts`、`web/e2e/workbench-relations-nonfresh-diagnostics.spec.ts` | `PENDING-E2E-001..009` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖从 `已支付待开票` 到关联台确认后回待找发票显示 `已支付已开票`、发票号码和 OA 申请人，也覆盖默认状态过滤、对方户名列筛选、金额升/降序排序、未正式化 decision / 历史 candidate 兼容值不驱动 linked-only 状态、relation-backed refreshing/stale 诊断、规则保存的 PUT contract、`pending_invoice:expense:requires_invoice` barrier、rows refetch 和保存反馈、规则保存暂时 503 时抽屉内错误/草稿保持/无全局阻塞/barrier 与 rows 不触发且重试成功，选择已有发票的多选流水/发票、候选“流水关联”chip、preview/confirm、confirm 暂时 503 时错误可见、drawer/preview/选择保持、rows 不半写且重试成功、conflict 禁用确认、零半写和 rows refetch，收入批量标记的单次 mutation、拒绝错误可见和零半写、保存暂时 503 时选择保持/rows 不半写且重试成功，confirm 后 export-preview/export 的筛选/排序、不带分页、真实 download event 和 OA/发票/relation 字段，以及 row-limit 下载失败错误可见且无下载文件；剩余风险为真实生产 Postgres 大数据/EXPLAIN/锁等待/长分页、真实 RabbitMQ/Redis/systemd pending/search/invoice-lifecycle worker drain、真实 XLSX workbook 解析和 withdraw 等其它 mutation 网络恢复 smoke | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 search Browser route、真实网络恢复 UI、真实下载解析 gate、待找发票写入口时追加 E2E |
| 10 | `oa-pending-payments` | spec-first-covered | `docs/modules/oa-pending-payments/e2e-spec.md`、`docs/modules/oa-pending-payments/e2e-coverage.md`、`tests/test_oa_pending_payment_projection_rows.py`、`tests/test_oa_pending_payment_api.py`、`tests/test_invoice_lifecycle_page_integration.py`、`tests/test_invoice_usage_collection_sql_runtime.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_app_status_overview_service.py`、`tests/test_runtime_worker_registry.py`、`web/src/test/OaPendingPaymentsPage.test.tsx`、`web/src/test/TableAlignmentStyles.test.ts`、`web/e2e/oa-pending-payments-flow.spec.ts`、`web/e2e/oa-pending-payments-confirm-paid-flow.spec.ts`、`web/e2e/oa-pending-payments-bank-link-flow.spec.ts`、`web/e2e/oa-pending-payments-nonfresh-flow.spec.ts`、`web/e2e/workbench-relations-oa-pending-fanout.spec.ts` | 模块入口、测试矩阵、状态机、实施记录和 Spec-first E2E 覆盖矩阵已补齐；真实 Chromium 覆盖 OA 待付款首屏、rows 暂时 503 错误态/普通空态防伪成功/手动刷新恢复、搜索、支付状态筛选、交易时间排序、OA/流水/发票详情抽屉和支出流水无需开票规则抽屉，也覆盖进行中 OA confirm-paid 的单次 mutation、防重、POST body、rows/read model 重新读取后 `已写回`、失败错误可见且零半写，进行中 OA bank-link 的抽屉默认全部、禁选、筛选、提交、rows/read model 重新读取后仍 `未写回`、confirm-paid 零调用和失败零半写，rows `refreshing` 不显示真实空态、不泄露 stale reason，detail 202 显示“详情暂不可用”，并覆盖 Workbench confirm 后 OA pending rows 重新读取、从 `未支付` 更新为 `已支付`、候选标记消失并显示 `关联台已确认`；剩余风险为真实 OA/Mongo/OA sync 字段变体、真实生产 Postgres 大数据/EXPLAIN/锁等待/长分页、真实 RabbitMQ/Redis/systemd OA 专属 worker drain、真实 rows/detail non-fresh 恢复、真实浏览器大数据表格/mutation 网络恢复/像素级视觉 smoke | 下一轮可转真实基础设施 worker drain smoke，或继续推进其他页面/功能的网络恢复链路 |
| 11 | `turnover-ledger` | spec-first-covered | `docs/modules/turnover-ledger/e2e-spec.md`、`docs/modules/turnover-ledger/e2e-coverage.md`、`tests/test_turnover_ledger_query_service.py`、`tests/test_turnover_ledger_api.py`、`tests/test_turnover_ledger_export_service.py`、`tests/test_turnover_relation_service.py`、`tests/test_turnover_ledger_extra_service.py`、`tests/test_workbench_turnover_grouping.py`、`tests/test_turnover_ledger_source_versions.py`、`tests/test_turnover_ledger_read_model_refresh.py`、`tests/test_turnover_ledger_uow_contract.py`、`tests/test_turnover_workbench_integration.py`、`web/src/test/TurnoverLedgerApi.test.ts`、`web/src/test/TurnoverLedgerPage.test.tsx`、`web/src/test/domainEvents.test.ts`、`web/e2e/turnover-ledger-flow.spec.ts` | `TURNOVER-E2E-001..009` 已有 Browser/组件/API/后端/integration 映射；真实 Chromium 覆盖首屏 grouped GET 暂时 503 后错误态/防普通空态/手动刷新台账恢复、标签准入保存、`PUT` body、`turnover_ledger:all` barrier、ledger reload、同组两条流水 manual closure confirm、operation barrier、成本统计 fresh read model 和闭环成本行、回外部往来 toolbar withdraw、barrier、open recovery 和零浏览器错误；Workbench/OA 合并边界、stale 防 false-empty、extra、导出/权限由后端/组件/API 覆盖；剩余风险为真实生产 PostgreSQL 历史数据/半迁移/大数据 EXPLAIN、真实 RabbitMQ/Redis/systemd turnover-ledger/cost-statistics/search worker drain、浏览器真实 XLSX 下载/视觉/大数据滚动 | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 search Browser route、mutation 级网络恢复、turnover 新写入口时追加 E2E |
| 12 | `no-oa-bank-batches` | spec-first-covered | `docs/modules/no-oa-bank-batches/e2e-spec.md`、`docs/modules/no-oa-bank-batches/e2e-coverage.md`、`tests/test_no_oa_bank_batch_service.py`、`tests/test_no_oa_bank_batch_application_service.py`、`tests/test_no_oa_bank_batch_api.py`、`tests/test_no_oa_bank_batch_routes.py`、`tests/test_no_oa_bank_batch_tag_selection_api.py`、`tests/test_no_oa_bank_batch_workbench_integration.py`、`tests/test_no_oa_bank_batch_read_model_refresh.py`、`tests/test_bankdetail_write_uow_contract.py`、`tests/test_bank_auto_tag_rules_api.py`、`tests/test_runtime_worker_registry.py`、`tests/test_app_status_overview_service.py`、`web/src/test/BankFlowRuleBatchApi.test.ts`、`web/src/test/BankFlowRuleBatchPage.test.tsx`、`web/src/test/domainEvents.test.ts`、`web/src/test/useActiveFinanceDomainEvent.test.tsx`、`web/e2e/bank-flow-rule-batches-flow.spec.ts`、`web/e2e/permissions-role-matrix.spec.ts` | `NO-OA-E2E-001..009` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖首屏 list GET 暂时 503 后错误态、防普通空态、手动刷新恢复和无可见错误残留，也覆盖标签准入保存、`PUT` body、`no_oa_bank_batch:all` barrier、列表重读、选择未提交手续费流水、提交 selection、等待 operation barrier fresh、进入成本统计验证 fresh read model 和免 OA 成本行、切已提交 bucket 撤回、进入历史 bucket 验证只读；权限矩阵覆盖 read-export 用户不能提交/撤回/保存标签且零 mutation；internal transfer/Workbench relation boundary、read model stale/missing 和大分页由后端/组件覆盖；剩余风险为真实生产 PostgreSQL 历史 no-OA 批次/legacy relation/半迁移/重复 relation 全量回放、真实 RabbitMQ/Redis/systemd no-oa-bank-batch worker drain、大数据月份/长标签树/长流水列表浏览器性能、mutation 级网络恢复、Bankdetail/no-OA 写 UoW 真实事务内收敛 | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 search Browser route、mutation 级网络恢复、no-OA 新写入口时追加 E2E |
| 13 | `batch-accounting` | spec-first-covered | `docs/modules/batch-accounting/e2e-spec.md`、`docs/modules/batch-accounting/e2e-coverage.md`、`tests/test_batch_accounting_api.py`、`tests/test_workbench_relation_read_facade.py`、`tests/test_workbench_relation_sql_projection.py`、`tests/test_runtime_worker_registry.py`、`tests/test_app_status_overview_service.py`、`tests/test_derived_data_lifecycle_service.py`、`web/src/test/BatchAccountingPage.test.tsx`、`web/src/test/domainEvents.test.ts`、`web/src/test/useActiveFinanceDomainEvent.test.tsx`、`web/e2e/batch-accounting-flow.spec.ts` | `BATCH-E2E-001..009` 已有 Browser/组件/API/后端/integration 映射；真实 Chromium 覆盖首屏 GET 暂时 503 后错误态/防普通空态/手动刷新恢复、窄桌面银行 rail 可读性、未提交选择银行/OA、submit、等待 `workbench_relation` barrier、进入已提交 bucket 展示 OA 明细、withdraw 等待 barrier、回到未提交 bucket 和零浏览器隐藏错误；non-fresh 诊断、差额说明、command boundary、权限和 downstream relation fan-out 由后端/组件/API/WorkBench relation 覆盖；剩余风险为真实生产 PostgreSQL 历史批量账务 relation/legacy collision/半迁移全量回放、真实 RabbitMQ/Redis/systemd workbench-relation/search/cost worker drain、大数据浏览器性能和其他下游页面最终显示 smoke | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增批量账务搜索/导出/新写入口时追加 E2E |
| 14 | `imports-bank-transactions` | spec-first-covered | `docs/modules/imports-bank-transactions/e2e-spec.md`、`docs/modules/imports-bank-transactions/e2e-coverage.md`、`tests/test_import_api.py`、`tests/test_import_service.py`、`tests/test_import_file_service.py`、`tests/test_import_file_api.py`、`tests/test_import_preview_audit.py`、`tests/test_import_job_queue.py`、`tests/test_import_formalization_api.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_runtime_worker_registry.py`、`tests/test_app_status_overview_service.py`、`tests/test_bank_account_balance_read_model.py`、`tests/test_bank_details_sql_runtime.py`、`tests/test_write_operation_slo_audit.py`、`tests/test_workbench_v2_api.py`、`web/src/test/ImportsApi.test.ts`、`web/src/test/ImportCenterPage.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/e2e/imports-bank-transactions-flow.spec.ts` | `IMPORT-BANK-E2E-001..008` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖上传两份银行流水、选择账户、慢预览期间预览/清空/确认动作锁定且只提交一次 preview、预览 audit/重复项明细、损坏文件混合上传 file-level error/未导入项明细/confirm 只提交正常文件 ID、账户冲突取消零 confirm/零 Workbench refresh/保留 preview 并可重新确认、确认账户冲突、preview stale 错误可见且不刷新 Workbench、confirm 失败错误可见且不误报成功、confirm 后刷新 Workbench并回银行明细看到账户余额 fresh gate 和导入行、成本统计 fresh read model 下游导入流水证据；`bank_import_confirmed` SLO profile 已要求真实银行确认产生银行账户余额 refresh、通用下游 refresh 和银行明细 `import.fact.changed` dirty scope drain；`IMPORT-BANK-E2E-009` 为 external-risk，真实银行大文件/历史模板、真实 Postgres/RabbitMQ/Redis/systemd import worker drain、worker crash/retry、Workbench/search 最终展示 smoke 仍需 staging | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 search Browser route/导入进度 UI/银行模板时追加 E2E |
| 15 | `imports-invoices` | spec-first-covered | `docs/modules/imports-invoices/e2e-spec.md`、`docs/modules/imports-invoices/e2e-coverage.md`、`tests/test_import_api.py`、`tests/test_import_service.py`、`tests/test_import_file_service.py`、`tests/test_import_file_api.py`、`tests/test_import_preview_audit.py`、`tests/test_import_job_queue.py`、`tests/test_import_formalization_api.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_invoice_lifecycle_page_integration.py`、`tests/test_tax_offset_api.py`、`tests/test_input_invoice_usage_api.py`、`tests/test_oa_pending_payment_api.py`、`tests/test_output_invoice_collection_api.py`、`tests/test_runtime_worker_registry.py`、`tests/test_app_status_overview_service.py`、`tests/test_workbench_v2_api.py`、`web/src/test/ImportsApi.test.ts`、`web/src/test/ImportCenterPage.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/e2e/imports-invoices-flow.spec.ts` | `IMPORT-INVOICE-E2E-001..008` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖发票导入两文件销项/进项选择、慢预览期间预览/清空/确认动作锁定且只提交一次 preview、预览 audit/重复项明细/未导入项/需复核文案、损坏文件混合上传 file-level error 且 confirm 只提交正常文件 ID、confirm 后刷新 Workbench 并清空草稿、confirm 后销项收款/进项使用/税金抵扣/待找发票/OA 待付款/成本统计 API `read_model_status=fresh` 且页面展示导入影响行、`preview_stale` 错误可见且不刷新 Workbench、confirm 失败错误可见且不误报成功；search 当前无独立前端 route，由 API/runtime 证据覆盖；`IMPORT-INVOICE-E2E-009` 为 external-risk，真实发票大文件/历史模板/信息汇总表样本、真实 Postgres/RabbitMQ/Redis/systemd import worker drain、worker crash/retry、search 外层 UI 和导出 smoke 仍需 staging | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 search Browser route/导入进度 UI/发票模板时追加 E2E |
| 16 | `imports-etc-invoices` | spec-first-covered | `docs/modules/imports-etc-invoices/e2e-spec.md`、`docs/modules/imports-etc-invoices/e2e-coverage.md`、`tests/test_etc_backend.py`、`tests/test_etc_reconciliation_service.py`、`tests/test_import_job_queue.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_platform_runtime_boundary_guards.py`、`tests/test_write_operation_slo_audit.py`、`web/src/test/EtcApi.test.ts`、`web/src/test/ImportCenterPage.test.tsx`、`web/src/test/EtcTicketManagementPage.test.tsx`、`web/e2e/imports-etc-invoices-flow.spec.ts` | `IMPORT-ETC-E2E-001..010` 已有 Browser/组件/API/integration 映射；Browser 覆盖 ready task、zip preview、confirm job、ETC/Tax/Cost fresh 页面和负面路径；后端保护 mutation-sensitive existing link、精确 `include_all=false` lifecycle、OA status 窄 Workbench fan-out、Tax/Cost 前端隔离以及 Workbench publish 后 Cost 收敛。`IMPORT-ETC-E2E-011` 仍需真实大 zip、对象存储、OA、Postgres/RabbitMQ/Redis/systemd worker drain、Nginx 和生产数据性能验证；显式 historical repair 是独立运维 gate，不在导入热路径 | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 search Browser route/历史修复页面/ETC 模板时追加 E2E |
| 17 | `output-invoice-collections` | spec-first-covered | `docs/modules/output-invoice-collections/e2e-spec.md`、`docs/modules/output-invoice-collections/e2e-coverage.md`、`tests/test_output_invoice_collection_api.py`、`tests/test_output_invoice_collection_service.py`、`tests/test_output_invoice_collection_lifecycle.py`、`tests/test_invoice_usage_collection_sql_runtime.py`、`tests/test_invoice_lifecycle_page_integration.py`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_runtime_worker_registry.py`、`tests/test_app_status_overview_service.py`、`web/src/test/OutputInvoiceCollectionsPage.test.tsx`、`web/src/test/TaxOffsetPage.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/src/test/domainEvents.test.ts`、`web/e2e/output-invoice-collections-flow.spec.ts`、`web/e2e/output-invoice-red-relation-fanout.spec.ts` | 已建立 Spec-first E2E 基线；真实 Chromium 覆盖 fresh 首屏 rows、keyword search、发票号码排序、收款状态/发票号码筛选、page-size 切换、状态/提醒保存、collection-status 暂时 503 时 drawer/草稿保持、reminder 零半提交、rows 不提前刷新且重试成功、status 已 200 但 collection-reminder 暂时 503 时 drawer/提醒草稿保持、rows 不提前刷新、重试不重复提交 status 且 reminder 成功后才刷新 rows、receipt create 暂时 503 时 idempotency key/preview/error 保持、rows/history 不伪成功且重试成功、receipt void/reissue 暂时 503 时原因弹窗/输入保持、history/rows 不伪成功且重试成功、rows refresh、正式收据 create/void/reissue/history、作废/重开 reason body、history/rows refresh、stale read model -> refreshing 诊断且不显示普通空态/旧 rows/写入口、当前筛选导出 download event/字段/不带分页/row-limit 零下载错误、红蓝票 relation 字段 export-preview/download、`read_export_only` 可读/可导出/可看只读规则和收据历史但状态/红蓝票/收据/编号设置写入口零 mutation、红蓝票 relation confirm/revoke -> rows refresh -> 人工依据展示/消失并恢复行状态，并在确认后导航到税金抵扣/成本统计验证两个下游页面重新请求自己的 fresh read model 且展示 relation 影响后的结果；search 当前没有独立前端 route，由 API/runtime 覆盖 relation 写入到 search fresh group context；剩余风险为真实生产 PostgreSQL 大数据/历史半迁移、真实 RabbitMQ/Redis/systemd invoice-usage-collection 与 invoice-lifecycle worker drain、正式收据真实并发锁等待、未来 search 外层 UI Browser fan-out、浏览器大数据视觉性能和 money/date 等所有字段组合的 Playwright 笛卡尔覆盖 | 本模块 Spec-first E2E ID 已覆盖；若新增独立 search 页面/入口，再补 Browser search fan-out。后续转真实 infra/staging smoke 或继续推进 shared partial / mutation recovery 队列 |
| 18 | `etc-tickets` | spec-first-covered | `docs/modules/etc-tickets/e2e-spec.md`、`docs/modules/etc-tickets/e2e-coverage.md`、`tests/test_etc_backend.py`、`tests/test_etc_reconciliation_service.py`、`tests/test_import_service.py`、`tests/test_postgres_core_repository.py`、`tests/test_workbench_sql_runtime.py`、`tests/test_workbench_pair_relation_service.py`、`tests/test_platform_runtime_boundary_guards.py`、`tests/test_cleanup_orphan_etc_reconciliation_tasks_tool.py`、`tests/test_historical_etc_business_batch_migration_service.py`、`tests/test_migrate_historical_etc_business_batches_tool.py`、`tests/test_app_status_overview_service.py`、`web/src/test/EtcTicketManagementPage.test.tsx`、`web/src/test/EtcApi.test.ts`、`web/src/test/CandidateGroupGrid.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/e2e/etc-tickets-flow.spec.ts` | `ETC-TICKET-E2E-001..010` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖 ETC 票据管理未提交业务批次首屏、business-batches 首屏 GET 暂时失败错误态/防普通空态/手动刷新恢复、未提交 business batch delete 暂时失败不误删/不关弹窗且可重试、已提交 business batch reset/delete 暂时失败不误删/不改计数/不关弹窗且可重试、source file delete 暂时失败不误删/不关弹窗且可重试、ticket-root source upload 暂时失败不伪上传且可重试、发票明细、创建 OA 草稿、OA draft 暂时失败不进入提交确认伪成功且可重试、manual OA status 暂时失败不切已提交 bucket 且可重试、人工确认已提交、已提交 bucket 和严格浏览器错误捕获；真实对象存储 upload、大 ZIP timeout、task-only/new batch/durable import recovery、Workbench summary fan-out、权限和旧入口 regression 由组件/API/后端覆盖；`ETC-TICKET-E2E-010` 为 external-risk，真实大 ZIP/票根网 PDF/XML/TXT 混合包、真实对象存储/Nginx 上传、真实 OA 草稿系统、生产历史迁移 dry-run/execute、Workbench/税金/成本/search 全量重建最终页面 smoke、真实 worker drain、import confirm 和真实对象存储/Nginx 上传中断仍需 staging | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 ETC import confirm 等 mutation 级网络恢复 Browser 主链路时追加 E2E |
| 19 | `settings` | spec-first-covered | `docs/modules/settings/e2e-spec.md`、`docs/modules/settings/e2e-coverage.md`、`tests/test_app_settings_service.py`、`tests/test_settings_data_reset_service.py`、`tests/test_oa_applicant_credentials_service.py`、`tests/test_oa_applicant_credentials_api.py`、`tests/test_postgres_oa_applicant_credentials_repository.py`、`tests/test_target_oa_applicant_token_provider.py`、`tests/test_postgres_migrations.py`、`tests/test_app_status_overview_service.py`、`tests/test_derived_data_lifecycle_service.py`、`web/src/test/SettingsPage.test.tsx`、`web/src/test/WorkbenchSelection.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/e2e/settings-data-reset-flow.spec.ts` | `SETTINGS-E2E-001..010` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖设置页 data reset 影响确认、OA 密码复核、job polling、settings reload、全局反馈、reset 后银行明细 fresh empty 和待找发票 fresh rows，也覆盖项目标记完成 -> 保存 settings -> 成本统计 active/all fresh project scope；settings payload/OA 凭据泄密防护、银行标签写边界、OA manual import 权限、data reset 安全边界和 App Status/read model/worker 可见性由组件/API/service/integration 覆盖；`SETTINGS-E2E-010` 为 external-risk，真实生产 data reset 备份/worker drain/Redis cache、真实 OA 登录/草稿、真实 PostgreSQL pgcrypto key/历史 settings payload、所有下游页面最终 smoke 仍需 staging | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke，或新增 settings section/mutation route 时追加 E2E |
| 20 | `app-health-operations` | spec-first-covered | `docs/modules/app-health-operations/e2e-spec.md`、`docs/modules/app-health-operations/e2e-coverage.md`、`tests/test_app_health_api.py`、`tests/test_app_health_service.py`、`tests/test_app_health_alert_service.py`、`tests/test_app_status_overview_service.py`、`tests/test_runtime_monitoring.py`、`tests/test_app_status_readiness_backfill.py`、`tests/test_runtime_worker_registry.py`、`tests/test_runtime_queue.py`、`tests/test_runtime_queue_ops.py`、`tests/test_deploy_runtime_examples.py`、`web/src/test/AppHealthOperationsPage.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/src/test/AppStatusApi.test.ts`、`web/src/test/AppHealthStatusContext.test.tsx`、`web/src/test/AppHealthResolver.test.ts`、`web/src/test/AppHealthBroadcast.test.tsx`、`web/e2e/app-shell.spec.ts` | `APP-HEALTH-E2E-001..010` 已有 Browser/组件/API/integration 映射；真实 Chromium 覆盖 admin shell/dashboard、read-export admin-only gate、forbidden session gate、expired session gate、dashboard API 零调用边界和严格浏览器错误捕获，expired session 的预期 401 resource error 作为认证 gate 例外；App Status overview、runtime/readiness/worker/queue/ready/SSE gate、registry completeness 和 dashboard stale/error 行为由组件/API/service/tool tests 覆盖；`APP-HEALTH-E2E-010` 为 external-risk，真实 PostgreSQL/RabbitMQ/Redis/systemd worker heartbeat、queue backlog、Nginx/OA iframe SSE、真实大库 dashboard metrics、pg_stat_statements 配置和 authenticated runtime closure 仍需 staging | 本模块 Spec-first E2E ID 已覆盖；后续转真实 infra/staging smoke |
| 21 | `permissions-and-audit` | spec-first-partial | `docs/modules/permissions-and-audit/e2e-spec.md`、`docs/modules/permissions-and-audit/e2e-coverage.md`、`docs/modules/permissions-and-audit/write-entry-inventory.md`、`tests/test_permissions_write_entry_inventory.py`、`tests/test_auth_guard.py`、`tests/test_session_api.py`、`tests/test_audit_service.py`、`tests/test_app_settings_service.py`、`tests/test_settings_data_reset_service.py`、`tests/test_oa_applicant_credentials_api.py`、`tests/test_bank_auto_tag_rules_api.py`、`tests/test_tax_offset_api.py`、`tests/test_pending_invoice_api.py`、`tests/test_turnover_ledger_api.py`、`tests/test_turnover_workbench_integration.py`、`tests/test_workbench_auth_context_idempotency.py`、`tests/test_bankdetail_write_uow_contract.py`、`tests/test_platform_runtime_boundary_guards.py`、`tests/test_app_health_api.py`、`web/src/test/SessionGate.test.tsx`、`web/src/test/SessionApi.test.ts`、`web/src/test/SettingsPage.test.tsx`、`web/src/test/WorkbenchSelection.test.tsx`、`web/src/test/AppHealthOperationsPage.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/src/test/TaxOffsetPage.test.tsx`、`web/e2e/app-shell.spec.ts`、`web/e2e/permissions-role-matrix.spec.ts` | `PERM-E2E-001..010` 已建立 shared Spec-first 合同、覆盖映射和写入口 inventory；真实 Chromium 覆盖 read-export 全页面可读零 mutation、首屏和 opener registry 已打开动态区域 visible enabled 写控件候选扫描、settings/tax/import/no-OA 高风险入口、关联台列顺序拖拽 settings 保存、关联台 open candidate 确认/异常/撤回、关联台已配对撤回、关联台已处理异常/已忽略恢复、银行分类确认、银行人工待分类、银行自动标签、pending invoices 选择已有发票/收入状态/规则保存、进项支付规则、进项 OA reverse、销项收款规则/收据历史、OA pending confirm/link-bank/支出流水无需开票规则、ETC source file 上传/确认对账/人工核对、batch accounting submit 与 submitted withdraw、turnover tag/closure/extra 入口、ETC submit/new/delete 代表性写入口、full/admin gate、admin 销项收据编号设置保存、AppHealth/session gate 和严格浏览器错误捕获；进项 OA reverse preview 作为 read-like POST 例外，仍断言 OA draft、batch 和 manual status durable write endpoints 零调用；后端/API 覆盖 body actor spoofing、write/admin 403、audit、敏感数据保护；新增 registry/role-matrix/inventory/dynamic opener/static proof gate 已拦截新增页面未登记、非 admin route 未进入 read-export smoke、covered row 缺 Browser 证据、Browser 证据路径指向已删除/改名 Playwright spec、covered-browser row 缺 dynamic opener 或静态覆盖证明、dynamic/static registry 引用非 covered-browser 模块或重复归类、Playwright opener 漏 inventory 登记，以及 inventory 声明了不存在的 Playwright opener；`PERM-E2E-003` 仍为 partial，剩余风险是尚未自动打开的所有页面特定抽屉/弹窗和真实环境权限 smoke；`PERM-E2E-010` 为 external-risk，真实 OA 菜单/角色同步、生产 token 过期语义、审计查询/导出、代理层真实下载权限仍需 staging | 下一轮优先补页面特定抽屉/弹窗 opener registry 或真实环境权限 smoke；发布前补 staging 权限验证 |
| 22 | `app-shell-navigation` | spec-first-covered | `docs/modules/app-shell-navigation/e2e-spec.md`、`docs/modules/app-shell-navigation/e2e-coverage.md`、`web/src/test/PageRouteHost.test.tsx`、`web/src/test/AppSidebar.test.tsx`、`web/src/test/App.test.tsx`、`web/src/test/SessionGate.test.tsx`、`web/src/test/GlobalOperationOverlayContext.test.tsx`、`web/src/test/PageSessionStateContext.test.tsx`、`web/src/test/useFinanceTableSession.test.tsx`、`web/src/test/domainEvents.test.ts`、`web/e2e/app-shell.spec.ts`、`web/e2e/app-shell-responsive.spec.ts` | `APP-SHELL-E2E-001..010` 已建立 Spec-first 合同和覆盖矩阵；route registry、mount/unmount、event cleanup、sidebar preload/active、compact drawer、embedded OA shell、session gate、operation overlay、page session 和 App Status shell 边界均有 Browser/组件证据；剩余风险为 OA iframe 像素级视觉、真实触摸手势惯性、真实 chunk 网络失败后浏览器缓存行为 | 本模块本地 Spec IDs 已 covered；发布前按真实浏览器/iframe smoke 补 |
| 23 | `finance-table-system` | spec-first-covered | `docs/modules/finance-table-system/e2e-spec.md`、`docs/modules/finance-table-system/e2e-coverage.md`、`web/src/test/FinanceTable.test.tsx`、`web/src/test/TableLayoutTokens.test.ts`、`web/src/test/TableAlignmentStyles.test.ts`、`web/src/test/useFinanceTableSession.test.tsx`、`web/src/test/PageSessionStateContext.test.tsx`、`web/src/test/BankDetailsPage.test.tsx`、`web/src/test/TaxOffsetPage.test.tsx`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`web/src/test/PendingInvoicesPage.test.tsx`、`web/src/test/OutputInvoiceCollectionsPage.test.tsx`、`web/src/test/OaPendingPaymentsPage.test.tsx`、`web/src/test/CostStatisticsPage.test.tsx`、`web/src/test/TurnoverLedgerPage.test.tsx`、`web/src/test/AppHealthOperationsPage.test.tsx`、`web/src/test/ImportCenterPage.test.tsx`、`web/e2e/finance-table-system-flow.spec.ts` | `FIN-TABLE-E2E-001..010` 已建立 Spec-first 合同和覆盖矩阵；共享 primitive、CSS token、table session、代表性真实 Chromium 宽表、页面级筛选/排序/分页/导出、non-fresh 防 false-empty、详情不可用诊断和页面 wrapper 归属均有证据；剩余风险为真实生产大数据浏览器滚动、超宽列、真实浏览器下载保存行为和新增 wrapper 自动登记 | 本模块本地 Spec IDs 已 covered；下一轮补真实大数据/下载保存 staging smoke 或新增 wrapper 自动发现 |
| 24 | `oa-integration` | spec-first-external-risk | `docs/modules/oa-integration/e2e-spec.md`、`docs/modules/oa-integration/e2e-coverage.md`、`tests/test_mongo_oa_adapter.py`、`tests/test_worker_oa_sync.py`、`tests/test_oa_identity_service.py`、`tests/test_oa_applicant_credentials_service.py`、`tests/test_oa_applicant_credentials_api.py`、`tests/test_postgres_oa_applicant_credentials_repository.py`、`tests/test_target_oa_applicant_token_provider.py`、`tests/test_input_invoice_usage_oa_reverse_service.py`、`tests/test_input_invoice_usage_api.py`、`tests/test_oa_pending_payment_projection_rows.py`、`tests/test_oa_pending_payment_api.py`、`tests/test_oa_projection_sql_runtime.py`、`tests/test_oa_manual_import_service.py`、`tests/test_oa_manual_import_api.py`、`tests/test_oa_role_sync_service.py`、`tests/test_deploy_oa_script.py`、`tests/test_deploy_oa_nginx_config.py`、`web/src/test/SessionApi.test.ts`、`web/src/test/SessionGate.test.tsx`、`web/src/test/OaPendingPaymentsPage.test.tsx`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`web/src/test/InputInvoiceUsageFiltersAndDrawers.test.tsx`、`web/src/test/SettingsPage.test.tsx`、`web/src/test/SettingsOaManualSearchImportTable.test.tsx`、`web/src/test/EtcApi.test.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`web/src/test/EtcOaNavigation.test.ts` | Spec ID 已建立；本地合同和页面 mock 覆盖主要流程。剩余风险为真实 OA 登录/RSA/草稿页面、真实 OA Mongo 字段/附件/性能、同域 cookie/iframe/Nginx、真实 worker drain、真实 admin 登录态和 OA role sync | 发布前按真实 OA/staging smoke 补 |
| 25 | `data-safety-reset` | spec-first-partial | `docs/modules/data-safety-reset/e2e-spec.md`、`docs/modules/data-safety-reset/e2e-coverage.md`、`tests/test_settings_data_reset_service.py`、`tests/test_app_health_api.py`、`tests/test_background_job_service.py`、`tests/test_runtime_state_policy.py`、`tests/test_platform_runtime_boundary_guards.py`、`web/src/test/SettingsPage.test.tsx`、`web/src/test/WorkbenchSelection.test.tsx`、`web/src/test/AppHealthOperationsPage.test.tsx`、`web/src/test/AppStatusIndicator.test.tsx`、`web/src/test/AppHealthStatusContext.test.tsx`、`web/src/test/AppHealthBroadcast.test.tsx`、`web/e2e/settings-data-reset-flow.spec.ts` | Spec ID 已建立；设置页 Browser flow、job/API/service covered；本地 Browser 已覆盖 reset 完成后银行明细 fresh empty 和待找发票 fresh rows。剩余风险为真实 PostgreSQL PITR/staging restore、对象存储备份恢复、真实 Redis/RabbitMQ/systemd worker drain、大库 reset 后真实多页面最终 fresh、真实 OA Mongo/草稿/附件 | 发布前按 staging data reset smoke 补真实环境验证 |
| 26 | `deploy` | spec-first-external-risk | `docs/modules/deploy/e2e-spec.md`、`docs/modules/deploy/e2e-coverage.md`、`tests/test_deploy_oa_script.py`、`tests/test_deploy_oa_nginx_config.py`、`tests/test_nightly_ci.py`、`tests/test_deploy_runtime_examples.py`、`tests/test_runtime_worker_registry.py`、`tests/test_app.py`、`tests/test_app_postgres_mode.py`、`web/e2e/app-shell.spec.ts` | Spec ID 已建立；脚本、workflow、nginx example 和 release contract covered。剩余风险为真实 GitHub Actions 启用状态、SSH/sudo/root-owned helper、systemd restart、PostgreSQL migration/PITR、Nginx live config、Redis/RabbitMQ 真连接、OA iframe cookie、rollback 和真实浏览器缓存 | 发布前按 staging/production release smoke 补真实环境验证 |

## 最近验证命令

最近运行：

```bash
PYTHONPATH=backend/src python3 -m unittest tests.test_nightly_ci -v

bash scripts/verify.sh backend

bash scripts/verify.sh docs

PYTHONPATH=backend/src python3 -m unittest tests.test_nightly_ci -v

bash scripts/verify.sh infra-smoke

missing=0; for f in docs/modules/*/tests.md; do
  for p in "七类测试适用性" "Nightly CI" "未测风险"; do
    if ! rg -q "$p" "$f"; then
      echo "missing $p in $f"
      missing=1
    fi
  done
done
exit $missing

cd web && npm test -- --run \
  src/test/App.test.tsx \
  src/test/ImportCenterPage.test.tsx \
  src/test/TaxOffsetPage.test.tsx \
  src/test/WorkbenchSelection.test.tsx

cd web && npm test -- --run src/test/WorkbenchSelection.test.tsx

cd web && npm test -- --run src/test/AppHealthStatusContext.test.tsx

bash scripts/verify.sh frontend

bash scripts/verify.sh e2e

cd web && npx playwright test e2e/workbench-relation-fanout.spec.ts

cd web && npx playwright test e2e/bank-details-export-download.spec.ts

cd web && npx playwright test e2e/bank-details-initial-state.spec.ts

cd web && npx playwright test e2e/bank-details-stale-refreshing.spec.ts

cd web && npx playwright test e2e/bank-details-filtered-export-permissions.spec.ts

cd web && npx playwright test e2e/bank-details-category-flow.spec.ts

cd web && npx playwright test e2e/workbench-withdraw-flow.spec.ts

cd web && npx playwright test e2e/workbench-stale-error-flow.spec.ts

cd web && npx playwright test e2e/pending-invoices-fanout.spec.ts

cd web && npx playwright test e2e/batch-accounting-flow.spec.ts

cd web && npx playwright test e2e/turnover-ledger-flow.spec.ts

cd web && npx playwright test e2e/imports-bank-transactions-flow.spec.ts

cd web && npx playwright test e2e/imports-bank-transactions-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/imports-invoices-flow.spec.ts --project=chromium

PYTHONPATH=backend/src python3 -m unittest tests.test_write_operation_slo_audit -v

cd web && npx playwright test e2e/imports-invoices-flow.spec.ts

cd web && npx playwright test e2e/imports-etc-invoices-flow.spec.ts

cd web && npx playwright test e2e/tax-offset-flow.spec.ts

cd web && npx playwright test e2e/settings-data-reset-flow.spec.ts

cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts

cd web && npx playwright test e2e/output-invoice-red-relation-fanout.spec.ts

cd web && npx playwright test e2e/input-invoice-relation-fanout.spec.ts

cd web && npx playwright test e2e/input-invoice-usage-flow.spec.ts

cd web && npx playwright test e2e/etc-tickets-flow.spec.ts

cd web && npx playwright test e2e/oa-pending-payments-flow.spec.ts

cd web && npx playwright test e2e/oa-pending-payments-confirm-paid-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/oa-pending-payments-bank-link-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/oa-pending-payments-nonfresh-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/workbench-relations-oa-pending-fanout.spec.ts --project=chromium

cd web && npx playwright test e2e/bank-flow-rule-batches-flow.spec.ts

cd web && npx playwright test e2e/cost-statistics-flow.spec.ts

cd web && npm run e2e:smoke

cd web && npx playwright test e2e/finance-table-system-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/pending-invoices-attach-existing-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/pending-invoices-income-status-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/pending-invoices-export-download.spec.ts --project=chromium

cd web && npx playwright test e2e/pending-invoices-rules-save-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/pending-invoices-filter-sort-flow.spec.ts --project=chromium

cd web && npx playwright test \
  e2e/pending-invoices-fanout.spec.ts \
  e2e/pending-invoices-filter-sort-flow.spec.ts \
  e2e/pending-invoices-attach-existing-flow.spec.ts \
  e2e/pending-invoices-income-status-flow.spec.ts \
  e2e/pending-invoices-export-download.spec.ts \
  e2e/pending-invoices-rules-save-flow.spec.ts \
  e2e/workbench-relations-candidate-semantics.spec.ts \
  e2e/workbench-relations-nonfresh-diagnostics.spec.ts \
  --project=chromium

cd web && npx playwright test e2e/bank-flow-rule-batches-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/turnover-ledger-flow.spec.ts --project=chromium

cd web && npx playwright test e2e/batch-accounting-flow.spec.ts --project=chromium
```

结果：

- 本轮 no-OA Spec-first covered 校准命令 `cd web && npx playwright test e2e/bank-flow-rule-batches-flow.spec.ts --project=chromium` 通过 2 tests；覆盖标签准入保存、`no_oa_bank_batch:all` freshness barrier、列表重读、selected-row submit、成本统计 fresh read model fan-out、withdraw、history 只读和前端运行时报错捕获。
- 本轮 turnover Spec-first covered 校准命令 `cd web && npx playwright test e2e/turnover-ledger-flow.spec.ts --project=chromium` 通过 2 tests；覆盖标签准入保存、`turnover_ledger:all` freshness barrier、台账重读、manual closure confirm、operation barrier、成本统计 fresh read model fan-out、withdraw、grouped recovery 和前端运行时报错捕获。
- 本轮 batch-accounting Spec-first covered 校准命令 `cd web && npx playwright test e2e/batch-accounting-flow.spec.ts --project=chromium` 通过 2 tests；覆盖窄桌面银行 rail 可读性、未提交选择银行/OA、submit、`workbench_relation` freshness barrier、已提交 bucket、withdraw、回到未提交 bucket 和前端运行时报错捕获。
- 本轮 settings/data reset Spec-first covered 校准命令 `cd web && npx playwright test e2e/settings-data-reset-flow.spec.ts --project=chromium` 通过 2 tests；覆盖 data reset 影响确认、OA 密码复核、job 202、job polling、settings reload、全局反馈、reset 后银行明细 fresh empty、待找发票 fresh rows、项目完成保存到成本统计 active/all fresh read model fan-out 和前端运行时报错捕获。
- 本轮 ETC 票据 Spec-first covered 校准命令 `cd web && npx playwright test e2e/etc-tickets-flow.spec.ts --project=chromium` 通过 1 test；覆盖未提交业务批次、发票明细、OA 草稿、manual submitted bucket 切换和前端运行时报错捕获。
- 本轮 App Health Spec-first covered 校准命令 `cd web && npx playwright test e2e/app-shell.spec.ts --project=chromium` 通过 4 tests；覆盖 admin dashboard、read-export admin-only gate、forbidden/expired session gate、dashboard protected API 零调用和前端运行时报错捕获，expired session 的预期 401 resource error 被限定为认证 gate 例外。
- 本轮 permissions shared Spec-first 校准命令 `cd web && npx playwright test e2e/permissions-role-matrix.spec.ts --project=chromium` 现已通过 7 tests；覆盖 read-export 全页面可读零 mutation、首屏和 opener registry 已打开动态区域 visible enabled 写控件候选扫描、settings/tax/import/no-OA 高风险入口、关联台列顺序拖拽 settings 保存、关联台 open candidate 确认/异常/撤回、关联台已配对撤回、关联台已处理异常/已忽略恢复、银行分类确认、银行人工待分类、银行自动标签、pending invoices 选择已有发票/收入状态/规则保存、进项支付规则、进项 OA reverse、销项收款规则/收据历史、OA pending confirm/link-bank/支出流水无需开票规则、ETC source file 上传/确认对账/人工核对、batch accounting submit、batch accounting submitted withdraw、turnover tag/closure/extra 入口、ETC submit/new/delete 代表性写入口、full/admin gate 和前端运行时报错捕获；进项 OA reverse preview 作为 read-like POST 例外，仍断言 OA draft、batch 和 manual status durable write endpoints 零调用；新增 `PYTHONPATH=backend/src python3 -m unittest tests.test_permissions_write_entry_inventory -v` 通过 14 tests，拦截新增页面未登记、非 admin route 未进入 read-export smoke、covered row 缺 Browser 证据、Playwright opener 缺 inventory 登记、inventory 声明了不存在的 Playwright opener，以及关键写控件关键词误删；`PERM-E2E-003` 的未自动打开所有页面特定抽屉/弹窗和真实环境权限 smoke 仍为 partial，不伪装 covered。
- 后续最新完整本地 Browser smoke 命令 `cd web && npm run e2e:smoke` 通过 151 tests / 151 passed；覆盖当前 smoke 清单里的页面级 Spec-first E2E、跨页面 fan-out、权限矩阵、non-fresh 防 false-empty、导入/导出和 Workbench 网络/异常/重复提交回归。
- 本轮生产服务器 runtime smoke：`/health` 与 `/health/ready` 为 ready，release `main-3ac07068-20260619013223` runtime consistent；20 个 `fin-ops-worker@*.service` active/running，RabbitMQ dispatcher active；最新 20 个当前 worker heartbeat lag 0-2s；PostgreSQL 聚合显示 `job.outbox_events` 156907 行全为 `done`、`job.read_model_dirty_scopes` 142872 行全为 `done`、`read_model.app_status_readiness` 169 行全为 `fresh`；`finops-deploy-control read-model-scope-contract ... --json` 返回 `ok=true`、`violation_count=0`、`current_uncovered_outbox_failure_count=0`。
- 本轮生产 critical read model apply gate：`read_model_slo_smoke --critical-only --apply --target-ms 5000 --timeout-seconds 120` 计划 15 个 critical scope，15/15 均达到 outbox/dirty `done` 且 readiness `fresh`，证明真实 worker drain 和 read model 最新状态可以收敛；但初次 5 秒 SLO 有 3 个 scope 超时：`invoice_lifecycle` 约 6.11s、`oa_pending_payment` 约 6.07s、`cost_statistics` 约 10.48s。聚焦重试后 `oa_pending_payment` 约 0.82s、`cost_statistics` 约 3.43s 通过，`invoice_lifecycle` 仍约 5.76s；只读分段显示 `invoice_lifecycle` 2026-04 projection 读取低于 1s，瓶颈在 rows 保存。本地将 `save_invoice_lifecycle_rows(...)` 改为 batch save 并补测试后，通过 release `main-99ea9b35-invoice-lifecycle-batch-20260619145710` 激活到生产；发布后重新执行 critical apply，15/15 pass，summary p95/max 约 3.52s，`invoice_lifecycle` 约 1.29s，发布后 `/health/ready` ready，DB 汇总为 `job.outbox_events=done`、`job.read_model_dirty_scopes=done`、`read_model.app_status_readiness=fresh`。
- 本轮真实业务写操作只读 audit：生产 `write_operation_slo_audit --lookback-hours 168 --target-ms 1000 --p99-target-ms 3000 --limit 5000` 返回 `status=fail`、`event_sample_count=5000`、`expectation_count=56`、`failed=56`、`missing=12`；非 missing 的历史样本主要是 Workbench relation cross-page、turnover、bank auto-tag 等写链路超过 1s/3s SLO，不能证明当前目标闭环。以 hotfix 激活时间 `2026-06-19T14:58:07+08:00` 作为 `--since` 后，`event_sample_count=21` 但 56/56 expectations 全部 `missing`，说明新 release 后还没有真实高影响业务写操作 profile 样本。
- 本轮生产只读 scenario discovery：`write_operation_scenario_discovery --limit 10` 返回 `status=ready`，候选计数为 turnover 10、Workbench withdraw context 10、no-OA withdraw context 10，并生成 30 个 scenario 到生产 `/tmp/finops-write-e2e-scenarios-20260619.json`；`write_operation_e2e_smoke --scenario /tmp/finops-write-e2e-scenarios-20260619.json` dry-run 通过，计划执行 `turnover_manual_closure_or_withdraw` 10 个、`workbench_relation_withdraw` 10 个、`no_oa_bank_batch_withdraw` 10 个。dry-run 未带 `--apply`，没有执行写操作；这些 scenario 都要求真实 OA/Admin auth 和人工/业务审批后才能 `--apply`，不能直接盲目撤回生产关系。
- 本轮生产 minimal write-operation dry-run：只读分级显示 turnover 候选 10 个中 7 个为 `suggested`、2 个为 `deterministic`、1 个为 `confirmed`；Workbench withdraw 候选 10 个均为 `active`；no-OA withdraw 候选 10 个均为 `submitted`。因此下一条最小真实 apply smoke 应优先选择 turnover `suggested` relation，并仍需业务批准。已生成生产 `/tmp/finops-write-e2e-scenarios-20260619-minimal-turnover-dryrun.json`，只包含 `turnover-withdraw-turnover_rel_05cac958eb8c7c74` 一条 scenario；`write_operation_e2e_smoke --scenario ...` dry-run 通过，输出 `/tmp/finops-write-e2e-minimal-turnover-dry-run-20260619.json`，`status=dry_run`、`scenario_count=1`、`auth_configured=false`。计划写入口为 `POST /api/turnover-ledger/relations/turnover_rel_05cac958eb8c7c74/withdraw`，后置探针为 `/api/turnover-ledger?view=grouped&page=1&page_size=50` 和 `/api/operations/app-health-dashboard`；没有配置 auth、没有 `--apply`，因此没有执行任何业务写操作。随后工具合同已补强并发布到生产：release `main-33a150e7-write-e2e-approval-gate-20260619151922` active，`/health/ready` 为 ready，`write_operation_e2e_smoke --apply` 还必须带 `--approval-ticket` / `FIN_OPS_WRITE_E2E_APPROVAL_TICKET`，`runtime_sync_closure_gate --apply-write-scenarios` 还必须带 `--write-approval-ticket`；生产本机缺审批引用 smoke 返回 exit code 2、`status=approval_missing`、`error=write_operation_e2e_requires_approval_ticket`、`scenario_count=1`、`approval_configured=false`，并停止在业务写操作前。发布后复跑 critical read-model apply，15/15 pass，summary p50 约 926.619ms、p95/max 约 4960.071ms；DB 汇总为 `job.outbox_events=done`、`job.read_model_dirty_scopes=done`、`read_model.app_status_readiness=fresh`。
- 本轮生产只读巡检补充：2026-06-19 16:50-16:52 CST 通过 SSH 只读检查 release `main-33a150e7-write-e2e-approval-gate-20260619151922` 仍为 API、RabbitMQ dispatcher 和 20 个 worker 的 `WorkingDirectory`，`finops-deploy-control status` 显示 API、dispatcher 和所有 `fin-ops-worker@*.service` active；公网 `health_ready_payload_probe` 通过，`elapsed_ms=45.58`、`status=pass`、`health_status=ready`、`runtime_blocker_count=0`、`runtime_release.consistent=true`；`runtime_sync_closure_gate` 只读模式的 `runtime_health` pass，snapshot 显示 `queue_backlog={}`、`failed_jobs=0`、`missing_required_worker_count=0`、`stale_required_worker_count=0`、`stale_dirty_scope_count=0`、RabbitMQ queue/DLQ/unacked 均为 0。`read_model_slo_smoke --critical-only` 未带 `--apply`，仅 dry-run 规划 15 个 critical scope，不能替代 enqueue-to-fresh 证明；`write_operation_slo_audit --lookback-hours 24 --limit 50` 返回 `expectation_count=56`、`failed=56`、`missing=56`，说明最近 24 小时没有足够真实业务写入 profile 样本；`write_operation_scenario_discovery --limit 20` 只读发现 turnover、Workbench withdraw、no-OA withdraw 候选各 20 条，但这些候选都需要真实认证和业务审批后才能 apply。公网只读 `runtime_sync_closure_gate` 的页面 shell 探针通过，但 API 探针因未配置认证返回 401，因此 authenticated HTTP SLO 仍未闭环。
- 本轮生产 critical read model apply 复验：2026-06-19 16:54 CST 在 release `main-33a150e7-write-e2e-approval-gate-20260619151922` 执行 `read_model_slo_smoke --critical-only --apply --target-ms 5000 --timeout-seconds 120`，15 个 critical scope 均达到 dirty/outbox `done`，readiness `fresh` 或 `dirty_done`，但 5 秒 SLO 失败 2 个：`invoice_lifecycle:2026-04` enqueue-to-fresh 约 5286.961ms，`cost_statistics:active:2026-04` 约 6459.019ms。随后只重跑失败 scope，`invoice_lifecycle` 约 2336.86ms 通过，`cost_statistics:active:2026-04` 仍约 7003.227ms 失败；公网 health ready probe 仍通过，`elapsed_ms=232.682`、`runtime_blocker_count=0`。根因调查定位到 `read_model.cost_statistics_rows` 仍逐行 insert/upsert；已在隔离 clean worktree 基于生产 commit `33a150e7` 提交 `3d88ce99 Optimize cost statistics read model row saves`，将成本统计 rows 保存改为 `_execute_many` 批量写并补 `tests/test_postgres_repositories_boundaries.py::test_cost_statistics_rows_are_saved_in_batch`。该修复通过 release `main-3d88ce99-coststats-batch-20260619170500` 发布并激活到生产；发布后 `health_ready_payload_probe` 通过，`runtime_release.consistent=true`、`runtime_blocker_count=0`，API/dispatcher/worker 工作目录指向新 release。发布后重新执行 critical apply，15/15 pass，summary p50 约 490.393ms、p95/max 约 3176.5ms，`cost_statistics:active:2026-04` 降至约 3176.5ms、`invoice_lifecycle:2026-04` 约 1400.792ms。
- 本轮 ETC tickets 权限 gate 回归命令 `cd web && npx playwright test e2e/etc-tickets-flow.spec.ts --project=chromium` 通过 1 test；证明新增 read-export gate 没有破坏 ETC OA 草稿创建和人工提交 full-access 主流程。
- 本轮 pending invoices 权限 gate 回归命令 `cd web && npx playwright test e2e/pending-invoices-attach-existing-flow.spec.ts e2e/pending-invoices-income-status-flow.spec.ts e2e/pending-invoices-rules-save-flow.spec.ts --project=chromium` 通过 5 tests；证明新增 read-export gate 没有破坏待找发票选择已有发票、收入状态批量更新和规则保存 full-access 主流程。
- 本轮权限 gate 回归命令 `cd web && npx playwright test e2e/oa-pending-payments-confirm-paid-flow.spec.ts e2e/oa-pending-payments-bank-link-flow.spec.ts e2e/batch-accounting-flow.spec.ts e2e/turnover-ledger-flow.spec.ts --project=chromium` 通过 8 tests；证明新增 read-export gate 没有破坏 OA pending、batch accounting、turnover 的 full-access 主写流程。
- 本轮待找发票 Spec-first covered 校准命令 `cd web && npx playwright test e2e/pending-invoices-fanout.spec.ts e2e/pending-invoices-filter-sort-flow.spec.ts e2e/pending-invoices-attach-existing-flow.spec.ts e2e/pending-invoices-income-status-flow.spec.ts e2e/pending-invoices-export-download.spec.ts e2e/pending-invoices-rules-save-flow.spec.ts e2e/workbench-relations-candidate-semantics.spec.ts e2e/workbench-relations-nonfresh-diagnostics.spec.ts --project=chromium` 通过 14 tests；覆盖页面 ready/文字选择、Workbench confirm fan-out、列筛选/排序、选择已有发票成功与 conflict、收入批量状态成功与失败、当前筛选导出和 row-limit、规则保存 freshness barrier、candidate 负面语义，以及 relation-backed refreshing/stale 诊断。
- 最新完整 `cd web && npm run e2e:smoke` 通过 151 tests / 151 passed；本轮目标命令 `cd web && npx playwright test e2e/cost-statistics-flow.spec.ts --project=chromium` 通过 5 tests，`cd web && npx playwright test e2e/imports-etc-invoices-flow.spec.ts --project=chromium` 通过 5 tests，旧 no-OA 单测命令通过 1 test，`cd web && npx playwright test e2e/turnover-ledger-flow.spec.ts --project=chromium` 通过 3 tests，`cd web && npx playwright test e2e/settings-data-reset-flow.spec.ts --project=chromium` 通过 2 tests，新增/校准成本统计 `read_export_only` time-view download event、请求不带分页、成本字段断言、ETC 导入后成本统计 fresh read model 证据、no-OA submit 后成本统计 fresh read model 证据、turnover stale grouped ledger 禁写、turnover 标签准入保存和 manual closure 后成本统计 fresh read model 证据、settings project scope active/all fresh 证据。完整 Browser smoke 覆盖清单以 `docs/dev/testing.md` 和各模块 `e2e-coverage.md` 为准。
- 本轮目标命令 `cd web && npx playwright test e2e/imports-invoices-flow.spec.ts --project=chromium` 通过 6 tests；新增发票导入慢预览期间预览/清空/确认动作锁定且只提交一次 preview、损坏文件混合上传 file-level error、未导入项明细、confirm 只提交正常文件 ID，以及销项收款/进项使用/税金抵扣/待找发票/OA 待付款/成本统计 downstream fresh read model 和导入影响行。`PYTHONPATH=backend/src python3 -m unittest tests.test_write_operation_slo_audit -v` 通过，新增/扩展导入类 write-operation profiles：`invoice_import_confirmed` 要求真实发票确认写链路产生 Workbench、Workbench relation、invoice lifecycle、search、待找发票、进项使用、销项收款、OA 待付款、成本统计和税金抵扣 read model refresh scopes；`bank_import_confirmed` 要求银行导入产生通用下游 refresh scopes、银行账户余额 `bank_account_balance.read_model.refresh`，并要求银行明细 `import.fact.changed` dirty scope drain；账户余额 API freshness gate 仍需页面或 staging smoke 观察；`etc_import_confirmed` 要求 ETC 导入产生 Workbench、Workbench relation、invoice lifecycle、tax offset refresh，并要求 Cost 由后续 `workbench_shard_published` 顺序收敛。
- 近期目标命令 `cd web && npx playwright test e2e/imports-bank-transactions-flow.spec.ts --project=chromium` 通过 7 tests；新增慢预览期间预览/清空/确认动作锁定且只提交一次 preview、银行账户冲突弹窗取消零 confirm、零 Workbench refresh、保留 preview，并证明用户随后仍可重新确认导入；同时新增损坏文件混合上传 file-level error、未导入项明细、confirm 只提交正常文件 ID、银行明细账户余额 fresh gate，以及成本统计 downstream fresh read model 导入流水证据。
- 近期目标命令 `cd web && npx playwright test e2e/finance-table-system-flow.spec.ts --project=chromium` 通过 1 test；`cd web && npm test -- --run src/test/InputInvoiceUsagePage.test.tsx src/test/InputInvoiceUsageFiltersAndDrawers.test.tsx` 通过 30 tests；`cd web && npx playwright test e2e/input-invoice-usage-flow.spec.ts --project=chromium` 通过 9 tests；`cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts --project=chromium` 通过 6 tests；`cd web && npx playwright test e2e/output-invoice-red-relation-fanout.spec.ts --project=chromium` 通过 1 test。
- 近期导入/待找/OA/税金验证中，`imports-etc-invoices-flow` 通过 5 tests，新增 ETC 票据/税金抵扣/成本统计 downstream fresh read model；`imports-invoices-flow` 通过 6 tests，`oa-pending-payments-nonfresh-flow` 通过 2 tests，`oa-pending-payments-bank-link-flow` 通过 2 tests，`oa-pending-payments-confirm-paid-flow` 通过 2 tests，`pending-invoices-*` 目标 specs 通过，`workbench-relations-oa-pending-fanout` 通过 1 test，`workbench-relations-tax-offset-fanout` 通过 1 test。
- 近期 tax-offset 目标验证中，`cd web && npm test -- --run src/test/TaxOffsetPage.test.tsx` 通过 19 tests；`cd web && npx playwright test e2e/tax-offset-flow.spec.ts --project=chromium` 通过 11 tests，新增 read-export/forbidden/expired/admin 权限细分、`refreshing` / `stale` / `missing` / `failed` read model 非 fresh Browser gate、防 false-empty、禁用保存计划、stale 自动恢复、plan save 409 conflict 防假成功，以及 390px 窄屏 81/92 行大表搜索/排序/筛选/共享横向滚动/按钮无遮挡/筛选弹层 viewport clamp。
- 近期 workbench-relations 目标验证中，`cd web && npx playwright test e2e/bank-details-export-download.spec.ts e2e/pending-invoices-export-download.spec.ts e2e/output-invoice-red-relation-fanout.spec.ts e2e/input-invoice-relation-fanout.spec.ts e2e/workbench-relations-oa-pending-fanout.spec.ts e2e/workbench-relations-tax-offset-fanout.spec.ts --project=chromium` 通过 7 tests；`PYTHONPATH=backend/src python3 -m pytest tests/test_search_pending_sql_runtime.py tests/test_workbench_relation_repository.py -q` 通过 60 tests，证明 relation fan-out 到 bank/pending/output/input/OA/tax/cost/search 和关键 relation 字段下载已覆盖。
- `bash scripts/verify.sh infra-smoke` 通过 74 tests、18 skipped，并因当前环境缺少 `FIN_OPS_TEST_DATABASE_URL` / `RABBITMQ_TEST_URL` 跳过真实连接；`infra-smoke` 已校准为 explicit apply gate：默认覆盖 read model SLO、runtime sync closure gate、write-operation SLO 和 RabbitMQ staging preflight 工具合同；`FIN_OPS_TEST_DATABASE_URL` 只追加 critical scope dry-run discovery，只有同时设置 `FIN_OPS_INFRA_SMOKE_APPLY=1` 才会 enqueue refresh events 并等待 worker drain；设置 `FIN_OPS_WRITE_OPERATION_AUDIT_OPERATIONS=bank_import_confirmed` 等 profile 后会运行只读 write-operation SLO audit，审计真实业务写入产生的 durable refresh events。
- `PYTHONPATH=backend/src python3 -m unittest tests.test_nightly_ci -v` 通过 5 tests；`PYTHONPATH=backend/src python3 -m pytest tests/test_search_pending_sql_runtime.py tests/test_workbench_relation_repository.py -q` 通过 60 tests；`bash scripts/verify.sh docs` 通过；`git diff --check` 通过。
- 此前 3 个完整 smoke 失败已按“加固测试和 mock，不改产品逻辑”执行第一轮修复：新增 route-level page-ready 诊断 helper，受影响 spec 改用统一页面就绪等待，并补齐 auto-tag E2E mock 的外部往来 canonical 字段；`bank-details-auto-tag-rules-flow.spec.ts`、`bank-details-export-download.spec.ts` 和 `output-invoice-red-relation-fanout.spec.ts` 均已在完整 smoke 中通过。
- 已新增或校准的 Browser 覆盖包括：search API/runtime group jump target、银行明细 relation 导出筛选/权限、待找发票 relation 导出/选择已有发票/收入批量状态/row-limit 导出失败/规则保存/列筛选排序、OA pending linked fan-out/confirm-paid/bank-link/non-fresh、银行流水导入慢预览动作锁定/preview stale/confirm 失败/账户冲突取消/损坏文件混合上传、银行明细账户余额 fresh gate 和成本统计 fresh read model、发票导入慢预览动作锁定/损坏文件混合上传/preview stale/confirm 失败/销项收款进项使用税金抵扣待找发票OA待付款成本统计 fresh read model、ETC 导入 preview stale/confirm 失败和 ETC 票据/税金抵扣/成本统计 fresh read model/ETC 成本行、no-OA 标签准入保存、no-OA submit 到成本统计 fresh read model/免 OA 成本行、turnover manual closure 到成本统计 fresh read model/外部往来闭环成本行、settings project scope 到成本统计 active/all fresh read model、销项收款列表/权限/stale/read model fan-out/红蓝票 relation 字段导出、进项发票使用列表/权限/non-fresh/detail/export、税金抵扣权限细分/非 fresh 防 false-empty/禁用保存计划/stale 自动恢复/plan save 409 conflict 防假成功、成本统计非 fresh 防 false-empty 和 time-view download event、销项红蓝票到税金抵扣/成本统计下游展示，以及 Finance Table System 共享宽表窄屏横向滚动。
- 本地 Playwright `test-results` 产物不作为闭环状态事实源。全局剩余风险仍按 `documented-risk` 处理：真实 GitHub Actions 启用状态、SSH/sudo/root-owned helper、systemd restart、PostgreSQL migration/PITR、Nginx live config、Redis/RabbitMQ 真连接、OA iframe cookie、真实浏览器缓存、外部 OA、真实生产数据、大数据 worker drain、真实 XLSX/ZIP 完整解析、没有独立 Browser route 的 search 外层 UI、更多撤销链路等跨页面链路。

## 2026-06-19 - 生产 authenticated runtime gate 取证与成本统计 500 hotfix

- 本轮生产 authenticated 取证使用现有目标 OA 申请人凭据临时登录，只在内存中作为 Bearer token 调用 GET/SSE，不输出 token、不落盘、不执行业务写操作。生产共有 2 个可用目标 OA 凭据；两者 `/api/session/me` 均 `200`，`access_tier=full_access`，`can_mutate_data=true`，但 `can_admin_access=false`，因此 admin-only `/api/operations/app-health-dashboard` 的 authenticated SLO 仍缺专用 admin 登录态，不能标记为 full gate closed。
- 生产 SSE authenticated smoke 已通过：`/api/app-health/stream` 与 `/api/workbench/events?month=all` 均返回首个预期 SSE event，`max_first_event_ms` 约 `481.936ms`。
- 生产 full authenticated HTTP probe 暴露真实缺陷：`/api/cost-statistics/explorer?month=2026-03&project_scope=active` 和 `/api/cost-statistics?month=2026-03&project_scope=active` 返回 `500 internal_server_error`；日志显示 `read_model.cost_statistics_read_models` 查询引用不存在的 `schema_version` 列。
- 根因：`0006_read_models.sql` 中 `read_model.cost_statistics_read_models` 没有顶层 `schema_version` 列，schema version 保存在 `payload` / `raw_payload`；现有本地测试模拟 row 时带了 `schema_version` 字段，没有覆盖真实表结构。
- 修复：基于生产 commit `3d88ce99` 创建干净 hotfix worktree 和分支 `codex/coststats-schema-hotfix`，提交 `bf02acc5 Fix cost statistics read model schema query`，从 `get_cost_statistics_view(...)` 的父表 select 中移除不存在的 `schema_version` 列，并新增 `tests/test_cost_statistics_sql_runtime.py::CostStatisticsSqlRuntimeTests::test_repository_reads_cost_statistics_schema_version_from_payload_not_table_column`。
- 验证：hotfix worktree 中 `PYTHONPATH=backend/src python3 -m pytest tests/test_cost_statistics_sql_runtime.py tests/test_postgres_repositories_boundaries.py -q` 通过 `38 passed`；`PYTHONPATH=backend/src python3 -m compileall backend/src/fin_ops_platform/services/postgres_repositories/read_models.py` 通过；`git diff --check` 通过；`VITE_APP_BASE_PATH=/fin-ops/ npm run build` 通过，保留既有 CSS minify warnings。
- 发布：release `main-bf02acc5-coststats-schema-20260619172500` 已激活；API、RabbitMQ dispatcher 和 20 个 worker WorkingDirectory 指向该 release；`health_ready_payload_probe` 通过，`status=pass`、`health_status=ready`、`elapsed_ms` 约 `90.4ms`、`runtime_blocker_count=0`。
- 发布后复验：`read_model_slo_smoke --apply --read-model-key output_invoice_collection --read-model-key cost_statistics --target-ms 5000` 通过，2/2 scopes done/fresh，`cost_statistics:active:2026-04` 约 `2843.542ms`，`output_invoice_collection:2026-01` 约 `242.92ms`；authenticated 成本统计两个原 500 endpoint 均变为 `200`。
- 仍未闭合：full authenticated HTTP gate 仍失败，原因包括缺少 admin 登录态导致 admin-only dashboard 403、`output-invoice-collections` 默认 `all` 读路径仍返回 `read_model_status=refreshing`，以及全量三轮采样中 Workbench/ETC 个别 p95 超过 1s。真实业务 write-operation apply 仍缺业务审批 ticket，不能自动执行。

## 完成审计记录

- 旧测试闭环已完成首轮，所有登记模块至少达到 `documented-risk`；这不代表每个页面/功能都已经完成 Spec-first E2E Audit。
- 当前没有旧测试闭环遗留的 P0 `needs-code-fix` 或 `needs-user-decision`。
- 本轮已完成 Browser e2e 基础设施、CI/verify 接入、app-shell/admin 权限 smoke、多条关联台 relation 写入/撤回/候选/异常/权限/网络恢复 smoke，以及银行明细、待找发票、批量账务、往来款、销项收款、销项红蓝票到税金/成本和进项发票使用的关键 downstream fan-out；银行明细还新增了默认首屏、fresh 空态、transaction/account read model refreshing/stale/schema_mismatch/missing 的诊断、false-empty、account retry、network recovery、导出业务错误、账户/自定义日期/关键字/分类筛选导出、分页状态不限制导出、长列表/宽字段/窄屏/菜单遮挡、`read_export_only` 零 mutation、forbidden/expired session gate 零 protected API、admin 分类写入、自动标签规则保存/reapply freshness、候选确认/撤销和外部往来三层人工补分类/清除保护；销项收款已补列表 search/filter/sort/page-size、`read_export_only` 写入口零 mutation和红蓝票 relation 字段 export-preview/download，进项发票使用已补 fresh rows/filter/sort/page-size、rows read model false-empty 防护、relation detail 非 fresh 长期 loading 防护、fresh `+N` detail 展开和当前筛选导出/download/row-limit/export non-fresh 禁用下载；免 OA 已补 no-OA read model stale -> fresh 期间保持可见 rows、不显示普通空态并自动重读的 Browser 保护；批量账务已补 relation read model stale 时保留当前银行/OA rows、显示 warning/reason/scope、防普通空态且零 mutation 的 Browser 保护；外部往来已补 turnover grouped ledger stale 时保留 rows、显示 warning、选中真实 flow rows 后仍禁用确认闭环且零 confirm mutation 的 Browser 保护。这不代表所有页面/功能的 Playwright 业务流已完整覆盖。
- Spec-first E2E Audit 已建立全局规则和 inventory，并完成 17 个页面模块的页面级 `spec-first-covered` 闭环：`reconciliation-workbench`、`workbench-relations`、`bank-details`、`input-invoice-usage`、`output-invoice-collections`、`oa-pending-payments`、`tax-offset`、`cost-statistics`、`imports-bank-transactions`、`imports-invoices`、`imports-etc-invoices`、`pending-invoices`、`no-oa-bank-batches`、`turnover-ledger`、`batch-accounting`、`settings`、`etc-tickets` 与 `app-health-operations`。本轮补齐 `read-models`、`runtime-workers`、`domain-events-lifecycle`、`oa-integration`、`data-safety-reset`、`deploy` 的 `e2e-spec.md` / `e2e-coverage.md`，使 27 个登记模块都有 Spec-first E2E 文档入口；这些 shared/runtime 模块仍按 partial/external-risk gate 管理。
- 下一轮按 Spec-first 队列推进：页面模块已完成页面级 covered，下一阶段应转向 shared/cross-page partial 项和真实 staging/runtime smoke；真实基础设施 worker drain、真实 zip/对象存储/OA 草稿/真实超大文件/信息汇总表样本/账户余额 API fresh gate/search 外层 UI 继续作为 staging/runtime smoke。每轮结束都必须更新本状态文件、对应模块 `tests.md`、`e2e-spec.md` 和 `e2e-coverage.md`。
## 2026-06-19 - 生产 output invoice all-scope freshness hotfix

- 本轮 authenticated HTTP gate 继续追踪 `output-invoice-collections` 默认 all 读路径长期 `202 refreshing`。生产只读 DB 证据显示 `job.read_model_dirty_scopes` 中 `output_invoice_collection` 无 pending/processing/failed，`job.outbox_events` 对应 refresh 均为 done，2026-01..2026-06 月度 scope 均为 fresh；但 authenticated HTTP `/api/output-invoice-collections/rows?page=1&page_size=20` 返回 `202 refreshing`，其中一组登录态暴露 `read_model_stale_reasons=["oa_projection_sync_version_missing"]`。
- 根因：`PostgresReadModelRepository._invoice_relation_scope_row(scope_key="all")` 聚合所有月度 scope 的共同 source versions 时，把历史 `row_count=0` 的 2025 空 scope 也纳入计算。这些空 scope 保存的是旧 `oa_projection_sync_version`，导致 all 聚合删除共同版本字段，API 反复认为 all stale；worker fan-out 又只刷新当前有 shard 的月份，因此形成 default all 永久 refreshing。
- 修复：隔离 hotfix branch `codex/output-invoice-all-scope-hotfix` 提交 `9e9546ac Fix output invoice all-scope freshness`。当 all scope 存在非空月份时，source version/cache status 聚合只使用 `row_count > 0` 的 scope rows；若没有非空 scope，则保持原 all-empty 行为。
- 自动化回归：新增 `tests.test_invoice_usage_collection_sql_runtime.InvoiceUsageCollectionSqlRuntimeTests.test_output_api_all_scope_ignores_stale_empty_month_scope_versions`，先 RED 证明旧逻辑返回 202，再 GREEN 证明默认 all rows 返回 `200 fresh`、不带 stale reasons、不 enqueue `api_source_versions_stale`。
- 验证命令：`PYTHONPATH=backend/src python3 -m unittest tests.test_invoice_usage_collection_sql_runtime -v` 通过 24 tests；`PYTHONPATH=backend/src python3 -m compileall backend/src/fin_ops_platform/services/postgres_repositories/read_models.py` 通过；`git diff --check` 通过；`VITE_APP_BASE_PATH=/fin-ops/ npm run build` 通过，保留既有 CSS minify warnings。
- 发布与生产复验：release `main-9e9546ac-output-invoice-all-scope-20260619173552` 已激活，`health_ready_payload_probe` 通过，`elapsed_ms=197.298`、`runtime_blocker_count=0`、`runtime_release.consistent=true`；生产只读 DB 复查 `output_invoice_collection` current dirty scope 和非 done outbox 均为空；两个目标 OA 登录态下 `/api/output-invoice-collections/rows?page=1&page_size=20` 和 `/api/output-invoice-collections/filter-options` 均返回 `200 fresh`，rows total=22，stale reasons 为空。
- 修复后 authenticated HTTP SLO probe（API only，2 measured iterations + 1 warmup）仍为 fail，但 output invoice 已不在失败项中。剩余失败为：`operations_app_health_dashboard` 返回 403（现有目标 OA 登录态非 admin）、`workbench_groups_all_paired` p95 约 2448.69ms、`cost_statistics_explorer_all` p95 约 1144.864ms、`etc_reconciliation_tasks` p95 约 1278.426ms；这些是下一轮 runtime/performance/auth gate 的剩余缺口。
- 仍未闭合：admin-only dashboard authenticated gate 仍缺 admin 登录态；真实业务 write-operation apply 仍需要审批 ticket；full authenticated HTTP gate 还剩 Workbench、成本统计和 ETC 的 p95 超时需要继续优化或分层定标。

## 2026-06-19 - 生产 authenticated API SLO 复跑

- 本轮继续使用现有目标 OA 申请人凭据临时登录，只在内存中作为 Bearer token 调用只读 GET；不输出 token、不落盘、不执行业务写操作。
- Workbench 单项复验：正式 `http_slo_probe.collect_http_slo` 对 `/api/workbench/groups?month=all&zone=paired&page=1&page_size=50` 运行 `1` 次 warmup + `8` 次 measured，全部 `200 fresh`，p95 `274.362ms`，`response_bytes=1925996`。这说明上一轮记录的 `workbench_groups_all_paired` p95 `2448.69ms` 不是当前稳定可复现状态；当前无需改业务代码或 repository 查询。
- 全量 API-only authenticated SLO 复跑：默认 `38` 个 API probes，`1` 次 warmup + `3` 次 measured，总 `114` 个 measured samples，`max_p95_ms=710.177`，没有 `>800ms` 慢项；Workbench、成本统计和 ETC 均已通过 `<=1000ms` 目标。
- 当前唯一失败项为 `operations_app_health_dashboard`：`/api/operations/app-health-dashboard` 三次均返回 `403`，p95 约 `23.559ms`。只读认证检查显示生产有 `2` 个目标 OA 凭据，均为 `access_tier=full_access`、`can_mutate_data=true`、`can_admin_access=false`；环境存在 `FIN_OPS_ADMIN_USERNAMES` 但未配置 `FIN_OPS_HTTP_SLO_ADMIN_TOKEN` 或 cookie。因此这是 admin-only gate 缺少专用 admin 登录态，不是性能问题，也不应通过放宽 dashboard 权限修复。
- 生产 read model / worker 只读巡检：release `main-9e9546ac-output-invoice-all-scope-20260619173552` 当前为 API WorkingDirectory；`fin-ops.service`、RabbitMQ dispatcher 和 20 个 worker service 共 `22` 个 fin-ops systemd service 处于 running；`job.read_model_dirty_scopes` 非 done 为空，`job.outbox_events` 非 done 为空，recent failed/dead-letter/publish-failed outbox 为空；`read_model.app_status_readiness` 中 `bank_account_balance`、`bank_detail`、`cost_statistics`、`input_invoice_usage`、`invoice_lifecycle`、`no_oa_bank_batch`、`oa_pending_payment`、`output_invoice_collection`、`pending_invoice`、`search`、`tax_offset`、`turnover_ledger`、`workbench`、`workbench_relation` 均只有 `fresh`。heartbeat 表仍保留历史旧 worker id 的 stale/stopped 行，但当前 running worker kinds 均有新鲜 heartbeat；`health_ready_payload_probe --base-url http://127.0.0.1:18001 --target-ms 1000` 返回 `status=pass`、`elapsed_ms=82.849`、`runtime_blocker_count=0`、`runtime_release.consistent=true`。
- 工具闭环补强：`http_slo_probe` 已支持 `HttpProbe.auth_scope` 和 `admin_headers`，默认 `operations_app_health_dashboard` 使用 admin auth scope；`runtime_sync_closure_gate` 会把普通 bearer headers 和 admin headers 分别传给 HTTP SLO probe。新增/更新测试已证明普通 API probe 不会被 admin token 覆盖，admin-only dashboard 可在提供 `FIN_OPS_HTTP_SLO_ADMIN_TOKEN` 后同轮验证。
- 仍未闭合：admin-only dashboard authenticated gate 需要真实 admin token/cookie 或目标 OA admin 凭据；真实业务 write-operation apply 仍需要审批 ticket。当前生产 API-only 性能/read-model freshness/worker runtime gate 除 admin-only 凭证缺口外已恢复通过。

## 2026-06-19 - admin-scoped HTTP SLO probe hotfix 发布

- 目标：把上一轮已实现的 admin-scoped HTTP SLO probe 工具补强发布到生产，避免 `/api/operations/app-health-dashboard` 用普通目标 OA bearer 采样时固定 403，并让无人值守 closure gate 能在同一轮中区分普通 API 登录态和 admin dashboard 登录态。
- 修复：隔离 hotfix branch `codex/http-slo-admin-scope-hotfix` 基于生产 commit `9e9546ac` 提交 `8b5942e4 Support admin-scoped HTTP SLO probes`；`HttpProbe` 增加 `auth_scope`，默认 `operations_app_health_dashboard` 标记为 `admin`；`collect_http_slo(...)` 和 `runtime_sync_closure_gate` 分别传递 user/admin headers；CLI 支持 `FIN_OPS_HTTP_SLO_ADMIN_TOKEN` / `--admin-token`，且普通 bearer 不会被 admin token 覆盖。
- 本地验证：hotfix worktree 复跑 `PYTHONPATH=backend/src python3 -m unittest tests.test_http_slo_probe tests.test_runtime_sync_closure_gate tests.test_slo_tool_defaults -v`，31 tests 通过；`VITE_APP_BASE_PATH=/fin-ops/ npm run build` 通过，保留既有 CSS minify warnings；deploy dry-run 通过。
- 发布：release `main-8b5942e4-http-slo-admin-scope-202606191805` 已激活；发布脚本完成 backend readiness、deploy-control status、runtime worker ensure、frontend hash check、public session route check 和 old release cleanup。
- 发布后只读验证：API WorkingDirectory 为 `/opt/fin-ops/releases/main-8b5942e4-http-slo-admin-scope-202606191805/src`，`fin-ops.service` active，RabbitMQ dispatcher active，20 个 `fin-ops-worker@*.service` running；`/health/ready` 返回 ready；PostgreSQL 权威表显示 `job.outbox_events` 非 done 为空、`job.read_model_dirty_scopes` 非 done 为空、`read_model.app_status_readiness` 非 fresh 为空，近 30 分钟 failed/dead-letter/publish-failed outbox 为空。
- 发布后代码验证：生产 release 中 `backend/src/fin_ops_platform/tools/http_slo_probe.py` 和 `runtime_sync_closure_gate.py` 通过 `py_compile`；生产 release import 断言 `operations_app_health_dashboard_auth_scope=admin`。生产发布包不包含 `tests/` 目录，因此远端 unittest 不作为发布后验证入口。
- 仍未闭合：生产环境仍未配置真实 `FIN_OPS_HTTP_SLO_ADMIN_TOKEN` / admin cookie 或目标 OA admin 凭据，所以 full authenticated HTTP gate 还不能证明 admin dashboard 200；真实业务 write-operation apply 仍需要审批 ticket，不能由无人值守流程直接执行 mutating smoke。

## 2026-06-19 - 生产只读 runtime gate 复查

- 本轮只读取生产证据，不执行部署、restart、数据库写入、read model `--apply` 或 mutating HTTP。公网 `health_ready_payload_probe --base-url https://www.yn-sourcing.com --api-prefix /fin-ops-api --target-ms 1000 --json` 通过，`elapsed_ms=144.671`、`runtime_blocker_count=0`，release `main-8b5942e4-http-slo-admin-scope-202606191805` 且 `runtime_release.consistent=true`。
- SSH 只读检查显示 `fin-ops.service`、RabbitMQ dispatcher 和 20 个 `fin-ops-worker@*.service` 全部 `active/running`；API、dispatcher 和 worker 的 systemd WorkingDirectory 均为 `/opt/fin-ops/releases/main-8b5942e4-http-slo-admin-scope-202606191805/src`。
- 生产本机加载 systemd env 后，`FIN_OPS_POSTGRES_DATABASE_URL` 已配置，`FIN_OPS_HTTP_SLO_ADMIN_TOKEN`、`FIN_OPS_HTTP_SLO_BEARER_TOKEN`、`FIN_OPS_HTTP_SLO_COOKIE` 和 `FIN_OPS_WRITE_E2E_APPROVAL_TICKET` 均未配置；使用生产 venv `/opt/fin-ops/venv/bin/python` 运行 `read_model_slo_smoke --critical-only --target-ms 5000 --json` 返回 `status=dry_run`、`planned_scope_count=15`，覆盖 workbench、workbench_relation、bank_detail、bank_account_balance、pending_invoice、search、invoice_lifecycle、input_invoice_usage、output_invoice_collection、oa_pending_payment、cost_statistics、tax_offset、no_oa_bank_batch、turnover_ledger 等 critical scopes。
- 生产 PostgreSQL 权威表只读汇总显示 `job.outbox_events=[["done", 157060]]`、`job.read_model_dirty_scopes=[["done", 143020]]`、`read_model.app_status_readiness=[["fresh", 169]]`，说明当前持久化队列和 app status readiness 没有非 done / 非 fresh 残留。
- 公网 `runtime_sync_closure_gate --base-url https://www.yn-sourcing.com --api-prefix /fin-ops-api --allow-unauthenticated-http --health-ready-target-ms 1000 --json` 中 `runtime_health` 通过，`health_ready_payload` 通过；full closure gate 仍为 fail，且失败项是预期未闭合条件：`read_model_direct_smoke` 未 apply，只能说明 dry-run 规划 15 scopes；缺 bearer/admin token 导致 authenticated HTTP/SSE 未闭合；`write_operation_audit` 有 447 个事件样本但 56/56 expectations 未达标或缺样本；`write_operation_e2e` 缺 `--write-scenario`、`--apply-write-scenarios` 和 `--write-approval-ticket`。这些不能被当作 runtime 当前阻塞，也不能被伪装为 complete。
- 下一步：要继续缩小外部缺口，需要安全提供 admin token/cookie 跑 authenticated HTTP/SSE gate；在批准窗口显式执行 `read_model_slo_smoke --critical-only --apply` 证明 direct enqueue-to-fresh；并提供业务审批 ticket、真实认证和安全 scenario 才能执行 write-operation E2E apply。

## 2026-06-19 - 生产 critical read model apply 复验

- 本轮在同一 release `main-8b5942e4-http-slo-admin-scope-202606191805` 上执行公网 `runtime_sync_closure_gate --apply-read-model-smoke`，该命令会 enqueue critical read model refresh 并等待 worker 收敛；未传 `--apply-write-scenarios`、`--write-scenario` 或 approval ticket，因此没有执行业务写接口。
- 首轮 full gate 中 `runtime_health` 与 `health_ready_payload` 通过，`read_model_direct_smoke` 最终 done/fresh 但 2 个 scope 超过 5 秒目标：`invoice_lifecycle:2026-04` 约 `7116.852ms`、`cost_statistics:active:2026-04` 约 `7273.758ms`。DB 汇总仍为 `job.outbox_events=[["done", 157081]]`、`job.read_model_dirty_scopes=[["done", 143040]]`、`read_model.app_status_readiness=[["fresh", 169]]`。
- 聚焦复验只跑 `invoice_lifecycle=2026-04` 和 `cost_statistics=active:2026-04`，2/2 pass：`invoice_lifecycle` 约 `1221.935ms`，`cost_statistics` 约 `3056.738ms`。
- 随后完整 `read_model_slo_smoke --apply --critical-only --target-ms 5000 --timeout-seconds 120` 复跑 15/15 pass，summary p50 约 `580.34ms`，p95/max 约 `3863.253ms`，handler p95/max 约 `3535.364ms`。复跑后 DB 汇总为 `job.outbox_events=[["done", 157126]]`、`job.read_model_dirty_scopes=[["done", 143083]]`、`read_model.app_status_readiness=[["fresh", 169]]`。
- 当前 release 的 direct critical read model enqueue-to-fresh 证据已闭合；full closure gate 仍未完成，剩余原因是 authenticated HTTP user-level 性能慢项、缺 admin token 的 admin-only dashboard，以及缺 approval ticket/安全 scenario 的真实 write-operation E2E。

## 2026-06-19 - 当前 release authenticated user HTTP/SSE gate 复验

- 本轮使用生产已配置目标 OA 申请人凭据临时登录 OA 获取 bearer token；token 只在远端 Python 进程内存中使用，不输出、不落盘、不通过命令行参数传递，不执行业务写接口。生产当前有 2 个 configured target OA credentials；两者 `/api/session/me` 均为 `200`，`access_tier=full_access`、`can_access_app=true`、`can_mutate_data=true`、`can_admin_access=false`；没有可用 admin session。
- User-scope authenticated API probe 排除 admin-only `operations_app_health_dashboard` 后运行 37 个 API probes、111 个 measured samples，所有失败项均为 `200` 或 `200 fresh` 但超过 1000ms，没有 401/403/500/refreshing/false-empty。首轮失败 5 项：`workbench_groups_all_paired` p95 约 `2566.521ms`、`bank_details_auto_tag_rules` p95 约 `1141.526ms`、`cost_statistics_explorer_all` p95 约 `1366.847ms`、`turnover_ledger_grouped` p95 约 `1119.901ms`、`etc_reconciliation_tasks` p95 约 `1442.455ms`。
- 对 5 个慢项执行 focused probe，每项 2 warmup + 8 measured；`bank_details_auto_tag_rules` 复验通过，p95 约 `120.949ms`。仍失败 4 项：`workbench_groups_all_paired` 8/8 `200 fresh`、p95 约 `2680.029ms`；`cost_statistics_explorer_all` 8/8 `200 fresh`、p95 约 `1160.09ms`；`turnover_ledger_grouped` 8/8 `200`、p95 约 `1181.292ms`；`etc_reconciliation_tasks` 8/8 `200`、p95 约 `1396.46ms`。
- Authenticated SSE first-event gate 已闭合：`/api/app-health/stream` 返回 `app_health`，first event 约 `350.665ms`；`/api/workbench/events?month=all` 返回 `workbench.read_model.completed`，first event 约 `217.741ms`；2/2 pass。
- 当前结论：user-level 认证、session 和 SSE 已闭合；user-level HTTP gate 仍为 performance partial，剩余 4 个慢项需要后续优化、分层目标或运行窗口复验。admin-only dashboard 仍缺真实 admin token/cookie；mutating write-operation E2E 仍缺 approval ticket 和安全 scenario。

## 2026-06-19 - gzip-aware HTTP SLO probe 发布与关键慢项复验

- 根因修正：上一轮公网 user-level HTTP 慢项包含明显传输口径偏差。Nginx 生产已启用 gzip，真实浏览器会请求压缩响应；旧 `http_slo_probe` 未发送 `Accept-Encoding: gzip`，导致 Workbench、成本统计、往来款和 ETC 等大 JSON 按未压缩传输耗时统计，不能代表浏览器首屏。
- 修复：隔离 hotfix branch `codex/http-slo-gzip-probe` 基于生产 commit `8b5942e4` 提交 `3546e985 Make HTTP SLO probe gzip-aware`；`http_slo_probe` 默认发送 `Accept-Encoding: gzip`，gzip JSON/HTML body 先解压再提取 read model/cache metadata 和 HTML fallback，`response_bytes` 保留压缩后的网络传输字节数。
- 本地验证：hotfix worktree 和主工作树均通过 `PYTHONPATH=backend/src python3 -m unittest tests.test_http_slo_probe tests.test_runtime_sync_closure_gate tests.test_slo_tool_defaults -v`，共 32 tests；`python3 -m py_compile backend/src/fin_ops_platform/tools/http_slo_probe.py tests/test_http_slo_probe.py` 通过。
- 发布：release `codex-http-slo-gzip-probe-3546e985-20260619210708` 已激活；`fin-ops.service`、`fin-ops-rabbitmq-dispatcher.service`、`rabbitmq-server.service` 均 active，20 个 `fin-ops-worker@*.service` active；生产 release import 断言 `_auth_headers().Accept-Encoding=gzip`。
- 发布后关键复验：使用生产目标 OA 凭据只在远端内存中登录为 user-scope bearer，不输出 token、不落盘、不执行业务写接口；session 为 `access_tier=full_access`、`can_mutate_data=true`、`can_admin_access=false`。先对 `session_me`、`workbench_groups_all_paired`、`cost_statistics_explorer_all`、`turnover_ledger_grouped`、`etc_reconciliation_tasks` 执行 1 warmup + 2 measured 的关键 probe，`status=pass`、`failure_count=0`、`probe_count=5`、`sample_count=10`、`max_p95_ms=190.321`。
- 发布后完整 user-scope 复验：排除 admin-only `operations_app_health_dashboard` 后，gzip-aware authenticated HTTP probe 跑完 37 个 API probes、111 个 measured samples，`status=pass`、`failure_count=0`、`max_p95_ms=735.265`；authenticated SSE 2/2 pass，`max_first_event_ms=343.0`。
- 当前结论：上一轮 user-level HTTP performance partial 的核心慢项已被证明主要是未压缩探针口径导致的误报；user-scope 完整 API matrix 和 SSE 在 gzip 浏览器口径下通过。full authenticated HTTP gate 仍不能标记 complete，因为 admin-only dashboard 仍缺真实 admin token/cookie。
- 仍未闭合：真实业务 write-operation apply 仍需要业务审批 ticket、安全 scenario 和真实认证；admin dashboard authenticated gate 仍需要 admin 登录态；生产级 Browser Playwright smoke 仍需单独覆盖真实页面、下载、弹窗、iframe/SSE 和隐藏浏览器错误。

## 2026-06-19 - 生产 user-scope Browser route-shell smoke

- 本轮使用生产目标 OA 申请人凭据在远端内存中临时登录，拿到 full-access user bearer 后只作为本地 Playwright `Admin-Token` cookie 注入；token 不输出、不落盘。Browser smoke 只打开路由，不点击业务写入口；任何 `POST`/`PUT`/`PATCH`/`DELETE` 请求都视为失败。
- 生产真实 Chromium 打开 16 个核心路由：`/fin-ops/`、银行明细、待找发票、进项使用、OA 待付款、销项收款、税金抵扣、成本统计、免 OA、批量账务、往来款、ETC、银行流水导入、发票导入、ETC 导入和设置。一次性 smoke 结果 `status=pass`、`page_count=16`、`failed_page_count=0`、`diagnostic_count=0`、`mutating_request_count=0`、`max_elapsed_ms=1908`；随后固化为 `web/e2e/production-route-shell.spec.ts` / `npm run e2e:production-shell`，默认 skip、需要显式 `FIN_OPS_E2E_PRODUCTION_SMOKE=1` 和 `FIN_OPS_E2E_OA_TOKEN`，并关闭 trace/screenshot/video。使用同类真实临时 user token 复跑新增 npm gate 通过 `1 passed`，耗时约 26.3s。
- 该证据证明真实域名、真实 full-access user cookie、SPA route shell 和浏览器错误通道基本可用：没有 session gate、没有长时间停在“正在加载页面”、没有 `console.error` / `pageerror` / 原生 dialog / 非预期 requestfailed，也没有意外 mutating HTTP。
- 当前结论：生产 user-scope route-shell smoke 已闭合一层真实浏览器风险，但它不是页面功能深测；不能替代每个页面的弹窗、下载、iframe、焦点、滚动、大表格、网络恢复、写后 read model/worker 收敛和跨页面 fan-out Playwright。
- 仍未闭合：admin-only dashboard Browser smoke 仍缺 admin cookie/token；真实业务 write-operation apply 仍需要审批 ticket、安全 scenario 和真实认证。

## 2026-06-19 - Playwright 严格浏览器诊断统一入口

- 本轮将所有 `web/e2e/*.spec.ts` 的 Playwright 导入统一改为 `web/e2e/fixtures/strictTest.ts`，该 auto fixture 默认捕获 app 主动打印的 `console.error`、`pageerror`、非预期 `requestfailed` 和原生 `dialog`，让“业务操作成功但浏览器报错/弹窗报错/请求异常”的隐藏问题变成 E2E 失败。浏览器自动生成的 HTTP status resource log 只代表 API response 状态，不作为隐藏 JS 错误；对应 spec 仍必须用页面 alert、禁用下载、零 mutation、零半写等业务断言证明 400/401/403/409/500/503 已被正确消费。SPA 路由切换产生的 `net::ERR_ABORTED` 生命周期中断也不作为隐藏错误。
- 新增 `tests/test_playwright_e2e_strict_diagnostics.py`，静态校验所有 `web/e2e/*.spec.ts` 不得直接从 `@playwright/test` 导入，确认 strict fixture 覆盖 console/pageerror/requestfailed/dialog 四类通道、测试结束断言，并防止把 HTTP status resource log 重新误判为隐藏错误。
- 验证：`PYTHONPATH=backend/src python3 -m unittest tests.test_playwright_e2e_strict_diagnostics -v` 通过 3 tests；`cd web && npx playwright test e2e/bank-details-stale-refreshing.spec.ts e2e/cost-statistics-flow.spec.ts e2e/input-invoice-usage-flow.spec.ts e2e/output-invoice-collections-flow.spec.ts e2e/pending-invoices-export-download.spec.ts e2e/workbench-network-recovery-flow.spec.ts --project=chromium` 通过 36 tests；后续最新补充复跑 `cd web && npm run e2e:smoke` 通过 151 tests / 151 passed。
- 组件测试闭环修复：本轮前端全量 Vitest 原失败 29 tests 已修复。`BatchAccountingPage.test.tsx` 补 session context wrapper；`WorkbenchZone.test.tsx` mock row 补 `availableActions`；`WorkbenchColumns.test.tsx` 断言更新为当前默认 action column / 更多菜单 / 忽略 action 合同。验证：`cd web && npm test -- --run src/test/BatchAccountingPage.test.tsx src/test/WorkbenchColumns.test.tsx src/test/WorkbenchZone.test.tsx` 通过 56 tests；`cd web && npm test -- --run --passWithNoTests` 通过 69 files / 752 tests；`cd web && npm run build` 通过，保留既有 CSS minify warnings。
## 2026-06-20 - Production read model / worker direct drain current status

- 本轮在当前 active release `codex-http-slo-gzip-probe-3546e985-20260619210708` 上继续推进唯一 production 环境的 read model / worker 闭环。执行前先做只读巡检：API、RabbitMQ dispatcher 和 20 个 worker unit 均 active；本机 `/health` 与 `/health/ready` 返回 ready；scope contract `ok=true`、`violation_count=0`；生产 PostgreSQL 聚合显示 outbox/dirty/readiness 无非 done / 非 fresh 残留。
- 本地 `bash scripts/verify.sh infra-smoke` 通过 77 tests、18 skipped；跳过项均为未配置真实外部依赖、auth token、RabbitMQ 或写场景的预期外部 gate。生产 `read-model-slo-smoke --critical-only` dry-run 返回 15 个 planned scopes、`missing_read_model_keys=[]`。
- 经显式批准后，生产 direct `read_model_slo_smoke --critical-only --apply --target-ms 5000 --timeout-seconds 90` 已实际 enqueue 15 个 critical refresh 并等待 worker drain；首轮 15/15 均达到 outbox/dirty `done` 且 readiness `fresh` 或 `dirty_done`，但 5 个 scope 超过 5 秒 target：`bank_detail:2026-01` 约 5.80s、`invoice_lifecycle:2026-04` 约 9.11s、`input_invoice_usage:2026-06` 约 5.69s、`cost_statistics:active:2026-04` 约 7.45s、`turnover_ledger:all` 约 6.48s。
- 聚焦复验后，5 个慢 scope 中 4 个通过，只剩 `invoice_lifecycle:2026-04` 约 5.80s；随后单项 `invoice_lifecycle:2026-04` 复验通过，约 1.28s。最终完整 critical apply 复跑 15/15 pass，summary p50 约 693.739ms、p95/max 约 4122.628ms，handler p95/max 约 3963.749ms。
- 最终复核聚合显示 `job.outbox_events=done 157193`、`job.read_model_dirty_scopes=done 143148`、`read_model.app_status_readiness=fresh 169`，本轮 retry/full-rerun dirty scopes 全部 `done`。因此当前 release 的 direct critical read model / worker drain 已闭合；首轮长尾作为后续性能观察项保留。
- `runtime_sync_closure_gate` 只读复核中 `runtime_health` 与 `health_ready_payload` 通过；authenticated HTTP/SSE、write-operation audit 和 write-operation E2E 仍因缺少真实 auth、admin token、业务写 scenario 和 approval ticket 保持 fail/input_required。该失败不能通过放宽权限、伪造 fresh 或无审批执行业务写操作解决。

## 2026-06-20 - Production write-operation E2E scenario discovery dry-run

- 本轮按 production 安全边界只执行只读 discovery 和 dry-run，不执行 `--apply`，不调用任何 mutating HTTP，不写业务数据，不 enqueue read model refresh。
- Token 状态：当前本地 shell 和生产 runtime env 均未检测到 `FIN_OPS_HTTP_SLO_ADMIN_TOKEN`、`FIN_OPS_E2E_ADMIN_TOKEN`、`FIN_OPS_HTTP_SLO_BEARER_TOKEN`、`FIN_OPS_HTTP_SLO_COOKIE` 或 `FIN_OPS_E2E_OA_TOKEN`；因此本轮未能验证 `/api/session/me` 或 admin dashboard token 权限。
- 只读 discovery：生产 `write_operation_scenario_discovery --limit 20` 返回 `status=ready`，候选计数为 `turnover_manual_closure_or_withdraw=20`、`workbench_pair_withdraw_context=20`、`no_oa_bank_batch_withdraw_context=20`，并写出 scenario 文件 `/tmp/finops-write-e2e-scenarios-20260620.json`。
- 最小候选：从 turnover `suggested` relation 中选择 `turnover-withdraw-turnover_rel_05cac958eb8c7c74` 生成 `/tmp/finops-write-e2e-scenarios-20260620-minimal-turnover.json`。计划写入口为 `POST /api/turnover-ledger/relations/turnover_rel_05cac958eb8c7c74/withdraw`；后置只读 probes 为 `/api/turnover-ledger?view=grouped&page=1&page_size=50` 和 `/api/operations/app-health-dashboard`。
- Dry-run 结果：`write_operation_e2e_smoke --scenario /tmp/finops-write-e2e-scenarios-20260620-minimal-turnover.json --base-url https://www.yn-sourcing.com --api-prefix /fin-ops-api --json` 返回 `status=dry_run`、`scenario_count=1`、`auth_configured=false`、`approval_configured=false`；没有执行写操作。
- 下一步要求：真实 apply 前必须重新提供可用认证 token，并明确批准该具体 scenario 与 approval ticket；否则保持 dry-run，不允许 production mutation。

## 2026-06-20 - Production write-operation E2E apply rejected without mutation

- 用户明确批准对 `turnover-withdraw-turnover_rel_05cac958eb8c7c74` 执行单条 production Write Operation E2E apply，approval reference 为 `FINOPS-PROD-WRITE-SMOKE-20260620-TURNOVER-001`；执行前只读快照显示目标 relation 为 `suggested`、version `1`、counterparty `合肥钩知专利代理事务所（特殊普通合伙）`、bank row `txn_imported_1245`、金额 `2925.00`。
- 第一次 token 传递未可靠到达远端命令，未生成 apply 输出文件，复核显示 relation 仍为 `suggested`、无新增 outbox/dirty/readiness 变化。
- 第二次通过交互式 SSH 隐藏 token 执行同一条 scenario；`write_operation_e2e_smoke --apply` 返回 `status=fail`、`scenario_count=1`、`failed_scenario_count=1`，写步骤 `POST /api/turnover-ledger/relations/turnover_rel_05cac958eb8c7c74/withdraw` 返回 `400`，post API probes 和 write SLO 均按 `write_step_failed` 跳过，没有执行其它 scenario。
- 执行后只读复核显示目标 relation 仍为 `suggested`、version `1`、无 `app.turnover_relation_events` 行、`job.outbox_events` 全部 `done`、`job.read_model_dirty_scopes` 全部 `done`、`read_model.app_status_readiness` 全部 `fresh`，且 apply 时间窗口内无新增 outbox/dirty。结论：本次真实写接口被业务校验拒绝，没有造成数据库业务变更，因此不需要回滚。
- 修复：隔离 hotfix branch `codex/write-scenario-discovery-hotfix` 基于生产 commit `3546e985` 提交 `21a734b0 Fix write operation scenario discovery source filter`，让 `write_operation_scenario_discovery` 对齐 production withdraw request boundary：只选择 `source=manual` 的 turnover relation，并正确读取 `raw_payload.normalized_payload.source`；不再用旧的 `source <> system` 逻辑，避免 nested `source=system` 的 `suggested` relation 被误选为可 apply 候选。`tests/test_write_operation_scenario_discovery.py` 新增回归锁定该 SQL 合同。
- 发布与复验：release `codex-write-scenario-discovery-hotfix-21a734b0-20260620` 已激活；`fin-ops.service`、RabbitMQ dispatcher 和 20 个 `fin-ops-worker@*.service` 均 active。生产只读重跑 `write_operation_scenario_discovery --limit 20` 返回 `status=ready`，候选计数为 turnover 5、Workbench 20、no-OA 20；turnover 候选 `turnover_sources=["manual"]`、`non_manual_turnover_candidates=0`。本轮发布后没有执行新的 `--apply`，没有业务写入。
- 后续要求：下一轮 production apply 必须基于新 discovery 输出重新让用户批准具体 scenario 和 approval ticket，不得沿用已被 400 拒绝的旧 relation，也不得自动换用其它 relation apply。

## 2026-06-20 - Production 贾小花 turnover scenario 被 Workbench 前置条件拒绝

- 用户提供页面样本：贾小花个人往来收支闭环，2026-02-04 两笔收款 200,000.00 / 100,000.00，2026-03-04 一笔付款 300,000.00，合计已闭合 300,000.00。只读核对定位到 `turnover_rel_89e8fb47e3ffce91`，`status=confirmed`、`source=manual`、`bank_row_ids=["txn_imported_1292","txn_imported_1277","txn_imported_1344"]`、`settled_amount=300000.00`、`balance_amount=0.00`；三笔 `app.bank_transactions` 的日期、方向、金额、counterparty、摘要和备注均与页面样本一致。
- 经用户本轮文字批准后，使用生产目标 OA 凭据 `zhou_jieying` 在远端内存中临时登录，session 为 `full_access` 且 `can_mutate_data=true`；token 未输出、不落盘。执行 scenario `/tmp/finops-write-e2e-jiaxiaohua-turnover-withdraw-20260620.json`，approval reference `FINOPS-PROD-WRITE-SMOKE-20260620-JIAXIAOHUA-TURNOVER-001`。
- Apply 结果：`write_operation_e2e_smoke --apply` 的写步骤 `POST /api/turnover-ledger/relations/turnover_rel_89e8fb47e3ffce91/withdraw` 返回 `409 unexpected_status`，工具返回 `status=fail`、`failed_scenario_count=1`，write SLO 和 post API probes 均按 `write_step_failed` 跳过。
- 根因核对：该 turnover relation 对应 active Workbench relation `case_id=turnover:turnover_rel_89e8fb47e3ffce91`，`relation_mode=manual_confirmed`，`row_ids=["txn_imported_1344","txn_imported_1292","txn_imported_1277","oa-pay-2025"]`，`row_types=["bank","bank","bank","oa"]`。按接口合同，已补齐 OA/发票或其他业务 row type 的外部往来闭环不能在外部往来页直接撤回，必须到关联台撤回完整关系；这是预期业务前置条件拒绝，不是 worker/read model 失败。
- 执行后只读复核：`app.turnover_relations` 中 relation 仍为 `confirmed`、version `1`；`app.turnover_relation_events` 无新增 withdraw 事件；apply 时间窗口内无新增 `job.outbox_events` 或 `job.read_model_dirty_scopes`；`job.outbox_events`、`job.read_model_dirty_scopes` 无非 done，`read_model.app_status_readiness` 无非 fresh。结论：本次真实写接口被 409 拒绝，没有业务数据落地变更，不需要回滚。
- 后续要求：如果要继续使用这组贾小花样本，下一步 scenario 必须改为关联台撤回完整关系，而不是外部往来页 `relations/{relation_id}/withdraw`；由于它会影响 `oa-pay-2025` 的 active Workbench 关系，必须重新取得用户对“关联台完整撤回”的明确批准。

## 2026-06-20 - Production 贾小花 Workbench 完整关系撤回

- 用户明确批准继续使用同一贾小花样本，并从关联台撤回完整关系。执行前 active relation 为 `case_id=turnover:turnover_rel_89e8fb47e3ffce91`，`row_ids=["txn_imported_1344","txn_imported_1292","txn_imported_1277","oa-pay-2025"]`，`row_types=["bank","bank","bank","oa"]`。
- Scenario `/tmp/finops-write-e2e-jiaxiaohua-workbench-withdraw-20260620.json` 使用 `POST /api/workbench/actions/withdraw-link`，operation profile 先按严格 `workbench_relation_withdraw_cross_page` 执行，approval reference `FINOPS-PROD-WRITE-SMOKE-20260620-JIAXIAOHUA-WORKBENCH-001`。生产目标 OA 凭据只在远端内存中临时登录，token 不输出、不落盘。
- 写步骤结果：HTTP `200`，elapsed 约 `762.793ms`；post API probes 通过，`workbench_groups` fresh 约 `202.937ms`、`turnover_ledger_grouped` 约 `68.565ms`。业务状态复核显示包含 `oa-pay-2025` 的完整关系已撤回，当前恢复为 bank-only active Workbench relation：`case_id=turnover:turnover_rel_89e8fb47e3ffce91`、`relation_mode=turnover_manual_closure`、`row_ids=["txn_imported_1292","txn_imported_1277","txn_imported_1344"]`、`row_types=["bank","bank","bank"]`；外部往来 relation 仍为 `confirmed`、`settled_amount=300000.00`、`balance_amount=0.00`。
- SLO 结果：窄口径 `workbench_relation_withdraw` 通过，`workbench_relation` scope `2026-03` enqueue-to-done 约 `2711.816ms`。严格 `workbench_relation_withdraw_cross_page` 失败：`workbench:all` 约 `20851.862ms`，`pending_invoice income:cash_income:2026-02` 约 `9878.571ms`，`cost_statistics active:2026-03` 约 `7264.401ms`；`invoice_lifecycle`、`input_invoice_usage`、`tax_offset` 未产生近期必需 refresh event。`bank_detail` 和 `search` 通过。
- 2026-06-20 当时曾为该“不对称恢复样本”增加 `workbench_relation_*_bank_turnover_cross_page` profile。2026-07-12 终审确认：一次 Workbench withdraw 恢复 bank-only turnover relation，不能定义可逆 confirm/withdraw pair；按 profile 反向制造事件还会遮蔽真实 planner。该旧 pair 已退休，Phase 20 的 bank+turnover 只使用正式 turnover closure confirm/withdraw 与 `turnover_relation_changed` scopes。历史 `workbench:all`、`pending_invoice`、`cost_statistics` 慢尾仍保留为当次生产证据，不因合同纠正而删除。
- 生产只读下钻：按 `2026-06-20T15:08:00+08:00` 到 `15:10:30+08:00` 窗口重算 bank/turnover profile，`workbench_relation`、`bank_detail`、`search` 通过；`workbench:all` 约 `20851.862ms`、`pending_invoice` p95 约 `9878.571ms`、`cost_statistics` p95 约 `7264.401ms` 失败。`pending_invoice` 的 `runtime_result.duration_ms` 只有约 `25-176ms`，但多次 `runtime_defer.reason=bank_detail_read_model_not_fresh`，并把 `bank_detail:2026-03` 从初始 `source_version=44635` 连续补投到 `44638`，导致下游等待被自身 dependency refresh 放大。
- 修复方向：`RuntimeWorker` 在 dependency scope 已 active 时原本不会补投；本轮进一步要求 dependency scope 已 fresh 时也不再 enqueue 依赖 refresh，只 defer 当前事件并记录 `already_fresh`。这避免多个 pending scope 在银行明细已 fresh 后继续 bump `bank_detail` source version。该修复只影响 read model dependency refresh 防抖，不改变业务写接口或业务事实。
- 复核状态：`job.outbox_events` 无非 done，`job.read_model_dirty_scopes` 无非 done，`read_model.app_status_readiness` 无非 fresh；`/health/ready` 返回 `ready`，API、RabbitMQ dispatcher 和 20 个 worker service 均 active。结论：真实写入和基础 worker drain 成功，但严格跨页 SLO/profile 未闭合，需要后续区分该样本不适用的下游 scope 与真实长尾性能问题。
