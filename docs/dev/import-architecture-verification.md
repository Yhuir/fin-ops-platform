# 导入架构修复验证记录

日期：2026-09-21。实施基线：`501f72273`。范围与验收要求见 [实施计划](import-architecture-implementation-plan.md)。本记录只包含脱敏结果，原始发票、流水、来源文件与逐条取证留在本地，不发布到公开仓库。

## 实际变更

- 普通流水、发票、ETC 使用持久导入任务，准备、待确认、正式提交和失败恢复有明确状态。只在正式事务提交后报告成功；预览不创建财务事实。
- 普通导入使用请求内实例，避免共享快照覆盖新数据。去重针对业务项；同文件再次上传仍与当前事实比较。上传请求重发与主动再次导入分别处理。
- 原件登记与任务受理同事务；领域事实、批次、行证据、必要审计及任务结果同事务。并发领取采用数据库锁和领取版本，旧 worker 不能提交已被接管的任务。
- ETC 一次解析 ZIP/XML 后保存 manifest，确认不重新解压。metadata、附件、批次成员和已有 canonical 发票的受控关联一起提交，不取得正式发票财务字段的写权限。
- 取消、重新预览、重复确认和提交回执丢失均有明确结果；页面读取失败不再次发起导入。全局任务直接呈现持久任务。
- 删除旧 import outbox 执行入口、重复后台进度状态、整文件去重阻断和旧 ETC business-batch 预览确认入口。共享 OA 手工导入和税务认证调用方同步迁移。
- 关联台追加发票入口按价税合计与 OA 付款项比较，修复未税金额导致的虚假缺口；未知价税合计不猜值，金额/关系不变。
- 修复负借方/贷方与文本零解析、未知税额误记零、OA OCR 财务字段顺序和已存在税务金额被覆盖的问题。历史金额修复仅接受已登记税务原件的精确证据与 CAS。
- 生产原件只读预检发现税务导出末尾的标准“合计行”被误当作缺号码业务行。修复只识别明确汇总标记、合法金额字段的尾行，并逐列核对金额、税额和可选价税合计；缺号码业务行、伪汇总、非法金额及不一致的控制合计仍明确报错。三个原件核对登记 SHA/大小后分别解析 699、167、249 张，控制合计与表头/明细身份核验通过；解析服务 56 项回归通过。

## 性能测量

以下是隔离 PostgreSQL、固定合成业务数据的五次独立样本，p95 为 nearest-rank（五次样本取最大值）。ETC 使用内存对象存储，不包含生产对象存储网络耗时。

| 数据 | 准备 p50 / p95 | 提交 p50 / p95 | 总耗时 p50 / p95 |
| --- | --- | --- | --- |
| 1,000 张普通发票 | 0.559 / 0.584 s | 1.175 / 1.275 s | 1.769 / 1.843 s |
| 1,000 条银行流水 | 0.573 / 0.584 s | 0.959 / 0.999 s | 1.524 / 1.582 s |
| 120 张 ETC | 112.75 / 117.80 ms | 251.74 / 261.23 ms | 分阶段测量，不推算总 p95 |

银行强编号索引消除了相同弱指纹候选的重复扫描；优化前该样本提交为 6.39 s。ETC manifest 校验 p50 / p95 为 3.17 / 3.50 ms。以上不能替代生产端到端上传、排队和最终可见耗时。

部署前生产只读基线：56 类接口、280 次请求，全部通过，零错误；各接口 p95 的最大值为 962.056 ms。未以 HTTP 202 的受理耗时冒充导入完成耗时。

## 验证范围与状态

七类测试均适用：业务核心（身份、金额、重复、状态）、服务事务（故障回滚、回执丢失）、API（owner、权限、版本、幂等）、后台任务/读模型（领取、恢复、刷新）、前端交互（异步预览、重试和反馈）、跨模块全链路、既有功能回归。稳定版本后端全量 4,423 项、前端 107 个文件 / 1,431 项全部通过，未跳过 PostgreSQL 集成。浏览器 smoke 共 252 项；首轮 245 通过，其余 7 项单独复跑：6 项直接通过，1 项揭示追加按钮误用未税金额，修正后所属 fanout 文件 8 项全部通过；原 252 项均已有通过记录。

真实 PostgreSQL 故障测试使用本任务独立临时实例，绝不使用主数据库。当前没有创建主库备份。历史数据修复、生产链路复测和任务工件清理尚待正式部署后执行，不能把测试通过视为这些操作已经完成。

实际验证命令：

- `FIN_OPS_TEST_DATABASE_URL=<本任务隔离实例> FIN_OPS_CASH_TEST_DATABASE_URL=<本任务隔离实例> bash scripts/verify.sh backend`
- `cd web && npm test -- --run`
- `cd web && npm run build`
- `cd web && npm run e2e:smoke`，以及对失败场景的定向浏览器复跑（性能阈值保持不变）。
- `bash scripts/verify.sh lint`、`bash scripts/verify.sh docs`、`git diff --check`。
- 最后 owner、取消、ETC 和 direct queue 的 45 项真实 PG / 服务回归通过。
- 价税合计修正后 `groupDisplayModel`、`RelationGroupGrid`、`WorkbenchSelection` 共 187 项通过，并重新完成生产构建。

首轮全量运行期间完成的 OA 终态修正和旧文件登记 mock 更新，已由冻结后的整套回归重新验证。关联台旧 E2E 断言按现有产品合同更新：只有不足额时显示追加入口，足额时不显示，保留精确目标与高度对齐验证。前端下拉框测试等待真实焦点恢复后再输入，不延长超时或删除筛选断言。

## 历史证据的边界

历史取证发现上传成功但未提交的预览、明确的解析遗漏和部分金额错误；不能由“重传新增”推导出“曾入池后被删除”。缺失的旧原件或旧逐行审计无法从当前去重结果重建，禁止编造历史 created/duplicate 决策。已存在的事实与历史审计缺口分别核对。

## 发布限制

迁移 `0175` 停用旧 import outbox 消费并引入新任务状态，必须先停止旧 consumer，再迁移和启动新 worker。该版本属于 forward-only：旧代码不能安全消费新准备任务。仓库部署控制器已标记此迁移，生产 root-owned 控制器需要同步后才能正式激活；不能伪造旧代码兼容证据绕过发布检查。

代码提交 `f5fd71da3` 与税务汇总修正 `b86585878` 已推送 remote main。候选 `main-b86585878-20260921-import-atomic` 已通过正式入口上传与服务器校验（包括依赖审计），尚未激活。以该候选对已登记税务原件执行生产只读 dry-run，28 个精确目标均有有效证据、当前需要更新 28 个，尚未执行写入。金额修复工具定向 6 项测试通过。

当前部署账号 sudo 仅允许两个现有部署 helper，没有安装 root-owned helper 的权限，root SSH 也不可用。已提供经 SHA 校验的精确脚本供服务器管理员安装，改动是将 `0175` 加入不可回退迁移列表。等待此外部权限操作期间，线上服务保持 active，运行 `main-501f72273-20260921-etc-evidence`。部署后性能、新链路生产导入、历史缺项补入及金额修复仍未完成；不得将候选上传称为部署完成。
