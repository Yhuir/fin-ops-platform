# 成本统计实施决策

## 2026-09-10：修复实际记录没有预填

- 上一版本只接受银行原始显式OA引用，生产30项全为空，没有完成用户实际预填目标。本次替换该限制性路径；下面旧版记录仅作历史，当前规则以README/boundary-io为准。
- 范围保持现有来源纯函数/详情service/DTO/草稿/事务保存链。已确认的UI配色和合并单元格不重做；前端现有字段映射、草稿优先级可直接消费修复结果，新增真实结构浏览器验证而不重复实现。
- 唯一是限定业务组合（整笔来源→单元、固定单元→单来源）中的唯一；不代表任意拆金额矩阵唯一，也不代表已核实付款事实。只给独立且唯一的组件建议，歧义/超限留空；不按顺序、比例、日期或屏幕同行推断。
- 不新增数据库迁移、备份、依赖、缓存、worker或门禁；保留既有权限、版本、fingerprint、事务和审计。当前canonical OA投影没有完整别名事实，不增加猜测映射；退款/非固定目标缺少确定分配依据时保留人工。
- 验收案例：88050→64996.69+23053.31；29350→29350；469600→469600。预填保持pending/version0；保存前零写入、保存后真实PG审计和跨月三视角对账。

- 发布前验证：104项成本后端测试（含11项真实PG）、98文件1293项前端测试、23项发布构建Chromium业务流通过；lint/docs/build/diff检查通过。先新增回归案例观察旧实现失败，再替换规则。
- 本地纯计算30次：截图3×4 p95 0.085ms；20×20重复金额3.669ms；40×40密集候选12.540ms（未确定则无建议）；40×40唯一二进制金额3.330ms。输入范围和50000步上限明确，不宣称无限规模。
- 发布构建Chromium/100行/每类20次：输入、增删、菜单打开、切换来源p95为34.3/42.9/42.8/49.8ms；无额外HTTP。开发构建并发测试期间曾测得150–187ms，发布构建独立测量达到100ms目标，不将开发测量隐藏或冒充生产数据。
- 生产改前公网HTTPS20次：详情p50/p95/max=91.19/145.65/189.43ms；列表516.19/706.28/878.38ms。总成本12226597.09、563行、待分配30、已完成2、未定月份2596896.40/72行，发布后对照。

## 2026-09-10：明确来源预填、四色 Block 与成本单元合并

- 复用现有来源纯函数模块、定向 canonical repository、人工分配 service 和会话草稿；没有引入独立推荐系统、求解器、数据库表、依赖或 GSD。
- 自动预填必须有原始 OA 来源引用及唯一金额分布，仅存在于待分配详情/草稿。既有自动完成规则不变；无法证明的组留空，不按金额、时间、比例或屏幕同行猜配对。
- CASE-AUTO-0016 的 Workbench 对齐来自 `exact_amount` / `unique_bank_sum`；不能把它当成已经持久化的逐 OA 付款归属。缺少明确原始引用时，本轮有意保留人工分配。
- 项目与 OA/成本项按成本单元原生 rowSpan 合并；来源行保持独立。四色采用橄榄绿、杏色、玫瑰灰、蓝灰，白色表格与输入形成可编辑区域。旧双色样式与续行空单元格删除。
- 生产统计改前：全部期间成本 12,226,597.09 元、563 行；待分配30、已完成2；未确定月份72行、2,596,896.40元。发布后复核同一口径；本轮不创建数据库备份或迁移，不删除主数据库。
- 验证明细见本模块测试矩阵；运行日志与浏览器截图放本地临时目录，不提交真实业务截图。
- 本轮发布前验证：真实 PostgreSQL 成本专项94项通过；全量98个前端文件、1,293项测试通过；成本抽屉/五视图/关系链路22项 Chromium 测试通过；lint、docs、生产构建和 diff 检查通过。测试环境先补齐独占PG容器的既有角色，数据样例修正遵守既有 correction_reason 事务约束；未修改生产安全措施。

### 发布与生产验证

- 应用提交 `ca9cafcda5830f9cea736a2c5a85c923a7107262` 已推送 remote main，通过正式入口发布为 `main-ca9cafcd-20260910103016`。runtime profile 的 T+0 / T+30 均 PASS；4个登记 worker 就绪，无未知 worker，无失败/死信增量，未回滚。既有发布临时写探针完成，不对真实成本归属做测试性保存。
- 生产三个成本根视角、8/9月跨月对账、15条成本来源的金额/账户/标签/日期一致性、分页、14列导出及旧参数拒绝均通过。总成本12,226,597.09元、563行、待分配30、已完成2、未定月份72行均与改前一致；两个银行流水视角一致。
- 逐个读取当前30项待分配详情，30项建议均为 null，没有猜填。截图 CASE-AUTO-0016 仍 pending/version 0/source null；这是当前没有满足严格来源证据的实际结果，不宣称截图记录已自动预填。具备明确原始引用的建议→人工确认→真实持久化由隔离 PG 测试验证，生产没有伪造引用或财务决定。
- 生产 Chrome 实际打开新版抽屉、检查四色、点击两次新增来源并视觉核对项目/OA两格合并；仅新增空白会话行，随后删除，没有点击保存。原生关闭确认框使自动化连接超时，未继续操作其它用户标签；不把此工具清理异常写成财务保存成功或失败。
- 本地发布构建 Chromium / 1440×1000 / 每类20样本：100行输入、增删、下拉打开、来源切换 p95 为33.9/40.5/39.3/47.4ms，无额外HTTP。数据到表DOM43.2ms是单次观察，不冒充p95；3项发布构建复测通过。首轮开发服务器大表测试曾超时，独立复测和22项整套复测通过，未放宽超时或断言。
- 生产公网HTTPS顺序20样本/接口，含网络/TLS/鉴权/SQL/JSON：任务详情 p50/p95/max=95.35/107.87/115.57ms；任务列表=490.71/636.33/874.24ms。改前相同方法详情p95=117.24ms、列表=604.88ms。详情没有明显退化；列表既有全量聚合限制仍存在，不能据此宣称高并发/大规模全部达标。真实财务保存的生产p95未测，合法保存及失败路径由PG测试保护。
- 精确删除本次独占 `finops-cost-prefill-test` PG16容器及匿名卷，停止本次5189端口预览进程。没有创建数据库备份、没有删除主数据库。此节为上线后的文档记录，不改变已部署应用产物。

## 2026-09-09：双表格、Chip 与同行分配操作（第二轮）

- 用户取消 Make 后，按 Impeccable product register、现有 PRODUCT.md/DESIGN.md 直接改进 App。新 Make 的空白生成不作为实现或还原证据。
- 上方复用 FinanceTable 展示 OA 成本单元和银行流水，标题简化为 `OA · N 条`、`银行流水 · M 条`；Chip 使用 FinanceStatusTag。两表独立，不暗示逐行一一配对；来源选择沿用上表实际序号（包括退款占据的序号）。
- 下表保留六列，每项首行操作格提供新增，删除最后一行保留项目/OA身份和新增入口；退款、非成本和显式零成本均保留。选项面板按需挂载，选中态短银行/日期，完整面板包含时间、金额、对方与主子标签；更换来源保留金额。
- 来源/金额/owner 错误键独立；常驻说明及单元格错误段落移除。错误使用红框、可聚焦图标及短浮层，提交主动展开首个原因；不会把金额错误挂在来源下面。网络与整体失败留在操作区，既有版本、闭合、事务、权限及未知写结果核实不变。
- Block 交替浅蓝 `#EAF2FF` / 浅紫 `#F2EDFF`，展开后仍保留；表格白底、局部样式。旧 OA 卡片/全文展开行、专用新增行、原生 select 和旧灰白展开覆盖规则已删除，无并行旧 UI。
- 浏览器发现并修复金额输入最小宽度溢出遮挡新增按钮、选择控件 onAction 不能保证切换时单击生效；改用选择变化事件，补充坐标和真实点击测试。
- 高负载实测后为未变化的全文/Chip/关闭的来源选择控件避免重复渲染。来源可用性在打开时读取最新已提交草稿，稳定事件回调不捕获旧金额；没有新 store、缓存层、虚拟表格库或网络请求。
- 本地发布构建、Chromium1440×1000，各20样本：2行输入/增删/菜单打开/实际切换来源 p95 = 40.1/33.5/30.9/29.8ms；100个成本项、100条初始合法分配行、10个来源选项分别33.7/41.8/39.8/48.3ms。数据 responseEnd→首表 DOM 13.4/52.7ms为单次观测，不称 p95。实际切换先编辑另一行释放来源，再交替两笔流水；编辑无新增 GET/PUT。该结果是本地发布构建，不冒充生产网络耗时。
- 本地 Chrome 用只读代理加载实际30个待分配任务，审阅两表、相邻组色、选项完整信息、全文和错误浮层。实际切换账户后350.00保持；本地代理拒绝写请求，验证草稿已通过重载丢弃。E2E额外验证390px精确视口；Chrome手动视口受原浏览器缩放影响，不将其观测宽度当作精确390px。
- 验证：已有独立PG16成本专项90项通过、独占容器与匿名卷已删除；本轮6份前端单元/组件/API/公共表格文件通过，30条相关浏览器回归及2项独立性能通过。新增核心错误分离、来源/金额、删除顺序、全文键盘、退款/非成本与序号测试。六份前端测试合计51项（最后新增退款序号后定向复跑组件10项通过）；类型与发布构建、docs检查均通过。发布结果在下方补充。
- 测试类别：业务核心/组件/跨模块E2E/回归适用；服务/API使用既有真实PG和合同测试，本轮无后端I/O变化；read model/cache/worker生命周期测试不适用。数据库迁移和备份不适用，主库未修改。生产正式发布及发布后验证结果见下一节。


### 第二轮正式发布与生产验证

- 应用提交 `f5fa2d10aa054470d189475f0ef7a97ab8ac8eb8` 已推送 remote main。正式入口 `scripts/with-production-admin-token.sh ./scripts/deploy-oa.sh` 成功，release `main-f5fa2d10-20260909234719`，frontend profile 的 preflight/T+0 为 PASS，无回滚；公开资源 `index-D4DB26AK.js`。四个 required worker ready，unknown worker 0。此 profile 未执行 runtime T+30，不把 null 项宣称为通过。
- 生产 Chrome 实际看到 `OA · 3 条` / `银行流水 · 4 条`，前三组背景为234/242/255、242/237/255、234/242/255；旧独占新增行数量0。生产新增、选来源、350金额换账户保留、删末行回焦新增入口均通过，随后重载丢弃草稿，没有提交测试业务写入。
- 三个成本视角总额均12,226,597.09，共563条成本明细；待分配30、已完成2。抽查15条实际来源金额、账户、主/子标签、日期一致，8/9月份视角对账通过；两个银行视角同口径。导出预览及下载14列、来源明细sheet通过，退役参数继续400拒绝。
- 公网HTTPS（含网络/TLS、鉴权、SQL与JSON）各20样本：任务摘要列表p50/p95/max=490.30/577.97/616.60ms；定向详情111.08/135.92/224.31ms。
- 生产loopback HTTP（含鉴权/SQL/JSON、不含公网）各30样本：项目/标签/账户根p95=460.72/460.62/460.69ms，下钻463.57ms，成本详情20.24ms，任务列表548.75ms、定向详情22.96ms。额外4并发20请求的项目汇总p95=1026.22ms、最大1083.53ms，略高于既有1000ms目标；上一版已记录类似并发限制。本轮UI交互达标，不把后端并发压力下的这一限制宣称为达标，也不扩展本次UI改造为后端查询重构。
- 本地只读代理、Vite开发/预览服务已停止，端口确认无监听。未创建数据库备份，未删除或修改主数据库。

实际验证入口：

```bash
# 前端定向核心、组件、API和公共表格测试（最后序号回归单独复跑组件文件）
npm --prefix web test -- --run src/test/CostSourceAllocation.test.ts src/test/CostSourceAllocationForm.test.tsx src/test/CostStatisticsPage.test.tsx src/test/CostStatisticsApi.test.ts src/test/FinanceTableMigration.test.ts src/test/FinanceTable.test.tsx
npm --prefix web run build
# 在本地发布构建preview 5190上执行30条相关浏览器回归
FIN_OPS_E2E_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5190 npm --prefix web run e2e -- e2e/cost-source-allocation.spec.ts e2e/cost-statistics-flow.spec.ts e2e/cost-statistics-relation-fanout.spec.ts e2e/bank-details-initial-state.spec.ts e2e/app-shell.spec.ts e2e/workbench-withdraw-flow.spec.ts --project=chromium --grep-invert 'measures local'
# CPU测量单独执行，不与回归并行
FIN_OPS_E2E_SKIP_WEBSERVER=1 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5190 npm --prefix web run e2e -- e2e/cost-source-allocation.spec.ts --project=chromium --grep 'measures local'
bash scripts/verify.sh docs
git diff --check
# 本轮已有90项真实PG16专项：FIN_OPS_TEST_DATABASE_URL指向任务独占临时数据库
PYTHONPATH=backend/src python3 -m unittest discover -s tests -p 'test_cost_statistics*.py'
```

生产只读链路、公网20次和loopback30次测量由本机临时脚本通过认证包装器执行；输出保留本机 `/tmp/cost-grid-prod-chain.log`、`/tmp/cost-grid-public-perf.json`、`/tmp/cost-grid-origin-perf.log`，不提交含业务明细的原始数据或 token。

## 2026-09-09：紧凑两栏待分配抽屉重构

- 根据用户截图在同一 Figma Make 原型实际生成 V5，浏览器发现常驻合计、完成态不可编辑、单据计数/显式零和窄屏布局问题后，定点迭代至 V8。通过浏览器 Code 编辑器读取实际展示源码/局部样式，接入现有 AppDrawer 与 Cost GET/PUT；没有复制原型假保存或 mock 数据到生产。
- 桌面抽屉1320px，上方40/60 OA与银行成员、下方六列分组输入表；≤960px证据上下排列，≤640px工具栏换行。移除所有可见内部ID、大证据卡片、常驻已分/剩余、净额公式、输入区重复原额和独立 targets 编辑状态。业务识别采用项目、事由、申请人和原始凭据。
- 固定目标来自原OA；可编辑成本由来源行精确派生，未分配和显式零区分。保留退款/非成本、权限、逐来源闭合、版本、事务和审计。网络写响应不明确时只GET核实，不再次PUT。共享HTTP客户端仅新增调用方关闭既有HTML重试的选项，Cost保存使用关闭；其他页面默认行为不变。
- 模块仍为容器网络编排、无I/O表单、纯草稿工具、局部CSS。FinanceTable仅登记本表单的特定原生输入表例外，没有新增通用编辑引擎/库、后端运行时代码、数据库迁移、worker、缓存、hash或门禁。
- 性能问题来自每次输入重复格式化100行×10个来源的日期。按任务来源预先计算下拉描述后，独立Chromium/1440×1000、本地mock接口下，2行输入/选择/增删p95为33.4/33.5/33.4ms，100行为35.2/66.6/56.5ms；每类20样本。详情responseEnd到首张表DOM出现分别14.4/109.5ms（单次观测，非p95）。输入无额外HTTP，展开一次定向详情GET。改前100行输入p95 211.1ms、选择191.7ms；测量没有与全量Vitest并行。
- 真实PG16成本专项90项通过，包括新前端派生600/400请求结构持久化重读、跨月三视角、固定目标、审计失败回滚和并发冲突。浏览器保存测试使用HTTP mock；真实持久化由PG测试验证，并通过现有 Application HTTP 路由执行 PUT→GET→三视角跨月读取；没有浏览器直接写生产数据。
- 本地验证：98个前端文件1,285项测试通过；构建发现 aria-invalid 的布尔类型问题后已修正，15项相关组件复跑和生产构建通过。成本/银行明细/导航/关联撤回29项浏览器回归通过，另有2项独立性能测量通过；后端Cost 90项在PG16配置下通过，无跳过；lint/docs与diff检查通过。
- 七类测试中业务核心、service/持久化、API、前端交互、跨模块链路和旧功能回归适用；Cost直接canonical读取且未改变read model/cache/worker，第4类新生命周期测试不适用。生产正常财务写入耗时不通过编造分配测试。发布结果将在本节追加；以下“逐来源成本分配”是此前版本实施历史。

### 本次发布与生产复核

- 应用提交 `bb133f522f62e2a91090cc712a7b3eb2252f3367` 已推送 remote main，发布为 `main-bb133f52-20260909213858`。通过 `scripts/with-production-admin-token.sh ./scripts/deploy-oa.sh --activate-existing --release-name main-bb133f52-20260909213858` 激活已校验发布包。初次打包命令缺少认证包装器，在激活前停止；补用既有本机凭据后成功，未绕过发布保护。
- 既有发布系统判定为 frontend profile，release gate PASS，公共页面/静态资源验证通过，API与4个登记worker就绪，无回滚。该profile没有执行完整后端T+30队列审计（字段为null），不能写成该审计已通过。本次没有后端运行时/schema变更。
- 生产全部期间总成本12,226,597.09元、563行，与发布前相同；三个成本视角及8/9月分别对账一致；原银行两个视角一致。跨视角15次来源明细核对金额/账户/标签/付款时间与银行证据一致，分页无重复；聚合导出含14列成本来源明细。待分配30、已完成2；72行2,596,896.40元来源/月尚未确定，保持事实而不猜分配。
- 生产Chromium实际走4/3/5栏下钻、打开新版抽屉、检查无内部ID/解释公式、1728px桌面与390px窄屏。JS错误0、API错误0、财务变更请求0。只在当前页面草稿中测量输入后关闭丢弃；20样本金额输入p95 33.7ms，详情responseEnd至表DOM出现10.8ms（单次）。
- 浏览器验证结束后独立测量生产本机HTTP（含鉴权/SQL/policy/JSON，排除外部网络/TLS），每场景30样本：项目/流水标签/银行账户根查询p95 457.90/456.71/473.53ms；项目下钻456.16ms；成本详情19.92ms；任务列表524.57ms；任务详情23.59ms。4并发20个根查询p95 1,049.05ms。前端输入目标达到；根查询/列表的全量聚合与并发容量限制仍在，不宣称所有后端耗时目标达到。
- 正常财务保存的生产p95没有测量，不为压测编造人工归属。合法保存→真实PG持久化→GET→三视角跨月由隔离库测试保护；生产仅做只读链路和未保存草稿验证。本轮没有数据库备份或迁移，独占 `fin-ops-drawer-test-20260909` 容器及其匿名数据卷已精确移除，主数据库未删除。截图/运行日志仅作本地验证产物，没有写入仓库。
- 本节为上线后的验证记录，后续仅文档提交不改变上述已部署的应用产物。

## 2026-09-09：逐来源成本分配与银行标签统计

- 用户确认按来源真实付款日期归月；成本行统一为 OA 单元+支出来源+金额。多对多来源不确定时人工新增来源行；O=N的OA单元目标固定，O!=N保留人工金额政策。
- 新纯来源模块只负责双边闭合与唯一解；现有service/repository负责事务/CAS/audit。0169仅新增一个nullable JSONB字段，不引入新表、worker、cache、依赖、GSD或额外门禁。
- 三视角4/3/5栏均用银行主/子标签，账户日期同源。未知来源保持显式pending/undated，缺标签保存不伪报完成。详情与导出带真实来源；项目汇总同时附成本明细。
- 旧OA费用分面/导出、整组账户/最后付款月推断、include_cost_row_tags开关、标签字段猜测、旧单金额抽屉与372行旧样式已移除。历史migration和既有安全措施保留。
- Figma Make：[成本统计来源分配原型](https://www.figma.com/make/RVS6lniW3V8ZXKbFREjx2E/)。已真实生成、迭代并浏览器验证V3，下载源码参考后移植到既有HeroUI/AppDrawer；页面源码不依赖Make的mock或生成运行时。移植后校正选择框密度、移动端宽度/内边距和来源详情字段显示。
- 354项后端unittest通过；125项pytest通过（包含源码函数式回归、真实PostgreSQL和migration；与unittest有重叠，不能相加视为独立测试）。全量97个前端文件、1,275项单元/组件测试通过，生产构建通过。跨页面39项浏览器回归首轮38通过，发现移动端组件默认负边距导致3px溢出；修正仅限Cost抽屉，来源4项复跑通过。随后成本页面13项浏览器复跑通过；新来源测试已加入原有e2e:smoke入口及成功页面错误检查，相关26项仓库检查通过。全仓后端首轮发现的迁移清单与测试登记问题已修正；全量复跑和现金专用库补测结果见下文。

- 缺标签时可保存来源决定，银行 owner 补齐标签后自动收敛。账户或日期变化仍可能触发现有 fingerprint 失效，需重新确认；不承诺所有元数据变更均无须复核。
- 单条任务及成本详情按关联读取，并保留共享成员邻接关联的重复归属检查。列表分页缩小传输量，但任务状态与成本根汇总仍扫描当前候选事实；未宣称数据库分页或恒定扫描量。
- 扩容测量：本机真实 PostgreSQL 6,230 个关联组、12,460 条银行流水及12,460个OA单元，service 单并发 p50/p95 为1,971/2,126ms；4并发为5,429/5,974ms；8并发为10,899/13,023ms。此项不是生产HTTP测量，说明当前直接聚合在较大数据量高并发下存在容量限制；不以缓存或旧数据伪装提速。生产耗时已在发布后另外测量，见下文。

### 发布与生产验证

- 首次应用提交 `0f9dc2bee468cb27e5d02fcd02fee04c6c2aa000` 已推送 remote main，并通过正式入口部署为 `main-0f9dc2be-20260909040727`。迁移0169成功（27ms）。后续修正提交 `cd513a28dccfb9e1563351790640f32d0b38d7fa` 也已推送并部署，该次最终应用release为 `main-cd513a28-20260909042655`。两次发布pre/T+0/T+30均PASS，没有回滚；最终API和4个登记worker正常，pending/failed outbox与dead-letter均0，页面canonical审计通过。
- 首次激活因现有schema兼容证据缺失而在停服/迁移前拒绝。使用精确上一release `main-11520b19-20260908190445` 的代码，在独占PG16.14容器应用0169后跑固定5项真实写兼容测试通过；安装既有格式证据后激活，不跳过发布保护。本次没有新增发布门禁或业务hash。
- 后端全量4,157项运行：4,105通过、52项因现金专用变量缺失跳过；52项随后在正确配置的PG16隔离库中补跑通过。新成本PG16专项89项通过。前端全量1,275项通过；日期展示修正后18项组件/工具与4项来源浏览器复跑通过，构建与lint/docs检查通过。
- 生产浏览器实际完成4/3/5栏下钻、待分配抽屉、390px布局，以及管理员AppHealth只读验证；无JS/API错误。Make与App均已真实浏览器查看。上线后把来源日期改用现有上海时区格式器，且“来源或月份待定”明确区分于银行信息缺失。
- 生产全部期间总成本仍为12,226,597.09元；拆来源后563条成本展示行。三个成本视角、8/9月分别一致，原银行两视角收支一致。跨视角抽查15次明细金额/来源/账户/标签/日期与银行证据一致；列表分页无重复，聚合导出包含成本来源明细14列。缺项目参数的探测请求按合同返回400，补入真实所选项目后预览和xlsx成功；未改接口绕过输入校验。空分配PUT为400，前后version和分配内容未变，未代替用户填写真实生产分配。以上完整链路在第二次发布后再次运行通过，最终金额、行数和待分配统计与第一次验证一致。
- 当前人工队列30个pending、2个已保存完成；72条已知成本行、2,596,896.40元尚未确定付款来源/月份，只在全部期间计入，不猜月份。另有1个金额尚待分配关联及14个OA关系完整性排除，保持既有资格口径。
- 第一轮HTTPS测量（每次独立连接，31个根查询样本/视角）：项目/标签/账户p95分别568/569/550ms；成本详情44样本p95 172ms；任务明细30样本p95 125ms；任务列表30样本p95 586ms。4并发20次根查询p95 1,231ms。浏览器持久连接、cache:no-store复测各30次，三视角p95 532/515/536ms，4并发p95 1,216ms。这不满足所有预期性能目标，不能把正式发布检查通过当作容量目标全通过。
- 后续小范围修正删除Cost分支里已不可达的旧按月候选查询和重复来源ID处理；完全自动完成、无持久化决定的只读项不再生成未被消费的fingerprint。所有待分配项、已保存决定、实际写入仍保留原fingerprint/版本/事务/锁与审计。后端89项PG16回归通过；该修正已随第二次发布上线，生产浏览器再次完成三个视角、抽屉及移动端验证，无JS/API错误。
- 最终服务端测量使用生产本机HTTP，包含鉴权、SQL、policy和JSON，排除外部网络/TLS；串行每场景30样本。项目/标签/账户根查询p95分别470.04/458.51/452.60ms；成本详情21.09ms，待分配详情21.73ms，待分配列表468.85ms。项目下钻459.17ms，未达到计划300ms目标。4并发20次项目根查询p95为1,144.58ms，不能宣称高并发目标已达成。项目根查询首轮与浏览器验证同时进行时p95为781.33ms；上述470.04ms是浏览器验证完成后的独立30样本复测，保留并报告两次结果，不把混合负载结果删除。
- 普通有效保存的生产p95未测：不为压测编造人工财务判断。有效保存、金额双边闭合、撤回/银行修改并发、409冲突和审计失败回滚已在真实PG16隔离库验证；生产仅发送确定无效的空分配并确认400且原记录不变。功能链路已交付，性能部分达标；较大数据量/并发下的全量聚合扫描仍是明确限制。
- 测试覆盖七类要求中的业务核心、service/持久化、API、前端交互、跨模块端到端和旧功能回归。Cost直接读取canonical事实，本次没有Cost read model、cache或worker变更，第4类的新建刷新生命周期测试不适用；既有跨页面队列/worker通过全仓回归与生产发布健康检查保护。关键入口为 `bash scripts/verify.sh backend`、`bash scripts/verify.sh lint`、`bash scripts/verify.sh docs`、`npm --prefix web test -- --run`、Cost及银行明细/Workbench/导入/往来款/权限的Playwright回归和 `npm --prefix web run e2e:production-admin`。
- 清理完成：本次两个本机隔离测试库、PG16测试容器及其匿名卷、下载的上一release源码、Make源码ZIP及解压目录均已移除。没有创建数据库备份，没有删除主数据库；仅在生产执行已授权的0169迁移。验证记录保留在本模块文档，临时截图和日志未提交仓库。

> 以下为历史实施记录；与2026-09-09决策冲突的旧金额/来源/月份规则已退役，不能作为当前实现依据。

## 2026-09-01：五视图信息密度与基础流水统计统一

- 五个视图页头统一只展示银行流水、支出流水和收入流水。两个银行流水 view 直接使用当前范围的 canonical `bank_rows`；三个项目成本 view 在存在无 OA 项目时复用完整候选流水，在未配置无 OA 项目时只加载关系成员，并在同一只读 snapshot 用一条基础聚合取得全量收支数量。该聚合不执行标签分类；不物化带标签的 `bank_flow_rows`，也不新增 API、缓存、read model 或 worker。
- 左中栏统一使用 HeroUI `ListBox` 和 `Chip`。项目、费用类型、银行账户与标签名称完整自然换行；金额右对齐并以“支/收”短 Chip 标识方向。删除百分比、归集数、成本数、净额和所有文本展开/折叠按钮。
- 旧的溢出测量、`ResizeObserver`、animation frame、展开状态和 disclosure 样式已从运行时代码与测试删除，不保留兼容分支。人工分配 Drawer 的关系级 Disclosure 是编辑导航，不属于统计列表文字展开，继续保留。
- `银行账户未确定`是零个或多个付款账户时的确定性归因结果，不是第六个真实银行账户加载失败；页面保留该事实分组。
- API 和导出继续计算净支出，页面不显示净支出或净额。此处仅改变展示层与基础统计字段，不改变成本人口、账户归属、分页、详情或导出口径。

## 2026-08-31：三个项目成本视图、两个银行流水视图与账户归属

> 本节已被2026-09-09来源分配决策取代。下文仅作为演进历史；不得据此恢复原始`按银行`、旧 time-tag 规则或兼容 payload。

- 页面保留两个清晰分组：`项目成本`包含`按项目`、`按费用类型`、`按银行账户`；`银行流水`包含`按标签`、`按时间`。没有新增“OA 配对”等术语；用户已经认可的`按银行账户`名称保持不变。
- 三个 view 必须基于同一成本事件集合，根汇总人口与金额一致。`按银行账户`采用“账户→项目→成本明细”三栏，不再等同于银行明细页的原始流水列表。
- 两个流水 view 必须基于同一 canonical 银行流水集合：`按时间`直接列出范围内流水；`按标签`采用“主标签→子标签→流水明细”三栏。两者均按真实资金方向计算`净支出=支出-收入`，例如两笔支出 4200 与一笔收入 2100 的净支出必须是 2100，而不是绝对值合计 6300。
- OA 成本关系只用支出流水确定账户：恰好一个非空账户时归该账户；零个或多个不同账户时归`银行账户未确定`；退款账户忽略。无 OA 成本使用来源支出账户。
- 恢复的是当前 canonical repository/policy/query service 上的 `time|bank_tag` 读取与导出，不恢复已删除的原始 `bank` view、旧规则抽屉、`/time-tag-rules` route、`cost_statistics_time_tag_selection` 或旧兼容 payload。
- 继续复用现有 canonical repository、policy、query service、分页和导出边界；不新增层、表、worker、cache 或依赖。
- 两个流水 view 的 repository 热路径只读取范围内银行流水和批量有效标签投影，在 OA/关系/人工分配读取前返回；项目成本热路径保持不变。银行明细继续负责账户余额、账户级筛选和流水维护，成本统计只提供时间/标签分析。跨模块回归以银行明细、Workbench、导入、往来款、设置和权限的定向测试保护。

## 2026-08-28：等额关系自动归因与逐 OA 单元人工成本

> 本节取代同日“全局复杂关系队列与显式多维分摊”中“仅 1×1 自动”和“每个 OA 单元重复按银行流水录入”决策，也取代 2026-08-27 的单元总额/合成多维分摊方案；旧段落仅保留历史。

- 自动/人工边界统一为 `O=N`：任意关系拓扑只要 OA 合计等于净支出，就按 canonical OA 单元原金额自动形成成本；只有 `O!=N` 才进入全局人工队列。`N=0` 不形成成本或任务，`N<0` 作为冲突失败，不提供绝对值、OA 金额或历史值 fallback。
- 人工 DTO 收敛为“每个 OA 单元一个金额”。支付申请整单是一个单元；日常报销逐 canonical 子付款项是一个单元；银行流水数量不增加输入数。删除旧来源字段、逐流水格子和任何按流水比例伪造归属的路径。
- 记 `C=sum(unit allocations)`、`X=non_cost_amount`、`N=gross outflow-wrong-payment refund`。未勾选“不计入成本金额”时 `X=0` 且 `C=N`；勾选时 `0<X<=N`、原因必填且 `C+X=N`。checkbox 仅由 `X>0` 派生，不持久化第二个布尔事实。
- 视图会计口径明确分离：`按项目/按费用类型=C`，`按银行/按标签/按时间=N`。项目/费用事件使用关系最新有效支出时间作确定性时间锚点；这只解决期间排序，不代表 OA 单元来自该流水、账户或标签。
- 人工分配 UI 使用 Drawer 内折叠列表；标题行放 pending/allocated tabs，每个关系独立展开、独立维护草稿、独立保存。折叠标题不显示内部 ID；展开只显示 OA/流水业务事实、逐 OA 单元输入、可选 X/原因。日期统一为 `Asia/Shanghai YYYY-MM-DD HH:mm:ss`。
- schema 通过后继 migration 把旧多维分配列收敛为 `unit_allocations`、`non_cost_amount`、`non_cost_reason`；运行时仍复用 `app.cost_statistics_manual_allocations`、version/source fingerprint 和同事务 audit。`0162` 仅迁移能够严格闭合的旧决策，并保持已持久化 fingerprint 的字节级身份合同，因此有效旧决策不会因 DTO 改名被误判 stale；关系成员、版本、OA 单元、流水或金额事实变化仍会使 fingerprint 改变并失效。不增加 read model、worker、cache 或数据库备份。

## 2026-08-28：短暂方案（已撤销）

> 原“仅 1×1 自动”和“按流水来源拆分 OA 单元”的方案未成为当前合同，已由本文件首节完全取代；不得据此恢复旧计算路径。

- 仍沿用的只有全局任务列表、`pending|allocated` 状态、按关系独立保存、乐观版本、事实指纹和同事务审计边界。
- 人工分配只保存逐 OA 单元成本金额及可选不计入成本金额；流水只提供净支出证据，不用于伪造 OA 单元的账户、标签、期间或资金来源归属。
- schema 已通过后继 migration 收敛为当前 `unit_allocations`、`non_cost_amount`、`non_cost_reason` 合同；不得兼容读取旧聚合 JSON 或保留并行 fallback。
- Drawer 的保存动作归属当前关系，五个视图按“成本归因 / 银行流水”分组；金额与视图口径以本文件首节为唯一依据。

## 2026-08-27：OA 与净支出不等改为显式人工分配

> 本节的“仅 O!=N 进入人工分配”、Popover、单元总额 DTO 和合成比例矩阵已由 2026-08-28 决策取代；表、审计与乐观并发边界继续有效。

- `按银行`、`按标签`、`按时间`共用原始 canonical 银行事实链；`按项目`、`按费用类型`只消费已完成 OA 的 active relation 与显式无 OA 项目。五个 HeroUI 切换按钮在同一扁平工具区按“成本归因/银行流水”分组，不增加页面、卡片或请求层。
- 关系净支出定义 `N=支出合计-同关系明确付错退款` 继续有效；本段当时复用比例/最大余数算法的决定已由 2026-08-28 首节撤销。当前 `O=N` 按 canonical OA 单元原金额自动形成成本，`O!=N` 进入 pending/stale，禁止恢复比例路径。
- 新增唯一持久化边界 `app.cost_statistics_manual_allocations`，以 relation case、source fingerprint 和乐观 version 保存完整稳定 OA 单元金额。Popover 首次输入为空且按需加载；允许显式零，不预填、不推荐、不做前端比例计算。服务端在单事务内锁定当前 case，校验完整单元集合、非负两位小数与合计严格等于 N，并同时写入 `audit.events`。
- 当时尝试过把 OA 单元金额合成到各流水来源；该做法已删除。当前只校验逐 OA 单元成本 `C`、可选不计入成本金额 `X` 与关系净支出 `N` 的 `C+X=N`，不得推断 OA 单元来自哪条流水、账户或标签。关系成员、OA 单元或金额变化仍会改变 fingerprint，使旧记录 stale 并要求重新填写；不提供自动比例、首项默认、旧值沿用或任何 fallback。
- canonical repository 对两种归因视图一次批量读取人工分配；`bank|time|bank_tag` 在 OA/关系查询前返回并跳过该表。保存只锁定当前 case，不新增 read model、worker、cache、queue、数据库备份或跨页面 I/O。

## 2026-08-21：分面长文本改为条目内展开

> 本节已由 2026-09-01 的完整换行列表取代；运行时代码不再保留文字展开、溢出测量或 disclosure。

- 项目、费用类型、银行账户、主标签和子标签继续使用同一个 `CostExplorerList`；折叠态保持最低 66px 高度与单行省略，浏览器实测发生溢出时才显示 HeroUI `展开/折叠`。
- 完整文字直接在当前条目内自然换行并撑高条目，不再创建 Popover、Dialog、Portal 或浮层定位；同一列表最多展开一个，左中栏状态互不影响，三栏工作区高度与相邻栏位置保持不变。
- 删除成本统计列表专属 Popover 组件、隐藏 trigger、浮层样式、固定条目裁切及 Escape/点外部关闭合同。列表回归普通文档流，展开项会参与父级高度计算，不再被旧 Grid 行高压缩或覆盖下一项。ResizeObserver 继续按列表复用并用 animation frame 合并测量；展开态保留已确认的溢出事实，折叠后再测量，不增加逐行 observer、API、依赖、数据库或跨页面 I/O。

## 2026-08-20：无 OA 成本范围层级与有效项目边界

- 候选事实、GET/PUT、成本归集 policy 和 canonical 查询保持不变；前端直接按已有标签 `path` 展示主子层级，不新增 DTO、接口、组件层、依赖或 I/O。非叶子主标签只显示一次且没有 checkbox，叶子按稳定 code 选择；单层 path 本身就是可选叶子。
- 服务端现有 AppSettings 归一化边界增加“每个已提交虚拟项目至少一个标签”的写入校验；`projects=[]` 继续合法，历史读取继续保留不可用选择。校验失败不写设置、不推进 CAS version、不产生部分状态。
- 删除旧 `leafLabel()` 和扁平双列渲染合同，不保留隐藏 fallback。前端保存按钮与行内提示提前阻止空项目，后端校验仍是最终事实边界。
- 候选只在打开/保存抽屉时读取，前端线性分组并 memoize。生产实测确认通用成本快照会为该抽屉读取不消费的 OA payload 并分类收入/已有 OA 流水，因此改为 repository 专用只读快照：先按 active 关系与方向缩小候选，再复用同一 canonical 分类投影；删除继续调用通用 explorer 快照的旧路径，不增加 endpoint、表、worker、cache 或跨页面刷新。

## 2026-08-18：付错退款收口为关系净支出

- 两种归因视图先在 active 关系内计算 `净支出 N = 支出原额 B - 明确付错退款 R`；普通收入不参与。删除把退款收入独立拆成负成本行的旧路径，右栏、搜索、汇总、分页和导出统一只消费净归因行。
- 当时为保留多支出、多账户日期而做的两级比例归因已由 2026-08-28 首节删除；当前不得把 OA 单元映射到具体流水、账户或标签。银行、标签、时间只消费真实 `N`，项目、费用类型只消费 canonical OA 单元成本 `C`。
- 当时 `O!=N` 使用 `OA 单元原额 × N / O` 自动缩放的路径已删除；当前只有 `O=N` 自动，`O!=N` 必须经人工逐 OA 单元分配并满足 `C+X=N`。
- allocation detail API shape 不变，付款证据仍保留原始正金额和方向；成本抽屉把退款显示为 `-35.00`，同时展示本项净成本、支出原额和关系净支出。`bank|time|bank_tag` 仍展示真实 1050 支出与 35 收入。
- 未新增数据库、repository 查询、worker、read model、cache、兼容分支或前端隐藏逻辑；旧的独立退款成本事件循环已从唯一 policy 链删除。

## 2026-08-18：成本统计复用银行规范分类投影

- 生产 PostgreSQL 路径删除成本专属 `_postgres_category_provider`、分类/确认表重复装载和 Python 自动重分类。
- `PostgresCostStatisticsCanonicalRepository` 在原有 `REPEATABLE READ READ ONLY` 快照内，对当前银行流水 ID 一次调用银行分类 owner 的 `effective_category_projection_rows(...)`；`bank|time|bank_tag` 不读取 OA/关系，两种归因视图只扩展命中关系的完整成员。
- canonical-only confirmation 通过规范分类 SQL 的 UUID/legacy identity 连接解析为公开流水 ID；人工确认不再错误落入“未标记”，内部转账也不再由成本模块维护第二套算法。
- 自动识别和人工覆盖的 `internal_transfer` 统一形成一个“内部往来款”主/子标签；无 effective code 的外部往来候选即使携带展示文案也继续归入“未标记”。
- API shape、五视图人口和金额口径不变；空银行集合跳过分类查询，不新增 cache、read model、worker、migration 或 fallback。

## 2026-08-18：五视图银行事实边界与无 OA 范围

> 本节“退款作为独立负成本事件”的金额实现已由同日“付错退款收口为关系净支出”决策取代；原始视图边界和无 OA 范围仍有效。

- 五个视图统一消费银行成本事件并按银行交易日期筛选。已完成 OA 关系中的支出为正成本；同一 active 关系中明确标记“付错退款”的收入为负成本；普通收入不进入成本统计。
- 每条银行事件独立按关系内 OA 原始金额比例拆到 OA 归集单元，按分采用最大余数法闭合。详情公开 OA 原始金额、比例、银行事件原额，以及关系总支出、退款、实际现金成本、差额和现金比例。
- 关系中存在进行中 OA 时整组不统计；零/缺失权重只保留内部防除零判断，不新增“待分摊”或“数据异常”产品状态。真实 OA 费用类型缺失时进入“未填写 OA 费用类型”，不再用质量排除隐藏。
- 原有“成本统计标签规则”抽屉原位收敛为“无 OA 成本范围”：虚拟项目名和标签默认空；候选仅来自当前实际无 active OA 关系的支出流水标签；命中选中标签后仍按每笔流水复核无 OA。设置对全部历史期间生效。
- `按 OA 费用类型`更名为`按费用类型`，无 OA 行进入“无 OA 分类”。两种归因视图右栏更名为“成本明细”，详情请求由行级 `row_kind` 决定。
- 删除旧 OA-first 金额、OA 完成日期范围、混合支付账户、完整银行收支 time/tag、默认全选标签、按页签推断详情类型和标签归档时静默移除成本选择等旧链路；不新增表、read model、worker、cache、endpoint 或第二个抽屉。

## 2026-08-18：支付申请费用类型读取修复

- OA Mongo 归一化按表单读取权威字段：支付申请读取可配置 `category`，日常报销明细读取 `purposeType`；删除两种表单共用候选键和递归同名字段扫描。
- OA projection 版本提升至 `2026-08-18-form-specific-expense-type-v8`，通过既有幂等同步重投影历史 OA；不直接修改 PostgreSQL 投影数据，也不按申请事由猜测费用类型。
- 修复验收以源字段真实有效数量为准：有效 `category` 必须恢复为标准费用类型，空值或非法值继续显示“未填写 OA 费用类型”，不承诺无证据地把历史缺失数强制归零。

## 2026-08-16：详情抽屉统一公开字段视图

- OA 成本与付款流水详情继续使用共享 HeroUI `AppDrawer`，内容收敛为紧凑的分区 label/value 行；抽屉宽度统一为 800px，并保留窄屏自适应。
- 删除用户无须知晓的 `OA 单号`、`子付款项 ID`、`关系组`，将关系校验改为业务可理解的“金额核对”；不改变成本归集、金额计算、详情 API 或导出 I/O。
- 没有新增 backend、数据库、read model、worker、缓存或兼容链路；旧详情字段和旧排版不再参与渲染。

## 2026-08-14：OA 当前金额归集与详情链路拆分

> 本节的 OA-first 金额、OA 日期、混合账户和收入隔离口径已由 2026-08-18 决策取代；仅保留当时的详情 endpoint 拆分历史。

- `按项目`、`按银行`、`按 OA 费用类型` 改为 OA-first：年/月按 OA 完成时间，支付申请按 OA 当前金额，日常报销逐个按子付款项当前金额、项目、费用类型和内容归集。多 OA、多流水关系不按流水做比例或顺序拆分。
- active 正式关系只证明付款关联。关系内单一账户用于银行归属；多个账户统一显示 `混合支付账户`。收入不进入 OA 成本，关系组金额差异只在详情中如实展示，不修改归集金额。
- 旧单一 `/transactions/{id}` 详情链删除，改为银行事实 `/bank-transactions/{id}` 与 OA 成本 `/allocations/{id}` 两条有界读接口。页面右栏统一命名为“OA 成本归集明细”，并显示“归集金额”；真实流水只出现在“关联付款流水”证据区。
- OA 投影补齐 `approved_at`，来源为已完成 Mongo 文档的 `modifiedTime`；版本提升后原子重建当前投影。未新增表、索引、read model、worker、cache 或 fallback。
- 缺少完成时间、项目、费用类型、正数金额或子付款项 ID 的归集单元明确排除并返回质量统计；重复归集单元跨 active relation 时返回完整性冲突。不存在“成本待分配”或表头金额回退。
- 口径限制：这是“有付款关系证据的已完成 OA 当前成本”，不是部分实付或退款净额。没有 canonical 部分付款/退款分摊事实时，不从 N:M 关系猜测。

## 2026-08-10：紧凑筛选栏与按时间双栏布局

- 五个视图改用共享 HeroUI 切换控件；非时间视图复用共享搜索和“全部 + 年/月”控件，标题筛选带收敛为 52px 紧凑高度。按时间视图使用固定高度的 `15% / 85%` 永久展开时间栏与内部滚动明细表。
- 删除页面私有搜索、scope picker 与旧 CSS；canonical API、scope 参数、下钻、导出和 direct-read I/O 不变。

## 2026-08-07：流水详情右侧抽屉与完成态 OA 回归锁定

> 详情 endpoint 与 OA 金额分配口径已由 2026-08-14 决策取代；本节只保留抽屉交互和 scoped canonical read 的历史决策。

- 删除成本统计专属详情 modal 及其大卡片样式，复用全站 HeroUI `AppDrawer`、`Chip`、`Separator` 和 `Button`；详情内容改为扁平分区与字段行，不保留解释性文案。
- 点击流水先打开抽屉，再发起既有单次详情请求；loading 使用无文字 skeleton，失败与重试只在抽屉内处理，不占用 explorer/导出状态。
- 生产性能探针发现单条详情仍走全期间 snapshot 后，将详情请求已有的 `scope/view` 下推到 canonical repository，并固定 `include_statistics=false`；不增加缓存、read model、endpoint 或兼容路径。
- 保留 policy 中共享的完成态 OA 判定作为唯一业务入口，不新增过滤器、API、查询或状态；补齐 project、bank、expense_type 的 policy/API 回归，明确进行中 OA 不进入成本视图，time/bank_tag 继续只读银行事实。
- 删除 `CostTransactionDetailModal` 和旧 `.cost-detail-modal*` 样式，不保留兼容容器或双路径。

## 2026-07-26：改为直接 canonical read

- 保留现有 API、视图、标签规则和导出业务合同。
- 删除 Cost 专属 read model、投影、worker、scope、source version、runtime service 与生命周期入口。
- 每个请求从一个 PostgreSQL `REPEATABLE READ READ ONLY` 快照读取统一事实源。
- 业务计算集中在无 I/O 的 `CostStatisticsPolicy`；repository 只负责读取，route 只负责 HTTP。
- Audit 改为直接 canonical proof，Cost 不再出现在 App Status read-model/worker 诊断中。
- migration `0126` 终止遗留 Cost runtime 行并删除旧 Cost read-model 表。

这是本模块当前唯一读链。历史的 parent/shard、freshness gate、dependency defer、conditional publish 和 Redis cache 设计已经被本决策取代，不再作为实现依据。

## 2026-07-28：局部加载与 canonical scope 下推

- 删除后续 explorer 请求中的 `setLoadedExplorer(null)` 全页清空链路；保留已加载的上游栏位，按 `surface / children / rows` 只替换受影响区域。
- 页头统计仅在本次页面会话的首次 explorer 请求计算；后续请求使用 `include_statistics=false`，页头沿用同一 canonical 响应得到的统计值。
- repository 只在后续非 `all` 请求下推 `txn_month` 范围；成本视图以命中银行流水筛 active relation 后扩展全部 relation 成员，避免破坏跨月份配对。
- `time / bank_tag` 后续请求不读取 OA 与配对关系。所有路径仍在单个 `REPEATABLE READ READ ONLY` 快照内直接读取 canonical tables，没有 read model、cache、worker 或 fallback。
- 按标签桌面列宽为 `20% / 20% / 60%`；支出在上、收入在下，零金额方向项不进入金额区。

## 2026-07-28：日常报销付款明细级成本分配

> 本节的“仅精确等额才拆分、否则流水金额只计一次”已由 2026-08-14 OA-first 口径取代，不再是当前业务事实。

- repository 和 API 保持不变；现有 canonical OA payload 已包含稳定 `expense_item_id`、项目、费用类型、内容和金额，不新增表、read model、worker 或查询。
- 支付申请继续作为一个 OA 分配单元；日常报销在 `CostStatisticsPolicy` 内展开为 `expense_items` 分配单元。
- 单流水仅在全部单元 ID 有效唯一、金额为正且合计按分等于流水金额时拆分。其它情况不做比例、顺序或子集推断，流水金额只计一次。
- 歧义场景仅保留所有单元完全一致的共同项目/费用维度，否则使用 `未归集项目` / `未分类`；删除成本统计运行时的 `多项目` / `多费用类型` 合成口径。
- project、bank、expense_type、详情与导出共享同一组分配行；`transaction_count` 继续按银行流水去重，详情 `linked_oa_count` 按 OA 去重而不是按付款明细计数。
- canonical repository 直接从同一 snapshot 的 `app.oa_applications.normalized_payload` 映射成本 policy 所需父单/付款明细字段，避免 OA repository 构造完整 record 并递归复制成本页面不消费的附件与发票树；不减少成本归因字段，也不新增查询或缓存。

## 2026-07-29：direct-read 热路径与交互请求收敛

- 保持 canonical API 直读，不引入 Redis、read model、新 endpoint 或 fallback。
- bank snapshot 不再读取成本计算未消费的 `raw_payload`；账户解析器按 snapshot 构造一次，不再按流水逐行构造。
- explorer route/service 删除从未命中的 ETag/304 占位合同，响应继续使用 `private, no-cache` 并每次读取当前事实源。
- 时间范围选择在一次 session state 更新中同时提交 mode/year/month，并在发请求前清除下游选择；项目、银行、OA 费用类型、标签视图不再先携带旧选择请求一次、随后再补发清理后的请求。
- 页面切换和三栏下钻继续局部加载；同一次用户操作最多产生一个 explorer 请求，不清空整页。

## 2026-07-29：当前视图搜索、真实时间字段与自动分页

> 本节的两套事实域描述已由 2026-08-18 的统一银行成本事件取代；其中自动滚动分页合同已由 2026-08-31 的显式分页取代。

- explorer 增加一个最长 200 字符的规范化 `query`，并将其绑定 cursor；policy 在聚合、facets、summary 和分页之前过滤当前视图事实行，不增加 repository 查询。
- `project / expense_type` 只搜索成本归因 allocation；`bank / time / bank_tag` 只搜索完整 canonical 银行事实。三个银行流水视图不渲染“未配对OA / 未分类”占位字段。
- 主标签和子标签复用同一个仅支出、混合、仅收入、零金额排序键；UI 将支出放在收入上方，用独立笔数字段和既有方向颜色表达。
- 五个视图复用一个紧凑搜索框，使用 200ms debounce、IME composition 保护和 AbortController；项目/银行三栏采用 `24% / 24% / 52%`，右侧明细禁止横向滚动。
- 历史实现曾复用 cursor API 在表格接近底部时自动追加；该交互和对应滚动监听现已删除，不再是当前合同。
- 保持 canonical API 直读，不引入 Redis、read model、worker、新 endpoint、依赖或跨页面 I/O。

## 2026-08-31：五视图显式分页与紧凑时间选择

- 五个视图只对右侧流水/成本明细分页，固定每页 20 条；左侧和中间分面继续作为完整导航集合。
- 保留现有 `row_count + next_cursor` API，通过上一页/下一页逐页建立少量 cursor 索引；每次成功后替换当前页 rows，不追加、不预取、不缓存历史页行数据。
- 删除表格滚动监听、接近底部请求、旧 loading-more 状态和旧样式；分页失败保留当前已确认页并重试同一目标页。
- `按时间`左栏复用现有 HeroUI `BusinessPeriodPicker` Popover，删除常驻 inline 年/月矩阵；筛选、下钻、搜索、规则保存和手动刷新回到第一页，详情开关不改变页码。
- API、数据库、read model、worker、统计口径和导出完整性合同均不变化。

## 2026-07-29：按标签零金额方向项清理

- 主标签和子标签的金额区复用同一个 `DirectionAmount` 合同：对应金额为零时，方向字样、金额值和无障碍标签整体不渲染；非零金额的颜色、顺序和格式保持不变。
- 笔数区继续显示 `支出/收入 N 笔`，包括零笔；页面总计、排序、搜索、下钻和 API DTO 不变。
- 删除旧 `hideZeroValue` 语义，不保留 CSS 隐藏、调用方分支或兼容属性；本次没有新增 I/O、状态、依赖或跨页面行为。
## 2026-08-10 - 导出中心原生控件收敛

- 成本导出中心的按钮、复选框、单选组和日期输入统一为 HeroUI 原生组件，普通导出抽屉从 `xl` 收敛为 `lg`。
- 导出类型、列选择、日期范围、预览和下载 API 合同不变；删除原生 input 的旧私有样式，不保留双控件路径。
- 成本页继续 direct canonical read，不引入 read model、worker、cache 或跨页面 I/O。
