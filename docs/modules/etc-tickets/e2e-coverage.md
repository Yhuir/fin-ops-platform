# ETC票据管理 Spec-first E2E Coverage

本文件把 `e2e-spec.md` 的 ETC 票据管理 Browser 合同映射到自动化覆盖。

| Spec ID | 状态 | 当前覆盖 | 缺口/说明 |
| --- | --- | --- | --- |
| `ETC-TICKET-E2E-001` | `covered` | `web/e2e/etc-tickets-flow.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`web/src/test/EtcApi.test.ts`、`tests/test_etc_backend.py` | Browser 覆盖 `/etc-tickets` ready、标题、未提交/已提交状态切换、计数和未提交业务批次行，并覆盖首屏 `GET /api/etc/business-batches` 暂时 503 时显示错误、不显示普通空态、点击刷新后恢复批次和发票明细且无可见错误残留；组件/API 覆盖 task-only orphan 不进入业务批次列表、新建批次不由前端创建空 task、筛选计数与 items 口径一致。 |
| `ETC-TICKET-E2E-002` | `covered` | `web/e2e/etc-tickets-flow.spec.ts`、`web/e2e/imports-etc-invoices-flow.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`web/src/test/CandidateGroupGrid.test.tsx`、`tests/test_workbench_sql_runtime.py`、`tests/test_etc_backend.py` | Browser 覆盖 ETC 发票明细表、发票号、批次号、ETC 票数量和补充凭证数量；ETC 导入 fan-out 额外验证导入后的 business batch row 展示 `externalEtcBatchId` 批次号；后端/组件覆盖 `etc_invoice_summary` 展开明细、展示金额、结构化 `amount_value` 和 searchable text。 |
| `ETC-TICKET-E2E-003` | `covered` | `web/e2e/etc-tickets-flow.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`tests/test_etc_backend.py` | Browser 覆盖提交 OA、创建草稿 dialog、批次证据、单次 `oa-draft` POST、`oa-draft` 暂时 503 时错误可见/不进入 OA 提交确认伪成功/dialog 保持可重试、重试成功后 OA 提交确认 dialog、打开草稿入口和成功后无可见错误残留；后端覆盖创建草稿前 linked task 一致性补偿。 |
| `ETC-TICKET-E2E-004` | `covered` | `web/e2e/etc-tickets-flow.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`tests/test_etc_backend.py`、`tests/test_workbench_sql_runtime.py` | Browser 覆盖单次 `manual-oa-status` POST、`manual-oa-status` 暂时 503 时错误可见/不切 submitted bucket/提交确认保持可重试、重试成功后 submitted bucket、提交 OA 入口消失、人工确认已提交状态、批次证据、严格浏览器错误捕获和成功后无可见错误残留；后端覆盖业务批次闭环、submitted list/filter counts 和 Workbench summary 创建。 |
| `ETC-TICKET-E2E-005` | `covered` | `web/e2e/etc-tickets-flow.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`tests/test_etc_backend.py`、`tests/test_workbench_relation_command_service.py`、`tests/test_workbench_pair_relation_service.py`、`tests/test_platform_runtime_boundary_guards.py` | Browser 覆盖未提交 business batch delete 第一次暂时 503 时错误可见、确认弹窗和原批次行保持、第二次重试 200 后弹窗关闭、列表刷新为空且失败文案清除；Browser 也覆盖已提交 bucket 下 reset/delete 第一次暂时 503 时带 submitted expectedVersion/reason、不误删已提交批次、不改 tab 计数、确认弹窗保持可重试，第二次 200 后才清空已提交列表且失败文案清除；后端覆盖已提交批次 delete/reset 释放发票、取消 summary relation、distribution stale 不阻断 canonical command、缺 command service fail-fast、不恢复旧 OA+银行二栏关系；组件覆盖删除确认、失败保持和 local reset UI。 |
| `ETC-TICKET-E2E-006` | `covered` | `web/e2e/etc-tickets-flow.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`tests/test_etc_reconciliation_service.py`、`tests/test_etc_backend.py`、`tests/test_object_storage_repository.py`、`tests/test_file_object_storage.py`、`web/src/test/EtcApi.test.ts`、`web/src/test/ImportCenterPage.test.tsx` | Browser 覆盖 ticket-root source upload 第一次暂时 503 时错误可见、不追加文件、上传入口保持可重试、第二次重试 200 后追加 `ticket-root-upload.txt` 且失败文案清除；Browser 也覆盖 source file delete 第一次暂时 503 时错误可见、删除确认弹窗和文件行保持、第二次重试 200 后弹窗关闭、文件列表刷新为空且失败文案清除；后端/API 覆盖 source file 先落对象存储、对象存储失败结构化 503、无半写 source file/版本/审计、票根网 TXT 编码、ZIP preview 大文件 timeout 配置。真实大 ZIP/Nginx/对象存储权限和上传中断归 `ETC-TICKET-E2E-010`。 |
| `ETC-TICKET-E2E-007` | `covered` | `tests/test_etc_backend.py`、`tests/test_etc_reconciliation_service.py`、`tests/test_import_service.py`、`web/src/test/EtcTicketManagementPage.test.tsx`、`web/src/test/EtcApi.test.ts` | 覆盖省略 `taskId` 创建 linked task + active business batch、创建失败 tombstone 新 task、durable import active session/retry/idempotency、restart 后 OA 草稿前补齐 linked task、deleted task 不 rehydrate。 |
| `ETC-TICKET-E2E-008` | `covered` | `tests/test_workbench_sql_runtime.py`、`tests/test_workbench_pair_relation_service.py`、`tests/test_workbench_relation_command_service.py`、`web/src/test/CandidateGroupGrid.test.tsx`、`web/e2e/etc-tickets-flow.spec.ts` | 后端覆盖 submitted business batch -> open `etc_invoice_summary`、active relation 过滤陈旧 summary、delete/reset 后 summary 消失、ETC metadata 释放和 summary active relation 取消；Browser 覆盖人工 submitted 的页面侧前置事实，Workbench 跨页最终 smoke 仍由 staging/下游模块补。 |
| `ETC-TICKET-E2E-009` | `covered` | `web/e2e/permissions-role-matrix.spec.ts`、`web/src/test/EtcTicketManagementPage.test.tsx`、`tests/test_etc_backend.py`、`tests/test_platform_runtime_boundary_guards.py`、`tests/test_rabbitmq_staging_preflight.py`、`tests/test_oa_projection_sql_runtime.py`、`tests/test_mongo_oa_adapter.py` | 覆盖 role matrix 页面读取和高风险入口；Browser read-export 下提交 OA、新建批次、删除、source file 上传、确认对账和人工核对动作禁用且零 mutation；static guard 禁止旧 `/api/etc/batches*` 后端兼容入口、前端测试 mock 假后端、`/api/etc/invoices/revoke-submitted` 和旧 ETC `oa-status/refresh` mock 回归；ETC OA 自动检测 route/worker/字段移除、旧 pickle 兼容和非 ETC OA 能力不受影响。若新增独立 ETC 按钮权限，需补更细按钮矩阵。 |
| `ETC-TICKET-E2E-010` | `external-risk` | `bash scripts/verify.sh infra-smoke` staging gate、runtime worker/import/read model tests、ETC backend/import/workbench tests | 本地 contract 已覆盖 import job、business batch、source file、Workbench summary、dirty/readiness、Browser 页面主链路、business-batches 首屏 GET 失败恢复、OA draft mutation 暂时失败重试恢复、manual OA status mutation 暂时失败重试恢复、未提交和已提交 business batch delete/reset mutation 暂时失败重试恢复、source file delete mutation 暂时失败重试恢复和 ticket-root source upload mutation 暂时失败重试恢复；真实 PostgreSQL/RabbitMQ/Redis/systemd/OA/对象存储/Nginx、大 ZIP、真实对象存储写入中断、import confirm 和全页面 drain 必须在 staging/runtime smoke 验证。 |

## Operation latency baseline

本轮已为 `web/e2e/etc-tickets-flow.spec.ts` 接入 Playwright `operation-latency-*.json` 附件。当前记录的操作覆盖：页面打开、business-batches 首屏失败后的刷新重试、未提交/已提交 bucket 删除弹窗打开、删除失败与重试、source file disclosure、source file 删除失败与重试、ticket-root 上传失败与重试、提交审批、创建 OA 草稿失败与重试、人工已提交失败与重试，以及未提交批次创建草稿到人工已提交 bucket 的 happy path。

## 下一轮补测建议

1. staging 运行真实 ETC ZIP/source file/OA 草稿 smoke，确认对象存储、Nginx、OA iframe/session 和附件上传。
2. staging 运行 ETC import -> business batch -> manual submitted -> Workbench summary -> 税金/成本/search fresh 的真实 worker drain smoke。
3. 对生产历史 migration/cleanup 先 dry-run，再在运维窗口 execute，并做 Workbench paired/open 口径 smoke。
4. 若新增 ETC 页面 import confirm 或其它 mutation 级网络恢复 Browser 主链路，必须捕获 destructive mutation 失败、relation command failure 和页面恢复状态；OA draft、manual OA status、未提交/已提交 business batch delete/reset、source file delete 与 ticket-root source upload 暂时失败重试已由本地 Browser 覆盖。
