# 权限与审计 Spec-first E2E Coverage

本文件把 `e2e-spec.md` 的权限与审计横切合同映射到自动化覆盖。

| Spec ID | 状态 | 当前覆盖 | 缺口/说明 |
| --- | --- | --- | --- |
| `PERM-E2E-001` | `covered` | `web/e2e/app-shell.spec.ts`、`web/e2e/permissions-role-matrix.spec.ts`、`web/e2e/bank-details-filtered-export-permissions.spec.ts`、`web/e2e/tax-offset-flow.spec.ts`、`web/src/test/SessionGate.test.tsx`、`tests/test_auth_guard.py`、`tests/test_session_api.py` | Browser 覆盖 admin/full/read-only/forbidden/expired shell gate、AppHealth protected dashboard API 零调用、银行明细/tax forbidden/expired 零 protected API；后端覆盖 session/auth 401/403 contract。 |
| `PERM-E2E-002` | `covered` | `web/e2e/permissions-role-matrix.spec.ts` | Browser 覆盖 `read_export_only` 打开 16 个非 admin 页面并断言全程 mutation API 为 0、dashboard API 为 0、严格浏览器错误为 0。 |
| `PERM-E2E-003` | `partial` | `web/e2e/permissions-role-matrix.spec.ts`、`web/e2e/workbench-permissions-flow.spec.ts`、`web/e2e/bank-details-filtered-export-permissions.spec.ts`、`web/e2e/output-invoice-collections-flow.spec.ts`、`web/e2e/input-invoice-usage-flow.spec.ts`、`web/e2e/tax-offset-flow.spec.ts`、`docs/modules/permissions-and-audit/write-entry-inventory.md`、`tests/test_permissions_write_entry_inventory.py`、`web/src/test/*Page.test.tsx` | 已覆盖 settings/tax/三类 import/no-OA 高风险入口、Workbench 逐入口、银行明细分类/自动标签、成本统计标签准入规则、销项收款状态/红蓝票/收据/编号设置、进项使用支付规则/OA reverse、税金保存/导入；本轮补强 role matrix 对关联台列顺序拖拽 settings 保存入口、open candidate 选择后的确认/异常/撤回入口、已配对候选撤回入口、现金处理行级菜单、已处理异常/已忽略恢复弹窗、银行明细分类确认、银行明细未匹配人工待分类、成本统计标签规则、pending invoices 选择已有发票、支出/收入规则保存、收入状态按钮、OA pending confirm/link-bank/支出流水无需开票规则、batch accounting submit 与 submitted bucket withdraw、turnover tag/closure/extra 编辑入口、ETC submit/new/delete/source file 上传/确认对账/人工核对动作的 Browser read-export 零 mutation。当前已知页面写入口已在 `write-entry-inventory.md` 逐页登记；`tests/test_permissions_write_entry_inventory.py` 会双向校验 `pageRegistry.tsx` 与 inventory row 一致、`pageRegistry.tsx` 与 role matrix readable route 一致、每个非 admin route 都进入 role matrix read-export smoke、covered row 必须引用 Browser E2E 证据、Browser E2E 证据路径必须解析到当前文件或匹配真实 glob、covered-browser row 必须有 dynamic opener 或页面级静态覆盖证明、dynamic/static registry 只能引用当前 `covered-browser` 模块且不能重复归类、role matrix opener id 与 inventory 双向一致、mutating feature API client 必须映射到 inventory、写控件关键词 registry 与 role matrix pattern 双向一致，并校验源码中保存设置、保存凭据、清空密码、保存收据编号设置、创建正式收据、确认关系、创建 OA 草稿、确认已支付、关联支出流水、关联 OA 项、确认闭环、关联台现金处理、ETC 人工核对、自动标签规则和导入 preview/confirm 等 sentinel 文案仍存在且已登记；`permissions-role-matrix` 的 read-export 全页面 smoke 会扫描 visible enabled DOM 控件，并通过 opener registry 在关联台列拖拽 handle、关联台未配对候选动作、关联台已配对撤回动作、关联台现金处理行级菜单、关联台已处理/已忽略恢复、银行分类确认、银行人工待分类、银行明细自动标签、成本统计标签规则、no-OA 标签、pending invoice 支出/收入规则、收入批量、进项支付规则、进项 OA reverse、销项收款状态规则、销项收据历史、OA pending 进行中/规则、ETC 对账流程、batch accounting 选择、batch accounting 已提交撤回、turnover 标签、turnover 明细等动态区域复跑扫描；没有 dynamic opener 的导入页、tax-offset、settings 和 AppHealth 已进入静态覆盖 registry。写控件关键词 registry 已包含确认关联、异常处理、取消异常处理、撤回忽略、确认拆分/撤回、确认买票、确认为买票、确认为过账、取消现金处理、保存补充信息、拖动列、提交/继续异常等深层动作；OA reverse preview 是 read-like POST 例外，仍断言创建 OA 草稿、batch 和 manual status durable write endpoints 零调用；若出现保存/提交/确认导入/撤回/删除/关联/写回/拖动列等高风险动作短语且未禁用会失败。本项仍为 partial，因为尚未由 role matrix 自动打开的所有页面特定抽屉/弹窗、真实 OA/代理/生产审计仍需 staging/production 或后续更深爬取。 |
| `PERM-E2E-004` | `covered` | `web/e2e/permissions-role-matrix.spec.ts`、`web/e2e/settings-data-reset-flow.spec.ts`、`web/e2e/app-shell.spec.ts`、settings/import/no-OA component tests | Browser 覆盖 `full_access` 可执行普通 settings 保存 POST、返回 200、显示成功反馈且无隐藏浏览器错误；同时可见 import file input、可见 no-OA 提交入口，但不能进入访问账户、数据重置或 AppHealth dashboard。 |
| `PERM-E2E-005` | `covered` | `web/e2e/permissions-role-matrix.spec.ts`、`web/e2e/app-shell.spec.ts`、settings/AppHealth component/API tests | Browser 覆盖 admin settings 访问账户/OA 凭据/data reset、AppHealth dashboard 可见且 admin-only API 被调用；本轮补强 admin 访问账户管理，新增只读导出用户并断言普通 `POST /api/workbench/settings` body 写入 `allowed_usernames`、`readonly_export_usernames`、`admin_usernames`，保存返回 200 后页面继续显示该持久化账户；同时覆盖 admin 维护 OA 申请人凭据，完成一次 `PUT /api/workbench/settings/oa-applicant-credentials/fan_zufang`、返回 200、密码输入清空、页面不回显密码，再完成一次 `DELETE /api/workbench/settings/oa-applicant-credentials/fan_zufang`、返回 200、显示未配置，且普通 settings 保存 body 不含密码/凭据字段；还覆盖 admin 打开数据重置影响确认和 OA 密码复核弹窗，证明密码前 `确认清理` 禁用、填入密码后启用、取消后不创建 reset job；还覆盖 admin 打开销项收款 `收据编号设置` drawer、保存一次 `PUT /api/output-invoice-collections/receipt-settings`、返回 200 且无隐藏浏览器错误。 |
| `PERM-E2E-006` | `covered` | `tests/test_auth_guard.py`、`tests/test_workbench_settings_sync_api.py`、`tests/test_oa_manual_import_api.py`、`tests/test_settings_data_reset_service.py`、`tests/test_oa_applicant_credentials_api.py`、`tests/test_tax_offset_api.py`、`tests/test_pending_invoice_api.py`、`tests/test_bank_auto_tag_rules_api.py`、`tests/test_turnover_ledger_api.py` | 后端覆盖 read/write/admin guard、readonly export representative routes、settings/OA manual import body actor spoofing、data reset/OA credential/tax/pending/bank tag/turnover mutation 403 和下游写服务零调用。 |
| `PERM-E2E-007` | `covered` | `tests/test_audit_service.py`、`tests/test_workbench_auth_context_idempotency.py`、`tests/test_bankdetail_write_uow_contract.py`、`tests/test_turnover_ledger_uow_contract.py`、relation/import/business service tests | 覆盖 actor/tenant/action/metadata、事务内 audit + dirty/outbox、idempotency 和 rollback 防半写。生产审计查询/导出仍为 `PERM-E2E-010`。 |
| `PERM-E2E-008` | `covered` | `tests/test_postgres_migrations.py`、`tests/test_app_postgres_mode_integration.py`、`tests/test_oa_applicant_credentials_api.py`、`tests/test_settings_data_reset_service.py`、settings/input/OA tests | 覆盖 SQL/ready payload secret safe、OA credentials 无密码回显、data reset 密码不泄露、settings payload 不含 OA secret。 |
| `PERM-E2E-009` | `covered` | `web/e2e/permissions-role-matrix.spec.ts`、`web/e2e/app-shell.spec.ts`、`web/e2e/bank-details-filtered-export-permissions.spec.ts`、page-level Browser specs | Role matrix 和 app-shell 现在捕获隐藏浏览器错误；expired session 的预期 401 resource error 只在 app-shell expired gate 中显式豁免。 |
| `PERM-E2E-010` | `external-risk` | staging/production smoke、`bash scripts/verify.sh infra-smoke` 的 authenticated HTTP/SSE/runtime gates、后端 auth/audit tests | 真实 OA 菜单/角色同步、生产 token 过期、代理层下载 header、生产审计查询/导出和真实安全审计必须在 staging/production 验证。 |

## 下一轮补测建议

1. 把 `PERM-E2E-003` 的缺口继续收敛：新增页面/route、visible enabled 写控件和 opener registry 双向一致性已有 gate，下一步继续向 registry 增加更多页面特定抽屉/弹窗 opener，优先覆盖尚未由 role matrix 安全打开的深层 drawer/dialog。
2. 持续维护 `write-entry-inventory.md`：从 `useSessionPermissions()` 调用点和 mutating API client 生成候选写入口清单，再映射到页面 E2E。
3. staging 验证真实 OA role sync、真实 token 过期、真实导出 header 和生产审计查询/导出。
