# 进项发票使用情况 Spec-first E2E Coverage

本文件把 `input-invoice-usage` 的 Spec ID 映射到自动化测试。状态定义见 `docs/dev/spec-first-e2e-audit.md`。

## 覆盖矩阵

| Spec ID | 状态 | 当前覆盖 | 缺口/说明 |
| --- | --- | --- | --- |
| `IN-USAGE-E2E-001` | `covered` | `web/e2e/input-invoice-usage-flow.spec.ts`、`web/e2e/input-invoice-relation-fanout.spec.ts`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`tests/test_input_invoice_usage_api.py` | Browser 覆盖 direct rows 首屏、发票/支付状态/OA/流水列、首屏 `page_size=20`、全局 filter-options 不从当前页伪造、销方筛选 URL contract、开票日期排序、page-size 切换和可见行同步；也覆盖 rows 首屏暂时 503 时错误 alert、错误态空行、普通空态消失、导出禁用和点击重试/refetch 后 rows/pagination/export 恢复。Vitest/API 覆盖搜索、筛选、排序、分页和 response shape。 |
| `IN-USAGE-E2E-002` | `covered` | `web/e2e/input-invoice-relation-fanout.spec.ts`、`tests/test_input_invoice_usage_service.py`、`tests/test_workbench_relation_read_facade.py` | Browser 已证明 candidate OA/流水证据显示但支付状态保持 `待处理`，Workbench confirm 后重新进入页面显示 linked 证据和 `已支付`。 |
| `IN-USAGE-E2E-003` | `covered` | `web/e2e/input-invoice-relation-fanout.spec.ts`、`web/src/test/InputInvoiceUsageFiltersAndDrawers.test.tsx`、`tests/test_input_invoice_usage_oa_reverse_service.py`、`tests/test_input_invoice_usage_api.py` | Browser 已证明 candidate/linked 发票在 OA reverse 预览中分别显示 `候选oa`/`已关联oa`，且不可勾选、不触发创建草稿 API。 |
| `IN-USAGE-E2E-004` | `covered` | `web/e2e/input-invoice-usage-flow.spec.ts`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`web/src/test/InputInvoiceUsageFiltersAndDrawers.test.tsx`、`tests/test_input_invoice_usage_oa_reverse_service.py`、`tests/test_input_invoice_usage_api.py` | Browser 已覆盖候选子集重新 preview、创建 OA 草稿、草稿创建后当前 rows direct refetch、确认已提交、manual status 后当前 rows direct refetch、submitted history 不暴露内部 batch id，并在草稿创建和已提交成功节点检查没有操作失败/保存失败/同步失败残留；Vitest/API/service 覆盖关闭确认弹窗后暂存恢复、暂存列表不展示 OA 草稿链接，以及 OA reverse 写成功后前端不再轮询 `input_invoice_usage` operation barrier、直接重新请求 rows。 |
| `IN-USAGE-E2E-005` | `covered` | `tests/test_invoice_usage_collection_sql_runtime.py`、`tests/test_input_invoice_usage_api.py`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`web/src/test/InputInvoiceUsageFiltersAndDrawers.test.tsx`、`web/e2e/input-invoice-usage-flow.spec.ts` | Vitest/API 覆盖 rows/filter-options/export-preview/export/relation-details 不返回页面级 freshness 字段，按 direct API payload/file 渲染，不自动重试、不隐藏普通空态/table、不禁用导出；API/Vitest 覆盖 direct relation detail lookup 和 `detailAvailable=false` mapper，Browser 覆盖 relation detail 不可用时 drawer 显示详情暂不可用、不泄露内部 reason、不长期 loading。旧 read-model stale/source mismatch 测试只作为 legacy projection 删除前保护。 |
| `IN-USAGE-E2E-006` | `covered` | `tests/test_input_invoice_usage_api.py`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`web/e2e/input-invoice-usage-flow.spec.ts` | 单元/API/Vitest 覆盖多关系和 `+N`；Browser 覆盖点击 `+N` 后从 direct relation detail endpoint 展开两条 OA 摘要、不显示 loading/不可用态、零 mutation。 |
| `IN-USAGE-E2E-007` | `covered` | `web/e2e/input-invoice-usage-flow.spec.ts`、`web/e2e/permissions-role-matrix.spec.ts`、`web/src/test/InputInvoiceUsageFiltersAndDrawers.test.tsx`、`tests/test_input_invoice_usage_api.py` | Browser 覆盖 `read_export_only` 用户可读列表/导出、支付规则 drawer 只读且无保存控件、OA reverse preview 只返回不可创建草稿、创建草稿/批次/manual status/payment rules save 等 durable write endpoint 零调用；role matrix/API/Vitest 覆盖页面读取、权限字段和只读 drawer mapper。 |
| `IN-USAGE-E2E-008` | `covered` | `tests/test_input_invoice_usage_api.py`、`web/src/test/InputInvoiceUsagePage.test.tsx`、`web/e2e/input-invoice-usage-flow.spec.ts` | Browser 覆盖当前筛选导出预览、真实 download event、导出请求不带分页、导出样例/下载内容包含 relation/OA/payment 字段、row-limit 错误零下载；导出 drawer 不再消费 read model freshness。 |
| `IN-USAGE-E2E-009` | `covered` | `web/e2e/input-invoice-relation-fanout.spec.ts`、`web/e2e/input-invoice-usage-flow.spec.ts`、`web/e2e/tax-offset-flow.spec.ts`、`tests/test_derived_data_lifecycle_service.py`、`tests/test_input_invoice_usage_oa_reverse_service.py`、`tests/test_input_invoice_usage_payment_rules.py`、`tests/test_search_pending_sql_runtime.py`、`tests/test_workbench_relation_repository.py`、相关下游 API/Vitest 测试 | Browser 已覆盖进项使用页看到 Workbench candidate 证据 -> Workbench confirm -> 进项使用页 linked 证据 -> OA待付款、税金抵扣、成本统计分别重新读取 direct payload 并显示 confirmed 结果；也覆盖 full-access 用户保存支付状态规则时提交版本/幂等键，保存成功后当前页重新读取 rows、显示新规则版本下的支付状态，并检查没有操作失败/保存失败/同步失败残留；Vitest 锁定支付规则/OA reverse 写成功后前端直接重新请求 rows，不再等待 `input_invoice_usage` operation barrier。`/api/search` 当前没有独立前端 route，Browser E2E 不适用；API/runtime 已覆盖 Search direct payload 不回流页面 read-model refresh、遗留 SQL projection 保留 linked group jump target、`/api/search` direct payload 返回 group context；支付规则后端测试覆盖保存后不再 enqueue `input_invoice_usage` 或 `invoice_lifecycle` page read-model refresh。税务认证导入的用户可见 Browser 行为归 `tax-offset-flow` 覆盖，后端 lifecycle 测试固定 `tax_certified_import_confirmed -> invoice_lifecycle/tax_offset/tax_offset_month_cache; Search direct payload`，不错误触发 cost/OA pending 页面 refresh。OA reverse `evidence_detected` 当前没有用户可见 Browser 入口，`canRefreshStatus` 在现有 UI 流程不暴露可点击动作；service/API 测试覆盖 evidence payload -> `WorkbenchRelationCommandService.confirm_relation`、relation command 409 no half-write，以及成功 detected 后 direct response。真实 PostgreSQL/RabbitMQ/Redis/systemd direct API 收敛保留为 staging/runtime risk；未来若暴露 OA 状态重新检查按钮，再补对应 Browser E2E。 |

## 下一轮补测建议

1. 本模块本地 Spec-first ID 已覆盖；真实基础设施 direct API smoke 只用于后端外部依赖/运行环境风险，不再作为页面级 rows/filter/export 验收前置。
2. 如后续新增 OA reverse 撤销/刷新外部状态入口、全局 search UI 或支付规则下游 UI 入口，再补对应 Browser 权限和 fan-out 组合。
