# Prompt 12：补齐主表字段与银行/发票行内动作

目标：把三栏主表字段和行内动作补齐到需求文档口径。

前提：

- `11-workbench-v2-row-selection-and-drawer.md` 已完成

要求：

- OA 栏直接显示：
  - 申请人、项目名称、申请类型、金额、对方户名、申请事由、OA和流水关联情况
- 银行栏直接显示：
  - 交易时间、借方发生额、贷方发生额、对方户名、支付账户、和发票关联情况、支付/收款时间、备注、还借款日期
- 发票栏直接显示：
  - 销方识别号、销方名称、购方识别号、购买方名称、开票日期、金额、税率、税额、价税合计、发票类型
- 银行栏行内动作必须包含：
  - 详情
  - 关联情况
  - 取消关联
  - 异常处理
- OA / 发票未配对行需要有：
  - 确认关联
  - 标记异常

注意：

- 表格列会很多，必须支持横向滚动
- 保持 Excel 样式，不改成卡片

交付要求：

- 三栏字段和按钮与需求文档一致
- 状态标签和动作不会挤坏表格

验证：

- 对照 `docs/product/银企核销需求.md` 手工逐列验收
