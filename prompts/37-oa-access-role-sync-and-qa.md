# Prompt 37：OA 菜单可见性同步与权限联调 QA

## 目标

把新的 app 内访问模型与 OA 菜单可见性串起来，并补齐部署、同步和联调验收文档。

## 本次范围

- 更新 OA 集成部署口径
- 明确 app 内账户配置与 OA 菜单可见性的同步规则
- 补 QA 和回滚文档
- 不要求在本 prompt 中完成真实生产执行

## 必须完成

1. 文档明确：
   - `allowed_usernames` 之外的账户必须在 OA 菜单中也不可见
   - `readonly_export_usernames` 与 `full_access` 都属于可访问账户
   - 只有 `YNSYLP005` 是管理员
2. 更新部署说明、SQL 模板或操作指南，使其能承接：
   - 隐藏用户
   - 只读导出用户
   - 全操作用户
3. 增加手工 QA 清单，覆盖：
   - 不可见用户
   - 只读导出用户
   - 全操作用户
   - 管理员
4. 补必要测试与回归说明

## 验收标准

- 文档可指导真实环境区分三种账户类型
- QA 清单能覆盖菜单、页面、API、按钮和导出
- 与旧的“单一 `finops:app:view` 口径”冲突处已被明确替换或标注升级

## 实现提示

- 修改：
  - `deploy/oa/README.md`
  - OA 集成相关总方案文档
  - `docs/README.md`
  - `README.md`
- 如有 SQL/角色模板，也要同步口径
