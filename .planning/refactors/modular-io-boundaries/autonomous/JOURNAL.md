# Autonomous Journal

This file records autonomous execution events.

## 2026-06-23

- Autonomous runbook created.
- Dev branch workflow created.
- Stop gates created.
- Module queue initialized.
- Preflight for autonomous execution passed on direct `dev`; `dev`, `origin/dev`, and `origin/main` were aligned at `6e8ed50d`.
- Completed local slice for `bank-details:auto-tag-category-boundary`: removed unused legacy `server.py` auto-tag write finalizer/scope helper, added platform guard for route/application/settings/lifecycle ownership, and documented IO contract evidence.
- Paused by user request after the nearest stable slice; next pending boundary is `reconciliation-workbench:amount-check-query-contract`.
- Reprioritized the queue after user requested read model-first analysis: next pending boundary is now `read-models:manifest-and-boundary-inventory`, with page-specific slices deferred until read model foundation is inventoried.
- Completed `read-models:manifest-and-boundary-inventory` as analysis-only slice: documented 14 App Status read model keys, scope/event/worker/query/repository/strategy/permission/test owners, and queued query gateway/status parity as the next boundary.
- Completed `read-models:query-gateway-contract-and-status-parity`: added `read_model_manifest.py`, manifest parity tests, read model docs updates, and analysis file. The slice makes read model key/scope/event/worker/query owner/test owner contracts code-testable without changing runtime behavior. Next boundary is `read-models:refresh-gateway-force-refresh-and-operation-barrier`.
- Completed `read-models:refresh-gateway-force-refresh-and-operation-barrier`: extended the read model manifest with force refresh and operation barrier contracts, added tests tying manifest targets to `OperationFreshnessTarget` and `read_model_slo_smoke` special scope planning, updated docs and analysis. Next boundary is `read-models:repository-port-and-sql-owner-split-plan`.
- Completed `read-models:repository-port-and-sql-owner-split-plan`: extended the read model manifest with per-key `PostgresReadModelRepository` port contracts and tests proving registered methods exist with a single owner. No SQL was split. Next boundary is `read-models:workbench-active-generation-contract`.
- Completed `read-models:workbench-active-generation-contract`: added a manifest guard preserving Workbench active generation semantics and documented why Workbench must not be mechanically converted to a generic read model rebuild path. Next boundary is `read-models:bank-detail-and-bank-account-balance-contract`.
- Completed `read-models:bank-detail-and-bank-account-balance-contract`: added a manifest guard keeping `bank_detail` and `bank_account_balance` scope/repository/test contracts separate, corrected the account balance test owner, and documented the no-runtime-change boundary. Next boundary is `read-models:pending-invoice-and-oa-pending-payment-contract`.
- Completed `read-models:pending-invoice-and-oa-pending-payment-contract`: added a manifest guard preserving pending invoice page-first-screen scope, forbidden bare-all semantics, OA pending payment fan-out semantics, and disjoint repository ports. Next boundary is `read-models:invoice-lifecycle-and-usage-contract`.
- Completed `read-models:invoice-lifecycle-and-usage-contract`: added a manifest guard preserving invoice lifecycle, input invoice usage and output invoice collection scoped incremental contracts, fan-out all semantics, worker/query/permission owners and disjoint repository ports. Next boundary is `read-models:cost-tax-ledger-summary-contract`.
- Completed `read-models:cost-tax-ledger-summary-contract`: added a manifest guard preserving cost statistics queryable parent aggregate semantics, tax offset and turnover ledger fan-out contracts, primary/compat worker ownership and disjoint repository ports. Next boundary is `read-models:search-and-no-oa-bank-batch-contract`.
- Clarified autonomous state-machine update gate after user review: `03-autonomous-start.md` now has an explicit pre-commit state-machine gate, `03-REFACTOR-STATE-MACHINE.md` records that `NEXT-PROMPT.md` cannot replace `STATE.md`/`MODULE-QUEUE.md`/`JOURNAL.md`/analysis accounting, and the read-model queue remains unchanged.
- Completed `read-models:search-and-no-oa-bank-batch-contract`: added a manifest guard preserving search partitioned scoped index ownership, no-OA scoped incremental read-side ownership, fan-out `all` semantics, worker/query/permission owners and disjoint repository ports. Next boundary is `read-models:legacy-read-path-removal-guards`.
- Completed `read-models:legacy-read-path-removal-guards`: added an architecture guard classifying all direct `enqueue_read_model_refresh(...)` call sites as gateway-backed wrappers or compat-only app/query/runtime wrappers, preventing new unclassified legacy refresh producers from bypassing scope policy. Next boundary is `reconciliation-workbench:amount-check-query-contract`.
- Completed `reconciliation-workbench:amount-check-query-contract`: added a Workbench amount-check regression guard proving explicit `reconciliation_amount` from the new query/read payload wins over legacy `detail_fields.明细金额合计` fallback, and documented that global/module state-machine definitions remain unchanged. Next boundary is `batch-accounting:legacy-route-contract`.
- Completed `batch-accounting:legacy-route-contract`: added a static route boundary guard proving batch-accounting GET stays read-only through `BatchAccountingService`, and submit/withdraw route handlers do not bypass service boundaries with direct relation writes. Next boundary is `server-py:route-owner-inventory`.
- Completed `server-py:route-owner-inventory`: added a static route owner inventory guard proving every existing `routes_*.py` owner is registered, imported, and delegated from `server.py`; documented the route owner guard in runtime ownership docs. No runtime behavior changed. Next boundary is `go-hot-path:workbench-compute-admission`.
- Completed `planning:state-reconciliation-and-roadmap-alignment`: reconciled `.planning/ROADMAP.md` as the page-analysis roadmap, `04-IMPLEMENTATION-ROADMAP.md` as the modular IO phase roadmap, and `autonomous/MODULE-QUEUE.md` as the executable boundary queue; updated status labels, completion metric rules and autonomous prompt requirements. No runtime behavior changed. Next boundary remains `go-hot-path:workbench-compute-admission`.

## 2026-06-24

- Completed `planning:completion-semantics-and-queue-reclassification`: corrected queue semantics after user review. Prior `closed-autonomous` entries are now classified as `analysis-closed`, `contract-guard-closed`, `static-guard-closed`, `regression-guard-closed`, `route-guard-closed`, `inventory-guard-closed`, or `planning-closed`; these labels close their slices but do not claim module implementation closure. Go hot-path candidates are now `blocked-by-prerequisite`, and the next executable boundary is `read-models:pilot-gap-audit-and-contract-selection`.
- Completed `read-models:pilot-gap-audit-and-contract-selection`: selected `bank_detail` as the first read model implementation pilot after comparing `bank_detail`, `workbench_relation`, `pending_invoice`, and `oa_pending_payment`; documented entry points, IO gaps, freshness/force-refresh/operation-barrier gaps, legacy contamination risks and seven-category test plan. Next boundary is `read-models:bank-detail-repository-port-extraction`.
- Completed `read-models:bank-detail-repository-port-extraction`: added a narrow BankDetails read-side repository port, wired `PostgresStateStore.bank_detail_sql_read_repository` to that port, and downgraded `server.py` legacy bank detail SQL helpers to application-service delegates. Tests now prove the port does not expose unrelated read model methods and legacy helpers do not bypass the application boundary. Next boundary is `read-models:bank-detail-refresh-freshness-operation-barrier`.
- Completed `read-models:bank-detail-refresh-freshness-operation-barrier`: added BankDetails write/force-refresh response `read_model_scope_keys` and `freshness_targets`, preserved refresh enqueue via `ReadModelRefreshGateway`, and added tests for exact month operation barrier targets plus other-month outbox isolation. Next boundary is `read-models:bank-detail-legacy-contamination-removal`.
- Completed `read-models:bank-detail-legacy-contamination-removal`: removed the unused `server.py` bank detail SQL read compat helpers, moved freshness tests to the route/application public boundary, and added a guard proving `Application._get_bank_detail_*_from_sql_read_model` cannot return. Next boundary is `read-models:bank-detail-pilot-verification-and-template-revision`.
- Completed `read-models:bank-detail-pilot-verification-and-template-revision` as a verification/accounting slice: confirmed the pilot cannot be marked module-closed because remaining `server.py` bank detail scope/cache/refresh/callback helpers still need classification, migration/removal or compat-only quarantine, and production DB/worker evidence remains unavailable without staging/local `PGSQL_URL`. No template rewrite was needed; the queue now inserts `read-models:bank-detail-server-helper-quarantine` before broader server-py or Go boundaries.
- Completed `read-models:bank-detail-server-helper-quarantine`: removed unused `server.py` bank detail scope/freshness/cache/payload helpers, added a static guard proving `BankDetailsApplicationService` owns those helpers and the remaining refresh wrapper stays gateway-backed, and documented retained callback/wrapper classifications. Next boundary is `read-models:bank-detail-category-side-effect-port-extraction`.
- Completed `read-models:bank-detail-category-side-effect-port-extraction`: removed `Application._after_bank_category_confirmation_mutation(...)`, added `BankDetailCategoryMutationSideEffectPort`, injected it into `BankDetailsApplicationService`, preserved bank detail refresh / turnover all-scope fan-out / Workbench invalidation / audit semantics, and guarded the old callback from returning. Next boundary is `server-py:legacy-handler-extraction-implementation`.
- Completed `planning:queue-semantics-and-master-goal-prompt-revision`: inserted an explicit planning slice before the next implementation boundary, kept `bank_detail` as `implementation-gap-open`, clarified that `server-py:legacy-handler-extraction-implementation` is the next executable boundary rather than proof of full pilot closure, and strengthened the master goal prompt so every autonomous iteration reconciles state/queue/prompt semantics before implementation. Next boundary remains `server-py:legacy-handler-extraction-implementation`.
- Completed `server-py:legacy-handler-extraction-implementation` as an ETC business-batch legacy handler removal slice: removed definition-only `server.py` handlers that were superseded by `EtcBusinessBatchApiRoutes`, kept active `/api/etc/business-batches*` HTTP wrappers delegated to the route owner, and added a static guard preventing removed handlers or direct `EtcBusinessBatchActor` construction from returning. Next boundary is `batch-accounting:legacy-route-implementation`.
- Completed `batch-accounting:legacy-route-implementation` as a GET route owner extraction slice: moved read-only `/api/batch-accounting` query normalization and list error mapping into `BatchAccountingApiRoutes`, kept `BatchAccountingService.build_payload(..., use_sql_read_model=True)` as the read contract owner, and extended route owner/static boundary guards. The batch-accounting module remains open; next boundary is `batch-accounting:submit-withdraw-route-side-effect-port`.
- Refreshed `prompts/04-master-goal-controller.md` after `c212f73e` so a new Codex run starts from the current queue state instead of repeating the already-completed server-py and batch-accounting GET slices. Next boundary remains `batch-accounting:submit-withdraw-route-side-effect-port`.
- Completed `batch-accounting:submit-withdraw-route-side-effect-port`: moved submit/withdraw mutation DTO/service/error mapping plus affected-scope lifecycle/read-model persist orchestration into `BatchAccountingApiRoutes` with explicit callbacks, kept `server.py` responsible only for mutation session, JSON body parsing and response mapping, and preserved submit persist-failure rollback behavior. The batch-accounting module remains open; next boundary is `batch-accounting:repair-compat-quarantine`.
- Completed `batch-accounting:repair-compat-quarantine` as removal rather than quarantine: CodeGraph found no callers for `Application._repair_batch_accounting_relation_case_ids(...)`, so the unused app-level repair wrapper was deleted and a static guard now prevents it from returning. Service-level `BatchAccountingService.repair_legacy_case_id_collisions(...)` remains tested and command-service backed. Next boundary is `batch-accounting:module-closure-audit-and-production-evidence-defer`.
- Completed `batch-accounting:module-closure-audit-and-production-evidence-defer`: local IO/route/service/legacy/read-model freshness/operation-barrier/test/docs evidence is recorded, but the module is not full-closed because real PostgreSQL/worker/App Status/history/high-row production evidence is unavailable without production validation. Next boundary returns to the first read model pilot: `read-models:bank-detail-module-closure-audit-and-production-evidence-defer`.
- Completed `read-models:bank-detail-module-closure-audit-and-production-evidence-defer` as analysis/accounting only: bank_detail cannot be marked closed or merely production-evidence-deferred because local implementation gaps remain in the Application-owned latest suggestion callback, refresh/wakeup wrappers, available-month scope helper, derived lifecycle executor and service factory injection. Next boundary is `read-models:bank-detail-suggestion-provider-port-extraction`.
- Completed `read-models:bank-detail-suggestion-provider-port-extraction`: removed `Application._latest_bank_detail_auto_category_suggestion(...)`, added `BankDetailAutoCategorySuggestionProvider`, exposed `BankDetailsService.auto_category_input_row(...)`, updated API tests to use the new provider seam, and guarded the old callback from returning. Next boundary is `read-models:bank-detail-refresh-producer-port-extraction`.
- Completed `read-models:bank-detail-refresh-producer-port-extraction`: removed `Application._enqueue_bank_detail_read_model_refreshes(...)` and `_delete_bank_detail_redis_cache(...)`, added `BankDetailReadModelRefreshProducer`, kept bank detail enqueue behind `ReadModelRefreshGateway`, preserved Redis as optional wakeup, and guarded the old wrappers from returning. Next boundary is `read-models:bank-detail-available-month-scope-provider-extraction`.
- Completed `read-models:bank-detail-available-month-scope-provider-extraction`: removed `Application._bank_detail_available_month_scope_keys(...)`, added `BankDetailAvailableMonthScopeProvider`, preserved import transaction date-field month extraction and `all` fallback, and guarded the old app helper from returning. Next boundary is `read-models:bank-detail-derived-lifecycle-executor-port-extraction`.
- Completed `read-models:bank-detail-derived-lifecycle-executor-port-extraction`: removed `Application._derived_lifecycle_bank_detail_executor(...)`, added `BankDetailDerivedLifecycleExecutor`, preserved explicit month/all/default scope selection, refresh metadata filtering and enqueue payload shape, and guarded the old app executor from returning. Next boundary is `read-models:bank-detail-service-factory-collaborator-closure-audit`.
- Completed `read-models:bank-detail-service-factory-collaborator-closure-audit`: audited `Application._bank_details_application_service(...)` as dependency assembly only, recorded bank_detail local implementation closure evidence, deferred only real PostgreSQL/worker/App Status/high-row/browser evidence, and queued next read model pilot selection before any Go admission.
- Completed `planning:semantic-queue-state-and-master-goal-refresh`: corrected state/queue wording so local slice evidence is not confused with full module closure, inserted this planning slice into the queue, and regenerated `prompts/04-master-goal-controller.md` from current queue facts. Next boundary remains `read-models:next-pilot-selection-after-bank-detail`.
- Completed `read-models:next-pilot-selection-after-bank-detail`: selected `workbench_relation` as the next read model implementation pilot because it is the shared relation distribution source for the highest-risk cross-page freshness chains, and queued `read-models:workbench-relation-repository-port-extraction` as the first narrow implementation boundary. Go hot-path candidates remain blocked.
- Completed `read-models:workbench-relation-repository-port-extraction`: added `WorkbenchRelationReadModelRepositoryPort`, wired app/worker/projection builder relation read-model paths through it, updated the read model manifest owner, and added a port test proving unrelated read model methods are not exposed. Next boundary is `read-models:workbench-relation-derived-lifecycle-executor-port-extraction`.
- Completed `read-models:workbench-relation-derived-lifecycle-executor-port-extraction`: removed the app-level derived lifecycle refresh helper, added `WorkbenchRelationDerivedLifecycleExecutor`, preserved explicit scope/default all/gateway-backed enqueue payload shape, and added unit/static guard coverage. The module remains implementation-gap-open; next boundary is `read-models:workbench-relation-local-implementation-closure-audit`.
- Completed `read-models:workbench-relation-local-implementation-closure-audit` as analysis/accounting only: `workbench_relation` remains implementation-gap-open because transaction persist still uses broad `PostgresWorkbenchRepository`, app-level command repository snapshot/apply helpers remain, and production PostgreSQL/worker/App Status/high-row/browser evidence is unavailable. Next boundary is `workbench-relations:transaction-persist-repository-owner-split`.
- Completed `workbench-relations:transaction-persist-repository-owner-split`: changed transaction-bound pair relation persistence to use `PostgresWorkbenchRelationRepository` instead of broad `PostgresWorkbenchRepository`, added a static guard, and verified relation repository plus Workbench UoW/write characterization tests. Next boundary is `workbench-relations:command-repository-snapshot-adapter-audit`.
- Completed `workbench-relations:command-repository-snapshot-adapter-audit` as analysis/accounting only: CodeGraph showed the app-level command repository helper group has a compact call chain and can be extracted as one explicit adapter/port. Next boundary is `workbench-relations:command-repository-snapshot-adapter-extraction`.
- Completed `workbench-relations:command-repository-snapshot-adapter-extraction`: added `WorkbenchRelationCommandRepositoryAdapter`, removed app-level command repository save/apply/history helpers, preserved runtime mirror update and post-apply callback behavior, and verified adapter/command/UoW/write characterization tests. Next boundary is `workbench-relations:pair-relation-persist-schedule-helper-audit`.
- Completed `workbench-relations:pair-relation-persist-schedule-helper-audit` as analysis/accounting only: audited non-transactional pair relation persist/schedule/background helpers and selected `workbench-relations:pair-relation-persist-service-extraction` as the next narrow implementation boundary, leaving rollback restore separate.
- Completed `workbench-relations:pair-relation-persist-service-extraction`: added `WorkbenchPairRelationPersistService`, moved non-transactional persist/schedule/background/timing behavior behind that explicit service, kept app wrappers as compat-only delegates, removed app-owned pair persist lock state, and verified service/scheduler/timing/UoW/write characterization guards. Next boundary is `workbench-relations:restore-pair-relation-snapshot-helper-audit`.
- Completed `workbench-relations:restore-pair-relation-snapshot-helper-audit` as analysis/accounting only: audited `_restore_workbench_pair_relation_snapshot(...)`, confirmed it is required by WorkbenchWriteFacade failure rollback paths, and selected `workbench-relations:pair-relation-rollback-restore-service-extraction` as the next narrow implementation boundary.
- Completed `workbench-relations:pair-relation-rollback-restore-service-extraction`: added `WorkbenchPairRelationRollbackRestoreService`, made `_restore_workbench_pair_relation_snapshot(...)` a compat-only delegate, centralized pair relation service replacement so cached persist service state is cleared after rollback, and verified rollback/write/UoW guards. Next boundary is `workbench-relations:exception-restore-helper-audit`.
- Completed `workbench-relations:exception-restore-helper-audit` as analysis/accounting only: audited remaining exception/pair/candidate/override rollback helpers, confirmed they are required recovery paths, and selected `workbench-relations:exception-rollback-restore-service-extraction` as the next narrow implementation boundary.
- Completed `workbench-relations:exception-rollback-restore-service-extraction`: added `WorkbenchExceptionRollbackRestoreService`, moved exception/pair/candidate/override rollback restore behavior behind that service, converted wrappers and inline restore paths to delegates, and verified service/write/UoW guards. Next boundary is `workbench-relations:post-restore-local-implementation-closure-audit`.
- Completed `workbench-relations:post-restore-local-implementation-closure-audit` as analysis/accounting only: re-audited local relation gaps after persist/rollback restore extraction, kept `workbench_relation` implementation-gap-open, and selected `workbench-relations:batch-accounting-pair-restore-helper-audit` as the next narrow boundary.
- Completed `workbench-relations:batch-accounting-pair-restore-helper-audit` as analysis/accounting only: audited BatchAccountingApiRoutes pair relation snapshot/restore wiring, confirmed the restore callback is required for submit persist-failure rollback, and selected `workbench-relations:batch-accounting-pair-restore-service-delegation` as the next narrow implementation boundary.
- Completed `workbench-relations:batch-accounting-pair-restore-service-delegation`: made the batch-accounting restore callback delegate to `WorkbenchPairRelationRollbackRestoreService` in in-memory mode, added a static guard, and verified submit persist-failure rollback behavior. Next boundary is `workbench-relations:post-batch-restore-local-implementation-closure-audit`.
- Completed `workbench-relations:post-batch-restore-local-implementation-closure-audit` as analysis/accounting only: re-audited local relation gaps after batch-accounting restore delegation, kept `workbench_relation` implementation-gap-open, and selected `workbench-relations:turnover-workbench-pair-port-boundary-audit` as the next narrow boundary.
- Completed `workbench-relations:turnover-workbench-pair-port-boundary-audit` as analysis/accounting only: confirmed turnover pair writes are command-service gated, classified pair service as read-only compat fallback, and selected unused persist callback removal as the next narrow implementation boundary.
- Completed `workbench-relations:turnover-workbench-pair-port-unused-persist-callback-removal`: removed unused persist callback parameter/storage/wiring from `TurnoverLedgerWorkbenchPairPort` and turnover builder/fallback construction while preserving command-service-gated turnover behavior. Next boundary is `workbench-relations:pending-invoice-pair-service-boundary-audit`.
- Completed `workbench-relations:pending-invoice-pair-service-boundary-audit` as analysis/accounting only: audited `PendingInvoiceQueryService` and `PendingInvoiceApplicationService`, found their `pair_relation_service` constructor injection is unused, confirmed relation reads use `relation_facade` and writes use `relation_command_service`, and selected `workbench-relations:pending-invoice-unused-pair-service-removal` as the next narrow implementation boundary.
- Completed `workbench-relations:pending-invoice-unused-pair-service-removal`: removed unused pending invoice pair service constructor parameters, app wiring and test fixture injection while preserving relation reads through `relation_facade` and writes through `relation_command_service`. Next boundary is `workbench-relations:no-oa-pair-service-boundary-audit`.
- Completed `workbench-relations:no-oa-pair-service-boundary-audit` as analysis/accounting only: audited no-OA relation dependencies, confirmed normal writes are command-service gated and active reads mostly facade-backed, classified application snapshot/persist/rollback pair service usage separately from domain repair/read compatibility usage, and selected `workbench-relations:no-oa-application-pair-snapshot-port-extraction` as the next narrow implementation boundary.
- Completed `workbench-relations:no-oa-application-pair-snapshot-port-extraction`: added `NoOaPairRelationSnapshotPort`, moved no-OA application snapshot/version/persist/rollback restore usage behind that port, preserved command-service relation writes, facade reads, persistence payload shapes and rollback behavior, and added static guard coverage. Next boundary is `workbench-relations:no-oa-domain-repair-read-port-audit`.
- Completed `workbench-relations:no-oa-domain-repair-read-port-audit` as analysis/accounting only: audited `NoOaBankBatchService` remaining pair relation reads in submitted relation repair, stale-as-submitted projection and month-scope child service wiring, confirmed writes are already command-service backed, and selected `workbench-relations:no-oa-domain-repair-read-port-extraction` as the next implementation boundary.
- Completed `workbench-relations:no-oa-domain-repair-read-port-extraction`: added `NoOaRelationRepairReadPort`, moved `NoOaBankBatchService` active relation reads behind that port, forwarded the port through month-scoped child services, preserved command-service-backed relation writes and stale-as-submitted behavior, and added static guard coverage. Next boundary is `workbench-relations:post-no-oa-local-implementation-closure-audit`.
- Completed `workbench-relations:post-no-oa-local-implementation-closure-audit` as analysis/accounting only: re-audited local gaps after no-OA extraction, confirmed ETC is not the highest-risk direct pair service holder, and selected `workbench-relations:workbench-write-facade-pair-service-boundary-audit` as the next narrow boundary.
- Completed `workbench-relations:workbench-write-facade-pair-service-boundary-audit` as analysis/accounting only: classified all WorkbenchWriteFacade pair service call sites into read/preflight, snapshot/rollback, command write, special metadata mutation and compat-only surfaces, and selected relation read/snapshot port extraction as the next implementation boundary.
- Completed `workbench-relations:workbench-write-facade-relation-read-snapshot-port-extraction`: added `WorkbenchWriteRelationReadSnapshotPort`, moved WorkbenchWriteFacade active relation reads, withdraw preview fallback and pair snapshots behind the port, injected it from `Application._workbench_write_facade(...)`, preserved command-service-backed writes and cash special metadata mutation for a later boundary, and added static guard coverage. Next boundary is `workbench-relations:workbench-write-facade-cash-special-metadata-boundary-audit`.
- Completed `workbench-relations:workbench-write-facade-cash-special-metadata-boundary-audit` as analysis/accounting only: audited the remaining cash special metadata direct pair mutations, confirmed the existing command service is not a drop-in replacement for clear/replace semantics, and selected `workbench-relations:workbench-write-facade-cash-special-metadata-port-extraction` as the next narrow implementation boundary.
- Completed `workbench-relations:workbench-write-facade-cash-special-metadata-port-extraction`: added `WorkbenchWriteRelationSpecialMetadataMutationPort`, moved cash special metadata update/clear calls behind it, injected the port from `Application._workbench_write_facade(...)`, removed WorkbenchWriteFacade's broad `_pair_relation_service` field, and strengthened the boundary guard. Next boundary is `workbench-relations:workbench-write-facade-post-port-local-implementation-closure-audit`.
- Completed `workbench-relations:workbench-write-facade-post-port-local-implementation-closure-audit` as analysis/accounting only: confirmed WorkbenchWriteFacade relation calls now go through explicit ports and selected constructor cleanup as the next narrow boundary because `pair_relation_service` is still accepted only for default port construction.
- Completed `workbench-relations:workbench-write-facade-required-port-constructor`: removed broad `pair_relation_service` from WorkbenchWriteFacade constructor, required explicit relation ports, updated Application and auth-context test helper wiring, and strengthened the boundary guard. Next boundary is `workbench-relations:post-workbench-write-facade-local-implementation-closure-audit`.
- Completed `workbench-relations:post-workbench-write-facade-local-implementation-closure-audit` as analysis/accounting only: re-audited broader local gaps after WorkbenchWriteFacade stopped accepting broad pair service, confirmed ETC repair/link/migration services are already command-boundary guarded, and selected turnover pair port constructor cleanup because `TurnoverLedgerWorkbenchPairPort` still accepts broad `pair_relation_service`. Next boundary is `workbench-relations:turnover-workbench-pair-port-required-command-constructor`.
- Completed `workbench-relations:turnover-workbench-pair-port-required-command-constructor`: removed broad `pair_relation_service` from `TurnoverLedgerWorkbenchPairPort`, removed the port's pair-service read fallback, kept local turnover rollback snapshot behavior outside the port, updated turnover port tests and strengthened the static guard. Next boundary is `workbench-relations:workbench-matching-pair-service-boundary-audit`.
- Completed `workbench-relations:workbench-matching-pair-service-boundary-audit` as analysis/accounting only: classified Workbench matching/orchestrator pair service reads as canonical active relation reads for held-row suppression and auto-completion preconditions, and selected `workbench-relations:workbench-matching-relation-read-port-extraction` as the next narrow implementation boundary.
- Completed `workbench-relations:workbench-matching-relation-read-port-extraction`: added `WorkbenchMatchingRelationReadPort`, moved Workbench matching/orchestrator canonical active relation reads behind the port, wired it from `Application` with `WorkbenchRelationCommandService`, preserved matching suppression and auto-completion behavior, and added static guard coverage. Next boundary is `workbench-relations:server-relation-read-helper-boundary-audit`.
- Completed `workbench-relations:server-relation-read-helper-boundary-audit` as analysis/accounting only: classified remaining `server.py` direct relation read/snapshot helpers into payload/live-row enrichment, source-version, transaction-persist, rollback, repair/precondition and whole-state persistence surfaces, and selected Workbench payload/live-row relation read port extraction as the next narrow implementation boundary.
- Completed `workbench-relations:server-workbench-payload-relation-read-port-extraction`: added `WorkbenchPayloadRelationReadPort`, backed it with `WorkbenchRelationCommandService`, moved Workbench payload/live-row active relation reads in `_apply_pair_relations_to_payload`, `_supplement_missing_active_pair_relation_rows`, `_relation_for_group` and `_resolve_live_rows_direct` behind the port, and added static guard/API regression coverage. Next boundary is `workbench-relations:server-source-version-relation-snapshot-provider-extraction`.
- Completed `workbench-relations:server-source-version-relation-snapshot-provider-extraction`: added `WorkbenchRelationSourceVersionProvider`, moved Workbench/no-OA `pair_relation_snapshot_version` source-version reads behind it, preserved `WorkbenchReadModelService.snapshot_version(...)` hash behavior, and added provider/static guard coverage. Next boundary is `workbench-relations:server-repair-precondition-relation-read-port-audit`.
- Completed `workbench-relations:server-repair-precondition-relation-read-port-audit` as analysis/accounting only: classified OA invoice offset sync, OA attachment context repair, confirm-link context expansion and auto-pair conflict checks as separate write-adjacent read surfaces, and selected OA invoice offset sync relation read port extraction as the next narrow implementation boundary.
- Completed `workbench-relations:server-oa-invoice-offset-relation-read-port-extraction`: added `WorkbenchOaInvoiceOffsetRelationReadPort`, moved OA invoice offset sync active relation reads behind mode-constrained port access, preserved command-service confirm/cancel writes and OA invoice offset sync regression behavior. Next boundary is `workbench-relations:server-oa-attachment-repair-relation-read-port-extraction`.
- Completed `workbench-relations:server-oa-attachment-repair-relation-read-port-extraction`: added `WorkbenchOaAttachmentRepairRelationReadPort`, moved OA attachment context repair active relation reads behind the port, preserved dedicated-withdraw filtering, missing attachment detection, before relation payload, replace-existing command-service repair behavior and Workbench V2 repair regression coverage. Next boundary is `workbench-relations:server-confirm-link-context-relation-read-port-extraction`.
- Completed `workbench-relations:server-confirm-link-context-relation-read-port-extraction`: added `WorkbenchConfirmLinkContextRelationReadPort`, moved confirm-link context expansion active relation reads behind the port, preserved row-id normalization, existing relation context expansion and confirm-link preview/submit regressions. Next boundary is `workbench-relations:server-auto-pair-conflict-relation-read-port-extraction`.
- Completed `workbench-relations:server-auto-pair-conflict-relation-read-port-extraction`: added `WorkbenchAutoPairConflictRelationReadPort`, moved auto-pair conflict active relation reads behind the port, preserved system-auto relation allowlist conflict behavior and auto-match candidate regressions. Next boundary is `workbench-relations:post-server-precondition-local-implementation-closure-audit`.
- Completed `workbench-relations:post-server-precondition-local-implementation-closure-audit` as analysis/accounting only: re-audited local `workbench_relation` gaps after server repair/precondition read ports, kept the module implementation-gap-open, kept Go admission blocked, and selected retained-OA supplemental relation read port extraction as the next narrow implementation boundary.
- Completed `workbench-relations:server-retained-oa-supplemental-relation-read-port-extraction`: added `WorkbenchRetainedOaSupplementalRelationReadPort`, moved retained-OA all-scope supplemental active relation reads behind the port, and preserved cutoff/manual retained row behavior through retained-OA Workbench API regressions. Next boundary is `workbench-relations:server-case-id-allocation-relation-read-owner-audit`.
- Completed `workbench-relations:server-case-id-allocation-relation-read-owner-audit` as analysis/accounting only: audited `_next_workbench_relation_case_id(...)`, confirmed it intentionally avoids active relation case id collisions but still parses relation snapshot shape in `server.py`, and selected explicit case-id allocation service extraction next.
- Completed `workbench-relations:server-case-id-allocation-service-extraction`: added `WorkbenchRelationCaseIdAllocator`, moved `pair_relations` snapshot parsing out of `server.py`, preserved active `CASE-AUTO-0001` skip behavior, and kept `workbench_relation` implementation-gap-open. Next boundary is `workbench-relations:transaction-persist-closure-accounting-audit`.
- Completed `workbench-relations:transaction-persist-closure-accounting-audit` as analysis/accounting only: audited non-transaction persist service ownership, transaction repository ownership, changed-case snapshot behavior and broad repository compatibility delegation; persist-specific surfaces are locally accounted for, but rollback restore and whole-state persistence closure remain open. Next boundary is `workbench-relations:rollback-closure-accounting-audit`.
- Completed `workbench-relations:rollback-closure-accounting-audit` as analysis/accounting only: audited pair relation, exception and batch-accounting rollback restore surfaces; wrappers are compat-only delegates to explicit rollback services, and rollback-specific behavior is locally accounted for. Whole-state persistence and bootstrap compatibility remain open. Next boundary is `workbench-relations:whole-state-persistence-closure-accounting-audit`.
- Completed `workbench-relations:whole-state-persistence-closure-accounting-audit` as analysis/accounting only: audited startup domain loader, PostgresStateStore relation delegate/fallback, Mongo/local state-store compatibility and broad app `_persist_state(...)`; selected `workbench-relations:persist-state-relation-snapshot-quarantine` because broad full-state persistence still serializes relation facts.
- Completed `workbench-relations:persist-state-relation-snapshot-quarantine`: removed Workbench relation snapshot facts from broad `Application._persist_state(...)`, added a static guard to prevent the old full-state relation serialization from returning, and preserved relation-specific save/load paths. Next boundary is `workbench-relations:app-health-route-builder-pair-service-injection-audit`.
- Completed `workbench-relations:app-health-route-builder-pair-service-injection-audit` as analysis/accounting only: confirmed App Health services have no pair-service dependency, classified no-OA and batch-accounting route callbacks as existing explicit/compat boundaries, and selected turnover local pair snapshot/restore port extraction because turnover primary builders still pass broad pair service into local rollback support.
- Completed `workbench-relations:turnover-local-pair-snapshot-port-extraction`: added `TurnoverLedgerLocalPairSnapshotPort`, removed broad pair service from turnover primary builders and `TurnoverLedgerLocalClosureConnection`, preserved turnover local rollback behavior, and added static guard coverage. Next boundary is `workbench-relations:settings-data-reset-pair-service-boundary-audit`.
- Completed `workbench-relations:settings-data-reset-pair-service-boundary-audit` as analysis/accounting only: confirmed data reset relation clearing/filtering is a legitimate settings reset boundary, but broad `workbench_pair_relation_service` injection remains and requires an explicit settings reset pair snapshot port before local closure/defer accounting.
- Completed `workbench-relations:settings-data-reset-pair-snapshot-port-extraction`: added `SettingsDataResetPairSnapshotPort`, removed broad pair service from `SettingsDataResetService`, preserved bank/invoice/OA reset relation semantics, and added static guard coverage. Next boundary is `workbench-relations:local-implementation-closure-and-production-evidence-defer`.
- Completed `workbench-relations:local-implementation-closure-and-production-evidence-defer` as analysis/accounting only: local closure cannot move to production-evidence-deferred yet because ETC repair/link/migration `persist_pair_relations` callback accounting remains open. Next boundary is `workbench-relations:etc-repair-link-migration-persist-callback-closure-audit`.
- Completed `workbench-relations:etc-repair-link-migration-persist-callback-closure-audit` as analysis/accounting only: classified ETC `persist_pair_relations` callbacks as explicit post-command persist boundaries and confirmed direct pair fallback remains removed/guarded. Next boundary is `workbench-relations:final-local-implementation-closure-and-production-evidence-defer`.
- Completed `workbench-relations:final-local-implementation-closure-and-production-evidence-defer`: classified remaining local `workbench_relation` pair-service references as explicit ports/adapters/repositories/runtime snapshot support/tools/tests, recorded real PostgreSQL/worker/App Status/high-row/browser evidence as deferred, kept the module not globally closed, and inserted `read-models:next-pilot-selection-after-workbench-relation` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-workbench-relation` as analysis/planning only: selected `pending_invoice` as the next non-Go read model pilot because it consumes both `bank_detail` and `workbench_relation` source versions and has high user-visible stale-read risk. Next boundary is `read-models:pending-invoice-repository-port-extraction`.
- Completed `read-models:pending-invoice-repository-port-extraction`: added `PendingInvoiceReadModelRepositoryPort`, wired PostgreSQL pending invoice read path and pending projection save/mark behavior through the narrow port, preserved search index repository behavior, and added a port guard test. Next boundary is `read-models:pending-invoice-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:pending-invoice-refresh-freshness-operation-barrier-audit` as analysis/accounting only: audited pending invoice fresh gate, source-version checks, force-refresh/page-first scope, worker expansion and operation barrier contract; selected scope policy filter allowlist enforcement as the next narrow implementation boundary before any Go admission.
- Completed `read-models:pending-invoice-scope-policy-filter-allowlist`: tightened pending invoice gateway scope validation so expense/income only accept their supported filter groups, added invalid-filter rejection coverage, and kept `pending_invoice` implementation-gap-open for mutation freshness target contract work.
- Completed `read-models:pending-invoice-mutation-freshness-target-contract`: kept backend response shape unchanged, made income-status batch mutations wait for `pending_invoice` operation barrier targets before refetching rows, and added PendingInvoicesPage regression coverage. Next boundary is `read-models:pending-invoice-local-implementation-closure-audit`.
- Completed `read-models:pending-invoice-local-implementation-closure-audit`: accounted for local pending_invoice repository port, freshness gate, source-version proof, scope policy, worker fan-out, operation barrier, legacy contamination and tests/docs; deferred real PostgreSQL/worker/App Status/high-row/browser evidence, kept the module not globally closed, and inserted `read-models:next-pilot-selection-after-pending-invoice` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-pending-invoice` as analysis/planning only: selected `oa_pending_payment` as the next non-Go read model pilot because it combines completed OA projection, in-progress payment-admitted OA, Workbench relation, invoice lifecycle and pending bank claims while still allowing a narrow repository-port first slice. Next boundary is `read-models:oa-pending-payment-repository-port-extraction`.
- Completed `read-models:oa-pending-payment-repository-port-extraction`: added `OaPendingPaymentReadModelRepositoryPort`, wired PostgreSQL OA pending payment read route and projection save/mark/prune paths through it, moved Workbench relation source-version lookup to the Workbench relation port, and added port/source-version regression coverage. Next boundary is `read-models:oa-pending-payment-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:oa-pending-payment-refresh-freshness-operation-barrier-audit`: audited OA pending payment fresh gates, scope policy, worker fan-out, source-version proof and operation barrier behavior; fixed frontend barrier target selection so mutations returning concrete month scopes plus `all` wait for the concrete `oa_pending_payment:<YYYY-MM>` target instead of preferring fan-out-only `all`. Next boundary is `read-models:oa-pending-payment-local-implementation-closure-audit`.
- Completed `read-models:oa-pending-payment-local-implementation-closure-audit`: removed unused app-level OA pending payment rebuild/list/mark/live helpers, accounted for repository port, fresh gate, source-version proof, scope policy, worker fan-out, operation barrier, legacy contamination and tests/docs, deferred real PostgreSQL/worker/App Status/high-row/browser evidence, kept the module not globally closed, and inserted `read-models:next-pilot-selection-after-oa-pending-payment` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-oa-pending-payment` as analysis/planning only: selected `input_invoice_usage` as the next non-Go read model pilot because it shares the invoice-usage-collection worker/projection family with OA pending payment while still having high stale-read/cross-page relation risk and a narrow repository-port first slice. Next boundary is `read-models:input-invoice-usage-repository-port-extraction`.
- Completed `read-models:input-invoice-usage-repository-port-extraction`: added `InputInvoiceUsageReadModelRepositoryPort`, returned it from PostgreSQL state-store read wiring, routed input usage projection save/mark/prune through it, and added a port guard proving output/OA/pending read model methods are not exposed. Next boundary is `read-models:input-invoice-usage-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:input-invoice-usage-refresh-freshness-operation-barrier-audit`: audited input usage rows/detail/filter/export fresh gates, source-version proof, scope policy, worker all-scope fan-out, operation barrier behavior and retained helper classifications; removed unused app-level input usage rebuild/list/mark projection helpers from `Application` and added a guard preventing them from returning. Next boundary is `read-models:input-invoice-usage-local-implementation-closure-audit`.
- Completed `read-models:input-invoice-usage-relation-detail-production-repository-fail-closed`: split from the local closure audit after finding that production SQL runtime relation detail could fall back to live query when the SQL detail repository was unavailable. The route now returns `202`/refreshing and enqueues `input_invoice_usage:all` instead. Next boundary remains `read-models:input-invoice-usage-local-implementation-closure-audit`.
- Completed `read-models:input-invoice-usage-local-implementation-closure-audit` as analysis/accounting only: accounted for local input usage repository port, rows/filter/export/detail fresh gates, source-version proof, scope policy, worker fan-out, operation barrier, legacy contamination and tests/docs; deferred real PostgreSQL/worker/App Status/high-row/browser evidence, kept the module not globally closed, and inserted `read-models:next-pilot-selection-after-input-invoice-usage` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-input-invoice-usage` as analysis/planning only: selected `output_invoice_collection` as the next non-Go read model pilot because it is the remaining invoice-usage-collection page read model with high stale-read/export/lifecycle risk and a narrow repository-port first slice. Next boundary is `read-models:output-invoice-collection-repository-port-extraction`.
- Completed `read-models:output-invoice-collection-repository-port-extraction`: added `OutputInvoiceCollectionReadModelRepositoryPort`, returned it from PostgreSQL state-store read wiring, routed output collection projection save/mark/prune through it, and added port/SQL runtime/API regression coverage. Next boundary is `read-models:output-invoice-collection-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:output-invoice-collection-refresh-freshness-operation-barrier-audit`: audited output collection fresh gates, force refresh, all fan-out/month proof, operation barrier targets and app-level projection helper contamination; added mutation response `read_model_scope_keys`/`freshness_targets`, made frontend lifecycle/receipt/red relation flows wait on concrete month targets, removed unused app-level output projection helpers from `Application`, and guarded them from returning. Next boundary is `read-models:output-invoice-collection-local-implementation-closure-audit`.
- Completed `read-models:output-invoice-collection-relation-detail-production-repository-fail-closed`: split from the local closure audit after finding that production SQL runtime relation detail could fall back to live query when the SQL detail repository was unavailable. Relation details now use `OutputInvoiceCollectionReadModelDetailService` and return `202`/refreshing with `output_invoice_collection:all` enqueue when the SQL detail lookup is unavailable. Next boundary remains `read-models:output-invoice-collection-local-implementation-closure-audit`.
- Completed `read-models:output-invoice-collection-local-implementation-closure-audit`: accounted for local output collection repository port, rows/filter/export/detail fresh gates, source-version proof, scope policy, worker fan-out, mutation operation barrier targets, app-level projection helper removal, legacy/live path classification and tests/docs; deferred real PostgreSQL/worker/App Status/high-row/browser evidence, kept the module not globally closed, and inserted `read-models:next-pilot-selection-after-output-invoice-collection` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-output-invoice-collection` as analysis/planning only: selected `invoice_lifecycle` as the next non-Go read model pilot because it is the shared upstream lifecycle state boundary for pending invoice, input/output usage, OA pending payment, tax, cost/search and import fan-out. Next boundary is `read-models:invoice-lifecycle-repository-port-extraction`.
- Completed `read-models:invoice-lifecycle-repository-port-extraction`: added `InvoiceLifecycleReadModelRepositoryPort`, wired `InvoiceLifecycleReadFacade` lookups and `InvoiceLifecycleSqlProjectionBuilder` save/mark paths through the narrow port, guarded that unrelated read model methods are not exposed, and avoided adding a speculative state-store property because no existing caller exists. Next boundary is `read-models:invoice-lifecycle-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:invoice-lifecycle-refresh-freshness-operation-barrier-audit`: audited lifecycle facade/read model refresh/scope policy/manifest/App Status/worker wiring, added an operation barrier regression proving exact invoice lifecycle month targets are not blocked by other-month pending outbox, and selected app-owned derived lifecycle executor extraction as the next narrow boundary.
- Completed `read-models:invoice-lifecycle-derived-lifecycle-executor-port-extraction`: added `InvoiceLifecycleDerivedLifecycleExecutor`, moved invoice lifecycle derived lifecycle refresh execution out of `Application`, kept enqueue behind the gateway-backed generic read model refresh callback, preserved scope/reason/metadata/result shape, and added unit/static guard coverage. Next boundary is `read-models:invoice-lifecycle-local-implementation-closure-audit`.
- Completed `read-models:invoice-lifecycle-local-implementation-closure-audit` as audit/accounting only: repository port, freshness/barrier, worker/manifest/App Status registration, derived lifecycle executor and legacy/app-owned helper classifications are locally accounted for; real PostgreSQL/worker/App Status/high-row/browser evidence remains deferred, so the module is not globally closed. Next boundary is `read-models:next-pilot-selection-after-invoice-lifecycle`.
- Completed `read-models:next-pilot-selection-after-invoice-lifecycle` as analysis/planning only: selected `tax_offset` as the next non-Go read model pilot because it directly consumes invoice lifecycle/certification state, has high stale-read risk after plan save/certified import/import fan-out, and has a narrow repository-port first slice. Next boundary is `read-models:tax-offset-repository-port-extraction`.
- Completed `read-models:tax-offset-repository-port-extraction`: added `TaxOffsetReadModelRepositoryPort`, wired PostgreSQL state-store tax read/write and optional SQL read connection paths through the narrow port, made `TaxOffsetSqlProjectionBuilder` save rebuilt month scopes through the port, and added port/projection/state-store tests proving unrelated read model methods are not exposed. A broader tax API OA attachment invoice regression remains failing outside this slice and is recorded for the next audit. Next boundary is `read-models:tax-offset-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:tax-offset-refresh-freshness-operation-barrier-audit`: audited tax offset fresh gate, force refresh scope policy, `all` fan-out/month shard proof, operation barrier and retained legacy/app-owned wrappers; fixed the recorded OA attachment invoice regression by letting centralized object identity classify `invoice_type=进项发票` / `销项发票` as formal invoice evidence when `evidence_type` is absent, while receipt/unknown evidence remains excluded. Next boundary is `read-models:tax-offset-local-implementation-closure-audit`.
- Completed `read-models:tax-offset-local-implementation-closure-audit` as analysis/accounting only: local closure cannot move to production-evidence-deferred yet because `Application.rebuild_tax_offset_read_model_scope(...)` still owns worker rebuild, persistence and fresh Redis cache publish behavior. Next boundary is `read-models:tax-offset-worker-rebuild-executor-port-extraction`.
- Completed `read-models:tax-offset-worker-rebuild-executor-port-extraction`: added `TaxOffsetWorkerRebuildExecutor`, moved app-owned tax offset worker rebuild/persist/fresh Redis cache publish behavior out of `Application`, kept the app method as a thin delegate, and added executor/static guard tests. Next boundary is `read-models:tax-offset-derived-lifecycle-executor-boundary-audit`.
- Completed `read-models:tax-offset-derived-lifecycle-executor-boundary-audit`: added `TaxOffsetDerivedLifecycleExecutor`, moved tax offset derived lifecycle read model invalidation and month-cache clearing out of `Application`, registered explicit executor methods for `tax_offset_read_model` and `tax_offset_month_cache`, removed the app-owned helper methods, and added unit/static guard coverage. Next boundary is `read-models:tax-offset-post-derived-local-implementation-closure-audit`.
- Completed `read-models:tax-offset-post-derived-local-implementation-closure-audit` as analysis/accounting only: repository port, freshness/barrier, worker rebuild executor and derived lifecycle executor support are accounted for, but optional cache warmup scheduling/execution still lives in `Application._schedule_tax_offset_cache_warmup(...)` / `_run_tax_offset_cache_warmup_job(...)` and can build tax payloads, upsert read models and persist snapshots. `tax_offset` stays implementation-gap-open; next boundary is `read-models:tax-offset-cache-warmup-executor-port-extraction`.
- Completed `read-models:tax-offset-cache-warmup-executor-port-extraction`: added `TaxOffsetCacheWarmupExecutor`, moved optional cache warmup env gating, month normalization, idempotent background job scheduling, run-job progress/success handling, read model upsert and snapshot persistence out of `Application`, removed the app-owned run-job/env helpers, and guarded the remaining app helper as a thin delegate only. `tax_offset` stays implementation-gap-open until `read-models:tax-offset-final-local-implementation-closure-audit` re-audits the current code.
- Completed `read-models:tax-offset-final-local-implementation-closure-audit` as analysis/accounting only: repository port, fresh gate, operation barrier, worker rebuild executor, derived lifecycle executor and cache warmup executor are locally accounted for, but broad `Application._persist_state(...)` still writes `tax_offset_read_models` into the legacy full-state snapshot path. `tax_offset` stays implementation-gap-open; next boundary is `read-models:tax-offset-full-state-read-model-snapshot-quarantine`.
- Completed `read-models:tax-offset-full-state-read-model-snapshot-quarantine`: removed broad `Application._persist_state(...)` writes of `tax_offset_read_models`, kept explicit runtime/executor persistence via `_persist_tax_offset_read_models_best_effort(...)`, and added a read model architecture guard preventing the old full-state snapshot writer from returning. `tax_offset` stays implementation-gap-open until `read-models:tax-offset-post-full-state-local-implementation-closure-audit` re-audits current code.
- Completed `read-models:tax-offset-post-full-state-local-implementation-closure-audit` as analysis/accounting only: re-audited tax offset after full-state snapshot quarantine, found no remaining local implementation gap, moved local support to `production-evidence-deferred` without claiming module closure, recorded real PostgreSQL/worker/App Status/high-row/browser evidence gaps, and inserted `read-models:next-pilot-selection-after-tax-offset` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-tax-offset` as analysis/accounting only: compared remaining non-Go read model candidates and selected `cost_statistics` because of high cross-page stale-read risk, special parent aggregate scope semantics, old `cost-tax` compatibility worker lane and a narrow repository-port first slice. Next boundary is `read-models:cost-statistics-repository-port-extraction`.
- Completed `read-models:cost-statistics-repository-port-extraction`: added `CostStatisticsReadModelRepositoryPort`, routed cost statistics SQL read/projection save paths through the narrow port, verified optional SQL read connection wiring, and added a guard that the port does not expose unrelated read model methods. Next boundary is `read-models:cost-statistics-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:cost-statistics-refresh-freshness-operation-barrier-audit` as analysis/accounting only: verified existing local evidence for SQL fresh gate, production repository unavailable behavior, scope normalization, parent aggregate, primary/compat worker split and App Status registration, but found `Application._derived_lifecycle_cost_statistics_executor(...)` still owns cost statistics derived lifecycle invalidation/warmup-vs-refresh fallback and enqueued-job accounting. Next boundary is `read-models:cost-statistics-derived-lifecycle-executor-port-extraction`.
- Completed `read-models:cost-statistics-derived-lifecycle-executor-port-extraction`: added `CostStatisticsDerivedLifecycleExecutor`, moved cost statistics derived lifecycle invalidation, `pending_invoice_rules_changed` persist-empty behavior, no-warmup refresh fallback metadata and enqueued-job accounting out of `Application`, removed the old app-owned helper, and added unit/static guard coverage. Next boundary is `read-models:cost-statistics-post-derived-local-implementation-closure-audit`.
- Completed `read-models:cost-statistics-post-derived-local-implementation-closure-audit` as analysis/accounting only: re-audited cost statistics after repository port, freshness/barrier and derived lifecycle executor extraction; warmup/retry/rebuild app methods are compat delegates to `CostStatisticsRuntimeService`, but broad `Application._persist_state(...)` still serializes `cost_statistics_read_models`. Next boundary is `read-models:cost-statistics-full-state-read-model-snapshot-quarantine`.
- Completed `read-models:cost-statistics-full-state-read-model-snapshot-quarantine`: removed broad `Application._persist_state(...)` writes of `cost_statistics_read_models`, preserved explicit runtime/query persistence through `_persist_cost_statistics_read_models_best_effort(...)`, and expanded the broad full-state read model guard to cover both cost statistics and tax offset. Next boundary is `read-models:cost-statistics-post-full-state-local-implementation-closure-audit`.
- Completed `read-models:cost-statistics-post-full-state-local-implementation-closure-audit` as analysis/accounting only: re-audited cost statistics after full-state snapshot quarantine, found no remaining local implementation gap, moved local support to `production-evidence-deferred` without claiming module closure, recorded real PostgreSQL/worker/App Status/high-row/browser evidence gaps, and inserted `read-models:next-pilot-selection-after-cost-statistics` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-cost-statistics` as analysis/planning only: compared remaining non-Go read model candidates and selected `turnover_ledger` because of high user-visible stale grouped-ledger risk, direct Workbench/cost/search fan-out impact and a narrow manifest-listed repository-port first slice. Next boundary is `read-models:turnover-ledger-repository-port-extraction`; Go/Fiber/Go Worker admission remains blocked.
- Completed `read-models:turnover-ledger-repository-port-extraction`: added `TurnoverLedgerReadModelRepositoryPort`, routed PostgreSQL state-store read wiring, app query injection and worker projection save paths through the narrow port, and added a guard proving unrelated read model methods are not exposed. `turnover_ledger` remains implementation-gap-open; next boundary is `read-models:turnover-ledger-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:turnover-ledger-refresh-freshness-operation-barrier-audit` as analysis/accounting only: existing code has SQL fresh gate, month/all scope policy, manifest/App Status/worker registry, Workbench relation source-version proof and operation barrier evidence, but `Application._enqueue_turnover_ledger_read_model_refreshes(...)` and `_clear_turnover_ledger_read_model_best_effort(...)` still own refresh/clear behavior and one path clears via broad workbench SQL repository. `turnover_ledger` remains implementation-gap-open; next boundary is `read-models:turnover-ledger-refresh-producer-clear-port-extraction`.
- Completed `read-models:turnover-ledger-refresh-producer-clear-port-extraction`: added `TurnoverLedgerReadModelRefreshProducer`, moved turnover refresh enqueue and best-effort clear wiring out of app-owned helpers, kept enqueue behind `ReadModelRefreshGateway`, and made clear use the turnover-specific read repository port instead of broad workbench SQL read repository. `turnover_ledger` remains implementation-gap-open until local closure audit; next boundary is `read-models:turnover-ledger-local-implementation-closure-audit`.
- Completed `read-models:turnover-ledger-local-implementation-closure-audit` as analysis/accounting only: audited route/service/repository/read model/worker/frontend/API surfaces after repository port, freshness/barrier audit and refresh producer/clear extraction, classified remaining fallback surfaces as explicit boundaries or compat-only local adapters, found no local implementation gap, and moved `turnover_ledger` local support to `production-evidence-deferred` without claiming module closure. Real PostgreSQL/worker/App Status/high-row/browser evidence remains deferred; next boundary is `read-models:next-pilot-selection-after-turnover-ledger`.
- Completed `read-models:next-pilot-selection-after-turnover-ledger`: compared `no_oa_bank_batch`, `search` and `bank_account_balance`, selected `no_oa_bank_batch` as the eleventh non-Go read model pilot because of page-level stale-read risk, Workbench relation adjacency, public snapshot persistence and operation-barrier sensitivity, and inserted `read-models:no-oa-bank-batch-repository-state-store-boundary-audit` before blocked Go candidates. Target verification exposed and fixed a stale no-OA refresh-service constructor keyword by switching to `NoOaPairRelationSnapshotPort`; no repository-port extraction or Go work was done. `search` and `bank_account_balance` remain later candidates; Go/Fiber/Go Worker admission remains blocked.
- Completed `read-models:no-oa-bank-batch-repository-state-store-boundary-audit` as analysis/accounting only: audited no-OA manifest/scope/worker registration, route mapping, application list path, refresh handler, public snapshot, PostgresStateStore, PostgresWorkbenchRepository and SQL list rows. Selected `read-models:no-oa-bank-batch-refresh-persistence-boundary-extraction` as the next narrow implementation slice because the refresh worker still directly persists `public_snapshot()` through broad state-store; list-only repository port extraction remains a later slice. Go/Fiber/Go Worker admission remains blocked.
- Completed `read-models:no-oa-bank-batch-refresh-persistence-boundary-extraction`: added `NoOaBankBatchReadModelPersistencePort`, wired the no-OA worker refresh service through the explicit persistence boundary, preserved existing SQL/state-store delegation semantics, and added refresh/static guard tests proving `handle_runtime_event(...)` no longer directly calls broad `state_store.save_no_oa_bank_batches(...)`. Next boundary is `read-models:no-oa-bank-batch-read-model-repository-port-extraction`; Go/Fiber/Go Worker admission remains blocked.
- Completed `read-models:no-oa-bank-batch-read-model-repository-port-extraction`: added `NoOaBankBatchReadModelRepositoryPort`, wired PostgreSQL state-store and no-OA list/query construction through the narrow port, updated manifest owner to the no-OA port, and guarded that `list_batches_payload(...)` no longer reads broad `workbench_sql_read_repository`. `no_oa_bank_batch` remains implementation-gap-open; next boundary is `read-models:no-oa-bank-batch-freshness-derived-lifecycle-boundary-audit`.
- Completed `read-models:no-oa-bank-batch-freshness-derived-lifecycle-boundary-audit` as analysis/accounting only: verified no-OA refresh enqueue goes through `ReadModelRefreshGateway`, scope policy/manifest/App Status/worker registration exists, and frontend writes wait through operation barrier targets; found app-owned derived lifecycle enqueue behavior and no-OA mutation persistence fallback broad state-store writes as remaining implementation gaps. Next boundary is `read-models:no-oa-bank-batch-derived-lifecycle-executor-port-extraction`.
- Completed `read-models:no-oa-bank-batch-derived-lifecycle-executor-port-extraction`: added `NoOaBankBatchDerivedLifecycleExecutor`, moved no-OA derived lifecycle target selection, refresh metadata forwarding and enqueued-job accounting out of `Application`, wired the derived lifecycle registry through the explicit executor, and added executor/static guard coverage. `no_oa_bank_batch` remains implementation-gap-open; next boundary is `read-models:no-oa-bank-batch-mutation-persistence-fallback-quarantine`.
- Completed `read-models:no-oa-bank-batch-mutation-persistence-fallback-quarantine`: removed the no-OA service-layer broad state-store fallback, made `persist_mutation(...)` require `save_no_oa_bank_batch_mutation(...)`, added the same explicit boundary to `ApplicationStateStore`, and guarded the old fallback from returning. `no_oa_bank_batch` remains implementation-gap-open until local closure audit; next boundary is `read-models:no-oa-bank-batch-local-implementation-closure-audit`.
- Completed `read-models:no-oa-bank-batch-local-implementation-closure-audit` as analysis/accounting only and immediately closed `read-models:no-oa-bank-batch-full-state-snapshot-quarantine`: the audit found broad `Application._persist_state(...)` still serialized `no_oa_bank_batches`; the implementation removed that broad full-state snapshot write, preserved explicit mutation/worker persistence boundaries, and added an architecture guard. `no_oa_bank_batch` remains implementation-gap-open until post-full-state local closure audit; next boundary is `read-models:no-oa-bank-batch-post-full-state-local-implementation-closure-audit`.
- Completed `read-models:no-oa-bank-batch-post-full-state-local-implementation-closure-audit`: re-audited no-OA after full-state snapshot quarantine, removed dead app-owned source-version/stale-reason helpers from `Application`, moved local support to `production-evidence-deferred` without claiming module closure, and inserted `read-models:next-pilot-selection-after-no-oa-bank-batch` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-no-oa-bank-batch` as analysis/planning only: compared `search` and `bank_account_balance`, selected `search` as the twelfth non-Go read model pilot because it has broader cross-page stale-index risk and app-owned query/enqueue/rebuild helper contamination, created the `docs/modules/search/` maintenance skeleton, and inserted `read-models:search-repository-port-extraction` before blocked Go candidates. Go/Fiber/Go Worker admission remains blocked.
- Completed `read-models:search-repository-port-extraction`: added `SearchReadModelRepositoryPort`, routed PostgreSQL state-store search SQL read access and `SearchPendingSqlProjectionBuilder` search index saves through the narrow port, updated manifest owner, and added a port guard preventing unrelated read model methods from being exposed. `search` remains implementation-gap-open; next boundary is `read-models:search-freshness-helper-boundary-audit`.
- Completed `read-models:search-freshness-helper-boundary-audit` and `read-models:search-app-rebuild-helper-quarantine`: audit classified remaining app-owned search helpers, found unused app-level search rebuild helpers as the first concrete gap, removed `Application.rebuild_search_index_scope(...)` and `_build_search_index_rows_for_month(...)`, and added a guard proving search rebuild ownership remains with `SearchPendingSqlProjectionBuilder`. `search` remains implementation-gap-open; next boundary is `read-models:search-query-freshness-service-extraction`.
- Completed `read-models:search-query-freshness-service-extraction`: added `SearchQueryFreshnessService` and `SearchIndexSourceVersionsProvider`, moved `/api/search` SQL miss/stale/source-version payload assembly and expected source-version proof out of `Application`, preserved search API shape and enqueue reasons, and added service/API/static guard coverage. `search` remains implementation-gap-open because app-owned refresh producer/invalidation helpers remain; next boundary is `read-models:search-refresh-producer-invalidation-boundary-audit`.
- Completed `read-models:search-refresh-producer-invalidation-boundary-audit` and `read-models:search-refresh-producer-invalidation-service-extraction`: audited app-owned search refresh enqueue/invalidation helpers, added `SearchReadModelRefreshProducer`, moved search refresh enqueue and invalidation scope normalization out of `Application`, preserved gateway-backed enqueue/reason/metadata behavior, and guarded old helpers from returning. `search` remains implementation-gap-open pending local closure audit; next boundary is `read-models:search-local-implementation-closure-audit`.
- Completed `read-models:search-local-implementation-closure-audit` as analysis/accounting only and immediately closed `read-models:search-production-repository-unavailable-fail-closed`: the audit found production PostgreSQL `/api/search` could still fall back to legacy/local live search when the SQL repository was unavailable; the implementation now returns `read_model_unavailable`, enqueues `api_sql_repository_unavailable`, and avoids live scan. `search` remains implementation-gap-open pending post-fail-closed local closure audit.
- Completed `read-models:search-post-fail-closed-local-implementation-closure-audit` as analysis/accounting only and immediately closed `read-models:search-oa-projection-sync-refresh-producer-boundary-extraction`: the audit found `OAProjectionSyncService` still directly enqueued Search refresh scopes through generic `ReadModelRefreshGateway.enqueue_many("search", ...)`; the implementation routes that fan-out through `SearchReadModelRefreshProducer`, wires the producer explicitly in worker assembly, and adds service/static guard coverage. `search` remains implementation-gap-open pending post-OA-sync local closure audit.
- Completed `read-models:search-post-oa-projection-sync-local-implementation-closure-audit` as analysis/accounting only and immediately closed `read-models:search-runtime-import-state-refresh-producer-boundary-extraction`: the audit found runtime import-state Search fan-out still used generic `_enqueue_scopes("search", ...)`; the implementation routes that fan-out through `SearchReadModelRefreshProducer` and adds runtime worker scope/static guard coverage. `search` remains implementation-gap-open pending post-runtime-import-state local closure audit.
- Completed `read-models:search-post-runtime-import-state-local-implementation-closure-audit` as analysis/accounting only and immediately closed `read-models:search-all-scope-worker-fanout-producer-boundary-extraction`: the audit found Search worker `search:all` shard fan-out still directly used `ReadModelRefreshGateway.enqueue_many("search", ...)`; the implementation routes that fan-out through `SearchReadModelRefreshProducer.enqueue_scope_keys(...)`, preserves shard order/payload shape, and extends service/static guard coverage. `search` remains implementation-gap-open pending post-all-scope-worker-fanout local closure audit.
- Completed `read-models:search-post-all-scope-worker-fanout-local-implementation-closure-audit` as analysis/accounting only: re-audited Search after repository port, query freshness service, refresh producer, production fail-closed behavior, OA projection sync producer boundary, runtime import-state producer boundary and all-scope worker fan-out producer boundary; found no remaining local implementation gap, moved Search local support to `production-evidence-deferred` without claiming module closure, and inserted `read-models:next-pilot-selection-after-search` before blocked Go candidates.
- Completed `read-models:next-pilot-selection-after-search` as analysis/planning only: selected `bank_account_balance` as the next non-Go read model pilot because it is the remaining known read model candidate, is user-visible on Bank Details accounts, participates in bank import write-operation SLO, and still has repository port/query ownership gaps. Created `docs/modules/bank-account-balance/` maintenance skeleton and queued `read-models:bank-account-balance-repository-port-extraction` as the first implementation boundary.
- Completed `read-models:bank-account-balance-repository-port-extraction`: added `BankAccountBalanceReadModelRepositoryPort`, routed account balance projection save and Bank Details accounts SQL read paths through the explicit account-balance port, updated manifest owner, and kept the Bank Detail port balance method as transition compatibility fallback. `bank_account_balance` remains implementation-gap-open; next boundary is `read-models:bank-account-balance-refresh-freshness-operation-barrier-audit`.
- Completed `read-models:bank-account-balance-refresh-freshness-operation-barrier-audit` as analysis/accounting only: audited app-owned refresh enqueue, derived lifecycle executor, runtime import-state fan-out, all-only worker/storage vs month/all scope policy, operation barrier evidence and Bank Detail compatibility fallback. Selected `read-models:bank-account-balance-refresh-producer-extraction` as the next narrow implementation boundary; `bank_account_balance` remains implementation-gap-open and Go/Fiber/Go Worker admission remains blocked.
- Completed `read-models:bank-account-balance-refresh-producer-extraction`: added `BankAccountBalanceReadModelRefreshProducer`, routed Application, Bank Details service injection, runtime import-state fan-out, runtime derived lifecycle fan-out and backfill enqueue through it, and preserved `bank_account_balance:all` as the only publish scope. `bank_account_balance` remains implementation-gap-open; next boundary is `read-models:bank-account-balance-derived-lifecycle-executor-extraction`.
- Completed `read-models:bank-account-balance-derived-lifecycle-executor-extraction`: added `BankAccountBalanceDerivedLifecycleExecutor`, moved account-balance derived lifecycle response assembly out of `Application`, preserved `deleted_counts` / `invalidated_scopes` / `enqueued_jobs` payload shape, and guarded the old app helper from returning. `bank_account_balance` remains implementation-gap-open; next boundary is `read-models:bank-account-balance-all-only-scope-contract`.
- Completed `read-models:bank-account-balance-all-only-scope-contract`: changed `bank_account_balance` scope policy to all-only, proved `ReadModelRefreshGateway` accepts only `bank_account_balance:all` and rejects month/account/active scopes before durable enqueue, and kept producer/worker all-only behavior unchanged. `bank_account_balance` remains implementation-gap-open; next boundary is `read-models:bank-account-balance-operation-barrier-regression`.
- Completed `read-models:bank-account-balance-operation-barrier-regression`: added dedicated `OperationFreshnessBarrierService` regressions proving `bank_account_balance:all` dirty/readiness and outbox pending states keep the accounts freshness target refreshing, while unrelated read model outbox pending does not block it. `bank_account_balance` remains implementation-gap-open; next boundary is `read-models:bank-account-balance-bank-detail-fallback-quarantine`.
- Completed `read-models:bank-account-balance-bank-detail-fallback-quarantine`: removed the Bank Detail repository port account-balance fallback, made accounts SQL reads require `BankAccountBalanceReadModelRepositoryPort`, preserved missing-table refreshing/enqueue behavior through the account-balance repository slot, and added a static guard preventing fallback from returning. `bank_account_balance` remains implementation-gap-open pending local closure audit.
- Completed `read-models:bank-account-balance-local-implementation-closure-audit` as analysis/accounting only: re-audited account-balance routes, service, repository port, projection save, refresh producer, runtime worker fan-out, backfill, all-only scope policy, operation barrier and fallback guards; found no remaining local implementation gap, moved local support to `production-evidence-deferred` without claiming module closure, and inserted `go-hot-path:performance-baseline-and-admission-reconciliation` before any Go implementation.
- Completed `go-hot-path:performance-baseline-and-admission-reconciliation` as planning/accounting only: reconciled Go/Fiber/Go Worker admission gates against current local read model support and SLO tooling, found no candidate can start implementation yet, kept actual Go admissions `blocked-by-prerequisite`, and inserted `go-hot-path:workbench-compute-performance-baseline-contract` as the next bounded planning slice.
- Completed `go-hot-path:workbench-compute-performance-baseline-contract` as planning/accounting only: documented the Workbench compute Python reference boundary, candidate input/output contract, state/events/read model dependencies, permissions/audit assumptions, shadow forbidden writes, minimum performance evidence, local-vs-production evidence split and rollback gates. Go implementation/admission remains blocked; next boundary is `go-hot-path:workbench-compute-python-reference-contract-guards`.
- Completed `go-hot-path:workbench-compute-python-reference-contract-guards`: added static guards proving Workbench compute state writes stay owned by Python dirty worker/orchestrator/engine boundaries and that Go shadow/admission remains blocked behind forbidden-write and queue prerequisites. Go implementation/admission remains blocked; next boundary is `go-hot-path:workbench-compute-performance-evidence-collector-contract`.
- Completed `go-hot-path:workbench-compute-performance-evidence-collector-contract`: added read-only `workbench_compute_evidence` tooling and fake-connection tests covering matching p95/p99, scope samples, worker heartbeat, candidate/decision counts, active generation row counts, matching-originated enqueue-to-fresh, query timing/EXPLAIN, structured configuration-missing output and partial evidence admission blocking. Go implementation/admission remains blocked; next boundary is `go-hot-path:workbench-compute-production-evidence-gate`.
- Completed `go-hot-path:workbench-compute-production-evidence-gate` as production-evidence-deferred: local collector returned structured `configuration_missing`; production SSH read-only discovery confirmed active workers but the deployed release lacks the collector, and a deployed-runtime read-only PostgreSQL sampling attempt could not connect. No production writes, deploys, restarts, requeues, readiness mutations or Go implementation were performed. Go admission remains blocked; next boundary is `planning:post-workbench-compute-evidence-gate-next-boundary-selection`.
- Completed `planning:post-workbench-compute-evidence-gate-next-boundary-selection`: reconciled roadmap/queue after the Workbench compute evidence defer, skipped all Go admission rows because performance evidence, shadow diff and rollback proof are still missing, and selected `server-py:residual-route-handler-boundary-audit` as the next safe non-Go shared-boundary audit. No runtime behavior changed.
- Completed `server-py:residual-route-handler-boundary-audit` as analysis/accounting only: scanned residual `server.py` route/handler/helper surfaces, found 21519 lines, 1062 functions, 205 `_handle_api_*` functions and Workbench as the largest residual owner group, then selected `server-py:workbench-legacy-action-handler-quarantine-audit` as the next narrow audit. No runtime behavior changed.
- Completed `server-py:workbench-legacy-action-handler-quarantine-audit` as analysis/accounting only: classified old `/workbench/actions/*` handlers as test-observed compat paths backed by `ManualReconciliationService`/`LedgerService`, classified modern `/api/workbench/actions/*` wrappers as `WorkbenchWriteFacade` delegates, and selected `server-py:legacy-workbench-action-route-module-quarantine` as the next narrow implementation boundary. No runtime behavior changed.
- Completed `server-py:legacy-workbench-action-route-module-quarantine`: added `LegacyWorkbenchActionRoutes`, moved old `/workbench/actions/confirm|difference|exception|offline|offset` payload mapping and reconciliation/ledger calls behind it, removed the five old app-owned handlers from `Application`, and guarded modern `/api/workbench/actions/*` facade delegation. Next boundary is `server-py:legacy-workbench-exception-helper-dead-code-audit`.
- Completed `server-py:legacy-workbench-exception-helper-dead-code-audit`: removed no-caller `_handle_legacy_workbench_exception_via_application(...)`, cleaned the unused conflict import, and extended the legacy Workbench action quarantine guard. Modern `/api/workbench/exception/*` and legacy `/workbench/actions/*` behavior are unchanged. Next boundary is `server-py:modern-workbench-action-route-owner-audit`.
- Completed `server-py:modern-workbench-action-route-owner-audit` as analysis/accounting only: classified modern `/api/workbench/actions/*` and `/api/workbench/exception/*` wrappers by JSON/auth/freshness/timing responsibility, facade/application-service delegate, tests and target owner. Selected `server-py:workbench-exception-preview-route-owner-extraction` as the first narrow modern route-owner implementation boundary. No runtime behavior changed.
- Completed `server-py:workbench-exception-preview-route-owner-extraction`: added `WorkbenchActionApiRoutes`, moved `/api/workbench/exception/preview` payload/error mapping behind it, kept `Application` as HTTP dispatch/body parser/response serializer, and guarded route owner registration plus no app-owned preview mapping regression. Next boundary is `server-py:workbench-exception-apply-route-owner-extraction`.
- Completed `server-py:workbench-exception-apply-route-owner-extraction`: extended `WorkbenchActionApiRoutes` with explicit Workbench write facade provider dependency, moved `/api/workbench/exception/apply` facade delegation, actor fallback, request-id forwarding and `exception_apply` action-name mapping behind it, and kept `Application` as HTTP dispatch/body parser/freshness gate/write-response serializer. Added static guard coverage for app-owned mapping removal. Next boundary is `server-py:workbench-confirm-link-preview-route-owner-extraction`.
- Completed `server-py:workbench-confirm-link-preview-route-owner-extraction`: added `WorkbenchActionApiRoutes.confirm_link_preview(...)`, moved `/api/workbench/actions/confirm-link/preview` facade delegation and invalid-request mapping behind it, kept `Application` as HTTP dispatch/body parser/response serializer, and guarded no app-owned preview mapping regression. Next boundary is `server-py:workbench-confirm-link-submit-route-owner-extraction`.
- Completed `server-py:workbench-confirm-link-submit-route-owner-extraction`: added `WorkbenchActionApiRoutes.confirm_link(...)`, moved `/api/workbench/actions/confirm-link` live facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/auth context/timing/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-mark-exception-route-owner-extraction`.
- Completed `server-py:workbench-mark-exception-route-owner-extraction`: added `WorkbenchActionApiRoutes.mark_exception(...)`, moved `/api/workbench/actions/mark-exception` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-cancel-link-route-owner-extraction`.
- Completed `server-py:workbench-cancel-link-route-owner-extraction`: added `WorkbenchActionApiRoutes.cancel_link(...)`, moved `/api/workbench/actions/cancel-link` live facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/auth context/timing/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-withdraw-link-route-owner-extraction`.
- Completed `server-py:workbench-withdraw-link-route-owner-extraction`: added `WorkbenchActionApiRoutes.withdraw_link(...)`, moved `/api/workbench/actions/withdraw-link` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/auth context/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-cash-special-route-owner-extraction`.
- Completed `server-py:workbench-cash-special-route-owner-extraction`: added `WorkbenchActionApiRoutes.confirm_cash_pass_through(...)`, `confirm_cash_ticket_purchase(...)` and `cancel_cash_special(...)`, moved cash special facade delegation behind them, kept `Application` as HTTP dispatch/body parser/freshness guard/request-id/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-update-bank-exception-route-owner-extraction`.
- Completed `server-py:workbench-update-bank-exception-route-owner-extraction`: added `WorkbenchActionApiRoutes.update_bank_exception(...)`, moved `/api/workbench/actions/update-bank-exception` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-oa-bank-exception-route-owner-extraction`.
- Completed `server-py:workbench-oa-bank-exception-route-owner-extraction`: added `WorkbenchActionApiRoutes.oa_bank_exception(...)`, moved `/api/workbench/actions/oa-bank-exception` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-personal-advance-repayment-route-owner-extraction`.
- Completed `server-py:workbench-personal-advance-repayment-route-owner-extraction`: added `WorkbenchActionApiRoutes.confirm_personal_advance_repayment(...)`, moved `/api/workbench/actions/confirm-personal-advance-repayment` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/request-id/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-cancel-exception-route-owner-extraction`.
- Completed `server-py:workbench-cancel-exception-route-owner-extraction`: added `WorkbenchActionApiRoutes.cancel_exception(...)`, moved `/api/workbench/actions/cancel-exception` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/live-workbench dispatch/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-ignore-row-route-owner-extraction`.
- Completed `server-py:workbench-ignore-row-route-owner-extraction`: added `WorkbenchActionApiRoutes.ignore_row(...)`, moved `/api/workbench/actions/ignore-row` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/response owner, and guarded no app-owned direct facade call regression. Next boundary is `server-py:workbench-unignore-row-route-owner-extraction`.
- Completed `server-py:workbench-unignore-row-route-owner-extraction`: added `WorkbenchActionApiRoutes.unignore_row(...)`, moved `/api/workbench/actions/unignore-row` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/freshness guard/response owner, and removed the last modern direct facade helper exception from the legacy quarantine guard. Next boundary is `server-py:modern-workbench-action-route-owner-post-extraction-audit`.
- Completed `server-py:modern-workbench-action-route-owner-post-extraction-audit`: audited modern Workbench action route-owner residuals after ignore/unignore closure, found `Application._handle_api_workbench_withdraw_link_preview(...)` still directly delegates to `WorkbenchWriteFacade.preview_withdraw_link(...)`, and selected `server-py:workbench-withdraw-link-preview-route-owner-extraction` as the next boundary.
- Completed `server-py:workbench-withdraw-link-preview-route-owner-extraction`: added `WorkbenchActionApiRoutes.withdraw_link_preview(...)`, moved `/api/workbench/actions/withdraw-link/preview` facade delegation behind it, kept `Application` as HTTP dispatch/body parser/response owner, and guarded no app-owned direct preview facade call regression. Next boundary is `server-py:modern-workbench-action-route-owner-final-residual-audit`.
- Completed `server-py:modern-workbench-action-route-owner-final-residual-audit`: found no remaining app-owned direct `WorkbenchWriteFacade` action delegation in the audited modern Workbench action surface, confirmed legacy route quarantine remains compat-only, and selected `server-py:workbench-cancel-exception-live-dispatch-noop-cleanup` as the next boundary.
- Completed `server-py:workbench-cancel-exception-live-dispatch-noop-cleanup`: removed the redundant `has_rows_for_month(...)` branch from `/api/workbench/actions/cancel-exception`, kept JSON parsing/freshness guard/route-owner delegation/response mapping unchanged, and guarded against reintroducing the no-op branch. Next boundary is `server-py:modern-workbench-action-route-owner-local-closure-audit`.
- Completed `server-py:modern-workbench-action-route-owner-local-closure-audit` as analysis/accounting only: verified modern Workbench action delegation is locally owned by `WorkbenchActionApiRoutes`, confirmed no direct `_workbench_write_facade().` action call sites remain in `server.py`, `routes_workbench_actions.py` or `routes_legacy_workbench_actions.py`, kept legacy Workbench actions quarantined as compat-only, and selected `server-py:workbench-row-detail-route-owner-audit` as the next narrow server ownership boundary. No runtime behavior changed.
- Completed `server-py:workbench-row-detail-route-owner-audit` as analysis/accounting only: audited `GET /api/workbench/rows/{row_id}` ownership, confirmed the live/cache/SQL `WorkbenchQueryFacade` fallback and no-write relation boundary are locally tested, identified `Application` fallback orchestration as the remaining ownership gap, and selected `server-py:workbench-row-detail-route-owner-extraction` as the next narrow implementation boundary. No runtime behavior changed.
- Completed `server-py:workbench-row-detail-route-owner-extraction`: added `WorkbenchRowDetailApiRoutes`, moved row detail payload/fallback orchestration out of `Application`, preserved ETC/live/cache/SQL/opaque-OA/legacy fallback order plus row override behavior, and kept production PostgreSQL route fallback blocking. Next boundary is `server-py:workbench-group-detail-route-owner-audit`.
- Completed `server-py:workbench-group-detail-route-owner-audit` as analysis/accounting only: audited `GET /api/workbench/groups/detail` ownership, confirmed `WorkbenchQueryFacade.group_detail(...)` owns freshness/source-version/read-model-status proof and stale refresh enqueue behavior, classified `Application._handle_api_workbench_group_detail(...)` as HTTP validation plus response mapping only, and selected `server-py:workbench-group-detail-route-owner-extraction` as the next narrow implementation boundary. No runtime behavior changed.
- Completed `planning:parallel-orchestration-workflow`: added controller-led parallel orchestration rules, direct-dev write lease, controller/worker permissions, file ownership, worker handoff format, final closure audit gate and 10 thread prompts. This enables worker auto-progress inside assigned workstreams while keeping global state and closure controller-owned. Next executable boundary remains `server-py:workbench-group-detail-route-owner-extraction`.
- Completed `planning:parallel-handoff-review-and-state-update`: T0 consumed T1-T8 handoffs, accepted the worker batch in commit `b60a343a`, verified targeted backend/frontend/docs checks, marked group detail route-owner extraction implemented, accepted read-model/worker/frontend/legacy/module-contract guard slices, accepted T6 as partial production-read-only evidence, and kept Go admission deferred. Next boundary is `planning:post-parallel-handoff-next-boundary-selection`.

## 2026-06-25

- Prepared `planning:commit-backed-state-reconciliation` as the next mandatory controller boundary after user challenged state-file accuracy. Future T0 runs must compute completion percentages from actual git commits/diffs/tests, classify stale state-file claims, update `STATE.md` / `MODULE-QUEUE.md` / `JOURNAL.md` / `NEXT-PROMPT.md` from commit evidence, and only then select the next implementation or worker wave. This entry does not claim a new real completion percentage.
- Strengthened the primary T0 Meta Orchestrator prompt so T0 and all generated workers must report user-facing results in Simplified Chinese, and every worker prompt must include a bounded GSD workflow: preflight, full analysis, plan, narrow implementation, verification, self-review, handoff and Chinese final report. This does not change the next executable boundary.
- Clarified the production closure policy: because there is no staging database and no local `PGSQL_URL`, `ssh finops-prod-root` is the sanctioned T0-only path for non-secret production evidence and controlled production operations. T0 must use a bounded runbook, pre/post checks and rollback/cleanup proof before any production operation; workers must not execute the gate.
- Expanded the T0 production-operation authorization: T0 may perform all reasonable controlled production operations required for full modular IO closure, including bounded explicit-scope refresh/rebuild, queue/requeue/worker-drain checks, deploy/restart when required by a selected boundary, and reversible explicit-scope repair/apply commands, provided the runbook gate proves bounded scope, pre/post checks and rollback/cleanup safety.
- Completed `planning:commit-backed-state-reconciliation` as `planning-closed`: wrote `analysis/commit-backed-state-reconciliation-2026-06-25.md`, verified `dev` at `5c9fe947` is clean and aligned with `origin/dev`, confirmed 226 commits beyond `origin/main`, reconciled `b60a343a` / `5653f982` as accepted worker integration/accounting commits, and computed evidence metrics from queue + git evidence. Current accounting: 124 local proof/guard rows, 79 docs/analysis-only rows, 22 deferred rows, no `Module Closure = closed`, production evidence closure 0/17 and Go admission 0/5. Next boundary is `planning:post-parallel-handoff-next-boundary-selection`.
- Completed `planning:post-parallel-handoff-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-parallel-handoff-next-boundary-selection-2026-06-25.md` and selected `production:readiness-and-worker-status-controlled-read-only-runbook` as the next safe T0-only boundary. Reason: T6 found `/health/ready` timeouts and `fin-ops-worker@workbench.service` `activating/auto-restart`, which blocks production evidence closure across modules. No worker wave was created because controlled production evidence is controller-owned; no production command was executed in this planning slice.
- Completed `production:readiness-and-worker-status-controlled-read-only-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-readiness-worker-status-controlled-read-only-2026-06-25.md` using only non-secret read-only root SSH checks. Evidence: `/health` returned ready, `/health/ready` still timed out after 12s, selected workers/dispatcher were active but had high restart counts, and Workbench worker logs showed `psycopg_pool.PoolTimeout` plus local PostgreSQL shared-memory errors. No env secrets, DSNs, DB writes, readiness/queue mutations, service restarts, deploys or worker replays were performed. Next boundary is `production:postgres-shared-memory-read-only-diagnosis`.
- Completed `production:postgres-shared-memory-read-only-diagnosis` as `production-evidence-deferred`: wrote and executed `analysis/production-postgres-shared-memory-read-only-diagnosis-2026-06-25.md` using only non-secret read-only root SSH checks. Evidence: PostgreSQL is active and listening on 5432, app/worker processes are repeatedly young, `/dev/shm` has free capacity, but PostgreSQL file logs continuously report missing `/PostgreSQL.2926794240` shared-memory segment and `/dev/shm` has no matching PostgreSQL objects. No DB connection, secret read, service restart, readiness/queue mutation or worker replay was performed. Next boundary is `production:postgres-controlled-restart-runbook`.
- Completed `production:postgres-controlled-restart-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-postgres-controlled-restart-runbook-2026-06-25.md`. T0 ran exactly one `systemctl restart postgresql.service`; it exited 0, PostgreSQL returned `active/running`, `/dev/shm/PostgreSQL.*` objects reappeared and PostgreSQL logs show the new postmaster ready. `/health/ready` still timed out after 15s and selected workers/dispatcher remained recently restarted, so production readiness and worker evidence remain deferred. No secrets, DB writes, queue/readiness mutations, deploys or worker replays were performed. Next boundary is `production:app-worker-controlled-restart-readiness-runbook`.
- Completed `production:app-worker-controlled-restart-readiness-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-app-worker-controlled-restart-readiness-runbook-2026-06-25.md`. T0 ran one bounded restart of `fin-ops.service`, `fin-ops-rabbitmq-dispatcher.service` and 20 explicit `fin-ops-worker@*.service` units after PostgreSQL shared-memory recovery. The restart exited 0; API, dispatcher, PostgreSQL and all selected workers returned `active/running` with `NRestarts=0`; `/health/ready` returned `status=ready` in 2.8s immediately and 0.6s on stability recheck; sampled post-restart logs showed no new PoolTimeout/shared-memory errors. No secrets, DB writes, queue/readiness mutations, deploys, requeues, worker replays or business-data mutation were performed. Full production closure remains deferred because one stale `no_oa_bank_batch:all` dirty scope and one dead-lettered `no_oa_bank_batch.read_model.refresh` event remain. Next boundary is `production:no-oa-bank-batch-dead-letter-read-only-diagnosis`.
- Completed `production:no-oa-bank-batch-dead-letter-read-only-diagnosis` as `production-evidence-deferred`: wrote and executed `analysis/production-no-oa-bank-batch-dead-letter-read-only-diagnosis-2026-06-25.md` with root SSH health summary and read-only PostgreSQL transaction. Evidence: `no_oa_bank_batch:all` dirty scope is pending at source version 35430, readiness is failed, 14 all-scope refresh events dead-lettered on `app.no_oa_bank_batch_events_no_oa_bank_batch_id_fkey`, and the failed referenced `superseded` batch still exists with 6 event rows. No DB writes, requeue, repair, replay, readiness mutation, deploy or secret output were performed. Next boundary is `read-models:no-oa-bank-batch-event-fk-delete-order-fix`.
- Completed `read-models:no-oa-bank-batch-event-fk-delete-order-fix` as `implementation-closed`: fixed `PostgresWorkbenchRepository.save_no_oa_bank_batches(...)` to delete events for removed no-OA batches before deleting removed batch rows, including empty snapshot replacement, added repository boundary regressions, and updated no-OA module implementation notes. Verification passed for repository boundary tests, no-OA refresh tests, py_compile, docs verify and diff check. Production closure remains deferred until deploy/convergence proof. Next boundary is `production:no-oa-bank-batch-fk-fix-deploy-and-convergence-runbook`.
- Completed `production:no-oa-bank-batch-fk-fix-deploy-and-convergence-runbook` as `production-controlled`: deployed release `dev-no-oa-fk-20260625014906` from clean `dev`, verified active release `git_commit=cc43e262eeb13c1a459d0f96e991666d0db2f280`, requeued exact event `3bc506fd-5662-4902-a9b9-19b0d8fbe4a6` once after deploy, and proved the event done, `no_oa_bank_batch:all` dirty scope done, readiness fresh and `/health/ready` clean with no active queue/dirty/failed/stale blockers. No broad DB mutation, broad replay, manual mark-done, repair apply, secret output or arbitrary SQL mutation was performed. Historical obsolete dead-letter rows remain but are not current health blockers. Next boundary is `planning:post-no-oa-production-convergence-next-boundary-selection`.
- Completed `planning:post-no-oa-production-convergence-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-no-oa-production-convergence-next-boundary-selection-2026-06-25.md` and selected `production:post-convergence-readiness-worker-db-aggregate-evidence-sweep` as the next read-only boundary. Rationale: after PostgreSQL restart, app/worker restart, no-OA deploy and exact event convergence, T0 needs a fresh production aggregate baseline before selecting further cleanup or claiming more closure. No production command was executed in this planning slice.
- Completed `production:post-convergence-readiness-worker-db-aggregate-evidence-sweep` as `production-evidence-deferred`: read-only production evidence proved the deployed release `dev-no-oa-fk-20260625014906` has `/health` and `/health/ready` ready, no active dirty scopes, all App Status readiness rows fresh, exact no-OA event `3bc506fd-5662-4902-a9b9-19b0d8fbe4a6` done and no-OA readiness fresh. The same sweep found `fin-ops-worker@workbench-matching.service` in `activating/auto-restart` with rising restart count; logs show `WorkbenchMatchingOrchestrator.__init__()` rejecting stale keyword `pair_relation_service` from `runtime_worker_handlers.py`. No production writes, requeues, resolve, repair, deploy, restart, worker replay, readiness mutation or secret output occurred. Next boundary is `runtime-workers:workbench-matching-orchestrator-constructor-fix`.
- Completed `runtime-workers:workbench-matching-orchestrator-constructor-fix` as `implementation-closed`: fixed `WorkbenchMatchingWorkerFactory.build_dirty_scope_worker(...)` so the orchestrator receives `relation_read_port=WorkbenchMatchingRelationReadPort(pair_relation_service)` instead of the stale `pair_relation_service` keyword, preserved existing pair-relation snapshot and relation command-service wiring, added runtime boundary guard coverage, and verified py_compile, targeted guard, Workbench matching orchestrator tests, docs verify and diff check. Production convergence remains deferred to the next bounded deploy/runbook boundary.
- Completed `production:workbench-matching-constructor-fix-deploy-and-convergence-runbook` as `production-controlled`: deployed release `dev-workbench-matching-port-20260625020818` at `b256db3a8fc370ce93e7b51bf62b1cd33176475d`, verified `fin-ops-worker@workbench-matching.service` stayed `active/running` across a 15s recheck with stable `MainPID=3380166` and `NRestarts=0`, `/health` and `/health/ready` returned ready, PostgreSQL aggregates showed dirty scopes all done and readiness rows all fresh, and post-deploy workbench-matching logs contained no constructor `TypeError`. No DB writes, requeue, resolve, repair, worker replay, readiness mutation, historical dead-letter cleanup or secret output occurred. Next boundary is `planning:post-workbench-matching-production-convergence-next-boundary-selection`.
- Completed `planning:post-workbench-matching-production-convergence-next-boundary-selection` as `planning-closed`: reconciled the latest no-OA and workbench-matching production-controlled evidence, confirmed current health/readiness/dirty scopes are clean, and selected `production:historical-dead-letter-covered-resolution-read-only-maintenance-plan` as the next safe boundary. The next slice must classify the 24 historical dead-letter rows with read-only inspect/dry-run evidence only; no cleanup, DB/queue/readiness mutation or global/module closure is claimed.
- Completed `production:historical-dead-letter-covered-resolution-read-only-maintenance-plan` as `analysis-closed`: `/health/ready` remained ready, dirty scopes were all done, readiness rows were all fresh, and `runtime_queue_ops resolve-covered-dead-letters --limit 100 --dry-run` reported `candidate_count=24`, `eligible_count=24`, `resolved_count=0`; every candidate had `fresh_readiness`, `later_done` and `active_dirty_count=0` proof. No cleanup or DB/queue/readiness mutation occurred. Decision: next boundary is a separate controlled apply runbook.
- Completed `production:historical-dead-letter-covered-resolution-apply-runbook` as `production-controlled`: wrote `analysis/production-historical-dead-letter-covered-resolution-apply-runbook-2026-06-25.md`, rechecked `/health/ready`, dirty scopes, readiness and `resolve-covered-dead-letters --dry-run`, then executed `resolve-covered-dead-letters --execute --reason historical_covered_dead_letter_resolution_20260625` once after dry-run stayed `candidate_count=24`, `eligible_count=24`. The command resolved 24 covered historical read-model dead-letter rows. Post-check proved `job.outbox_events` has only `done=203169`, dead-letter groups are empty, dirty scopes remain `done=187007`, readiness remains `fresh=498`, `/health/ready` remains ready and follow-up dry-run has zero candidates. No requeue, repair, worker replay, direct SQL, readiness mutation or secret output occurred. Next boundary is `planning:post-historical-dead-letter-resolution-next-boundary-selection`.
- Completed `planning:post-historical-dead-letter-resolution-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-historical-dead-letter-resolution-next-boundary-selection-2026-06-25.md`, reconciled row241 production-controlled evidence, confirmed dead-letter residue cleanup does not prove module/global closure, and selected `production:post-dead-letter-resolution-global-readiness-worker-db-evidence-sweep` as the next safe T0-owned boundary. No production command or runtime mutation occurred in this planning slice.
- Completed `production:post-dead-letter-resolution-global-readiness-worker-db-evidence-sweep` as `production-controlled`: wrote and executed `analysis/production-post-dead-letter-resolution-global-readiness-worker-db-evidence-sweep-2026-06-25.md` using only non-secret read-only health, systemd, deployed-runtime DB aggregate, dry-run and log-grep checks. Evidence: `/health` and `/health/ready` ready and release-consistent; required worker missing/stale/mismatch counts all 0; API, dispatcher and 20 worker units active/running with `NRestarts=0` across a stability recheck; outbox only `done=203169`; dirty scopes `done=187007`; readiness `fresh=498`; no read-model dead-letter groups; covered-dead-letter dry-run zero candidates; recent worker error grep returned no matches. No deploy, restart, requeue, repair, worker replay, DB/readiness/dirty-scope mutation or secret output occurred. Next boundary is `planning:post-production-baseline-module-closure-wave-selection`.
- Completed `planning:post-production-baseline-module-closure-wave-selection` as `planning-closed`: wrote `analysis/planning-post-production-baseline-module-closure-wave-selection-2026-06-25.md`, reconciled the clean row243 production baseline against remaining production-evidence-deferred/module-open rows, rejected final closure and Go work as premature, deferred worker waves until production facts are matrixed, and selected `production:read-model-production-evidence-matrix-read-only-sweep` as the next T0-only boundary. No production command or runtime mutation occurred in this planning slice.
- Completed `production:read-model-production-evidence-matrix-read-only-sweep` as `production-controlled`: wrote and executed `analysis/production-read-model-production-evidence-matrix-read-only-sweep-2026-06-25.md` using only non-secret read-only `/health/ready`, deployed-runtime PostgreSQL aggregate, information_schema row-count/source-version discovery and worker heartbeat/systemd evidence. Evidence: all App Status read-model readiness rows are fresh; all dirty scopes are done; all read-model outbox refresh events are done; no read-model dead-letter groups remain; current read-model workers have fresh heartbeats; read-model row-count/source-version tables are queryable; Workbench high-row counts are visible. The matrix identified historical legacy `cost`/`tax` done dirty-scope rows and remaining browser/API/high-row/module-specific closure gaps. No production mutation or secret output occurred. Next boundary is `production:read-model-scope-contract-runtime-dry-run-classification`.
- Completed `production:read-model-scope-contract-runtime-dry-run-classification` as `production-controlled`: wrote and executed `analysis/production-read-model-scope-contract-runtime-dry-run-classification-2026-06-25.md` using only non-secret `/health/ready`, `read-model-scope-contract --json`, `--repair invalid-read-model-scopes --json` and a read-only deployed-runtime legacy `cost`/`tax` aggregate. Evidence: `/health/ready` ready on the active API port, cost-statistics scope contract `ok=true` with `violation_count=0`, no covered historical or current uncovered failures, invalid scope dry-run `ok=true` with `invalid_scope_count=0`, and legacy `cost`/`tax` rows are historical `done` dirty scopes only with no non-done samples, no active outbox rows and no readiness rows. No `--apply`, production mutation, requeue, repair, replay, restart or secret output occurred. Next boundary is `planning:post-scope-contract-runtime-classification-next-boundary-selection`.
- Completed `planning:post-scope-contract-runtime-classification-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-scope-contract-runtime-classification-next-boundary-selection-2026-06-25.md`, reconciled row245 clean read-model production matrix and row246 clean scope-contract classification, rejected final closure, production cleanup, immediate worker creation and Go admission as premature, and selected `planning:read-model-module-closure-evidence-ownership-map` as the next safe boundary. The next boundary must map read-model-heavy modules, local evidence, row245/246 production facts, remaining API/browser/high-row gaps, T0-vs-worker ownership and exact worker/single-thread execution before any closure claim or worker wave.
- Completed `planning:read-model-module-closure-evidence-ownership-map` as `planning-closed`: wrote `analysis/read-model-module-closure-evidence-ownership-map-2026-06-25.md`, mapped read-model-heavy modules to route/API surfaces, local docs/test owners, row245/246 production facts, remaining authenticated API/browser/high-row gaps and evidence type, then selected `planning:read-model-module-closure-worker-wave-1-prompts` as the next boundary. No worker thread, production command, runtime mutation, module closure or global closure was claimed.
- Started `planning:read-model-module-closure-worker-wave-1-prompts`: wrote `analysis/read-model-module-closure-worker-wave-1-prompts-2026-06-25.md`, committed the prompt file in `71ef441d`, released the direct-dev write lease, and created four local project worker threads. W1 `019efb08-6669-7eb1-b5a2-166639ce50af` owns Workbench/relations/turnover evidence; W2 `019efb08-8ff0-74a1-b0c9-300f39c96f73` owns invoice/OA family evidence; W3 `019efb08-b871-7e00-9c36-8b621210d64b` owns bank/pending/no-OA/search evidence; W4 `019efb08-e2a8-7722-8acd-452cd9629269` owns cost/tax evidence. Row249 remains open until T0 monitors final answers and verifies handoffs; no worker result has been accepted and no closure is claimed.
- Completed `planning:read-model-module-closure-worker-wave-1-monitor-and-accept` as `planning-closed`: T0 read final answers for W1-W4, verified commits `bf03ba98`, `82eb8919`, `cfc495f1` and `525818ba` only added assigned handoff files, and accepted all four handoffs as local evidence/gap maps in `analysis/read-model-module-closure-worker-wave-1-acceptance-2026-06-25.md`. The accepted handoffs preserve `closure-not-claimed`; browser/API/high-row smoke and module-specific closure evidence remain open. Next boundary is `planning:read-model-authenticated-api-browser-smoke-runbook-selection`.
- Completed `planning:read-model-authenticated-api-browser-smoke-runbook-selection` as `planning-closed`: wrote `analysis/read-model-authenticated-api-browser-smoke-runbook-selection-2026-06-25.md`, selected `production:read-model-authenticated-api-response-shape-smoke-runbook` as the next T0-owned boundary, and deferred browser smoke until the API runbook proves a non-secret authentication/harness path. No production command, mutation, deploy, restart, requeue, repair, replay or closure claim occurred.
- Completed `production:read-model-authenticated-api-response-shape-smoke-runbook` as `production-evidence-deferred`: wrote `analysis/production-read-model-authenticated-api-response-shape-smoke-runbook-2026-06-25.md`, confirmed `/health/ready` was ready, found no configured `FIN_OPS_HTTP_SLO_BEARER_TOKEN`, `FIN_OPS_HTTP_SLO_ADMIN_TOKEN` or `FIN_OPS_HTTP_SLO_COOKIE`, and therefore did not run authenticated API smoke. Post-checks showed dirty scopes `done=187007`, readiness `fresh=498` and read-model outbox `done=202898`. No production mutation, deploy, restart, requeue, repair, replay, secret output or closure claim occurred. Next boundary is `production:read-model-public-page-shell-smoke-runbook`.
- Completed `production:read-model-public-page-shell-smoke-runbook` as `production-controlled`: wrote and executed `analysis/production-read-model-public-page-shell-smoke-runbook-2026-06-25.md` using unauthenticated page-shell-only production checks. The initial `http://127.0.0.1:18001` probe returned 17/17 404 because it used the API listener as frontend base, so T0 classified it as wrong-base operator evidence. The public base `https://www.yn-sourcing.com/fin-ops/` was reachable, and the rerun returned `status=pass`, `probe_count=17`, `failed_probe_count=0`, `max_p95_ms=27.782`; all default `/fin-ops/*` page-shell routes returned 200 and `/health/ready` stayed ready before and after. No API probes, production mutation, deploy, restart, requeue, repair, replay, DB/queue/readiness mutation, response body storage, secret output or closure claim occurred. Next boundary is `planning:post-public-page-shell-smoke-next-boundary-selection`.
- Completed `planning:post-public-page-shell-smoke-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-public-page-shell-smoke-next-boundary-selection-2026-06-25.md`, reconciled row252 auth-missing API defer and row253 public page-shell success, rejected authenticated API retry/browser data smoke/final closure/new worker wave as premature, and selected `production:read-model-shadow-read-rehearsal-read-only-runbook` as the next T0-owned evidence boundary. No production command, deploy, restart, requeue, repair, replay, DB/queue/readiness mutation, secret output or closure claim occurred.
- Completed `production:read-model-shadow-read-rehearsal-read-only-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-read-model-shadow-read-rehearsal-read-only-runbook-2026-06-25.md`. `/health/ready` stayed ready before and after, the deployed tool was available, direct shell execution failed safely due missing DB config, and runtime-env execution returned redacted JSON with `gate_recommendation=BLOCKED`, `mismatched_domains=5`, `primary_errors=1`, `shadow_errors=1`. The blocker is evidence quality rather than a closure proof: current production `local_pickle` is not a comparable primary for PostgreSQL runtime, and `workbench_read_models` hit a PostgreSQL statement timeout. No payload rows, secrets, deploy, restart, requeue, repair, replay, DB/queue/readiness mutation or closure claim occurred. Next boundary is `planning:post-shadow-read-rehearsal-next-boundary-selection`.
- Completed `planning:post-shadow-read-rehearsal-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-shadow-read-rehearsal-next-boundary-selection-2026-06-25.md`, reconciled Row255, rejected `local_pickle` parity retry, `postgres` vs `postgres`, authenticated API retry, browser smoke and worker waves as premature, and selected `production:workbench-read-model-high-row-query-plan-read-only-runbook` as the next T0-owned evidence boundary. No production command, deploy, restart, requeue, repair, replay, DB/queue/readiness mutation, secret output or closure claim occurred.
- Completed `production:workbench-read-model-high-row-query-plan-read-only-runbook` as `production-controlled`: wrote and executed `analysis/production-workbench-read-model-high-row-query-plan-read-only-runbook-2026-06-25.md` using only read-only PostgreSQL aggregate/index/EXPLAIN probes. Evidence: active Workbench generations are bounded (`active` metadata rows 3491, largest active scope 1624 rows) while historical physical tables are large (`workbench_rows=654911`, `workbench_group_rows=729629`); representative active page-like queries use generation/scope indexes and corrected EXPLAIN completed inside the timeout. The first EXPLAIN attempt had an ambiguous `scope_key` runbook bug and was corrected; no writes occurred. No payload rows, secrets, deploy, restart, requeue, repair, replay, DB/queue/readiness mutation or closure claim occurred. Next boundary is `planning:post-workbench-high-row-query-plan-next-boundary-selection`.
- Completed `planning:post-workbench-high-row-query-plan-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-workbench-high-row-query-plan-next-boundary-selection-2026-06-25.md`, reconciled Row257, rejected final closure, authenticated HTTP retry, browser data smoke and new in-process harness work as premature, and selected `production:read-model-unauthenticated-api-status-shape-classification-runbook` as the next evidence boundary. It will classify existing API probe status/shape metadata without auth secrets or response bodies and will not claim authenticated API closure.
- Completed `production:read-model-unauthenticated-api-status-shape-classification-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-read-model-unauthenticated-api-status-shape-classification-runbook-2026-06-25.md` using API-only `http_slo_probe --allow-unauthenticated`. All 38 default API probes returned 401 with low latency, so public API surfaces are consistently auth-gated and unauthenticated probing cannot produce read-model response-shape closure evidence. `/health/ready` stayed ready before and after. No response bodies, payload rows, secrets, deploy, restart, requeue, repair, replay, DB/queue/readiness mutation or closure claim occurred. Next boundary is `planning:post-unauthenticated-api-classification-next-boundary-selection`.
- Completed `planning:post-unauthenticated-api-classification-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-unauthenticated-api-classification-next-boundary-selection-2026-06-25.md`, reconciled Row259's 38/38 unauthenticated API 401 classification, rejected authenticated HTTP retry, public browser data smoke, another unauthenticated route sweep and final closure as premature, and selected `planning:read-model-internal-api-contract-harness-design` as the next safe boundary. CodeGraph confirmed the backend is not Flask and existing local tests exercise `Application.handle_request(...)`, so the next slice should design a local/internal response-shape harness using existing auth/session seams and route-owner/application seams. No production command, runtime mutation, implementation change, worker thread or closure claim occurred.
- Completed `planning:read-model-internal-api-contract-harness-design` as `planning-closed`: wrote `analysis/planning-read-model-internal-api-contract-harness-design-2026-06-25.md`, designed the local/internal API contract harness around `Application.handle_request(...)`, existing unittest default auth plus explicit negative auth guard checks, `http_slo_probe.DEFAULT_API_PROBES` as route inventory, and sanitized envelope/readiness metadata assertions. The design keeps production auth/browser evidence deferred, rejects Flask test client and production auth bypass, and selects `contract:read-model-internal-api-contract-harness-implementation` as the next smallest implementation boundary. No production command, runtime auth/code change, worker thread or closure claim occurred.
- Completed `contract:read-model-internal-api-contract-harness-implementation` as `contract-guard-closed`: added `tests/test_read_model_api_contract_harness.py`, a local GET-only `Application.handle_request(...)` harness covering representative read-model-heavy API envelopes and explicit auth guard negatives. Targeted verification `PYTHONPATH=backend/src pytest -q tests/test_read_model_api_contract_harness.py` passed with 2 tests and 51 subtests. This adds local API contract evidence only; production API/browser/high-row/module closure remains open. Next boundary is `planning:post-internal-api-contract-harness-next-boundary-selection`.
- Completed `planning:post-internal-api-contract-harness-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-internal-api-contract-harness-next-boundary-selection-2026-06-25.md`, reconciled Row262 local API contract harness evidence, rejected production auth retry, final closure and immediate full e2e smoke as premature, deferred broadening all API probes, and selected `planning:read-model-browser-data-harness-coverage-map` as the next safe boundary. No production command, runtime code change, worker thread or closure claim occurred.
- Completed `planning:read-model-browser-data-harness-coverage-map` as `planning-closed`: wrote `analysis/planning-read-model-browser-data-harness-coverage-map-2026-06-25.md`, mapped read-model-heavy modules to existing deterministic Playwright/Vitest evidence, Row262 local API harness coverage, Row245/246/257 production-controlled facts and external-risk gaps, and selected `browser:read-model-browser-data-targeted-smoke-runbook` as the next smallest executable boundary. No browser test, production command, runtime mutation, worker thread or closure claim occurred.
- Completed `browser:read-model-browser-data-targeted-smoke-runbook` as `browser-guard-closed`: first targeted Playwright run produced 49/53 with four stale spec assertions; T0 updated `input-invoice-usage-flow.spec.ts` and `workbench-stale-error-flow.spec.ts` to match current operation-barrier, non-fresh copy, exact locator and Workbench projection contracts, reran the two affected specs with 20/20 passed, then reran the full Row265 subset with 53/53 passed. No production command, product code change, worker thread or closure claim occurred.
- Completed `planning:post-browser-data-targeted-smoke-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-browser-data-targeted-smoke-next-boundary-selection-2026-06-25.md`, reconciled Row265 targeted Browser evidence, rejected production/auth retry and new harness work as premature, and selected `browser:read-model-full-deterministic-e2e-smoke-runbook` because Row265 found stale assertions in smoke specs and full deterministic smoke is the repository's broad local Browser evidence layer. No browser test, production command, runtime mutation, worker thread or closure claim occurred.
- Completed `browser:read-model-full-deterministic-e2e-smoke-runbook` as `browser-guard-closed`: wrote `analysis/browser-read-model-full-deterministic-e2e-smoke-runbook-2026-06-25.md` after running `cd web && npm run e2e:smoke`; the full deterministic Chromium smoke inventory passed with 175/175 tests in 7.6m. This adds broad local browser regression evidence only. No production command, product code change, smoke membership change, runtime mutation, worker thread or closure claim occurred. Next boundary is `planning:post-full-deterministic-e2e-smoke-next-boundary-selection`.
- Completed `planning:post-full-deterministic-e2e-smoke-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-full-deterministic-e2e-smoke-next-boundary-selection-2026-06-25.md`, reconciled Row267 full local smoke against remaining production/API/high-row/worker external-risk gaps, rejected browser/high-row/write-after-read/closure boundaries as premature, and selected `production:read-model-auth-preflight-and-api-smoke-runbook`. No production command, runtime mutation, worker thread or closure claim occurred.
- Completed `production:read-model-auth-preflight-and-api-smoke-runbook` as `production-evidence-deferred`: wrote `analysis/production-read-model-auth-preflight-and-api-smoke-runbook-2026-06-25.md`, confirmed `/health/ready` ready, found `http_slo_auth_configured=no`, skipped authenticated API smoke per stop gate, and confirmed dirty scopes `done=187007`, readiness `fresh=498` and read-model outbox `done=202898`. No secrets, response bodies, production mutation, worker thread or closure claim occurred.
- Completed `planning:post-auth-preflight-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-auth-preflight-next-boundary-selection-2026-06-25.md`, reconciled Row269 auth-missing production preflight, rejected repeating production auth/browser/high-row/write evidence while auth is absent, deferred human gate packaging, and selected `contract:read-model-default-api-probe-harness-broadening` as the next executable local evidence boundary. No test, production command, mutation, worker thread or closure claim occurred.
- Completed `contract:read-model-default-api-probe-harness-broadening` as `contract-guard-closed`: wrote `analysis/contract-read-model-default-api-probe-harness-broadening-2026-06-25.md`, broadened `tests/test_read_model_api_contract_harness.py` over `http_slo_probe.DEFAULT_API_PROBES`, classified admin 403, local unavailable 503 and import facts 501 explicitly, and verified `PYTHONPATH=backend/src pytest -q tests/test_read_model_api_contract_harness.py` with 2 tests and 84 subtests passed. No production command, worker thread or closure claim occurred.
- Completed `planning:post-default-api-probe-harness-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-default-api-probe-harness-next-boundary-selection-2026-06-25.md`, reconciled Row271 all-probe local API harness evidence with Row245/269 production facts and remaining API/browser/high-row/worker gaps, rejected waiting for human-provided auth secrets because the active goal authorizes T0 root SSH controlled production evidence, and selected `production:read-model-controlled-production-api-browser-runbook` as the next T0-owned boundary. No production command, mutation, worker thread or closure claim occurred.
- Completed `production:read-model-controlled-production-api-browser-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-read-model-controlled-production-api-browser-runbook-2026-06-25.md`. T0 used `ssh finops-prod-root` and target OA applicant credentials in a remote Python process to run user-scope authenticated API metadata probes without printing/storing tokens, cookies, passwords, env values, response bodies or payload rows. The session was full-access user scope with `can_admin_access=false`. Initial 37 user-scope probes had 30 pass / 7 fail; focused retry reduced failures to pending invoice rows/filter-options and no-OA bank batches. GET fresh gates triggered bounded read-model refresh enqueues; final `/health/ready` remained ready, dirty scopes were all done, readiness all fresh, read-model outbox all done and dead letters empty. Browser/admin/write closure remains open. Next boundary is `production:pending-invoice-no-oa-api-freshness-mismatch-read-only-diagnosis`.
- Completed `production:pending-invoice-no-oa-api-freshness-mismatch-read-only-diagnosis` as `production-diagnosis-closed`: wrote and executed `analysis/production-pending-invoice-no-oa-api-freshness-mismatch-read-only-diagnosis-2026-06-25.md` using root SSH and direct deployed PostgreSQL read repositories only. Pending invoice `expense:all` repository payload is fresh/populated but API expected-source gate reports source-version mismatches, explaining rows HTTP 200/refreshing and filter-options HTTP 202/refreshing. no-OA dirty/outbox/readiness are clean, but API stale is explained by application-level row source-version comparison being stricter than App Status aggregate readiness. No production API endpoint call, payload row output, secret output or production mutation occurred. Next boundary is `production:pending-invoice-no-oa-source-version-contract-deep-diagnosis`.
- Completed `production:pending-invoice-no-oa-source-version-contract-deep-diagnosis` as `production-diagnosis-closed`: wrote and executed `analysis/production-pending-invoice-no-oa-source-version-contract-deep-diagnosis-2026-06-25.md` using root SSH and direct deployed PostgreSQL read-only metadata. Pending invoice expected source versions hash `8ecc010b5db0bd95` still mismatched aggregate actual hash `ffdfe1c6e3e27b01`; aggregate proof uses 32 historical `expense:all:%` shard rows, recent outbox completed aggregate plus 2026 month shards, and writer/API contracts drift on `invoice_lifecycle_policy_schema_version`. no-OA base expected hash `65e9060b8cee23f2` versus row hash `6d33251a850b453d` produced exact base mismatch `bank_transaction_category_snapshot_version_mismatch`, while App Status readiness remains `all/fresh` with only aggregate `source_version`. No production API endpoint call, payload row output, secret output or production mutation occurred. Next boundary is `read-models:pending-invoice-source-version-contract-alignment`.
- Completed `read-models:pending-invoice-source-version-contract-alignment` as `implementation-closed`: aligned pending invoice API expected source versions with SQL projection writer output, including `invoice_lifecycle_policy_schema_version` and stable downstream source-version dict keys, and changed aggregate source-version proof to prefer non-empty month shards over zero-row historical shards. Added `tests.test_search_pending_sql_runtime` coverage for writer/API parity and zero-row historical shard pollution; updated pending invoice module docs. Verification passed for `tests.test_search_pending_sql_runtime`, `tests.test_pending_invoice_api`, `tests.test_read_model_architecture_guards` and py_compile. No production command or mutation occurred. Next boundary is `production:pending-invoice-source-version-contract-deploy-and-convergence-runbook`.
- Completed `production:pending-invoice-source-version-contract-deploy-and-convergence-runbook` as `production-controlled`: wrote `analysis/production-pending-invoice-source-version-contract-deploy-and-convergence-runbook-2026-06-25.md`, deployed release `dev-pending-invoice-source-17d13466-20260625` at git commit `3329d8954a7219a1c21641392aaa3f5448ec20f5`, and ran one bounded `pending_invoice=expense:all` refresh smoke. The smoke event was `done`, dirty scope was `done`, enqueue-to-done was 326.029ms and handler duration was 134.48ms; the smoke tool timed out only because the explicit override scope had no App Status readiness row. A no-enqueue sanitized metadata probe proved pending invoice rows and filter-options for `expense:all` are `fresh`, source-version stale reasons are empty, and actual source-version keys include `invoice_lifecycle_policy_schema_version`, `bank_detail_source_versions` and `workbench_relation_source_versions`. Post-checks kept `/health/ready` ready, selected pending invoice dirty scopes/outbox done and pending invoice readiness fresh. No broad repair, no-OA repair, manual mark-done, direct SQL mutation, payload-row output or secret output occurred. Next boundary is `production:no-oa-bank-batch-category-source-version-mismatch-diagnosis`.
- Completed `production:no-oa-bank-batch-category-source-version-mismatch-diagnosis` as `production-diagnosis-closed`: wrote and executed `analysis/production-no-oa-bank-batch-category-source-version-mismatch-diagnosis-2026-06-25.md` using only read-only root SSH/deployed PostgreSQL metadata. Current `month=2026-06,bucket=unsubmitted` no-OA rows have no source-version mismatch: actual category snapshot hash prefix `b1533c3ad8c74afa` equals the deployed expected category snapshot, row source-version hash is `6d33251a850b453d`, and extra downstream keys are ignored by the mismatch helper as designed. Dirty/outbox/readiness evidence shows completed `no_oa_bank_batch:all` refreshes at `2026-06-25 05:02:09+08`, readiness `all/fresh`, and no recent dead letters. No production API endpoint call, payload-row output, secret output, refresh command, requeue, repair, direct DB mutation or readiness mutation occurred. Next boundary is `planning:post-no-oa-category-source-version-diagnosis-next-boundary-selection`.
- Completed `planning:post-no-oa-category-source-version-diagnosis-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-no-oa-category-source-version-diagnosis-next-boundary-selection-2026-06-25.md`, reconciled Row273's remaining user-scope API failures with Row277 pending invoice convergence and Row278 no-OA category source-version diagnosis, and selected `production:read-model-focused-user-scope-api-metadata-resmoke-runbook` as the next T0-owned boundary. The next boundary must first probe `pending_invoices_rows`, `pending_invoices_filter_options` and `no_oa_bank_batches`, then optionally run all non-admin user-scope default API probes if the focused set passes. No production command, mutation, worker thread or closure claim occurred in this planning slice.
- Completed `production:read-model-focused-user-scope-api-metadata-resmoke-runbook` as `production-evidence-deferred`: wrote and executed `analysis/production-read-model-focused-user-scope-api-metadata-resmoke-runbook-2026-06-25.md`. The focused user-scope API metadata re-smoke proved `pending_invoices_rows` and `pending_invoices_filter_options` now pass with HTTP 200, `read_model_status=fresh`, no refresh enqueue and p95 under 1s. `no_oa_bank_batches` still failed with HTTP 200, `read_model_status=stale` and one GET-triggered refresh enqueue, so T0 obeyed the stop gate and did not run the full non-admin user-scope probes. Postcheck kept `/health/ready` ready, dirty scopes done, readiness fresh, read-model outbox done and no dead letters; the GET-triggered no-OA refresh was done. No browser/admin/write probe, manual enqueue/requeue/repair/replay/direct DB mutation/readiness mutation/secret output occurred. Next boundary is `production:no-oa-bank-batches-api-stale-read-only-diagnosis`.
- Completed `production:no-oa-bank-batches-api-stale-read-only-diagnosis` as `production-diagnosis-closed`: wrote and executed `analysis/production-no-oa-bank-batches-api-stale-read-only-diagnosis-2026-06-25.md` using only read-only root SSH/deployed PostgreSQL metadata. After correcting deployed constructor/import/table-column compatibility and exact pair-relation snapshot reconstruction, current `month=2026-06,bucket=unsubmitted` no-OA rows have 0 source-version mismatches against both exact base expected and optional downstream expected contracts. Summary/detail scopes each have 8 `draft/unsubmitted` rows generated at `2026-06-25 06:11:29+08`; dirty/outbox/readiness are done/fresh and no read-model dead letters exist. Row280's API stale result is classified as stale persisted rows before the GET-triggered refresh, now cleared by the completed refresh. No production API endpoint call, payload-row output, secret output, refresh command, requeue, repair, direct DB mutation or readiness mutation occurred. Next boundary is `production:no-oa-bank-batches-focused-api-freshness-recheck`.
- Completed `production:no-oa-bank-batches-focused-api-freshness-recheck` as `production-evidence-deferred`: wrote and executed `analysis/production-no-oa-bank-batches-focused-api-freshness-recheck-2026-06-25.md`. T0 reused the Row280 in-process target OA applicant credential seam without printing/storing credentials or tokens, ran only `no_oa_bank_batches`, and proved the endpoint still fails freshness metadata despite Row281 DB parity: HTTP 200, p95 `165.274ms`, `read_model_status=stale`, `refresh_enqueued_count=1`. Full user-scope/browser/admin/write probes were not run. Postcheck kept `/health/ready` ready, dirty scopes done, readiness fresh, read-model outbox done and no dead letters; the GET-triggered no-OA refresh converged to done/fresh. No response body, payload-row output, secret output, manual refresh/requeue/repair/replay/direct DB mutation/readiness mutation occurred. Next boundary is `production:no-oa-bank-batches-api-stale-reasons-sanitized-probe`.
- Completed `production:no-oa-bank-batches-api-stale-reasons-sanitized-probe` as `implementation-closed`: wrote and executed `analysis/production-no-oa-bank-batches-api-stale-reasons-sanitized-probe-2026-06-25.md`. T0 printed only sanitized freshness metadata and proved the live no-OA API stale reason is `workbench_read_model_schema_version_mismatch`; response payload rows and identifiers were stripped. The GET-triggered refresh converged done/fresh. Root cause was no-OA API expected source versions using legacy `WORKBENCH_READ_MODEL_SCHEMA_VERSION` while worker rows use `WORKBENCH_SQL_PROJECTION_SCHEMA_VERSION`. Added `_no_oa_bank_batch_workbench_source_versions()`, wired no-OA API service to it, added regression coverage in `tests/test_no_oa_bank_batch_read_model_refresh.py`, updated no-OA tests docs, and verified no-OA application/refresh/workbench integration plus targeted platform guard. No production deploy occurred yet. Next boundary is `production:no-oa-source-version-provider-fix-deploy-and-convergence`.
- Completed `production:no-oa-source-version-provider-fix-deploy-and-convergence` as `production-controlled`: wrote `analysis/production-no-oa-source-version-provider-fix-deploy-and-convergence-2026-06-25.md`, committed/pushed pre-deploy runbook evidence, and deployed release `dev-no-oa-source-version-480d2d0e-20260625` at git commit `d117b4519284db00c0fa88bdf7faaa938a5b1f69`, which contains fix commit `480d2d0ebe3bbf3543a8b0a855ec252fae4bbc1a`. Focused authenticated `no_oa_bank_batches` metadata now passes with HTTP 200, `read_model_status=fresh`, `refresh_enqueued_count=0`, p95 `138.167ms`, and non-admin full-access session metadata. Postcheck kept `/health/ready` ready, dirty scopes done, readiness fresh, outbox done and read-model dead letters empty. No payload rows, response bodies, secrets, direct DB/queue/readiness mutation, repair, requeue or worker replay occurred. Next boundary is `production:read-model-full-user-scope-api-metadata-smoke-after-no-oa-fix`.
- Completed `production:read-model-full-user-scope-api-metadata-smoke-after-no-oa-fix` as `production-evidence-deferred`: wrote and executed `analysis/production-read-model-full-user-scope-api-metadata-smoke-after-no-oa-fix-2026-06-25.md`. T0 reused the in-process target OA applicant credential seam and ran all 37 non-admin user-scope `http_slo_probe.DEFAULT_API_PROBES` with `include_samples=False`; all probes passed, failed probe count was 0, max p95 was `757.465ms`, all reported read-model metadata was fresh and the SLO report showed zero refresh enqueues. Postcheck stayed `/health/ready` ready, readiness fresh and no read-model dead letters, but aggregate dirty/outbox totals each increased by one due to `turnover_ledger.read_model.refresh` for `turnover_ledger:all`, already done. Because aggregate no-enqueue was not proven, the next boundary is `production:turnover-ledger-user-scope-hidden-refresh-enqueue-diagnosis`.
- Completed `production:turnover-ledger-user-scope-hidden-refresh-enqueue-diagnosis` as `production-diagnosis-closed`: wrote and executed `analysis/production-turnover-ledger-user-scope-hidden-refresh-enqueue-diagnosis-2026-06-25.md`. Focused authenticated `GET /api/turnover-ledger?view=grouped&page=1&page_size=50` returned HTTP 200 with no top-level read-model metadata (`read_model_status`, `refresh_enqueued`, `refresh_reason`, stale reasons all null), and postcheck proved it created one additional `turnover_ledger.read_model.refresh` / `turnover_ledger:all` dirty scope that converged to done. Health/readiness/dead letters stayed clean, no payload rows or secrets were printed, and no direct mutation occurred beyond the GET-triggered fresh gate. Diagnosis: grouped GET can still return legacy grouped payload without freshness metadata while causing a read-model refresh side effect. Next boundary is `read-models:turnover-ledger-grouped-query-metadata-boundary-fix`.
- Completed `read-models:turnover-ledger-grouped-query-metadata-boundary-fix` as `implementation-closed`: wrote `analysis/read-model-turnover-ledger-grouped-query-metadata-boundary-fix-2026-06-25.md`, changed `_flat_payload_to_grouped(...)` to preserve top-level SQL/read-model freshness/enqueue/source-version metadata while removing legacy `rows`, and added grouped fresh/stale API regressions. Verification passed for targeted turnover grouped/query tests, the broader turnover API/query/read-facade suite, compileall, docs verification and diff checks. No production deploy occurred yet. Next boundary is `production:turnover-ledger-grouped-metadata-fix-deploy-and-resmoke`.
- Completed `production:turnover-ledger-grouped-metadata-fix-deploy-and-resmoke` as `production-controlled`: wrote and executed `analysis/production-turnover-ledger-grouped-metadata-fix-deploy-and-resmoke-2026-06-25.md`, deployed release `dev-turnover-grouped-metadata-20260625` at git commit `2dbacf9f6054baabe7084fc87b87511a49bbdb95`, and ran one focused authenticated grouped turnover metadata probe. The response now exposed `read_model_status=refreshing`, `refresh_enqueued=true`, `refresh_reason=source_version_mismatch` and stale reason `turnover_relation_snapshot_version_mismatch`; postcheck proved the visible refresh converged done, readiness stayed fresh and dead letters stayed empty. Next boundary is `production:turnover-ledger-relation-snapshot-source-version-mismatch-diagnosis`.
- Completed `production:turnover-ledger-relation-snapshot-source-version-mismatch-diagnosis` as `production-diagnosis-closed`: wrote and executed `analysis/production-turnover-ledger-relation-snapshot-source-version-mismatch-diagnosis-2026-06-25.md` with read-only production metadata only. API expected source versions and `TurnoverLedgerSqlProjectionBuilder` provider source versions agree on the current relation snapshot hash, but persisted turnover read-model top-level and first-row source versions still carry the older hash after the Row288 refresh reached done; App Status readiness remains fresh and no dead letters exist. Next boundary is `read-models:turnover-ledger-refresh-source-version-persistence-contract-fix`.
- Completed `read-models:turnover-ledger-refresh-source-version-persistence-contract-fix` as `implementation-closed`: wrote `analysis/read-model-turnover-ledger-refresh-source-version-persistence-contract-fix-2026-06-25.md`, moved turnover projection source-version capture before grouped row collection can mutate relation snapshot state, and added a regression proving saved top-level and row-level source versions use the pre-rebuild value. Verification passed for turnover read-model refresh, turnover query/read-facade/API suites and compileall. Next boundary is `production:turnover-ledger-source-version-persistence-fix-deploy-and-convergence`.
- Completed `production:turnover-ledger-source-version-persistence-fix-deploy-and-convergence` as `production-controlled`: wrote and executed `analysis/production-turnover-ledger-source-version-persistence-fix-deploy-and-convergence-2026-06-25.md`, deployed release `dev-turnover-source-version-persistence-20260625` at git commit `8f525563e10972168014356ff410c4fc8456f377`, and proved convergence. The first focused grouped GET visibly enqueued against old persisted source versions; after worker convergence, persisted top-level/row-level relation snapshot source versions matched API expected, focused grouped GET returned `read_model_status=fresh` and `refresh_enqueued=false`, and final aggregate dirty/outbox counts did not increase. Next boundary is `production:read-model-full-user-scope-api-metadata-smoke-after-turnover-fixes`.
- Completed `production:read-model-full-user-scope-api-metadata-smoke-after-turnover-fixes` as `production-controlled`: wrote and executed `analysis/production-read-model-full-user-scope-api-metadata-smoke-after-turnover-fixes-2026-06-25.md`. Full non-admin user-scope API metadata smoke ran 37 probes through target OA applicant credentials; all passed with 0 failed probes, 0 non-fresh probes and 0 refresh-enqueued probes. Pre/post aggregate dirty scopes, readiness, read-model outbox and dead letters were unchanged. Next boundary is `planning:post-full-user-api-smoke-browser-admin-write-evidence-selection`.
- Completed `planning:post-full-user-api-smoke-browser-admin-write-evidence-selection` as `planning-closed`: wrote `analysis/planning-post-full-user-api-smoke-browser-admin-write-evidence-selection-2026-06-25.md`, reconciled Row292's clean full non-admin user-scope API smoke, rejected repeating API smoke and rejected closure because browser/admin/write evidence remains open. Selected `production:read-model-authenticated-browser-page-smoke-runbook` as the next lowest-risk evidence boundary. Admin evidence remains deferred without a proven non-secret admin seam, and write-flow evidence remains deferred until an operation-specific rollback/idempotency/audit/convergence runbook exists. No production command, browser/admin/write probe, mutation, worker thread or closure claim occurred.
- Started `production:read-model-authenticated-browser-page-smoke-runbook`: wrote `analysis/production-read-model-authenticated-browser-page-smoke-runbook-2026-06-25.md` with a bounded read-only browser runbook. The runbook logs into the target OA applicant seam on the production host, passes the token only as an in-memory subprocess env var to the existing `production-route-shell.spec.ts`, forbids admin/write/export/download/package-install operations, and requires pre/post health/dirty/readiness/outbox/dead-letter checks. Execution is pending until the runbook commit is pushed.
- Completed `production:read-model-authenticated-browser-page-smoke-runbook` as `production-evidence-deferred`: after committing/pushing the runbook, T0 prechecked active release `dev-turnover-source-version-persistence-20260625`, `/health/ready`, dirty scopes, readiness, read-model outbox and dead letters clean. Browser execution stopped because the deployed production source lacks both `web/node_modules/.bin/playwright` and `web/e2e/production-route-shell.spec.ts`. T0 did not install packages, download browsers, copy tokens, run local Playwright, run admin/write probes or mutate production; postcheck stayed clean. Next boundary is `planning:post-authenticated-browser-harness-missing-next-boundary-selection`.
- Completed `planning:post-authenticated-browser-harness-missing-next-boundary-selection` as `planning-closed`: wrote `analysis/planning-post-authenticated-browser-harness-missing-next-boundary-selection-2026-06-25.md`, reconciled Row294 as a deploy artifact/harness gap, and selected `deployment:production-browser-smoke-harness-packaging-feasibility-audit` as the next boundary. Direct deploy archive modification, production package install/browser download, local token-copy Playwright, admin seam classification, write-flow planning and closure were rejected or deferred for this slice. No production command, browser/admin/write probe, mutation, worker thread or closure claim occurred.
- Completed `deployment:production-browser-smoke-harness-packaging-feasibility-audit` as `analysis-closed`: wrote `analysis/deployment-production-browser-smoke-harness-packaging-feasibility-audit-2026-06-25.md`, audited deploy release artifacts and browser harness options, and kept production browser evidence deferred because packaging e2e files alone cannot run without Playwright/browser runtime while packaging `node_modules`/browser binaries or installing on production is too broad. Selected `production:admin-scope-auth-seam-read-only-classification` next. No production command, deploy change, browser/admin/write probe, mutation, worker thread or closure claim occurred.
- Started `production:admin-scope-auth-seam-read-only-classification`: wrote `analysis/production-admin-scope-auth-seam-read-only-classification-2026-06-25.md` with a read-only runbook to classify supported admin HTTP SLO env presence and target OA applicant live session admin capability. Execution is pending until the runbook commit is pushed; browser/write probes remain out of scope.
- Completed `production:admin-scope-auth-seam-read-only-classification` as `production-evidence-deferred`: after committing/pushing the runbook, T0 prechecked active release/health/dirty/readiness/outbox/dead-letter clean, confirmed no `FIN_OPS_HTTP_SLO_ADMIN_TOKEN` or `FIN_OPS_HTTP_SLO_COOKIE` is configured, and classified 2 configured target OA applicant live sessions as `full_access` non-admin with `can_admin_access=false`. Optional admin endpoint probe was not run. Postcheck stayed clean; no browser/write probe, admin API probe, secret output or production mutation occurred. Next boundary is `planning:controlled-write-flow-evidence-scenario-selection`.
- Completed `planning:controlled-write-flow-evidence-scenario-selection` as `planning-closed`: wrote `analysis/planning-controlled-write-flow-evidence-scenario-selection-2026-06-25.md`, reconciled write-flow evidence tools/docs, rejected controlled write apply without approval/reversible object/rollback/idempotency/audit acceptance/admin seam, and selected `production:write-flow-scenario-discovery-read-only-runbook` next. No production command, write/browser/admin probe, mutation, worker thread or closure claim occurred.
- Completed `production:write-flow-scenario-discovery-read-only-runbook` as `production-controlled`: wrote and committed/pushed `analysis/production-write-flow-scenario-discovery-read-only-runbook-2026-06-25.md` before execution, then ran read-only sanitized production discovery. All three known operation classes had candidate counts (`turnover_manual_closure_or_withdraw=6`, `workbench_pair_withdraw_context=10`, `no_oa_bank_batch_withdraw_context=10`) and `scenario_count=26`; no identifiers, scenario file, HTTP write, `--apply`, browser/admin probe, secret output or production mutation occurred. Pre/post health, dirty scopes, readiness, read-model outbox and dead letters were unchanged. Controlled write apply remains blocked pending explicit approval, reviewed reversible object, rollback/idempotency/audit acceptance and suitable auth. Next boundary is `planning:post-write-flow-discovery-closure-selection`.
- Completed `planning:post-write-flow-discovery-closure-selection` as `planning-closed`: wrote `analysis/planning-post-write-flow-discovery-closure-selection-2026-06-25.md`, reconciled Row292 API evidence, Row294/296 browser harness gap, Row297 admin seam gap and Row299 read-only write-flow discovery. Final/global closure remains rejected because browser/admin/write apply gates are still open. Controlled write apply and admin probe remain blocked by approval/auth gates. Hard stop was rejected because a safe owned browser ops runner design boundary remains. Next boundary is `deployment:production-browser-smoke-ops-runner-design`.
- Completed `deployment:production-browser-smoke-ops-runner-design` as `analysis-closed`: wrote `analysis/deployment-production-browser-smoke-ops-runner-design-2026-06-25.md`, selecting a dedicated controlled browser runner outside the normal app release and production app host. The design keeps Playwright/browser runtime out of app releases, requires in-memory target OA token handoff, emits sanitized metadata only, excludes admin/write flows and requires pre/post health/read-model aggregate checks. It found one pre-run contract gap: production route-shell failure output can include page body `textSample`. Next boundary is `frontend:production-route-shell-sanitized-output-contract`.
- Completed `frontend:production-route-shell-sanitized-output-contract` as `implementation-closed`: removed `textSample` and `bodyText.slice` from `web/e2e/production-route-shell.spec.ts` failure output while preserving route path, session/loading classifications and mutating request method/path evidence. Extended `tests/test_playwright_e2e_strict_diagnostics.py` to guard the sanitized output contract. Targeted verification passed with 8/8 tests. No production browser smoke, token broker, deploy, package install/download or production command occurred. Next boundary is `deployment:production-browser-smoke-runner-bundle-contract`.
- Completed `deployment:production-browser-smoke-runner-bundle-contract` as `analysis-closed`: wrote `analysis/deployment-production-browser-smoke-runner-bundle-contract-2026-06-25.md`, defining separate runner bundle included files, manifest fields, runtime contract, command shape and artifact redaction rules. Packaging implementation was deferred because `production-route-shell.spec.ts` still imports `strictTest.ts`, whose raw console/pageerror/requestfailed diagnostic details are unsafe for production artifacts. Next boundary is `frontend:production-strict-diagnostics-sanitized-output-contract`.
- Completed `frontend:production-strict-diagnostics-sanitized-output-contract` as `implementation-closed`: added production-only strict diagnostics redaction in `web/e2e/fixtures/strictTest.ts` gated by `FIN_OPS_E2E_PRODUCTION_SMOKE=1`. Local diagnostics remain raw; production console/pageerror/dialog details are `<redacted>`, and requestfailed output keeps only method plus redacted path classification. Extended `tests/test_playwright_e2e_strict_diagnostics.py`; targeted guard passed 9/9 and frontend build passed with existing CSS minification warnings. No production browser smoke, token broker, deploy, package install/download or production command occurred. Next boundary is `deployment:production-browser-smoke-runner-bundle-implementation`.
- Completed `deployment:production-browser-smoke-runner-bundle-implementation` as `implementation-closed`: added local `scripts/package_production_browser_smoke.py`, `tests/test_production_browser_smoke_bundle.py`, and docs updates in `docs/dev/testing.md`, `docs/operations/deployment.md` and `deploy/oa/README.md`. The bundle includes only approved production route-shell files/package metadata plus manifest, excludes `node_modules`, browser binaries, `web/dist`, admin spec and secrets, and does not modify normal app release packaging. Targeted bundle/deploy tests passed 14/14. No production browser smoke, token broker, deploy, package install/download or production command occurred. Next boundary is `deployment:production-browser-smoke-token-broker-runbook`.
- Completed `deployment:production-browser-smoke-token-broker-runbook` as `analysis-closed`: wrote `analysis/deployment-production-browser-smoke-token-broker-runbook-2026-06-25.md`, designing a future root-owned in-memory target OA token broker protocol with session scope checks, private token descriptor handoff, sanitized metadata and stop gates. No broker was implemented/installed, no token was output and no production command ran. Next boundary is `deployment:production-browser-smoke-runner-runtime-availability-classification`.
- Completed `deployment:production-browser-smoke-runner-runtime-availability-classification` as `analysis-closed`: wrote `analysis/deployment-production-browser-smoke-runner-runtime-availability-classification-2026-06-25.md`, classifying runner availability without installing/downloading browsers or running production smoke. Local Playwright exists but is not an approved production evidence runner because no private token broker/wrapper or pinned ops runtime exists; production app host remains unavailable from Row294/296. Browser/admin/write apply gates are now external/operational hard-stop gates. Next boundary is `planning:global-closure-hard-stop-report`.
- Completed `planning:global-closure-hard-stop-report` as `hard-stop-reported`: wrote `analysis/global-closure-hard-stop-report-2026-06-25.md` with commit-backed progress references, completed evidence, and precise browser/admin/write-apply blockers. Current queue had `307/308` non-pending rows before this report and `325` commits beyond `origin/main`, but module global closure remains `0.0%` because no module row is marked closed. T0 did not claim global closure and stopped because the remaining evidence gates require external/operational input: approved browser runner runtime/wrapper, supported admin auth seam, or approved reversible write target with rollback/idempotency/audit acceptance.
- Completed `planning:local-modular-code-closure-reconciliation` as `local-closure-reconciled`: refreshed commit-backed accounting at `1a8924fe`, inventoried `.planning/refactors/**/*.md`, verified CodeGraph status, and audited live local-code facts. The previous production hard stop is now treated as final validation only, because local modular implementation closure is not proven: `server.py` still has `21217` lines / `205` `_handle_api_*` methods, and `postgres_repositories/read_models.py` still has `11415` lines / `158` repository methods. Inserted Row310 `server-py:etc-reconciliation-route-owner-residual-audit` as the next local-first boundary.
- Completed `server-py:etc-reconciliation-route-owner-residual-audit` as `analysis-closed`: wrote `analysis/server-py-etc-reconciliation-route-owner-residual-audit-2026-06-25.md`, read ETC/import/reconciliation module docs and tests, used CodeGraph plus AST inventory to classify residual `Application` route ownership, confirmed `EtcBusinessBatchApiRoutes` already owns `/api/etc/business-batches*`, and selected Row311 `server-py:etc-reconciliation-task-route-owner-facade-extraction` as the next local implementation boundary. No runtime code, production command, production mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-task-route-owner-facade-extraction` as `local-implementation-closed`: added `EtcReconciliationTaskApiRoutes`, delegated `/api/etc/reconciliation-tasks*` from `server.py`, removed unused app-owned root/list/create/ready/detail/subroute helpers, added static route-owner guard coverage, updated ETC implementation notes, and passed py_compile, targeted ETC backend route tests and targeted platform boundary guards. Next boundary is `server-py:etc-reconciliation-task-delete-side-effect-service-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-task-delete-side-effect-service-audit` as `analysis-closed`: wrote `analysis/server-py-etc-reconciliation-task-delete-side-effect-service-audit-2026-06-25.md`, audited task delete/imported-invoice delete callbacks and shared cleanup helpers, rejected moving side effects into the route owner, and selected `server-py:etc-reconciliation-import-cleanup-service-extraction` as the next local implementation boundary. No runtime code, production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-import-cleanup-service-extraction` as `local-implementation-closed`: added `EtcReconciliationImportCleanupService`, moved imported-invoice/task import cleanup helpers out of `server.py`, preserved HTTP response/error mapping in `Application`, added direct service-layer coverage and updated platform guard coverage. Verification passed for py_compile, service/guard tests, targeted ETC cleanup/delete API regressions, docs verify and diff check. Next boundary is `server-py:etc-reconciliation-delete-route-callback-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-delete-route-callback-audit` as `local-implementation-closed`: audited the delete/imported-invoice HTTP callbacks, moved them into `EtcReconciliationTaskApiRoutes`, injected explicit cleanup/expected-version/error/refresh/persist ports from `server.py`, updated the route-owner guard to prevent callback regression, and passed py_compile, targeted route-owner guard and targeted ETC delete/imported-invoice API regressions. Next boundary is `server-py:etc-import-route-owner-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-import-route-owner-audit` as `local-implementation-closed`: added `EtcImportApiRoutes`, moved `/api/etc/import`, `/api/etc/import/preview` and `/api/etc/import/confirm` HTTP ownership out of `server.py`, injected explicit import/session/job/queue/execution ports, added a static route-owner guard, and passed py_compile, targeted import route-owner guard and targeted ETC import preview/confirm/direct import regressions. Next boundary is `server-py:etc-legacy-batch-route-owner-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-legacy-batch-route-owner-audit` as `local-implementation-closed`: added `EtcLegacyBatchApiRoutes`, moved legacy `/api/etc/batches*` URL parsing/dispatch out of `server.py`, kept heavy side effects as explicit callbacks, added a static compat route-owner guard, and passed py_compile, targeted compat route-owner guard and targeted legacy batch list/detail/delete/draft regressions. Next boundary is `server-py:etc-legacy-batch-delete-side-effect-service-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-legacy-batch-delete-side-effect-service-audit` as `local-implementation-closed`: added `EtcLegacyBatchDeleteService`, moved non-business legacy batch DELETE side effects out of `Application`, returned operation-result refresh/persist events, preserved business-batch v2 fallback plus HTTP/error mapping in `server.py`, added direct service coverage and static boundary guard coverage, and passed py_compile, service tests, targeted guard and targeted legacy batch delete/draft repair API regressions. Next boundary is `server-py:etc-legacy-batch-draft-confirm-callback-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-legacy-batch-draft-confirm-callback-audit` as `local-implementation-closed`: added `EtcLegacyBatchLifecycleService`, moved legacy batch OA draft/confirm/reopen lifecycle side effects out of `Application`, returned refresh events, preserved HTTP body/header/detail/error mapping in `server.py`, added service-layer tests and static lifecycle boundary guard coverage, and passed py_compile, service tests, targeted guard and targeted legacy draft/confirm API regressions. Next boundary is `server-py:etc-legacy-batch-read-payload-facade-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-legacy-batch-read-payload-facade-audit` as `analysis-closed`: wrote `analysis/server-py-etc-legacy-batch-read-payload-facade-audit-2026-06-25.md`, audited legacy batch list/detail/count/filter payload ownership, rejected direct route-owner migration as too broad because it would mix unified business/submission/import view and attachment/detail semantics into route code, and selected `server-py:etc-legacy-batch-read-facade-extraction` as the next local implementation boundary. No runtime code, production command, mutation or closure claim occurred.
- Completed `server-py:etc-legacy-batch-read-facade-extraction` as `local-implementation-closed`: added `EtcLegacyBatchReadFacade`, moved legacy batch list/detail/count/filter payload composition out of `Application`, preserved unified business/submission/import view semantics, added facade tests and static read facade guard coverage, and passed py_compile, facade tests, targeted guard, targeted legacy list/detail/query API regressions, docs verify and diff check. Next boundary is `server-py:etc-legacy-batch-route-callback-collapse-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-legacy-batch-route-callback-collapse-audit` as `local-implementation-closed`: moved the remaining legacy batch list/detail/delete/draft/confirm/reopen HTTP mapping into `EtcLegacyBatchApiRoutes`, injected explicit read facade/delete service/lifecycle service/OA client/error/refresh/persist ports, removed the old app-owned legacy batch callbacks from `server.py`, and updated static guards to prevent callback regression. Targeted py_compile, route-owner guards and legacy ETC batch API regressions passed. Next boundary is `server-py:etc-invoice-route-owner-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-invoice-route-owner-audit` as `local-implementation-closed`: added `EtcInvoiceApiRoutes`, moved ETC invoice list and revoke-submitted HTTP mapping out of `Application`, preserved `EtcService` status mutation plus invoice link/refresh ports, and expanded route-owner inventory/static guards. Targeted py_compile, route-owner guards and ETC invoice/revoke regressions passed. Next boundary is `server-py:etc-reconciliation-task-mutation-callback-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-task-mutation-callback-audit` as `analysis-closed`: audited residual reconciliation callbacks still injected from `Application`, split simple mutation mapping from upload/parser-heavy flows, and selected `server-py:etc-reconciliation-simple-mutation-callback-collapse` as the next narrow implementation boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-simple-mutation-callback-collapse` as `local-implementation-closed`: moved source-file delete, item patch, confirm, reopen and refresh-match HTTP mapping into `EtcReconciliationTaskApiRoutes`, removed the corresponding app-owned callbacks, and kept upload/parser-heavy callbacks unchanged for a dedicated audit. Targeted py_compile, route-owner Guard, source-file delete, confirm, stale confirmability and refresh-matches regressions passed. Next boundary is `server-py:etc-reconciliation-upload-parser-callback-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-upload-parser-callback-audit` as `analysis-closed`: audited the remaining upload/parser callbacks, selected supplement-for-card upload as the next narrow implementation boundary, and left generic source upload plus ticket-root text for later parser/source-mode slices. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-supplement-for-card-upload-callback-collapse` as `local-implementation-closed`: moved per-card supplement evidence upload HTTP mapping into `EtcReconciliationTaskApiRoutes`, injected explicit multipart/version/storage-error ports, removed `_handle_api_etc_reconciliation_supplement_for_card_upload(...)` from `server.py`, and added Guard plus storage-error API regression coverage. Next boundary is `server-py:etc-reconciliation-source-upload-parser-boundary-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-source-upload-parser-boundary-audit` as `analysis-closed`: audited generic source upload and confirmed it owns store+parse+apply orchestration plus ticket-root wrong-slot/source-mode/content-type policy, so direct route-owner migration would put parser policy in a route class. Selected `server-py:etc-reconciliation-source-upload-service-extraction` as the next implementation boundary; no runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-source-upload-service-extraction` as `local-implementation-closed`: added `EtcReconciliationSourceUploadService`, moved credit-card/ticket-root/task-level supplement source upload store+parse+apply orchestration and ticket-root source-mode/wrong-slot/content-type policy out of `Application`, updated Guard and targeted service/API regressions, and selected `server-py:etc-reconciliation-ticket-root-text-callback-audit` next. No production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-ticket-root-text-callback-audit` as `analysis-closed`: audited the remaining ticket-root text callback and selected `server-py:etc-reconciliation-ticket-root-text-service-extraction`, extending `EtcReconciliationSourceUploadService` rather than creating a parallel service. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-ticket-root-text-service-extraction` as `local-implementation-closed`: extended `EtcReconciliationSourceUploadService` to own ticket-root text source persistence, source naming, clipboard parser dispatch and parse-result application, leaving `Application` as thin JSON/HTTP mapping. Next boundary is `server-py:etc-reconciliation-upload-route-callback-collapse`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-upload-route-callback-collapse` as `local-implementation-closed`: moved generic source upload and ticket-root text HTTP mapping into `EtcReconciliationTaskApiRoutes`, removed the corresponding app callbacks from `server.py`, and guarded route-owner source upload service delegation. Next boundary is `server-py:etc-reconciliation-route-owner-local-closure-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-route-owner-local-closure-audit` as `analysis-closed`: confirmed `server.py` has zero `_handle_api_etc_reconciliation*` callbacks, but reconciliation task payload/import blocker/imported summary/can-confirm helpers still live in `Application`, so route-owner local closure is not proven. Next boundary is `server-py:etc-reconciliation-task-payload-facade-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-task-payload-facade-audit` as `analysis-closed`: wrote `analysis/server-py-etc-reconciliation-task-payload-facade-audit-2026-06-25.md`, confirmed the remaining reconciliation payload helper group in `Application` owns route-facing response shape, import blockers, imported invoice summary, source/parse issue shaping and `canConfirm` semantics, and selected `server-py:etc-reconciliation-task-payload-facade-extraction` as the next local implementation boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-task-payload-facade-extraction` as `local-implementation-closed`: added `EtcReconciliationTaskPayloadFacade`, moved reconciliation task payload/read-shaping helper implementations out of `Application`, wired `EtcReconciliationTaskApiRoutes` through explicit facade methods, and added facade tests plus API/static Guard regressions. No production command, mutation or closure claim occurred.
- Completed `server-py:etc-reconciliation-post-payload-facade-local-closure-audit` as `analysis-closed`: wrote `analysis/server-py-etc-reconciliation-post-payload-facade-local-closure-audit-2026-06-25.md`, confirmed the ETC reconciliation task route-owner surface is locally closed after route callback, upload/parser, cleanup and payload facade extraction, and selected `server-py:etc-business-batch-delete-fallback-audit` as the next local boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-batch-delete-fallback-audit` as `analysis-closed`: wrote `analysis/server-py-etc-business-batch-delete-fallback-audit-2026-06-25.md`, confirmed `_handle_api_etc_business_batch_delete(...)` still owns broad business-batch delete side-effect orchestration, rejected direct route-owner migration as too broad, and selected `server-py:etc-business-batch-delete-service-extraction` as the next local implementation boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-batch-delete-service-extraction` as `local-implementation-closed`: added `EtcBusinessBatchDeleteService`, moved business-batch delete side-effect orchestration out of `Application`, kept `_handle_api_etc_business_batch_delete(...)` as a thin HTTP body/error/response mapper, and added direct service tests plus API/static Guard regressions. No production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-batch-delete-route-callback-collapse-audit` as `analysis-closed`: wrote `analysis/server-py-etc-business-batch-delete-route-callback-collapse-audit-2026-06-25.md`, confirmed the business-batch DELETE callback is thin after service extraction, and selected `server-py:etc-business-batch-delete-route-callback-collapse` as the next local implementation boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-batch-delete-route-callback-collapse` as `local-implementation-closed`: moved business-batch DELETE HTTP mapping into `EtcBusinessBatchApiRoutes`, injected explicit delete service/JSON/refresh/persist ports, removed `_handle_api_etc_business_batch_delete(...)` from `server.py`, preserved legacy batch delete compatibility through an explicit resolver, and passed targeted py_compile, static Guard, business delete API and delete service regressions. Next boundary is `server-py:etc-business-oa-draft-revoke-callback-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-oa-draft-revoke-callback-audit` as `analysis-closed`: wrote `analysis/server-py-etc-business-oa-draft-revoke-callback-audit-2026-06-25.md`, confirmed OA draft revoke should move into `EtcBusinessBatchApplicationService` plus `EtcBusinessBatchApiRoutes` rather than a new service, and selected `server-py:etc-business-oa-draft-revoke-route-callback-collapse` as the next local implementation boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-oa-draft-revoke-route-callback-collapse` as `local-implementation-closed`: added `EtcBusinessBatchApplicationService.revoke_oa_draft_payload(...)`, moved OA draft revoke mapping into `EtcBusinessBatchApiRoutes`, removed `_handle_api_etc_business_oa_draft_revoke(...)` from `server.py`, added a route API regression and static Guard coverage, and passed targeted py_compile, Guard and OA draft/revoke service/API regressions. Next boundary is `server-py:etc-business-route-owner-local-closure-audit`; no production command, mutation or closure claim occurred.
- Completed `server-py:etc-business-route-owner-local-closure-audit` as `analysis-closed`: wrote `analysis/server-py-etc-business-route-owner-local-closure-audit-2026-06-25.md`, confirmed active ETC business-batch route-owner local support is accounted for and remaining app functions are dispatch/session/body/response wrappers or legacy compatibility resolver, and selected `server-py:input-invoice-usage-oa-reverse-route-owner-audit` as the next residual server.py boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-oa-reverse-route-owner-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-oa-reverse-route-owner-audit-2026-06-25.md`, confirmed OA reverse state/business behavior already lives in `InputInvoiceUsageOaReverseService` while `server.py` owns route dispatch and HTTP mapping, and selected `server-py:input-invoice-usage-oa-reverse-route-owner-facade-extraction` as the next local implementation boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-oa-reverse-route-owner-facade-extraction` as `local-implementation-closed`: added `InputInvoiceUsageOaReverseApiRoutes`, moved preview/history/staged/batch create/get HTTP mapping out of `server.py`, added route-owner inventory/static ownership Guard coverage and targeted OA reverse API regressions, and retained draft create/revoke/status refresh/manual status callbacks in `Application` for `server-py:input-invoice-usage-oa-reverse-draft-mutation-callback-audit`. No production command, runtime mutation or global closure claim occurred.
- Completed `server-py:input-invoice-usage-oa-reverse-draft-mutation-callback-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-oa-reverse-draft-mutation-callback-audit-2026-06-25.md`, confirmed remaining OA reverse mutation callbacks are HTTP/session/body mapping around service-owned state transitions, and selected `server-py:input-invoice-usage-oa-reverse-draft-mutation-route-callback-collapse` with explicit OA provider/client and parse ports. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-oa-reverse-draft-mutation-route-callback-collapse` as `local-implementation-closed`: moved one-step draft, batch draft, draft revoke, status refresh and manual status HTTP mapping into `InputInvoiceUsageOaReverseApiRoutes`, injected explicit OA provider/client and parse ports, removed the remaining OA reverse route handlers from `server.py`, and added Guard/API regressions. No production command, mutation or global closure claim occurred.
- Completed `server-py:input-invoice-usage-oa-reverse-route-owner-local-closure-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-oa-reverse-route-owner-local-closure-audit-2026-06-25.md`, confirmed all `/api/input-invoice-usage/oa-reverse*` HTTP mapping is route-owned and remaining OA reverse `Application` methods are explicit dependency/platform/helper ports, and selected `server-py:input-invoice-usage-core-route-owner-audit` as the next residual server.py boundary. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-core-route-owner-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-core-route-owner-audit-2026-06-25.md`, audited non-OA-reverse input usage rows/filter/export/detail/relation-detail/payment route ownership, and selected `server-py:input-invoice-usage-read-route-owner-facade-extraction` for rows/filter/detail/relation-detail/payment GET while deferring export and payment PUT. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-read-route-owner-facade-extraction` as `local-implementation-closed`: added `InputInvoiceUsageApiRoutes`, moved rows/filter-options/invoice detail/bank detail/OA detail/relation-details/payment-status-rules GET HTTP mapping out of `server.py`, preserved read-model fresh-gate helper ports, and left export plus payment PUT in `Application` for later slices. No production command, mutation or global closure claim occurred.
- Completed `server-py:input-invoice-usage-export-route-owner-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-export-route-owner-audit-2026-06-25.md`, confirmed export service owns preview/workbook/read-model refreshing semantics while `Application` owns auth/audit/XLSX response mapping, and selected export route callback collapse with explicit ports. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-export-route-callback-collapse` as `local-implementation-closed`: moved export preview/download HTTP mapping into `InputInvoiceUsageApiRoutes`, injected explicit export service/auth/query/audit/XLSX response ports, removed `_handle_api_input_invoice_usage_export_preview(...)` and `_handle_api_input_invoice_usage_export(...)` from `server.py`, and expanded static Guard/API regressions. Payment-status-rules PUT remains in `Application` for `server-py:input-invoice-usage-payment-rules-write-boundary-audit`. No production command, mutation or global closure claim occurred.
- Completed `server-py:input-invoice-usage-payment-rules-write-boundary-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-payment-rules-write-boundary-audit-2026-06-25.md`, confirmed payment-status-rules PUT business validation/persistence/audit live in the settings service/provider and the app callback is thin HTTP/session/body/error mapping plus gateway-backed refresh fan-out, and selected `server-py:input-invoice-usage-payment-rules-route-callback-collapse`. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-payment-rules-route-callback-collapse` as `local-implementation-closed`: moved payment-status-rules PUT HTTP mapping into `InputInvoiceUsageApiRoutes`, injected explicit settings/body/error/refresh ports, removed `_handle_api_input_invoice_usage_payment_status_rules_update(...)` from `server.py`, and updated payment rules/permission/static Guard regressions. Refresh fan-out remains an explicit `Application` port. No production command, mutation or global closure claim occurred.
- Completed `server-py:input-invoice-usage-route-owner-local-closure-audit` as `analysis-closed`: wrote `analysis/server-py-input-invoice-usage-route-owner-local-closure-audit-2026-06-25.md`, confirmed input usage route ownership is locally accounted for after route callback collapse, and identified the remaining app-owned implementation gap in SQL read-model fresh gate/source-version/export row-page helper logic. Selected `server-py:input-invoice-usage-read-model-fresh-gate-service-extraction`. No runtime code change, production command, mutation or closure claim occurred.
- Completed `server-py:input-invoice-usage-read-model-fresh-gate-service-extraction` as `local-implementation-closed`: added `InputInvoiceUsageReadModelFreshGateService`, delegated input usage rows/all-rows/relation-detail/export row-page fresh-gate behavior out of `Application`, preserved fail-closed refreshing/source-version/schema stale semantics, and updated static/API/export/service regressions. Output invoice collection behavior was protected by a targeted regression. Next boundary is `server-py:input-invoice-usage-post-fresh-gate-local-closure-audit`; no production command, mutation or module/global closure claim occurred.
- Completed `server-py:input-invoice-usage-post-fresh-gate-local-closure-audit` as `analysis-closed`: audited remaining input usage `Application` surfaces after route-owner collapse and fresh-gate extraction, classified them as dependency/platform/refresh/source-version/import-scope provider ports, and accounted local input usage `server.py` support without claiming module/global closure. Selected `server-py:output-invoice-collection-route-owner-audit`; no runtime code change, production command or mutation occurred.
- Completed `server-py:output-invoice-collection-route-owner-audit` as `analysis-closed`: audited output collection direct dispatch and `_handle_api_output_invoice_collections*` callbacks, split thin read/export/status/history wrappers from mutation wrappers and fresh-gate implementation, and selected `server-py:output-invoice-collection-read-export-route-callback-collapse`. No runtime code change, production command or mutation occurred.
- Completed `server-py:output-invoice-collection-read-export-route-callback-collapse` as `local-implementation-closed`: moved rows/filter-options/export-preview/export/status-rules/receipt-history/detail HTTP mapping into `OutputInvoiceCollectionApiRoutes`, injected explicit auth/JSON/XLSX/error ports, removed migrated app callbacks, updated tests/guards, and preserved fresh-gate plus mutation behavior. Next boundary is `server-py:output-invoice-collection-mutation-route-callback-audit`; no production command or mutation occurred.
- Completed `server-py:output-invoice-collection-mutation-route-callback-audit` as `analysis-closed`: audited remaining receipt preview/settings and lifecycle/receipt/red-relation callbacks, classified them as thin body/session/error/trace/idempotency wrappers around route-owner methods, and selected `server-py:output-invoice-collection-mutation-route-callback-collapse`. No runtime code change, production command or mutation occurred.
- Completed `server-py:output-invoice-collection-mutation-route-callback-collapse` as `local-implementation-closed`: moved remaining receipt preview/settings and lifecycle/receipt/red-relation mutation HTTP mapping into `OutputInvoiceCollectionApiRoutes`, injected `load_json_body`, preserved idempotency and trace headers, removed all output collection app callbacks, and updated guard/API coverage. Next boundary is `server-py:output-invoice-collection-read-model-fresh-gate-service-extraction`; no production command or mutation occurred.
- Completed `server-py:output-invoice-collection-read-model-fresh-gate-service-extraction` as `local-implementation-closed`: added `OutputInvoiceCollectionReadModelFreshGateService`, delegated rows/all-rows/relation-detail fresh-gate behavior out of `Application`, preserved fail-closed refreshing/source-version/schema stale semantics and `readModelStatus` compatibility, removed unused shared helper code, and updated tests/guards. Next boundary is `server-py:output-invoice-collection-post-fresh-gate-local-closure-audit`; no production command or mutation occurred.
- Completed `server-py:output-invoice-collection-post-fresh-gate-local-closure-audit` as `analysis-closed`: audited remaining output collection `Application` surfaces after route-owner collapse and fresh-gate extraction, classified them as composition-root/platform/provider ports, and accounted local output collection `server.py` support without module/global closure. Selected `server-py:oa-pending-payment-route-owner-audit`; no runtime code change, production command or mutation occurred.
- Completed `server-py:oa-pending-payment-route-owner-audit` as `analysis-closed`: audited remaining `/api/oa-pending-payments*` callbacks, classified them as thin auth/body/error/JSON/status wrappers around route/read-model/command services, and selected `server-py:oa-pending-payment-route-callback-collapse`. No runtime code change, production command or mutation occurred.
- Completed `server-py:oa-pending-payment-route-callback-collapse` as `local-implementation-closed`: moved `/api/oa-pending-payments*` HTTP mapping into `OaPendingPaymentApiRoutes.route(...)`, injected explicit read-session/write-auth/body/JSON/error ports, removed app-owned callbacks, updated API tests away from removed callback names, and added a runtime boundary Guard. Next boundary is `server-py:oa-pending-payment-route-owner-local-closure-audit`; no production command or mutation occurred.
- Completed `server-py:oa-pending-payment-route-owner-local-closure-audit` as `analysis-closed`: audited remaining OA pending payment `Application` surfaces after route callback collapse, classified them as composition-root/provider/platform ports, and accounted local OA pending payment `server.py` support without module/global closure. Selected `server-py:pending-invoice-route-owner-audit`; no runtime code change, production command or mutation occurred.
- Completed `server-py:pending-invoice-route-owner-audit` as `analysis-closed`: audited remaining `/api/pending-invoices*` callbacks, split read/detail/candidate/export wrappers from rules/attach/income-status write wrappers, and selected `server-py:pending-invoice-read-export-route-callback-collapse`. No runtime code change, production command or mutation occurred.
- Completed `server-py:pending-invoice-read-export-route-callback-collapse` as `local-implementation-closed`: moved pending invoice rows/filter-options/candidates/batch-candidates/detail/export-preview/export HTTP mapping into `PendingInvoiceApiRoutes.route(...)`, injected explicit read-session/body/JSON/error/export ports, removed the migrated app callbacks, and added a static route-owner Guard. Rules, attach-existing and income-status write callbacks remain in `Application` for the next write-boundary audit. No production command or mutation occurred.
- Completed `server-py:pending-invoice-write-route-callback-audit` as `analysis-closed`: audited the remaining rules, attach-existing and income-status callbacks and found server.py owns HTTP body/session/error/JSON/persist wrappers while `PendingInvoiceApiRoutes`, `PendingInvoiceApplicationService` and `PendingInvoiceRulesApplicationService` own business behavior. Selected full write route callback collapse with explicit write-session and persist-state ports. No runtime code change, production command or mutation occurred.
- Completed `server-py:pending-invoice-write-route-callback-collapse` as `local-implementation-closed`: moved remaining pending invoice rules, attach-existing and income-status HTTP mapping into `PendingInvoiceApiRoutes.route(...)`, injected explicit write-session and persist-state ports, removed the migrated app callbacks, and extended static Guard coverage. No production command or mutation occurred. Next boundary is `server-py:pending-invoice-route-owner-local-closure-audit`.
- Completed `server-py:pending-invoice-route-owner-local-closure-audit` as `analysis-closed`: confirmed no `_handle_api_pending_invoice*` callbacks remain in `server.py`; remaining pending invoice surfaces are composition-root, auth/session, export response, error response, settings lifecycle or read-model invalidation/provider ports. Pending invoice local `server.py` route-owner support is accounted for without module/global closure. Selected `server-py:tax-route-owner-audit` next.
- Completed `server-py:tax-route-owner-audit` as `analysis-closed`: audited `TaxApiRoutes` and tax offset `server.py` callbacks, selected month/summary/calculate/plan-save/import-job/certified-imports list route callback collapse as the first bounded implementation slice, and deferred certified import preview/confirm multipart/import-queue semantics. No runtime code change, production command or mutation occurred.
- Completed `server-py:tax-offset-read-plan-route-callback-collapse` as `local-implementation-closed`: moved tax offset month/summary/calculate/plan-save/import-job/certified-imports list HTTP mapping into `TaxApiRoutes.route(...)`, injected explicit read/mutation session, body, actor and certified-records ports, removed migrated app callbacks, preserved certified import preview/confirm in `server.py`, and added static Guard coverage. No production command or mutation occurred.
- Completed `server-py:tax-certified-import-route-callback-audit` as `local-implementation-closed`: audited and collapsed tax certified import preview/confirm HTTP mapping into `TaxApiRoutes.route(...)`, injected multipart loader, preview provider, import queue, serializer and inline confirm executor ports, removed migrated app callbacks, and added static Guard coverage. No production command or mutation occurred. Next boundary is `server-py:tax-route-owner-local-closure-audit`.
- Completed `server-py:tax-route-owner-local-closure-audit` as `analysis-closed`: wrote `analysis/server-py-tax-route-owner-local-closure-audit-2026-06-25.md`, confirmed no `_handle_api_tax*` callbacks remain in `server.py`, classified remaining tax surfaces as explicit composition-root/platform/runtime/query/read-model/cache/worker/source-version/scope-adapter ports, and accounted local tax route-owner support without module/global closure. Selected `server-py:cost-statistics-route-owner-audit`; no runtime code change, production command or mutation occurred.
- Completed `server-py:cost-statistics-route-owner-audit` as `analysis-closed`: wrote `analysis/server-py-cost-statistics-route-owner-audit-2026-06-25.md`, confirmed the remaining `/api/cost-statistics*` app callbacks are thin delegates to `CostStatisticsApiRoutes`, and selected `server-py:cost-statistics-route-callback-collapse` with explicit query/optional-bool parsing ports. No runtime code change, production command or mutation occurred.
- Completed `server-py:cost-statistics-route-callback-collapse` as `local-implementation-closed`: moved `/api/cost-statistics*` HTTP dispatch/query parsing into `CostStatisticsApiRoutes.route(...)`, injected optional bool parsing as an explicit route-owner port, removed the app-owned cost statistics route callbacks, and added API/runtime/static Guard coverage. No production command or mutation occurred. Next boundary is `server-py:cost-statistics-route-owner-local-closure-audit`.
- Completed `server-py:cost-statistics-route-owner-local-closure-audit` as `analysis-closed`: wrote `analysis/server-py-cost-statistics-route-owner-local-closure-audit-2026-06-25.md`, confirmed no `_handle_api_cost_statistics*` callbacks remain in `server.py`, classified remaining cost statistics surfaces as explicit composition-root/query/runtime/source-version/persistence/cache/worker/warmup/import-scope/platform ports, and accounted local cost statistics route-owner support without module/global closure. Selected `server-py:turnover-ledger-route-owner-audit`; no runtime code change, production command or mutation occurred.
- Completed `server-py:turnover-ledger-route-owner-audit` as `analysis-closed`: wrote `analysis/server-py-turnover-ledger-route-owner-audit-2026-06-25.md`, split turnover ledger route callbacks into thin read/export/GET callbacks and thicker mutation callbacks, and selected `server-py:turnover-ledger-read-export-route-callback-collapse` as the next bounded local implementation slice. No runtime code change, production command or mutation occurred.
- Completed `server-py:turnover-ledger-read-export-route-callback-collapse` as `local-implementation-closed`: moved turnover ledger list/grouped, export preview/export, tag-selection, relation detail and relation extra GET HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected JSON/export/tag-selection ports, removed migrated app-owned GET callbacks, and added API/read-facade/static Guard coverage. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-write-route-callback-audit`.
- Completed `server-py:turnover-ledger-write-route-callback-audit` as `analysis-closed`: wrote `analysis/server-py-turnover-ledger-write-route-callback-audit-2026-06-25.md`, audited the remaining turnover ledger PUT/POST callbacks, and selected `server-py:turnover-ledger-tag-selection-write-route-callback-collapse` as the smallest next implementation slice. No runtime code change, production command or mutation occurred.
- Completed `server-py:turnover-ledger-tag-selection-write-route-callback-collapse` as `local-implementation-closed`: moved `PUT /api/turnover-ledger/tag-selection` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected explicit session/body/tenant/request-boundary ports, removed the app-owned tag-selection update callback, and kept other turnover ledger mutation callbacks unchanged. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-bank-row-tags-route-callback-collapse`.
- Completed `server-py:turnover-ledger-bank-row-tags-route-callback-collapse` as `local-implementation-closed`: moved `POST /api/turnover-ledger/bank-row-tags/batch` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected the bank-row-tags request-boundary port, removed the app-owned bank-row-tags callback, and preserved target validation, affected-months and legacy fallback behavior. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-relation-extra-route-callback-collapse`.
- Completed `server-py:turnover-ledger-relation-extra-route-callback-collapse` as `local-implementation-closed`: moved `PUT /api/turnover-ledger/relations/{relation_id}/extra` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected relation-extra request-boundary, tenant and precondition-payload ports, removed the app-owned relation-extra update callback, and preserved stale precondition, idempotency and error response behavior. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-confirm-route-callback-collapse`.
- Completed `server-py:turnover-ledger-confirm-route-callback-collapse` as `local-implementation-closed`: moved `POST /api/turnover-ledger/relations/confirm` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected the confirm request-boundary port, removed the app-owned confirm callback, and preserved affected-months, stale precondition, idempotency and error response behavior. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-closure-confirm-route-callback-collapse`.
- Completed `server-py:turnover-ledger-closure-confirm-route-callback-collapse` as `local-implementation-closed`: moved `POST /api/turnover-ledger/closures/confirm` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected the closure request-boundary port, removed the app-owned closure confirm callback, and preserved affected-months, stale precondition, idempotency and error response behavior. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-closure-withdraw-route-callback-collapse`.
- Completed `server-py:turnover-ledger-closure-withdraw-route-callback-collapse` as `local-implementation-closed`: moved `POST /api/turnover-ledger/closures/withdraw` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, reused the closure request-boundary port with dynamic provider override, removed the app-owned closure withdraw callback, and preserved cash closure case id compatibility, idempotency and error response behavior. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-relation-withdraw-route-callback-collapse`.
- Completed `server-py:turnover-ledger-relation-withdraw-route-callback-collapse` as `local-implementation-closed`: moved `POST /api/turnover-ledger/relations/{relation_id}/withdraw` HTTP mapping into `TurnoverLedgerApiRoutes.route(...)`, injected the withdraw request-boundary port, removed the last app-owned turnover ledger route callback, and preserved unknown relation handling, stale precondition, idempotency, affected-months and error response behavior. No production command or mutation occurred. Next boundary is `server-py:turnover-ledger-route-owner-local-closure-audit`.
- Completed `server-py:turnover-ledger-route-owner-local-closure-audit` as `analysis-closed`: proved no `_handle_api_turnover_ledger*` callbacks remain in `server.py`, classified remaining turnover ledger app surfaces as composition-root/provider/platform/source-version/read-model/local-runtime/legacy fallback support candidates, and avoided claiming module/global closure. No production command or mutation occurred. Next boundary is `planning:post-turnover-ledger-route-owner-next-boundary-selection`.
- Completed `planning:post-turnover-ledger-route-owner-next-boundary-selection` as `analysis-closed`: compared remaining `server.py` route/support surfaces after turnover route-owner closure and selected `server-py:bank-details-read-export-route-callback-collapse` as the next safe local implementation boundary. No runtime code change, production command or mutation occurred.
- Completed `server-py:bank-details-read-export-route-callback-collapse` as `local-implementation-closed`: moved bank details accounts, transactions, transactions export and auto-tag-rules GET HTTP mapping into `BankDetailsApiRoutes.route(...)`, injected explicit read-session/json/export ports, removed migrated app callbacks, and retained bank details write callbacks for a dedicated follow-up audit. No production command or mutation occurred.
- Completed `server-py:bank-details-write-route-callback-audit` as `analysis-closed`: audited remaining bank details write callbacks, split auto-tag PUT/reapply/file replacement from category confirmation/assignment, and selected `server-py:bank-details-auto-tag-write-route-callback-collapse` as the next bounded implementation slice. No runtime code change, production command or mutation occurred.
- Completed `server-py:bank-details-auto-tag-write-route-callback-collapse` as `local-implementation-closed`: moved bank details auto-tag PUT, reapply and file-replacement HTTP mapping into `BankDetailsApiRoutes.route(...)`, injected explicit session/json-body/default-source/json-response ports, removed migrated app callbacks, and retained category confirmation/assignment callbacks for the next slice. No production command or mutation occurred.
- Completed `server-py:bank-details-category-write-route-callback-collapse` as `local-implementation-closed`: moved bank details category confirmation/assignment POST/DELETE HTTP mapping into `BankDetailsApiRoutes.route(...)`, removed migrated app callbacks, and preserved transaction id extraction, permission, body parsing and category validation semantics. No production command or mutation occurred.
- Completed `server-py:bank-details-route-owner-local-closure-audit` as `analysis-closed`: confirmed migrated bank-details callbacks are gone, but found `PATCH /api/bank-details/transactions/categories` still app-owned in `server.py`; selected `server-py:bank-details-transaction-categories-route-callback-collapse` next and did not claim route-owner closure. No runtime code change, production command or mutation occurred.
- Completed `server-py:bank-details-transaction-categories-route-callback-collapse` as `local-implementation-closed`: moved disabled `PATCH /api/bank-details/transactions/categories` HTTP mapping into `BankDetailsApiRoutes.route(...)`, removed `_handle_api_bank_transaction_categories(...)` from `server.py`, preserved the 410 no-mutation contract, and added direct route-owner plus static Guard coverage. No production command or mutation occurred. Next boundary is `server-py:bank-details-route-owner-local-closure-audit-retry`.
- Completed `server-py:bank-details-route-owner-local-closure-audit-retry` as `analysis-closed`: confirmed no app-owned bank-details route callback remains in `server.py`; remaining bank-related surfaces are composition-root, platform adapter, read-model/source-version/refresh provider or shared downstream support. No runtime code change, production command, mutation or module/global closure claim occurred. Next boundary is `server-py:no-oa-bank-batch-route-owner-audit`.
- Completed `server-py:no-oa-bank-batch-route-owner-audit` as `analysis-closed`: inventoried eight no-OA bank batch callbacks in `server.py`, classified them as thin HTTP dispatch/session/body/json wrappers around existing route/application-service boundaries, and selected `server-py:no-oa-bank-batch-route-callback-collapse` with explicit route-owner ports. No runtime code change, production command, mutation or module/global closure claim occurred.
- Completed `server-py:no-oa-bank-batch-route-callback-collapse` as `local-implementation-closed`: moved all `/api/no-oa-bank-batches*` HTTP dispatch into `NoOaBankBatchApiRoutes.route(...)`, injected explicit mutation-session/body/json ports, removed eight app-owned no-OA route callbacks from `server.py`, and added route/API/static Guard coverage. No production command, mutation or module/global closure claim occurred. Next boundary is `server-py:no-oa-bank-batch-route-owner-local-closure-audit`.
- Completed `server-py:no-oa-bank-batch-route-owner-local-closure-audit` as `analysis-closed`: confirmed no no-OA route callbacks remain in `server.py`, but found `_enqueue_no_oa_bank_batch_read_model_refreshes(...)` still owns no-OA scope normalization and direct gateway enqueue logic in `Application`; selected `server-py:no-oa-bank-batch-refresh-producer-extraction` next. No runtime code change, production command, mutation or module/global closure claim occurred.
