# planning:t0-gsd-chinese-worker-closure-contract

**Date:** 2026-06-25
**Status:** planning-closed
**Previous boundary:** `planning:commit-backed-state-reconciliation`
**Next executable boundary:** `planning:commit-backed-state-reconciliation`

## Goal

Strengthen the primary T0 Meta Orchestrator prompt so unattended execution is explicit about:

- GSD closed-loop execution;
- full modular IO closure as the goal, not prompt generation or one worker wave;
- Simplified Chinese user-facing feedback;
- bounded GSD workflow requirements for every T0-generated worker.

This is a planning/prompt slice only. It does not change runtime code.

## Decision

`prompts/06-t0-meta-orchestrator-goal.md` now states that T0 must:

- continue from analysis to implementation, review, state-machine update, commit/push, next boundary generation and immediate execution;
- treat final completion as evidence-backed global modular IO closure;
- stop only on a hard stop gate, with precise blocker evidence and next action;
- produce all user-facing T0 updates and final answers in Simplified Chinese.

Every worker prompt generated by T0 must now include a bounded worker GSD loop:

- preflight;
- full analysis before implementation;
- plan/handoff section;
- narrow implementation;
- verification;
- self-review;
- handoff;
- Simplified Chinese final report.

Workers remain evidence producers. They may auto-progress only inside assigned file ownership and must not edit controller-only files, create recursive threads, perform production operations, push to `main`, or decide global closure.

## State Machine Impact

No module state is closed by this slice. The next executable boundary remains:

```text
planning:commit-backed-state-reconciliation
```

That reconciliation remains mandatory before any new implementation or worker wave.

## Verification

Required verification for this planning slice:

```bash
bash scripts/verify.sh docs
git diff --check
```
