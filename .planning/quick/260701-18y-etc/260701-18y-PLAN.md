---
quick_id: 260701-18y
status: completed
---

# ETC票据管理移除月份筛选并支持未提交批次标题编辑

## Assumptions

- ETC 票据管理页不再按月份筛选批次；列表只按状态 bucket、车牌和关键词筛选。
- 只有未提交 bucket 的业务批次可以改标题；已提交批次标题锁定。
- 标题持久化在 business batch payload，并同步 linked reconciliation task title，ETC 发票导入 ready task 下拉自然显示最新标题。
- 空标题不保存；保存时使用 expectedVersion 防并发覆盖。

## Tasks

1. Backend contract - completed
   - Add `title` to `EtcBusinessBatch`.
   - Add business batch rename service + PATCH route.
   - Sync linked reconciliation task title.
   - Cover API/service behavior in `tests/test_etc_backend.py`.

2. Frontend interaction - completed
   - Remove ETC page month state/input/query param.
   - Add unsubmitted row inline title edit.
   - Add API mapper/type support for title update.
   - Keep existing status bucket behavior.

3. Verification/docs - completed
   - Update affected ETC/import docs for title edit and no month selector.
   - Add focused frontend API/page tests.
   - Run targeted backend and frontend checks.

## Verification

- `PYTHONPATH=backend/src python3 -m unittest tests.test_etc_backend.EtcServiceTests.test_business_batch_title_update_persists_and_locks_submitted tests.test_etc_backend.EtcApiTests.test_business_batch_title_patch_updates_linked_task_title tests.test_etc_backend.EtcApiTests.test_business_batch_create_without_task_id_creates_linked_task_and_batch -v`
- `cd web && npm test -- --run src/test/EtcApi.test.ts src/test/EtcTicketManagementPage.test.tsx`
- `cd web && npm exec tsc -- --noEmit`
- `cd web && npm run e2e -- e2e/etc-tickets-flow.spec.ts --project=chromium`
- `git diff --check`
- `scripts/verify.sh docs`
