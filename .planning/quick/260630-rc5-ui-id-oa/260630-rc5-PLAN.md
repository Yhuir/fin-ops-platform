# Quick Task 260630-rc5: 待找发票详情抽屉 UI 重构

日期：2026-06-30

## 目标

重构待找发票页面的流水、OA、发票详情右侧抽屉：

- 标题固定为 `流水详情`、`OA 详情`、`发票详情`，不使用人名、发票号或内部 ID。
- 抽屉标题栏保持左上标题、右侧关闭按钮，并压缩标题块高度。
- 字段名中文化，过滤内部英文 ID、raw JSON 和未映射英文内部字段。

## 任务

1. 修改 `web/src/components/pendingInvoices/PendingInvoiceDetailDrawer.tsx`
   - 固定详情标题。
   - 统一 OA 详情为右侧抽屉。
   - 增加最小字段过滤和中文映射。

2. 修改 `web/src/app/styles.css`
   - 收紧待找发票详情抽屉标题栏和详情卡片排版。

3. 修改 `web/src/test/PendingInvoicesPage.test.tsx`
   - 补充三类详情抽屉行为回归。
   - 更新样式/primitive 约束。

## 验证

```bash
cd web && npm test -- --run src/test/PendingInvoicesPage.test.tsx
```

## Docs Impact

仅前端展示重构，不改变 API shape、read model、worker、权限、模块边界或业务状态机；长期模块文档不需要更新。
