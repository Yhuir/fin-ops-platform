---
status: complete
completed: 2026-06-22
description: OA 待付款自动匹配支出流水并自动写回
---

# Quick Task 260622-oaw Summary

## Completed

- Added `POST /api/oa-pending-payments/auto-reconcile-bank-transactions` and backend service logic that:
  - scans completed and in-progress OA rows for existing active outflow relations and writes back matched OA;
  - auto-confirms only unambiguous in-progress OA to unpaired outflow bank matches generated by Workbench OA-bank exact amount/exact sum rules;
  - writes `t_payment_simple.pay_status=1` through the existing OA payment status repository after amount, direction, and `flow_id` checks.
- Updated `link-bank-transactions` so manual drawer fallback still creates the Workbench active relation, then automatically writes back OA payment status when the linked outflow total equals the OA amount.
- Removed the frontend manual confirm-paid action and styles; the page now runs auto reconcile once per scope, waits for `oa_pending_payment` operation barrier before refreshing rows, and keeps action errors visible independently from list loading errors.
- Updated deterministic browser mocks, Vitest, Playwright flows, module docs, API contract, and GSD state.

## Verification

- `PYTHONPATH=backend/src python3 -m py_compile backend/src/fin_ops_platform/services/oa_pending_payment_command_service.py backend/src/fin_ops_platform/app/routes_oa_pending_payments.py backend/src/fin_ops_platform/app/server.py`
- `PYTHONPATH=backend/src python3 -m unittest tests.test_oa_pending_payment_command_service tests.test_oa_pending_payment_api -v`
- `npm --prefix web test -- --run src/test/OaPendingPaymentsPage.test.tsx`
- `npm --prefix web run e2e -- e2e/oa-pending-payments-confirm-paid-flow.spec.ts e2e/oa-pending-payments-bank-link-flow.spec.ts --project=chromium`
- `npm --prefix web run build`
- `bash scripts/verify.sh docs`
- `git diff --check`

## Remaining Risk

- Local tests use fake OA MySQL/Mongo and deterministic browser mocks. Real `t_payment_simple` write latency, OA field variants, permissions, and lock behavior still need staging smoke with a true in-progress OA and real outflow bank transaction.
- The automatic matcher intentionally remains narrow: only exact amount and exact sum OA-bank rules are accepted. Ambiguous or fuzzy cases require the bank link drawer.
