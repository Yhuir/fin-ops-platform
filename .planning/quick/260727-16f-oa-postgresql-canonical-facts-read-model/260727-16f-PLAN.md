# Quick Task 260727-16f Plan

## Goal

把 OA 待付款核对页面从 `read_model.oa_pending_payment_*` freshness/cache/worker 合同迁移到页面专属 PostgreSQL canonical facts 直读，同时保持现有查询、详情、权限、写入、冲突、审计与写后重读业务合同。

## Must haves

- 浏览器仅调用 `/api/oa-pending-payments/*`；rows 与详情不再返回或依赖 ETag、202、read-model status/source versions、refresh barrier。
- 一个 rows 响应内的 rows、pagination、summary、statistics、filterOptions 来自同一 `REPEATABLE READ READ ONLY` snapshot。
- PostgreSQL 完成服务端筛选、排序、分页、summary/facets；Python 仅批量 hydrate 当前页 canonical facts，不做全量过滤或分页。
- 正式关系仅读取 `app.workbench_pair_relations.status = 'active'`，排除 turnover manual closure；在途关系只读取 `app.oa_pending_payment_bank_relations.status = 'active'`。
- 页面请求热路径不访问 OA Mongo/MySQL/对象存储，不新增 cache/worker/queue/materialized view/依赖/双读/fallback。
- 共享 read-model worker、manifest、registry、invoice lifecycle 依赖不在本分支删除，精确列入 HANDOFF。

## Direct and downstream I/O

- 输入：tenant-aware 单次读鉴权；`month/keyword/trade_date_from/trade_date_to/filters/sort/page/page_size/view_mode`；OA/bank/invoice/row detail identifiers。
- canonical 输入：`app.oa_applications`、`app.oa_pending_payment_admissions`、`app.oa_pending_payment_status_snapshots`、`app.oa_pending_payment_bank_relations`、`app.bank_transactions`、`app.invoices`、`app.workbench_pair_relations(status=active)`。
- 输出：既有 rows/pagination/summary/statistics/filterConfig/filterOptions/appliedFilters/sort/viewMode 与四类详情 payload，不含任何 read-model freshness/version 字段。
- 写侧下游：保留现有 `OaPendingPaymentCommandService`、OA 外部 adapter、pending relation claim/promotion/CAS/audit；成功后前端重新 GET canonical API。

## Tasks

1. 新增页面专属 canonical query repository/service：单快照、固定查询次数、服务端分页与批量当前页 hydrate；复用既有 query contract、row builder、detail builders 与 canonical repositories。
2. 最小切换 `routes_oa_pending_payments.py`、`server.py` 和前端 API/types/page，删除页面 ETag/304/202/polling/status/barrier 语义，保留 loading/empty/error 和写后重读。
3. 更新 OA pending payments 模块文档与长期页面/API事实；补 backend repository/service/API、frontend interaction、canonical relation、query-count/performance guard 和回归测试，运行 lint/tests/build。
