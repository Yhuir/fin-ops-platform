---
status: complete
completed: 2026-07-27
description: OA 待付款核对页面迁移到 PostgreSQL canonical facts 直读
---

# Quick Task 260727-16f Summary

## Completed

- 新增页面专属 `OaPendingPaymentQueryService` 与 `PostgresOaPendingPaymentQueryRepository`。
- rows、summary、statistics、facets、筛选、排序和服务端分页在同一显式
  `REPEATABLE READ READ ONLY` snapshot 内读取 canonical PostgreSQL facts。
- 正式关系仅读取 active `app.workbench_pair_relations`；进行中关系仅读取 active
  `app.oa_pending_payment_bank_relations`。
- 银行候选同样改为 canonical PostgreSQL 单 statement 服务端筛选/分页，删除 command service
  的 Python 全量候选扫描。
- 页面 API 和前端删除 ETag/202/304、freshness/source-version、Redis payload、enqueue/barrier
  和后台 polling 正确性依赖；保留 loading/empty/error、权限、详情、写入冲突/审计及写后 GET。
- 共享 read model、worker、manifest、App Status 与 invoice lifecycle 依赖保留，交由主控在所有页面
  分支合并后统一清理。

## Verification

- `pytest -q tests/test_oa_pending_payment_query_service.py tests/test_oa_pending_payment_api.py tests/test_oa_pending_payment_command_service.py tests/test_oa_pending_payment_postgres_integration.py`
  - 50 passed，7 skipped（未配置 `FIN_OPS_TEST_DATABASE_URL`）。
- `pytest -q tests/test_invoice_lifecycle_page_integration.py tests/test_invoice_lifecycle_policy.py tests/test_oa_payment_status_service.py tests/test_oa_pending_payment_relation_promotion_service.py tests/test_oa_projection_sync_service.py tests/test_app_status_overview_service.py`
  - 69 passed。
- `pytest -q tests/test_platform_runtime_boundary_guards.py`
  - 216 passed。
- `npm test -- --run src/test/OaPendingPaymentsPage.test.tsx src/test/OaPendingPaymentAuditIcon.test.tsx`
  - 24 passed。
- `npx playwright test e2e/oa-pending-payments-flow.spec.ts e2e/oa-pending-payments-confirm-paid-flow.spec.ts e2e/oa-pending-payments-bank-link-flow.spec.ts e2e/oa-pending-payments-nonfresh-flow.spec.ts --project=chromium`
  - 8 passed。
- `npm run build`
- `bash scripts/verify.sh lint`
- `bash scripts/verify.sh docs`
- `git diff --check`

## Performance Evidence

- rows selector 固定 1 个 SQL statement，并在 SQL 内 `limit/offset`。
- 银行候选固定 1 个 SQL statement，并在 SQL 内筛选和分页。
- 当前页 fact hydrate 在 1 与 200 descriptors 下查询次数相同，测试上限为 5。
- 未在本任务执行生产等量级 `EXPLAIN (ANALYZE, BUFFERS)` 或 endpoint p50/p95/p99；
  主控负责生产只读验证和任何经 EXPLAIN 证明必要的索引 migration。

## Remaining Risk / HANDOFF

- 无 disposable PostgreSQL URL，新增 direct SQL 集成测试被条件跳过；生产发布前必须运行。
- `OaPendingPaymentReadModelService`、`OaPendingPaymentReadModelRepositoryPort`、
  `OaPendingPaymentReadModelRefreshService`、`OaPendingPaymentSqlProjectionBuilder`、
  `oa_pending_payment.read_model.refresh`、对应 manifest/scope/worker/App Status/deploy 配置和
  `invoice_lifecycle` 依赖仍有共享调用方，不得由本分支删除。
- `tests/test_permissions_write_entry_inventory.py` 的 lifecycle/enqueue/barrier 双向 inventory
  在固定基准已因成本统计等范围外条目漂移失败；本任务未修改该共享清单。
