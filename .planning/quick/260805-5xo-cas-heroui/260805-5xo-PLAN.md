---
quick_id: 260805-5xo
status: complete
mode: quick-full-inline
must_haves:
  truths:
    - 批量账务未提交流水只由银行明细当前有效标签与显式已选标签共同准入
    - 已提交与历史不受标签规则过滤，提交事务拒绝过期规则或已失去准入资格的流水
    - 只看/只导出用户不能保存规则，所有更新有 CAS 与审计且 semantic no-op 不增版本
    - 前端复用 HeroUI 与 AppDrawer，左栏展示当前标签，抽屉紧凑且状态完整
    - 不恢复 Batch Accounting 旧 Workbench/read-model/freshness/worker/cache/fallback 链路
  artifacts:
    - backend/src/fin_ops_platform/services/app_settings_service.py
    - backend/src/fin_ops_platform/services/postgres_repositories/batch_accounting.py
    - backend/src/fin_ops_platform/services/batch_accounting_service.py
    - backend/src/fin_ops_platform/app/routes_batch_accounting.py
    - web/src/features/batchAccounting/api.ts
    - web/src/features/batchAccounting/types.ts
    - web/src/pages/BatchAccountingPage.tsx
  key_links:
    - Bank Details canonical category facts -> BankTransactionEffectiveCategoryProvider -> Batch Accounting query DTO
    - app.app_settings batch_accounting_tag_selection -> Batch Accounting rules GET/PUT -> unsubmitted query and submit guard
    - useSessionPermissions -> UI save affordance; backend mutation resolver -> authoritative write denial
---

# Quick Task 260805-5xo: 批量账务银行标签准入规则

## Task 1: 后端 canonical 查询、设置 UoW 与提交复核

**files**
- `backend/src/fin_ops_platform/services/app_settings_service.py`
- `backend/src/fin_ops_platform/services/postgres_state_store.py`
- `backend/src/fin_ops_platform/services/postgres_repositories/batch_accounting.py`
- `backend/src/fin_ops_platform/services/batch_accounting_application_service.py`
- `backend/src/fin_ops_platform/app/routes_batch_accounting.py`
- 必要 migration 与目标后端测试

**action**
- 在现有 Settings family 中加入稳定 `tag_code` 选择、版本、CAS、audit/UoW 和幂等初始化；复用现有 settings writer，不新增表或旁路 SQL。
- 在 Batch Accounting 单个 repeatable-read snapshot 中批量读取 effective category、过滤未提交 rows/total/summary，并向所有 bucket 返回当前标签展示字段。
- 提交事务重新校验 tag-selection version、当前有效标签和准入资格，过期返回 409；已提交/历史读取不应用过滤。
- GET/PUT 规则接口复用现有 route/service 权限和 HTTP 映射，read-export 只读、full/admin 可保存。

**verify**
- 业务规则、repository 固定查询次数、API shape、403/409/503、audit/no-op、提交并发测试通过。
- whole-repo guard 证明没有 Batch Accounting read-model/freshness/worker/cache/fallback 回归。

**done**
- canonical 事实源、设置 owner、Batch Accounting query/command owner 责任不交叉；失败无半写，旧列表契约只做 additive 扩展。

## Task 2: HeroUI 标签展示和规则抽屉

**files**
- `web/src/features/batchAccounting/api.ts`
- `web/src/features/batchAccounting/types.ts`
- `web/src/pages/BatchAccountingPage.tsx`
- 必要的同模块抽屉组件、样式与前端测试

**action**
- 左侧每条流水在“批量账务集中处理”右侧显示组合标签 Chip，超长标签可访问且不挤压金额。
- 页面右上角加入“批量账务标签规则”，复用 `AppDrawer` 和 HeroUI Checkbox/Chip/Button；懒加载规则，单次原子保存。
- 保存成功后清理过期选择、未提交分页回到第一页并只做一次普通 GET；处理 loading/empty/error/readonly/conflict 状态。

**verify**
- 前端 mapper、组件交互、权限、长标签、抽屉焦点、成功/失败/409、分页与选择重置测试通过；build 与 Browser E2E 通过。

**done**
- UI 符合 Ledger Calm：紧凑、克制、无大卡片、无重复说明、无新依赖、无客户端全量过滤。

## Task 3: 文档、回归、发布与生产闭环

**files**
- `docs/modules/batch-accounting/*`
- `docs/modules/settings/boundary-io.md`
- `docs/modules/bank-details/boundary-io.md`
- `docs/dev/api-contracts.md`
- `.planning/STATE.md` 与本 quick task summary/verification

**action**
- 更新边界 I/O、API、权限、测试矩阵、首次显式初始化和旧链删除条件。
- 跑后端、前端、build、E2E、lint、whole-repo guard、`git diff --check`；记录七类测试适用性。
- commit 并 push 精确 main，按 `scripts/deploy-oa.sh` 当前门禁制作/激活候选；只使用本机安全文件中的 005 token。
- 生产验证规则读取/保存/恢复、列表准入、已提交不消失、权限、SQL/HTTP 性能和健康状态。

**verify**
- 本地全门禁通过；remote main SHA 与候选/服务 release 精确一致；生产样本与 canonical SQL 对照一致，p95 相对基线恶化不超过 10%。

**done**
- 代码、remote main、生产 release、数据状态、性能证据和回滚/恢复结果全部闭环。
