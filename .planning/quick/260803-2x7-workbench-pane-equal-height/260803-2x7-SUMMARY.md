---
quick_id: 260803-2x7
status: complete
date: 2026-08-03
implementation_commit: 12097b67f
---

# Quick Task 260803-2x7 Summary

## 结果

- 关联台只有权威来源形成完整、唯一、无遗漏的一一对应时才共享行轨。
- 非一一对应、部分来源、重复来源和未链接记录均回到栏位独立布局；OA、流水、发票各自均分并占满完整关联组高度。
- 删除前端金额相等和金额组合推断的旧展示链路，不增加 API、状态、DOM 测量、依赖或后台任务。
- 展示判定复用现有 display model，复杂度保持 `O(OA + bank + invoice)`。

## 变更边界

- display model：`web/src/features/workbench/groupDisplayModel.ts`
- grid/layout：`web/src/components/workbench/RelationGroupGrid.tsx`、`web/src/app/styles.css`
- 回归测试：`web/src/test/groupDisplayModel.test.ts`、`web/src/test/RelationGroupGrid.test.tsx`、`web/e2e/workbench-relation-fanout.spec.ts`
- 模块事实：`docs/modules/reconciliation-workbench/`

## 验证

- 关联台目标单测：50 passed。
- 关联台相关回归：152 passed。
- 前端全量：73 files / 921 tests passed。
- Chromium 几何 E2E：2 passed，验证非一一对应发票栏及单条发票均占满组高。
- `npm run build`、`bash scripts/verify.sh lint`、`bash scripts/verify.sh docs`、`git diff --check` 全部通过。
- whole-repo runtime scan 无旧金额 fallback 残留。

## 七类测试评估

- 业务核心单元测试：适用并已覆盖完整双射、部分/重复/未链接和金额 fallback 删除。
- 服务层测试：不适用；未改后端 service、repository、持久化或审计。
- API 合同测试：不适用；未改请求或响应合同。
- read model/cache/worker：不适用；未改 read model、缓存、队列或 worker。
- 前端组件与交互：适用并已覆盖布局 class、选择行为和边界输入。
- 端到端业务流：适用并已用 Chromium 验证真实 DOM 几何与选择交互。
- 既有功能回归：适用并已运行前端全量 921 tests 与生产 build。

发布后的 active SHA、健康、真实生产布局和耗时证据由同一任务的标准 deploy-control/release-gate 记录与最终交付报告承载，避免把环境瞬时状态写成长期代码事实。
