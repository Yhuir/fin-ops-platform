---
quick_id: 260803-2x7
status: complete
mode: validate
date: 2026-08-03
description: 修复关联台非一一对应栏位未占满组高度并完成生产部署验证
must_haves:
  truths:
    - 非完整一一对应的 OA、银行流水、发票栏分别均分并占满关联组高度
    - 只有权威来源形成完整唯一双射的栏才与对应记录共享行轨
    - 前端金额相等与金额组合 fallback 不再决定视觉对齐
    - 关联台现有选择、详情、折叠、筛选、权限和滚动行为不回归
    - remote main 与生产 active release 精确绑定且生产健康、布局、请求数和性能验证通过
  artifacts:
    - web/src/features/workbench/groupDisplayModel.ts
    - web/src/components/workbench/RelationGroupGrid.tsx
    - web/src/components/workbench/RelationGroupCell.tsx
    - web/src/app/styles.css
    - web/src/test/groupDisplayModel.test.ts
    - web/src/test/RelationGroupGrid.test.tsx
    - docs/modules/reconciliation-workbench/
  key_links:
    - WorkbenchRelationGroup -> groupDisplayModel -> RelationGroupGrid -> RelationGroupCell -> scoped CSS
    - git HEAD -> origin/main -> packaged release -> production active release -> read-only browser/HTTP evidence
---

# Quick Task 260803-2x7: 关联台栏位完整高度

## Task 1：锁定展示合同并实现最小根因修复

**Files**

- `web/src/features/workbench/groupDisplayModel.ts`
- `web/src/components/workbench/RelationGroupGrid.tsx`
- `web/src/components/workbench/RelationGroupCell.tsx`
- `web/src/app/styles.css`

**Action**

- 在现有 display model 中用线性 Map/Set 判定完整一一对应；部分、重复、未链接和一对多均回到栏位独立布局。
- 删除只由旧展示链路调用的金额解析、精确金额和 2～6 条组合 fallback。
- 让非分段栏跨越完整关联组，多记录使用 CSS Flex 等分可用高度；不增加 API、state、effect、DOM 测量或依赖。
- 保留正式 `sourceOaId` / `sourceExpenseItemId`、多项目报销、折叠摘要、空栏和操作链路。

**Verify**

- TypeScript 编译通过。
- whole-repo scan 证明旧金额 fallback 无残留调用。
- 变更仅位于 reconciliation-workbench 前端展示边界。

**Done**

- 4 OA / 2 流水 / 1 发票分别按 25% / 50% / 100% 使用完整组高度。
- 完整唯一 1:1 仍同行；部分关联不同行。

## Task 2：测试、文档与性能回归

**Files**

- `web/src/test/groupDisplayModel.test.ts`
- `web/src/test/RelationGroupGrid.test.tsx`
- `docs/modules/reconciliation-workbench/README.md`
- `docs/modules/reconciliation-workbench/boundary-io.md`
- `docs/modules/reconciliation-workbench/tests.md`
- `docs/modules/reconciliation-workbench/implementation-notes.md`

**Action**

- 覆盖完整双射、4/2/1、部分来源、重复来源、金额相同无来源、多项目报销、单/多记录高度 class 合同。
- 更新模块长期事实，明确 2026-06-23 前端金额 fallback 已被替代，后端正式 alignment 保留。
- 运行关联台相关前端回归、build、lint、docs；按失败事实做最小修复。

**Verify**

- `groupDisplayModel`、`RelationGroupGrid`、选择、区域、筛选、列布局测试通过。
- `npm run build`、`scripts/verify.sh lint`、`scripts/verify.sh docs` 通过。
- 无新增请求、依赖、DOM 测量；展示判定为 O(OA + bank + invoice)。

**Done**

- 自动化测试保护新规则和受影响旧功能，文档与代码一致。

## Task 3：Git、部署与生产只读闭环

**Files**

- 当前任务全部变更与 GSD 记录
- 仓库既有 `scripts/deploy-oa.sh`、deploy-control 与生产 smoke 工具（只调用，不改写）

**Action**

- 审阅完整 diff，原子提交全部任务变更；fetch 后确认 main 可 fast-forward，push 到 `origin/main`。
- 使用仓库标准发布入口打包并部署与 origin/main 精确绑定的 release；不得复用旧候选。
- 验证 active release/commit、ready/health、关联台 API 请求数、目标布局几何和交互性能；失败时保持安全状态并 forward repair。

**Verify**

- 本地 `main == origin/main == deployed commit`。
- 生产 `/health/ready`、服务状态和 read-only route smoke 通过。
- 目标组或等价生产数据证明非 1:1 栏占满高度，完整 1:1 保持同行；无新增网络请求或控制台错误。

**Done**

- commit、push、部署、性能和业务链路均有可复核证据；剩余未测风险明确为零或被记录。
