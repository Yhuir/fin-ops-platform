# Business Operation Evidence Matrix

Working matrix for the quality-closure goal. Expand page-by-page as coverage moves forward. `covered` here means the listed business operation has a current source, a valid deterministic fixture, visible assertions, side-effect assertions where applicable, Playwright latency JSON artifacts, and the listed command passed.

## `/input-invoice-usage` 进项发票使用情况

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Fresh rows, load failure, refresh recovery, filter, sort, page-size | `docs/modules/input-invoice-usage/e2e-spec.md` `IN-USAGE-E2E-001`; `README.md`; `tests.md` | page open, `刷新`, `筛选 销方名称`, `清空`, `按开票日期排序`, `每页行数`, `查询` | Fresh rows show invoice/OA/bank/payment fields; transient rows failure shows error instead of false empty; refresh recovers rows/export; filter/sort/page-size update API query and visible rows without mutations. | `covered` | `cd web && npx playwright test e2e/input-invoice-usage-flow.spec.ts --project=chromium` passed 11 tests; latency operation ids include `open-page-load-failure`, `refresh-after-load-failure.*`, `open-page-filter-sort`, `apply-seller-filter`, `clear-seller-filter`, `sort-invoice-date`, `change-page-size-50`, `search-before-export`. |
| Read-export permission and durable-write denial | `IN-USAGE-E2E-007`; `boundary-io.md`; permissions inventory | `筛选内容导出`, `发票与支付状态规则设置`, `以发票反提 OA` under `read_export_only` | Read/export works; rules drawer is read-only; OA reverse preview is read-like but cannot create drafts; durable write endpoints remain uncalled. | `covered` | Same command passed; latency operation ids include `open-page-read-export`, `open-export-preview-read-export`, `close-export-preview-read-export`, `open-payment-rules-read-export`, `close-payment-rules-read-export`, `open-oa-reverse-read-export`. |
| Payment status rules full-access save | `IN-USAGE-E2E-009`; `payment-status-rules-ui-spec.md`; API tests | `发票与支付状态规则设置`, `支付状态`, `保存并刷新` | Save sends version and idempotency key, waits for rows refresh, visible payment status updates, and no success-path error remains. | `covered` | Same command passed; latency operation ids include `open-page-payment-rules-save`, `open-payment-rules-full-access`, `edit-payment-rule-label`, `save-payment-rules`. |
| Read model non-fresh and relation detail diagnostics | `IN-USAGE-E2E-005`; `README.md` read model contract; `tests.md` | page open with stale rows, `查看陈秀云关联OA 2 条` | Rows stale shows refreshing diagnostics, not stale rows or normal empty state; stale relation detail opens drawer with unavailable diagnosis and no mutation; fresh detail expands summaries from row read model. | `covered` | Same command passed; latency operation ids include `open-page-read-model-stale`, `open-page-relation-detail-stale`, `open-relation-detail-stale`, `open-page-relation-detail-fresh`, `open-relation-detail-fresh`. |
| Current-filter export, oversized export, export read model refreshing | `IN-USAGE-E2E-008`; API contract tests | `筛选内容导出`, `下载导出` | Export preview/download use current filters and no pagination; download event and file fields are asserted; row-limit shows backend message and zero download; export non-fresh disables download. | `covered` | Same command passed; latency operation ids include `open-page-export-download`, `open-export-preview`, `download-export`, `open-page-export-row-limit`, `open-export-preview-row-limit`, `open-page-export-stale`, `open-export-preview-stale`. |
| OA reverse draft and submitted history | `IN-USAGE-E2E-004`; `state-machine.md`; `oa-reverse-design.md` | `以发票反提 OA`, candidate checkbox, `创建 OA 草稿`, `我已在OA系统提交该草稿 / OA正在进行中` | Selected invoice subset is re-previewed; draft creation refreshes rows; confirmation moves to submitted history; internal batch id is not exposed. | `covered` | Same command passed; latency operation ids include `open-page-oa-reverse-draft`, `open-oa-reverse-draft`, `unselect-oa-reverse-candidate`, `create-oa-reverse-draft`, `confirm-oa-reverse-submitted`. |
| Workbench relation fan-out to input usage and downstream pages | `IN-USAGE-E2E-002`; `IN-USAGE-E2E-003`; `IN-USAGE-E2E-009`; downstream module specs | Workbench relation confirm; input usage OA reverse; links to `OA待付款核对`, `税金抵扣`, `成本统计`; cost project drill-down | Legacy/candidate OA evidence is non-selectable; Workbench confirm makes input usage row linked/paid; downstream OA pending, tax offset, and cost statistics re-read fresh data and show confirmed relation results. | `covered` | `cd web && npx playwright test e2e/input-invoice-relation-fanout.spec.ts --project=chromium` passed 1 test; latency operation ids include `open-page-relation-fanout-before-confirm`, `open-oa-reverse-before-confirm`, shared `reconciliation-workbench.*`, `reopen-after-workbench-confirm`, `open-oa-reverse-after-confirm`, `oa-pending-payments.open-after-input-invoice-confirm`, `tax-offset.open-after-input-invoice-confirm`, `cost-statistics.*after-input-invoice-confirm`. |

## `/output-invoice-collections` 销项发票收款情况

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Fresh rows, load failure, refresh recovery, filter, sort, page-size | `docs/modules/output-invoice-collections/e2e-spec.md` `OUT-COLL-E2E-001`; `README.md`; `tests.md` | page open, `刷新`, `查询`, `发票号码 排序`, `筛选 收款状态`, `筛选 发票号码`, `应用筛选`, `每页行数` | Fresh rows show invoice, collection status, bank/receipt fields; transient rows failure shows error instead of false empty; refresh recovers rows/export; filter/sort/page-size update API query and visible rows without mutations. | `covered` | `cd web && npx playwright test e2e/output-invoice-collections-flow.spec.ts e2e/output-invoice-red-relation-fanout.spec.ts --project=chromium` passed 12 tests; latency operation ids include `open-page-load-failure`, `refresh-after-load-failure.*`, `open-page-filter-sort`, `search-invoice-no`, `clear-search`, `sort-invoice-no`, `open-status-filter`, `apply-status-filter`, `open-invoice-no-filter`, `apply-invoice-no-filter`, `change-page-size-50`. |
| Collection status and reminder writes with recoverable failures | `OUT-COLL-E2E-002`; lifecycle tests | `状态/提醒`, status/reminder fields, `保存` | Successful save waits for rows refresh and shows backend-returned status; status 503 keeps drawer/draft, blocks reminder half-submit and rows refresh; reminder 503 keeps drawer/draft and retries reminder without resubmitting unchanged status. | `covered` | Same command passed; latency operation ids include `open-status-reminder`, `edit-status-reminder`, `save-status-reminder`, `open-status-reminder-failure`, `save-status-failure`, `retry-status-save`, `open-status-reminder-reminder-failure`, `save-reminder-failure`, `retry-reminder-save`. |
| Formal receipt lifecycle and recoverable failures | `OUT-COLL-E2E-003`; receipt lifecycle/API tests | `待出收据`, `创建正式收据`, `已出收据`, `作废收据`, `确认作废`, `重开收据`, `确认重开` | Receipt preview/create uses idempotency, waits for rows refresh, history shows real receipt facts; create/void/reissue 503 keeps drawer/dialog/input/history stable and retries before rows/history refresh. | `covered` | Same command passed; latency operation ids include `open-receipt-preview`, `create-receipt`, `open-receipt-history`, `open-receipt-void-dialog`, `confirm-receipt-void`, `open-receipt-reissue-dialog`, `confirm-receipt-reissue`, `create-receipt-failure`, `retry-receipt-create`, `confirm-void-failure`, `retry-void`, `confirm-reissue-failure`, `retry-reissue`. |
| Read model non-fresh and read-export permission | `OUT-COLL-E2E-005`; `OUT-COLL-E2E-006`; boundary docs; permissions inventory | page open with stale rows; `收款状态规则`; `已出收据`; `筛选内容导出` under `read_export_only` | Stale rows show refreshing diagnostics, no stale rows or write controls; read-export can read/export/rules/history, cannot see or trigger durable write controls, and mutation API count stays zero. | `covered` | Same command passed; latency operation ids include `open-page-read-model-stale`, `open-page-read-export`, `open-status-rules-read-export`, `close-status-rules-read-export`, `open-receipt-history-read-export`, `close-receipt-history-read-export`, `open-export-preview-read-export`. |
| Current-filter export and row-limit feedback | `OUT-COLL-E2E-007`; API export contract tests | `筛选内容导出`, `下载导出` | Export preview/download use current filters and no pagination; download event and XLSX fields are asserted; row-limit shows backend message and zero download. | `covered` | Same command passed; latency operation ids include `open-page-export-download`, `search-before-export`, `open-export-preview`, `download-export`, `open-page-export-row-limit`, `open-export-preview-row-limit`. |
| Red/blue invoice relation fan-out and revoke | `OUT-COLL-E2E-004`; `OUT-COLL-E2E-008`; downstream module specs | `红蓝票`, red invoice radio, `确认依据`, `确认关系`, export, links to `税金抵扣` and `成本统计`, `撤销人工关系 XSFP-E2E-0002` | Manual red relation waits for rows refresh and appears in drawer/export; tax offset and cost statistics re-read fresh downstream data; revoke waits for rows refresh and removes manual evidence while restoring collection status. | `covered` | Same command passed; latency operation ids include `open-red-relation-drawer`, `edit-red-relation-evidence`, `confirm-red-relation`, `open-export-preview-red-relation`, `download-export-red-relation`, `tax-offset.open-after-output-red-relation`, `cost-statistics.*after-output-red-relation`, `reopen-red-relation-drawer`, `revoke-red-relation`. |

## `/settings` 设置

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Data reset job and downstream fresh reads | `docs/modules/settings/e2e-spec.md` `SETTINGS-E2E-003`, `SETTINGS-E2E-004`, `SETTINGS-E2E-009`, `SETTINGS-E2E-010`; `README.md`; `boundary-io.md`; data reset service tests | `/settings`, `数据重置`, `清除所有银行流水数据`, `继续`, `当前 OA 用户密码`, `确认清理`, links to `/bank-details` and `/pending-invoices` | Data reset shows impact confirmation, requires OA password, creates a 202 job, shows first in-progress feedback, polls to completion, reloads settings, and downstream bank details / pending invoices read fresh empty rows instead of stale data. Production reset remains controlled-write external risk. | `covered` locally, `external-risk` for real production reset | `cd web && npx playwright test e2e/settings-data-reset-flow.spec.ts --project=chromium` passed 2 tests; latency operation ids include `settings.open-page-data-reset`, `settings.open-data-reset-section`, `settings.open-bank-data-reset-impact`, `settings.continue-data-reset-impact`, `settings.fill-data-reset-oa-password`, `settings.confirm-data-reset`, `settings.wait-data-reset-complete`, `bank-details.open-after-settings-data-reset`, `pending-invoices.open-after-settings-data-reset`. |
| Project completion setting fan-out to cost statistics | `docs/modules/settings/e2e-spec.md` `SETTINGS-E2E-002`; `docs/modules/cost-statistics/e2e-spec.md`; settings and derived data lifecycle tests | `/settings`, `项目状态`, project `标记完成`, `保存设置`, `/cost-statistics`, `按项目` | Save sends `completed_project_ids`, marks the project complete, refreshes settings without visible error, and cost statistics active scope fresh data excludes the completed project. Current Browser UI no longer exposes a project scope switch; `project_scope=all` remains an API/read-model contract, not a settings page button. | `covered` | Same command passed; latency operation ids include `settings.open-page-project-scope`, `settings.open-project-status-section`, `settings.mark-project-complete`, `settings.save-project-scope`, `cost-statistics.open-after-settings-project-scope-save`, `cost-statistics.switch-project-view-after-settings-project-scope-save`. |

## `/imports/bank-transactions` 银行流水导入

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Bank statement preview, conflicts, confirm, and downstream fresh pages | `docs/modules/imports-bank-transactions/e2e-spec.md` `IMPORT-BANK-E2E-001` - `IMPORT-BANK-E2E-007`; imports API tests; lifecycle tests | `/imports/bank-transactions`, file input, `对应账户`, `开始预览`, `确认导入`, `银行账户冲突确认`, `仍按所选账户 建设银行 8826 导入`, `取消`, links/routes to `/bank-details` and `/cost-statistics` | Files require account mapping before preview; preview shows audit/grid/duplicates; conflict dialog can be canceled without confirm; confirmed import waits for `/imports/files/confirm` and current Workbench summary refresh, then downstream bank details account balance and cost statistics read models are fresh and show imported row/cost evidence. | `covered` locally, `external-risk` for real worker drain | `cd web && npx playwright test e2e/imports-bank-transactions-flow.spec.ts --project=chromium` passed 7 tests; latency operation ids include `imports-bank-transactions.open-page`, `select-files`, `select-bank-account-mapping`, `preview-files`, `open-account-conflict-dialog*`, `confirm-account-conflict-*`, `confirm-import*`, `bank-details.open-after-bank-import`, and `cost-statistics.*after-bank-import`. |
| Negative import states | `IMPORT-BANK-E2E-002`, `IMPORT-BANK-E2E-003`, `IMPORT-BANK-E2E-005`, `IMPORT-BANK-E2E-006`; module state machine | `未导入项 1`, slow `开始预览`, `确认导入` after preview stale/server error | Corrupt file stays file-level and only valid file ID is confirmed; slow preview disables preview/clear/confirm; preview stale and confirm failure show errors, do not show success, and do not refresh Workbench summary. | `covered` | Same command passed; latency operation ids include `open-skipped-files-tab`, `preview-files-slow`, `confirm-import-with-corrupt-file`, `confirm-import-preview-stale`, `confirm-import-server-error`. |

## `/imports/invoices` 发票导入

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Invoice preview, confirm, and downstream fan-out | `docs/modules/imports-invoices/e2e-spec.md` `IMPORT-INVOICE-E2E-001` - `IMPORT-INVOICE-E2E-008`; imports API tests; lifecycle tests | `/imports/invoices`, file input, `票据方向`, `开始预览`, `确认导入`, routes to output collections/input usage/tax offset/pending invoices/OA pending/cost statistics | Files require input/output direction before preview; preview shows audit/grid/duplicates; confirm waits for `/imports/files/confirm` and current Workbench summary refresh; downstream invoice, tax, pending invoice, OA pending, and cost statistics pages return fresh read models and show imported evidence. | `covered` locally, `external-risk` for real worker drain | `cd web && npx playwright test e2e/imports-invoices-flow.spec.ts --project=chromium` passed 6 tests; latency operation ids include `imports-invoices.open-page`, `select-files`, `select-invoice-directions`, `preview-files`, `confirm-import*`, `output-invoice-collections.open-after-invoice-import`, `input-invoice-usage.open-after-invoice-import`, `tax-offset.open-after-invoice-import`, `pending-invoices.open-after-invoice-import`, `oa-pending-payments.open-after-invoice-import`, and `cost-statistics.*after-invoice-import`. |
| Negative import states | `IMPORT-INVOICE-E2E-002` - `IMPORT-INVOICE-E2E-005`; module state machine | `未导入项 1`, slow `开始预览`, `确认导入` after preview stale/server error | Corrupt invoice file stays file-level and only valid file ID is confirmed; slow preview disables preview/clear/confirm; preview stale and confirm failure show errors, do not show success, and do not refresh Workbench summary. | `covered` | Same command passed; latency operation ids include `open-skipped-files-tab`, `preview-files-slow`, `confirm-import-with-corrupt-file`, `confirm-import-preview-stale`, `confirm-import-server-error`. |

## `/imports/etc-invoices` ETC 发票导入

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| ETC zip preview, background confirm, and downstream fan-out | `docs/modules/imports-etc-invoices/e2e-spec.md` `IMPORT-ETC-E2E-001` - `IMPORT-ETC-E2E-010`; `docs/modules/etc-tickets/e2e-spec.md` `ETC-TICKET-E2E-002`; ETC backend/API tests | `/imports/etc-invoices`, `ETC对账任务`, zip file input, `开始预览`, `确认导入`, routes to `/etc-tickets`, `/tax-offset`, `/cost-statistics` | ETC import requires a ready task and zip files; preview uses ETC API, not generic imports files API; confirm shows background job feedback; downstream ETC tickets shows imported business batch, external batch id and invoice detail; tax offset and cost statistics read models are fresh and show ETC import evidence. | `covered` locally, `external-risk` for real worker/object storage drain | `cd web && npx playwright test e2e/imports-etc-invoices-flow.spec.ts --project=chromium` passed 5 tests; `cd web && npm test -- --run src/test/EtcTicketManagementPage.test.tsx` passed 79 tests; latency operation ids include `imports-etc-invoices.open-page`, `select-ready-task`, `select-zip-files`, `preview-zip-files`, `confirm-import*`, `etc-tickets.open-after-etc-import`, `tax-offset.open-after-etc-import`, and `cost-statistics.*after-etc-import`. |
| Negative ETC import states and discovered bug | `IMPORT-ETC-E2E-005` - `IMPORT-ETC-E2E-007`; `ETC-TICKET-E2E-002`; failure triage log | `确认导入` after preview stale, stale reconciliation task, or server error; imported business batch row | Preview stale does not start background job; stale reconciliation task clears old preview and disables confirm; server error stays visible. The run also found a confirmed UI bug where the ETC batch row omitted `externalEtcBatchId`; fixed by rendering `批次号 {externalEtcBatchId}` in the row fields. | `covered` | Same commands passed; linked triage entry: `2026-07-05T19:52:55+08:00 - ETC import downstream batch id visibility`. |

## `/operations/app-health` 系统状态

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Admin dashboard ready and refresh | `docs/modules/app-health-operations/e2e-spec.md` `APP-HEALTH-E2E-001`, `APP-HEALTH-E2E-004`, `APP-HEALTH-E2E-006`; app health API/component tests | `/operations/app-health`, `系统状态`, `刷新` | Admin route renders shell/nav, dashboard heading, data/request/runtime sections, calls `/api/operations/app-health-dashboard`, and clicking refresh calls the dashboard API again without hidden browser errors. | `covered` locally, `external-risk` for real production dashboard/runtime data | `cd web && npx playwright test e2e/app-shell.spec.ts e2e/app-shell-responsive.spec.ts --project=chromium` passed 6 tests; latency operation ids include `app-health.open-admin-dashboard` and `app-health.refresh-dashboard`. |
| App Health permission and session gates | `APP-HEALTH-E2E-002`, `APP-HEALTH-E2E-003`, `APP-SHELL-E2E-007`; permissions inventory | `/operations/app-health` under read-export, forbidden, expired sessions | Read-export users see admin-only message and make zero protected dashboard calls; forbidden/expired sessions render shell/session gates and make zero protected dashboard calls. | `covered` | Same command passed; latency operation ids include `app-health.open-read-export-admin-gate`, `app-shell.open-forbidden-session-gate`, `app-shell.open-expired-session-gate`. |

## App Shell 与导航

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Compact drawer navigation | `docs/modules/app-shell-navigation/e2e-spec.md` `APP-SHELL-E2E-005`; AppSidebar/PageRouteHost tests | mobile `/cost-statistics`, `打开菜单`, drawer link `设置` | Compact viewport opens the main navigation drawer, exposes expected links, clicking `设置` navigates to `/settings`, renders settings heading, and closes the drawer. | `covered` | `cd web && npx playwright test e2e/app-shell.spec.ts e2e/app-shell-responsive.spec.ts --project=chromium` passed 6 tests; latency operation ids include `app-shell.open-cost-statistics-mobile`, `app-shell.open-compact-navigation-drawer`, `app-shell.navigate-drawer-to-settings`. |
| Embedded OA shell menu | `APP-SHELL-E2E-006`; shell tests | `/?embedded=oa`, `展开菜单` | Embedded shell starts collapsed on desktop, keeps page body embedded, does not render compact `打开菜单`, and expands to show platform/OA labels and `折叠菜单`. | `covered` | Same command passed; latency operation ids include `app-shell.open-embedded-oa-shell`, `app-shell.expand-embedded-menu`. |
| Full deterministic browser smoke gate | `web/package.json` `e2e:smoke`; module `e2e-spec.md` and `e2e-coverage.md` files | 49 Browser specs covering finance pages, dialogs, drawers, downloads, permissions, stale/failed read models, write duplicate prevention, and cross-page fan-out | All deterministic Browser scenarios run under shared strict diagnostics and per-operation latency instrumentation; no hidden page/request/dialog/console failures are allowed by the harness. | `covered` locally | `cd web && npm run e2e:smoke` passed 180 Chromium tests in 6.8m; log stored at `.planning/quick/20260705-quality-closure-goal/e2e-smoke.log`. |

## Backend read model / worker / permission / audit contracts

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Read model fresh/cache/enqueue/readiness contracts | `docs/modules/read-models/e2e-spec.md`; `docs/architecture/module-boundaries/read-model-contracts.md`; `docs/dev/runtime-development.md` | `ReadModelQueryGateway`, refresh gateway, readiness reporter | Missing/stale/schema/source-version mismatch never masquerades as fresh; refresh enqueue goes through registered scope policy; readiness reporter records fresh/refreshing/failed explicitly and worker handlers are wrapped. | `covered` locally, `external-risk` for real production drain | `PYTHONPATH=backend/src python3 -m unittest tests.test_read_model_freshness tests.test_read_model_query_gateway tests.test_read_model_refresh_gateway tests.test_read_model_readiness_reporter -v` passed 46 tests. |
| Read model scope/barrier contracts | `READMODEL-E2E-003`, `READMODEL-E2E-007`; `WORKER-E2E-003`; operation barrier docs | Runtime worker refresh producers, scope contract service, operation freshness barrier | Worker lifecycle uses producer boundaries; invalid/orphaned scopes are reported or repaired idempotently; operation barriers block on exact target scope pending/failed dirty/outbox and do not fake fresh for unknown read models. | `covered` locally, `external-risk` for real production repair/apply | `PYTHONPATH=backend/src python3 -m unittest tests.test_runtime_worker_read_model_refresh_scopes tests.test_read_model_scope_contract tests.test_operation_freshness_barrier -v` passed 38 tests; includes `bank_flow_rule_batch` barrier not falling back to legacy no-OA readiness. |
| Runtime worker, queue, monitoring, transport and deploy contracts | `docs/modules/runtime-workers/e2e-spec.md`; `docs/operations/runtime-worker-governance.md` | Worker registry, queue repository, runtime monitoring, RabbitMQ transport, deploy env examples | Required workers/registry/manifest/env examples align; queue claim/complete/defer/retry/dead-letter contracts are guarded; worker dependency-not-fresh is deferred not failed; RabbitMQ remains transport only and PostgreSQL remains source of truth. | `covered` locally, `external-risk` for real RabbitMQ/systemd/long-running workers | `PYTHONPATH=backend/src python3 -m unittest tests.test_runtime_worker tests.test_runtime_worker_registry tests.test_runtime_queue tests.test_runtime_monitoring -v` passed 95 tests; `PYTHONPATH=backend/src python3 -m unittest tests.test_rabbitmq_runtime tests.test_runtime_queue_ops tests.test_runtime_state_policy tests.test_deploy_runtime_examples -v` passed 63 tests. |
| Permission, audit and controlled write tool contracts | `docs/modules/permissions-and-audit/e2e-spec.md`; `write-entry-inventory.md`; read model write SLO docs | Auth guard, session API, audit service, Workbench actor/idempotency, write-operation discovery/smoke/SLO audit tools | Missing/expired/forbidden sessions fail closed; read-export users can pass read/export routes but cannot mutate/admin; audit keeps actor/tenant/action metadata; Workbench write commands use explicit actor/idempotency/freshness targets; controlled write tools require auth, approval tickets, valid scenarios and redacted dry-run output. | `covered` locally, `external-risk` for real production audit/write apply | `PYTHONPATH=backend/src python3 -m unittest tests.test_auth_guard.AuthGuardTests.test_readonly_export_user_can_export_but_cannot_mutate_or_admin -v` passed 1 test after wrong-test triage; `PYTHONPATH=backend/src python3 -m unittest tests.test_auth_guard tests.test_session_api tests.test_audit_service tests.test_workbench_auth_context_idempotency tests.test_write_operation_slo_audit tests.test_write_operation_e2e_smoke tests.test_write_operation_scenario_discovery -v` passed 93 tests. |
| Full backend regression and lint gate | Repository `AGENTS.md`; `scripts/verify.sh backend`; `scripts/verify.sh lint` | Backend unittest suite and Python lint | After test/fixture/implementation fixes, all deterministic backend regression tests pass; local-only PostgreSQL/RabbitMQ/sample-file integration tests skip explicitly when env/sample inputs are absent; lint passes after backend Python changes. | `covered` locally, `external_input_required` for skipped external integration envs | `bash scripts/verify.sh backend` passed 4062 tests with 25 explicit skips; `bash scripts/verify.sh lint` passed with `All checks passed!`. |
| Full frontend component/build and docs gates | `scripts/verify.sh frontend`; `scripts/verify.sh docs`; module documentation rules | Vitest component/unit suite, TypeScript/Vite production build, docs structure checks | Frontend component/runtime contracts and production bundle build succeed; required module E2E docs exist and documentation boundary checks pass. Build warnings remain visible instead of being hidden. | `covered` locally, `partial` for build warnings | `bash scripts/verify.sh frontend` passed 69 test files / 802 tests and completed production build; Vite emitted CSS syntax warnings from generated CSS selectors and one chunk-size warning. `bash scripts/verify.sh docs` passed. |

## Production read-only validation

| Operation group | Sources | Controls / triggers | Expected business result and side effect | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Production external input preflight | `docs/dev/testing.md`; `docs/modules/app-health-operations/e2e-spec.md`; `FINOPS-PROD-READONLY-20260705-001` | `scripts/with-production-admin-token.sh`; `production_external_gate_preflight --json` | Secret values are redacted; admin HTTP/user HTTP/admin AppHealth browser gates are ready; ordinary OA route shell and write apply remain explicitly blocked when required inputs are absent. | `covered` for preflight, `external_input_required` for OA token and write apply DB/scenario inputs | `scripts/with-production-admin-token.sh bash -lc 'PYTHONPATH=backend/src python3 -m fin_ops_platform.tools.production_external_gate_preflight --json'` passed; ready gates: authenticated HTTP admin/user scope and production admin AppHealth browser; `FIN_OPS_E2E_OA_TOKEN`, write scenario/ticket, and PostgreSQL URL remained missing. |
| Production admin AppHealth browser smoke | `APP-HEALTH-E2E-001`, `APP-HEALTH-E2E-002`, `APP-HEALTH-E2E-004`; production admin smoke spec | `/fin-ops/operations/app-health` with admin cookie, no screenshots/traces/videos | Production dashboard loads navigation, heading, data/request/runtime panels, receives dashboard 200, shows no login/permission failure, and sends zero mutating requests. | `covered` read-only | `cd web && ../scripts/with-production-admin-token.sh npm run e2e:production-admin` passed 1 Chromium test in 2.9s. |
| Infra-smoke local contracts with production preflight | `docs/dev/testing.md`; `scripts/verify.sh infra-smoke`; runtime/read model/write-operation tool tests | `scripts/with-production-admin-token.sh bash scripts/verify.sh infra-smoke` | Local infra tool contracts pass; production preflight stays redacted; without `FIN_OPS_TEST_DATABASE_URL`, real Postgres read model apply, write-operation audit, and RabbitMQ staging preflight are skipped instead of guessed. | `partial` locally, `external_input_required` for DB/RabbitMQ/apply gates | Command passed 85 tests with 18 skipped; output explicitly skipped real Postgres read model check, real write-operation audit, and RabbitMQ staging preflight because PostgreSQL/RabbitMQ env was not configured. |
| Production authenticated HTTP SLO subset | `docs/dev/testing.md`; current page/API registry; `FINOPS-PROD-READONLY-20260705-001` | Custom `http_slo_probe` subset with current routes only: home shell, bank-flow-rule-batches shell, AppHealth shell, session, app health, admin dashboard, Workbench summary, bank-flow-rule-batches API, cost statistics explorer | Authenticated read-only page/API probes return expected statuses under 5s, do not use legacy no-OA page/API defaults, and read model payloads must be fresh. | `covered` for sampled subset, `external-risk` for full production route/API matrix | First Python run failed before business access due local CA trust (`CERTIFICATE_VERIFY_FAILED`); rerun with `SSL_CERT_FILE=$(python3 -m certifi)` reached production. Initial sampled run found transient `bank_flow_rule_batches_current` stale with one refresh enqueue; targeted rerun 5s later returned fresh in 531.202ms. Final full sampled rerun passed 9/9 probes with max p95 913.253ms. |
| Production authenticated SSE smoke | `docs/dev/testing.md`; SSE smoke tool | `/api/app-health/stream`, `/api/workbench/events?month=all` | Authenticated SSE endpoints return 200 `text/event-stream`, no errors, and first expected event arrives under 5s. | `covered` for sampled SSE endpoints | `scripts/with-production-admin-token.sh bash -lc 'export SSL_CERT_FILE=$(python3 -m certifi); PYTHONPATH=backend/src python3 -m fin_ops_platform.tools.sse_smoke_probe --base-url https://www.yn-sourcing.com --target-ms 5000 --timeout-seconds 15 --json'` passed 2 probes; max first event 969.364ms. |
| Production SSH read-only discovery | `FINOPS-PROD-SSH-READONLY-20260705-001`; `docs/operations/runtime-worker-governance.md`; deploy docs | SSH via local secret file; `whoami`, `hostname`, `systemctl list-units`, deployment/runtime-smoke directory listing | SSH can reach production without printing secrets; core app/runtime services and current deploy directories are discoverable; no remote files are edited and no services are restarted. | `partial` read-only, `external-risk` for failed system units | SSH read-only discovery succeeded as `root` on `VM-0-6-opencloudos`; `/opt/fin-ops` and `/opt/fin-ops/runtime-smoke` exist; `fin-ops.service`, PostgreSQL, RabbitMQ, Redis, `fin-ops-rabbitmq-dispatcher`, `fin-ops-worker@bank-flow-rule-batch`, `fin-ops-worker@workbench*`, `fin-ops-worker@cost-statistics`, `fin-ops-worker@tax-offset`, `fin-ops-worker@turnover-ledger`, and other listed workers were active/running. Failed units observed: `finops-prune-runtime-queue-history.service`, `logrotate.service`, plus unrelated/not-found `clamd@scan.service` and failed `systemd-fsck-disk.timer`. No remote mutation was performed. |
| Production DB/readiness read-only snapshot over SSH | `docs/architecture/module-boundaries/read-model-contracts.md`; runtime worker docs | `sudo -u postgres psql -d fin_ops -Atc select ...` read-only aggregate queries | Durable queue and read model readiness can be inspected on host without a local PostgreSQL URL; current outbox/dirty/readiness state should be converged. | `covered` read-only snapshot, `external-risk` for historical heartbeat cleanup | Read-only SQL snapshot: `job.outbox_events` status `done:260909`; `job.read_model_dirty_scopes` status `done:240884`; `read_model.app_status_readiness` status `fresh:540`; `job.runtime_worker_heartbeats` count 53 with fresh current worker rows at 2026-07-05 20:15:38 CST. Historical stale idle heartbeat rows from 2026-05-29 remain for old `bank-detail-read-model` worker ids, so raw max heartbeat lag is not a reliable current-health metric without registry-aware filtering. |
| Production read-model refresh apply | `FINOPS-PROD-REFRESH-APPLY-20260705-001`; `read_model_slo_smoke`; read-model worker contracts | Remote `read_model_slo_smoke --critical-only --apply --target-ms 5000` | Critical read-model refresh apply may enqueue refresh events and must drain workers to `done`/`fresh` under the 5s target. | `failed` performance, data convergence `covered` | Production apply ran 16 critical scopes. All scopes drained to `done`/`fresh`, but 2 scopes exceeded target: `bank_account_balance` 7212.511ms and `bank_flow_rule_batch` 7261.077ms; report `/opt/fin-ops/runtime-smoke/read-model-slo-apply-codex-20260705T130807Z.json`. Post-apply snapshot: outbox/dirty all `done`, readiness all `fresh`, recent failed counts `0`. Linked triage: `2026-07-05T21:08:07+08:00 - production read-model apply slow critical scopes`. |
| Controlled reversible production write smoke | `FINOPS-WRITE-SMOKE-20260705-001`; `write_operation_scenario_discovery`; `write_operation_e2e_smoke`; `write_operation_slo_audit` | Read-only scenario discovery, one approved `workbench_relation_withdraw`, pre/post health snapshots, apply, write SLO audit | Discovery must be read-only; apply is limited to approved reversible scenarios. Mutating step must pass, read-model/write SLO must pass, post probes should remain within targets, and post health must converge. | `failed` post API performance, write convergence `covered` | Discovery found 1 safe `workbench_relation_withdraw` candidate and 0 no-oa/bank-flow-rule candidates. Pre-write health was clean. Apply executed one withdraw: POST 200 in 2846.294ms; write SLO audit passed with `p95_enqueue_to_done_ms=879.533`. Overall apply report failed because `operations_app_health_dashboard` post probe took 2213.685ms against its 1000ms scenario target; `workbench_groups` passed at 228.69ms. Post-write health was clean. Reports: `/opt/fin-ops/runtime-smoke/write-operation-scenario-discovery-codex-20260705T130227Z.json`, `/opt/fin-ops/runtime-smoke/write-operation-e2e-apply-codex-20260705T130444Z.json`, `/opt/fin-ops/runtime-smoke/write-operation-slo-audit-codex-20260705T130659Z.json`. Linked triage: `2026-07-05T21:04:44+08:00 - controlled production Workbench withdraw post API SLO`. |
