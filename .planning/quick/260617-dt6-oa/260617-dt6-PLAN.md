# Quick Task 260617-dt6: 修正免OA流水批量处理提交/撤回状态展示并确认关联台配对收敛

## Goal

免 OA 流水批量处理页面不再向用户显示 `分类已变更，需复核` 这类 stale 中间态。已提交且仍有 active no-OA relation 的批次按 `已提交` 展示并可撤回；未提交 draft 继续按未提交展示并可提交。确认现有 no-OA submit/withdraw 已在关联台 paired/open 闭环。

## Tasks

1. Backend API projection
   - Normalize SQL read model rows where `status=stale` but `status_bucket=submitted` or `can_withdraw=true` to user-facing `status=submitted`.
   - Clear user-facing blocked reason for that projection and count it as submitted in summary.
   - Verify with a service-layer test.

2. Frontend compatibility projection
   - Normalize the same legacy payload shape in the no-OA API mapper so old/stale cached responses do not render `需复核`.
   - Verify with a page interaction test that the submitted bucket shows `已提交` and `撤回批次`, not `分类已变更` or `需复核`.

3. Closure confirmation
   - Reuse existing integration coverage proving submit enters Workbench paired and withdraw returns rows to Workbench open/unmatched.
   - Update no-OA module docs/notes only if the visible state contract changes.

## Verification

- `PYTHONPATH=backend/src python3 -m pytest tests/test_no_oa_bank_batch_application_service.py tests/test_no_oa_bank_batch_workbench_integration.py::NoOaBankBatchWorkbenchIntegrationTests::test_no_oa_salary_batch_relation_pairs_then_cancel_returns_to_open tests/test_no_oa_bank_batch_workbench_integration.py::NoOaBankBatchWorkbenchIntegrationTests::test_no_oa_internal_transfer_relation_groups_bank_rows_until_cancelled -q`
- `cd web && npm test -- --run src/test/NoOaBankBatchPage.test.tsx src/test/NoOaBankBatchApi.test.ts`
