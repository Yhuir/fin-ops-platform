---
quick_id: 260729-dg6
mode: quick-validate-inline
status: in_progress
must_haves:
  truths:
    - Every submitted ETC business batch can download one deterministic PDF with one invoice per page, including historical submitted batches without an OA draft id.
    - ETC OA uploads persist OA-relative attachment references instead of internal absolute URLs.
    - Existing malformed ETC OA attachment references are repaired only after dry-run, backup, and concurrent-change checks.
  artifacts:
    - ETC batch PDF eligibility and submitted-detail UI reuse the existing PDF bundle API.
    - The ETC OA HTTP adapter normalizes the upload response at the external boundary.
    - Focused backend, API, frontend, and production verification evidence exists.
  key_links:
    - The submitted-detail button calls the existing business-batch invoice PDF endpoint.
    - The application service owns lifecycle eligibility; the PDF bundle service owns document validation and assembly.
    - The OA payload builder receives an already-normalized attachment reference.
---

# ETC submitted batch PDF download and OA attachment URL repair

## Task 1: Complete the existing batch PDF path

- Move the lifecycle readiness check from the PDF assembly service to the ETC business-batch application boundary.
- Permit an OA draft or an existing submitted status; keep unsubmitted/no-draft batches blocked.
- Add a non-nested header action to the submitted invoice-detail disclosure and reuse the existing download handler.
- When production verification finds historical object loss, add one admin-only submitted-batch recovery command that restores only existing members whose original ZIP bytes match the recorded hashes; do not add a legacy storage fallback.
- Add focused service/API/UI regression coverage.

## Task 2: Normalize ETC OA upload attachment references

- Normalize known OA absolute `/fileManager/` or `/profile/` upload responses to OA-relative paths at `HttpEtcOAClient`.
- Preserve existing root-relative paths and opaque upload identifiers.
- Reject unknown absolute URLs and update the ETC/OA boundary documentation.
- Add focused adapter contract tests.

## Task 3: Verify, deploy, and repair production data

- Run focused tests, lint, type/build checks, and inspect the final diff.
- Commit and push `main`, deploy through `scripts/deploy-oa.sh`.
- Back up affected submitted batch details, restore missing PDF/XML objects from the original ZIPs, and prove the command is idempotent.
- Dry-run all strict ETC-origin OA records, back up matching records, repair only unchanged malformed attachment references, and re-read to verify.
- Validate 36/68-page downloads, OA attachment access, audit behavior, and measured response performance.
