---
quick_id: 260729-nh8
mode: quick-validate-inline
status: completed
must_haves:
  truths:
    - The all-scope relation preview treats byte-for-byte equivalent normalized canonical rows from active month generations as one logical row.
    - The same row id with divergent normalized content remains fail-closed before any relation mutation.
    - Selected-row order, OA attachment context, freshness/version gates, bounded SQL, and formal command rereads remain unchanged.
  artifacts:
    - The shared Workbench preview row-index boundary owns deterministic duplicate handling.
    - Focused repository and frontend error-contract tests protect the new behavior.
    - Module boundary, test, and implementation records describe the exact contract.
  key_links:
    - Confirm and withdraw preview both reuse the same repository row-index boundary.
    - Preview DTOs remain derived-only and never enter the formal relation UoW.
    - Production verification uses read-only preview requests unless a sanctioned test-owned reversible fixture exists.
---

# 关联台跨月 canonical row 预览去重

## Task 1: 修复共享预览选行边界

- 在现有 `_workbench_relation_preview_rows_by_id(...)` 中合并内容完全一致的重复 row。
- 同一 row id 的任意规范化内容不一致继续返回 `relation_preview_rows_ambiguous`。
- 不改 projection、generation、SQL、relation facts、schema、worker、cache 或正式写入链路。

## Task 2: 补齐最小回归合同

- 把 repository 的 bounded preview 测试扩成生产同形的 `month=all` 双 generation 重复 selected/context rows。
- 增加同 row id 的金额、状态和来源内容漂移 fail-closed 测试。
- 为前端增加批准的 `relation_preview_rows_ambiguous` 中文错误映射。

## Task 3: 验证、发布和生产只读证明

- 运行针对性后端、前端测试、repository lint、docs gate 和 diff 检查。
- 提交并 push `main`，通过 `scripts/deploy-oa.sh` 部署。
- 使用本机 production admin token 对真实复现选区验证 HTTP/DTO、fresh/version、耗时和 Page Audit；没有 test-owned 可逆 fixture 时不执行真实关系 mutation。
