---
quick_id: 260728-g9q
status: completed
mode: quick-validate
date: 2026-07-28
must_haves:
  truths:
    - 所有由 canonical OA 行自身显式字段证明的来源别名都确定性归一到该 OA 的 canonical row id
    - 同一别名若指向多个 canonical OA 必须 fail closed，不按金额、申请人、项目名或顺序猜测
    - OA 附件发票通过显式来源别名进入父 OA 正式关系，并按显式付款项 row_index 映射到 canonical expense item id
    - 原始 source_expense_item_id 保留在 canonical 发票事实中，页面归一不能反写或破坏来源证据
    - OA 附件绑定在正式关系撤回/扩展时保持不可拆散
  artifacts:
    - backend/src/fin_ops_platform/services/oa_attachment_invoice_linking.py
    - backend/src/fin_ops_platform/services/postgres_repositories/workbench_formal_relation.py
    - backend/src/fin_ops_platform/services/workbench_free_matching_engine.py
    - backend/src/fin_ops_platform/services/workbench_relation_command_service.py
    - backend/src/fin_ops_platform/services/workbench_pair_relation_service.py
    - backend/src/fin_ops_platform/services/workbench_relation_alignment_service.py
    - backend/src/fin_ops_platform/services/workbench_relation_grouping.py
  key_links:
    - OA canonical rows -> shared alias map -> formal relation references
    - FormalRelationPlan exact attachment bindings -> relation special_metadata -> immutable binding recovery
    - Active relation rows -> shared alias map + explicit row_index -> display-only canonical expense item id
---

# Quick Task 260728-g9q: OA 来源别名统一事实源归一

## Task 1：统一 OA alias 边界

**Files**
- `backend/src/fin_ops_platform/services/oa_attachment_invoice_linking.py`
- `backend/src/fin_ops_platform/services/postgres_repositories/workbench_formal_relation.py`
- `backend/src/fin_ops_platform/services/workbench_relation_alignment_service.py`

**Action**
- 扩展现有 `oa_row_source_ids`，从 canonical OA row 与 normalized payload 的显式身份字段生成唯一 alias map。
- 正式匹配 repository 在构造引用前用该 map 把 OA source/item parent identity 归一为 canonical OA row id。
- 显式历史引用使用同一组字段做有界精确查询；冲突直接失败。
- relation alignment 使用同一 map，不新增 fallback 或模糊匹配。

**Verify**
- 当前窗口与历史窗口的 Mongo 文档 ID / 流程请求 ID 别名都解析到 canonical OA。
- alias collision 抛错且零 relation plan。

**Done**
- 正式匹配和 relation alignment 不再依赖来源 ID 与 canonical ID 字符串相同。

## Task 2：保护附件绑定并归一付款项展示

**Files**
- `backend/src/fin_ops_platform/services/workbench_free_matching_engine.py`
- `backend/src/fin_ops_platform/services/workbench_relation_command_service.py`
- `backend/src/fin_ops_platform/services/workbench_pair_relation_service.py`
- `backend/src/fin_ops_platform/services/workbench_relation_grouping.py`

**Action**
- Formal plan 携带由 explicit `attachment_source` 直接证明的 OA/invoice typed binding。
- command 将 binding 写入正式关系 metadata；纯 OA+附件关系标记 immutable，混合关系标记 contains immutable。
- pair service 从显式 metadata 恢复 exact binding；兼容既有纯 immutable 关系但不按 invoice row id 猜测。
- grouping 只在同一正式 relation 内，用显式 OA alias + `source_expense_row_index` 唯一映射 canonical `expense_items.id`；canonical 发票事实保留原来源 ID。
- bump matching rule version，确保生产重新执行新合同。

**Verify**
- OA ¥413 的五条 item 保持父 OA 唯一 relation member；¥60 发票映射到 row_index=2 的 canonical item。
- relation 扩展/撤回后 OA 与附件发票绑定仍保留。

**Done**
- 关系 ownership 与页面复合行展示均使用 canonical OA/item identity，旧 source identity 只作为来源证据。

## Task 3：测试、文档、发布与生产闭环

**Files**
- 相关 backend unit/service/read-model regression tests
- `docs/modules/oa-integration/boundary-io.md`
- `docs/modules/reconciliation-workbench/boundary-io.md`
- `docs/modules/workbench-relations/boundary-io.md`
- 对应 tests / implementation notes

**Action**
- 增加业务核心、service、read model/worker 与现有功能回归测试。
- 更新 alias、immutable binding、display-only item normalization 的长期合同。
- 仅运行针对性 pytest、backend lint/docs；不运行全浏览器 suite 或完整 CI。
- commit/push `main`，使用正式部署入口发布；触发目标 scope matching 与 Workbench rehydrate。
- 生产只读验证目标 OA、5 items、目标发票同组、freshness/Audit/耗时。

**Verify**
- 针对性测试全绿，lint/docs 无新增失败。
- remote main 与部署 commit 一致。
- 生产目标链路和健康检查通过。

**Done**
- 代码、数据收敛、正式关系、页面展示和生产证据完整闭环。
