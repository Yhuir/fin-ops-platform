---
quick_id: 260729-037
status: completed
completed_at: 2026-07-29
commit: a21cb8e36
release: main-959cd2ff-20260729001729
---

# Quick Task 260729-037 Summary

## 结果

- 搜索只决定关联组是否命中，不再自动展示 ETC 发票或流水规则批次的折叠明细。
- 搜索条件变化会清理展开、加载和失败状态；旧搜索发出的 detail 请求不能污染新搜索结果。
- Workbench summary 不再返回 `collapsed_rows`，仅保留 `collapsed_row_counts`；完整明细只在用户点击展开后从 detail API 获取。
- 删除旧折叠搜索预览常量、helper、组装分支和对应旧测试合同，没有新增表、worker、read model、兼容旁路或依赖。
- 既有 groups response cache schema 已提升，部署后不会继续读取最长 600 秒的旧 summary payload。

## 验证

- 前端定向测试：5 个文件，173 tests passed。
- 后端 Workbench 定向测试：256 tests passed。
- `npm run build`、`bash scripts/verify.sh lint`、`bash scripts/verify.sh docs`、`git diff --check` 均通过。
- 生产 Audit：`overall_status=pass`，integrity pass、freshness fresh、queue drained、issues 0。
- 生产搜索 summary：ETC 与流水规则均无 `collapsed_rows`；detail 分别返回完整 34 张发票和 13 条流水。
- 生产 UI：搜索和非搜索状态下，ETC 与流水规则批次均可正常展开和收起；展开时切换搜索再返回会恢复默认折叠；无加载失败。
- 20 次生产采样：
  - ETC 搜索 summary p95 337.4ms，detail p95 177.2ms。
  - 流水规则搜索 summary p95 549.1ms，detail p95 185.0ms。
  - 四项均低于既有 1000ms 目标。

## 发布

- 代码提交：`a21cb8e36 fix(workbench): decouple search from collapsed detail`
- 计划提交：`959cd2ff5 docs(quick-260729-037): plan workbench collapse fix`
- 已推送 `origin/main`。
- 正式 release `main-959cd2ff-20260729001729` 已激活；API、前端资源、dispatcher 和 11 个注册 worker 正常。
- 本次没有 migration、rehydrate 或生产业务写入。
