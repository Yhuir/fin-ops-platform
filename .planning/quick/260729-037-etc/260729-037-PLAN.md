---
quick_id: 260729-037
status: completed
mode: validate
description: 修复关联台搜索状态下 ETC 与流水规则批次折叠展开状态，移除旧折叠搜索预览并完成生产验证
must_haves:
  truths:
    - 搜索只决定关联组命中，不改变 ETC 或流水规则批次的展开状态
    - 折叠、展开、收起在搜索和非搜索状态下行为一致
    - 搜索切换期间返回的旧详情请求不能重新展开新结果
    - summary 不再携带折叠搜索预览，detail 仍返回完整明细
  artifacts:
    - web/src/components/workbench/RelationGroupGrid.tsx
    - backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
    - backend/src/fin_ops_platform/services/workbench_groups_page_cache.py
    - web/src/test/RelationGroupGrid.test.tsx
    - tests/test_workbench_sql_runtime.py
    - docs/modules/reconciliation-workbench/boundary-io.md
    - docs/modules/reconciliation-workbench/tests.md
    - docs/modules/reconciliation-workbench/implementation-notes.md
  key_links:
    - RelationGroupGrid 的 expandedPaneGroups 是明细可见性的唯一状态源
    - Workbench summary 保留 collapsed_row_counts，完整 collapsed_rows 只由 detail 读取
---

# Quick Task 260729-037

## Task 1：收敛折叠状态与异步请求

**files**
- `web/src/components/workbench/RelationGroupGrid.tsx`
- `web/src/test/RelationGroupGrid.test.tsx`

**action**
- 删除搜索时直接渲染 `collapsedRows` 的旧分支。
- 搜索条件变化时清理展开、加载和失败状态。
- detail 请求完成前若搜索条件已经变化，忽略旧请求的展开结果。
- 保留既有 click-only detail 加载、失败重试和已加载数据复用。
- 覆盖 ETC 与流水规则批次在搜索/非搜索、展开/收起、失败/重试及请求竞态下的用户可见行为。

**verify**
- 定向运行 `RelationGroupGrid.test.tsx`。

**done**
- 搜索与展开状态完全解耦，按钮、ARIA 和可见行一致。

## Task 2：删除后端旧折叠搜索预览

**files**
- `backend/src/fin_ops_platform/services/postgres_repositories/read_models.py`
- `backend/src/fin_ops_platform/services/workbench_groups_page_cache.py`
- `tests/test_workbench_sql_runtime.py`

**action**
- 删除最多 3 条折叠搜索预览的常量、helper 和 summary 组装调用。
- 保留隐藏成员参与服务端搜索、`collapsed_row_counts` 和完整 detail 数据。
- 更新 repository/API 合同测试，证明 summary 不返回折叠明细且 detail 合同不变。
- 提升既有 groups response cache schema，避免部署后继续命中旧 summary payload。

**verify**
- 定向运行 Workbench SQL runtime 测试。

**done**
- 旧预览代码和测试合同全部移除，搜索命中能力未退化。

## Task 3：同步合同并完成发布验证

**files**
- `docs/modules/reconciliation-workbench/boundary-io.md`
- `docs/modules/reconciliation-workbench/tests.md`
- `docs/modules/reconciliation-workbench/implementation-notes.md`

**action**
- 把“闭合态显示最多 3 条真实命中”替换为“搜索只决定组命中，用户显式展开才显示明细”。
- 运行定向前后端测试、类型/构建、lint、docs 和旧路径扫描。
- 提交并推送 `main`，使用 `./scripts/deploy-oa.sh` 部署。
- 使用本机生产 admin token 做只读 API/Audit/性能验证，并对 ETC 与流水规则批次执行有意义的生产交互验证。

**verify**
- 所有定向门禁通过；生产 release、Audit、折叠交互、请求次数和性能达到既有合同。

**done**
- 新合同、代码、测试、文档、远端 main 与生产运行版本一致。
