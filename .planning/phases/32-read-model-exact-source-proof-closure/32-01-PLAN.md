---
phase: 32-read-model-exact-source-proof-closure
plan: "01"
type: execute
wave: 1
depends_on:
  - 30-05
  - 31-01
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-11
  - AUDIT-12
  - RMF-09
files_modified:
  - backend/src/fin_ops_platform/services/workbench_relation_sql_projection.py
  - backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
  - backend/src/fin_ops_platform/services/workbench_relation_read_model_repository.py
  - tests/test_workbench_relation_sql_projection.py
  - tests/test_postgres_repositories_boundaries.py
  - tests/test_turnover_ledger_postgres_integration.py
  - tests/test_workbench_etc_relation_enrichment_postgres.py
  - tests/test_oa_pending_payment_read_model_query.py
  - tests/test_oa_pending_payment_postgres_integration.py
  - docs/architecture/module-boundaries/read-model-contracts.md
  - docs/modules/workbench-relations/boundary-io.md
  - docs/modules/cost-statistics/boundary-io.md
  - docs/modules/read-models/boundary-io.md
  - docs/modules/runtime-workers/boundary-io.md
  - docs/operations/runtime-worker-governance.md
must_haves:
  truths:
    - "Canonical active typed relation edges and shared workbench_relation edges are equal in both directions for every registered scope."
    - "Legacy bank IDs and canonical UUIDs resolve to one logical member and cannot leave an orphan relation group after withdraw or replacement."
    - "Cost rejects old Bank Detail schema and real Workbench business drift while ignoring only the documented active-generation execution cursor."
    - "Ordinary writes remain zero read-model fan-out; exact page access owns recovery and concurrent access dedupes to one current-effective job."
    - "Production closure uses one candidate sweep, records all issues before any correction, restores its test-owned fixture and ends with 16/16 internal page proofs."
  artifacts:
    - path: "backend/src/fin_ops_platform/services/workbench_relation_sql_projection.py"
      provides: "membership-safe exact-scope projection and delta cleanup"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/read_models.py"
      provides: "canonical source and published-scope proof with atomic derived-state publish"
    - path: "backend/src/fin_ops_platform/services/cost_statistics_source_versions.py"
      provides: "existing semantic Cost dependency proof contract, verified without duplicate implementation"
  key_links:
    - from: "app.workbench_pair_relations"
      to: "read_model.workbench_relation_groups"
      via: "typed canonical membership proof"
      pattern: "relation_membership"
    - from: "page query freshness gate"
      to: "ReadModelRefreshGateway"
      via: "exact scope mismatch"
      pattern: "refreshing"
    - from: "Cost Statistics"
      to: "Workbench active generation and Bank Detail"
      via: "semantic source-version comparison"
      pattern: "bank_detail_schema_version"
---

# Phase 32 Plan 01：全页面精确事实证明生产闭环

## Objective

修复生产中 queue 已清空但共享关系与 Cost 历史 scope 仍被错误认定为 fresh 的共享根因。不得重写所有页面、不得恢复写后 fan-out、不得新增 coordinator/queue/cache/table，也不得用直接 SQL 或人工 mark-fresh 掩盖问题。

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Freeze RED counterexamples and current production contract</name>
  <read_first>
    docs/architecture/module-boundaries/read-model-contracts.md
    docs/modules/workbench-relations/boundary-io.md
    docs/modules/cost-statistics/boundary-io.md
    docs/modules/read-models/boundary-io.md
    docs/modules/runtime-workers/boundary-io.md
    docs/operations/runtime-worker-governance.md
    backend/src/fin_ops_platform/services/workbench_relation_sql_projection.py
    backend/src/fin_ops_platform/services/workbench_relation_read_facade.py
    backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
    backend/src/fin_ops_platform/services/cost_statistics_source_versions.py
  </read_first>
  <action>
    Add deterministic failing tests for canonical UUID versus legacy bank ID, withdrawal of an older relation while a newer relation remains, member replacement, cross-month membership, source-version equality with edge mismatch, and Cost Bank Detail v11 versus v12. Reuse existing fixtures and disposable PostgreSQL helpers; do not add a parallel test runner.
  </action>
  <acceptance_criteria>
    - The new tests fail against the current implementation for the intended contract mismatch, not for import or fixture errors.
    - Existing Phase 30/31 confirm, withdraw, turnover selection and zero-fan-out tests remain green.
    - No production source file changes are included in the RED commit.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repair exact shared relation proof at the existing repository/projection boundary</name>
  <action>
    Reuse existing canonical identity and source-summary boundaries. Make exact typed active-edge membership part of scope proof, normalize canonical/legacy aliases before overlap cleanup, delete every old group touched by any alias, and fall back to a concrete-month full rebuild when the affected set cannot be proven. Publish rows, groups and proof atomically; bump the projection schema. Keep turnover_manual_closure excluded from the shared distribution.
  </action>
  <acceptance_criteria>
    - RED relation tests pass.
    - Source-version equality without exact membership cannot return fresh.
    - Active to withdrawn and member replacement remove old alias groups.
    - Cross-month members are indexed in every affected month.
    - No ordinary canonical relation write enqueues a page read model.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify Cost dependency proof and delete only proven weak runtime paths</name>
  <action>
    Verify that the existing Cost semantic comparison used by query gate, worker and Audit already ignores only the Workbench execution cursor while retaining all business proof and Bank Detail schema. Do not rewrite or duplicate it when the RED/current tests pass. Bump the Cost projection contract only if the stored proof shape changes. Delete only weak max-updated/count-only/legacy-schema/fallback paths proven live by whole-repository caller checks. Recover historical production scopes through the formal gateway/queue after deployment; do not modify page code that already satisfies the contract.
  </action>
  <acceptance_criteria>
    - Cost v11 Bank Detail proof is stale against v12.
    - Workbench execution cursor-only drift remains semantically equal.
    - Real Workbench business drift remains non-fresh.
    - time and bank_tag views do not enqueue Workbench/Cost jobs.
    - No second coordinator, writer, table, worker, cache or fallback is introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Close targeted local regression and boundary documentation</name>
  <action>
    Run the applicable business, service, API, read-model/background-job, frontend and critical E2E slices. Run disposable PostgreSQL integration, lint, docs, frontend production build and query-plan checks. Update only long-term boundary facts changed by the implementation. Do not run the unrelated 183-browser suite or full CI.
  </action>
  <acceptance_criteria>
    - All targeted backend and disposable PostgreSQL tests pass.
    - Relevant frontend interaction tests and critical Chromium confirm/withdraw flow pass.
    - lint, docs, build and git diff check pass.
    - Fresh hot-path queries stay set-based with no per-row/N+1 I/O.
    - Six previously passing pages retain their registered contracts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Push one main candidate, deploy, repair derived scopes and prove production closure</name>
  <action>
    Review the whole diff, commit all intended files, push exact main to origin/main and deploy through scripts/deploy-oa.sh. Use the local production admin-token helper. Dry-run then execute only formal gateway/queue derived-scope recovery. Use the sanctioned reversible fixture to prove confirm, exact page-access recovery, zero write fan-out, unrelated-page isolation, same-scope dedupe, withdraw, inverse recovery, per-page latency, queue/worker drain and final System Audit. Complete the entire production sweep before any corrective code change; if issues exist, consolidate them into one correction batch and at most one subsequent candidate.
  </action>
  <acceptance_criteria>
    - Remote main exact SHA equals the deployed active release.
    - No direct SQL writes canonical facts, read model rows or readiness.
    - The test-owned fixture returns to its original canonical fingerprint.
    - Ordinary writes create zero page refresh events; unvisited pages create no unrelated I/O.
    - Every visited scope becomes fresh within the bounded validation window and fresh-page p95 is recorded.
    - Required workers are current, queue is drained and 16/16 internal page Audit proofs pass in one read-only snapshot.
    - External control evidence unknown is reported separately and not misrepresented as an internal failure or pass.
  </acceptance_criteria>
</task>

## Verification

- `python3 -m unittest` on the targeted Workbench relation, Cost, runtime gateway/worker and Audit modules.
- Disposable PostgreSQL relation/Cost integration suites.
- Targeted Vitest and critical Chromium confirm/withdraw flow.
- `bash scripts/verify.sh lint`
- `bash scripts/verify.sh docs`
- frontend production build.
- `git diff --check`.
- Official deployment health, exact release, worker, queue, reversible fixture, page timing and System Audit evidence.

## Success Criteria

- All five tasks and acceptance criteria pass.
- Old weak runtime paths have zero callers or are deleted.
- No unrelated page implementation is rewritten.
- No hard 3-second claim is made without evidence; correctness and measured performance are reported separately.
- Summary and verification artifacts contain exact commits, release, timings, queue/Audit evidence and remaining external-evidence boundary.
