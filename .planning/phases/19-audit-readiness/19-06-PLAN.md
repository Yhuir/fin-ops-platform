---
phase: 19-audit-readiness
plan: 06
type: execute
wave: 6
depends_on:
  - 19-05
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-10
---

<objective>
封堵流水规则批量处理的状态/成员/关系绿色遗漏：canonical batch、页面 read-model payload 和 `bank_flow_rule_batch` active relation 必须按 batch id 与完整 bank member set 收敛。
</objective>

<grill_gate>

- 现有 check 仅在 relation 存在时比较 member IDs，不能证明 submitted relation 必然存在。
- structured `row_count` 相等不能证明 page payload 的 member IDs 相等。
- submitted batch 必须有同 case id 的 active `bank_flow_rule_batch` relation；非 submitted batch 不得保留该 active relation。
- expected source 是 `app.bank_flow_rule_batches`；page consumer 是 `read_model.bank_flow_rule_batch_rows.payload`；relation fact 是 `app.workbench_pair_relations`，职责不混合。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Batch/page/relation member equality</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/page_consumer_relation_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, tests/test_audit_page_business_read_model_tool.py</files>
  <action>按 batch id 比较 canonical bank_transaction_ids、fresh page payload bank_transaction_ids/row_ids、active bank_flow_rule_batch relation bank edges；检测 submitted missing relation、unsubmitted active relation、relation orphan、任一 member-set mismatch。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Revision/docs/regression</name>
  <files>backend/src/fin_ops_platform/services/page_audit_registry.py, docs/architecture/module-boundaries/read-model-contracts.md, docs/modules/bank-flow-rule-batches/boundary-io.md, tests/test_app_health_api.py, web/src/test</files>
  <action>bump revision，登记该 consumer contract；更新 docs/fixtures，运行 Audit/API/runtime guards、lint/docs/diff。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py tests/test_page_audit_registry.py tests/test_platform_runtime_boundary_guards.py -q</verify>
</task>

</tasks>

<success_criteria>

- submitted batch 缺 relation 时 Audit 失败。
- page payload、canonical batch 或 active relation 任一 member ID 缺失/多余时 Audit 失败。
- withdrawn/draft batch 仍有 active batch relation 或 relation 无 canonical batch 时 Audit 失败。
- 不修改批次/关系事实或 refresh 链路。

</success_criteria>
