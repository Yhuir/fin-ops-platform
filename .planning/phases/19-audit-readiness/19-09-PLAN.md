---
phase: 19-audit-readiness
plan: 09
type: execute
wave: 9
depends_on:
  - 19-08
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-04
  - AUDIT-05
  - AUDIT-09
  - AUDIT-10
---

<objective>
把关联台 active-generation relation display proof 收敛到统一 page Audit：证明 canonical/shared relation 与所有 active Workbench generation 的可见成员、同组 owner、case/mode/alignment 和 generation 时序一致；将旧独立工具降为薄 CLI，页面新增统一 Audit 控件。
</objective>

<grill_gate>

- 成本统计以 Workbench active generation 为输入；在关联台 active generation 未被统一证明前，cost canonical expected-set 仍是相关投影来源，不能完成最终 lineage claim。
- 现有 `audit_workbench_relation_display` 有有效 production checks，但输出/快照/freshness/queue 合同独立，属于并行 runtime proof。
- 运维 CLI 仍有正式 runbook caller，不能直接删除入口；必须迁移核心后把 tool 留作薄 adapter，并用 source guard 防逻辑回流。
- Workbench 使用 active-generation 原子发布，不机械套普通 read-model gateway；proof 必须检查 active generation/all/member scopes 与 durable workbench/workbench_relation queue。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Unified Workbench page proof owner</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/workbench_page_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/operations_audit.py, backend/src/fin_ops_platform/services/page_audit_registry.py, tests/test_audit_workbench_relation_display_tool.py, tests/test_app_health_api.py</files>
  <action>迁移旧 tool checks 到 repository proof owner，复用 read-only repeatable-read snapshot、AuditIssue/evaluation、canonical/shared edge equality，并增加 workbench/workbench_relation dirty/outbox gates。注册有限 `workbench` executor，将 reconciliation-workbench 从 unavailable 改为 ready。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_workbench_relation_display_tool.py tests/test_app_health_api.py tests/test_page_audit_registry.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Legacy tool thin adapter 与删除 guard</name>
  <files>backend/src/fin_ops_platform/tools/audit_workbench_relation_display.py, tests/test_audit_workbench_relation_display_tool.py, tests/test_page_audit_registry.py</files>
  <action>tool 只保留 parser/connection/调用统一 proof/输出/exit code；删除其中 SQL、normalization、issue/evaluation 复制逻辑。whole-repo/source guard 证明核心只存在一个 owner。保留正式 CLI module path 兼容 runbook，不保留 fallback。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_workbench_relation_display_tool.py tests/test_page_audit_registry.py -q</verify>
</task>

<task type="auto">
  <name>Task 3: Page control、revision 与 docs</name>
  <files>web/src/pages/ReconciliationWorkbenchPage.tsx, web/src/test/WorkbenchSelection.test.tsx, backend/src/fin_ops_platform/services/page_audit_registry.py, docs/modules/reconciliation-workbench/boundary-io.md, docs/modules/workbench-relations/README.md, docs/operations/object-identity-dedup.md</files>
  <action>关联台标题接统一 Page Audit 控件；bump revision，runbook 改为同一 proof core 的 CLI/API 两入口。运行 Workbench/Audit/frontend/runtime guards、lint/docs/diff。</action>
  <verify>bash scripts/verify.sh frontend &amp;&amp; bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
</task>

</tasks>

<success_criteria>

- reconciliation-workbench page Audit 为 ready，统一 API/页面按钮/CLI 都调用同一 proof owner。
- active relation member 在 all 或成员 scope generation 缺失、拆组、多 owner、case/mode/alignment 错误、all generation 落后、visible automatic decision 污染均阻断。
- canonical/shared relation equality、generation display、dirty/outbox/snapshot 同一报告闭环。
- legacy tool 不再包含 SQL/业务 proof；没有第二 runtime implementation。

</success_criteria>
