---
phase: 19-audit-readiness
plan: 07
type: execute
wave: 7
depends_on:
  - 19-06
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-04
  - AUDIT-10
---

<objective>
证明批量账务页面作为 direct shared-relation consumer 的完整 case set 与业务身份：canonical relation 与页面实际读取的 shared groups 必须双向同集，且 relation_mode/source 同为 batch_accounting。
</objective>

<grill_gate>

- 页面没有独立 read model；不得新建第二 projection 或复制 relation fact。
- 现有 missing/orphan/key-field checks 分散覆盖部分条件，但没有显式证明 canonical relation_mode 必须是 batch_accounting。
- shared global edge equality 负责完整 member edges；本合同负责页面筛选后的 case set、mode 与 source identity。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Direct shared consumer case equality</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/page_consumer_relation_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, tests/test_audit_page_business_read_model_tool.py</files>
  <action>比较 active canonical batch-accounting case 与 linked shared group logical case union；检测 canonical wrong mode、missing consumer、consumer orphan、payload mode/source mismatch。成员 edge 继续复用全局 canonical/shared equality。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Revision/docs/regression</name>
  <files>backend/src/fin_ops_platform/services/page_audit_registry.py, docs/architecture/module-boundaries/read-model-contracts.md, docs/modules/batch-accounting/boundary-io.md, tests/test_app_health_api.py, web/src/test</files>
  <action>bump revision，记录 direct shared ownership，运行 Audit/API/runtime guards、lint/docs/diff。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py tests/test_page_audit_registry.py tests/test_platform_runtime_boundary_guards.py -q</verify>
</task>

</tasks>

<success_criteria>

- canonical case 缺 page group、page group 无 canonical case、mode/source 不一致均阻断。
- 不新增 read model、worker、refresh 或 relation 写路径。
- report 明确登记 direct shared consumer proof，而不是用“无独立表”跳过。

</success_criteria>
