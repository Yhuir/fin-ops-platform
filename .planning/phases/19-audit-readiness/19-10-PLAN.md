---
phase: 19-audit-readiness
plan: 10
type: execute
wave: 10
depends_on:
  - 19-09
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-10
---

<objective>
把关联台从“relation display 已证明”推进到“页面全部 canonical 对象已证明”：在现有 Workbench Audit snapshot 内证明每个月 active generation 对 eligible OA、银行流水、普通/附件发票与 ETC summary/detail 的 expected-set 双向相等，并独立重算关键展示字段、scope/generation counts 与 all-scope union；继续复用现有 relation display proof，不创建第二事实源或第二 Audit runtime。
</objective>

<grill_gate>

- 19-09 只以 active relation members 为 expected-set；未被 relation 占用的 open rows、被 OA pending claim 排除的 bank rows、ETC 折叠行、ignored/handled-exception 归属和普通 canonical rows 尚未证明。
- 成本统计从 Workbench active generation 读取 OA-bank cost entries；在 Workbench 全对象 expected-set 未闭环前，直接做 cost lineage 仍可能让 upstream/downstream 同源遗漏同时绿色。
- canonical owner 已由当前 projection SQL 明确：completed `app.oa_applications`/OA projection、active `app.bank_transactions`（扣除 active OA-pending claims，但 active relation supplement 例外）、eligible `app.invoices`（提交 ETC 隐藏/overlap 规则）、`app.etc_business_batches`/`app.etc_invoices`、active relation 与 override/exception facts。Audit 必须独立查询这些 facts，不调用 builder 生成 expected payload。
- Workbench active-generation 原子发布、row identity policy 与现有 object-identity 工具均保留；本计划只增加只读 proof，不迁移写路径、不新建 projection、不把 object-identity CLI 复制进页面 Audit。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Canonical Workbench object inventory equality</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/workbench_projection_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/workbench_page_audit.py, tests/test_audit_workbench_relation_display_tool.py</files>
  <action>新增单一只读 projection proof module，按 month/source-kind/row-id 构造 projection-independent expected inventory；与每个 active month generation 的 structured `workbench_rows`/`workbench_group_rows` 可见 owner 做双向 equality。显式覆盖 OA completed/attachment-parent、bank active/OA-pending claim exclusion/relation supplement、invoice ETC hidden/overlap、ETC summary/detail、ignored/handled-exception 与 display-only summary 不冒充 canonical member。缺失、多余、跨 scope、重复 owner 均 blocking。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_workbench_relation_display_tool.py tests/test_workbench_sql_runtime.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Independent critical-field and aggregate proof</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/workbench_projection_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/workbench_page_audit.py, tests/test_audit_workbench_relation_display_tool.py</files>
  <action>从 canonical facts 独立重算 row type/source kind/scope、OA applicant/project/amount/status、bank direction/amount/counterparty/account identity、invoice identity/type/date/amount/visibility、ETC batch membership，以及 generation row/group counts、summary counts 和 all-scope union。不得只比较同一 payload 的复制字段；无法由登记事实证明的字段必须从 guarantee 中移除或 fail closed。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_workbench_relation_display_tool.py tests/test_workbench_sql_runtime.py tests/test_audit_object_identity_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 3: Exact omission fixtures, contract truthfulness and docs</name>
  <files>backend/src/fin_ops_platform/services/page_audit_registry.py, tests/test_app_health_api.py, tests/test_page_audit_registry.py, docs/modules/reconciliation-workbench/boundary-io.md, docs/architecture/module-boundaries/read-model-contracts.md, .planning/phases/19-audit-readiness/19-INVENTORY.md</files>
  <action>增加 canonical OA/bank/invoice/ETC 任一对象缺失而 relation display 仍正常时必须失败的确定性 fixtures；更新 Workbench audit_contract 的 expected-set/field/summary claim 并 bump revision。只有这些 proof 全部组合后，registry ready 才表示页面全部 App-internal objects 已证明；外部银行/OA/发票证据仍单列 unknown。运行全量边界、lint/docs/frontend/diff。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_workbench_relation_display_tool.py tests/test_app_health_api.py tests/test_page_audit_registry.py tests/test_platform_runtime_boundary_guards.py -q &amp;&amp; bash scripts/verify.sh frontend &amp;&amp; bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
</task>

</tasks>

<success_criteria>

- relation-free open canonical row 缺失不能因 shared relation equality 正常而通过。
- canonical OA、bank、invoice、ETC eligibility 与每个 active month generation、all union 双向相等；display-only summaries 不计为 canonical member。
- key fields、generation counts 与 summary 独立重算；缺失/多余/重复/错误 scope/字段/汇总均 blocking。
- 19-09 relation display、dirty/outbox/snapshot proof 继续由同一个 `workbench_page_audit` 报告组合，没有第二 SQL runtime 或写 I/O。
- Workbench proof 成为成本统计 lineage 可以依赖的已证明 upstream，而不是相关投影假设。

</success_criteria>
