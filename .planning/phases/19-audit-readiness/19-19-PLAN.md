---
phase: 19-audit-readiness
plan: 19
type: execute
wave: 19
depends_on:
  - 19-18
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-03
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
  - AUDIT-09
  - AUDIT-10
---

<objective>
把完整 backend 的 13 个已分类 baseline failures 收敛到 0，同时保持 Phase 19 已建立的 canonical expected-set、active relation、freshness、权限和幂等合同不被放宽。12 个 stale test/docs/guard 必须改为当前正式事实；银行异常 legacy endpoint 的重复提交必须恢复稳定幂等重放，不因第一次写入改变 classifier scenario 而错误 409。
</objective>

<grill_gate>

- `month=all` Workbench query 当前正式合同是 composed active month shards，不是旧 materialized aggregate。测试必须断言 `WORKBENCH_ALL_SCOPE_COMPOSED_SCHEMA_VERSION`；禁止把实现退回旧 aggregate 以迎合断言。
- 成本统计只消费 paired active relation 和明确 cost-eligible open special group。candidate-only OA/bank 相似性不是正式配对，不能产生成本。7 个空结果来自 fixture 未创建 active relation；测试必须通过正式 Workbench command/API 建立 relation 后再 materialize cost read model，禁止恢复 candidate fallback 或直接伪造 cost payload。
- OA 待付款当前付款状态是 `未支付/已支付` 二态；`支付少了` 已从页面合同移除。deterministic evidence 和长期测试文档必须更新为 `未支付 -> 已支付`，不能把旧标签重新放回运行时。
- `cost-statistics/api.ts` 已有 tag-rules PUT，权限 inventory 不能继续宣称页面无 mutation。必须登记 mutating API、保存入口和 read-export gate，并增加/更新真实 browser/组件证据；不能只把文件名塞入映射而不承认写能力。
- input-invoice freshness owner 已从 route/all_rows 迁移为 `InputInvoiceUsageReadModelFreshGateService.filter_options/rows_by_invoice_ids`。architecture allowlist 只能在逐函数确认 schema/status/source-version gate 后更新，不能扩大通配、降低扫描或删除 guard。
- OA reset 只移除 OA 派生/人工状态，必须保留普通 canonical input/output invoice。Workbench 完整 expected-set 会显示保留的 `iv-o-202604-001`；测试应按 identity 找到 OA attachment row，不得用第一行位置或要求非 OA 发票消失。
- `update-bank-exception` 相同 row/action/relation code 的重复请求必须重用第一次的 scenario/idempotency identity并安全 replay；不同 relation code、不同 row set、pair relation conflict仍返回冲突。修复限于 legacy adapter/facade，不污染通用 exception service，不新增 fallback或第二套 case owner。
- 13 项修复后必须运行每组精确回归、权限 Chromium gate、完整 frontend/build、完整 backend（目标零失败）、lint/docs/diff。禁止 skip、xfail、降低断言、扩大 allowlist glob或删除失败测试。
</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Align version, OA evidence, permission inventory, and freshness ownership contracts</name>
  <files>tests/test_bank_auto_tag_rules_api.py, docs/dev/page-read-model-fact-display-matrix.json, docs/dev/testing-closure-state.md, docs/dev/testing.md, docs/dev/nightly-ci.md, docs/modules/permissions-and-audit/write-entry-inventory.md, docs/modules/permissions-and-audit/e2e-coverage.md, docs/modules/permissions-and-audit/tests.md, web/e2e/permissions-role-matrix.spec.ts, web/src/test/CostStatisticsPage.test.tsx, tests/test_permissions_write_entry_inventory.py, tests/test_read_model_architecture_guards.py</files>
  <action>把 all-scope builder expectation 更新为 composed shards；将 OA fan-out evidence 从旧“支付少了”改为“未支付→已支付”；登记成本统计标签规则 PUT、页面保存控件和 read-export 零 mutation证据；将 direct-fresh allowlist 精确迁移到两个当前 service owner，并保留逐函数理由。删除相应旧文案/旧 owner，不能增加宽松例外。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_bank_auto_tag_rules_api.py tests/test_page_read_model_fact_display_matrix.py tests/test_permissions_write_entry_inventory.py tests/test_read_model_architecture_guards.py -q &amp;&amp; cd web &amp;&amp; npm test -- --run src/test/CostStatisticsPage.test.tsx &amp;&amp; npx playwright test e2e/permissions-role-matrix.spec.ts --project=chromium</verify>
</task>

<task type="auto">
  <name>Task 2: Make cost API fixtures prove active relation lineage</name>
  <files>tests/test_cost_statistics_api.py, tests/app_test_support.py, docs/modules/cost-statistics/tests.md, docs/modules/cost-statistics/boundary-io.md</files>
  <action>用一个测试 helper 经正式 Workbench confirm command/API建立 OA↔bank active relation，再经当前 projection/read-model入口生成 cost month/all scopes。更新 7 个旧 candidate-dependent案例和 fallback detail-fields案例，使它们证明 active relation→Workbench→cost lineage、project scope、preview/export和字段重算；不得直接写 cost payload或让 candidate进入成本。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_cost_statistics_api.py tests/test_cost_statistics_service.py tests/test_audit_page_business_read_model_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 3: Preserve non-OA inventory and restore stable legacy exception replay</name>
  <files>backend/src/fin_ops_platform/services/workbench_write_facade.py, tests/test_workbench_write_characterization.py, tests/test_settings_data_reset_service.py, docs/modules/reconciliation-workbench/boundary-io.md, docs/modules/settings/tests.md</files>
  <action>OA reset regression改为证明普通 output invoice与重建的 OA attachment invoice共同保留，并按 id 定位 attachment字段。legacy exception adapter在调用 classifier preview前，若存在完全相同 row set、manual_review action和 legacy relation code的 active case，则复用该 case原 scenario code进入通用 idempotent apply；其他 active case仍冲突。补同码 replay、异码 conflict、relation conflict、schedule/persistence回归。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_settings_data_reset_service.py tests/test_workbench_write_characterization.py tests/test_workbench_exception_application_service.py -q</verify>
</task>

<task type="auto">
  <name>Task 4: Zero-failure local closure and next production gate classification</name>
  <files>.planning/phases/19-audit-readiness/19-INVENTORY.md, .planning/phases/19-audit-readiness/19-19-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>运行完整 backend直到零失败；运行完整 frontend、build、目标 Chromium、lint/docs/diff。记录七类测试和无放宽证据。完成后重新 Grill：若唯一剩余是外部证据/生产只读授权，则生成下一安全计划并停在明确授权 gate；不得自行部署、生产写入、refresh或repair。</action>
  <verify>PYTHONPATH=backend/src:. pytest -q &amp;&amp; cd web &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
</task>

<success_criteria>

- 完整 backend 从 13 failures 收敛为 0，且没有 skip/xfail/断言放宽或 guard删除。
- 成本统计测试只在正式 active relation存在后产生行；candidate-only仍被排除。
- Workbench all-scope、OA 二态、fresh gate owner和cost tag-rules权限 inventory与当前运行时一致。
- OA reset保留非 OA canonical invoice；attachment cache复用仍不重跑 OCR。
- 相同 legacy bank exception重复请求稳定 200/idempotent；异码/异行/active relation conflict仍 fail closed。
- 完整 frontend/build、权限 Chromium、lint/docs/diff通过；未触碰生产。

</success_criteria>
