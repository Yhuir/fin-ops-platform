---
phase: 19-audit-readiness
plan: 05
type: execute
wave: 5
depends_on:
  - 19-04
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-10
---

<objective>
证明银行明细页面实际展示的 relation consumer 合同：每条 active bank transaction 的 linked OA/发票布尔标签、linked case id 和 linked status 必须由 shared relation 唯一、完整地重算；候选关系不得混入已配对证明。
</objective>

<grill_gate>

- 页面没有持久化 OA/invoice member id 全摘要，不能声称页面 consumer edge DTO 完整；全量 edge 仍由 canonical/shared equality 证明。
- 页面真正消费的是 `oa_relation_tag`、`invoice_relation_tag`、`relation_case_id`、payload `relation_status`。
- active relation command 禁止 row overlap，但 Audit 不能依赖写路径假设；shared bank member 出现在多个 linked case 必须阻断。
- candidate tag 是候选展示，不是已配对关系；本计划只证明 linked relation，不把 candidate 当 orphan。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Bank-detail linked tag/case/status equality</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/page_consumer_relation_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, tests/test_audit_page_business_read_model_tool.py</files>
  <action>从 active bank facts + linked shared groups 重算 has_oa/has_invoice/case/status；与 fresh bank_detail structured columns/payload 双向比较，并检测一个 bank member 多 linked case。复用现有 consumer proof port，不创建新模块。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Revision、docs 与边界回归</name>
  <files>backend/src/fin_ops_platform/services/page_audit_registry.py, docs/architecture/module-boundaries/read-model-contracts.md, docs/modules/bank-details/boundary-io.md, web/src/test, tests/test_app_health_api.py</files>
  <action>bump contract revision，登记 bank-detail consumer proof；文案继续限制为已登记 App 内部合同。更新 docs/fixtures，运行 Audit/API/runtime guards、lint/docs/diff。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py tests/test_page_audit_registry.py tests/test_platform_runtime_boundary_guards.py -q</verify>
</task>

</tasks>

<success_criteria>

- shared linked relation 有 OA/发票而 bank-detail 标签遗漏时 Audit 失败。
- bank-detail 伪报 linked 标签/case 或 case 不一致时 Audit 失败。
- 一个 bank row 属于多个 linked cases 时 Audit 失败，不选择任意 first case 冒充完整。
- candidate-only 展示不被误判为正式 linked orphan。

</success_criteria>
