---
phase: 19-audit-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-05
  - AUDIT-09
  - AUDIT-10
---

<objective>
直接封堵“关联台/共享 relation 有配对，但进项或销项页面 consumer 漏配对”的绿色遗漏：在同一 Audit snapshot 中证明 canonical/shared relation 与 invoice page consumer summaries 双向相等；随后删除已无页面需要的 specialized HTTP/service/repository 双入口。
</objective>

<scope>

- 本计划覆盖 `input-invoice-usage` 和 `output-invoice-collections` 两个高风险 invoice consumer。
- shared relation 三边 equality 仍由现有 `workbench_relation_edge_equality_issues` 证明；invoice core 新增 shared → consumer 双向 equality，组合为 canonical → shared → consumer。
- consumer identity 使用 `relationCaseId + typed member id`；OA attachment invoice source row 通过现有 canonical invoice/source-link lookup 归一为 invoice id。
- 本计划不扩展到其它 7 个 ready 页面 consumer；它们由后续唯一计划逐组完成。
- 不写数据、不 refresh、不更改 relation 事实。

</scope>

<tasks>

<task type="auto">
  <name>Task 1: Invoice consumer relation edge 双向 equality</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/invoice_read_model_audit.py, tests/test_audit_input_invoice_usage_read_model_tool.py, tests/test_audit_output_invoice_collection_read_model_tool.py</files>
  <action>
    在现有 invoice audit core 增加一条只读 SQL check：从 linked `workbench_relation_groups` 形成该方向页面适用的 expected typed edges，从页面 row payload 的 `oa.summaries`、`bankTransactions.summaries`、`invoiceRelations.summaries` 形成 actual consumer edges；比较 expected→consumer missing 和 consumer→expected orphan。只比较带 linked `relationCaseId` 的正式 relation，不把 candidate/red-invoice 专用关系混入。不得创建第二 relation fact model。
  </action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_input_invoice_usage_read_model_tool.py tests/test_audit_output_invoice_collection_read_model_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Proof contract revision 与 pass 文案边界</name>
  <files>backend/src/fin_ops_platform/services/page_audit_registry.py, web/src/components/common/PageAuditIcon.tsx, web/src/features/appHealth/types.ts, tests/test_page_audit_registry.py, web/src/test/PageAuditIcon.test.tsx</files>
  <action>
    proof revision bump；invoice report 明确列出 `consumer_relation_edge_equality`。UI 仍只显示受限的“已登记证明一致”，因为其余 consumer 尚未完成；不得提前升级成所有页面全量正确。
  </action>
  <verify>cd web && npm test -- --run PageAuditIcon.test.tsx InputInvoiceUsagePage.test.tsx OutputInvoiceCollectionsPage.test.tsx</verify>
</task>

<task type="auto">
  <name>Task 3: 删除 specialized HTTP runtime 双入口</name>
  <files>backend/src/fin_ops_platform/app/server.py, backend/src/fin_ops_platform/services/operations_audit_service.py, backend/src/fin_ops_platform/services/postgres_repositories/operations_audit.py, web/src/features/appHealth/api.ts, web/src/pages/AppHealthOperationsPage.tsx, tests/test_app_health_api.py, tests/test_operations_audit_service.py, web/src/test/AppHealthOperationsPage.test.tsx, web/src/test/apiMock.ts</files>
  <action>
    App Health 面板改用统一 `fetchPageAudit('input-invoice-usage')`；删除两个 specialized GET route、handler、service/repository public methods 和 frontend clients/mocks。保留 invoice proof core 与 CLI thin adapters，因它们仍是统一 repository executor/正式只读 CLI 的实现，不是并行 HTTP runtime。whole-repo literal guard 证明旧 HTTP 路径归零。
  </action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_app_health_api.py tests/test_operations_audit_service.py tests/test_page_audit_registry.py -q</verify>
</task>

<task type="auto">
  <name>Task 4: Docs、全量 frontend 与 architecture guards</name>
  <files>docs/dev/api-contracts.md, docs/operations/monitoring.md, docs/modules/app-health-operations/boundary-io.md, docs/architecture/module-boundaries/read-model-contracts.md, .planning/phases/19-audit-readiness/19-INVENTORY.md</files>
  <action>
    删除旧 HTTP 合同，记录 invoice consumer proof identity/边界。运行 frontend、lint、docs 和 relevant architecture guards；已知无关基线失败必须继续单列，不得修改断言掩盖。
  </action>
  <verify>bash scripts/verify.sh frontend && bash scripts/verify.sh lint && bash scripts/verify.sh docs && git diff --check</verify>
</task>

</tasks>

<threat_model>

- 只比较 relation count 不足；必须比较 typed member edge 集合双向相等。
- 只比较 consumer union 而没有 case id 会把不同配对串在一起；identity 必须包含 relation case。
- 不能把 red-invoice、candidate 或 standalone fallback summary 当作 Workbench linked relation。
- 不能因为删除 HTTP route 而删除仍被统一 repository/CLI 使用的核心 proof SQL。

</threat_model>

<success_criteria>

- 任意 linked relation member 从 input/output consumer summaries 缺失时，页面 Audit integrity 失败并给出 mismatch kind/case/member。
- consumer 多出不存在于 shared linked relation 的 edge 同样失败。
- invoice Audit contract 明确 canonical/shared/consumer proof 链。
- whole repo runtime 不再包含两个 specialized HTTP endpoint/client；App Health 面板和页面均走统一 page-key API。

</success_criteria>
