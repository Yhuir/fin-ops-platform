---
phase: 19-audit-readiness
plan: 08
type: execute
wave: 8
depends_on:
  - 19-07
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-04
  - AUDIT-05
  - AUDIT-10
---

<objective>
让外部往来款页面具备可证明的 relation consumer identity：保留每个 ledger/flow row 已消费 relation 的 case/status/mode/source/typed members，并与 shared linked relation 做逐 ledger-row 双向 edge equality。
</objective>

<grill_gate>

- 当前 case-id union + member-id union 无法证明 case↔member pairing，必须提升 projection evidence shape。
- relation detail 已在 builder 内部生成后被删除；复用该现有结构，不新增表、worker 或事实源。
- candidate 可继续展示，但正式配对 proof 只比较 status linked/active。
- schema version 必须 bump，禁止旧 payload 被当 fresh。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Persist structured turnover relation summaries</name>
  <files>backend/src/fin_ops_platform/services/turnover_ledger_sql_projection.py, backend/src/fin_ops_platform/services/turnover_ledger_service.py, tests/test_turnover_ledger_read_model_refresh.py, tests/test_turnover_ledger_source_versions.py</files>
  <action>在现有 summary helper 输出 `workbench_relations`（case/status/mode/source/row_ids/row_types），保留现有 compact fields；bump turnover schema version，让 gateway/worker 重建旧 payload。不得保留内部临时 key 或新增敏感事实。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_turnover_ledger_read_model_refresh.py tests/test_turnover_ledger_source_versions.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Turnover per-row consumer edge equality</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/page_consumer_relation_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, tests/test_audit_page_business_read_model_tool.py</files>
  <action>对每个 turnover ledger row，以其 canonical bank_row_ids 选择 relevant linked shared groups，形成 ledger_id+case+typed-member expected edges；从 payload workbench_relations 形成 actual edges，检测双向 mismatch。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 3: Revision/docs/regression</name>
  <files>backend/src/fin_ops_platform/services/page_audit_registry.py, docs/architecture/module-boundaries/read-model-contracts.md, docs/modules/turnover-ledger/boundary-io.md, docs/modules/turnover-ledger/implementation-notes.md, tests/test_app_health_api.py, web/src/test</files>
  <action>bump Audit revision，记录 schema/rebuild gate；运行 turnover、Audit/API/runtime guards、lint/docs/diff。不得直接刷新生产。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_turnover_ledger_read_model_refresh.py tests/test_turnover_ledger_source_versions.py tests/test_audit_page_business_read_model_tool.py tests/test_platform_runtime_boundary_guards.py -q</verify>
</task>

</tasks>

<success_criteria>

- turnover payload 不再丢失 case↔typed-member 映射。
- 任一 ledger row 漏 shared linked relation edge 或多出 edge 时 Audit 失败。
- schema bump 使旧 payload 不可冒充当前 proof-ready projection。
- 无新 fact/read-model/worker/I/O 污染。

</success_criteria>
