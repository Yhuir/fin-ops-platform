---
phase: 19-audit-readiness
plan: 11
type: execute
wave: 11
depends_on:
  - 19-10
autonomous: true
requirements:
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-10
---

<objective>
关闭成本统计的相关遗漏与输入 lineage 风险：在成本页面同一只读 snapshot 内复用已完成的 Workbench/bank-detail upstream proof，把全银行支出 expected-set 改为 canonical bank facts，证明 cost rows/summary/标签/项目费用上下文及 month-parent source versions 与当前 upstream generation 精确一致。
</objective>

<grill_gate>

- 当前 `expected_cost` 读取 Workbench active generation，`expected_bank_flow` 读取 `bank_detail_rows`；若 upstream 与 cost 同时漏同一对象，现有 cost equality 可绿色。
- 19-10 已提供可复用的 Workbench canonical object/field/version proof；不应在 cost module 复制 Workbench SQL，也不需要新 lineage 表。
- 全银行支出 identity/amount 可直接从 `app.bank_transactions` canonical facts构造；标签只能由已登记 bank-detail projection 提供，但必须同时复用 bank-detail canonical/field proof并绑定其 source_versions。
- cost month payload 已保存 `workbench_source_versions` 与 `bank_detail_source_versions`；正确修复是审计这些现有字段与当前 upstream 相等，不增加新的版本 envelope。

</grill_gate>

<tasks>

<task type="auto">
  <name>Task 1: Same-snapshot upstream dependency proof</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/workbench_page_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/workbench_projection_audit.py, backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, tests/test_audit_page_business_read_model_tool.py</files>
  <action>把 Workbench projection/display issue collection 提取为可复用的 snapshot helper；cost_statistics audit 在自己的 snapshot 内组合该 helper以及 bank-details canonical expected-set/key-field/source-version proof。不得嵌套 transaction、调用 HTTP/CLI 或复制 SQL。增加 Workbench 与 cost 同时漏一条普通 bank row 时 cost 仍失败的反例。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py tests/test_audit_workbench_relation_display_tool.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Canonical cost set and critical fields</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, tests/test_audit_page_business_read_model_tool.py, tests/test_cost_statistics_sql_runtime.py</files>
  <action>全银行支出 expected-set 从 active `app.bank_transactions` 独立构造 identity/月份/金额；cost OA-bank entries逐行比较已证明 Workbench group 的 transaction/group/project/expense/content/applicant/amount/tag fields。双向 missing/orphan、重复 transaction、错误 group/context/tag/summary 均 blocking；不以 row_count 代替集合相等。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py tests/test_cost_statistics_sql_runtime.py tests/test_cost_statistics_service.py -q</verify>
</task>

<task type="auto">
  <name>Task 3: Exact input version and parent-shard binding</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/page_business_audit.py, backend/src/fin_ops_platform/services/page_audit_registry.py, tests/test_audit_page_business_read_model_tool.py, tests/test_app_health_api.py, docs/modules/cost-statistics/boundary-io.md, docs/architecture/module-boundaries/read-model-contracts.md, .planning/phases/19-audit-readiness/19-INVENTORY.md</files>
  <action>逐月比较 cost model 保存的 Workbench/bank-detail source_versions 与当前 active generation/scope；parent `active:all`/`all:all` 的 source_shards 必须精确覆盖当前 month models 和版本。缺失、旧版、额外 shard 或 mixed generation 均 blocking。bump revision，运行真实 migrated PostgreSQL SQL、完整 backend/frontend/lint/docs/diff。</action>
  <verify>PYTHONPATH=backend/src:. pytest tests/test_audit_page_business_read_model_tool.py tests/test_cost_statistics_sql_runtime.py tests/test_app_health_api.py tests/test_page_audit_registry.py tests/test_platform_runtime_boundary_guards.py -q &amp;&amp; bash scripts/verify.sh frontend &amp;&amp; bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
</task>

</tasks>

<success_criteria>

- Workbench/bank-detail 与 cost 同时遗漏同一 canonical bank transaction 时，成本页面 Audit 必须失败。
- cost project/time/expense/bank views 的全部 row identity、关键字段和 summary 双向正确；bank flow set不再依赖 bank-detail row存在性推导完整性。
- 每个 month cost payload 精确绑定当前 Workbench active generation 与 bank-detail source versions；parent shard map精确绑定当前 month models。
- 所有 proof 在一个成本页面 repeatable-read read-only snapshot 内执行，没有第二事实源、第二 runtime 或写 I/O。

</success_criteria>
