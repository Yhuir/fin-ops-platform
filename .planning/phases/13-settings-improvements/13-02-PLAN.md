---
phase: 13-settings-improvements
plan: "02"
type: execute
wave: 1
depends_on:
  - "13-01"
files_modified:
  - backend/src/fin_ops_platform/app/routes_settings.py
  - backend/src/fin_ops_platform/app/http_adapter.py
  - backend/src/fin_ops_platform/app/auth.py
  - backend/src/fin_ops_platform/app/server.py
  - backend/src/fin_ops_platform/services/app_settings_service.py
  - backend/src/fin_ops_platform/services/access_control_service.py
  - backend/src/fin_ops_platform/services/oa_role_sync_service.py
  - deploy/oa/env/fin-ops.secrets.env.example
  - deploy/oa/README.md
  - backend/src/fin_ops_platform/services/pending_invoice_rules_application_service.py
  - backend/src/fin_ops_platform/services/state_store_protocol.py
  - backend/src/fin_ops_platform/services/state_store.py
  - backend/src/fin_ops_platform/services/postgres_state_store.py
  - backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py
  - scripts/verify.sh
  - tests/test_app_settings_service.py
  - tests/test_workbench_settings_sync_api.py
  - tests/test_session_api.py
  - tests/test_auth_guard.py
  - tests/test_http_adapter.py
  - tests/test_read_model_api_contract_harness.py
  - tests/test_oa_role_sync_service.py
  - tests/test_permissions_write_entry_inventory.py
  - tests/test_app_health_api.py
  - tests/test_app_postgres_mode.py
  - tests/test_bank_auto_tag_rules_api.py
  - tests/test_batch_accounting_api.py
  - tests/test_etc_backend.py
  - tests/test_etc_invoice_pdf_bundle_service.py
  - tests/test_oa_manual_import_api.py
  - tests/test_oa_pending_payment_api.py
  - tests/test_pending_invoice_api.py
  - tests/test_postgres_state_store_integration.py
  - tests/test_tax_offset_api.py
  - tests/test_turnover_ledger_api.py
  - tests/test_workbench_dirty_queue_wiring.py
  - tests/test_workbench_v2_api.py
autonomous: true
requirements:
  - PAGE-15
  - PAGE-04
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "普通 GET/POST /api/workbench/settings 不返回、不接受、不写 ACL；full_access 仍可保存普通设置且零 OA I/O。"
    - "只有 YNSYLP005 admin session 能 GET/PUT /api/workbench/settings/access-control；请求 DTO 不存在可写 admin tier 或 body actor。"
    - "ACL PUT在ACL专用critical-section内基于locked current完成stale/no-op、bounded OA target和同步DB commit；generic writer共享同一session advisory lock。"
    - "服务器生成且不可由客户端伪造的 request ID 贯穿 adapter→route→service→audit，并与 response X-Request-ID 完全相同。"
    - "每次权限判断最多读取一次 normalized ACL snapshot，任何 env/DB/dynamic 来源都不能产生非 YNSYLP005 admin。"
    - "只有 ACL 真变化调用现有 OA role sync；no-op 与 generic save 零 OA，OA/DB 失败遵守目标失败与补偿合同。"
    - "backend runtime 中旧 generic ACL、dynamic admin provider、auth clone 和 pending-invoice replay 路径已删除。"
  artifacts:
    - path: "backend/src/fin_ops_platform/app/routes_settings.py"
      provides: "严格 generic boundary 与 admin-only access-control GET/PUT HTTP mapping"
    - path: "backend/src/fin_ops_platform/app/http_adapter.py"
      provides: "可信 server-generated request ID 的内部转发与 response绑定"
    - path: "backend/src/fin_ops_platform/services/app_settings_service.py"
      provides: "ACL normalize/read/command orchestration、CAS/audit port 接入与 OA compensation"
    - path: "backend/src/fin_ops_platform/services/access_control_service.py"
      provides: "唯一 protected admin 与单 snapshot 权限决策"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py"
      provides: "Wave 0 CAS/audit/family-preserving writer 的生产接线"
  key_links:
    - from: "backend/src/fin_ops_platform/app/routes_settings.py"
      to: "backend/src/fin_ops_platform/services/app_settings_service.py"
      via: "admin resolver 后的 typed access-control command；actor/trace 只来自可信 session/request"
    - from: "backend/src/fin_ops_platform/app/http_adapter.py"
      to: "backend/src/fin_ops_platform/app/routes_settings.py"
      via: "adapter覆盖/注入内部request context；客户端X-Request-ID不能替换server ID"
    - from: "backend/src/fin_ops_platform/services/app_settings_service.py"
      to: "backend/src/fin_ops_platform/services/postgres_state_store.py"
      via: "dedicated ACL CAS+audit port 与 generic family-preserving save port"
    - from: "backend/src/fin_ops_platform/app/server.py"
      to: "backend/src/fin_ops_platform/services/access_control_service.py"
      via: "单一 get_access_control_snapshot provider"
    - from: "backend/src/fin_ops_platform/services/app_settings_service.py"
      to: "backend/src/fin_ops_platform/services/oa_role_sync_service.py"
      via: "仅 semantic ACL change 的 normalized target/previous snapshots"
---

<objective>
关闭 T0-01 的后端可信边界：把 ACL 从 generic settings 完全拆出，以唯一 admin-only GET/PUT、独立 CAS、同事务 audit、单 snapshot auth 和受控 OA 同步取代所有旧路径。

Purpose: 落实 D-01～D-14、D-16、D-18、D-19；安全修复必须位于后端授权、service 与 persistence 边界，而不是依赖前端隐藏。
Output: 新 ACL API 合同、AppSettingsService command/read model、单 snapshot AccessControlService wiring、OA no-op/补偿闭环，以及 backend legacy deletion。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-PATTERNS.md
@.planning/phases/13-settings-improvements/13-VALIDATION.md
@.planning/phases/13-settings-improvements/13-01-SUMMARY.md
@backend/src/fin_ops_platform/app/routes_settings.py
@backend/src/fin_ops_platform/app/http_adapter.py
@backend/src/fin_ops_platform/services/app_settings_service.py
@backend/src/fin_ops_platform/services/access_control_service.py
@backend/src/fin_ops_platform/services/oa_role_sync_service.py
@backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py
@tests/test_workbench_settings_sync_api.py
@tests/test_http_adapter.py
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 切断 generic ACL I/O 并实现唯一 admin-only ACL API</name>
  <files>backend/src/fin_ops_platform/app/http_adapter.py, backend/src/fin_ops_platform/app/routes_settings.py, backend/src/fin_ops_platform/services/app_settings_service.py, tests/test_http_adapter.py, tests/test_app_settings_service.py, tests/test_workbench_settings_sync_api.py, tests/test_session_api.py, tests/test_auth_guard.py, tests/test_read_model_api_contract_harness.py</files>
  <behavior>
    - GET /api/workbench/settings response 不含 access_control 或三组 legacy ACL arrays。
    - POST /api/workbench/settings 出现 access_control、allowed_usernames、readonly_export_usernames 或 admin_usernames 中任一 key 时返回 400 access_control_write_forbidden；省略 ACL 时不改变 ACL。
    - full_access 可继续保存普通 settings 并获得 200；read_export/denied 仍按原合同拒绝 mutation。
    - admin GET /api/workbench/settings/access-control 返回 version、只读 protected administrator 与 accounts；非 admin 返回 403。
    - admin PUT 仅接受 expected_version 与 accounts；actor 只取 session；非法字段/tier/username 为 400，stale version 为 409 access_control_version_conflict，响应携带 current_version。
    - adapter生成的request ID写入成功audit且等于response X-Request-ID；客户端伪造header/body trace不能替换它；failure/no-op/conflict没有success audit。
    - read-model API contract harness固定断言generic settings response schema与runtime-style fixtures均不存在`access_control`或三组legacy ACL keys。
  </behavior>
  <action>按 D-01～D-07、D-11 在同一TDD task先写并运行direct attack/API/session/request-id测试记录RED，再实现并要求正常gating command变绿。`SettingsApiRoutes.route` 增加 exact GET/PUT path并复用 `_resolve_admin_session`；DTO固定为 `accounts[{username, access_tier=full_access|read_export_only}]`，administrator固定YNSYLP005且不可写。generic POST对四类旧key明确400，不能silent ignore/fallback。移除 `AppSettingsService.update_settings` ACL参数、generic payload access_control与普通save OA sync；增加最小ACL read/update方法。修改 `WsgiHttpAdapter`：生成authoritative request ID后，通过可信内部request context或覆盖的internal-only header传给Application/route，并将同一ID写response；绝不能直接信任客户端inbound X-Request-ID或body trace。route只传server-derived actor/request_id，service不解析header/cookie。错误映射固定400/403/409/502/503；failure/no-op/conflict不写success audit。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_http_adapter tests.test_app_settings_service tests.test_workbench_settings_sync_api tests.test_session_api tests.test_auth_guard tests.test_read_model_api_contract_harness -v</automated>
  </verify>
  <acceptance_criteria>generic settings与ACL API shape完全分离；full_access普通save仍200；direct self-elevation/body actor/admin tier/protected admin编辑/重复空username/unknown fields/stale overwrite均被拒且state不变；成功durable audit request_id等于response X-Request-ID，spoof无法替换，targeted gating command exit 0。</acceptance_criteria>
  <done>可信 HTTP/service boundary 不再允许 ordinary mutation session 触及 ACL。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 ACL CAS/durable audit 与 OA no-op/补偿状态机</name>
  <files>backend/src/fin_ops_platform/services/app_settings_service.py, backend/src/fin_ops_platform/services/state_store_protocol.py, backend/src/fin_ops_platform/services/state_store.py, backend/src/fin_ops_platform/services/postgres_state_store.py, backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py, backend/src/fin_ops_platform/services/oa_role_sync_service.py, deploy/oa/env/fin-ops.secrets.env.example, deploy/oa/README.md, scripts/verify.sh, tests/test_app_settings_service.py, tests/test_workbench_settings_sync_api.py, tests/test_oa_role_sync_service.py, tests/test_postgres_state_store_integration.py</files>
  <behavior>
    - ACL semantic no-op 只读当前 snapshot，不 UPDATE、不 audit、不调用 OA，version 不变。
    - service进入Wave 0 ACL专用guard后只使用`locked_current`做normalize/no-op；stale在任何OA调用前返回409。
    - 真变化在同一guard内调用带显式timeout的OA target，再调用`guard.commit(next_acl, durable_audit)`；commit返回前settings+audit DB commit已完成。
    - generic writer共享固定session advisory lock，锁内读取最新row并只合并非ACL family；ACL commit后保存普通setting仍保留最新ACL/version且零OA。
    - OA target失败时零DB write/audit并返回502；OA成功但DB write/audit/commit失败时，service在session advisory lock仍持有时同步补偿到`locked_current` previous，再退出guard。
    - compensation 失败返回 access_control_sync_inconsistent，critical metadata 只有 actor/version/error category/trace，无 secret。
    - 确定性双并发测试证明A成功后B以旧version在OA前stale；DB失败补偿期间后续writer不能越过共享lock提交，故补偿不会覆盖后续committed OA；所有路径finally unlock且最终settings/audit/OA一致。
    - known pre-commit/rollback失败在原锁内补偿previous；COMMIT outcome unknown绝不盲补偿，而以fresh connection重取同一lock并用mutation_id+canonical version/ACL证明结果。
    - unknown恢复判定固定：audit mutation_id存在且canonical=new target为commit proven，幂等保持/重apply target OA并success；audit缺失且canonical=previous且无later mutation为rollback proven，锁内补偿previous并failure；later/mismatch/indeterminate不写OA并返回access_control_sync_inconsistent。
    - `OARoleSyncSettings`增加正整数read/write timeout；env为`FIN_OPS_OA_ROLE_SYNC_READ_TIMEOUT_SECONDS=10`、`FIN_OPS_OA_ROLE_SYNC_WRITE_TIMEOUT_SECONDS=10`，connect timeout仍默认5，并传入PyMySQL connect参数；禁止detached thread timeout。
    - `scripts/verify.sh settings-acl-postgres`是本安全路径的fail-closed专用gate：缺少visibly disposable `FIN_OPS_TEST_DATABASE_URL`时exit 2，设置`FIN_OPS_REQUIRE_SETTINGS_ACL_POSTGRES=1`运行精确lost-ACK测试，任何Skip均失败；普通全仓integration保持既有opt-in语义。
  </behavior>
  <action>接入专用guard并为每个command生成不可复用mutation_id写同事务audit。known pre-commit/known rollback时原lock仍持有，补偿locked previous。若COMMIT期间connection loss/timeout，标记outcome unknown且绝不使用旧connection/盲补偿；fresh connection取得同一advisory lock，repository返回mutation audit与canonical ACL/version事实，由service执行三分支：mutation存在+canonical target/new version=>commit proven，幂等保持或重apply target OA后success；mutation缺失+canonical previous version/ACL+无later mutation=>rollback proven，fresh lock内补偿previous后返回failure；later version、ACL mismatch或任一证据不完整=>零OA覆盖、`access_control_sync_inconsistent`并人工reconcile。真实PG integration fault在server完成COMMIT后由client合成lost ack，并覆盖fresh-lock reacquire、intervening writer和mutation proof。为禁止`SkipTest`伪绿，在现有`scripts/verify.sh`增加唯一专用target `settings-acl-postgres`：先要求非空且通过现有disposable-database校验的`FIN_OPS_TEST_DATABASE_URL`，缺失exit 2；设置`FIN_OPS_REQUIRE_SETTINGS_ACL_POSTGRES=1`后只运行`tests.test_postgres_state_store_integration.PostgresStateStoreIntegrationTests.test_settings_acl_commit_lost_ack_reconciles_under_fresh_lock`，若skip或未执行必须nonzero。普通`verify.sh all`不因此强制所有历史integration连接。另扩展现有`OARoleSyncSettings`/`MySQLOARoleSyncExecutor`：解析正整数read/write timeout env（默认各10秒，connect仍5），直接传`pymysql.connect(read_timeout=..., write_timeout=...)`；不使用detached thread。secrets example与README登记两个env和错误语义。tests覆盖connect/read/write timeout mapping、target timeout零DB、compensation timeout=>inconsistent、guard/recovery unlock。repository只返回事实，service决定OA；无新表/outbox/worker/cache/framework。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_app_settings_service tests.test_workbench_settings_sync_api tests.test_oa_role_sync_service tests.test_postgres_state_store tests.test_postgres_repositories_boundaries -v &amp;&amp; bash scripts/verify.sh settings-acl-postgres</automated>
  </verify>
  <acceptance_criteria>unit tests全绿；`settings-acl-postgres`在无disposable DB或精确测试skip时nonzero，并证明known rollback、server commit后lost ack、fresh-lock reacquire、commit/rollback proof、intervening writer=>inconsistent、generic隔离和最终unlock；OA connect/read/write timeout及target/compensation映射全绿；无新表/outbox/worker/cache/general UoW。</acceptance_criteria>
  <done>ACL command 的本地 canonical、audit 与 OA external effect 具备明确成功/失败/补偿合同。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 收敛单 snapshot 鉴权并删除 backend legacy ACL runtime paths</name>
  <files>backend/src/fin_ops_platform/services/access_control_service.py, backend/src/fin_ops_platform/app/auth.py, backend/src/fin_ops_platform/app/server.py, backend/src/fin_ops_platform/services/app_settings_service.py, backend/src/fin_ops_platform/services/pending_invoice_rules_application_service.py, tests/test_session_api.py, tests/test_auth_guard.py, tests/test_permissions_write_entry_inventory.py, tests/test_read_model_api_contract_harness.py, tests/test_app_health_api.py, tests/test_app_postgres_mode.py, tests/test_bank_auto_tag_rules_api.py, tests/test_batch_accounting_api.py, tests/test_etc_backend.py, tests/test_etc_invoice_pdf_bundle_service.py, tests/test_oa_manual_import_api.py, tests/test_oa_pending_payment_api.py, tests/test_pending_invoice_api.py, tests/test_postgres_state_store_integration.py, tests/test_tax_offset_api.py, tests/test_turnover_ledger_api.py, tests/test_workbench_dirty_queue_wiring.py, tests/test_workbench_v2_api.py</files>
  <behavior>
    - YNSYLP005 在单 snapshot provider 故障时仍是唯一 protected admin；任何 env/static/dynamic/DB 来源都不能产生第二 admin。
    - 每次 evaluate 最多调用一次 get_access_control_snapshot；full/read/denied non-admin admission characterization 不发生无关漂移。
    - auth local dev/test synthetic session 不复制 retired allowed/readonly/admin providers 或 admin list。
    - server 不再 wiring 三组 provider；dynamic_admin_usernames_provider、get_admin_usernames 与 FIN_OPS_ADMIN_USERNAMES runtime authority 消失。
    - pending-invoice gateway 不再通过 getattr generic fallback 读取/重放 ACL；正式专属规则方法仍工作。
  </behavior>
  <action>按 D-01～D-03、D-07、D-13、D-16 把 AccessControlService三次provider union改为一次normalized snapshot；protected administrator显式唯一，provider异常对非protected admin fail closed。删除runtime/env admin authority、dynamic_admin provider、三个username getters、server三provider wiring和auth clone。保留non-admin admission characterization，不全面重定义required_permission/allowed roles。删除pending-invoice getattr+generic replay。对B-06列出的全部active tests/fixtures和`test_read_model_api_contract_harness.py`做whole-repo迁移：管理员场景改用固定YNSYLP005 identity；需要配置其他full/read账号的测试改用dedicated ACL test command/helper；删除 `FIN_OPS_ADMIN_USERNAMES`、dynamic_admin injection和 `update_settings(...admin_usernames/allowed_usernames/readonly...)` seeding，禁止以兼容签名、legacy fallback或测试专用runtime bypass保绿。whole-repo sentinel必须覆盖函数签名、response shape、fixture key、API contract harness及runtime serializer/parser；legacy strings仅允许rejection/migration/rollback-defense fixtures。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_session_api tests.test_auth_guard tests.test_permissions_write_entry_inventory tests.test_read_model_api_contract_harness tests.test_app_settings_service tests.test_app_health_api tests.test_app_postgres_mode tests.test_bank_auto_tag_rules_api tests.test_batch_accounting_api tests.test_etc_backend tests.test_etc_invoice_pdf_bundle_service tests.test_oa_manual_import_api tests.test_oa_pending_payment_api tests.test_pending_invoice_api tests.test_postgres_state_store_integration tests.test_tax_offset_api tests.test_turnover_ledger_api tests.test_workbench_dirty_queue_wiring tests.test_workbench_v2_api -v &amp;&amp; bash scripts/verify.sh lint</automated>
  </verify>
  <acceptance_criteria>权限判断一次 snapshot；唯一 admin 不依赖 HTTP/DB可写字段；runtime 无 dynamic admin/三 provider/auth clone/pending replay；AppHealth、OA credentials 与 data reset admin guards 的 session 回归保持通过。</acceptance_criteria>
  <done>后端旧提权链与重复 ACL authority 已从 runtime 删除。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser/API client → SettingsApiRoutes | 不可信 session、ACL keys、accounts、expected_version、actor 和 trace headers。 |
| SettingsApiRoutes → AppSettingsService | route 只传 validated DTO 与 server-derived actor，不得跨越 SQL/权限职责。 |
| AppSettingsService → PostgreSQL | canonical ACL、CAS 与 durable audit 原子边界。 |
| AppSettingsService → OA MySQL | 非原子外部角色副作用，需要 target/compensation 状态机。 |
| ACL snapshot → AccessControlService | 动态业务权限事实进入 session capability 决策。 |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Risk | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T13-01 | HIGH | Elevation of Privilege | generic POST / dedicated PUT / AccessControlService | mitigate | ASVS V4：admin resolver、strict DTO、唯一 protected admin、direct attack tests、state unchanged。 |
| T13-02 | HIGH | Tampering | expected_version 与 concurrent settings writers | mitigate | ASVS V5：ACL/generic共享固定session advisory lock；guard内stale/no-op/OA target/DB commit；仅known rollback原锁内补偿，unknown COMMIT走fresh-lock mutation proof且不盲补偿。 |
| T13-03 | HIGH | Repudiation | ACL success/failure audit | mitigate | ASVS V7：adapter生成request ID与response/audit绑定、session actor、secret-safe summary、settings+audit同事务、失败无success audit。 |
| T13-20 | HIGH | Spoofing | inbound X-Request-ID/body trace | mitigate | adapter覆盖客户端值并只转发server-generated ID；http adapter与audit=response tests。 |
| T13-04 | HIGH | Tampering | OA role sync partial failure | mitigate | OA target transaction、canonical commit gate、previous snapshot compensation、inconsistent explicit error/runbook signal。 |
| T13-05 | HIGH | Elevation of Privilege | env/dynamic admin/provider clone | mitigate | 删除 runtime env/dynamic admin authority与 auth clone；每次仅一份 normalized snapshot，非 protected admin 不可得到 admin。 |
| T13-08 | MEDIUM | Denial of Service | ACL provider/OA/DB failure | mitigate | 非 protected fail closed、connect/read/write显式timeout、typed 409/502/503，不以无限重试、detached thread或隐藏 fallback 掩盖失败。 |
| T13-09 | MEDIUM | Information Disclosure | error/audit/log metadata | mitigate | 不回显 token/password/full body；错误只含稳定 code/message/current_version，log 只含 actor/version/category/trace。 |
</threat_model>

<verification>
1. 每个 task 先运行 targeted unittest，再运行 `bash scripts/verify.sh lint`。
2. Wave 1 组合门禁：`PYTHONPATH=backend/src python3 -m unittest tests.test_app_settings_service tests.test_workbench_settings_sync_api tests.test_session_api tests.test_auth_guard tests.test_http_adapter tests.test_read_model_api_contract_harness tests.test_oa_role_sync_service tests.test_postgres_state_store tests.test_postgres_repositories_boundaries tests.test_permissions_write_entry_inventory -v &amp;&amp; bash scripts/verify.sh settings-acl-postgres`；后一个gate缺disposable DB或精确lost-ACK测试skip时必须nonzero。
3. 运行 backend whole-repo legacy sentinel scan；`dynamic_admin_usernames_provider`、`get_admin_usernames`、generic route parse/response ACL、runtime fixture/API contract harness ACL keys、pending getattr replay 和 runtime `FIN_OPS_ADMIN_USERNAMES` 不得命中；仅拒绝/迁移/rollback-defense fixture白名单可命中。
4. B-06 targeted suites全部exit 0后运行 `bash scripts/verify.sh backend`；不得通过保留legacy signatures/providers/fallback减少迁移范围。
</verification>

<success_criteria>
- full_access 无法通过 generic 或 dedicated API 提权，但普通 settings 保存仍为 200。
- admin ACL GET/PUT的DTO、403/400/409/502/503稳定；audit request ID等于response X-Request-ID且不可客户端spoof。
- CAS、durable audit、generic preservation、OA no-op/compensation及一次 snapshot 全有自动化证明。
- backend runtime 旧 ACL write/read/provider/replay path 删除，无隐藏 fallback/双写。
- 未引入表、worker、outbox、dirty scope、cache、service framework或新依赖；其它页面/read-model/worker I/O 不变。
</success_criteria>

## Artifacts this phase produces

- admin-only `GET/PUT /api/workbench/settings/access-control` 与严格 generic settings boundary。
- `AppSettingsService` ACL read/command orchestration及 Wave 0 CAS/audit ports 的生产接线。
- 单 snapshot `AccessControlService`、唯一 protected admin 和更新后的 auth/server wiring。
- OA real-change/no-op/target failure/compensation contracts。
- backend legacy ACL runtime deletion与 focused backend regression suite。
- `.planning/phases/13-settings-improvements/13-02-SUMMARY.md`。

<output>
Create `.planning/phases/13-settings-improvements/13-02-SUMMARY.md` when done.
</output>
