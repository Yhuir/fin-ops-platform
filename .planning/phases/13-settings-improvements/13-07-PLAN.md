---
phase: 13-settings-improvements
plan: "07"
type: execute
wave: 4
depends_on:
  - "13-06"
files_modified:
  - backend/src/fin_ops_platform/services/state_store_protocol.py
  - backend/src/fin_ops_platform/services/app_settings_service.py
  - backend/src/fin_ops_platform/services/oa_role_sync_service.py
  - backend/src/fin_ops_platform/services/access_control_service.py
  - backend/src/fin_ops_platform/app/auth.py
  - backend/src/fin_ops_platform/app/server.py
  - tests/app_test_support.py
  - tests/test_state_store_contract.py
  - tests/test_app_settings_service.py
  - tests/test_oa_role_sync_service.py
  - tests/test_session_api.py
  - tests/test_auth_guard.py
  - tests/test_permissions_write_entry_inventory.py
autonomous: true
requirements:
  - PAGE-15
  - PAGE-04
  - PAGE-05
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "D-20/D-21：除固定 YNSYLP005 外，APP tier只由一次 canonical ACL snapshot决定；缺席即denied。"
    - "OA permissions/roles及FIN_OPS_ALLOWED_USERNAMES/FIN_OPS_ALLOWED_ROLES/FIN_OPS_READONLY_EXPORT_USERNAMES不再授予APP；provider失败对非admin fail closed；固定finops:app:view只可定位OA menu。"
    - "D-26：同一cached OA identity下一次session/direct API仍重读ACL并立即撤权；鉴权最多一次snapshot、零OA role lookup。"
    - "13-06锁定的comparison key只在既有settings_access_control_from_payload规范化边界实现一次，并由DTO/evaluator/OA assignments共用。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/access_control_service.py"
      provides: "fixed 005 + canonical ACL-only evaluator"
    - path: "backend/src/fin_ops_platform/services/state_store_protocol.py"
      provides: "唯一共享username comparison/canonical spelling boundary"
  key_links:
    - from: "OAIdentityService.resolve_identity"
      to: "AccessControlService.evaluate"
      via: "username only; roles/permissions informational"
    - from: "AccessControlService.evaluate"
      to: "settings_access_control_from_payload"
      via: "one provider call per evaluation"
---

<objective>
从根因处把 APP authorization 收敛为固定005加一次Settings ACL snapshot，并删除全部 permission/role/env admission branch。

Purpose: 落实 D-20、D-21、D-22、D-26、D-27；保留13-01..03已完成的专用ACL API/CAS/audit/Settings唯一UI，不重做它们。
Output: 共享username boundary、ACL-only evaluator、最小auth/server wiring和显式ACL测试seed。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-02-SUMMARY.md
@.planning/phases/13-settings-improvements/13-03-SUMMARY.md
@.planning/phases/13-settings-improvements/13-06-SUMMARY.md
@docs/modules/settings/boundary-io.md
@docs/modules/permissions-and-audit/boundary-io.md
@docs/modules/oa-integration/boundary-io.md
@backend/src/fin_ops_platform/services/access_control_service.py
@backend/src/fin_ops_platform/services/state_store_protocol.py
@backend/src/fin_ops_platform/app/auth.py
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在既有snapshot normalizer内统一username contract</name>
  <files>backend/src/fin_ops_platform/services/state_store_protocol.py, backend/src/fin_ops_platform/services/app_settings_service.py, backend/src/fin_ops_platform/services/oa_role_sync_service.py, tests/test_state_store_contract.py, tests/test_app_settings_service.py, tests/test_oa_role_sync_service.py</files>
  <behavior>
    - comparison key严格采用13-06结果；canonical spelling保留OA sys_user.user_name。
    - empty/control-character/case-equivalent duplicate/cross-tier overlap/YNSYLP005普通account均在OA I/O前失败。
    - settings DTO、snapshot full/read lists、evaluator membership与OA assignments不各自normalize。
  </behavior>
  <action>按 D-20～D-23 把13-06锁定规则实现到现有`settings_access_control_from_payload`及其最窄同文件helper；`AppSettingsService` validation/public payload与`OARoleSyncService.build_assignments`调用该边界，不复制trim/case/dedupe。保留canonical OA spelling，comparison key仅用于相等/去重。禁止新增normalizer class、配置开关、表或第二payload；检测碰撞/跨tier重复必须在OA/PG写前fail closed。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_state_store_contract tests.test_app_settings_service tests.test_oa_role_sync_service -v</automated></verify>
  <acceptance_criteria>exact/case contract、empty/control/duplicate/overlap/protected-admin矩阵通过；三个consumer得到相同membership与canonical spelling。</acceptance_criteria>
  <done>用户名规范化只有一个共享owner。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 删除APP permission/role/env authority并重接单snapshot evaluator</name>
  <files>backend/src/fin_ops_platform/services/access_control_service.py, backend/src/fin_ops_platform/app/auth.py, backend/src/fin_ops_platform/app/server.py, tests/app_test_support.py, tests/test_session_api.py, tests/test_auth_guard.py, tests/test_permissions_write_entry_inventory.py</files>
  <behavior>
    - YNSYLP005在provider前固定admin；其它identity只按一次snapshot full/read membership决定tier，缺席denied。
    - permission-only（含finops:app:view）、role-only、allowed-env-only、readonly-env-only及provider-error identity全部denied；roles/permissions仍可原样出现在session信息字段。
    - 同一identity cache hit在ACL删除后下一次session/direct API立即denied；provider调用计数恰为一次、OA role lookup为零。
    - default/local test identity必须由tests/app_test_support.py显式seed canonical ACL，不存在production evaluator test bypass。
  </behavior>
  <action>按 D-20、D-21、D-26、D-27 从`AccessControlService`、`from_environment`、auth clone与APP evaluator删除`_parse_csv_environment`、`required_permission`、`allowed_usernames`、`allowed_roles`、`readonly_export_usernames` fields和所有permission/role/env admission branches；`from_environment`只接受snapshot provider。`FIN_OPS_OA_REQUIRED_PERMISSION=finops:app:view`保留在OA integration/deploy边界作为固定menu selector，但不得传入、克隆或读取进APP evaluator。provider缺失/异常时005仍admin，其余denied并保留secret-safe warning。auth.py只构造identity，server只注入snapshot provider。更新shared test helper显式经现有dedicated ACL command seed非admin身份，禁止特殊环境变量或 evaluator bypass。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_session_api tests.test_auth_guard tests.test_permissions_write_entry_inventory -v</automated></verify>
  <acceptance_criteria>005/full/read/denied、permission/role/env/provider-error、single-snapshot、identity-cache-immediate-revoke和direct API矩阵通过；旧constructor/env authority无法实例化或生效。</acceptance_criteria>
  <done>Settings ACL成为唯一APP授权事实源。</done>
</task>

</tasks>

<scope_note>
R1-W01 accepted intentionally: 13 files remain in one two-task plan because Task1 defines the single username comparison/canonical-spelling contract and Task2 rewires its only APP evaluator consumer plus existing auth/server test seams. Splitting would duplicate or temporarily fork that security contract. Each task has its own targeted command; any caller expansion beyond these listed files requires replanning rather than opportunistic scope growth.
</scope_note>

<docs_impact>
改变permissions/settings/OA/app-shell的授权I/O与旧代码删除条件；13-04 Task1在13-09后更新对应长期文档。本plan不新增read model/worker/cache/outbox/table。
</docs_impact>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| OA identity → evaluator | roles/permissions是不可信授权提示，只允许username进入APP tier判断。 |
| Settings snapshot provider → evaluator | provider可能失败或返回碰撞/非法payload。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T13-33 | Elevation of Privilege | permission/role/env union | mitigate | 删除APP evaluator全部admission分支/fields/env读取；OA-only selector精确allowlist；negative matrix。 |
| T13-34 | Elevation of Privilege | provider failure | mitigate | 005 before provider；所有非admin denied。 |
| T13-35 | Spoofing | username normalization divergence | mitigate | 13-06 evidence+one shared comparison boundary+collision reject。 |
</threat_model>

<verification>
运行两个targeted commands、`bash scripts/verify.sh lint`，并由13-09完成跨页面/前端/全仓sentinel。
</verification>

<success_criteria>
- 非005 tier只来自canonical ACL，一次snapshot、零OA role lookup。
- permission/role/env/provider failure都不能grant。
- cached identity不延迟ACL撤权。
- 无新framework/cache/table/worker/outbox。
</success_criteria>

<output>Create `.planning/phases/13-settings-improvements/13-07-SUMMARY.md`.</output>
