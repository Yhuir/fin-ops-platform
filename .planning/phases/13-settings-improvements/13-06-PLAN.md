---
phase: 13-settings-improvements
plan: "06"
type: execute
wave: 3
depends_on:
  - "13-03"
files_modified: []
autonomous: false
requirements:
  - PAGE-15
  - PAGE-05
  - PAR-01
  - PAR-03
must_haves:
  truths:
    - "D-20/D-21 的用户名比较键由目标 OA `sys_user.user_name` collation、实际查询行为和 fresh `/system/user/getInfo.userName` 证据决定，不由 planner 猜测。"
    - "YNSYLP005 与 YNSYLP006 使用 fresh、互异且未过期的 OA token；证据只保留 salted hash、collation、collision count 和 equality outcome，不保留 token、DSN、密码或用户名明文。"
    - "只有 exact-trim 或 casefold-preserve-canonical 两种结论可进入 13-07；大小写碰撞、API/DB identity mismatch、过期 token 或不完整证据一律 blocking。"
  artifacts:
    - path: "/opt/fin-ops/evidence/settings-acl-username-contract.json"
      provides: "root-owned 0600 target-OA username collation/equality/identity evidence"
    - path: ".planning/phases/13-settings-improvements/13-06-SUMMARY.md"
      provides: "后续实现唯一可引用的 comparison-key/canonical-spelling 决议与 artifact sha256"
  key_links:
    - from: "/system/user/getInfo.userName"
      to: "OA sys_user.user_name"
      via: "fresh 005/006 identity hashes + parameter-bound exact/native comparison"
    - from: "13-06-SUMMARY.md"
      to: "13-07-PLAN.md"
      via: "locked normalization outcome; mismatch/collision prevents dependency completion"
---

<objective>
在任何用户名规范化实现前，以只读目标 OA 证据锁定唯一比较合同。

Purpose: 落实 D-20、D-21、D-26 的“OA 只认证 username”边界，并关闭 RESEARCH A1；避免 DTO、ACL snapshot、evaluator 与 OA adapter 各自猜大小写规则。
Output: secret-safe username contract artifact/hash，以及只允许 13-07 消费的一项明确 normalization decision。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-03-SUMMARY.md
@docs/modules/permissions-and-audit/boundary-io.md
@docs/modules/oa-integration/boundary-io.md
@backend/src/fin_ops_platform/services/state_store_protocol.py
@backend/src/fin_ops_platform/services/oa_identity_service.py
@backend/src/fin_ops_platform/services/oa_role_sync_service.py
</read_first>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: 用 fresh 005/006 身份与目标 OA 只读查询采集用户名合同</name>
  <files>None — 仅生成 root-owned production evidence；不修改仓库、OA、PostgreSQL、服务或角色。</files>
  <action>授权 operator 在目标 OA 上以只读事务采集 `information_schema.columns` 中 `sys_user.user_name` 的 character set/collation；对 YNSYLP005/YNSYLP006 使用参数绑定分别执行 native equality、`BINARY` equality 与大小写变体查询；统计 trim/casefold 后映射到多个 binary username 的 collision count。使用两枚 fresh、互异 token 调 `/system/user/getInfo`，只比较返回 `userName` 与唯一 DB canonical row，禁止沿用旧 session payload、旧浏览器 DOM 或当前 APP tier。artifact固定schema字段为`contract_version`、`captured_at_utc`、`character_set`、`collation`、`native_case_sensitive`、`binary_exact_match`、`collision_count`、`api_db_identity_match`、`identities_fresh`、`identity_hashes`（恰好两枚不同salted sha256）与`decision_inputs_complete`；token、密码、DSN、完整用户清单、username/user_name明文与可逆salt不得落盘。文件必须root:root 0600并有独立sha256。所有SQL必须是SELECT/SHOW；不得INSERT/UPDATE/DELETE/ALTER、刷新OA session cache或修改ACL/角色/menu。</action>
  <verify>
    <automated>ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n test "$(sudo -n stat -c "%U:%G:%a" /opt/fin-ops/evidence/settings-acl-username-contract.json)" = root:root:600 &amp;&amp; sudo -n sha256sum --check /opt/fin-ops/evidence/settings-acl-username-contract.json.sha256 &amp;&amp; sudo -n python3 -c "import json,re; p=\"/opt/fin-ops/evidence/settings-acl-username-contract.json\"; d=json.load(open(p)); req={\"contract_version\",\"captured_at_utc\",\"character_set\",\"collation\",\"native_case_sensitive\",\"binary_exact_match\",\"collision_count\",\"api_db_identity_match\",\"identities_fresh\",\"identity_hashes\",\"decision_inputs_complete\"}; assert set(d)==req; assert d[\"contract_version\"]==\"settings-acl-identity-v1\"; assert d[\"collision_count\"]==0; assert d[\"api_db_identity_match\"] is True and d[\"identities_fresh\"] is True and d[\"decision_inputs_complete\"] is True; h=d[\"identity_hashes\"]; assert len(h)==2 and len(set(h))==2 and all(re.fullmatch(r\"[0-9a-f]{64}\",x) for x in h); raw=json.dumps(d).lower(); assert not any(x in raw for x in (\"token\",\"password\",\"dsn\",\"username\",\"user_name\",\"ynsylp005\",\"ynsylp006\"))"'</automated>
    <human-check>root operator 确认查询为只读、005/006 token fresh 且不同，artifact 已 redaction，并返回 artifact sha256。</human-check>
  </verify>
  <acceptance_criteria>artifact/hash有效；两身份唯一对应 OA canonical row；collation/equality/collision facts完整；无 secret/明文成员；任一 token 过期、identity mismatch 或 collision 都阻断。</acceptance_criteria>
  <done>目标 OA 的用户名事实已实测且未发生生产 mutation。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 机械选择并锁定共享 comparison key</name>
  <files>None — 决议写入 13-06-SUMMARY.md；不修改实现。</files>
  <action>按 artifact 机械决策：仅当 native equality 对大小写不敏感、005/006 各唯一命中、`/getInfo.userName` 可唯一回连 canonical row且全表 casefold collision count=0时，锁定 `casefold-preserve-canonical`；当 native equality为case-sensitive且API值逐字等于唯一DB row时，锁定 `exact-trim`。其它结果不得自创第三模式或配置开关，必须 blocking。SUMMARY记录算法名、canonical spelling owner=`sys_user.user_name`、artifact path/hash和执行时间；后续所有 DTO/snapshot/evaluator/OA adapter只能共用该决定。</action>
  <verify>
    <automated>EXPECTED=$(ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n python3 -c "import json; d=json.load(open(\"/opt/fin-ops/evidence/settings-acl-username-contract.json\")); print(\"casefold-preserve-canonical\" if d[\"native_case_sensitive\"] is False and d[\"collision_count\"]==0 and d[\"api_db_identity_match\"] is True else \"exact-trim\" if d[\"native_case_sensitive\"] is True and d[\"binary_exact_match\"] is True and d[\"collision_count\"]==0 and d[\"api_db_identity_match\"] is True else \"BLOCK\")"') &amp;&amp; test "$EXPECTED" != BLOCK &amp;&amp; rg -q "normalization: $EXPECTED" .planning/phases/13-settings-improvements/13-06-SUMMARY.md &amp;&amp; HASH=$(ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n sha256sum /opt/fin-ops/evidence/settings-acl-username-contract.json' | awk '{print $1}') &amp;&amp; rg -q "artifact_sha256: $HASH" .planning/phases/13-settings-improvements/13-06-SUMMARY.md</automated>
    <human-check>确认选择严格由 artifact facts 推导；没有猜测、fallback、运行时双模式或 username 明文泄露。</human-check>
  </verify>
  <acceptance_criteria>SUMMARY只记录 exact-trim 或 casefold-preserve-canonical；证据不充分则 plan 不完成且13-07不能开始。</acceptance_criteria>
  <done>用户名 normalization contract 已成为后续实现的 locked input。</done>
</task>

</tasks>

<docs_impact>
本 plan 不改长期文档；13-04 Task 1 在 13-09 完成后把已实测的 comparison-key、canonical spelling owner 与 collision fail-closed 合同写入 settings、permissions、OA integration 文档。
</docs_impact>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| OA token → `/getInfo` | token 可能过期或指向错误身份。 |
| OA MySQL → contract artifact | collation与username rows是外部事实，输出必须只读且脱敏。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T13-30 | Spoofing | stale/reused 005/006 token | mitigate | fresh双token、identity hash互异、API/DB唯一回连；失败blocking。 |
| T13-31 | Elevation of Privilege | case collision/guessed normalization | mitigate | native/BINARY/collision实测；只允许两种机械结论，无fallback。 |
| T13-32 | Information Disclosure | evidence artifact | mitigate | root-owned 0600、salted hashes、无token/DSN/password/member明文。 |
</threat_model>

<verification>
只读 artifact/hash与human-check均通过；没有 production DML、服务切换、ACL/role/menu变更。
</verification>

<success_criteria>
- OA username contract由实测证据而非假设确定。
- 13-07获得唯一 comparison-key/canonical-spelling input。
- collision、identity mismatch或过期token均fail closed。
</success_criteria>

<output>
Create `.planning/phases/13-settings-improvements/13-06-SUMMARY.md`; do not begin 13-07 without a passing artifact and explicit normalization outcome.
</output>
