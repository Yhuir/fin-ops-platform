---
phase: 13-settings-improvements
plan: "15"
type: execute
wave: 12
depends_on:
  - "13-14"
files_modified: []
autonomous: false
requirements: [PAGE-15, PAGE-04, PAGE-05, PAR-01, PAR-02, PAR-03]
must_haves:
  truths:
    - "Candidate upload、manual-root helper bootstrap与最终activation approval是连续但分离的明确授权边界。"
    - "Bootstrap不调用legacy self-update、不改变runtime-worker helper、active app、DB、OA或ACL。"
    - "Remote read-only preflight以fresh 005/006、fixed selector、exact cleanup/hash/fingerprints通过后才批准13-05 activation。"
  artifacts:
    - path: "/opt/fin-ops/evidence/<release>/settings-access-control-bootstrap.json"
      provides: "candidate/helper hashes and no-application/data/OA-change proof"
    - path: "/opt/fin-ops/evidence/<release>/settings-access-control-preflight.json"
      provides: "approved remote facts/hash bound to activation"
  key_links:
    - from: "13-14 candidate"
      to: "live deploy-control helper"
      via: "approved same-filesystem root-owned temp + hash/contract + atomic replace/restore"
    - from: "remote preflight artifact"
      to: "13-05 JIT activation"
      via: "explicit user approval and no-drift equality"
---

<objective>
连续完成candidate upload、一次性helper bootstrap、remote只读preflight与activation批准，不激活application。

Purpose: 保留原13-04 Tasks4-7的生产控制面连续性，并将其与release-prep实现分离。
Output: uploaded candidate、bootstrap artifact、approved remote preflight hash；activation仍由13-05执行。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>
<read_first>
@AGENTS.md
@.planning/phases/13-settings-improvements/13-14-SUMMARY.md
@deploy/oa/README.md
@scripts/deploy-oa.sh
@deploy/oa/bin/finops-deploy-control.sh
</read_first>
<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 批准candidate upload与manual-root helper bootstrap</name>
  <files>None — 审阅local/candidate evidence、hashes与精确runbook。</files>
  <action>展示13-14全绿证据、release/source/helper sha256、no-activate范围及same-filesystem root temp→prevalidate→atomic replace→postvalidate→atomic restore步骤。请求用户只批准candidate filesystem upload与一次性root helper替换；明确排除activation、service restart、migration、OA/ACL mutation。模糊回复不批准。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_settings_access_control_preflight tests.test_deploy_oa_script -v &amp;&amp; bash scripts/verify.sh lint</automated><human-check>用户明确批准candidate upload和manual-root bootstrap的exact hashes/runbook。</human-check></verify>
  <acceptance_criteria>local evidence全绿且用户明确批准限定范围；否则blocking。</acceptance_criteria>
  <done>只取得upload/bootstrap授权，不等于activation批准。</done>
</task>
<task type="auto">
  <name>Task 2: 上传并校验非激活candidate</name>
  <files>None — approved candidate filesystem mutation；不修改root helper或active app。</files>
  <action>执行`./scripts/deploy-oa.sh --no-activate --release-name &lt;release&gt;`上传/check candidate，记录source/migration/capability/helper hashes；SSH只读断言active release、API/service、DB/CHECK、OA/ACL与runtime-worker helper等于before。不得self-update、install helper或activate；不变量变化blocking。</action>
  <verify><automated>./scripts/deploy-oa.sh --no-activate --release-name &lt;release&gt; &amp;&amp; ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n /usr/local/sbin/finops-deploy-control check-release &lt;release&gt;'</automated></verify>
  <acceptance_criteria>candidate上传/check exit0；application/data/OA/root helper不变。</acceptance_criteria>
  <done>candidate可供bootstrap审阅但未激活。</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: root执行hash-pinned原子deploy-control bootstrap</name>
  <files>None — Task1批准的一次性root control-plane mutation。</files>
  <action>root按runbook把candidate helper复制到`/usr/local/sbin`同文件系统root-owned temp，核验approved sha256、`bash -n`和contract-version，保存0600 old backup，以`mv -f`原子替换。失败时从backup复制成root:root 0755 rollback temp，核验old hash/contract后原子恢复。记录before/after/rollback helper与runtime-worker helper hashes、active/API/DB/OA/ACL fingerprints；禁止legacy self-update、service restart、migration和OA/ACL写。</action>
  <verify><automated>ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n sha256sum --check /opt/fin-ops/evidence/&lt;release&gt;/settings-access-control-bootstrap.json.sha256 &amp;&amp; sudo -n /usr/local/sbin/finops-deploy-control contract-version --require settings-access-control-v1'</automated><human-check>root operator确认atomic replace/restore合同、runtime-worker helper和application/data/OA不变量。</human-check></verify>
  <acceptance_criteria>live helper hash/owner/mode/contract有效；不变量不变；失败已原子恢复并block。</acceptance_criteria>
  <done>candidate helper安全bootstrap，application尚未激活。</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 远端只读preflight并取得最终activation批准</name>
  <files>None — SSH只读取production facts并审阅artifact。</files>
  <action>用0600 wrapper把fresh 005/006 tokens经SSH stdin传给remote `settings-access-control-preflight &lt;release&gt; --http-tokens-stdin --dry-run --json`；独立SSH验sha256。展示username-contract hash、steady/cutover/blocked state与非敏感blockers、006 canonical ACL四集合缺席、DB三项全false待0132或全true（禁止partial）、legacy admin空/仅005、最多三个fixed retired env、fixed selector、unique menu/三role exact set、exact cleanup/rollback targets、candidate/active fingerprints和activation/rollback方案。取得第二次明确activation批准；preflight只读，不要求只有quiesced cutover后才能成立的strict稳态。</action>
  <verify><automated>./scripts/with-production-admin-token.sh sh -c 'printf "%s\n%s\n" "$FIN_OPS_HTTP_SLO_ADMIN_TOKEN" "$FIN_OPS_HTTP_SLO_BEARER_TOKEN" | ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod "sudo -n /usr/local/sbin/finops-deploy-control settings-access-control-preflight &lt;release&gt; --http-tokens-stdin --dry-run --json"' &amp;&amp; ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n sha256sum --check /opt/fin-ops/evidence/&lt;release&gt;/settings-access-control-preflight.json.sha256'</automated><human-check>用户明确批准当前remote artifact/hash、exact cleanup、fingerprints与activation窗口。</human-check></verify>
  <acceptance_criteria>eligible=true或精确cutover_eligible=true、blockers为空、hash/facts完整且用户明确批准；`oa.cleanup_eligible`只有cutover eligible且存在exact extra binding时为true；否则13-05不能开始。</acceptance_criteria>
  <done>最终activation批准绑定当前artifact/fingerprints。</done>
</task>
</tasks>

<scope_note>四步保持单plan是为维持candidate/bootstrap/preflight approval连续性；任何失败停止且不进入13-05。此plan不激活app、不迁移DB、不改OA/ACL。</scope_note>
<docs_impact>不修改仓库docs；严格执行13-13/14已写合同。</docs_impact>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| User approval → filesystem/root mutation | 两级批准必须绑定exact hash与范围。 |
| Candidate helper → live helper | bootstrap不能依赖legacy self-update。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T13-56 | Tampering | helper bootstrap | mitigate | same-filesystem temp/hash/contract/atomic restore。 |
| T13-57 | Repudiation | activation approval | mitigate | remote artifact sha + explicit second approval。 |
| T13-58 | Information Disclosure | fresh tokens | mitigate | 0600 wrapper + remote stdin + redacted artifact。 |
</threat_model>
<verification>四步顺序完成；candidate/bootstrap/preflight artifacts有效，active app/DB/OA/ACL未变化。</verification>
<success_criteria>
- Candidate和helper安全准备且production application未激活。
- Remote facts/hash与activation批准精确绑定。
- 任一failure/drift/模糊批准均阻止13-05。
</success_criteria>
<output>Create `.planning/phases/13-settings-improvements/13-15-SUMMARY.md`; only then may 13-05 begin.</output>
