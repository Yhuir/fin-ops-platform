---
phase: 13-settings-improvements
plan: "08"
type: execute
wave: 5
depends_on:
  - "13-07"
files_modified:
  - backend/src/fin_ops_platform/services/oa_role_sync_service.py
  - backend/src/fin_ops_platform/services/app_settings_service.py
  - backend/src/fin_ops_platform/app/routes_settings.py
  - tests/test_oa_role_sync_service.py
  - tests/test_app_settings_service.py
  - tests/test_workbench_settings_sync_api.py
autonomous: true
requirements:
  - PAGE-15
  - PAGE-04
  - PAGE-05
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "D-23/D-24：固定finops:app:view只定位唯一OA menu，该menu只绑定finops_read_export/full_access/admin三类dedicated roles；permission marker绝不授予APP。"
    - "D-25：role sync disabled、missing config、menu/role/binding缺失、non-dedicated drift或timeout均失败；不提交PG成功、不返回假成功。"
    - "runtime adapter只管理三类dedicated role membership；不删除业务role/member、menu或其它binding，cleanup留给依赖计划13-10。"
    - "ACL no-op零OA/PG/audit写；真实变化复用现有OA target→PG/audit→OA compensation与502/503错误体系。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/oa_role_sync_service.py"
      provides: "strict fixed-menu selector, dedicated role/menu exact-set validation and membership projection"
    - path: "tests/test_oa_role_sync_service.py"
      provides: "permission-marker separation, disabled/missing/drift/timeout and no-broad-mutation matrix"
  key_links:
    - from: "FIN_OPS_OA_REQUIRED_PERMISSION=finops:app:view"
      to: "sys_menu.perms"
      via: "OA integration-only fixed selector; never AccessControlService input"
    - from: "AppSettingsService.update_access_control"
      to: "MySQLOARoleSyncExecutor.apply"
      via: "real semantic change only, existing compensation boundary"
---

<objective>
把OA runtime同步收敛为canonical ACL的严格、可失败menu投影，不混入deployment cleanup责任。

Purpose: 落实 D-23、D-24、D-25、D-27，并修正B-01：`finops:app:view`保留为OA menu identifier，但永远不是APP authorization source。
Output: focused runtime adapter、现有settings compensation wiring与focused tests；deployment exact cleanup/evidence由13-10承接。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-07-SUMMARY.md
@docs/modules/oa-integration/boundary-io.md
@docs/modules/settings/boundary-io.md
@backend/src/fin_ops_platform/services/oa_role_sync_service.py
@backend/src/fin_ops_platform/services/app_settings_service.py
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 以固定permission marker验证OA menu三专用角色exact set</name>
  <files>backend/src/fin_ops_platform/services/oa_role_sync_service.py, tests/test_oa_role_sync_service.py</files>
  <behavior>
    - `FIN_OPS_OA_REQUIRED_PERMISSION`仅在OA adapter/config解析并必须精确等于`finops:app:view`；任何其它值或缺失均configuration error。
    - 同一OA transaction唯一解析`sys_menu.perms=finops:app:view`、三dedicated roles及三bindings，并要求menu全部role keys精确等于三者。
    - missing/duplicate menu、missing role/binding、non-dedicated drift、connect/read/write timeout均rollback；不自动cleanup、不宽删。
    - 只exact replace三dedicated `sys_user_role` membership；其它role membership、menu及bindings不变。
  </behavior>
  <action>按 D-23～D-25 在现有`MySQLOARoleSyncExecutor.apply` transaction内增加最窄SELECT validation：使用固定`finops:app:view` selector唯一定位menu，验证三dedicated role唯一存在、三binding完整且该menu role-key set exact。只有验证通过才执行已有三role membership replacement；不触碰其它role/member/menu/binding。`FIN_OPS_OA_REQUIRED_PERMISSION`可留在OA integration config但必须等于固定marker，禁止import、传递或映射进`AccessControlService`。tests同时证明identity/session带`finops:app:view`但不在Settings ACL时仍denied，而同一marker可成功定位OA menu。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_oa_role_sync_service tests.test_auth_guard -v</automated></verify>
  <acceptance_criteria>fixed-selector、permission-present APP denied、menu/role/binding exact set、其它role/member不变及missing/drift/timeout矩阵通过。</acceptance_criteria>
  <done>permission marker的OA定位责任与APP授权责任机械分离。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让Settings mutation对runtime projection失败保持现有补偿与错误合同</name>
  <files>backend/src/fin_ops_platform/services/app_settings_service.py, backend/src/fin_ops_platform/app/routes_settings.py, tests/test_app_settings_service.py, tests/test_workbench_settings_sync_api.py</files>
  <behavior>
    - enabled=false/missing config抛既有OARoleSyncConfigurationError；ACL真变化返回502且PG/audit零写。
    - target success后PG失败执行一次existing compensation；compensation失败返回既有503 inconsistent合同。
    - disabled/missing/menu/role/binding/drift/timeout不得返回成功；no-op保持零OA/PG/audit写。
  </behavior>
  <action>按 D-23～D-25 只调整现有wiring：`OARoleSyncService.sync_access_control`在executor缺失时抛既有configuration error而非silent return；保留`AppSettingsService`现有no-op、target/PG/compensation critical section和route 502 `oa_role_sync_failed`/503 `access_control_sync_inconsistent`。不得新增错误体系、worker、outbox、adapter或cleanup DML；13-10负责deployment exact cleanup和evidence。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_app_settings_service tests.test_workbench_settings_sync_api tests.test_oa_role_sync_service -v</automated></verify>
  <acceptance_criteria>disabled/missing/menu/role/binding/drift/timeout/compensation矩阵通过；失败时PG/audit/其它OA state不被半写。</acceptance_criteria>
  <done>runtime ACL mutation不能在OA菜单投影不完整时伪成功。</done>
</task>

</tasks>

<docs_impact>
改变OA integration与settings runtime failure I/O；13-04在13-10/09完成后更新长期文档。本plan不改deploy/read model/worker/cache/outbox/table。
</docs_impact>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Settings ACL → OA MySQL | 跨DB投影可能失败或漂移。 |
| OA permission marker → APP evaluator | 同一字符串可能被误接为APP grant。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T13-36 | Elevation of Privilege | permission marker / non-dedicated binding | mitigate | fixed OA-only selector、APP denied test、exact-set validation。 |
| T13-38 | Denial of Service | disabled/missing/timeout | mitigate | bounded existing timeout、typed 502/503、no false success。 |
| T13-SC | Tampering | package installs | mitigate | no package installs; existing PyMySQL only。 |
</threat_model>

<verification>
两个focused commands通过；files_modified为6，runtime compensation与deployment cleanup边界不混合。
</verification>

<success_criteria>
- `finops:app:view`只定位唯一OA menu，不授予APP。
- OA menu role set严格为三dedicated roles，runtime只替换其members。
- disabled/missing/drift/timeout不假成功，现有compensation/502/503合同保留。
- 不删除业务role/member/menu/其它binding，不新增runtime抽象。
</success_criteria>

<output>Create `.planning/phases/13-settings-improvements/13-08-SUMMARY.md`.</output>
