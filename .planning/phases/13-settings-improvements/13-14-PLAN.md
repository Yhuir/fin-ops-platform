---
phase: 13-settings-improvements
plan: "14"
type: execute
wave: 11
depends_on:
  - "13-13"
files_modified:
  - backend/src/fin_ops_platform/tools/settings_access_control_preflight.py
  - tests/test_settings_access_control_preflight.py
  - deploy/oa/bin/finops-deploy-control.sh
  - deploy/oa/README.md
  - tests/test_deploy_oa_script.py
  - scripts/with-production-admin-token.sh
  - scripts/deploy-oa.sh
  - scripts/deploy_oa.py
  - deploy/oa/env/fin-ops.common.env.example
  - deploy/oa/fin_ops_user_role_sync.mysql.sql
autonomous: true
requirements: [PAGE-15, PAGE-04, PAGE-05, PAR-01, PAR-02, PAR-03]
must_haves:
  truths:
    - "Existing collector/deploy-control支持secret-safe fresh 005/006 preflight/postdeploy、fixed selector、exact cleanup/rollback与new router/session证据。"
    - "Canonical deploy支持zero-reupload activate-existing，API quiesce和ACL-safe fingerprint阻止unsafe rollback。"
    - "Local/candidate deterministic tests、唯一inventory guard、七类backend/frontend/E2E与verify all通过后才允许production checkpoint。"
  artifacts:
    - path: "deploy/oa/bin/finops-deploy-control.sh"
      provides: "candidate preflight/postdeploy, safe cutover and rollback gate"
    - path: "backend/src/fin_ops_platform/tools/settings_access_control_preflight.py"
      provides: "existing secret-safe production evidence collector"
    - path: "scripts/deploy_oa.py"
      provides: "canonical activate-existing entrypoint"
  key_links:
    - from: "13-10 exact artifact"
      to: "finops-deploy-control"
      via: "same collector/hash/before-image contract"
    - from: "scripts/deploy_oa.py"
      to: "root release gate"
      via: "zero-reupload activate-existing mode"
    - from: "13-09/11 regressions"
      to: "release eligibility"
      via: "rerun existing tests without duplicate scanner"
---

<objective>
完成local/candidate release-preparation实现和确定性验证，不执行任何production mutation。

Purpose: 沿R1-B01建议分离deploy/preflight implementation与生产bootstrap checkpoints；落实 D-17～D-27。
Output: 10文件candidate tooling、canonical deploy gate与全回归证据。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>
<read_first>
@AGENTS.md
@.planning/phases/13-settings-improvements/13-13-SUMMARY.md
@.planning/phases/13-settings-improvements/13-09-SUMMARY.md
@.planning/phases/13-settings-improvements/13-11-SUMMARY.md
@.planning/phases/13-settings-improvements/13-10-SUMMARY.md
@deploy/oa/README.md
@deploy/oa/bin/finops-deploy-control.sh
@backend/src/fin_ops_platform/tools/settings_access_control_preflight.py
</read_first>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 完成candidate preflight/postdeploy collector与safe root gate</name>
  <files>backend/src/fin_ops_platform/tools/settings_access_control_preflight.py, tests/test_settings_access_control_preflight.py, deploy/oa/bin/finops-deploy-control.sh, deploy/oa/README.md, tests/test_deploy_oa_script.py</files>
  <behavior>
    - preflight只读采集ACL/version/CHECK、三retired env absence、fixed selector、unique menu、三role exact set、exact cleanup/rollback、fresh distinct 005/006 identity；secret永不进入argv/artifact/log。
    - postdeploy验证permission-present 006的session/direct API denied、新OA router/session菜单、三role exact set与finally restore/read-back。
    - cutover固定preflight→approval→API quiesce→migration/CHECK→safe activation→smoke；unsafe previous保持maintenance/forward repair。
  </behavior>
  <action>扩展13-10同一collector与现有deploy-control，不建第二工具/transport：实现candidate contract/status、remote artifact/hash、fresh token expiry/identity/redaction、new router/session与restore证据。保留exact cleanup no-broad-delete、fixed selector OA-only、root-owned env和existing timeouts。更新deploy/oa runbook与既有测试，覆盖drift、failure、safe rollback、maintenance及runtime-worker helper不变。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_settings_access_control_preflight tests.test_deploy_oa_script tests.test_postgres_migrations -v &amp;&amp; bash scripts/verify.sh lint</automated></verify>
  <acceptance_criteria>collector/control tests覆盖全部eligibility、redaction、exact cleanup、router/restore与cutover失败；本task不连接生产写入。</acceptance_criteria>
  <done>candidate root gate可执行且fail closed。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 完成canonical zero-reupload deploy entrypoint</name>
  <files>scripts/with-production-admin-token.sh, scripts/deploy-oa.sh, scripts/deploy_oa.py, tests/test_deploy_oa_script.py</files>
  <behavior>
    - `--activate-existing --release-name`要求exact uploaded release、approved artifact/helper/source/migration fingerprints与unchanged active previous。
    - 不build/upload/replace/self-update；与no-activate/replace/skip-build等组合拒绝；双token只经0600 wrapper与remote stdin。
  </behavior>
  <action>在现有canonical deploy Python/shell wrapper维护最窄activate-existing模式，只SSH调用root release gate。禁止legacy self-update、token argv/log、本地direct sudo与无意义参数组合；tests证明zero reupload、exact fingerprints、failure blocked和unsafe rollback拒绝。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_deploy_oa_script -v &amp;&amp; bash scripts/verify.sh lint</automated></verify>
  <acceptance_criteria>canonical parser/command contracts、token用途隔离、fingerprint gates和zero-reupload tests通过。</acceptance_criteria>
  <done>production activation拥有唯一canonical entrypoint。</done>
</task>
<task type="auto">
  <name>Task 3: 对齐env/OA sync assets并完成全部local/candidate回归</name>
  <files>deploy/oa/env/fin-ops.common.env.example, deploy/oa/fin_ops_user_role_sync.mysql.sql, tests/test_deploy_oa_script.py, tests/test_settings_access_control_preflight.py</files>
  <action>保持`FIN_OPS_OA_REQUIRED_PERMISSION=finops:app:view` fixed selector，删除三retired APP admission env authority；user-role SQL仍只管理三dedicated role members。复跑13-09唯一inventory guard、13-11 frontend/E2E、targeted backend/OA/deploy、docs/lint及`bash scripts/verify.sh all`。只复用已有tests/scanner，不复制实现或宣称外部事实pass；真实DB/OA/session/router/latency留给13-15/05。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_permissions_write_entry_inventory tests.test_app_settings_service tests.test_oa_role_sync_service tests.test_settings_access_control_preflight tests.test_deploy_oa_script -v &amp;&amp; cd web &amp;&amp; npm test -- --run src/test/SessionApi.test.ts src/test/SessionGate.test.tsx src/test/App.test.tsx src/test/PageRouteHost.test.tsx &amp;&amp; npx playwright test e2e/permissions-role-matrix.spec.ts &amp;&amp; cd .. &amp;&amp; bash scripts/verify.sh all</automated></verify>
  <acceptance_criteria>七类backend/frontend/E2E、唯一inventory/I-O guards、docs/lint和verify all全绿；无production命令。</acceptance_criteria>
  <done>release candidate满足进入production bootstrap checkpoint的全部本地条件。</done>
</task>
</tasks>

<docs_impact>更新`deploy/oa/README.md`实际runbook；长期模块docs已由13-13完成。若实现合同偏离，回13-13修订，不在runbook创建例外。</docs_impact>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Candidate tooling → root deploy gate | fingerprint/capability决定是否可进入production。 |
| HTTP tokens → evidence collector | tokens必须用途隔离且不泄露。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T13-53 | Tampering | candidate/fingerprint | mitigate | exact hashes, zero reupload, active-previous gate。 |
| T13-54 | Information Disclosure | tokens/evidence | mitigate | 0600 wrapper, stdin, redaction tests。 |
| T13-55 | Elevation of Privilege | unsafe rollback | mitigate | ACL-safe capability only; otherwise maintenance/forward repair。 |
</threat_model>
<verification>三个targeted/all commands通过；files_modified=10；不执行production。</verification>
<success_criteria>
- Candidate collector/deploy gate完整覆盖fixed selector、exact cleanup、fresh identities/router/restore。
- Canonical activation为zero-reupload且unsafe rollback fail closed。
- 七类/唯一inventory/full verification通过后才进入13-15。
</success_criteria>
<output>Create `.planning/phases/13-settings-improvements/13-14-SUMMARY.md`.</output>

