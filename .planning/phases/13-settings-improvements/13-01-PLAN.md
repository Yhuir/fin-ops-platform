---
phase: 13-settings-improvements
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/fin_ops_platform/postgres/migrations/0132_settings_access_control_guard.sql
  - backend/src/fin_ops_platform/services/state_store_protocol.py
  - backend/src/fin_ops_platform/services/state_store.py
  - backend/src/fin_ops_platform/services/postgres_state_store.py
  - backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py
  - tests/test_postgres_migrations.py
  - tests/test_postgres_repositories_boundaries.py
  - tests/test_state_store_contract.py
  - tests/test_postgres_state_store.py
autonomous: true
requirements:
  - PAGE-15
  - PAGE-04
  - PAR-01
  - PAR-03
must_haves:
  truths:
    - "ACL 使用独立 expected_version/CAS；语义 no-op 不写 settings、不写 durable audit。"
    - "ACL writer 与 generic settings writer 共享固定 PostgreSQL session advisory lock；generic writer 在锁内读取最新 row 并只合并非 ACL family。"
    - "ACL 专用 critical-section port 在同一 guard 内暴露 locked current snapshot并完成 settings+audit DB commit；它不是通用 UoW。"
    - "每次ACL mutation使用server-generated unique mutation_id写入同事务audit metadata；COMMIT outcome unknown必须fresh-lock恢复判定，绝不盲补偿。"
    - "数据库约束阻断除 YNSYLP005 外的管理员，即使旧 binary 被误回滚。"
  artifacts:
    - path: "backend/src/fin_ops_platform/postgres/migrations/0132_settings_access_control_guard.sql"
      provides: "历史非法管理员 fail-closed 清理、迁移 audit、access_control_version 初始化和 CHECK invariant"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py"
      provides: "共享固定 advisory lock、row-lock family merge与ACL专用critical-section/commit持久化实现"
  key_links:
    - from: "backend/src/fin_ops_platform/services/postgres_state_store.py"
      to: "backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py"
      via: "显式 generic-preserve 与 begin_settings_acl_critical_section ports，共享同一固定session advisory lock"
    - from: "backend/src/fin_ops_platform/postgres/migrations/0132_settings_access_control_guard.sql"
      to: "app.app_settings / audit.events"
      via: "同一 migration transaction 内 repair、mirror alignment、audit、validated CHECK"
---

<objective>
建立 Phase 13 可独立全绿的持久化原子基础，使后续后端重接线不能绕过 CAS、durable audit、generic family preservation 或数据库兜底。

Purpose: 落实 D-08、D-09、D-10、D-11、D-17、D-19；提供可被 Wave 1 调用且本 plan 内验证全绿的窄 repository/state-store 合同。
Output: 0132 migration、family-preserving generic writer、ACL row-lock/CAS/audit writer及 local/PostgreSQL parity tests。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-PATTERNS.md
@.planning/phases/13-settings-improvements/13-VALIDATION.md
@backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py
@backend/src/fin_ops_platform/services/postgres_state_store.py
@backend/src/fin_ops_platform/services/state_store.py
@backend/src/fin_ops_platform/postgres/migrations/0005_tax_etc_turnover_settings_jobs.sql
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 0132 fail-closed 数据迁移与旧 binary 数据库兜底</name>
  <files>backend/src/fin_ops_platform/postgres/migrations/0132_settings_access_control_guard.sql, tests/test_postgres_migrations.py</files>
  <behavior>
    - 历史非 YNSYLP005 admin 被 fail-closed 清理，formal settings_payload 与 raw_payload.normalized_payload 保持一致。
    - 缺失/非法 access_control_version 初始化为正整数；迁移重复验证不产生第二事实源。
    - audit.events 记录迁移 actor、前后版本、受影响账号摘要/计数与 hash，不记录 token、密码或完整 payload。
    - CHECK 拒绝非法 admin、protected admin 出现在 readonly/full、非正整数 version，并允许唯一 YNSYLP005 canonical 形态。
  </behavior>
  <action>按 D-08、D-10、D-11、D-17 使用现有 4 位 migration runner 新增 0132。先 repair 再 validate CHECK；同一 SQL transaction 更新 formal/raw 镜像并写 migration audit。CHECK 必须同时防旧 top-level admin_usernames 写法和目标 canonical ACL keys，确保误回滚旧 binary 也无法写入其它 admin。只复用 app.app_settings 与 audit.events，不建表、不建 trigger/framework。对未知生产事实不猜 tier：非法历史 admin 按 denied 清理，发布前事实冲突仍由 13-04 checkpoint 阻断。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_postgres_migrations -v</automated>
  </verify>
  <acceptance_criteria>migration runner 可发现 0132；repair、raw mirror、audit 与 CHECK 的正常/非法/旧 binary payload fixtures 全部通过，且 migration 不创建新表、worker、outbox、dirty scope或 cache。</acceptance_criteria>
  <done>数据库具备唯一 protected administrator 的可验证 fail-closed invariant。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 family-preserving generic save 与 ACL CAS/audit ports</name>
  <files>backend/src/fin_ops_platform/services/state_store_protocol.py, backend/src/fin_ops_platform/services/state_store.py, backend/src/fin_ops_platform/services/postgres_state_store.py, backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py, tests/test_postgres_repositories_boundaries.py, tests/test_state_store_contract.py, tests/test_postgres_state_store.py</files>
  <behavior>
    - PostgreSQL ACL与generic writer均先在同一connection取得session-level `pg_advisory_lock(hashtextextended('fin_ops.app_settings.acl', 0))`；ACL port随后BEGIN、`SELECT app_settings ... FOR UPDATE`、检查expected_version，并向service暴露immutable `locked_current`。
    - ACL专用 `begin_settings_acl_critical_section(expected_version)` guard提供且只提供 `locked_current`、`commit(next_acl, durable_audit)`、rollback/close；它不是通用UoW，repository不知权限、normalize或OA。
    - `guard.commit(...)`只合并ACL family、version+1并写audit.events，在返回service前完成同一transaction的DB COMMIT；仅known pre-commit/known rollback失败可确认原session advisory lock仍持有，COMMIT lost-ACK/connection loss必须按outcome unknown进入fresh-lock恢复，不能沿用旧锁或盲补偿。
    - generic save取得同一session advisory lock后才BEGIN/锁行/读取最新snapshot，只合并非ACL family并保留最新ACL/version；不得使用service旧snapshot。
    - stale在OA前返回显式conflict；semantic no-op由service在guard内判定并零UPDATE/零audit/零OA；local ApplicationStateStore用已有RLock实现同一guard合同。
    - advisory unlock位于finally；异常、stale、no-op、OA失败、DB失败和补偿失败均释放且不泄漏connection lock。
    - repository另提供窄`recover_settings_acl_commit(mutation_id)`：fresh connection取得同一advisory lock后只返回canonical ACL/version与该mutation_id audit存在性；不判断OA或业务结果。
  </behavior>
  <action>按 D-07～D-11 定义`begin_settings_acl_critical_section(expected_version)`与窄恢复port。正常路径同一connection依次固定session advisory lock、BEGIN、`SELECT ... FOR UPDATE`、version compare并暴露`locked_current`；`commit(next_acl, durable_audit)`把server-generated unique `mutation_id`写入audit metadata，与formal/raw ACL-only merge及version+1同事务COMMIT。明确区分：(1) known pre-commit failure/known rollback：原session lock仍持有，service可补偿previous；(2) commit success：返回前已确认；(3) connection loss/timeout during COMMIT导致outcome unknown：不得声称旧lock仍在、不得盲补偿。恢复port必须用fresh connection重新取得同一advisory lock，读取canonical ACL/version及mutation_id audit presence后把纯事实返回service，并finally unlock。generic writer共享同一key且锁内重读/仅merge非ACL。local store实现等价mutation proof。repository不知权限/OA/结果分类，不新建表/outbox/worker/cache/general UoW。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_postgres_repositories_boundaries tests.test_state_store_contract tests.test_postgres_state_store -v</automated>
  </verify>
  <acceptance_criteria>专用guard与recovery read/lock合同固定；tests区分known rollback和unknown COMMIT，证明fresh-lock、mutation-id proof、intervening writer/mismatch不被覆盖、generic writer不受污染及所有路径unlock；repository只返回事实且无通用UoW。</acceptance_criteria>
  <done>Wave 1 可调用的可信持久化原子合同已就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Settings service → PostgreSQL | canonical ACL、并发版本与 durable audit 必须原子。 |
| New binary → old binary rollback | 旧代码可能再次尝试写任意 admin_usernames。 |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Risk | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T13-02 | HIGH | Tampering | app.app_settings concurrent writers | mitigate | ASVS V5：ACL/generic共享固定session advisory lock；锁内BEGIN+FOR UPDATE+CAS+family merge，DB commit返回前完成。 |
| T13-03 | HIGH | Repudiation | ACL mutation audit | mitigate | ASVS V7：canonical write 与 secret-safe audit.events 同事务，失败 rollback。 |
| T13-06 | HIGH | Elevation of Privilege | rollback to vulnerable binary | mitigate | validated DB CHECK + migration fixtures，阻断非 YNSYLP005 admin。 |
| T13-07 | MEDIUM | Information Disclosure | audit metadata | mitigate | 只记录 actor/version/tier 摘要/changed usernames/trace，不记录 token、密码或完整 body。 |
</threat_model>

<verification>
1. 逐 task 运行其 targeted unittest；Wave 0 结束运行 `bash scripts/verify.sh lint`。
2. 运行 `PYTHONPATH=backend/src python3 -m unittest tests.test_postgres_migrations tests.test_postgres_repositories_boundaries tests.test_state_store_contract tests.test_postgres_state_store -v`；该 gating command 必须 exit 0。
3. 使用 `rg` 删除哨兵只做正向清单；测试/migration 中 legacy 字段仅允许用于拒绝、清理、rollback-defense fixtures。
</verification>

<success_criteria>
- 专用critical-section、CAS、no-op、原子audit、advisory unlock、确定性并发交错和DB CHECK全有本plan内可变绿的证明；API/session攻击TDD由13-02 Task 1闭环。
- 没有新表、worker、outbox/dirty scope、cache、service/repository framework或依赖。
- Wave 0 产物提供明确 ports，Wave 1 不需重新决定事务或错误语义。
</success_criteria>

## Artifacts this phase produces

- `0132_settings_access_control_guard.sql`：repair + audit + validated CHECK。
- state-store/repository ACL transaction ports 与 local parity。
- repository/local-store/migration contract tests。
- `.planning/phases/13-settings-improvements/13-01-SUMMARY.md`。

<output>
Create `.planning/phases/13-settings-improvements/13-01-SUMMARY.md` when done.
</output>
