---
phase: 13-settings-improvements
plan: "04"
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
  - "13-03"
files_modified:
  - SECURITY.md
  - docs/product-specs/platform-settings-health.md
  - docs/business-flows/settings.md
  - docs/app-architecture/pages.md
  - docs/app-architecture/runtime-and-ownership.md
  - docs/dev/api-contracts.md
  - docs/modules/settings/README.md
  - docs/modules/settings/boundary-io.md
  - docs/modules/settings/state-machine.md
  - docs/modules/settings/tests.md
  - docs/modules/settings/e2e-spec.md
  - docs/modules/settings/e2e-coverage.md
  - docs/modules/permissions-and-audit/README.md
  - docs/modules/permissions-and-audit/boundary-io.md
  - docs/modules/permissions-and-audit/state-machine.md
  - docs/modules/permissions-and-audit/tests.md
  - docs/modules/oa-integration/README.md
  - docs/modules/oa-integration/boundary-io.md
  - docs/modules/oa-integration/state-machine.md
  - docs/modules/oa-integration/tests.md
  - deploy/oa/env/fin-ops.common.env.example
  - deploy/oa/bin/finops-deploy-control.sh
  - deploy/oa/fin_ops_user_role_sync.mysql.sql
  - deploy/oa/README.md
  - scripts/with-production-admin-token.sh
  - scripts/deploy-oa.sh
  - scripts/deploy_oa.py
  - backend/src/fin_ops_platform/tools/settings_access_control_preflight.py
  - tests/test_settings_access_control_preflight.py
  - tests/test_deploy_oa_script.py
autonomous: false
requirements:
  - PAGE-15
  - PAGE-04
  - PAGE-05
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "长期文档明确唯一 protected administrator、generic/dedicated API、CAS/audit/OA 边界、旧代码删除和七类验证责任。"
    - "发布前secret-safe只读preflight同时盘点PostgreSQL ACL、OA三类角色，并通过真实/api/session/me验证admin与专用bearer身份；token永不进入artifact/log。"
    - "migration/deploy/rollback 顺序阻止旧 vulnerable binary 在安全约束缺失时重新上线；OA 不一致有明确人工处置路径。"
    - "deploy control在migration前quiesce API，并用ACL-safe capability/fingerprint拒绝自动回滚unsafe release；无safe previous时保持maintenance并forward repair。"
    - "local/candidate deterministic回归证明admin/full/read/denied合同、ACL OA sync/compensation、普通settings、既有admin-only功能与可测I/O调用预算；外部事实只由production preflight/post-deploy证明。"
    - "全仓 legacy sentinel、七类回归、lint/docs/backend/frontend/E2E 与 verify.sh all 全部通过。"
    - "production control-plane bootstrap与最终activation分属两个明确批准；bootstrap只上传candidate并由单独批准的root人工执行hash-pinned同文件系统原子替换，不调用legacy self-update、不触碰runtime-worker helper、不激活API、不迁移DB、不改OA/ACL。"
  artifacts:
    - path: "/opt/fin-ops/evidence/<release>/settings-access-control-bootstrap.json"
      provides: "approved candidate/helper hashes、manual-root atomic replace/restore结果及application/data/OA不变证明"
    - path: "backend/src/fin_ops_platform/tools/settings_access_control_preflight.py"
      provides: "DB/env/OA roles 只读盘点、合同差异与 secret-safe dry-run cleanup manifest"
    - path: "deploy/oa/bin/finops-deploy-control.sh"
      provides: "可执行root-owned preflight、双HTTP token用途隔离、API quiesce安全cutover与ACL-safe fingerprint rollback gate"
    - path: "docs/modules/settings/boundary-io.md"
      provides: "最终 Settings ACL canonical/API/persistence/OA/old-code deletion 边界"
    - path: "deploy/oa/README.md"
      provides: "生产 checkpoint、顺序、回滚、角色核对、性能采样和事故处置 runbook"
  key_links:
    - from: "/opt/fin-ops/releases/<release>/src/deploy/oa/bin/finops-deploy-control.sh"
      to: "/usr/local/sbin/finops-deploy-control"
      via: "单独批准的manual-root same-filesystem temp + approved sha256 + prevalidation + atomic replace/restore；禁止legacy self-update"
    - from: "deploy/oa/bin/finops-deploy-control.sh"
      to: "backend/src/fin_ops_platform/tools/settings_access_control_preflight.py"
      via: "runtime env wrapper 中的 read-only settings-access-control-preflight command"
    - from: "backend/src/fin_ops_platform/tools/settings_access_control_preflight.py"
      to: "app.app_settings / root env / OA fin-ops roles"
      via: "只读查询与 normalized comparison；输出 hash/count/member diff，不输出 secret"
    - from: "deploy/oa/README.md"
      to: "0132 migration + release activation + session/OA smoke"
      via: "preflight → user checkpoint → API quiesce → migration/CHECK → safe candidate → smoke → safe rollback或maintenance/forward repair"
    - from: "docs/modules/settings/tests.md"
      to: ".planning/phases/13-settings-improvements/13-VALIDATION.md"
      via: "七类测试与高风险验证顺序沉淀为长期合同"
---

<objective>
完成 T0-01 的文档、部署、production只读盘点、local/candidate确定性回归与全回归闭环，并在任何生产写入前设置不可绕过的用户确认 checkpoint。

Purpose: 落实 D-01～D-19 的发布面，特别是 D-10、D-14、D-17、D-18、D-19；确保安全修复可部署、可证明、可审计且不能回滚到重新开放提权链的 binary。
Output: 更新后的长期事实源、secret-safe production preflight、deploy/rollback gates、local/candidate I/O证据、whole-repo deletion证明和全套回归结果。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-PATTERNS.md
@.planning/phases/13-settings-improvements/13-VALIDATION.md
@.planning/phases/13-settings-improvements/13-01-SUMMARY.md
@.planning/phases/13-settings-improvements/13-02-SUMMARY.md
@.planning/phases/13-settings-improvements/13-03-SUMMARY.md
@SECURITY.md
@docs/modules/settings/boundary-io.md
@docs/modules/permissions-and-audit/boundary-io.md
@docs/modules/oa-integration/boundary-io.md
@deploy/oa/README.md
@deploy/oa/bin/finops-deploy-control.sh
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: 把最终 ACL 安全合同沉淀到长期文档与七类测试矩阵</name>
  <files>SECURITY.md, docs/product-specs/platform-settings-health.md, docs/business-flows/settings.md, docs/app-architecture/pages.md, docs/app-architecture/runtime-and-ownership.md, docs/dev/api-contracts.md, docs/modules/settings/README.md, docs/modules/settings/boundary-io.md, docs/modules/settings/state-machine.md, docs/modules/settings/tests.md, docs/modules/settings/e2e-spec.md, docs/modules/settings/e2e-coverage.md, docs/modules/permissions-and-audit/README.md, docs/modules/permissions-and-audit/boundary-io.md, docs/modules/permissions-and-audit/state-machine.md, docs/modules/permissions-and-audit/tests.md, docs/modules/oa-integration/README.md, docs/modules/oa-integration/boundary-io.md, docs/modules/oa-integration/state-machine.md, docs/modules/oa-integration/tests.md</files>
  <action>按 D-01～D-19 和三份 SUMMARY 更新长期事实源：唯一 protected administrator=YNSYLP005 且 APP 不可轮换；generic settings 无 ACL 且 legacy keys 明确400；dedicated admin GET/PUT DTO、expected_version/409、session actor、durable audit；settings/permissions/OA 分工；generic zero OA、ACL no-op zero I/O、真实变化同步+补偿；单 snapshot auth；frontend唯一入口与完整 backend/frontend旧路径删除。模块 boundary 必须同步 input/output/file range/dependency direction/old-code deletion，state-machine 必须覆盖 no-op/conflict/OA target failure/DB failure/compensation failure，tests/e2e 文档按 13-VALIDATION 的七类测试和高风险顺序登记。明确本 phase 不新增/不改变 read model、worker、dirty scope、cache、其它页面 response或业务事实；不要复制 planning prompt到长期 docs。</action>
  <verify>
    <automated>bash scripts/verify.sh docs</automated>
  </verify>
  <acceptance_criteria>产品、安全、API、business flow 与三模块文档对同一 protected admin/API/CAS/audit/OA/rollback事实无冲突；七类测试、性能 I/O 预算和 legacy deletion sentinels有长期维护入口；docs check通过。</acceptance_criteria>
  <done>Phase 13 的长期事实不再依赖 planning artifacts 或旧 generic ACL 说明。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 secret-safe production只读 preflight、dry-run cleanup与安全部署/回滚 gate</name>
  <files>backend/src/fin_ops_platform/tools/settings_access_control_preflight.py, tests/test_settings_access_control_preflight.py, scripts/with-production-admin-token.sh, scripts/deploy-oa.sh, scripts/deploy_oa.py, deploy/oa/env/fin-ops.common.env.example, deploy/oa/bin/finops-deploy-control.sh, deploy/oa/fin_ops_user_role_sync.mysql.sql, deploy/oa/README.md, tests/test_deploy_oa_script.py</files>
  <behavior>
    - preflight只读采集 app.app_settings ACL/version/CHECK状态、root-owned admin env和OA readonly/full/admin三角色成员；不会执行UPDATE/INSERT/DELETE、migration或OA role mutation。
    - 输出唯一admin合同、非法/缺失/重叠成员、DB/env/OA差异、拟清理tier与before/after hash/count；不输出token、密码、DSN或完整settings payload。
    - `finops-deploy-control settings-access-control-preflight <release> --http-tokens-stdin --dry-run --json` 使用server root-owned runtime/migrator/OA env及stdin HTTP tokens执行；dry-run始终产出可审阅artifact并exit 0，eligibility冲突由release assert gate非零阻断。
    - HTTP credentials仅复用`FIN_OPS_HTTP_SLO_ADMIN_TOKEN`（`FIN_OPS_E2E_ADMIN_TOKEN`由现有wrapper归一为同值）与`FIN_OPS_HTTP_SLO_BEARER_TOKEN`；同一0600 `FIN_OPS_LOCAL_ADMIN_TOKEN_ENV`加载后经stdin传给root command，不作为DB/OA凭据。
    - preflight用两个token分别调用真实`/api/session/me`：admin必须identity=YNSYLP005；bearer必须来自专用SLO bearer输入、identity非YNSYLP005、非admin且initial tier=denied。artifact只记加salt username hash、initial tier与identity-check结果。
    - 任一token缺失/过期、admin identity错误、bearer为protected/admin、bearer initial tier非denied、两token解析为同一identity或bearer不是专用SLO输入，`eligible=false`；token值不得进入argv、artifact或log。
    - cutover顺序固定为read-only preflight→用户批准→API quiesce→0132 migration/CHECK validation→ACL-safe candidate activation→post-activation role/API smoke；quiesce期间旧generic endpoint不可服务。
    - release metadata记录ACL-safe capability与source/migration fingerprint；自动rollback只接受匹配安全能力的previous release，无safe previous时保持maintenance/fail-closed并forward repair，绝不重启vulnerable binary。
    - `finops-deploy-control settings-access-control-post-deploy <release> --http-tokens-stdin --json`只接受相同双token的stdin输入，先核验两个username hash等于approved preflight，再执行D-18并生成redacted artifact/hash。
    - canonical deploy新增`--activate-existing --release-name <release>`：不build/upload/replace/self-update，只经SSH调用root release gate；与`--no-activate`、`--replace-release`、`--skip-build`等无意义组合互斥拒绝。
  </behavior>
  <action>实现candidate helper的preflight/postdeploy、`contract-version`/`candidate-status`只读合同及safe cutover；另在现有canonical `scripts/deploy_oa.py`/shell wrapper新增`--activate-existing --release-name`最小模式：要求exact release已上传且candidate/helper fingerprints与approved remote artifact一致，active previous未变化，然后只SSH调用root release gate；禁止build、upload、replace、self-update，双token如需要只经remote stdin。parser必须拒绝与`--no-activate`、`--replace-release`、`--skip-build`及其它upload/build选项组合。由于当前production helper的legacy `self-update`是直接覆盖且还会更新runtime-worker helper，本phase首次bootstrap明确禁止调用它；`deploy/oa/README.md`写入一次性manual-root runbook：root把candidate复制到`/usr/local/sbin`同文件系统root-owned temp、校验approved sha256/`bash -n`/`contract-version`，保存不可执行old-helper backup，以`mv -f`原子替换live helper，post-validate失败则以同方式原子恢复，并证明runtime-worker helper hash、active release、service、DB、OA、ACL均不变。tests覆盖runbook/sentinel不调用legacy self-update、remote stdin/token不进argv/log、zero reupload、exact release、approved hash/fingerprint/active previous gates、failure blocked、完整cutover及unsafe rollback拒绝；不建第二transport/framework。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_settings_access_control_preflight tests.test_deploy_oa_script tests.test_postgres_migrations -v &amp;&amp; bash scripts/verify.sh lint</automated>
  </verify>
  <acceptance_criteria>fixture覆盖合法/非法admin、DB/env/OA冲突、双token missing/expired/wrong identity/tier/same identity/non-dedicated、username hash绑定、artifact/hash、redaction、token用途隔离和无DML/OA mutation；deploy tests证明安全cutover及unsafe rollback拒绝；targeted verifier exit0，本task不连接生产执行写入。</acceptance_criteria>
  <done>生产事实可在零写入前提下被完整盘点，且部署控制面不能绕开安全门禁。</done>
</task>

<task type="auto">
  <name>Task 3: 完成local/candidate确定性回归、可测I/O计数、legacy sentinels与verify all</name>
  <files>tests/test_settings_access_control_preflight.py, tests/test_deploy_oa_script.py, web/e2e/permissions-role-matrix.spec.ts</files>
  <action>按 D-12～D-19 只运行repository-local/candidate deterministic测试与现有mock Browser/E2E：覆盖YNSYLP005 admin、full_access、read_export_only、denied合同；dedicated ACL real change/no-op/stale、OA target/compensation；generic save、column-layout、pending rules、AppHealth、OA credentials和data reset回归。用spy/barrier/fault fixture机械记录generic save零OA、session每次最多一次snapshot、ACL no-op零write/audit/OA、real PUT一次target OA+一次app transaction、失败补偿最多一次OA，以及13-01/02定义的advisory lock交错；这些是调用/I/O计数，不伪造外部延迟或环境pass。运行whole-repo sentinels：runtime `dynamic_admin_usernames_provider`、`get_admin_usernames`、generic ACL function signature/parse/serialize/response shape、runtime fixture key、`test_read_model_api_contract_harness.py`、WorkbenchSettingsModal access_accounts、pending getattr replay、runtime FIN_OPS_ADMIN_USERNAMES必须为零；legacy词只允许rejection/migration/rollback fixtures/docs retirement说明。依次跑七类targeted tests、lint/docs/backend/frontend/E2E，最后`bash scripts/verify.sh all`。真实外部DB/OA/session/latency事实只能由Task4 production只读preflight和13-05 approval-gated post-deploy产生。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_app_settings_service tests.test_session_api tests.test_auth_guard tests.test_workbench_settings_sync_api tests.test_oa_role_sync_service tests.test_state_store_contract tests.test_postgres_state_store tests.test_postgres_repositories_boundaries tests.test_postgres_migrations tests.test_permissions_write_entry_inventory tests.test_settings_access_control_preflight tests.test_deploy_oa_script -v &amp;&amp; cd web &amp;&amp; npm test -- --run src/test/SettingsPage.test.tsx src/test/WorkbenchSelection.test.tsx src/test/WorkbenchColumnLayout.test.tsx src/test/SessionApi.test.ts &amp;&amp; npx playwright test e2e/permissions-role-matrix.spec.ts &amp;&amp; cd .. &amp;&amp; bash scripts/verify.sh all</automated>
  </verify>
  <acceptance_criteria>七类验证全部有结果：business core、service/repository、API contract、read-model/cache/worker负向、frontend component、E2E integration、existing regression；legacy runtime sentinels清零；deterministic I/O计数符合合同；verify.sh all exit0。</acceptance_criteria>
  <done>local/candidate代码、浏览器、I/O计数与全仓删除证据满足production只读checkpoint输入要求。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 用户批准candidate upload与一次性manual-root helper bootstrap</name>
  <files>None — 只审阅local/candidate test evidence、approved hashes与精确manual-root runbook。</files>
  <action>展示Task1-3全绿证据、release/source/candidate-helper sha256、`--no-activate`上传范围，以及manual-root同文件系统temp→prevalidate→atomic replace→postvalidate→atomic restore步骤。明确说明当前live helper的legacy self-update不具备本次所需原子保证且还会改runtime-worker helper，因此本次禁止调用；请求用户只批准candidate filesystem upload与一次性root helper原子替换。明确排除API activation/service restart、DB migration/CHECK mutation、OA/ACL mutation和production facts结论；默认/模糊回复不批准。</action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_deploy_oa_script tests.test_settings_access_control_preflight -v</automated>
    <human-check>用户确认approved candidate/helper hash、manual-root原子替换/恢复命令、legacy self-update禁用及runtime-worker helper不变，并只批准该bootstrap范围。</human-check>
  </verify>
  <acceptance_criteria>仅收到针对当前release/hash和精确manual-root runbook的明确批准；否则blocking且production filesystem/control-plane不变。</acceptance_criteria>
  <done>upload/manual-root bootstrap范围获明确批准，不等于production facts或activation批准。</done>
</task>

<task type="auto">
  <name>Task 5: 上传并校验非激活candidate</name>
  <files>None — approved production candidate filesystem mutation；尚不修改root helper。</files>
  <action>执行`./scripts/deploy-oa.sh --no-activate --release-name &lt;release&gt;`上传/check candidate，记录candidate source/migration/capability/helper sha256，并通过SSH只读断言active release、API/service状态、DB migration/CHECK、OA/ACL与runtime-worker helper hash全部等于before evidence。不得调用legacy self-update、不得安装helper、不得激活app；任一不变量变化立即blocking。</action>
  <verify>
    <automated>./scripts/deploy-oa.sh --no-activate --release-name &lt;release&gt; &amp;&amp; ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n /usr/local/sbin/finops-deploy-control check-release &lt;release&gt;'</automated>
  </verify>
  <acceptance_criteria>upload/check exit0；remote candidate/helper hashes等于Task4批准值；active release、API、DB、OA、ACL与runtime-worker helper均未变化。</acceptance_criteria>
  <done>candidate已上传并校验，application/data/OA/root helper仍未变化。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: root人工执行hash-pinned原子deploy-control bootstrap</name>
  <files>None — 经Task4批准的一次性root control-plane mutation；不由旧helper自更新。</files>
  <action>由有root权限的生产操作员按`deploy/oa/README.md`逐条执行，不通过旧helper self-update：把`/opt/fin-ops/releases/&lt;release&gt;/src/deploy/oa/bin/finops-deploy-control.sh`复制为同目录文件系统中的root-owned `/usr/local/sbin/.finops-deploy-control.&lt;release&gt;.tmp`；对temp核验Task4 approved sha256、`bash -n`及`contract-version --require settings-access-control-v1`；把当前live helper复制为chmod 0600不可执行backup；使用`mv -f`原子替换`/usr/local/sbin/finops-deploy-control`；核验live hash/owner/mode/contract。postvalidate失败时不能直接把0600 backup移回live：必须从backup复制为同文件系统root-owned rollback temp，设置`root:root 0755`，核验old approved hash、`bash -n`及旧helper contract后，再以`mv -f`原子替换live并复核恢复后的hash/owner/mode/contract，然后block。全程记录before/after/rollback helper hash、runtime-worker helper hash、active release/API/DB/OA/ACL fingerprints到root-owned bootstrap artifact；runtime-worker helper必须字节不变，禁止service restart/migration/OA/ACL写入。root操作员完成后再由普通deploy账号做只读验证。</action>
  <verify>
    <automated>ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n /usr/local/sbin/finops-deploy-control contract-version --require settings-access-control-v1 &amp;&amp; sudo -n /usr/local/sbin/finops-deploy-control candidate-status &lt;release&gt; --json &amp;&amp; sudo -n sha256sum --check /opt/fin-ops/evidence/&lt;release&gt;/settings-access-control-bootstrap.json.sha256'</automated>
    <human-check>root操作员确认使用的是approved temp/hash和atomic mv流程；未运行legacy self-update；失败时已恢复旧helper且未继续。</human-check>
  </verify>
  <acceptance_criteria>新helper hash/contract有效，bootstrap artifact/hash通过；runtime-worker helper、active release、API、DB、OA、ACL fingerprints不变。失败恢复后的旧helper也必须是root:root 0755、approved old hash且contract可执行；任何失败均blocking且不进入preflight。</acceptance_criteria>
  <done>candidate helper经独立manual-root原子路径bootstrap完成；未依赖旧helper安装自己，application/data/OA未变化。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: 远端只读preflight并取得facts/hash与最终activation批准</name>
  <files>None — SSH调用production只读facts preflight并审阅remote artifact。</files>
  <action>通过本地0600 wrapper把双token经SSH stdin传至remote helper；固定边界为`ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod`，远端执行`sudo -n /usr/local/sbin/finops-deploy-control settings-access-control-preflight &lt;release&gt; --http-tokens-stdin --dry-run --json`。随后独立SSH执行remote sha256 check并读取redacted artifact；不得本地direct sudo。展示eligible、双identity hashes/tier、DB/OA facts、cleanup、candidate/active-previous fingerprints与activation/rollback方案，取得第二次明确批准。preflight facts本身只读；candidate/root bootstrap mutation单独列示。</action>
  <verify>
    <automated>./scripts/with-production-admin-token.sh sh -c 'printf "%s\n%s\n" "$FIN_OPS_HTTP_SLO_ADMIN_TOKEN" "$FIN_OPS_HTTP_SLO_BEARER_TOKEN" | ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod "sudo -n /usr/local/sbin/finops-deploy-control settings-access-control-preflight &lt;release&gt; --http-tokens-stdin --dry-run --json"' &amp;&amp; ssh -o StrictHostKeyChecking=accept-new -o ControlMaster=no finops-deploy@finops-prod 'sudo -n sha256sum --check /opt/fin-ops/evidence/&lt;release&gt;/settings-access-control-preflight.json.sha256'</automated>
    <human-check>用户明确批准remote artifact hash、facts/cleanup、candidate fingerprint、active previous与最终activation窗口。</human-check>
  </verify>
  <acceptance_criteria>remote preflight/hash exit0且eligible=true，用户针对当前artifact/fingerprints明确批准；否则blocking，不activation。</acceptance_criteria>
  <done>第二级facts/hash与activation批准完成，13-05方可开始。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production DB/env/OA → preflight output | 三个权威系统可能漂移，且连接信息/成员数据需要secret-safe处理。 |
| Deploy control → migration/API/OA | 命令顺序可决定旧漏洞是否在切换/回滚窗口重新开放。 |
| Local/candidate evidence → production decision | deterministic测试只证明代码合同；production事实必须由只读preflight和批准后的post-deploy证明。 |
| Documentation → future operators | 错误runbook可能导致跳过CHECK、OA核对或回滚到vulnerable binary。 |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Risk | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T13-06 | HIGH | Elevation of Privilege | historical data / old binary rollback | mitigate | ASVS V4：read-only inventory、0132 validated CHECK、禁止回滚vulnerable binary、用户checkpoint。 |
| T13-13 | HIGH | Tampering | deploy/migration order | mitigate | deploy tests固定preflight→approval→API quiesce→migration/CHECK→safe binary→smoke；任一gate失败保持maintenance。 |
| T13-14 | HIGH | Tampering | OA roles vs canonical ACL | mitigate | production只读三方diff、deterministic target/compensation tests、批准后的post-deploy role/session smoke。 |
| T13-15 | HIGH | Repudiation | cleanup/deploy approval | mitigate | dry-run hash/count/member摘要、migration durable audit、明确用户approval与traceable evidence。 |
| T13-16 | HIGH | Information Disclosure | production DB/OA env/token/DSN | mitigate | HTTP双token由0600 env经stdin传递且只访问HTTP；DB/OA由root env加载；artifact仅username hash/tier并有redaction tests。 |
| T13-21 | HIGH | Elevation of Privilege | automatic rollback | mitigate | ACL-safe capability/source+migration fingerprint gate；unsafe/missing previous保持maintenance并forward repair。 |
| T13-17 | MEDIUM | Denial of Service | production smoke | mitigate | bounded existing timeouts、local no-op/compensation tests、专用可逆账号、finally恢复、production写入前checkpoint。 |
| T13-18 | MEDIUM | Tampering | stale docs/mocks/sentinels | mitigate | docs gate、whole-repo sentinel、七类回归和verify.sh all，不以放宽断言消除失败。 |
| T13-19 | MEDIUM | Denial of Service | auth/OA I/O | mitigate | local/candidate provider/OA/DB确定性调用计数；真实latency仅由13-05 production post-deploy实测。 |
</threat_model>

<verification>
1. `bash scripts/verify.sh docs` 与 targeted preflight/deploy/migration tests通过。
2. 七类targeted backend/frontend/Playwright测试按13-VALIDATION高风险顺序通过。
3. whole-repo legacy sentinel仅允许rejection/migration/rollback fixtures与retirement docs命中。
4. local/candidate deterministic session/OA/restore与I/O调用计数完整；不得宣称外部环境pass。
5. 最终 `bash scripts/verify.sh all` 通过。
6. Task4批准后，Task5 `--no-activate`上传/check必须exit0且application/data/OA/root helper不变；失败blocking。
7. Task6只允许approved manual-root同文件系统temp/sha/prevalidate/atomic replace/rollback路径；禁止legacy self-update且runtime-worker helper必须不变。
8. Task7双token经SSH stdin运行remote preflight与remote sha256检查exit0并获第二批准；不得本地direct sudo。
</verification>

<success_criteria>
- 长期文档、代码合同、mocks、deploy runbook对唯一admin、API、CAS/audit/OA/rollback没有漂移。
- production preflight由真实root-owned deploy-control执行、只读、secret-safe、artifact/hash可复核，并通过eligibility对非法admin/env/OA差异fail closed。
- deploy-control在migration前quiesce API；自动rollback只允许ACL-safe fingerprint，无safe previous时maintenance+forward repair。
- local/candidate deterministic七类回归证明代码合同，legacy runtime path清零，verify.sh all通过；外部事实留给production gates。
- filesystem/control-plane bootstrap有独立批准/evidence，首次helper安装不依赖legacy self-update且不触碰runtime-worker helper、不激活app；migration/API/OA/ACL仍被第二checkpoint阻挡。
- 没有新表、worker、outbox/dirty scope、cache、service framework、依赖或其它页面/read-model业务事实变化。
</success_criteria>

## Multi-Source Coverage Audit

| Source | IDs/items | Plans | Status |
| --- | --- | --- | --- |
| GOAL | Phase 13 Settings T0-01完整生产闭环 | 13-01..13-05 | COVERED |
| REQ | PAGE-15, PAGE-04, PAGE-05, PAR-01, PAR-02, PAR-03 | 13-01..13-05 | COVERED |
| CONTEXT | D-01..D-07 trusted admin/API/module boundary | 13-02, 13-03, 13-04 | COVERED |
| CONTEXT | D-08..D-11 persistence/concurrency/audit | 13-01, 13-02, 13-04 | COVERED |
| CONTEXT | D-12..D-14 OA/performance | 13-02, 13-04 | COVERED |
| CONTEXT | D-15..D-16 frontend/legacy deletion | 13-02, 13-03, 13-04 | COVERED |
| CONTEXT | D-17..D-19 production/regression/scope fence | 13-01, 13-04 | COVERED |
| RESEARCH | Reconciliation column caller、Workbench modal、pending fallback、generic writer、env admin遗漏 | 13-01..13-04 | COVERED |
| RESEARCH | 七类测试、高风险顺序、性能I/O预算、production checkpoint | 13-04 | COVERED |
| DEFERRED | 多admin/outbox worker/全面auth优先级重构/巨型service拆分/无关settings | none | EXCLUDED AS DEFERRED |

## Artifacts this phase produces

- Settings、permissions-and-audit、OA integration及安全/API/business-flow长期文档更新。
- secret-safe DB/env/OA production read-only preflight与dry-run cleanup manifest。
- migration/deploy/rollback gate及测试，明确禁止vulnerable binary rollback。
- local/candidate role/session/OA compensation与可测I/O调用计数报告。
- whole-repo legacy sentinel与七类回归/verify.sh all证据。
- 用户明确批准或精确blocking facts的production checkpoint记录。
- `.planning/phases/13-settings-improvements/13-04-SUMMARY.md`。

<output>
Create `.planning/phases/13-settings-improvements/13-04-SUMMARY.md` when done; approval resumes into 13-05, not directly into an unplanned production action.
</output>
