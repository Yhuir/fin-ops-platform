---
phase: 13-settings-improvements
plan: "10"
type: execute
wave: 6
depends_on:
  - "13-08"
files_modified:
  - backend/src/fin_ops_platform/tools/settings_access_control_preflight.py
  - deploy/oa/fin_ops_menu.mysql.sql
  - deploy/oa/fin_ops_role_binding.mysql.sql
  - deploy/oa/fin_ops_user_role_sync.mysql.sql
  - deploy/oa/env/fin-ops.common.env.example
  - deploy/oa/bin/finops-deploy-control.sh
  - deploy/oa/README.md
  - tests/test_settings_access_control_preflight.py
  - tests/test_deploy_oa_script.py
autonomous: true
requirements:
  - PAGE-15
  - PAGE-04
  - PAGE-05
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "D-23/D-24：preflight用固定finops:app:view定位唯一menu，证明三专用role exact set，并生成non-dedicated exact cleanup/rollback证据。"
    - "cleanup只删除approved artifact列出的exact non-dedicated role-menu rows；不删业务role、role members、finops menu或其它menu binding。"
    - "FIN_OPS_ALLOWED_USERNAMES/FIN_OPS_ALLOWED_ROLES/FIN_OPS_READONLY_EXPORT_USERNAMES从APP/deploy authority退休；FIN_OPS_OA_REQUIRED_PERMISSION保留且只允许固定finops:app:view OA selector。"
    - "role sync disabled/missing、menu/role/binding drift、artifact/hash/before-image mismatch均fail closed且零写；rollback可执行并有read-back。"
  artifacts:
    - path: "backend/src/fin_ops_platform/tools/settings_access_control_preflight.py"
      provides: "secret-safe fixed-menu inventory and exact cleanup/rollback manifest"
    - path: "deploy/oa/fin_ops_role_binding.mysql.sql"
      provides: "artifact-bound exact cleanup and rollback without broad deletion"
    - path: "deploy/oa/bin/finops-deploy-control.sh"
      provides: "hash/before-image/selector/role-sync release gate"
  key_links:
    - from: "FIN_OPS_OA_REQUIRED_PERMISSION=finops:app:view"
      to: "settings_access_control_preflight.py"
      via: "fixed OA menu selector only"
    - from: "preflight exact target hashes"
      to: "fin_ops_role_binding.mysql.sql"
      via: "approved before-image and exact `(role_id,menu_id)` rows"
    - from: "13-10-SUMMARY.md"
      to: "13-04-PLAN.md"
      via: "deploy collector/gate contract extended there for candidate and production evidence"
---

<objective>
在现有deploy/preflight边界建立OA menu exact cleanup/rollback与secret-safe evidence，不扩大runtime adapter。

Purpose: 落实 D-23～D-27 并关闭B-02；runtime consistency由13-08负责，本plan只负责deployment inventory、exact mutation与release gate。
Output: focused preflight manifest、exact SQL cleanup/rollback、deploy-control/env/runbook tests。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/13-settings-improvements/13-CONTEXT.md
@.planning/phases/13-settings-improvements/13-RESEARCH.md
@.planning/phases/13-settings-improvements/13-08-SUMMARY.md
@docs/modules/oa-integration/boundary-io.md
@docs/modules/deploy/boundary-io.md
@backend/src/fin_ops_platform/tools/settings_access_control_preflight.py
@deploy/oa/fin_ops_role_binding.mysql.sql
@deploy/oa/bin/finops-deploy-control.sh
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展既有preflight collector为fixed-menu exact inventory</name>
  <files>backend/src/fin_ops_platform/tools/settings_access_control_preflight.py, tests/test_settings_access_control_preflight.py</files>
  <behavior>
    - `FIN_OPS_OA_REQUIRED_PERMISSION`缺失、非`finops:app:view`、menu不唯一、三role/binding缺失、non-dedicated binding或members drift均eligible=false。
    - artifact只输出role/menu ids的salted hashes/counts与exact cleanup/rollback target hashes，不输出username/token/secret。
    - disabled role sync不再match；collector只执行SELECT且复用现有redaction/timeout/artifact机制。
  </behavior>
  <action>按 D-23～D-25 扩展现有collector/report，不建立新工具：先强制OA-only selector精确为`finops:app:view`，再读取menu uniqueness、三role ids、该menu全部bindings与三role members；为non-dedicated rows生成exact `(role_id,menu_id)` before/after/rollback hashes。三项retired APP admission env presence进入blocker；`FIN_OPS_OA_REQUIRED_PERMISSION=finops:app:view`是合法且必需的menu selector，不得被zero-residue guard拒绝。postdeploy fresh token/router流程仍由13-04扩展同一collector、13-05执行，避免重复工具。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_settings_access_control_preflight -v</automated></verify>
  <acceptance_criteria>selector wrong/missing、disabled/menu/role/binding/drift/redaction fixtures全覆盖；preflight只SELECT且artifact无明文identity/secret。</acceptance_criteria>
  <done>release gate可在零写入前精确识别允许cleanup的bindings。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立artifact-bound non-dedicated binding cleanup与rollback SQL</name>
  <files>deploy/oa/fin_ops_menu.mysql.sql, deploy/oa/fin_ops_role_binding.mysql.sql, deploy/oa/fin_ops_user_role_sync.mysql.sql, tests/test_deploy_oa_script.py</files>
  <behavior>
    - cleanup只删除approved artifact列出的exact non-dedicated `(role_id,menu_id)` rows；没有broad DELETE或role-name pattern deletion。
    - rollback只恢复before-image中由本operation删除且仍满足precondition的exact rows；drift零写并block。
    - menu/三dedicated role/member、业务role/member及其它menu binding fingerprints前后不变。
  </behavior>
  <action>按 D-24、D-25 扩展既有SQL：menu脚本以固定`finops:app:view`拒绝重复perms；role-binding脚本消费artifact-bound exact targets并输出对称rollback INSERT；user-role脚本仍只管理三dedicated role members。禁止删除role、role members、menu或其它binding。扩展既有deploy test以fixture证明exact target、before-image drift、rollback/read-back与所有非目标fingerprint不变。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_deploy_oa_script -v</automated></verify>
  <acceptance_criteria>approved exact rows才可删除/恢复；drift零写，SQL中无宽删，业务role/member与其它menu不变。</acceptance_criteria>
  <done>历史non-dedicated menu binding可控清理且可回滚。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 把fixed selector、retired admission env与exact artifact接入deploy gate</name>
  <files>deploy/oa/env/fin-ops.common.env.example, deploy/oa/bin/finops-deploy-control.sh, deploy/oa/README.md, tests/test_deploy_oa_script.py, tests/test_settings_access_control_preflight.py</files>
  <behavior>
    - production require role sync enabled/configured，且`FIN_OPS_OA_REQUIRED_PERMISSION`必须存在并精确为`finops:app:view`。
    - 三项retired APP admission env从example/required-key/fallback删除且presence阻断release；fixed selector不属于retired env。
    - deploy-control校验artifact sha、current before-image、三dedicated exact set和rollback payload后才写；失败/漂移零写。
  </behavior>
  <action>按 D-20、D-24、D-25、D-27 扩展现有deploy-control而不新增迁移框架：保留`FIN_OPS_OA_REQUIRED_PERMISSION=finops:app:view`作为OA menu selector并拒绝覆盖成其它值；删除三项allowed/read-only env的APP/deploy authority与示例。release gate只消费Task1 artifact和Task2 exact SQL，验证hash/current before-image后执行与read-back；失败或rollback失败保持blocking。保留现有preflight/compensation/cutover和runtime-worker helper边界。</action>
  <verify><automated>PYTHONPATH=backend/src python3 -m unittest tests.test_deploy_oa_script tests.test_settings_access_control_preflight -v &amp;&amp; bash scripts/verify.sh lint</automated></verify>
  <acceptance_criteria>fixed selector、三retired env、disabled/missing、artifact drift、exact cleanup/rollback tests全绿；无新增工具/framework/runtime抽象。</acceptance_criteria>
  <done>deployment边界区分合法menu selector与被删除的APP authorization fallback。</done>
</task>

</tasks>

<docs_impact>
同步`deploy/oa/README.md`操作合同；13-04随后更新SECURITY、OA architecture及settings/permissions/OA/app-shell/deploy长期边界。
</docs_impact>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| OA MySQL → preflight artifact | external role/menu facts必须只读、脱敏、hash-bound。 |
| Preflight artifact → cleanup SQL | exact target与before-image决定唯一允许删除的rows。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T13-42 | Elevation of Privilege | selector误接APP auth | mitigate | OA-only fixed marker、evaluator zero-reference test。 |
| T13-43 | Tampering | broad cleanup/rollback | mitigate | exact targets、before-image/hash/precondition、non-target fingerprints。 |
| T13-44 | Information Disclosure | evidence artifact | mitigate | salted hashes/counts、existing redaction、无identity/token/DSN。 |
| T13-SC | Tampering | package installs | mitigate | no package installs。 |
</threat_model>

<verification>
三个targeted commands通过；files_modified为9，runtime projection实现不在本plan。
</verification>

<success_criteria>
- 固定`finops:app:view`保留为OA menu selector且不授权APP。
- exact cleanup/rollback不触碰业务role/member、finops menu或其它menu。
- disabled/missing/drift/hash mismatch全部fail closed、零写。
- 仅三项APP admission env退休，无新framework/runtime抽象。
</success_criteria>

<output>Create `.planning/phases/13-settings-improvements/13-10-SUMMARY.md`.</output>
