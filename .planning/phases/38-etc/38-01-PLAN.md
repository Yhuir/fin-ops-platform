---
phase: 38-etc
plan: "01"
type: execute
wave: 1
depends_on:
  - 37-01
files_modified:
  - web/src/pages/EtcTicketManagementPage.tsx
  - web/src/features/etc/EtcBatchProgress.tsx
  - web/src/app/styles.css
  - web/src/test/EtcTicketManagementPage.test.tsx
  - web/e2e/etc-tickets-flow.spec.ts
  - docs/modules/etc-tickets/README.md
  - docs/modules/etc-tickets/boundary-io.md
  - docs/modules/etc-tickets/state-machine.md
  - docs/modules/etc-tickets/tests.md
  - docs/modules/etc-tickets/implementation-notes.md
  - docs/modules/etc-tickets/e2e-spec.md
  - docs/modules/etc-tickets/e2e-coverage.md
  - docs/product-specs/imports-and-etc.md
  - docs/dev/api-contracts.md
  - .planning/phases/38-etc/38-01-SUMMARY.md
  - .planning/phases/38-etc/38-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "ETC 页面使用左批次 rail 与右侧连续工作面，车牌/关键词前端链路和重复卡片 CSS 被删除。"
    - "四阶段生命周期只从现有 batch/task/import/OA 事实派生，不新增业务状态、API、read model、worker、缓存或依赖。"
    - "所有现有上传、核对、导入、OA、权限、错误、删除与竞态保护行为保持，独立导入页和 Workbench ETC summary 无回归。"
    - "本地全门禁、remote main、部署与生产性能/链路验证闭环通过。"
---

<objective>
完成 ETC 票据页面扁平化、可信流程摘要、页面旧筛选/卡片链路清理、跨页回归和生产发布验证闭环。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化四阶段展示合同并删除旧筛选链路</name>
  <files>web/src/features/etc/EtcBatchProgress.tsx, web/src/pages/EtcTicketManagementPage.tsx, web/src/test/EtcTicketManagementPage.test.tsx</files>
  <action>以一个 ETC 域内只读组件承载 O(1) 状态映射和语义化 ol；覆盖完成、当前、处理中、人工确认、阻塞、失败与未知组合。删除 plate/keyword state、DOM、请求参数、effect 依赖与页面测试，不删除后端/API client 的正式可选参数合同。保留 batch detail/task 并发加载、AbortController 和 stale selection 防护。</action>
  <verify>npm --prefix web test -- --run src/test/EtcTicketManagementPage.test.tsx src/test/EtcApi.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 压平 ETC 工作台并清理重复 CSS</name>
  <files>web/src/pages/EtcTicketManagementPage.tsx, web/src/app/styles.css, web/src/test/EtcTicketManagementPage.test.tsx</files>
  <action>将三 bucket 归入左侧列表，将批次上下文、流程摘要、指标条、当前任务和辅助详情组织为一个连续工作面；删除重复标题、状态和外层卡片，保留 dropzone、异常、表格与 dialog 边界。合并 ETC 重复 selector，删除旧筛选/card contract、侧边粗色条和被覆盖规则；落实单一纵向滚动、sticky 表头、WCAG AA、键盘与窄屏阶段摘要，不新增布局动画或依赖。</action>
  <verify>npm --prefix web test -- --run src/test/EtcTicketManagementPage.test.tsx &amp;&amp; npm --prefix web run build</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ETC/导入/Workbench 回归与文档闭环</name>
  <files>web/e2e/etc-tickets-flow.spec.ts, docs/modules/etc-tickets/README.md, docs/modules/etc-tickets/boundary-io.md, docs/modules/etc-tickets/state-machine.md, docs/modules/etc-tickets/tests.md, docs/modules/etc-tickets/implementation-notes.md, docs/modules/etc-tickets/e2e-spec.md, docs/modules/etc-tickets/e2e-coverage.md, docs/product-specs/imports-and-etc.md, docs/dev/api-contracts.md</files>
  <action>覆盖四阶段可见状态、三 bucket、无旧筛选、独立导入入口、权限/loading/empty/error、reopen、导入失败/not_submitted/OA 人工确认、批次切换竞态和窄屏可访问性。回归导入 worker 与 Workbench ETC summary，纠正文档的两 bucket 漂移，更新页面输入 I/O 与测试矩阵。完成 whole-repo legacy scan、全前端/build/lint/docs/e2e 门禁。</action>
  <verify>bash scripts/verify.sh frontend &amp;&amp; bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh docs &amp;&amp; npm --prefix web run e2e -- e2e/etc-tickets-flow.spec.ts e2e/imports-etc-invoices-flow.spec.ts --project=chromium</verify>
</task>

<task type="auto">
  <name>Task 4: main 发布与生产性能/链路闭环</name>
  <files>.planning/phases/38-etc/38-01-SUMMARY.md, .planning/phases/38-etc/38-VERIFICATION.md</files>
  <action>完成代码审阅和 phase verification 后提交全部授权变更并 push origin/main，使用 ./scripts/deploy-oa.sh 发布。通过安全加载的生产管理员会话验证三 bucket、批次详情、四阶段、独立导入入口、OA 状态、跨页 shell/Workbench summary、零 stepper I/O、请求计数、console、CLS、long task、健康/队列/worker 与回滚锚点。</action>
  <verify>scripts/with-production-admin-token.sh ./scripts/deploy-oa.sh</verify>
</task>

</tasks>

<success_criteria>
- ETC 页面结构、流程语义、异常/回退状态和无旧筛选合同满足已批准设计。
- 新增依赖/API/read model/worker/stepper 请求为 0，后端业务和导入边界不变。
- 组件、API、E2E、跨页、build、lint、docs 与生产性能/健康验证全部通过。
- 本地 main、origin/main 和生产部署 SHA 一致。
</success_criteria>
