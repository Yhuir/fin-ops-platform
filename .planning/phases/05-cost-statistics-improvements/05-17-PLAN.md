---
phase: 05-cost-statistics-improvements
plan: 17
status: passed
created_at: 2026-07-16
completed_at: 2026-07-16
next_state_on_pass: IMPLEMENTING
next_state_on_partial: GAP_CLOSURE
next_state_on_failure: GAP_CLOSURE
requirements:
  - COST-PERF-01
  - COST-AUDIT-01
  - COST-ISOLATION-01
  - COST-LEGACY-01
---

# 05-17 Bounded Execution Prompt：成本 Audit v9 结构化 bank-flow 单读链路

## 本轮唯一目标

删除成本统计 Audit owner 中全部 3 处 `cost_statistics_read_models.payload.bank_flow_time_rows` 旧读取，改为只读
`read_model.cost_statistics_bank_flow_rows` 的结构化字段。保持既有 issue code/message/details 类别、sample limit、caller-owned
repeatable-read snapshot、Audit report envelope 和包含 active relation 的固定 26 条 query budget不变。不得引入双读、fallback、新抽象、
schema/index、缓存或其他页面改动。

## Grill-me 门禁答案

| 问题 | 本轮答案 |
| --- | --- |
| 根因 | migration 0107 已把 bank-flow detail发布到结构化表，父 JSON又在持久化前删除该数组；但 canonical expected-set、bank-flow关键字段和summary重算仍展开已禁止的旧数组，可能把真实结构化行误判为空并浪费JSON解析。 |
| 直接模块 | 仅成本统计 Audit repository、它的直接测试和成本模块维护文档。 |
| 输入 I/O | caller-owned只读数据库snapshot、`tenant_id`、`example_limit + 1`；结构化表提供month/project scope、identity、amount、direction、展示与标签字段。 |
| 输出 I/O | 外部仍是原有三类blocking `AuditIssue`及相同report envelope；不向API/页面泄漏表结构。 |
| 事实源 | canonical bank仍是`app.bank_transactions`，标签/银行详情仍是`read_model.bank_detail_rows`，投影事实只来自`read_model.cost_statistics_bank_flow_rows`。 |
| 旧链路 | 删除成本Audit文件中全部`payload->...->'bank_flow_time_rows'`和对应`jsonb_array_elements`；禁止保留兼容分支。发布时从构建payload写结构化行不属于读取旧链路，本轮不改。 |
| 父范围 | 结构化表只存具体月；summary Audit以month rows本身加`project_scope || ':all'`逻辑rollup重算父范围，和已有cost rows合同一致。 |
| 隔离性 | 不改共享page Audit、Workbench/Bank Detail owner、成本query API、read-model发布、worker、前端、其他页面read model或全局pool。 |
| 反过度设计 | 直接改3段既有SQL；不加repository adapter、mapper class、feature flag、migration、临时表、并行连接或generic proof framework。 |
| 回滚 | 协调部署前为纯代码/测试/文档变更，可按本轮diff回退；无数据和schema回滚。 |
| 部署 | 本轮不部署、不访问生产、不运行生产性能验证；不branch/stage/commit/push/PR/stash/reset/clean。 |

## 允许修改范围

- 本计划与`05-17-SUMMARY.md`；
- `backend/src/fin_ops_platform/services/postgres_repositories/cost_statistics_page_audit.py`；
- `tests/test_cost_statistics_page_audit.py`与直接断言成本Audit SQL的
  `tests/test_audit_page_business_read_model_tool.py`、直接执行成本 owner 的 System Audit PostgreSQL fixture；
- 成本统计README、boundary I/O、tests、implementation notes与唯一性能/freshness/遮罩设计文档。

禁止修改数据库migration/schema/index、read-model发布/query repository、Workbench/Bank Detail proof owner、共享Audit、API、worker、前端、
部署文件、连接池和其他页面代码。

## 执行任务

<tasks>

<task id="1" type="auto">
  <name>把三类成本 Audit proof切到v9结构化bank-flow事实</name>
  <read_first>
    backend/src/fin_ops_platform/postgres/migrations/0107_cost_statistics_structured_bank_flow_rows.sql
    backend/src/fin_ops_platform/services/postgres_repositories/cost_statistics_page_audit.py
    backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
  </read_first>
  <action>
    1. canonical expected-set的projected bank-flow按`project_scope='all'`从结构化表按month/transaction聚合count与`abs(amount)`；numeric非空字段不再做字符串解析。
    2. bank-flow key/display proof直接比较结构化列与canonical bank/bank-detail，不以父JSON或row payload作为投影事实；details仍提供可诊断的projected fields。
    3. summary重算先构造month rows与父scope rollup，再按scope聚合row/expense/income count和amount；空scope用left join得到0，避免`count(*)`空行误计1。
    4. 保留原check marker、issue code、limit、tenant join、排序、只读行为和统一business-value statement。
  </action>
  <acceptance_criteria>
    - 成本Audit文件对`bank_flow_time_rows`和对应旧JSON展开为0次引用。
    - 三个proof都显式读取`read_model.cost_statistics_bank_flow_rows`。
    - query budget仍精确为26，三类issue合同与其他proof不变。
  </acceptance_criteria>
  <verify>
    PYTHONPATH=backend/src:tests python3 -m unittest tests.test_cost_statistics_page_audit tests.test_audit_page_business_read_model_tool
  </verify>
</task>

<task id="2" type="auto">
  <name>锁定旧路径零残留、结构化SQL与跨页面隔离回归</name>
  <read_first>
    tests/test_cost_statistics_page_audit.py
    tests/test_audit_page_business_read_model_tool.py
    docs/modules/cost-statistics/tests.md
  </read_first>
  <action>
    增加针对三类marker的结构化表/父rollup/零旧JSON断言；运行成本Audit、通用Audit、operations/System分派、repository boundary、成本API/SQL runtime、lint/docs和diff检查。只修复本轮直接引入的失败。
  </action>
  <acceptance_criteria>
    - 测试机械证明成本Audit不再读取旧数组且仍从唯一结构化表读取。
    - 其他页面owner/read model零代码diff，所有相关回归通过。
    - 无生产访问、部署或Git写操作。
  </acceptance_criteria>
  <verify>
    rg -n "bank_flow_time_rows" backend/src/fin_ops_platform/services/postgres_repositories/cost_statistics_page_audit.py
    bash scripts/verify.sh lint
    bash scripts/verify.sh docs
    git diff --check
  </verify>
</task>

</tasks>

## 七类测试责任

1. Business core unit：适用；canonical exact-set、字段映射和summary count/amount/direction语义。
2. Service-layer：适用；唯一结构化I/O、只读snapshot、26 query budget和无fallback。
3. API contract：response shape未改；复跑page/operations/System Audit envelope和成本API回归。
4. Read model/cache/background job：适用；只读已发布结构化行，父scope为逻辑rollup，不写、不enqueue、不改worker。
5. Frontend interaction：不适用；页面、Audit icon和轻量遮罩未修改。
6. End-to-end integration：适用；page Audit、CLI、operations/System本地分派回归；真实PostgreSQL/浏览器留到统一部署后。
7. Existing regression：适用；Workbench/Bank Detail dependency proof、共享Audit、成本API/SQL runtime和其他页面零diff。

## 停止条件与状态转换

- PASS：3处旧读全部删除、结构化SQL与测试闭环、相关回归通过；写`05-17-SUMMARY.md`，next_state=`IMPLEMENTING`。
- PARTIAL：结构化列不能保持某项现有证明；不得恢复双读，进入`GAP_CLOSURE`并记录缺失契约。
- FAILED：必须修改schema、发布链路或其他页面才能正确实现；进入`DECISION_REQUIRED`。
- 无论结果如何，整体`/goal`保持active和`DEPLOYMENT_HOLD`。
