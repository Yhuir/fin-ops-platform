---
phase: 39-runtime-worker-topology-convergence
plan: "01"
type: execute
wave: 1
depends_on:
  - 38-01
files_modified:
  - backend/src/fin_ops_platform/services/runtime_worker_registry.py
  - backend/src/fin_ops_platform/services/read_model_manifest.py
  - backend/src/fin_ops_platform/services/app_status_read_model_registry.py
  - backend/src/fin_ops_platform/app/worker.py
  - backend/src/fin_ops_platform/app/server.py
  - backend/src/fin_ops_platform/services/no_oa_bank_batch_application_service.py
  - deploy/oa/env/
  - scripts/backfill-runtime-read-models.py
  - tests/
  - docs/modules/runtime-workers/
  - docs/modules/read-models/
  - docs/modules/search/
  - docs/modules/no-oa-bank-batches/
  - docs/operations/runtime-worker-governance.md
  - .planning/phases/39-runtime-worker-topology-convergence/39-01-SUMMARY.md
  - .planning/phases/39-runtime-worker-topology-convergence/39-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Required runtime instances converge from 11 to exactly 6: oa-sync, workbench-matching, workbench, workbench-relation, import and settings-maintenance."
    - "Search API/projection/workers are deleted because the application has no Search UI or registered consumer; PostgreSQL canonical page queries remain unchanged."
    - "No-OA canonical relation owner and Workbench internal-transfer commands remain, while its legacy list API reads current canonical facts synchronously and its read model/worker/refresh lifecycle is deleted."
    - "Workbench active-generation, shared relation distribution, imports, OA sync and durable settings maintenance keep their existing correctness and retry contracts."
    - "Registry, manifest, RabbitMQ topology, deploy env, App Health, maintenance tools, tests and docs expose one exact topology with no legacy worker or event path."
    - "Local gates, origin/main, deployment and production latency/queue/freshness/business-chain evidence close the change without a new dependency or abstraction."
---

<objective>
把已退役页面留下的 Search、No-OA 派生运行时和纯容量副本从生产拓扑中删除，在不重写业务 owner 的前提下将 required worker 从 11 个收敛为 6 个，并完成测试、运维、发布和生产性能闭环。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 删除容量副本和无消费者 Search 运行时</name>
  <files>runtime worker/read-model registries, Search route/service/projection/worker files, deploy env, scripts, tests, docs</files>
  <action>删除 workbench-secondary、search-secondary、search-tertiary；删除没有前端 route 或登记消费者的 /api/search、Search manifest/scope/worker/projection/refresh/source-version/health/tool 链及专属 env。保留各业务页面已有 PostgreSQL canonical 查询和索引，不引入替代 Search 服务、兼容 route 或隐藏 fallback。</action>
  <verify>目标 Search/registry/manifest/deploy/architecture tests；whole-repo legacy symbol/text scan；lint。</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: No-OA 保留事实 owner、删除派生 read model</name>
  <files>no_oa_bank_batch application/read-model/runtime files, server/worker wiring, lifecycle, state store adapters, tests, docs</files>
  <action>复用 NoOaBankBatchApplicationService 已有 canonical bank/category/active-relation 构建能力，让 legacy list route 每次请求从精确 canonical scope 构建并分页；保留 submit/withdraw、Workbench internal-transfer、idempotency、事务、审计和 app.no_oa_bank_batches facts。删除 read_model.no_oa runtime reader/writer、refresh producer/event/scope/worker/App Status/lifecycle wiring，不删除历史 migration/table。禁止新增 cache、queue、projection 或并行兼容路径。</action>
  <verify>No-OA service/API/Workbench integration、write rollback/idempotency、direct-canonical negative guards、性能 fixture。</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 对齐六 Worker 单一事实源与运维闭环</name>
  <files>registry/manifest/systemd/env/RabbitMQ/backfill/App Health/docs/tests</files>
  <action>让 registry 成为六个 required instance 的唯一来源；RabbitMQ 只发布仍有 eligible consumer 的 event；ensure-runtime-workers 在发布时停用未知旧实例。App Health、System Audit、reset lifecycle、backfill/rehydrate 和运维文档只报告仍存在的 read models。完成全仓公共符号扫描，删除所有会继续启动/入队/观测已退役 Search/No-OA runtime 的旧代码。</action>
  <verify>registry/manifest/RabbitMQ/deploy/health/lifecycle tests；infra smoke；docs；git diff --check。</verify>
</task>

<task type="auto">
  <name>Task 4: main 发布与生产数据、性能和回滚闭环</name>
  <files>39-01-SUMMARY.md, 39-VERIFICATION.md, STATE.md, ROADMAP.md</files>
  <action>全量 verify 和独立架构审阅通过后原子提交全部授权变更并 push origin/main；使用 ./scripts/deploy-oa.sh 发布。验证精确六 Worker、旧 unit 停用、RabbitMQ/PG durable queue drain、剩余 read model fresh、No-OA canonical list/detail 和 Workbench 内部转账只读链、16 页面 System Audit、API p95/错误率/线程与 DB pool 指标；保留 previous release 回滚锚点。</action>
  <verify>scripts/with-production-admin-token.sh ./scripts/deploy-oa.sh；T+0/T+60/T+300 evidence；authenticated production probes。</verify>
</task>

</tasks>

<success_criteria>
- required worker 从 11 个降为精确 6 个，Search/No-OA 派生 runtime 与三个副本没有代码、注册、env、queue 或 health 残留。
- No-OA 正式关系和 Workbench 内部转账功能保持，list API 使用同请求 canonical snapshot 且暖读 p95 小于 1 秒。
- Workbench、relation、OA、import、settings maintenance 的一致性、权限、审计、失败恢复和回滚门禁不变。
- 全量本地门禁、remote main、精确生产 release、T+300 稳定性和关键链路验证全部通过。
</success_criteria>
