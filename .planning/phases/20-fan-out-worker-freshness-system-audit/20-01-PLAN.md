---
phase: 20-fan-out-worker-freshness-system-audit
plan: 01
type: execute
wave: 1
depends_on:
  - 19-21
autonomous: false
requirements:
  - RELCL-01
  - RELCL-02
  - RELCL-03
  - RELCL-04
  - RELCL-05
  - RELCL-06
  - RELCL-07
files_modified:
  - backend/src/fin_ops_platform/tools/write_operation_e2e_smoke.py
  - backend/src/fin_ops_platform/tools/write_operation_slo_audit.py
  - backend/src/fin_ops_platform/tools/write_operation_scenario_discovery.py
  - tests/test_write_operation_e2e_smoke.py
  - tests/test_write_operation_slo_audit.py
  - tests/test_write_operation_scenario_discovery.py
  - tests/test_write_operation_impact_matrix.py
  - tests/test_reversible_relation_closure_postgres.py
  - docs/dev/write-operation-impact-matrix.json
  - docs/operations/monitoring.md
  - docs/operations/runtime-worker-governance.md
  - docs/modules/workbench-relations/tests.md
  - docs/modules/workbench-relations/e2e-coverage.md
  - docs/modules/read-models/implementation-notes.md
  - docs/dev/testing-closure-state.md
---

<objective>
把现有 `write_operation_e2e_smoke` 扩展为唯一可逆关系 closure runner：同一 scenario 内每个 confirm/withdraw checkpoint 都独立绑定 mutation、required fan-out、worker/durable queue、freshness、受影响页面 API 和新的只读 System Audit。用 bank+invoice、bank+turnover、bank+OA+invoice 三种关系形状证明正反向闭环；不新增第二套 relation/queue/freshness/Audit owner，不做 17×operation，并删除迁移后无 caller 的旧一次性 execution branch和伪闭环逻辑。
</objective>

<grill_gate>

- Canonical write 按 mutation contract 只能走两条正式边界：Workbench action API → `WorkbenchRelationCommandService` → `WorkbenchWriteUnitOfWork`，或 turnover closure API → `TurnoverLedgerWriteFacade` → `TurnoverLedgerWriteUnitOfWork`；测试不得直接写 relation/read-model/outbox/dirty/readiness。
- Runner 复用 `write_operation_slo_audit`、`http_slo_probe` 和 System Audit HTTP owner；可执行 shape/consumer 合同必须位于 backend release 内，operation impact matrix 作为测试约束的文档镜像。禁止运行时读取未打包的 `docs/`，也禁止复制SQL、worker、freshness或Audit算法。
- 每个 checkpoint在自己的数据库 started_at之后证明全部 required expectations；confirm与withdraw不得共享最终一次SLO/Audit。
- 每个 checkpoint 的正式写请求必须携带本场景唯一 `idempotency_key`。HTTP 成功后，通过 `write_operation_slo_audit` 内唯一只读 PostgreSQL evidence owner 从 `app.workbench_idempotency_records` 的 committed record 精确解析非空 `outbox_event_ids`；不把内部 event ID 暴露到业务 HTTP DTO。SLO 查询只能接受这些精确事件 ID；`started_at + profile` 仅用于时间窗/防御性过滤，不能作为归属证明。报告列出逐 expectation 命中的 event ID/scope。缺 required expectation、event ID 不属于本 checkpoint、或 scope 不在正式 required/optional impact contract 中才 fail closed；属于同一精确 ID 集且被合同允许的 optional scope只记录、不失败。
- System Audit必须解析并校验同一新报告的system audit id、snapshot、contract、17/16 page counts、integrity/freshness/queue；external unknown保持允许且不扩大声明。
- 首次 mutation 前必须先用固定 admin-only System Audit endpoint 做只读 preflight；scenario 不得覆盖 Audit path。affected consumer 必须绑定 fixture row/case identity，non-consumer 必须比较 mutation 前后 fresh baseline，不能用任意 `fresh` 断言冒充内容证明。
- 每个 affected consumer 必须同时通过 freshness gate 和最小 typed business assertion；断言只支持明确的 JSON Pointer `equals`/`contains` 合同，并由 scenario 对经过过滤的确定性 API payload 指定，不引入第二套页面业务算法。
- 新可逆scenario必须声明test-owned fixture、shape、cleanup=withdraw和approval；不得由discovery自动选择普通业务对象执行confirm。
- confirm 已提交而其后 closure gate 失败时，原失败结论保持不变，但 runner 必须进入 recovery cleanup：使用正式 preview + expected_versions 的 withdraw checkpoint 清理 test-owned relation；清理失败输出不含敏感数据的 recovery-required artifact。网络结果不明确时禁止盲重试，只有读回 canonical postcondition 证明 relation active 后才允许清理。
- 保留的legacy scenario输入只映射为一个checkpoint并走同一执行函数；迁移后不存在第二条运行分支。
- 真实PostgreSQL测试只允许显式disposable test DB；缺外部环境结构化skip/input-required，不得连接生产或伪造通过。

</grill_gate>

<tasks>

<task type="tdd">
  <name>Task 1: Lock checkpoint, System Audit, and fail-closed contracts with tests</name>
  <files>tests/test_write_operation_e2e_smoke.py, tests/test_write_operation_slo_audit.py, tests/test_write_operation_impact_matrix.py</files>
  <action>先增加失败测试：legacy scenario被规范化为单checkpoint；checkpoint缺operations/steps、可逆场景缺test-owned fixture或cleanup被拒绝；confirm和withdraw各自取得独立started_at并分别等待SLO；mutation缺唯一`idempotency_key`被拒绝；只读evidence owner遇到record缺失/非committed/多行/空或重复`outbox_event_ids`被拒绝，SLO查询按精确event IDs匹配且报告逐expectation命中的event ID/scope，同profile并发的其他事件不能满足本checkpoint；合法optional scope记录但不失败，未知extra scope失败；checkpoint写失败跳过其post API/Audit并停止后续正常mutation；required scope timeout失败；post API non-fresh失败；每个affected consumer缺typed assertion、JSON Pointer不存在、equals/contains不符均失败，non-consumer isolation保持不变；System Audit非JSON/403/contract缺失/snapshot缺失/16页未全过/integrity或freshness或queue非绿色均失败；external unknown允许；两个checkpoint必须产生不同非空system_audit_id。另覆盖confirm提交后SLO/consumer/Audit任一失败仍执行正式recovery withdraw、保留原失败、记录cleanup结果；ambiguous write没有canonical active postcondition时绝不重试/清理。测试request/connection只验证orchestration，不冒充真实worker。</action>
  <verify>PYTHONPATH=backend/src python3 -m unittest tests.test_write_operation_e2e_smoke tests.test_write_operation_impact_matrix -v</verify>
  <acceptance_criteria>新测试在实现前能暴露当前runner只做scenario级一次SLO且不验证Audit；实现后全部通过，无放宽/skip。</acceptance_criteria>
</task>

<task type="implementation">
  <name>Task 2: Implement one checkpoint runner and remove duplicate execution flow</name>
  <files>backend/src/fin_ops_platform/tools/write_operation_e2e_smoke.py, backend/src/fin_ops_platform/tools/write_operation_slo_audit.py, tests/test_write_operation_slo_audit.py</files>
  <action>用最小新dataclass/schema表示ordered checkpoints、typed consumer assertions和可选system_audit gate；legacy operations/steps/post_api输入只在load阶段规范化为单checkpoint。`_run_one_scenario`只循环一个`_run_checkpoint` owner：checkpoint started_at → 动态step执行/必要的最小response capture与typed placeholder解析 → 从mutation body取得唯一idempotency key并调用只读evidence owner解析committed record的精确`outbox_event_ids` → 按event IDs执行write SLO → post API freshness + JSON Pointer business assertions → system audit。给`write_operation_slo_audit`增加`committed_workbench_outbox_event_ids(...)`单一只读owner，并给`recent_read_model_refresh_events_since`增加可选event ID边界及匹配证据；不复制SQL到runner，不改变业务HTTP DTO。System Audit validator只解释HTTP JSON合同，不访问DB或复制Audit逻辑；报告不输出token、body或敏感capture。正常checkpoint失败停止后续业务mutation；如果已确认committed，则在finally调用同一个checkpoint owner执行预先声明的正式preview/version-bound recovery withdraw，保持原失败并记录cleanup/recovery_required。ambiguous mutation只有scenario声明的canonical read-back step证明active后才可清理，禁止盲重试。删除旧scenario级steps/SLO/post API重复branch与失去caller的helper。</action>
  <verify>PYTHONPATH=backend/src python3 -m unittest tests.test_write_operation_e2e_smoke tests.test_runtime_sync_closure_gate tests.test_slo_tool_defaults -v</verify>
  <acceptance_criteria>所有新旧scenario都只经过一个checkpoint执行路径；confirm/withdraw安全传递preview/version/response identity并只消费本次精确outbox events；已提交confirm在验证失败后可恢复，失败清理明确标记人工恢复；无direct SQL/read-model/queue/Audit写I/O。</acceptance_criteria>
</task>

<task type="implementation">
  <name>Task 3: Register the three reversible shapes and safe scenario boundary</name>
  <files>backend/src/fin_ops_platform/tools/write_operation_scenario_discovery.py, tests/test_write_operation_scenario_discovery.py, docs/dev/write-operation-impact-matrix.json, tests/test_write_operation_impact_matrix.py</files>
  <action>复用 SLO registry，并在 backend runner 内增加随 release 发布的最小 shape/consumer runtime contract；impact matrix 作为测试机械约束的文档镜像。精确登记三对：`workbench_relation_confirm_bank_invoice_cross_page`/`workbench_relation_withdraw_bank_invoice_cross_page`、`turnover_relation_confirm_cross_page`/`turnover_relation_withdraw_cross_page`、`workbench_relation_confirm_cross_page`/`workbench_relation_withdraw_cross_page`。bank+invoice/full 使用正式 Workbench preview/command；bank+turnover 使用正式 turnover closure confirm 与 relation-id withdraw，不伪装成 Workbench endpoint。为每个方向列出affected consumers及确定性过滤API的 JSON Pointer 断言，同时列出至少一个non-consumer isolation baseline；断言只能命中正式响应的业务根，不能以 freshness 元数据代替业务证明。三shape数据驱动复用同一执行器，不做17×operation。scenario schema要求fixture ownership=test_owned、bounded row ids、confirm+withdraw checkpoints、每个mutation唯一idempotency key和cleanup contract；discovery只验证/输出安全候选上下文，不自动对普通业务对象生成confirm apply。删除不对称 `workbench_relation_*_bank_turnover_cross_page` 旧 profile 和无 caller builder。增加whole-repo guard确保runtime registry与文档镜像、mutation contract、relation fact sources、target scopes/pages和scenario assertions双向一致。</action>
  <verify>PYTHONPATH=backend/src python3 -m unittest tests.test_write_operation_scenario_discovery tests.test_write_operation_impact_matrix tests.test_write_operation_slo_audit -v</verify>
  <acceptance_criteria>三种shape各有一对严格profiles和可审核scenario合同；没有任意生产fixture自动创建、旧API回退或重复impact registry。</acceptance_criteria>
</task>

<task type="integration">
  <name>Task 4: Prove reversible closure on disposable PostgreSQL and negative worker/freshness paths</name>
  <files>tests/test_reversible_relation_closure_postgres.py, tests/test_write_operation_e2e_smoke.py</files>
  <action>采用两层真实证据，避免为单测搭建 disposable Mongo/全 worker topology：opt-in PostgreSQL 层分别使用 `WorkbenchWriteUnitOfWork` 与 `TurnoverLedgerWriteUnitOfWork`，证明三组 profile 六 checkpoint 的 committed idempotency/exact durable event correlation、pending fail-closed、真实 queue completion 与同 profile 并发隔离；正式 HTTP runner/staging apply 层负责 canonical mutation、真实 workers、affected consumer、non-consumer baseline 和 System Audit。现有 Application/API/Browser integration 继续保护业务关系内容与用户可见结果，不把 mock Browser 冒充 durable worker。若本机没有`FIN_OPS_TEST_DATABASE_URL`，测试明确 SkipTest；有安全本地 disposable PostgreSQL 时必须实际运行并清理。完整 staging/production runtime proof 只有在 test-owned fixture、auth/admin、approval 输入齐备时执行。</action>
  <verify>FIN_OPS_TEST_DATABASE_URL存在时：PYTHONPATH=backend/src python3 -m unittest tests.test_reversible_relation_closure_postgres -v；否则运行bash scripts/verify.sh infra-smoke并记录external input required</verify>
  <acceptance_criteria>三组confirm/withdraw在真实disposable PostgreSQL形成6个独立 exact-event checkpoint；负例在queue/dirty未收敛时失败，完成后通过；测试DB安全guard和cleanup生效。不得把该层描述成页面业务闭环；页面/worker/System Audit runtime proof由受控 runner apply 独立给出。</acceptance_criteria>
</task>

<task type="verification">
  <name>Task 5: Delete retired legacy paths, update long-term contracts, and run full verification</name>
  <files>docs/operations/monitoring.md, docs/operations/runtime-worker-governance.md, docs/modules/workbench-relations/tests.md, docs/modules/workbench-relations/e2e-coverage.md, docs/modules/read-models/implementation-notes.md, docs/dev/testing-closure-state.md</files>
  <action>全仓扫描旧scenario runner、direct relation/read-model/outbox/dirty/readiness test writes、mock-only closure声明和无caller helper。删除与checkpoint owner重复且无正式caller的逻辑；保留项逐一写明owner、I/O、非hot-path和删除条件。更新生产dry-run/apply/runbook、三shape验收、System Audit bounded claim和测试矩阵，并记录七类测试：①business core不改变配对业务规则，N/A，runner schema/断言规则由unit覆盖；②service layer适用，UoW response/evidence与SLO repository边界测试；③API contract适用，mutation evidence、consumer freshness/business JSON、System Audit fail-closed；④read model/worker适用，精确event IDs、dirty→fresh、queue/error/recovery；⑤frontend适用，定向运行`workbench-relation-fanout.spec.ts`、`pending-invoices-fanout.spec.ts`、`input-invoice-relation-fanout.spec.ts`、`cost-statistics-relation-fanout.spec.ts`、`workbench-relations-oa-pending-fanout.spec.ts`、`workbench-relations-tax-offset-isolation.spec.ts`并补withdraw下游断言；⑥E2E适用，三shape×confirm/withdraw六checkpoint；⑦regression适用，legacy单checkpoint、旧API shape、filters/sort/pagination及其他read model非空。运行diff/check、targeted tests、backend/frontend/build/Chromium/docs/lint/infra-smoke；不因外部test DB缺失放宽任何断言。真实staging/production apply只在有test-owned fixture、auth、admin、approval和明确环境时执行。</action>
  <verify>git diff --check；bash scripts/verify.sh lint；bash scripts/verify.sh backend；bash scripts/verify.sh frontend；bash scripts/verify.sh docs；bash scripts/verify.sh infra-smoke；cd web && npm run e2e:smoke</verify>
  <acceptance_criteria>无旧平行execution path或污染I/O；长期文档与代码一致；适用七类测试和全量验证通过；外部环境未运行项精确报告，不冒充闭环。</acceptance_criteria>
</task>

</tasks>

<verification>

- 检查`write_operation_e2e_smoke`只有一个checkpoint执行owner，legacy只在load阶段转换。
- 检查三组shape/profile/impact matrix/scenario合同双向一致。
- 检查System Audit validator fail closed且不复制数据库proof。
- 检查任何测试或runner都不直接写read model、dirty/outbox/readiness或mark-fresh。
- 检查confirm/withdraw分别拥有started_at、event evidence、post API和新System Audit ID。
- 检查每个checkpoint只匹配其唯一idempotency committed record解析出的精确event IDs；同profile并发事件不能串线。
- 检查每个affected consumer有freshness与business content双重断言，non-consumer有隔离断言。
- 检查confirm提交后任一closure gate失败会走正式补偿withdraw；ambiguous写不盲重试，清理失败产出recovery-required证据。
- 检查production apply仍被auth、admin、approval、test-owned fixture和dry-run gate保护。
- 完成七类测试决策和文档影响评估。

</verification>

<success_criteria>

- 三组可逆关系各自以confirm和withdraw两个独立checkpoint形成完整closure，不共享历史绿色证据。
- 每个closure的fan-out证据与本次mutation的精确outbox event IDs绑定，不被同profile并发污染。
- required fan-out、worker drain、freshness、affected-page API和17页System Audit任一失败时场景fail closed。
- affected consumer的实际关系内容和non-consumer隔离均被执行式证明；确认后失败不会静默遗留test-owned active relation。
- canonical relation/UoW、durable queue、freshness和Audit owner边界不变且无污染I/O。
- legacy input仅作为load-time兼容映射；旧重复运行链、伪fresh/helper和错误closure声明被删除或有明确保留owner。
- deterministic、API/service/read-model/worker、Browser regression和opt-in disposable PostgreSQL测试合同完整；没有17×operation重复套件。
- 外部来源完整性继续独立unknown/unproven；本阶段不夸大为真实世界完整性证明。

</success_criteria>
