---
phase: 27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-read-model-fan-out/27-COVERAGE-MATRIX.md
  - docs/modules/permissions-and-audit/write-entry-inventory.md
  - docs/architecture/module-boundaries/read-model-contracts.md
  - docs/app-architecture/pages.md
  - tests/test_permissions_write_entry_inventory.py
  - tests/test_read_model_manifest.py
autonomous: true
requirements: [RMF-01, RMF-05, RMF-06]
---

<objective>
Freeze a mechanically checked, current-repository coverage and operation-classification contract before changing runtime behavior.
</objective>

<threat_model>
- Missing write endpoints can bypass the new contract and silently retain fan-out.
- Misclassifying a strict business mutation as display-only can expose stale financial data.
- Inventory artifacts must never become production I/O or a second runtime registry.
</threat_model>

<tasks>

<task type="execute">
  <name>Build the current page, mutation, Drawer, read-model and legacy-call coverage matrix</name>
  <read_first>
    <file>web/src/app/pageRegistry.tsx</file>
    <file>docs/modules/permissions-and-audit/write-entry-inventory.md</file>
    <file>backend/src/fin_ops_platform/services/read_model_manifest.py</file>
    <file>.planning/phases/00-cross-page-dependency-baseline/PAGE-DEPENDENCY-MATRIX.md</file>
    <file>.planning/phases/00-cross-page-dependency-baseline/LEGACY-ENTRYPOINTS.md</file>
  </read_first>
  <action>
Create `27-COVERAGE-MATRIX.md` from current source, not the stale Phase 0 page list. For every registered page and every mutating frontend API function record the UI operation/Drawer, route, command/query owner, canonical fact or settings owner, operation class, exact scope/version contract, dependent read models, current lifecycle/enqueue/barrier path, replacement behavior, deletion target, tests and production probe id. Include the 15 manifest models and non-page resources `search`, `bank_account_balance`, `workbench_relation`, `invoice_lifecycle` and legacy `no_oa_bank_batch`. Explicitly classify read-only Drawers and prove they have no mutation contract.
  </action>
  <acceptance_criteria>
    <criterion>The matrix lists exactly the current 17 `appPageDefinitions` entries.</criterion>
    <criterion>Every POST/PUT/PATCH/DELETE function under `web/src/features/*/api.ts` is mapped or explicitly classified read-like with evidence.</criterion>
    <criterion>All 15 `READ_MODEL_MANIFEST` keys have query owner, scope and page/resource consumers.</criterion>
    <criterion>Every direct lifecycle/enqueue/barrier caller has `retain`, `migrate`, or `delete` status and an owner.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Turn coverage into a blocking static test without adding runtime architecture</name>
  <read_first>
    <file>tests/test_permissions_write_entry_inventory.py</file>
    <file>tests/test_read_model_manifest.py</file>
    <file>docs/modules/permissions-and-audit/write-entry-inventory.md</file>
  </read_first>
  <action>
Extend the existing inventory tests so page routes, mutating feature APIs, writable Drawer/dynamic opener ids, manifest keys and direct lifecycle/enqueue source sentinels are bidirectionally covered. Keep the implementation as test-time parsing of existing source/docs; do not add a production registry, dependency or code-generation step.
  </action>
  <acceptance_criteria>
    <criterion>Tests fail when a synthetic or fixture mutation/page/Drawer/read-model entry lacks inventory coverage.</criterion>
    <criterion>Tests fail when an inventory row references a removed route, component, API module or manifest key.</criterion>
    <criterion>Coverage output reports zero unmapped pages, mutations, writable Drawers, read models and lifecycle/enqueue sites.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Update long-term architecture facts and performance contracts</name>
  <read_first>
    <file>docs/architecture/module-boundaries/read-model-contracts.md</file>
    <file>docs/app-architecture/pages.md</file>
    <file>docs/app-architecture/runtime-and-ownership.md</file>
    <file>docs/operations/runtime-worker-governance.md</file>
  </read_first>
  <action>
Document the four operation classes, access-time freshness semantics, hidden/visible behavior, strict stale consumers, ordinary-command zero-fan-out rule, exact-scope SLO and explicit full-history exception. Mark current write-time contracts that remain until their vertical slice migrates; do not document the target as already shipped.
  </action>
  <acceptance_criteria>
    <criterion>Docs distinguish current state, target contract and per-slice migration status.</criterion>
    <criterion>Docs do not claim every page has a read model or every Drawer save rebuilds.</criterion>
    <criterion>Docs retain Workbench active-generation exception and PostgreSQL durable queue truth.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `PYTHONPATH=backend/src python3 -m unittest tests.test_permissions_write_entry_inventory tests.test_read_model_manifest -v`
- `bash scripts/verify.sh docs`
</verification>

<success_criteria>
- Zero-unmapped coverage is mechanically enforced before runtime migration.
- No production runtime abstraction is added.
- Current and target contracts are distinguishable and source-grounded.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/27-read-model-fan-out/27-COVERAGE-MATRIX.md`
- Test-time coverage assertions extending the existing inventory and manifest guards.
- No new production runtime symbol.
