---
phase: 27
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - backend/src/fin_ops_platform/services/read_model_query_gateway.py
  - backend/src/fin_ops_platform/services/read_model_refresh_gateway.py
  - backend/src/fin_ops_platform/services/bank_details_application_service.py
  - backend/src/fin_ops_platform/services/bank_detail_category_side_effects.py
  - backend/src/fin_ops_platform/services/app_settings_service.py
  - web/src/pages/BankDetailsPage.tsx
  - web/src/features/bankDetails/AutoTagRulesDrawer.tsx
  - web/src/components/cost-statistics/CostStatisticsTagRulesDrawer.tsx
  - web/src/pages/CostStatisticsPage.tsx
  - tests/test_read_model_query_gateway.py
  - tests/test_bank_details_sql_runtime.py
  - tests/test_app_settings_service.py
  - web/src/test/BankDetailsPage.test.tsx
  - web/src/test/CostStatisticsPage.test.tsx
autonomous: true
requirements: [RMF-02, RMF-03, RMF-04, RMF-05, RMF-08]
---

<objective>
Prove the target architecture end to end with bank-details and cost-statistics, covering canonical fact mutation, semantic rules, read-time rules and explicit batch behavior.
</objective>

<threat_model>
- Rule saves may overwrite concurrent settings or publish unaudited versions.
- Removing fan-out before query expected-version proof exists can leave stale pages permanently fresh-looking.
- Reapply-all and normal save must not share an unbounded path.
</threat_model>

<tasks>

<task type="execute">
  <name>Harden the existing query/refresh gateway for access-time coalescing and strict status</name>
  <read_first>
    <file>backend/src/fin_ops_platform/services/read_model_query_gateway.py</file>
    <file>backend/src/fin_ops_platform/services/read_model_refresh_gateway.py</file>
    <file>backend/src/fin_ops_platform/services/read_model_scope_policy.py</file>
    <file>tests/test_read_model_query_gateway.py</file>
  </read_first>
  <action>
Extend only the existing gateway contracts needed by the slice: expected source-version mismatch must return explicit stale/refreshing and enqueue one normalized current target; fresh must neither enqueue nor read stale cache; missing must return the existing empty/202-compatible result. Reuse durable queue dedupe and do not create a second coordinator or cache.
  </action>
  <acceptance_criteria>
    <criterion>Fresh load performs zero enqueue.</criterion>
    <criterion>Repeated stale loads for the same target create one current-effective queue target.</criterion>
    <criterion>Redis payload is consumed only after the current request freshness proof passes.</criterion>
    <criterion>Invalid or bare-all ordinary scope fails validation.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Migrate bank category and auto-tag rule saves to fast canonical writes plus access-time refresh</name>
  <read_first>
    <file>backend/src/fin_ops_platform/services/bank_details_application_service.py</file>
    <file>backend/src/fin_ops_platform/services/bank_detail_category_side_effects.py</file>
    <file>backend/src/fin_ops_platform/services/app_settings_service.py</file>
    <file>backend/src/fin_ops_platform/services/bank_flow_rule_batch_application_service.py</file>
    <file>docs/modules/bank-details/boundary-io.md</file>
  </read_first>
  <action>
Reuse the existing settings normalization/CAS/audit transaction pattern. Ordinary auto-tag save commits rules/version/audit and returns affected source/version information without turnover `all`, derived lifecycle `all` or downstream barrier. Category confirmation/revoke commits the exact canonical month/version/audit and removes `affected_months or ["all"]`. Current page then invokes its normal query freshness path. Keep `reapply` as an explicit endpoint that enqueues bounded month shards with current visible scopes prioritized; no per-row jobs.
  </action>
  <acceptance_criteria>
    <criterion>Ordinary bank rule save and category mutation create zero downstream refresh jobs inside the command.</criterion>
    <criterion>Unknown month/scope fails closed rather than using `all`.</criterion>
    <criterion>Concurrent rule version conflict returns the existing formal conflict contract and cannot overwrite newer settings.</criterion>
    <criterion>Explicit reapply is distinguishable in audit/API response and does not block ordinary save.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Remove cost tag-rule rebuild/barrier semantics</name>
  <read_first>
    <file>backend/src/fin_ops_platform/services/cost_statistics_query_service.py</file>
    <file>web/src/components/cost-statistics/CostStatisticsTagRulesDrawer.tsx</file>
    <file>web/src/pages/CostStatisticsPage.tsx</file>
    <file>docs/modules/cost-statistics/boundary-io.md</file>
  </read_first>
  <action>
Preserve cost tag eligibility as a query/export-layer setting. Save settings/audit, update the current rule payload and re-run the current page query; remove any read-model target/barrier wait and misleading “保存并同步” behavior. Do not add the rule version to `cost_statistics` source versions.
  </action>
  <acceptance_criteria>
    <criterion>Cost tag-rule save creates zero cost or downstream refresh jobs.</criterion>
    <criterion>Rows/export reflect the saved eligibility through the existing query filter.</criterion>
    <criterion>The UI reports save success without global synchronization status.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Close bank/cost frontend lifecycle and slice performance tests</name>
  <read_first>
    <file>web/src/pages/BankDetailsPage.tsx</file>
    <file>web/src/features/bankDetails/AutoTagRulesDrawer.tsx</file>
    <file>web/src/pages/CostStatisticsPage.tsx</file>
    <file>web/src/hooks/useActiveFinanceDomainEvent.ts</file>
  </read_first>
  <action>
Use existing route/focus/visibility/domain-event patterns so current visible scope refetches after mutation and a hidden page waits until visible. Do not introduce per-page SSE. Add timing/job-amplification tests for ordinary save, current-month access, already-fresh repeat access and explicit reapply setup.
  </action>
  <acceptance_criteria>
    <criterion>Hidden bank/cost page creates no background refresh until visible.</criterion>
    <criterion>Visible current page becomes fresh through its query path.</criterion>
    <criterion>Sort/page/filter interactions do not enqueue rebuilds.</criterion>
    <criterion>Reference-data tests record the Phase 27 write/gate/access SLOs.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Targeted backend bank/cost/gateway tests.
- Targeted Vitest bank/cost/Drawer tests.
- `bash scripts/verify.sh lint`
- `bash scripts/verify.sh backend`
- `bash scripts/verify.sh frontend`
</verification>

<success_criteria>
- The first vertical slice proves all four operation classes without a new runtime system.
- Ordinary bank/cost saves are fast and have zero unrelated jobs.
- Current exact scopes reach fresh within the reference-data SLO.
</success_criteria>

## Artifacts this phase produces

- Extended existing gateway behavior only; no mandatory new coordinator class.
- Bank/cost access-time freshness and zero-write-fan-out tests.
- Explicit reapply batch contract using existing queue/worker infrastructure.
