---
phase: 27
plan: 06
type: execute
wave: 6
depends_on: [27-05]
files_modified:
  - backend/src/fin_ops_platform/services/derived_data_lifecycle_service.py
  - backend/src/fin_ops_platform/app/server.py
  - docs/app-architecture/runtime-and-ownership.md
  - docs/architecture/module-boundaries/read-model-contracts.md
  - docs/operations/runtime-worker-governance.md
  - scripts/verify.sh
autonomous: true
requirements: [RMF-01, RMF-06, RMF-07, RMF-08]
---

<objective>
Delete superseded fan-out/legacy paths, run all seven test categories, and produce an exact release-candidate performance and safety report.
</objective>

<threat_model>
- Dead callbacks/fallbacks can silently reactivate the old path after migration.
- Performance tests can pass while measuring only warm/fresh or mock data.
- Broad cleanup can delete explicit maintenance/repair paths that remain required.
</threat_model>

<tasks>

<task type="execute">
  <name>Whole-repository old-path deletion and architecture guards</name>
  <read_first>
    <file>.planning/phases/27-read-model-fan-out/27-COVERAGE-MATRIX.md</file>
    <file>backend/src/fin_ops_platform/services/derived_data_lifecycle_service.py</file>
    <file>backend/src/fin_ops_platform/app/server.py</file>
    <file>docs/modules/domain-events-lifecycle/boundary-io.md</file>
  </read_first>
  <action>
For every matrix entry marked delete, re-run CodeGraph impact and whole-repository text scan, migrate remaining callers, then remove callback/adapter/function/test/docs references. Retain explicit admin repair/deploy fan-out only under its existing permission/audit contract. Add static guards against ordinary mutation `include_all=True`, `affected_months or ["all"]`, direct service queue SQL, worker→Application, page cross-API and old operation-barrier waits.
  </action>
  <acceptance_criteria>
    <criterion>Every delete target has zero production caller/reference.</criterion>
    <criterion>Every retained maintenance path has explicit owner, permission, audit and reason.</criterion>
    <criterion>No hidden fallback or duplicate runtime path remains.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Run the seven-category and release verification closure</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/27-read-model-fan-out/27-VALIDATION.md</file>
    <file>docs/dev/testing.md</file>
    <file>scripts/verify.sh</file>
  </read_first>
  <action>
Run targeted then full backend/frontend/E2E/docs/runtime/infra checks. Use disposable PostgreSQL when configured and fail closed when required integration infrastructure is absent. Record tests by all seven categories, counts, skipped environment gates and unresolved risks in `27-VERIFICATION.md`.
  </action>
  <acceptance_criteria>
    <criterion>All applicable seven categories have concrete passing evidence.</criterion>
    <criterion>`bash scripts/verify.sh all` passes, or the phase remains blocked with exact failing evidence.</criterion>
    <criterion>No skipped/relaxed test hides a cleanup, worker or persistence failure.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Run complete reference-data page and operation performance validation</name>
  <read_first>
    <file>.planning/phases/27-read-model-fan-out/27-COVERAGE-MATRIX.md</file>
    <file>docs/operations/runtime-worker-governance.md</file>
    <file>scripts/with-production-admin-token.sh</file>
  </read_first>
  <action>
Use existing performance/smoke/controlled-operation entry points to execute every page read, every registered mutation and writable Drawer scenario against safe reference/test data. Capture count, p50/p95/p99/max, HTTP result, access-to-fresh, jobs created, target dirty deltas, unrelated dirty deltas and final Audit. Store bounded evidence under the Phase 27 directory; do not store secrets or raw sensitive payloads.
  </action>
  <acceptance_criteria>
    <criterion>Every page_key and operation_id in the coverage matrix has a result row.</criterion>
    <criterion>Ordinary command, freshness, fresh page and access-to-fresh SLOs pass.</criterion>
    <criterion>Every ordinary mutation has zero downstream jobs and zero unrelated dirty deltas.</criterion>
    <criterion>Full-history cases are reported separately and remain non-blocking to ordinary save.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Synchronize long-term docs and review the final diff for over-design</name>
  <read_first>
    <file>docs/modules/README.md</file>
    <file>docs/architecture/module-boundaries/inventory.md</file>
    <file>docs/app-architecture/runtime-and-ownership.md</file>
    <file>docs/operations/runtime-worker-governance.md</file>
  </read_first>
  <action>
Update only changed module/boundary/state/test/operations facts. Review the full diff for duplicate abstractions, unnecessary files, dead code, fallback branches, new dependencies and I/O boundary violations. Delete anything not required by a measured contract.
  </action>
  <acceptance_criteria>
    <criterion>Docs match shipped code and manifest/registry.</criterion>
    <criterion>No new dependency, runtime framework or speculative abstraction remains without current measured need.</criterion>
    <criterion>Git status contains only Phase 27 scoped changes and no temporary/sensitive evidence.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `bash scripts/verify.sh all`
- Whole-repository old-symbol and architecture guard suites.
- Reference-data performance matrix complete.
</verification>

<success_criteria>
- Release candidate is zero-failure, zero-unmapped and zero-legacy-pollution.
- Every page and operation meets its correctness/performance contract.
- The diff remains the smallest architecture that closes the requirement.
</success_criteria>

## Artifacts this phase produces

- `27-VERIFICATION.md`
- Bounded, redacted reference performance evidence keyed by page/operation.
- Static old-path and boundary guards.
