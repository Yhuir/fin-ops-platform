---
phase: 27
plan: 07
type: execute
wave: 7
depends_on: [27-06]
files_modified:
  - .planning/phases/27-read-model-fan-out/27-PRODUCTION-VALIDATION.md
autonomous: false
requirements: [RMF-07, RMF-08, RMF-09]
---

<objective>
Commit and push the exact verified release to remote main, deploy through the official entry, and close production correctness/performance for every page and operation.
</objective>

<threat_model>
- Direct main push/deploy can expose unverified code or overwrite unrelated remote work.
- Production writes can damage finance data without fingerprints, idempotency and rollback.
- Secrets and business payloads must not enter logs or planning artifacts.
</threat_model>

<tasks>

<task type="execute">
  <name>Pre-push main safety gate and commit</name>
  <read_first>
    <file>.planning/phases/27-read-model-fan-out/27-VERIFICATION.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>
Fetch origin, verify current branch is `main`, compare local/remote ancestry, and stop on unrelated remote divergence or uncommitted user changes outside Phase 27. Review final diff, secret scan and all release evidence. Run only release-critical targeted local gates; do not run the unrelated 183-browser suite or a redundant full CI. Stage all intended Phase 27 changes and create one or more coherent commits without amending unrelated history.
  </action>
  <acceptance_criteria>
    <criterion>All directly affected Phase 27 targeted local gates pass immediately before commit.</criterion>
    <criterion>No secret, production token, raw sensitive payload or unrelated file is staged.</criterion>
    <criterion>Commit SHA and complete file list are recorded.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Push exact changes to remote main and verify the remote SHA</name>
  <read_first>
    <file>docs/operations/deployment.md</file>
    <file>deploy/oa/README.md</file>
  </read_first>
  <action>
Push the committed `main` to `origin/main` as explicitly authorized. Verify remote `main` resolves to the exact local commit. Do not manually trigger or wait for unrelated/redundant full CI; the official deploy checks and production correctness/performance gates remain mandatory. Do not deploy a different SHA.
  </action>
  <acceptance_criteria>
    <criterion>`origin/main` equals the recorded local commit SHA.</criterion>
    <criterion>No unrelated or redundant full CI is manually triggered or awaited.</criterion>
    <criterion>No force push is used.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Deploy the exact main release and rehydrate Workbench v7 with rollback readiness</name>
  <read_first>
    <file>scripts/deploy-oa.sh</file>
    <file>deploy/oa/README.md</file>
    <file>docs/operations/runtime-worker-governance.md</file>
  </read_first>
  <action>
Capture the active/previous release and release-SHA rollback evidence required by the official deploy flow, then run `./scripts/deploy-oa.sh` for the exact main SHA. Do not create a PostgreSQL backup for the explicitly authorized test-owned, idempotent and automatically reversible production fixture, and do not delete any existing backup. Verify release activation, migration `0125`, process/worker registry, health endpoints and queue readiness. Because the candidate bumps Workbench month/all schema to v7, run the official `sudo /usr/local/sbin/finops-deploy-control workbench-rehydrate <release-name> --json`, then wait for Workbench outbox/dirty scopes/workers to drain before controlled writes. This explicit migration step is outside the ordinary page-load SLO and must not be replaced by page-driven full-history rebuild. If activation, migration or rehydrate correctness fails, use the documented previous-release rollback flow immediately.
  </action>
  <acceptance_criteria>
    <criterion>Active release identifies the exact pushed main SHA.</criterion>
    <criterion>Migration `0125` is applied, the v7 Workbench rehydrate completes, and all required services/workers are healthy with no unknown migration.</criterion>
    <criterion>Previous release and rollback procedure are recorded and usable.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Run production 17-page, every-operation and every-writable-Drawer validation</name>
  <read_first>
    <file>.planning/phases/27-read-model-fan-out/27-COVERAGE-MATRIX.md</file>
    <file>.planning/phases/27-read-model-fan-out/27-VALIDATION.md</file>
    <file>scripts/with-production-admin-token.sh</file>
  </read_first>
  <action>
Load the production admin token only through `scripts/with-production-admin-token.sh` without printing it. Run the read-only 17-page baseline/System Audit, then each authorized controlled write/withdraw or owner-specific reversible scenario and each writable Drawer operation. Probe consumers declared in the same checkpoint with bounded parallel HTTP requests so simultaneous open pages receive independent operation-commit-to-visible clocks; preserve the resolved path on failures so a first-request SLO miss cannot disappear behind a later fresh response. For every scenario capture sanitized latency distribution, command commit, page access-to-fresh, target jobs, unrelated dirty deltas and final Audit. Fail closed and rollback the release for correctness/data-safety failure; for isolated performance failure collect evidence, stop completion and enter the fix/release loop.
  </action>
  <acceptance_criteria>
    <criterion>Every coverage-matrix page_key and operation_id has production evidence.</criterion>
    <criterion>All ordinary SLOs pass and every ordinary write has zero downstream jobs/unrelated dirty deltas.</criterion>
    <criterion>All controlled facts are restored/withdrawn or intentionally retained by an audited business scenario.</criterion>
    <criterion>Final queue is drained/current, all read models are correctly fresh/stale by scope, and System Audit reports no internal issue.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Close the phase only after production proof</name>
  <read_first>
    <file>.planning/phases/27-read-model-fan-out/27-PRODUCTION-VALIDATION.md</file>
    <file>.planning/phases/27-read-model-fan-out/27-VALIDATION.md</file>
  </read_first>
  <action>
Record exact commit/release, verification commands, per-page/operation coverage, performance summary, rollback state, seven-category tests, docs impact and remaining external unknowns. Mark Phase 27 complete only when no required work remains.
  </action>
  <acceptance_criteria>
    <criterion>Production validation document has no missing required page/operation row.</criterion>
    <criterion>Any unavailable external-control proof remains clearly unknown and is not misreported as pass.</criterion>
    <criterion>Phase status is complete only after production gates pass.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Remote main exact-SHA confirmation; no redundant full-CI wait.
- Official deploy readiness/runtime checks.
- Production 17-page + all operation performance matrix.
- Final System Audit and queue/read-model current-effective closure.
</verification>

<success_criteria>
- Exact verified commit is on remote main and deployed.
- Every page, operation and writable Drawer passes production correctness/performance.
- No unrelated page I/O pollution, data damage, secret leakage or legacy fan-out remains.
</success_criteria>

## Artifacts this phase produces

- Git commit(s) on `main` and exact `origin/main` SHA.
- Deployed production release for the exact SHA.
- `.planning/phases/27-read-model-fan-out/27-PRODUCTION-VALIDATION.md` with sanitized evidence.
