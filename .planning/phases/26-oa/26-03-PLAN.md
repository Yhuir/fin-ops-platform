---
phase: "26"
plan: "03"
type: "tdd"
wave: 3
depends_on:
  - "26-02"
requirements:
  - "TURN-CLOSURE-05"
  - "TURN-CLOSURE-06"
autonomous: false
files_modified:
  - "backend/src/fin_ops_platform/services/runtime_worker.py"
  - "tests/test_runtime_worker.py"
  - "docs/modules/runtime-workers/boundary-io.md"
  - "docs/modules/runtime-workers/state-machine.md"
  - "docs/modules/runtime-workers/implementation-notes.md"
  - "docs/modules/read-models/boundary-io.md"
must_haves:
  truths:
    - "Phase 26 production gate may not pass while invoice_lifecycle events indefinitely defer behind an invalid pending_invoice dependency scope."
    - "A month-scoped invoice_lifecycle dependency refresh must enqueue the two valid pending_invoice base scopes for the same month and must never enqueue pending_invoice with a bare month."
    - "The repair reuses the existing RuntimeWorker dependency boundary, ReadModelRefreshGateway, scope policy and durable queue; it adds no table, worker, read model, service or compatibility path."
    - "Production closure requires the exact deployed commit, drained current-effective queue, fresh lifecycle scopes and affected-page Audit/performance evidence."
---

<objective>
Close the production-only Phase 26 dependency loop found during Plan 26-02 verification, then repeat exact-release production correctness and performance gates before Phase 27 starts.

Purpose: prevent a valid invoice lifecycle month event from cycling forever because the generic dependency mapper sends a scope rejected by the existing pending-invoice scope contract.
Output: one narrowly scoped worker fix, regression coverage, synchronized module facts, and production drain/freshness/Audit/SLO evidence.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Lock the invalid-scope failure with a red worker regression</name>
  <files>tests/test_runtime_worker.py</files>
  <action>Add one worker-level test for an `invoice_lifecycle:YYYY-MM` event whose handler raises `pending_invoice_read_model_not_fresh`. Require two exact refreshes, `expense:all:YYYY-MM` and `income:all:YYYY-MM`, one defer, preserved priority/trace metadata, and zero bare `pending_invoice:YYYY-MM` enqueue.</action>
  <verify>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_runtime_worker.py -k expands_invoice_lifecycle_pending_invoice_dependency</verify>
  <done>The test reproduces the production failure before implementation and passes only when the canonical pending-invoice scope contract is used.</done>
</task>

<task type="auto">
  <name>Task 2: Repair dependency scope derivation at the existing worker boundary</name>
  <files>backend/src/fin_ops_platform/services/runtime_worker.py, tests/test_runtime_worker.py</files>
  <action>Extend the existing dependency scope derivation to return the two valid pending-invoice direction scopes for a concrete source month and the two valid base scopes for an explicit all source. Keep all other dependency mappings unchanged; continue normalization, validation, active/fresh dedupe and enqueue through ReadModelRefreshGateway. Do not add fallback SQL, direct queue writes, new orchestration layers or extra runtime components.</action>
  <verify>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_runtime_worker.py tests/test_read_model_refresh_gateway.py tests/test_invoice_lifecycle_read_model_refresh.py tests/test_invoice_lifecycle_sql_projection.py tests/test_pending_invoice_service.py tests/test_runtime_queue.py</verify>
  <done>The invalid bare-month dependency cannot be emitted, both required monthly inputs can converge in parallel, and existing worker/queue/read-model behavior remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize the minimal worker/read-model boundary facts</name>
  <files>docs/modules/runtime-workers/boundary-io.md, docs/modules/runtime-workers/state-machine.md, docs/modules/runtime-workers/implementation-notes.md, docs/modules/read-models/boundary-io.md</files>
  <action>Record only the newly verified dependency scope contract, its production root cause and verification command. State explicitly that durable queue/freshness ownership and component inventory do not change.</action>
  <verify>bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
  <done>Maintainers can locate the exact contract and regression without creating a parallel architecture description.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Deploy the exact commit and close Phase 26 production gates</name>
  <files>No repository file is edited by this checkpoint.</files>
  <action>After automatic tests/lint/build pass, commit and push the scoped Phase 26 repair to remote main, deploy with `./scripts/deploy-oa.sh`, verify active release identity and release guard, and retain the captured previous release for rollback. Confirm the four previously looping invoice_lifecycle scopes become fresh and current-effective outbox/dirty backlog drains. Re-run App Health, the five affected page audits, turnover/workbench Phase 26 audits, `/health` and `/health/ready` latency, and the Plan 26-02 production identity/repair dry-run checks. Do not execute metadata repair when dry-run target count is zero. If correctness, queue drain, readiness semantics or performance still fails, stop Phase 26 completion and create the next smallest evidenced gap plan; do not hide it in Phase 27.</action>
  <verify>Exact active commit equals pushed main; `check-release` passes; the affected current-effective queue is drained; lifecycle and affected page scopes are fresh; no new failed/dead-letter event exists; production Audit and latency evidence are captured against the exact release.</verify>
  <done>Phase 26 has no unresolved production correctness or performance blocker and is safe to mark complete before Phase 27 implementation.</done>
</task>

</tasks>

<verification>
Run the focused worker/read-model suite, Phase 26 automatic regression suite, frontend tests/build, lint, docs and diff checks. Production verification is mandatory and remains blocking.
</verification>

<success_criteria>
- Invalid `pending_invoice:YYYY-MM` dependency enqueue is mechanically impossible in this path.
- Invoice lifecycle dependency refreshes use the existing validated/deduped durable queue boundary.
- No unrelated business/page behavior, API shape or runtime topology changes.
- Exact-release production evidence proves drain, freshness, Audit correctness and measured latency before Phase 26 completion.
</success_criteria>
