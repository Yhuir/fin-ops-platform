---
phase: "26"
plan: "05"
type: "tdd"
wave: 5
depends_on:
  - "26-04"
requirements:
  - "TURN-CLOSURE-06"
autonomous: false
files_modified:
  - "backend/src/fin_ops_platform/services/postgres_repositories/invoice_read_model_audit.py"
  - "tests/test_audit_input_invoice_usage_read_model_tool.py"
  - "tests/test_audit_output_invoice_collection_read_model_tool.py"
  - "docs/modules/input-invoice-usage/boundary-io.md"
  - "docs/modules/input-invoice-usage/tests.md"
  - "docs/modules/output-invoice-collections/boundary-io.md"
  - "docs/modules/output-invoice-collections/tests.md"
must_haves:
  truths:
    - "A bank-only relation timestamp advance does not make an invoice page integrity proof fail when every other embedded relation source-version key is unchanged."
    - "Any changed non-timestamp source-version key, or any later relation containing an invoice member for that page direction and scope, remains a blocking mismatch."
    - "The change is read-only Audit classification: no API, fact, read model, freshness gate, worker, queue or write fan-out behavior changes."
---

<objective>
Close the production-only invoice Audit false positive exposed by the controlled bank-only Turnover closure, then repeat the exact-release Phase 26 production write and Audit gates.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Lock the relevant-invoice relation mismatch contract</name>
  <files>tests/test_audit_input_invoice_usage_read_model_tool.py, tests/test_audit_output_invoice_collection_read_model_tool.py</files>
  <action>Add focused SQL-contract regressions requiring mismatch detection to remain unconditional for non-timestamp key drift and conditional on a later relation intersecting the canonical/alias invoice identities of the same direction and month when only `workbench_pair_relations_updated_at` differs.</action>
  <verify>PYTHONPATH=backend/src python3 -m unittest tests.test_audit_input_invoice_usage_read_model_tool tests.test_audit_output_invoice_collection_read_model_tool</verify>
  <done>Both invoice directions mechanically retain fail-closed relevant-relation detection.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the existing read-only Audit query</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/invoice_read_model_audit.py</files>
  <action>Reuse the existing active-invoice identity CTE. Report a mismatch when relation source-version keys other than `workbench_pair_relations_updated_at` differ, or when a relation updated after the embedded timestamp intersects a canonical/postgres/source-link invoice identity in the same direction and scope. Do not change projection or freshness behavior and do not add a compatibility branch.</action>
  <verify>Run both invoice Audit suites plus App Health/System Audit regression, lint, docs and diff checks.</verify>
  <done>Bank-only Turnover changes are not invoice integrity failures, while invoice-bearing relation drift remains blocking.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize minimal module facts</name>
  <files>docs/modules/input-invoice-usage/boundary-io.md, docs/modules/input-invoice-usage/tests.md, docs/modules/output-invoice-collections/boundary-io.md, docs/modules/output-invoice-collections/tests.md</files>
  <action>Record only the Audit relevance rule and its regression/production evidence boundary. State that page freshness may still rebuild on access and is owned by Phase 27.</action>
  <verify>bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
  <done>Audit semantics and the remaining freshness optimization boundary are explicit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Deploy exact commit and repeat the controlled production closure</name>
  <files>No repository file is edited by this checkpoint.</files>
  <action>Commit and push the scoped change, deploy through `./scripts/deploy-oa.sh`, verify exact release and 22 current workers, require the 17-page System Audit to pass before mutation, then run the approved test-owned Turnover confirm/withdraw scenario with new idempotency keys. Require final inactive relation state, exact receipt closure, affected/isolation consumers, queue drain and a new 17-page System Audit pass. Record the observed 5s/30s safety ceilings separately from the Phase 27 3s target.</action>
  <verify>Exact release, dry-run, apply report, final fixture state, queue/readiness and System Audit are all captured and pass.</verify>
  <done>Phase 26 production verification is closed without adding write fan-out.</done>
</task>

</tasks>

<success_criteria>
- Bank-only Turnover relation timestamps no longer create Input/Output invoice Audit integrity errors.
- Relevant invoice relation drift and all other source-version drift remain blocking.
- No runtime topology, page API, business writer or queue contract changes.
- Exact-release production confirm/withdraw restores the fixture and finishes with every registered page Audit passing.
</success_criteria>
