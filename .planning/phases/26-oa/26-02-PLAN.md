---
phase: "26"
plan: "02"
type: "tdd"
wave: 2
depends_on:
  - "26-01"
requirements:
  - "TURN-CLOSURE-04"
  - "TURN-CLOSURE-05"
  - "TURN-CLOSURE-06"
autonomous: false
files_modified:
  - "backend/src/fin_ops_platform/tools/workbench_relation_requirement_repair_ops.py"
  - "backend/src/fin_ops_platform/services/workbench_pair_relation_service.py"
  - "backend/src/fin_ops_platform/services/workbench_relation_command_service.py"
  - "backend/src/fin_ops_platform/services/workbench_read_model_version.py"
  - "deploy/oa/bin/finops-deploy-control.sh"
  - "deploy/oa/README.md"
  - "tests/test_workbench_relation_requirement_repair_ops.py"
  - "tests/test_workbench_pair_relation_service.py"
  - "tests/test_workbench_relation_command_service.py"
  - "tests/test_deploy_oa_script.py"
  - "tests/test_workbench_sql_runtime.py"
  - "tests/test_workbench_query_service.py"
  - "docs/modules/turnover-ledger/tests.md"
  - "docs/modules/turnover-ledger/implementation-notes.md"
  - "docs/modules/reconciliation-workbench/tests.md"
  - "docs/modules/reconciliation-workbench/implementation-notes.md"
  - "docs/modules/read-models/tests.md"
  - "docs/modules/read-models/implementation-notes.md"
must_haves:
  truths:
    - "现有 repair ops 识别 active legacy-invalid turnover snapshots，ETC/batch exempt，ordinary missing snapshot 兼容行为不变。"
    - "Turnover repair 用 fresh bank tags + canonical rules 覆盖旧写死 requires，并保留 dry-run/fingerprint/audit/idempotency。"
    - "同一 repair ops 提供 fingerprint-bound rollback-dry-run/rollback，经现有 metadata update command 原地精确恢复 special_metadata preimage，不 cancel/recreate relation，并支持 partial execute 与幂等 replay。"
    - "Workbench month/all schema 同步升级 v6，旧 v5 generation/cache 不可冒充 fresh。"
    - "自动任务不执行真实 repair/deploy/rehydrate；所有生产写操作停在 human-action checkpoint。"
    - "生产命令只使用已核实 deploy-control 子命令和 test-owned 可恢复 smoke scenario。"
  artifacts:
    - path: "backend/src/fin_ops_platform/tools/workbench_relation_requirement_repair_ops.py"
      provides: "legacy-invalid turnover requirement snapshot 的安全一次性 repair"
    - path: "backend/src/fin_ops_platform/services/workbench_read_model_version.py"
      provides: "Workbench month/all v6 freshness 与 cache 失效边界"
    - path: "tests/test_workbench_relation_requirement_repair_ops.py"
      provides: "四种 operator mode、target predicate、original-plan fingerprint、exact metadata rollback、partial execute/drift/replay 回归"
    - path: "backend/src/fin_ops_platform/services/workbench_pair_relation_service.py"
      provides: "默认 merge 不变、显式 replace_special_metadata 才精确替换 metadata 的现有 relation 边界"
    - path: "backend/src/fin_ops_platform/services/workbench_relation_command_service.py"
      provides: "透传并把 replace_special_metadata 纳入 request fingerprint 的 canonical command/UoW 边界"
    - path: "deploy/oa/bin/finops-deploy-control.sh"
      provides: "固定 release module 的 repair dry-run/execute/rollback-dry-run/rollback usage 与透传合同"
    - path: "tests/test_workbench_sql_runtime.py"
      provides: "v5 stale、v6 month/all/cache、原子 projection 回归"
  key_links:
    - from: "repair target selector"
      to: "build_bank_relation_requirement_metadata"
      via: "fresh bank tags + current canonical rules"
    - from: "repair execute"
      to: "WorkbenchRelationCommandService"
      via: "fingerprint + fixed actor/idempotency/history；无 direct SQL"
    - from: "repair history"
      to: "exact metadata rollback"
      via: "fingerprint selector + current after-image drift guard + update_relation_metadata_for_case_id(replace_special_metadata=True)"
    - from: "workbench_read_model_version v6"
      to: "groups/initial cache and active generation"
      via: "existing derived schema keys and freshness gate"
    - from: "human checkpoint"
      to: "production repair/rehydrate/SLO/E2E"
      via: "verified finops-deploy-control commands only"
threat_model:
  new_attack_surface: "none"
  controls:
    - "repair 仍是 operator-only CLI，无 HTTP surface。"
    - "execute 必须使用同一 dry-run source fingerprint；drift 在任何 relation write 前失败。"
    - "固定 system actor、stable per-case idempotency、history operation 和 command service audit。"
    - "rollback 只选择同一 execute fingerprint 的 repair histories，并在首写前批量验证 current active relation 等于 recorded after image。"
    - "replace_special_metadata 默认 false 保持所有旧 caller merge 合同；只有 repair rollback 显式 true，且 relation ownership/lifecycle/created fields 不变。"
    - "生产前备份 canonical relation/history 与 active generations；token/approval 只从受控 stdin，不写日志。"
    - "rehydrate 只通过现有 worker/active-generation 原子发布；禁止 direct SQL mark-fresh。"
---

<objective>
安全修复历史 active turnover requirement snapshots，升级 Workbench v6 并完成自动验证；随后停在人工生产门，由受控 deploy-control 完成 dry-run、fingerprint 审批、execute、rehydrate、SLO/E2E 和回滚证据。

Purpose: 防止旧错误 metadata 与旧 v5 generation/cache 在在线修复后继续污染页面。
Output: repair/v6 代码与测试、最小文档、自动验证结果和 human-gated 生产执行记录。
</objective>

<must_haves>
- 覆盖 Context D-05、D-06，并复核 D-01/D-02 的历史数据落地。
- 不新建 repair service/tool、表、migration、worker、read model、cache 或 fallback。
- Turnover legacy-invalid target 覆盖 source 非 canonical 或缺 tag/version；只处理 active。
- ordinary missing snapshot 保留旧兼容语义；ETC/batch 不纳入。
- repair plan fingerprint 覆盖 target current preimage 与 intended after image；execute history note/operation/idempotency 绑定该 fingerprint。
- 同一工具提供 `--rollback-dry-run` 和 `--rollback`，经现有 metadata update command 精确恢复 fingerprint-bound history 的完整 `special_metadata` preimage；不 cancel/recreate relation，支持 partial execute 与幂等 replay。
- 自动验证绝不运行裸 repair CLI；生产只在 checkpoint 使用已核实命令。
</must_haves>

<threat_model>
- dry-run report/fingerprint 是 execute 的 TOCTOU gate；fingerprint 不一致必须零写入失败。
- repair 只通过 canonical command service 更新 metadata/history，禁止 direct SQL、全量 snapshot replace 或运行时规则同步。
- rollback 在首写前批量验证 current active relation 与 recorded after image 完全一致；任一 drift hard stop，禁止盲回滚。
- rollback 只通过 `update_relation_metadata_for_case_id(... replace_special_metadata=True)` 和现有 UoW 原地恢复完整 metadata preimage，并写 durable history/outbox；members/status/mode/created fields 和 relation lifecycle 不变。
- v6 部署后旧 v5 只能显示 refreshing/stale，不能作为 fresh paired 结果。
- 生产 write-operation apply 只允许已审批 test-owned 可恢复 scenario；token/approval 由 stdin 提供，不落盘/日志。
</threat_model>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
26-01 修复未来写入与在线分区，但历史 active turnover relations 仍可能含旧 source/写死 requirements。completion 语义变化还要求 Workbench v6，使 v5 generation/cache fail closed。现有 repair ops 和 active-generation rehydrate 已具备安全边界，本计划只做最小扩展并把真实环境动作留给人工 checkpoint。

项目上下文：
@.planning/phases/26-oa/26-CONTEXT.md
@.planning/phases/26-oa/26-RESEARCH.md
@.planning/phases/26-oa/26-01-PLAN.md
@backend/src/fin_ops_platform/tools/workbench_relation_requirement_repair_ops.py
@backend/src/fin_ops_platform/services/workbench_read_model_version.py
@deploy/oa/bin/finops-deploy-control.sh
@deploy/oa/README.md
@docs/modules/reconciliation-workbench/boundary-io.md
@docs/modules/read-models/boundary-io.md
@docs/operations/runtime-worker-governance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1：扩展现有 repair 的 forward/rollback 四模式与 deploy-control 固定合同</name>
  <read_first>
    - backend/src/fin_ops_platform/tools/workbench_relation_requirement_repair_ops.py
    - tests/test_workbench_relation_requirement_repair_ops.py
    - backend/src/fin_ops_platform/services/workbench_relation_requirements.py
    - backend/src/fin_ops_platform/services/workbench_pair_relation_service.py
    - backend/src/fin_ops_platform/services/workbench_relation_command_service.py
    - tests/test_workbench_pair_relation_service.py
    - tests/test_workbench_relation_command_service.py
    - deploy/oa/bin/finops-deploy-control.sh
    - tests/test_deploy_oa_script.py
    - deploy/oa/README.md
  </read_first>
  <files>
    - backend/src/fin_ops_platform/tools/workbench_relation_requirement_repair_ops.py
    - backend/src/fin_ops_platform/services/workbench_pair_relation_service.py
    - backend/src/fin_ops_platform/services/workbench_relation_command_service.py
    - tests/test_workbench_relation_requirement_repair_ops.py
    - tests/test_workbench_pair_relation_service.py
    - tests/test_workbench_relation_command_service.py
    - deploy/oa/bin/finops-deploy-control.sh
    - tests/test_deploy_oa_script.py
    - deploy/oa/README.md
  </files>
  <action>
1. 先写 red tests：active turnover relation 在 source 为 `turnover_ledger_manual_closure`、`no_oa_bank_batch_tag_selection`、其它非 `bank_transaction_paired_policy`，或缺 canonical tag codes/version 时成为 target。
2. 断言 canonical source + tag codes + valid version 的 active turnover 不重复 target；cancelled/withdrawn 不处理；ETC/batch exempt。
3. 保留 ordinary missing-snapshot 现有兼容：已存在 canonical/legacy requires 值不被覆盖；只对 turnover legacy-invalid target 用 fresh tags + current rules 覆盖旧写死 requires。
4. 最小修改现有 predicate/plan builder，不创建第二 repair path。继续一次 bulk tag read、canonical helper和排序稳定 plan；forward fingerprint 必须同时覆盖每个 target 的 current preimage 与 intended after image，防止只绑定输出而漏掉执行前状态。
5. execute 保留固定 actor、command service、affected months 和 persist boundary；history note、operation type 和 stable per-case idempotency key 必须绑定 original execute fingerprint。重跑同 fingerprint 时，将当前 fresh legacy-invalid targets 与已存在的 fingerprint-bound execute histories 合并，按原 case 顺序重建 original plan：已有 history 的已应用 case 必须 `current == recorded after` 后 skip/幂等重放；没有 execute history 的未应用 case 必须 `current == recorded before` 后继续。重建的 original-plan fingerprint 与 expected 不同则在首写前零写入失败，禁止因已应用 case 被 target selector 排除而改变 fingerprint。
6. 在同一个 `workbench_relation_requirement_repair_ops.py` 增加明确 operator modes：`--rollback-dry-run` 与 `--rollback`；两者都强制 `--expected-fingerprint <original execute fingerprint>`。不得新建 runtime service/helper。
7. 在 `WorkbenchPairRelationService.update_relation_metadata_for_case_id` 增加显式 `replace_special_metadata: bool = False`：默认 false 的现有 merge 行为和全部旧 caller 不变；true 仅把 `special_metadata` 精确替换为传入 preimage，relation members/status/mode/created fields 均不变，只沿正常 update 产生新的 updated_at/history。在 `WorkbenchRelationCommandService.update_relation_metadata_for_case_id` 增加并透传同名 flag，纳入 command request fingerprint；仅 repair rollback 显式传 true。不新增 service/helper/API/DTO。
8. rollback selector 只读取 `list_history()` 中与该 fingerprint、repair actor/operation 匹配的 histories，并取完整 before/after metadata。任何 rollback write 前，批量验证每个 current active relation 与 recorded after image 完全一致；任一 drift 全部零写入失败。
9. rollback 使用现有 `update_relation_metadata_for_case_id(... replace_special_metadata=True)` canonical command/UoW 原地恢复完整 `special_metadata` preimage，使用固定 rollback actor、operation 和 fingerprint-bound idempotency；必须生成 durable history/outbox。重跑同 fingerprint 时，以 execute histories 减去已存在的 fingerprint-bound rollback histories 重建 pending rollback set：已恢复 case 必须 `current == recorded preimage` 后 skip，仍待恢复 case 必须 `current == recorded after` 后继续，任何其它 current state 都在首写前作为 drift 失败。该合同必须覆盖 rollback 中途失败后的安全重试；不得走 cancel/recreate closure command，全部恢复后同 fingerprint 的 `--rollback-dry-run` target count 为 0。
10. 自动测试覆盖四种 mode、exact preimage/intended-after fingerprint、original-plan partial execute reconstruction、current drift zero-write、partial rollback retry、rollback durable history/outbox、幂等 replay、provider unknown/missing fail closed、ordinary/exempt 回归，且无 direct SQL。service/command tests 必须证明：默认 false 维持旧 merge；true 精确删除 forward 新增 canonical keys 并恢复完整 metadata preimage；members/status/mode/lifecycle/created fields 不变，updated_at/history/outbox/audit/idempotency 正常。
11. 只更新 deploy-control 的 fixed usage/argument contract；函数继续透传同一个固定 repair module。精确支持：
    - `workbench-requirement-repair <release> --dry-run`
    - `workbench-requirement-repair <release> --execute --expected-fingerprint <sha>`
    - `workbench-requirement-repair <release> --rollback-dry-run --expected-fingerprint <sha>`
    - `workbench-requirement-repair <release> --rollback --expected-fingerprint <sha>`
    不增加 release rollback subcommand或其它猜测参数。
  </action>
  <verify>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_relation_requirement_repair_ops.py tests/test_workbench_pair_relation_service.py tests/test_workbench_relation_command_service.py</command>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_deploy_oa_script.py -k "workbench_requirement_repair"</command>
    <expected>所有 repair tests 使用 fake command/tag/rules/history providers，不构造 runtime application、不连接真实数据库；四模式和 fixed deploy-control usage 全绿，forward/rollback drift 均首写前零 writes。</expected>
  </verify>
  <acceptance_criteria>
    - 所有已确认 legacy source/缺 canonical fields 的 active turnover relation 被识别。
    - turnover target 的旧 requires 被真实 canonical policy 覆盖。
    - ordinary missing、ETC/batch、inactive relation 行为不变。
    - forward fingerprint 覆盖 exact preimage + intended after；execute history/idempotency 绑定 fingerprint。
    - rollback 只选择匹配 history，在 write 前完成全量 after-image drift check，并通过 metadata update command 原地精确恢复完整 special_metadata preimage；relation ownership/lifecycle/created fields 不变。
    - partial execute 通过 fresh targets + fingerprint histories 重建 original plan；已应用 skip、未应用 continue、fingerprint mismatch 首写前失败；partial rollback 通过 execute histories 减去 rollback histories 重建 pending set，已恢复 skip、未恢复 continue、其它 state 作为 drift 首写前失败；rollback 后 target=0 有机械测试。
    - deploy-control 精确暴露四种模式但继续透传同一 module；无新 runtime service/helper、SQL 或同步链。
  </acceptance_criteria>
  <done>现有 repair ops 可 forward repair 并以同一 fingerprint/history 原地 exact metadata rollback；fixed deploy-control 合同和自动回归完整。</done>
</task>

<task type="auto">
  <name>Task 2：升级 Workbench v6，完成自动 regression 与最小 docs</name>
  <read_first>
    - backend/src/fin_ops_platform/services/workbench_read_model_version.py
    - backend/src/fin_ops_platform/services/workbench_groups_page_cache.py
    - tests/test_workbench_sql_runtime.py
    - tests/test_workbench_query_service.py
    - docs/modules/turnover-ledger/tests.md
    - docs/modules/turnover-ledger/implementation-notes.md
    - docs/modules/read-models/tests.md
    - docs/modules/read-models/implementation-notes.md
    - docs/modules/reconciliation-workbench/tests.md
    - docs/modules/reconciliation-workbench/implementation-notes.md
  </read_first>
  <files>
    - backend/src/fin_ops_platform/services/workbench_read_model_version.py
    - tests/test_workbench_sql_runtime.py
    - tests/test_workbench_query_service.py
    - docs/modules/turnover-ledger/tests.md
    - docs/modules/turnover-ledger/implementation-notes.md
    - docs/modules/reconciliation-workbench/tests.md
    - docs/modules/reconciliation-workbench/implementation-notes.md
    - docs/modules/read-models/tests.md
    - docs/modules/read-models/implementation-notes.md
  </files>
  <action>
1. 先加版本 red tests：旧 v5 month generation、all composed version、groups cache 和 initial cache 不得通过 v6 freshness/version gate；v6 month/all 语义版本必须同步。
2. 只在 `workbench_read_model_version.py` 把 month/all schema 升至 v6；复用 cache 派生关系，不手写 cache eviction，不改变 `workbench_relation` schema、manifest、registry、worker/env。
3. 补 projection/query regression：v6 incomplete turnover relation 同 case unpaired、补 OA paired、active generation 原子 publish；building/failed/旧 v5 不可读为 fresh。
4. 运行 26-01 目标回归以及 repair/version/deploy fixed-contract tests；自动任务不得运行 `python -m ...repair_ops --dry-run`、deploy-control、真实 DB 或生产 token。
5. 只更新真正需要记录 v6、fingerprint-bound exact metadata rollback、repair/rehydrate 的 turnover/workbench/read-model tests/implementation notes；已经正确的边界文档不机械改。
  </action>
  <verify>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_relation_requirement_repair_ops.py tests/test_workbench_pair_relation_service.py tests/test_workbench_relation_command_service.py tests/test_workbench_sql_runtime.py tests/test_workbench_query_service.py -k "requirement or turnover or metadata or schema or cache or generation"</command>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_relation_grouping.py tests/test_turnover_ledger_uow_contract.py tests/test_turnover_workbench_integration.py tests/test_turnover_ledger_api.py tests/test_no_oa_bank_batch_tag_selection_api.py</command>
    <command>npm --prefix web test -- --run WorkbenchApi.test.ts RelationGroupGrid.test.tsx</command>
    <command>npm --prefix web run build</command>
    <command>bash scripts/verify.sh lint</command>
    <command>bash scripts/verify.sh docs</command>
    <command>git diff --check</command>
    <expected>自动测试与 docs/lint/build 全绿；无真实 repair/deploy/rehydrate；v5 fixture 明确 stale，v6 active generation 正确分区。</expected>
  </verify>
  <acceptance_criteria>
    - v6 month/all/cache version 链一致，旧 v5 不冒充 fresh。
    - workbench_relation ownership schema 与 runtime worker registry/manifest/env 均未改变。
    - repair、online fix、其它 relation/page regressions 全绿。
    - docs 只记录真实变化和生产 checkpoint，不扩散到正确文档。
  </acceptance_criteria>
  <done>代码与自动验证完成，部署前仅剩 human-action 的备份、dry-run/fingerprint 审批、execute、rehydrate 和受控生产证据。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3：exact release 基线、repair/rollback、v6、Audit、SLO/E2E 人工生产闭环</name>
  <read_first>
    - docs/operations/runtime-worker-governance.md
    - deploy/oa/README.md
    - .planning/phases/26-oa/26-01-SUMMARY.md
  </read_first>
  <files>
    - "No repository files are edited by this checkpoint; it produces external release evidence only."
  </files>
  <action>
1. 自动 gate 全绿后创建 clean commit；只上传、不激活 exact release：
   `./scripts/deploy-oa.sh --no-activate --release-name <release>`
   然后检查 release：
   `sudo -n /usr/local/sbin/finops-deploy-control check-release <release>`
2. 运行 `sudo -n /usr/local/sbin/finops-deploy-control status`，捕获并固化当前 active `<previous-release>`。确认 operator backup/restore-point reference 覆盖 canonical relation/history 与当前 Workbench active generations；不存在 restore point、approval ticket 或 previous release 时 hard stop。
3. 在 `<previous-release>` 上用同一已审批 test-owned、可恢复 turnover scenario 建立基线：先运行
   `sudo -n /usr/local/sbin/finops-deploy-control write-operation-e2e-smoke <previous-release> /tmp/finops-write-e2e-<run-id>.json --dry-run`
   只有 scenario/restore point/approval 明确时才经 `--apply-stdin` 执行，并完成业务 inverse/recovery；token/approval 从 stdin 输入。保存脱敏 command response、exact receipt response-to-fresh、consumer HTTP 基线报告。没有 fixture/approval/restore point 时 hard stop。
4. 使用未激活的新 release 预跑候选 repair：
   `sudo -n /usr/local/sbin/finops-deploy-control workbench-requirement-repair <release> --dry-run`
   审核 target count、preimage/intended-after coverage、candidate source fingerprint、备份和 rollback contract 自动测试。此候选 fingerprint 不是 execute 授权；execute 前尚无 repair history，因此此时不得要求或运行 production rollback-dry-run。
5. 候选 dry-run、restore point、approval 和 captured previous release 审核通过后，激活 exact release：
   `sudo -n /usr/local/sbin/finops-deploy-control activate <release>`
   该命令完成 migration、API/workers/frontend 切换并等待正式 `/fin-ops-api/health/ready`；v6 gate 保证 v5 不冒充 fresh。
6. 激活后立即重跑 final repair dry-run：
   `sudo -n /usr/local/sbin/finops-deploy-control workbench-requirement-repair <release> --dry-run`
   final report hash/source fingerprint 若与候选不同，停止、重新审核并取得新的明确批准；禁止使用旧 fingerprint execute。
7. 批准 final fingerprint 后执行：
   `sudo -n /usr/local/sbin/finops-deploy-control workbench-requirement-repair <release> --execute --expected-fingerprint <approved-final-source-fingerprint>`
   随后第二次运行同 release `--dry-run`，要求 `target_relation_count=0`；再执行：
   `sudo -n /usr/local/sbin/finops-deploy-control workbench-rehydrate <release> --json`
   等待 v6 generation、durable queue、readiness 和 required workers 收敛，禁止 direct SQL mark-fresh。
8. 运行 blocking identity/readiness/Audit：
   - `sudo -n /usr/local/sbin/finops-deploy-control workbench-audit-identity <release> --json --workbench-scope all --limit 20`，要求 `blocking_issue_count=0`。
   - 正式 `https://www.yn-sourcing.com/fin-ops-api/health/ready` 要求 `status=ready`、runtime release 精确等于 `<release>`，current blockers、dirty、active outbox、dead-letter、worker mismatch 全为 0；`activate` 自身检查不能替代保存最终证据。
   - 使用 `scripts/with-production-admin-token.sh` 按现有受控 docs 调用 admin-only page Audit URLs，不打印 token：
     `https://www.yn-sourcing.com/fin-ops-api/api/operations/app-health/page-audit?page=turnover-ledger`
     `https://www.yn-sourcing.com/fin-ops-api/api/operations/app-health/page-audit?page=reconciliation-workbench`
     `https://www.yn-sourcing.com/fin-ops-api/api/operations/app-health/page-audit?page=app-health-operations`
     要求 proof ready、integrity pass、freshness fresh、queue drained；System Audit 产生新 audit ID 且全部注册页 pass。
9. read-model SLO helper 只允许 dry-run，禁止 `--apply`：
   `sudo -n /usr/local/sbin/finops-deploy-control read-model-slo-smoke <release> --json --critical-only --target-ms 5000`
10. 对新 release 使用与 previous 完全相同的 test-owned scenario，先 `write-operation-e2e-smoke ... --dry-run`，获批后才 `--apply-stdin` 并完成 inverse/recovery。比较三类脱敏结果：command response、exact receipt response-to-fresh、consumer HTTP。新 release 不得比 approved baseline 显著退化；门限为 `max(10%, baseline documented measurement noise)`。若 baseline 样本不足，只判 safety ceilings，不得宣称性能提升。
11. 四个 standing safety ceilings 分开记录：同步写 5000ms、exact receipt 异步 refresh 30000ms、总等待 120000ms、consumer HTTP 1000ms。它们是安全上限，不冒充高性能证明；provider call-count 与 no relation scan tests 仍是主机械性能证据。
12. 激活前或 execute 前失败：若已切换 release，只执行 `activate <previous-release>`，再 `workbench-rehydrate <previous-release> --json`，然后重跑 readiness/Audit/previous test-owned smoke；没有 metadata repair 不运行 metadata rollback。
13. 任一 post-execute repair/rehydrate/readiness/Audit/SLO/E2E 失败：必须先在仍存在的新 release 上运行
   `sudo -n /usr/local/sbin/finops-deploy-control workbench-requirement-repair <release> --rollback-dry-run --expected-fingerprint <approved-final-source-fingerprint>`
   审核 exact histories/preimages/current after-image，无 drift 后执行
   `sudo -n /usr/local/sbin/finops-deploy-control workbench-requirement-repair <release> --rollback --expected-fingerprint <approved-final-source-fingerprint>`
   再跑 rollback-dry-run，要求 target=0；随后执行 `activate <previous-release>` 和 `workbench-rehydrate <previous-release> --json`，并重跑 status/readiness/Audit/test-owned smoke。rollback precondition drift 时 hard stop，不得盲回滚或 direct SQL。
  </action>
  <resume-signal>
仅当操作者提供以下全部值后继续：approved exact release；captured previous release；approved final post-activation repair dry-run report hash/source fingerprint；证明该 fingerprint 覆盖 rollback preimage 与 intended after image 的 assertion；operator backup/restore-point reference；approval ticket。候选 pre-activation fingerprint 不能代替 final fingerprint；首次 resume 不要求 rollback-dry-run report hash。
  </resume-signal>
  <verify>
    <command>sudo -n /usr/local/sbin/finops-deploy-control status</command>
    <command>sudo -n /usr/local/sbin/finops-deploy-control workbench-requirement-repair &lt;release&gt; --dry-run</command>
    <command>sudo -n /usr/local/sbin/finops-deploy-control workbench-audit-identity &lt;release&gt; --json --workbench-scope all --limit 20</command>
    <command>sudo -n /usr/local/sbin/finops-deploy-control workbench-rehydrate &lt;release&gt; --json</command>
    <command>sudo -n /usr/local/sbin/finops-deploy-control read-model-slo-smoke &lt;release&gt; --json --critical-only --target-ms 5000</command>
    <command>sudo -n /usr/local/sbin/finops-deploy-control write-operation-e2e-smoke &lt;release&gt; /tmp/finops-write-e2e-&lt;run-id&gt;.json --dry-run</command>
    <expected>所有 execute/apply/activate 受 human approval；final fingerprint 绑定 exact release，post-execute dry-run target=0，v6/queue/readiness/identity/Page/System Audit 收敛，previous/new 同 scenario 对比与四个 safety ceiling 分开通过。</expected>
  </verify>
  <acceptance_criteria>
    - exact release clean commit、check-release、captured previous release、backup/restore point 与 approval ticket 全部有证据。
    - final post-activation dry-run fingerprint 是唯一 execute 授权；变化必须重新审批，execute 后第二次 dry-run target=0。
    - v6 rehydrate 原子完成；health ready release 精确、current queue/worker blockers 为 0；identity Audit blocking_issue_count=0；三个 Page/System Audit 全 pass。
    - previous/new 使用同一 test-owned reversible scenario；E2E apply 只经 stdin secrets，完成 inverse/recovery。
    - 三类 baseline comparison 使用 max(10%, documented noise)；样本不足只判 safety ceilings，不宣称提升。
    - post-execute 失败先 exact metadata rollback，再 activate previous + previous rehydrate；rollback drift hard stop，rollback 后 dry-run target=0。
  </acceptance_criteria>
  <done>只有人工确认所有受控生产证据通过后，Phase 26 才可标记 production-verified；否则保持 blocked/pending 并记录精确失败与回滚结果。</done>
</task>

</tasks>

<verification>
自动部分：repair/version/online regression、frontend build、lint/docs/diff 全绿；绝不调用真实 repair CLI。

生产部分：仅在 checkpoint 使用已核实 deploy-control 命令；exact release/previous release/restore point/approval 有据，final post-activation fingerprint 才能 execute，execute 后 dry-run target=0 再 rehydrate。正式 readiness 要求 exact runtime release 与 blocker/dirty/outbox/dead-letter/worker mismatch 全零；identity Audit、三个 Page Audit/System Audit、同场景 previous/new E2E 和四个 safety ceilings 全部有据。post-execute 失败先 fingerprint-bound rollback 并证明 target=0，再 activate/rehydrate previous release；drift hard stop。
</verification>

<success_criteria>
1. TURN-CLOSURE-04/05/06 有自动测试和受控生产证据。
2. D-05/D-06 完整覆盖，并复核历史 relation 满足 D-01/D-02。
3. legacy-invalid active turnover snapshots 被幂等修复；partial execute/replay 可重建 original plan，exact metadata rollback 原地恢复完整 metadata preimage且不改变 relation ownership/lifecycle/created fields，drift 零写入，ordinary/exempt 关系不受影响。
4. Workbench v6 active generation/cache 正确，v5 fail closed。
5. 无新 runtime component/direct SQL/自动生产操作；所有生产写入有 human approval、backup、audit 和 exact metadata rollback，release rollback 不使用不存在的子命令。
</success_criteria>

<output>
只有 Task 1、Task 2 与 blocking human checkpoint 全部完成后，才创建 `.planning/phases/26-oa/26-02-SUMMARY.md`；SUMMARY 必须记录 exact release、captured previous release、approved final fingerprint、repair/rollback counts、rehydrate、queue/readiness、SLO/E2E、Audit 与必要回滚证据，并仅在全部通过时标记 `production-verified`。checkpoint pending/blocked 时不得提前创建 SUMMARY。
</output>
