---
phase: "26"
plan: "04"
type: "tdd"
wave: 4
depends_on:
  - "26-03"
requirements:
  - "TURN-CLOSURE-06"
autonomous: false
files_modified:
  - "backend/src/fin_ops_platform/services/postgres_repositories/invoice_import_page_audit.py"
  - "backend/src/fin_ops_platform/services/postgres_repositories/etc_import_page_audit.py"
  - "tests/test_audit_invoice_import_page.py"
  - "tests/test_audit_etc_import_page.py"
  - "docs/modules/imports-invoices/boundary-io.md"
  - "docs/modules/imports-invoices/tests.md"
  - "docs/modules/imports-etc-invoices/boundary-io.md"
  - "docs/modules/imports-etc-invoices/tests.md"
must_haves:
  truths:
    - "Phase 26 system Audit must not fail because a strict invoice keeps a known pre-contract invoice-batch source edge; only the strict batch subset participates in exact edge equality, while unknown batch references still fail closed."
    - "A succeeded ETC import session remains valid after its exact reconciliation task advances from imported to the legitimate closed state."
    - "The closure changes only read-only Audit classification; it writes no production data and changes no page API, read model, worker, queue or business state machine."
---

<objective>
Close the two production-only false-positive page Audit blockers discovered after Plan 26-03, then rerun the exact-release Phase 26 production gate.

Purpose: preserve strict current-contract proof without treating known legacy provenance or a legitimate later ETC task state as corruption.
Output: two narrow Audit fixes, regression tests, synchronized module facts and exact-release production System Audit evidence.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Separate strict and known-legacy invoice provenance edges</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/invoice_import_page_audit.py, tests/test_audit_invoice_import_page.py</files>
  <action>Pass the full known invoice-batch identity set into the existing canonical invoice proof. Keep exact row/source-link equality limited to version-registered strict batches; accept a source link to a known legacy invoice batch as unproven legacy provenance, and continue to reject a missing or non-invoice batch reference. Add red/green regressions for both cases.</action>
  <verify>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_audit_invoice_import_page.py</verify>
  <done>Known legacy invoice provenance no longer creates a false blocking orphan, while an unknown batch still fails closed.</done>
</task>

<task type="auto">
  <name>Task 2: Accept the legitimate ETC imported-to-closed transition</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/etc_import_page_audit.py, tests/test_audit_etc_import_page.py</files>
  <action>Reuse the existing shared covered task-status set for succeeded sessions so imported and closed are both valid. Keep partial-success and every other status contract unchanged. Add a focused regression with complete terminal output edges.</action>
  <verify>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_audit_etc_import_page.py tests/test_audit_etc_tickets_read_model_tool.py</verify>
  <done>A later closed task does not invalidate a succeeded import session, and invalid terminal task states remain blocking.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize contracts and run affected regression</name>
  <files>docs/modules/imports-invoices/boundary-io.md, docs/modules/imports-invoices/tests.md, docs/modules/imports-etc-invoices/boundary-io.md, docs/modules/imports-etc-invoices/tests.md</files>
  <action>Record the strict-versus-legacy provenance boundary and imported/closed task transition. Run affected page/System Audit tests, lint, docs and diff checks. Do not add a repair tool, migration, endpoint, compatibility branch or runtime component.</action>
  <verify>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_audit_invoice_import_page.py tests/test_audit_etc_import_page.py tests/test_audit_etc_tickets_read_model_tool.py tests/test_audit_app_health_system.py tests/test_app_health_api.py &amp;&amp; bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh docs &amp;&amp; git diff --check</verify>
  <done>The two page proofs and System Audit orchestration remain read-only and regression-protected.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Deploy exact commit and repeat the Phase 26 production gate</name>
  <files>No repository file is edited by this checkpoint.</files>
  <action>Commit and push the scoped closure, deploy through the production entrypoint, verify exact active release identity, then rerun both import page Audits, the 17-page System Audit, queue/worker/readiness checks, critical read-model SLO smoke, public/session route checks and measured latency. Any remaining blocking issue creates the next smallest evidenced gap; it is not hidden in Phase 27.</action>
  <verify>Both import page Audits and the database-internal System Audit pass; current-effective queue is drained; required workers are current; the exact release guard passes; measured page and readiness evidence meets the documented target.</verify>
  <done>Phase 26 has complete exact-release production correctness and performance evidence and can be closed before Phase 27 starts.</done>
</task>

</tasks>
