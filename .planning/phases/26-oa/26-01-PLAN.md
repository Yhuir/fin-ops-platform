---
phase: "26"
plan: "01"
type: "tdd"
wave: 1
depends_on:
  - "21-03"
requirements:
  - "TURN-CLOSURE-01"
  - "TURN-CLOSURE-02"
  - "TURN-CLOSURE-03"
  - "TURN-CLOSURE-06"
autonomous: true
files_modified:
  - "backend/src/fin_ops_platform/services/workbench_relation_requirements.py"
  - "backend/src/fin_ops_platform/services/turnover_ledger_write_adapters.py"
  - "backend/src/fin_ops_platform/services/no_oa_bank_batch_application_service.py"
  - "backend/src/fin_ops_platform/app/server.py"
  - "tests/test_workbench_relation_grouping.py"
  - "tests/test_turnover_ledger_uow_contract.py"
  - "tests/test_turnover_workbench_integration.py"
  - "tests/test_turnover_ledger_api.py"
  - "tests/test_workbench_sql_runtime.py"
  - "tests/test_no_oa_bank_batch_tag_selection_api.py"
  - "web/src/test/WorkbenchApi.test.ts"
  - "web/src/test/RelationGroupGrid.test.tsx"
  - "docs/product-specs/bank-turnover-and-no-oa.md"
  - "docs/modules/turnover-ledger/README.md"
  - "docs/modules/turnover-ledger/boundary-io.md"
  - "docs/modules/turnover-ledger/state-machine.md"
  - "docs/modules/turnover-ledger/tests.md"
  - "docs/modules/turnover-ledger/implementation-notes.md"
  - "docs/modules/reconciliation-workbench/tests.md"
  - "docs/modules/reconciliation-workbench/implementation-notes.md"
  - "docs/modules/bank-flow-rule-batches/implementation-notes.md"
must_haves:
  truths:
    - "turnover_manual_closure active relation 只表达 canonical ownership；冻结要求未满足时同 case 进入 unpaired。"
    - "确认时复用实际标签与 canonical policy helper，冻结 tag codes、rule version、source 和 requires flags。"
    - "合并后的所有 bank members 必须是 selected bank ids 子集，否则使用现有 conflict 路径 fail closed。"
    - "selected-row cache 覆盖最终 bank members，银行 list I/O 不增加，rules provider 每次确认至多一次。"
    - "legacy no-OA rule save 不再扫描或追溯改写 turnover relations；旧链完整删除。"
    - "API/DTO/production frontend、ETC/batch-accounting 例外和其它 relation/page I/O 不变。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/workbench_relation_requirements.py"
      provides: "不再绕过 turnover frozen requirements 的统一 completion 判定"
    - path: "backend/src/fin_ops_platform/services/turnover_ledger_write_adapters.py"
      provides: "selected-member 完整性校验和真实 policy snapshot"
    - path: "backend/src/fin_ops_platform/services/no_oa_bank_batch_application_service.py"
      provides: "无 turnover relation 追溯同步的 legacy no-OA 边界"
    - path: "tests/test_turnover_workbench_integration.py"
      provides: "bank-only 等待 OA、补 OA 后 paired、其它 relation mode 不回归"
  key_links:
    - from: "server.py"
      to: "TurnoverLedgerConfirmPrimaryWriteFacadeBuilder"
      via: "显式注入现有 rules provider；server 只 wiring"
    - from: "TurnoverLedgerConfirmPrimaryWriteFacadeBuilder"
      to: "TurnoverLedgerWorkbenchPairPort"
      via: "共享 request-scoped rows_by_ids 与 rules provider"
    - from: "TurnoverLedgerWorkbenchPairPort"
      to: "build_bank_relation_requirement_metadata"
      via: "selected effective tag codes + 单次 canonical rules payload"
    - from: "canonical active relation"
      to: "Workbench paired/unpaired"
      via: "evaluate_bank_relation_completion；无 route/frontend 二次推断"
threat_model:
  new_attack_surface: "none"
  controls:
    - "HTTP endpoint、权限 session、actor、request/response shape 不变。"
    - "既有 expected_versions、idempotency、member lock、UoW atomicity、audit/outbox 不得退化。"
    - "provider/rows/rules 不可解释时 fail closed，不回退写死 metadata 或 legacy settings family。"
    - "最终 bank member 超出 selected ids 时冲突退出，不静默读取或吸收额外成员。"
---

<objective>
修复在线 turnover closure 写入与 Workbench completion 根因：active 只表示 ownership，relation 创建时冻结真实 Bank Transaction Paired Policy；删除 legacy no-OA 追溯同步，并以最小测试和文档保护其它 relation/page 不受影响。

Purpose: 让截图场景 bank-only relation 留在未配对等待 OA，同时保持现有 canonical relation/UoW/read-model 架构。
Output: 在线代码、失败先行测试、旧链零引用证据和最小长期文档更新；不包含历史 repair、v6 或生产操作。
</objective>

<must_haves>
- 覆盖 Context D-01、D-02、D-03、D-04、D-06。
- 不新增 API、DTO、表、migration、worker、read model、service、repository、helper、frontend production component 或依赖。
- 只删除 turnover completion 例外；ETC/batch-accounting 既有例外不变。
- 最终 relation 所有 bank members 都由本次 selected rows 覆盖，才能冻结 policy。
- no-OA rule save relation list/update I/O 为零。
</must_haves>

<threat_model>
- 输入仍由既有 route/request boundary 校验；service 不读取 HTTP cookie/header。
- selected-member guard 在 relation command 前 fail closed，防止部分 policy snapshot 获得 active ownership。
- relation mutation 继续通过 WorkbenchRelationCommandService/UoW；禁止 direct SQL 或 parallel fact path。
- 删除旧同步后，历史纠错只能进入 26-02 的受控 repair，不得新增隐藏 fallback。
</threat_model>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
当前 bypass 使 turnover_manual_closure 无视 relation metadata 自动 complete；写 adapter 又写死 OA=true/invoice=false。已有 helper、request-scoped selected-row cache、command/UoW 和前端 missing-row 渲染全部可复用。在线修复必须把真实 policy 写入 relation，并在合并 OA-bank relation 时证明最终 bank members 没有超出 selected ids。

项目上下文：
@.planning/phases/26-oa/26-CONTEXT.md
@.planning/phases/26-oa/26-RESEARCH.md
@.planning/phases/01-turnover-ledger-improvements/PLAN.md
@docs/modules/turnover-ledger/README.md
@docs/modules/turnover-ledger/boundary-io.md
@docs/modules/turnover-ledger/state-machine.md
@docs/modules/turnover-ledger/tests.md
@docs/modules/reconciliation-workbench/boundary-io.md
@docs/modules/workbench-relations/boundary-io.md
@docs/modules/bank-flow-rule-batches/boundary-io.md
</context>

<tasks>

<task type="auto">
  <name>Task 1：先写失败测试，锁定 completion、policy snapshot 和 selected-member 合同</name>
  <read_first>
    - backend/src/fin_ops_platform/services/workbench_relation_requirements.py
    - backend/src/fin_ops_platform/services/turnover_ledger_write_adapters.py
    - tests/test_workbench_relation_grouping.py
    - tests/test_turnover_ledger_uow_contract.py
    - tests/test_turnover_workbench_integration.py
    - tests/test_turnover_ledger_api.py
    - tests/test_workbench_sql_runtime.py
    - tests/test_no_oa_bank_batch_tag_selection_api.py
    - web/src/test/WorkbenchApi.test.ts
    - web/src/test/RelationGroupGrid.test.tsx
    - web/e2e/turnover-ledger-flow.spec.ts
  </read_first>
  <files>
    - tests/test_workbench_relation_grouping.py
    - tests/test_turnover_ledger_uow_contract.py
    - tests/test_turnover_workbench_integration.py
    - tests/test_turnover_ledger_api.py
    - tests/test_workbench_sql_runtime.py
    - tests/test_no_oa_bank_batch_tag_selection_api.py
    - web/src/test/WorkbenchApi.test.ts
    - web/src/test/RelationGroupGrid.test.tsx
  </files>
  <action>
1. 把现有“bank-only turnover closure paired”断言改为：relation active、同 case 在 unpaired、`completion.is_complete=false`、`missing_row_types=["oa"]`、paired 不含这些 members。
2. 添加 requirement 矩阵：OA-only、invoice-only、双 true、双 false；补齐 required type 后 paired；missing/unknown/empty fail closed；多标签要求分别 OR。
3. 添加 adapter/UoW recorder：metadata source/tag codes/version/requires 来自 helper；rules provider 每次 confirm 恰好一次或至多一次；bank rows provider 调用数不高于当前基线。
4. 添加合并负例：既有 OA-bank relation 包含任何未在 selected ids 中的 bank member时，确认走现有 conflict error，relation/history/outbox 无半写，且不触发第二次 bank list I/O。
5. 添加正例：既有 relation 只含 selected banks + OA 时允许合并；bank-only OA-required unpaired，加入 OA 后同 case paired；双 false bank-only 直接 paired。
6. 保留 API response/freshness targets、permission/version/idempotency 断言；前端只验证 missing-row mapper 和“待补 OA/发票”render，不改 production TSX。
7. 添加回归证明 ETC、batch-accounting、ordinary manual relation、bank-flow/no-OA 和 withdraw/recover 语义不变。
  </action>
  <verify>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_relation_grouping.py tests/test_turnover_ledger_uow_contract.py tests/test_turnover_workbench_integration.py -k "turnover or requirement or closure"</command>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_sql_runtime.py -k "turnover_manual_closure"</command>
    <command>npm --prefix web test -- --run WorkbenchApi.test.ts RelationGroupGrid.test.tsx</command>
    <expected>实现前 backend 新测试分别因 turnover bypass、写死 metadata、缺 selected-member guard 或旧同步存在而失败；前端既有能力回归通过。</expected>
  </verify>
  <acceptance_criteria>
    - 截图合同有精确 red test，错误 paired 预期不再存在。
    - 四种 policy、多标签 OR、unknown/missing fail-closed 全部有业务断言。
    - 未选择 bank member 合并负例断言零 mutation、零额外 bank list I/O。
    - API、权限、幂等、atomicity 和其它 relation mode 回归没有被放宽。
  </acceptance_criteria>
  <done>失败测试明确区分直接根因、写入缺陷和 merge 完整性缺口，且不依赖截图真实数据。</done>
</task>

<task type="auto">
  <name>Task 2：修在线根因、冻结真实 policy 并删除 legacy 同步</name>
  <read_first>
    - backend/src/fin_ops_platform/services/workbench_relation_requirements.py
    - backend/src/fin_ops_platform/services/turnover_ledger_write_adapters.py
    - backend/src/fin_ops_platform/services/turnover_ledger_write_facade.py
    - backend/src/fin_ops_platform/services/no_oa_bank_batch_application_service.py
    - backend/src/fin_ops_platform/app/server.py
  </read_first>
  <files>
    - backend/src/fin_ops_platform/services/workbench_relation_requirements.py
    - backend/src/fin_ops_platform/services/turnover_ledger_write_adapters.py
    - backend/src/fin_ops_platform/services/no_oa_bank_batch_application_service.py
    - backend/src/fin_ops_platform/app/server.py
  </files>
  <action>
1. 在 `evaluate_bank_relation_completion` 只移除 `turnover_manual_closure` 无条件 complete 条件；保留 batch-accounting/ETC 条件与现有 metadata fail-closed 读取。
2. 给现有 Turnover builder/port 构造注入现有 rules payload provider，并把同一个 request-scoped `bank_row_selection_port.rows_by_ids` 传给 pair port；Postgres/local 共享同一 wiring，`server.py` 只传 `AppSettingsService.get_bank_flow_rule_batch_tag_rules_payload`。
3. 在 merge 后、command 前取最终 typed bank members，断言它们全部属于 normalized selected bank ids；不满足时复用现有 conflict/precondition error，不调用额外 bank provider。
4. 从 cached selected rows 读取 `effective_category_code`，必要时回退 `category_code`；一次读取 rules payload，调用 `build_bank_relation_requirement_metadata`，将返回 metadata 与 turnover evidence 合并。删除硬编码 OA/invoice/source/version。
5. provider 不可用、rows 缺失/重复/不对应 selected ids 或 rules payload 非法时 fail closed；unknown/empty tag 交给已有 helper，禁止写死 fallback。
6. 从 no-OA `update_tag_selection` 删除 turnover sync 调用，并删除 `_sync_turnover_rule_relation_requirements`、只为它服务的 relation scan/category-to-rule/current-check helpers、常量和无用 imports。保留 no-OA 自身保存、refresh、batch command 和仍被其它方法使用的 command error import。
7. whole-repo 扫描删除符号与 operation/source 名；不得留下 dead wrapper、兼容 branch 或平行 runtime path。
  </action>
  <verify>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_relation_grouping.py tests/test_turnover_ledger_uow_contract.py tests/test_turnover_workbench_integration.py tests/test_turnover_ledger_api.py tests/test_no_oa_bank_batch_tag_selection_api.py</command>
    <command>rg -n "_sync_turnover_rule_relation_requirements|turnover_rule_tag_requirement_sync|turnover_rule_tag_rules_changed|TURNOVER_RULE_REQUIREMENT_SOURCE|TURNOVER_RULE_CATEGORY_ROOTS" backend tests web</command>
    <expected>目标 tests 全绿；旧同步符号扫描无输出；recorder 证明 rules <= 1 次、bank list 调用数不增加、no-OA rule save relation scan/update 为 0。</expected>
  </verify>
  <acceptance_criteria>
    - 新 turnover relation 使用 `paired_requirement_source=bank_transaction_paired_policy`、真实 tags/version/requires。
    - 最终 bank members 严格为 selected ids 子集；超集情况 fail closed 且无半写。
    - bank-only OA-required relation 在 Workbench unpaired，OA 加入后同 case paired。
    - legacy no-OA rule save 不再枚举/更新 turnover relation。
    - server.py 无业务逻辑，service 无 HTTP/SQL 污染，无新抽象/fallback。
  </acceptance_criteria>
  <done>在线唯一链路为 cached selected rows + existing policy helper + existing relation command/UoW + shared completion grouping。</done>
</task>

<task type="auto">
  <name>Task 3：完成最小文档、七类回归与自动验证</name>
  <read_first>
    - docs/product-specs/bank-turnover-and-no-oa.md
    - docs/modules/turnover-ledger/README.md
    - docs/modules/turnover-ledger/boundary-io.md
    - docs/modules/turnover-ledger/state-machine.md
    - docs/modules/turnover-ledger/tests.md
    - docs/modules/turnover-ledger/implementation-notes.md
    - docs/modules/reconciliation-workbench/tests.md
    - docs/modules/reconciliation-workbench/implementation-notes.md
    - docs/modules/bank-flow-rule-batches/implementation-notes.md
  </read_first>
  <files>
    - docs/product-specs/bank-turnover-and-no-oa.md
    - docs/modules/turnover-ledger/README.md
    - docs/modules/turnover-ledger/boundary-io.md
    - docs/modules/turnover-ledger/state-machine.md
    - docs/modules/turnover-ledger/tests.md
    - docs/modules/turnover-ledger/implementation-notes.md
    - docs/modules/reconciliation-workbench/tests.md
    - docs/modules/reconciliation-workbench/implementation-notes.md
    - docs/modules/bank-flow-rule-batches/implementation-notes.md
  </files>
  <action>
1. 修正仍把 active 等同 paired、写死 turnover requirement、或描述规则保存追溯同步历史 relation 的旧段落；已正确文档不机械重写。
2. 在 turnover boundary/state/tests 记录 selected-bank 完整性、cached I/O、单次 rules provider、frozen snapshot、incomplete same-case unpaired 和 legacy sync 删除。
3. 在 Workbench/bank-flow 实施或测试记录中只补本次真实差异；明确 API/DTO/worker/read-model schema 本计划未变。
4. 七类测试全部登记：business、service/UoW、API、read-model projection、frontend mapper/render、E2E、existing regressions。
5. 按 targeted -> lint -> backend affected -> frontend -> E2E -> docs/diff 顺序验证；不运行 repair CLI，不连接生产 DB。
  </action>
  <verify>
    <command>bash scripts/verify.sh lint</command>
    <command>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_relation_grouping.py tests/test_turnover_ledger_uow_contract.py tests/test_turnover_workbench_integration.py tests/test_turnover_ledger_api.py tests/test_workbench_sql_runtime.py tests/test_no_oa_bank_batch_tag_selection_api.py</command>
    <command>npm --prefix web test -- --run WorkbenchApi.test.ts RelationGroupGrid.test.tsx</command>
    <command>npm --prefix web run e2e -- e2e/turnover-ledger-flow.spec.ts --project=chromium</command>
    <command>npm --prefix web run build</command>
    <command>bash scripts/verify.sh docs</command>
    <command>git diff --check</command>
    <expected>自动 gate 全绿；无 production repair/deploy/rehydrate；旧符号运行时零引用。</expected>
  </verify>
  <acceptance_criteria>
    - 只更新真正过时文档，事实源一致且无 prompt 复制。
    - 七类测试均有实际入口和失败/回归路径。
    - 自动验证不调用真实 repair、生产 token 或真实业务 fixture。
    - 变更文件限于本计划列表，无新 runtime component 或 I/O owner。
  </acceptance_criteria>
  <done>26-01 在线代码、测试、文档与旧链删除闭环完成，可进入 26-02 历史修复和 v6 发布准备。</done>
</task>

</tasks>

<verification>
- `git diff --check` 与 docs/lint 通过。
- 目标 backend/frontend/E2E 全绿，错误 paired 断言已消失。
- provider call-count、selected-member guard、no-OA zero-scan 有机械测试。
- whole-repo old symbol scan 无 runtime 引用。
- 本计划没有运行 repair CLI、deploy-control 或生产操作。
</verification>

<success_criteria>
1. TURN-CLOSURE-01/02/03/06 有测试与实现证据。
2. D-01/D-02/D-03/D-04/D-06 全部覆盖，无决策遗漏。
3. 在线链路最小且模块化：server wiring -> Turnover adapter -> existing helper -> relation UoW -> shared grouping。
4. 其它 relation mode、页面 API/DTO、worker/read model 合同不变。
5. 旧 no-OA turnover sync 完整删除且规则保存性能不再包含 relation 全表扫描。
</success_criteria>

<output>
完成后创建 `.planning/phases/26-oa/26-01-SUMMARY.md`，记录修改文件、删除符号、七类测试结果、provider I/O 计数证据、docs impact 和未执行的生产步骤。不要把 Phase 26 标记 complete；26-02 仍负责 repair/v6/production checkpoint。
</output>
