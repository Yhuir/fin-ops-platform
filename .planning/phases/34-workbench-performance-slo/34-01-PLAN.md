---
phase: 34-workbench-performance-slo
plan: "01"
type: execute
wave: 1
depends_on:
  - 30-05
files_modified:
  - backend/src/fin_ops_platform/app/server.py
  - backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
  - web/src/features/workbench/api.ts
  - tests/test_workbench_write_characterization.py
  - tests/test_postgres_repositories_boundaries.py
  - web/src/test/WorkbenchApi.test.ts
  - docs/modules/reconciliation-workbench/implementation-notes.md
  - docs/modules/reconciliation-workbench/tests.md
  - docs/modules/read-models/implementation-notes.md
  - docs/modules/read-models/tests.md
autonomous: true
requirements:
  - RMF-02
  - RMF-03
  - RMF-08
must_haves:
  truths:
    - "关联台页面继续只消费 fresh active-generation workbench，不直接消费共享 workbench_relation。"
    - "确认提交继续使用 operation projection 立即反馈；撤回继续等待真实 fresh，不伪造同步完成。"
    - "相同 combined-initial 只在请求仍进行且无独立 AbortSignal 时合并；完成后不保留 payload cache。"
    - "workbench_relation full/partial/empty scope writer 均能更新 metadata，失败 scope 通过正式队列恢复。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/read_models.py"
      provides: "共享 workbench_relation scope metadata writer 的正确绑定"
    - path: "web/src/features/workbench/api.ts"
      provides: "combined-initial exact URL in-flight request coalescing"
  key_links:
    - from: "web/src/pages/ReconciliationWorkbenchPage.tsx"
      to: "web/src/features/workbench/api.ts"
      via: "fetchWorkbenchInitialPage"
      pattern: "fetchWorkbenchInitialPage"
    - from: "backend/src/fin_ops_platform/services/workbench_relation_sql_projection.py"
      to: "backend/src/fin_ops_platform/services/postgres_repositories/read_models.py"
      via: "save/partial/empty workbench_relation scope"
      pattern: "workbench_relation_scope"
---

<objective>
用最小改动消除关联台重复首屏 I/O，并修复阻断共享 relation projection 的生产根因；保持现有
active-generation、freshness、queue、worker、CAS、幂等、审计与写后交互合同。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 workbench_relation scope writer 绑定</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/read_models.py, tests/test_postgres_repositories_boundaries.py</files>
  <action>把现有无实例状态 helper 明确为 staticmethod，并用 full、partial、empty 三条现有 repository 入口保护；不新增 writer、SQL、表、队列或兼容分支。</action>
  <verify>PYTHONPATH=backend/src python3 -m pytest -q tests/test_postgres_repositories_boundaries.py</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 合并 exact in-flight combined-initial HTTP 请求</name>
  <files>web/src/features/workbench/api.ts, web/src/test/WorkbenchApi.test.ts</files>
  <action>复用现有 API client，在同 URL、无 AbortSignal、请求仍进行时共享 raw payload Promise；完成或失败立即删除。搜索/筛选的可取消请求继续独立，不新增 Redis、TTL、全局数据缓存或状态库。</action>
  <verify>npm --prefix web test -- --run src/test/WorkbenchApi.test.ts</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 修复 relation action canonical row resolver 接线</name>
  <files>backend/src/fin_ops_platform/app/server.py, tests/test_workbench_write_characterization.py</files>
  <action>把正式 confirm 与 withdraw OA alias 解析接回生产已配置的 active-generation SQL row-detail owner；保留 canonical relation command/UoW、CAS、幂等、审计和 live-missing fallback，不把 preview selection DTO 传入正式写入。</action>
  <verify>PYTHONPATH=backend/src python3 -m pytest -q tests/test_workbench_write_characterization.py tests/test_platform_runtime_boundary_guards.py</verify>
</task>

<task type="auto">
  <name>Task 4: 定向验证、单次发布与生产闭环</name>
  <files>docs/modules/reconciliation-workbench/implementation-notes.md, docs/modules/reconciliation-workbench/tests.md, docs/modules/read-models/implementation-notes.md, docs/modules/read-models/tests.md</files>
  <action>运行受影响 repository/read-model/worker/API/frontend/build/lint 门，不跑无关全量浏览器套件。提交并 push main 后使用官方部署入口；正式重试 failed exact scope，验证 queue drained、read model fresh、Page/System Audit，并用认证 HTTP probe 与 test-owned 可逆 confirm/withdraw scenario 记录延迟。</action>
  <verify>bash scripts/verify.sh lint</verify>
</task>

</tasks>

<success_criteria>
- 相同 background combined-initial 并发请求只产生一次网络 I/O，完成后下一次读取仍访问服务器。
- workbench_relation 不再因 Python method binding 进入 dead letter。
- confirm committed feedback p95 目标不超过 1 秒；withdraw 最终 fresh 目标 p95 不超过 2 秒、p99 不超过 3 秒。
- 正确性、幂等、审计、zero write-time fan-out、queue drain 与 active-generation fresh 均为硬门。
</success_criteria>

<output>
Create `.planning/phases/34-workbench-performance-slo/34-01-SUMMARY.md` when done.
</output>
