---
phase: 12-etc-tickets-improvements
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/EtcTicketManagementPage.tsx
  - web/src/features/etc/api.ts
  - web/src/features/etc/types.ts
  - web/src/test/EtcTicketManagementPage.test.tsx
  - web/src/test/EtcApi.test.ts
  - web/e2e/etc-tickets-flow.spec.ts
  - backend/src/fin_ops_platform/app/routes_etc.py
  - backend/src/fin_ops_platform/app/routes_etc_reconciliation.py
  - backend/src/fin_ops_platform/app/server.py
  - backend/src/fin_ops_platform/services/etc_business_batch_application_service.py
  - backend/src/fin_ops_platform/services/etc_service.py
  - backend/src/fin_ops_platform/services/etc_reconciliation_service.py
  - backend/src/fin_ops_platform/services/etc_reconciliation_task_payload_facade.py
  - backend/src/fin_ops_platform/services/state_store_protocol.py
  - backend/src/fin_ops_platform/services/state_store.py
  - backend/src/fin_ops_platform/services/postgres_state_store.py
  - backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py
  - backend/src/fin_ops_platform/services/postgres_repositories/etc_tickets_page_audit.py
  - tests/test_etc_backend.py
  - tests/test_platform_runtime_boundary_guards.py
  - docs/modules/etc-tickets/README.md
  - docs/modules/etc-tickets/boundary-io.md
  - docs/modules/etc-tickets/state-machine.md
  - docs/modules/etc-tickets/tests.md
  - docs/modules/etc-tickets/implementation-notes.md
  - docs/dev/api-contracts.md
  - docs/operations/etc-business-batches.md
autonomous: false
requirements:
  - PAGE-14
  - PAGE-04
  - PAGE-05
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "ETC 页面使用 business batch 作为唯一可见事实源，并展示互斥的未提交、暂存、已提交三 bucket。"
    - "oa_confirmation_pending 是唯一暂存状态；未创建/需修改 OA 返回未提交且保留批次、发票、上传文件和核对结果。"
    - "首屏不读取全量 reconciliation task，不重复读取同一 detail，不在 list/detail 访问对象存储。"
    - "OA 外部 I/O 不持 ETC 业务锁或数据库事务；幂等、明确失败和结果未知都有 durable 收敛。"
    - "ETC Page Audit 会阻止悬挂 creating、无效 pending 和三 bucket/关系不一致通过。"
    - "旧双选择、全量 task consumer、重复 detail、详情级 list payload 和锁内 OA I/O 被删除且有静态 guard。"
    - "其他页面代码/read model/worker/DTO 不被修改，合法 canonical 下游影响仍走现有 lifecycle/relation 边界。"
  artifacts:
    - path: "web/src/pages/EtcTicketManagementPage.tsx"
      provides: "三 bucket、单选择事实源、一次 detail、明确 action reason"
    - path: "backend/src/fin_ops_platform/services/etc_business_batch_application_service.py"
      provides: "ETC query/command 编排和统一 action contract"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py"
      provides: "direct-canonical business batch/task 窄查询"
    - path: "backend/src/fin_ops_platform/services/etc_service.py"
      provides: "prepare/execute/finalize OA draft 状态闭环"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/etc_tickets_page_audit.py"
      provides: "三 bucket、creating/pending/占用关系 fail-closed Audit"
  key_links:
    - from: "web/src/pages/EtcTicketManagementPage.tsx"
      to: "/api/etc/business-batches*"
      via: "summary/detail/action contract"
    - from: "EtcBusinessBatchApplicationService"
      to: "state store/PostgresOpsTaxEtcRepository"
      via: "narrow query I/O; no full snapshot on page reads"
    - from: "EtcBusinessBatchApplicationService"
      to: "EtcOAClient"
      via: "prepare commit -> unlocked external execute -> finalize CAS"
    - from: "ETC command"
      to: "existing lifecycle/relation boundary"
      via: "canonical invoice/relation updates only after existing business transitions"
---

<objective>
在不引入 ETC read model/cache/worker 的前提下，完成 ETC 票据页面的高性能 direct-canonical 读取、三 bucket 暂存闭环、提交按钮真实原因与恢复、OA 草稿幂等/锁外 I/O、Page Audit fail-closed、旧链删除和跨页面回归保护。

Purpose: 把当前约 3 秒级请求瀑布和永久 `oa_draft_creating` 卡死改为可度量、可恢复、模块隔离的生产链路。
Output: 可部署代码、测试、文档和生产验证/回滚清单；计划执行不自动部署或修改生产。
</objective>

<execution_constraints>
- 在 `main` 工作，但开始前确认工作树归属；不得覆盖其他 thread 的未提交改动。
- 只改 ETC 及其直属 state store/Audit/测试/文档边界。若必须修改其他页面代码/read model/worker/DTO，停止并重新确认范围。
- 不新增依赖、read model、cache、queue、worker、WebSocket、通用 workflow/policy 框架或 feature flag。
- 复用 `oa_confirmation_pending`、`submission_batch_id`、`etc_batch_id/oa_marker`、现有 state store/repository、OA client、Audit 和 lifecycle/relation command。
- 默认不做 schema migration；只有 Task 1 的 durable-contract/EXPLAIN 证据证明不可避免时，先单独提出 migration 和回滚设计。
- 迭代阶段只跑相关检查；全量发布门只运行一次，禁止无意义重复 CI。
</execution_constraints>

<tasks>

<task type="execute" id="12-01-01">
  <name>锁定外部 OA 能力、consumer 和性能基线门</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/12-etc-tickets-improvements/12-CONTEXT.md</file>
    <file>.planning/phases/12-etc-tickets-improvements/12-RESEARCH.md</file>
    <file>docs/modules/etc-tickets/boundary-io.md</file>
    <file>docs/operations/etc-business-batches.md</file>
    <file>backend/src/fin_ops_platform/services/etc_service.py</file>
    <file>backend/src/fin_ops_platform/app/routes_etc.py</file>
    <file>web/src/features/etc/api.ts</file>
  </read_first>
  <action>
    在写业务代码前完成并记录四项证据：一是全仓扫描 `/api/etc/reconciliation-tasks`、`fetchEtcReconciliationTasks`、business batch list/detail、旧 `/api/etc/batches*`、OA detection、`UploadedEtcZipFile` 的 production/frontend/script/probe/deploy/test/docs 调用方；二是确认 `/api/etc/reconciliation-tasks` 的正式导入/核对 consumer，禁止把“从首屏移除”误写成“删除正式 API”；三是以现有 OA HTTP client/非破坏性 provider 文档或受控 probe 证明 OA 是否支持 idempotency 或按稳定 `business_batch_id/oa_marker` 查询；四是保存当前生产只读 list/task-list/detail/waterfall/payload/Audit 基线和卡死 batch 证据，不修改该 batch。把结论写入 `docs/modules/etc-tickets/implementation-notes.md` 的本轮实施段。若 OA 两项能力均无证据，锁定人工恢复路径，不计划自动 exactly-once；若发现外部 consumer，保留其正式合同但通过 architecture guard 隔离新页面。
  </action>
  <acceptance_criteria>
    <criterion>implementation notes 明确列出每个旧符号/route 的调用方、删除/保留结论和 owner attestation 状态。</criterion>
    <criterion>OA 能力结论为“verified idempotency”“verified marker lookup”或“neither verified”；不得使用猜测。</criterion>
    <criterion>生产基线包含至少 list、full task list、detail、重复 detail、payload bytes 和 ETC Page Audit。</criterion>
    <criterion>本任务不修改生产数据、不部署、不创建 OA 草稿。</criterion>
  </acceptance_criteria>
  <verify>
    <command>git diff --check</command>
    <command>rg -n "fetchEtcReconciliationTasks|/api/etc/reconciliation-tasks|fetchEtcBusinessBatchDetail|oa_draft_creating|oa_confirmation_pending|UploadedEtcZipFile" web/src backend/src tests scripts deploy docs</command>
  </verify>
</task>

<task type="execute" id="12-01-02">
  <name>先用合同测试固定三 bucket、唯一选择和性能调用预算</name>
  <read_first>
    <file>web/src/test/EtcTicketManagementPage.test.tsx</file>
    <file>web/src/test/EtcApi.test.ts</file>
    <file>tests/test_etc_backend.py</file>
    <file>tests/test_platform_runtime_boundary_guards.py</file>
    <file>backend/src/fin_ops_platform/services/postgres_repositories/etc_tickets_page_audit.py</file>
    <file>web/src/pages/EtcTicketManagementPage.tsx</file>
  </read_first>
  <action>
    在实现前新增/调整最少的失败测试，固定：`unsubmitted/staged/submitted` 三集合互斥且计数同口径；`oa_confirmation_pending` 只属于 staged；not_submitted 返回 unsubmitted 并保留 batch/invoices/task/source-file/核对摘要；页面首屏不调用 `GET /api/etc/reconciliation-tasks`；一个选中 batch 只调用一次 detail；list/detail 对 object storage spy 调用为 0；后端输出 `createOaDraftAction.enabled/code/message` 且 command 使用同一资格判定；read-only/错误状态显示明确原因；creating 超时和 pending 缺 draft 使 Audit 失败。architecture guard 直接禁止 ETC 页面重新导入/调用 full task list helper和旧双选择符号。
  </action>
  <acceptance_criteria>
    <criterion>测试在旧实现上至少因 full task 请求、重复 detail、两 bucket 或缺 Audit creating gate 之一失败。</criterion>
    <criterion>测试断言用户可见行为/API 字段和 I/O 次数，不绑定私有 React state 或 SQL 文本实现细节。</criterion>
    <criterion>guard 只约束 ETC 页面新链，不禁止 Import Center/核对流程的正式 ready/detail API。</criterion>
  </acceptance_criteria>
  <verify>
    <command>python3 -m unittest tests.test_etc_backend tests.test_platform_runtime_boundary_guards</command>
    <command>cd web &amp;&amp; npm test -- --run src/test/EtcApi.test.ts src/test/EtcTicketManagementPage.test.tsx</command>
  </verify>
</task>

<task type="execute" id="12-01-03">
  <name>把 ETC 页面读取收敛为 direct-canonical 窄查询</name>
  <read_first>
    <file>backend/src/fin_ops_platform/services/state_store_protocol.py</file>
    <file>backend/src/fin_ops_platform/services/state_store.py</file>
    <file>backend/src/fin_ops_platform/services/postgres_state_store.py</file>
    <file>backend/src/fin_ops_platform/services/postgres_repositories/ops_tax_etc.py</file>
    <file>backend/src/fin_ops_platform/services/etc_business_batch_application_service.py</file>
    <file>backend/src/fin_ops_platform/services/etc_reconciliation_task_payload_facade.py</file>
    <file>backend/src/fin_ops_platform/app/server.py</file>
  </read_first>
  <action>
    在现有 state store/repository 边界增加 `list_etc_business_batch_summaries(...)`、`get_etc_business_batch_record(...)`、`list_etc_invoice_records_by_ids(...)` 和 `get_etc_reconciliation_task_record(...)` 四个窄读合同，不新建 projection：business batch summary list 在 SQL 内完成 actor owner/org scope、`bucket=unsubmitted|staged|submitted`、plate/keyword、排序、分页和三 bucket counts；detail 只读取一个 business batch 及其 invoice IDs 对应行；task 只按绑定 task ID 读取当前面板需要的精确数据。PostgreSQL 不调用 `load_etc_state/load_etc_reconciliation_state`，local/test store 从现有 snapshot 实现同一合同，不能使用运行时 `hasattr` 隐藏 fallback。summary DTO 删除 `invoiceIds/importAttempts/auditEvents` 和 task 嵌套数组；detail 的 `hasPdf/hasXml` 根据已验证持久化引用判断，真实下载/Audit 继续读对象并校验 hash。增加纯函数 `evaluate_etc_oa_draft_action(batch, task, actor)`，由 list/detail action contract 与 create command 共用，不增加 generic policy engine。只有 EXPLAIN 证明查询不能满足门槛时才添加一个精确命中查询的索引提案；本任务默认无 migration。
  </action>
  <acceptance_criteria>
    <criterion>business batch list/detail production path 不调用 `_reload_from_state_store()` 或全量 load methods。</criterion>
    <criterion>list 输出不含 `invoiceIds`、`importAttempts`、`auditEvents`；detail 按业务需要保留。</criterion>
    <criterion>list/detail object storage call=0，下载路径仍验证真实字节/hash。</criterion>
    <criterion>owner scope、filter、pagination、counts 使用同一 canonical predicate；无 task-only 记录混入。</criterion>
    <criterion>query 模块只读 PostgreSQL/local state，不写 queue、read model 或其他页面表。</criterion>
  </acceptance_criteria>
  <verify>
    <command>python3 -m unittest tests.test_etc_backend</command>
    <command>bash scripts/verify.sh lint</command>
  </verify>
</task>

<task type="execute" id="12-01-04">
  <name>重构前端为三 bucket 和一个 business batch 选择事实源</name>
  <read_first>
    <file>web/src/pages/EtcTicketManagementPage.tsx</file>
    <file>web/src/features/etc/api.ts</file>
    <file>web/src/features/etc/types.ts</file>
    <file>web/src/test/EtcTicketManagementPage.test.tsx</file>
    <file>docs/modules/input-invoice-usage/oa-reverse-design.md</file>
  </read_first>
  <action>
    将顶部切换改为 `未提交 | 暂存 | 已提交`，更新 types/API mapper 使用新的 bucket/count/action contract；`selectedBusinessBatchId` 成为唯一 selection owner。删除 initial `loadReconciliationTasks`、`reconciliationTasks/selectedTaskId` 的并行选择、`selectedTaskImportBatchId/selectedTaskImportBatchCanSubmit`、第二个 batch detail effect 和旧两 bucket helper。首屏 summary 到达即显示并允许由 action contract 决定按钮；详情独立渐进加载，不用全页遮罩阻塞 list。暂存列表只展示确认所需摘要和“我已在 OA 提交”“我未在 OA 提交/需要修改”；关闭弹窗/刷新不清除 pending。not_submitted 成功响应立即把 batch 移回未提交并保留详情，随后只做精确校验读取。按钮禁用时显示后端 code 对应的明确中文原因；read-only 权限仍优先。复用 OA reverse 的交互语义和现有 ETC 样式组件，不跨模块 import state/repository，不做 UI 重设计或新依赖。
  </action>
  <acceptance_criteria>
    <criterion>首屏 network test 不出现 `GET /api/etc/reconciliation-tasks`，一个 batch ID 的 detail GET 恰好一次。</criterion>
    <criterion>pending 刷新后仍在暂存；not_submitted 后回未提交且原 invoice/task/source-file 摘要可见。</criterion>
    <criterion>按钮对 imported+invoice batch 可用；creating/权限/加载/错误均显示明确原因，不是无解释灰色。</criterion>
    <criterion>页面没有 `selectedTaskImportBatchId`、`selectedTaskImportBatchCanSubmit` 和旧两 bucket mapping。</criterion>
  </acceptance_criteria>
  <verify>
    <command>cd web &amp;&amp; npm test -- --run src/test/EtcApi.test.ts src/test/EtcTicketManagementPage.test.tsx</command>
    <command>cd web &amp;&amp; npm run build</command>
  </verify>
</task>

<task type="execute" id="12-01-05">
  <name>把 OA 草稿创建改为幂等、锁外、可恢复的三段 command</name>
  <read_first>
    <file>backend/src/fin_ops_platform/app/routes_etc.py</file>
    <file>backend/src/fin_ops_platform/services/etc_business_batch_application_service.py</file>
    <file>backend/src/fin_ops_platform/services/etc_service.py</file>
    <file>backend/src/fin_ops_platform/services/etc_reconciliation_service.py</file>
    <file>web/src/features/etc/api.ts</file>
    <file>web/src/pages/EtcTicketManagementPage.tsx</file>
    <file>tests/test_etc_backend.py</file>
  </read_first>
  <action>
    route 读取必填 `idempotencyKey`，前端对一次用户 intent 生成稳定 key，重试复用同 key。把当前锁内 `create_business_batch_oa_draft` 拆为 `prepare_business_batch_oa_draft(...)`、现有 OA client execute、`complete_business_batch_oa_draft(...)`、`fail_business_batch_oa_draft(...)`：prepare 在锁内校验并创建/复用 submission batch，将其 ID 立即绑定 business batch，写 creating audit/version/updatedAt 并持久化；随后释放锁。execute 使用不可变 attempt snapshot 读取附件、上传并创建 draft。complete/fail 再锁定并核对 businessBatchId、submissionBatchId、creating version/idempotency：成功写 draft ID/URL、pending 和 task draft metadata；明确 OA 拒绝写 failed；网络超时/响应丢失抛 `EtcOADraftOutcomeUnknownError`，不自动创建新 submission batch。复用 `submission_batch_id` 作为 attempt ID、`updated_at`/creating audit 作为 startedAt，不建立 attempt 表。按 Task 1 证据二选一实现恢复：provider 可可靠查询时按 marker 采纳唯一结果；否则增加 `recover_business_batch_oa_draft(...)` 和 `POST /api/etc/business-batches/{id}/oa-draft/recover` 管理员 command，要求 expected version、reason 和外部核实证据，允许采纳指定 draft ID/URL或确认外部未创建后转 failed。所有恢复写审计，禁止直接 SQL、自动 worker、盲目 retry。外部 I/O 不得持 business lock/DB transaction；对同 batch 并发 intent 由 idempotency+CAS 收敛。
  </action>
  <acceptance_criteria>
    <criterion>相同 idempotency key 重放返回同一 submission/draft 结果；不同 payload/key 与 stale version 返回稳定 conflict。</criterion>
    <criterion>OA client 调用期间可并发完成 ETC list/detail；测试证明 business lock 未被外部 I/O 持有。</criterion>
    <criterion>明确失败、ambiguous outcome、进程恢复和 finalize persistence failure 不产生第二个 submission batch。</criterion>
    <criterion>pending 只有在 draft ID/URL 成功持久化后产生；task metadata 与 batch finalize 同一业务闭环。</criterion>
    <criterion>管理员恢复有权限、expected version、reason、审计和 provider/人工证据门；普通用户不可调用。</criterion>
  </acceptance_criteria>
  <verify>
    <command>python3 -m unittest tests.test_etc_backend</command>
    <command>bash scripts/verify.sh lint</command>
  </verify>
</task>

<task type="execute" id="12-01-06">
  <name>补齐 Page Audit、删除旧链并同步模块事实</name>
  <read_first>
    <file>backend/src/fin_ops_platform/services/postgres_repositories/etc_tickets_page_audit.py</file>
    <file>docs/modules/etc-tickets/README.md</file>
    <file>docs/modules/etc-tickets/boundary-io.md</file>
    <file>docs/modules/etc-tickets/state-machine.md</file>
    <file>docs/modules/etc-tickets/tests.md</file>
    <file>docs/dev/api-contracts.md</file>
    <file>docs/operations/etc-business-batches.md</file>
  </read_first>
  <action>
    在现有 repeatable-read read-only Audit 中加入：creating 超过 15 分钟、creating 缺 submission attempt/idempotency/audit、pending 缺 draft ID/URL/submission batch、三个 bucket 集合/计数不等、submitted/not_submitted 后 task/import/submission/invoice 占用关系不闭合。15 分钟为模块常量，不新增配置；只有生产证据证明不够再增加校准项。更新 Audit contract/summary，不把 OA 外部真实存在伪装成 PostgreSQL 可证明事实。完成全仓 caller scan 后删除 Task 1 已确认无调用方的旧 helper、serializer、双选择 mocks、CSS 和重复 `UploadedEtcZipFile`；保留正式核对/导入 API，并增加静态 guard 防止旧首屏 consumer、旧两 bucket 和锁内 OA 创建重新出现。同步 ETC README、boundary I/O、state machine、tests、API contracts、operations 和实施说明；不更新无关页面文档。
  </action>
  <acceptance_criteria>
    <criterion>当前生产形态的长时 `oa_draft_creating` fixture 使 ETC Audit fail，正常短暂 creating 不误报。</criterion>
    <criterion>合法 staged/submitted/not_submitted fixture Audit pass，缺 draft/关系或占用未释放 fixture fail。</criterion>
    <criterion>whole-repo scan 中新 ETC 页面链不再出现旧 full-task consumer、双选择、duplicate detail 和两 bucket mapping。</criterion>
    <criterion>boundary-io 明确 query/command/OA adapter/Audit 的输入输出和其他页面隔离边界。</criterion>
  </acceptance_criteria>
  <verify>
    <command>python3 -m unittest tests.test_etc_backend tests.test_platform_runtime_boundary_guards</command>
    <command>bash scripts/verify.sh lint</command>
    <command>git diff --check</command>
  </verify>
</task>

<task type="execute" id="12-01-07">
  <name>执行七类回归、性能预算和部署前闭环</name>
  <read_first>
    <file>docs/modules/etc-tickets/tests.md</file>
    <file>web/e2e/etc-tickets-flow.spec.ts</file>
    <file>backend/src/fin_ops_platform/tools/http_slo_probe.py</file>
    <file>scripts/with-production-admin-token.sh</file>
    <file>docs/operations/etc-business-batches.md</file>
  </read_first>
  <action>
    先运行 ETC 后端、API、Audit、architecture guard、前端组件/API和 ETC Playwright；再运行 Import Center ready-for-import、Workbench/Tax/Cost/OA 受影响的已有定向 regression。业务核心覆盖状态/资格/idempotency/version；service 覆盖 persistence/partial failure/lock；API 覆盖权限/shape/error；direct-canonical/Audit 覆盖集合和 I/O budgets；前端覆盖 loading/empty/error/staged/disabled reason；E2E 覆盖 imported->staged->submitted 与 staged->unsubmitted；existing regression 证明其他页面无 bug。迭代通过后只执行一次发布级全量门。使用固定 64+ 发票 fixture 验证 request 次数/bytes/DB/object-store budget；生产部署必须另获授权。部署后按 read-only list/detail/Audit canary、可回滚测试批次两出口、当前历史 stuck batch OA 核实/恢复、写后可见、ETC+其他页面混合负载顺序验证。任何 Audit、canonical count/hash、权限、其他页面结果或 SLO 失败立即停止并按现有 release rollback，不用兼容 fallback 掩盖。
  </action>
  <acceptance_criteria>
    <criterion>七类测试逐类记录适用性、命令和结果；不适用项有明确理由。</criterion>
    <criterion>list p95<=300ms/p99<=500ms；detail p95<=500ms/p99<=800ms；action-ready p95<=500ms/p99<=800ms；核心 detail ready p95<=800ms/p99<=1.2s；本地写后可见 p95<=500ms/p99<=800ms。</criterion>
    <criterion>首屏 full task GET=0、duplicate detail=0、list/detail object-store calls=0、首屏 JSON<=250KB、DB query 数固定。</criterion>
    <criterion>ETC Audit 在创建、暂存、已提交、退回未提交后均通过；ambiguous/stuck 状态明确失败并可恢复。</criterion>
    <criterion>关联台、成本统计、OA 待付款、税务和 Import Center 定向回归通过，混合负载无功能错误或明显延迟回归。</criterion>
    <criterion>未获部署授权时停在 READY_FOR_UNIFIED_DEPLOYMENT，不执行生产 mutation。</criterion>
  </acceptance_criteria>
  <verify>
    <command>python3 -m unittest tests.test_etc_backend tests.test_platform_runtime_boundary_guards</command>
    <command>cd web &amp;&amp; npm test -- --run src/test/EtcApi.test.ts src/test/EtcTicketManagementPage.test.tsx src/test/ImportCenterPage.test.tsx</command>
    <command>cd web &amp;&amp; npx playwright test e2e/etc-tickets-flow.spec.ts</command>
    <command>cd web &amp;&amp; npm run build</command>
    <command>bash scripts/verify.sh lint</command>
    <command>git diff --check</command>
  </verify>
</task>

</tasks>

<verification>
1. `git diff --check`、后端 lint、ETC 定向 backend/frontend/API/E2E 全绿。
2. architecture guard 证明 ETC page 不消费 full reconciliation task list、无旧两 bucket/双选择/duplicate detail/lock-in-I/O 回归。
3. repository/query tests 证明 direct-canonical 精确集合、owner scope、固定查询数和零对象存储 list/detail I/O。
4. state/command tests 证明 prepare/execute/finalize、idempotent replay、version conflict、明确失败、ambiguous outcome 和 recovery。
5. ETC Page Audit 对正常三 bucket pass、对悬挂/缺字段/关系不等 fail。
6. 生产前基线对比达到全部 SLO；不靠缓存预热、mock 或最优单次值。
7. 其他页面定向回归和混合负载证明功能/数据边界不受污染。
</verification>

<success_criteria>
- 生产当前按钮灰色的真实原因可见，并有安全恢复路径；不再永久卡死或盲目重复创建 OA。
- ETC 页面三 bucket 与用户确认语义完全闭合，not-submitted 不丢核对成果。
- 首屏和详情达到已锁定的 p95/p99，且 I/O 次数有固定上界。
- 新链只使用 ETC direct-canonical facts 和现有 lifecycle/relation boundary；其他页面无代码/read-model 污染。
- 所有继续影响新链的旧逻辑和无调用方代码被删除，保留的正式共享 API 有 consumer 证据和 guard。
- Audit、测试、文档、生产验证和回滚条件一致，不存在“代码完成但无法证明”的尾项。
</success_criteria>

<threat_model>
- 权限：create/confirm/not-submitted/delete/recover 都必须使用现有 mutation/admin 权限；read-only 用户只接收 action reason。
- 重放：stable idempotency + version/submission attempt CAS 防重复 OA/local batch。
- 数据丢失：not-submitted 不删除 batch/invoices/files/reconciliation；永久删除仍独立二次确认。
- 外部歧义：OA 超时不视为失败，不自动 retry；恢复要求 provider 或人工证据并审计。
- I/O 阻塞：外部对象存储/OA 调用不持业务锁/事务，避免拖垮 ETC 和共享 runtime。
- 信息安全：日志/Audit 不记录 token、密码、附件内容；只记录 actor、attempt、marker、状态和安全错误分类。
</threat_model>

## Artifacts this phase produces

- `EtcBusinessBatchBucket`/等价的 `unsubmitted|staged|submitted` API 类型与三 bucket counts。
- `createOaDraftAction` response 字段及唯一 ETC eligibility 纯函数。
- `list_etc_business_batch_summaries(...)`、`get_etc_business_batch_record(...)`、`list_etc_invoice_records_by_ids(...)`、`get_etc_reconciliation_task_record(...)` state store/PostgreSQL 窄查询方法；不新增单实现 port/factory。
- `evaluate_etc_oa_draft_action(batch, task, actor)` 纯资格函数和 `createOaDraftAction` response 字段。
- `prepare_business_batch_oa_draft(...)`、`complete_business_batch_oa_draft(...)`、`fail_business_batch_oa_draft(...)`、`recover_business_batch_oa_draft(...)` focused 方法；复用 `submission_batch_id` 为 attempt ID。
- `EtcOADraftOutcomeUnknownError` 外部结果未知分类；不建立通用 error hierarchy。
- ETC Audit issue codes：`etc_oa_draft_creating_stale`、`etc_oa_draft_attempt_missing`、`etc_oa_confirmation_draft_missing`、`etc_business_batch_bucket_mismatch`、`etc_not_submitted_occupancy_mismatch`。
- architecture guards：禁止 ETC 首屏 full task consumer、旧两 bucket/双选择/duplicate detail 和锁内外部 OA I/O 回归。

## Decision coverage

| Decisions | Covered by |
|-----------|------------|
| D-01, D-02, D-03, D-04, D-05 | Tasks 02, 04, 06, 07 |
| D-06, D-07, D-08 | Tasks 01, 02, 04, 05 |
| D-09, D-10, D-11, D-12, D-13, D-14 | Tasks 02, 03, 04, 07 |
| D-15, D-16, D-17, D-18 | Tasks 01, 02, 05, 07 |
| D-19, D-20, D-21, D-22 | Tasks 01, 02, 03, 04, 05, 06, 07 |
| D-23, D-24 | Tasks 02, 07 |

## 计划自审

- 需求覆盖：D-01..D-24 均映射到 Task 1–7；PAGE-14/PAGE-04/PAGE-05/PAR-01..03 全部进入 frontmatter 和验收。
- 模块化：query、command、OA adapter、Audit 的 I/O 分离；route 只映射，SQL 只在 repository，其他页面 read model 不参与。
- 简洁性：没有新业务状态、read model、cache、worker、queue、通用 policy/workflow、dependency 或默认 migration；复用现有字段和边界。
- 性能：删除最大瀑布和 N+1，SLO、payload、HTTP/DB/object-store 次数均可机械验收；不依赖缓存掩盖慢查询。
- 旧链：给出删除清单、consumer/owner gate 和 architecture guard；不会误删 Import Center/核对仍使用的正式 API。
- 可靠性：补齐 idempotency、外部结果未知、进程中断、version conflict、权限、Audit、历史卡死恢复和回滚。
- 隔离：文件范围虽包含共享 state store/server 组装，但只新增 ETC focused 方法；测试要求其他页面功能和混合负载无回归。
- 剩余硬门：OA provider idempotency/marker lookup 只能靠 Task 1 证据决定；计划已提供无能力时的最小人工恢复，因此不会阻塞完整闭环或诱发过度设计。
