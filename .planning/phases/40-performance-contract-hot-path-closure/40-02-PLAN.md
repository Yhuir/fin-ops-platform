---
phase: 40-performance-contract-hot-path-closure
plan: "02"
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - backend/src/fin_ops_platform/services/pending_invoice_canonical_query.py
  - backend/src/fin_ops_platform/services/postgres_repositories/invoice_usage_collection_query.py
  - backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
  - tests/test_pending_invoice_canonical_query.py
  - tests/test_pending_invoice_postgres_integration.py
  - tests/test_invoice_usage_collection_canonical_query.py
  - tests/test_invoice_usage_collection_postgres_integration.py
  - tests/test_workbench_sql_runtime.py
  - tests/test_workbench_query_postgres_integration.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-04/D-06: 只修已有代码/EXPLAIN 证据确认的三个 SQL 热点，结果口径与排序严格等价。"
    - "Pending summary 与 invoice collection supporting groups 不再重复完整事实扫描。"
    - "Workbench all-scope 不再把全部 matching group ids 聚合到 Python/SQL 参数中。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/pending_invoice_canonical_query.py"
      provides: "单 scope aggregate 的三项摘要"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/invoice_usage_collection_query.py"
      provides: "同一 canonical CTE 内的 page/supporting groups"
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/read_models.py"
      provides: "set-based Workbench count/pagination without unbounded ids"
  key_links:
    - from: "canonical SQL CTE"
      to: "rows/summary/total"
      via: "one snapshot and shared filter contract"
      pattern: "with"
---

<objective>
修复三个已经证据确认的 SQL 热点，同时保持精确财务口径与 Workbench generation/freshness 合同。

Purpose: 删除重复扫描和无界 ID 聚合，不把 Phase 扩大到索引、JSONB、cursor 或新查询框架。
Output: 三个现有 SQL owner 的 set-based 根修及 PostgreSQL 等价回归。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@.planning/phases/40-performance-contract-hot-path-closure/40-RESEARCH.md
@docs/modules/pending-invoices/boundary-io.md
@docs/modules/input-invoice-usage/boundary-io.md
@docs/modules/output-invoice-collections/boundary-io.md
@docs/modules/reconciliation-workbench/boundary-io.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在现有 SQL owners 内删除重复扫描与无界 ID 聚合</name>
  <files>backend/src/fin_ops_platform/services/pending_invoice_canonical_query.py, backend/src/fin_ops_platform/services/postgres_repositories/invoice_usage_collection_query.py, backend/src/fin_ops_platform/services/postgres_repositories/read_models.py, tests/test_pending_invoice_canonical_query.py, tests/test_pending_invoice_postgres_integration.py, tests/test_invoice_usage_collection_canonical_query.py, tests/test_invoice_usage_collection_postgres_integration.py, tests/test_workbench_sql_runtime.py, tests/test_workbench_query_postgres_integration.py</files>
  <behavior>
    - "待找发票三项 summary 由一次 scope aggregate 得出，rows/total/options 与旧 fixture 等价。"
    - "销项收款 current page supporting groups 在同一 canonical CTE 执行中取得，不第二次跑完整事实 CTE。"
    - "Workbench all-scope 直接按 active member/filter contract 计数分页，不构造全量 matching_group_ids 数组。"
    - "repeatable-read、exact total/statistics、排序、active generation/version/freshness 与 HTTP DTO 不变。"
  </behavior>
  <action>先把 before/after fixture equality 与 query-shape 断言写入列出的 tests，再仅修改三个现有 SQL owner：合并 Pending scope aggregate；让 invoice collection page 与 supporting groups 共享同一次 canonical CTE；让 Workbench all-scope 在数据库内 set-based count/page。删除旧重复查询/无界参数路径，不保留 fallback/double read。没有目标规模 EXPLAIN 证据的 relation seed、索引、普通列、JSONB、cursor 一律不改，对应 D-04/D-06。</action>
  <verify><automated>test -n "${FIN_OPS_TEST_DATABASE_URL:-}" &amp;&amp; PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_pending_invoice_canonical_query.py tests/test_pending_invoice_postgres_integration.py tests/test_invoice_usage_collection_canonical_query.py tests/test_invoice_usage_collection_postgres_integration.py tests/test_workbench_sql_runtime.py tests/test_workbench_query_postgres_integration.py</automated></verify>
  <done>相同 fixtures 下 exact rows/summary/total/options/version 全等，三个确认热点不再重复扫描或传递无界 ID。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-02-01 | Tampering | financial SQL semantics | mitigate | before/after fixture equality + API/query integration。 |
| T-40-02-02 | Denial of Service | all-scope ID materialization | mitigate | set-based DB count/page + no-unbounded-parameter assertion。 |
| T-40-02-03 | Repudiation | performance claim | mitigate | test PostgreSQL query shape/evidence；无证据不做结构优化。 |
</threat_model>

<verification>
Business/API/read-model regressions 使用真实 test PostgreSQL；缺数据库 fail closed，不使用 skip/relaxed assertions。
</verification>

<success_criteria>
- 三个已证明 SQL 热点在原 owner 内根修，精确财务和 generation 合同不变。
- 无新 index/schema/migration/cache/read path/query framework。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-02-SUMMARY.md` when done.
</output>
