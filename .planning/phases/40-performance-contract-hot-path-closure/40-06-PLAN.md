---
phase: 40-performance-contract-hot-path-closure
plan: "06"
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - web/src/pages/ReconciliationWorkbenchPage.tsx
  - web/src/test/WorkbenchSelection.test.tsx
  - web/src/test/WorkbenchApiRuntimePath.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-12: visible entry/focus/hidden-to-visible 立即检查，紧邻触发由同一 in-flight 合并。"
    - "visible 时所有状态都在请求 settle 后等待 1000ms；hidden 零请求；任何时刻最多一个 status GET。"
    - "fresh generation version 变化后沿用 300ms debounce 只 reload 一次；stale/refreshing 不读 full payload。"
  artifacts:
    - path: "web/src/pages/ReconciliationWorkbenchPage.tsx"
      provides: "page-local completion-driven visible-only poller"
    - path: "web/src/test/WorkbenchSelection.test.tsx"
      provides: "timer/visibility/focus/status/reload interaction contract"
    - path: "web/src/test/WorkbenchApiRuntimePath.test.ts"
      provides: "refresh-status-only runtime path guard"
  key_links:
    - from: "Workbench status effect"
      to: "/api/workbench/refresh-status?month=all"
      via: "request finally -> setTimeout(1000), visible/focus immediate, single-flight"
      pattern: "fetchWorkbenchRefreshStatus"
    - from: "fresh changed generation"
      to: "loadWorkbenchData"
      via: "existing 300ms scheduleWorkbenchReadModelReload"
      pattern: "scheduleWorkbenchReadModelReload"
---

<objective>
把关联台固定 5 秒 setInterval 改为页面局部的 visible completion+1s single-flight poller。

Purpose: 有界检测窗口、慢 proof 不被下一 tick abort、隐藏页无容量浪费、generation reload once。
Output: 一个原生 effect 与可观察交互 tests；零抽象/依赖/endpoint。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@.planning/phases/40-performance-contract-hot-path-closure/40-PATTERNS.md
@web/src/features/backgroundJobs/BackgroundJobProgressProvider.tsx
@docs/modules/reconciliation-workbench/state-machine.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 visible completion+1000ms strict single-flight poller</name>
  <files>web/src/pages/ReconciliationWorkbenchPage.tsx, web/src/test/WorkbenchSelection.test.tsx, web/src/test/WorkbenchApiRuntimePath.test.ts</files>
  <behavior>
    - "entry/focus/visible immediate；focus+visibility+timer 同时触发仍只有一个 request。"
    - "request N settle 前请求数恒为 N；settle+999ms 不新增，+1000ms 恰好新增一项。"
    - "hidden 清 timer/停止请求；unmount 清 timer、abort lifecycle-owned request、移除 listeners。"
    - "same generation 零 payload reload；new fresh generation 恰好一次；stale/refreshing/failed/unavailable/throw 同 cadence。"
  </behavior>
  <action>先用 Vitest fake timers/deferred fetch 扩展两个命名 tests，再只在现有 Workbench refresh-status effect 内用一个 `setTimeout`、一个 in-flight controller/ref 和一个 active/visibility guard 替换 5000ms `setInterval`/tick-abort。`finally` 在 active+visible 时安排 1000ms；focus/visibility 共用 immediate 入口。复用现有 `applyWorkbenchRefreshStatus`、version ref 与 300ms reload debounce。不抽 generic hook，不新增 dependency/full-payload polling/cross-page signal。每 client 请求率保持 `<=1/(request_duration+1s)`，对应 D-12/D-13/D-15/D-16。</action>
  <verify><automated>npm --prefix web test -- --run src/test/WorkbenchSelection.test.tsx src/test/WorkbenchApiRuntimePath.test.ts</automated></verify>
  <done>可见关联台只有一个 completion-driven status loop；hidden 零请求、无 overlap/tick abort/full-payload polling，每个 generation 只 reload 一次。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-06-01 | Denial of Service | 1-second polling | mitigate | completion-driven+hidden pause+single-flight；40-08 capacity gate。 |
| T-40-06-02 | Tampering | stale UI writeability | mitigate | existing read_model_status/write gate and fresh-version reload。 |
| T-40-06-03 | Information Disclosure | browser errors | mitigate | existing authenticated client/error mapper；no token logging。 |
</threat_model>

<verification>
Frontend tests cover initial/error/all statuses/focus/visibility/cleanup/request count/reload count；capacity is blocking in 40-08。
</verification>

<success_criteria>
- 可见页面严格 settle+1000ms，hidden 零 I/O，同一时刻最多一个 status request。
- only changed fresh version triggers existing combined reload once。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-06-SUMMARY.md` when done.
</output>
