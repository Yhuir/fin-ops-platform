---
phase: 40-performance-contract-hot-path-closure
plan: "07"
type: execute
wave: 2
depends_on:
  - 40-05
  - 40-06
files_modified:
  - backend/src/fin_ops_platform/services/workbench_sql_projection.py
  - tests/test_workbench_source_proof_contract.py
  - tests/test_postgres_repositories_boundaries.py
  - tests/test_bank_flow_rule_batch_backend_boundary.py
  - tests/test_runtime_worker_read_model_refresh_scopes.py
  - web/src/features/workbench/api.ts
  - web/src/features/workbench/exceptionTypes.ts
  - web/src/test/WorkbenchApi.test.ts
  - web/e2e/bank-flow-rule-batches-flow.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-17: 每个 Workbench canonical dependency family 都有 production writer -> exact source proof 的真实 PostgreSQL contract。"
    - "affected scope proof changes/unaffected scope stays equal/fresh becomes stale；writer 零 dirty/outbox/target/barrier。"
    - "bank-flow commit -> status exact enqueue -> existing worker atomic generation -> fresh relation 闭环。"
    - "D-14: 只删除 consumer 证据确认 dead 的 Workbench-local target DTO/fallback；maintenance/repair/rehydrate/domain jobs 保留。"
  artifacts:
    - path: "tests/test_workbench_source_proof_contract.py"
      provides: "test-owned bidirectional writer-to-proof matrix and real mutation closure"
    - path: "tests/test_postgres_repositories_boundaries.py"
      provides: "ordinary writer zero queue SQL guard"
    - path: "web/e2e/bank-flow-rule-batches-flow.spec.ts"
      provides: "zero notification to browser self-convergence correctness flow"
  key_links:
    - from: "canonical production writer"
      to: "WorkbenchSqlProjectionBuilder.source_versions_for_scopes"
      via: "updated_at/version/proof key, no notification"
      pattern: "source_versions_for_scopes"
    - from: "bank-flow canonical commit"
      to: "refresh-status -> gateway -> existing worker"
      via: "one exact affected month and atomic generation activation"
      pattern: "workbench.read_model.refresh"
---

<objective>
证明所有 Workbench-affecting writer 都能被 exact proof 观察，闭合 bank-flow 后端/浏览器正确性链，并删除有证据的局部 target residue。

Purpose: 每秒检查只有在 proof 完整时才可靠；漏项只修 proof/version owner，禁止恢复 write-side notification。
Output: test-owned writer-proof matrix、真实 queue/worker closure、局部 legacy 删除和 deterministic browser regression。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@.planning/phases/40-performance-contract-hot-path-closure/40-RESEARCH.md
@.planning/phases/40-performance-contract-hot-path-closure/40-05-SUMMARY.md
@.planning/phases/40-performance-contract-hot-path-closure/40-06-SUMMARY.md
@docs/architecture/module-boundaries/read-model-contracts.md
@docs/modules/bank-flow-rule-batches/boundary-io.md
@docs/modules/runtime-workers/boundary-io.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立全 writer-family 的真实 PostgreSQL proof 与 bank-flow worker 闭环</name>
  <files>backend/src/fin_ops_platform/services/workbench_sql_projection.py, tests/test_workbench_source_proof_contract.py, tests/test_postgres_repositories_boundaries.py, tests/test_bank_flow_rule_batch_backend_boundary.py, tests/test_runtime_worker_read_model_refresh_scopes.py</files>
  <behavior>
    - "relation/cross-month、bank/category/confirmation、invoice/OA、exception/override/claim、ETC、Workbench-consumed settings/schema/rule/parser 全有真实 writer/proof row。"
    - "每 row affected proof changed、unaffected proof equal、fresh->stale；matrix 与 projection dependencies 双向覆盖。"
    - "真实 bank-flow submit canonical commit 成功且 writer queue rows 为零；status one exact active event；worker publish 后 fresh/relation visible。"
  </behavior>
  <action>新建 test-only `WORKBENCH_WRITER_SOURCE_PROOF_CASES`，每行命名 production mutation entrypoint、canonical table/field、old/new exact scopes、proof key；使用 `require_postgres_test_database_url()`、真实 migrations 和 production service/repository mutation，禁止 mock/direct UPDATE/skip。Coverage 双向比较 projection dependency fixtures 与矩阵，避免自证。Bank-flow 用真实 application/state-store、facade/gateway/worker 验证 commit、zero writer queue、exact enqueue、atomic publish、fresh relation/unrelated scope unchanged。`workbench_sql_projection.py` 只有真实测试证明既有 proof query 漏 dependency 时才修改；若是某 writer owner 未推进 version/updated_at，立即返回 plan review 并命名 owner，禁止修改未列文件或用 notification fallback。对应 D-10/D-11/D-16/D-17。</action>
  <verify><automated>test -n "${FIN_OPS_TEST_DATABASE_URL:-}" &amp;&amp; PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_source_proof_contract.py tests/test_postgres_repositories_boundaries.py tests/test_bank_flow_rule_batch_backend_boundary.py tests/test_bank_flow_rule_batch_application_service.py tests/test_bank_flow_rule_batch_routes.py tests/test_runtime_worker_read_model_refresh_scopes.py</automated></verify>
  <done>全部 canonical dependency families 有真实 writer-proof 证明；bank-flow 从零 writer fan-out 到 exact worker publish/fresh relation 后端闭环全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 删除 dead Workbench target fallback 并闭合 deterministic browser regression</name>
  <files>web/src/features/workbench/api.ts, web/src/features/workbench/exceptionTypes.ts, web/src/test/WorkbenchApi.test.ts, web/e2e/bank-flow-rule-batches-flow.spec.ts</files>
  <behavior>
    - "operationBarrierTargets raw/type/fallback/runtime fields removed；freshnessTargets 无 dead-consumer 证据则保留。"
    - "retired bank-flow refresh event/page barrier/targets/cross-page signal 当前 runtime references 为零。"
    - "maintenance/repair/rehydrate/backfill、workbench-matching、OA/import jobs 与 operator endpoint 正向保留。"
    - "browser submit 不调用 barrier/signal；status sequence leads to one changed-generation reload and expected identity。"
  </behavior>
  <action>先 whole-repo caller/consumer scan，再只删除两个命名 Workbench DTO owners 中 `operationBarrierTargets` 字段和 fallback，更新 mapper test。扩展现有 bank-flow Playwright spec 的 deterministic API mock sequence，断言 submit 本页合同不变、零 barrier/notification、status self-convergence、新 generation one reload 和 business identity visible。不要在此任务做 p99 计时（40-08 独占真实 same-clock evidence），不要删除 global operator/maintenance/domain paths或新增 bus/endpoint/dependency。对应 D-10/D-14/D-16。</action>
  <verify><automated>npm --prefix web test -- --run src/test/WorkbenchApi.test.ts src/test/WorkbenchSelection.test.tsx &amp;&amp; npm --prefix web run e2e -- e2e/bank-flow-rule-batches-flow.spec.ts --project=chromium</automated></verify>
  <done>局部 dead target/fallback 删除、正式 ops/domain paths 保留；browser correctness E2E 证明 zero notification 与 reload-once。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-07-01 | Tampering | omitted source proof | mitigate | bidirectional matrix + real writer mutation + affected/unaffected assertions。 |
| T-40-07-02 | Denial of Service | restored writer fan-out | mitigate | SQL/import/DTO negative guards + exact gateway integration。 |
| T-40-07-03 | Denial of Service | ops/job deletion | mitigate | whole-repo consumer proof + retained-path positive tests。 |
| T-40-07-04 | Repudiation | mock-only closure | mitigate | real PostgreSQL backend closure + separate browser behavior test。 |
</threat_model>

<verification>
Service/API/read-model-worker/frontend/E2E/regression categories apply；business core is regression-only because rules remain unchanged。
</verification>

<success_criteria>
- 全 writers advance exact proof with zero Workbench dependency。
- Bank-flow API/transaction/current-page reload unchanged；consumer status/gateway/worker/browser closes loop。
- 只删除 proven local residue，formal repair/domain/operator paths intact。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-07-SUMMARY.md` when done.
</output>
