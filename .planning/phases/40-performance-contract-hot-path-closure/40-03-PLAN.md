---
phase: 40-performance-contract-hot-path-closure
plan: "03"
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - backend/src/fin_ops_platform/services/postgres_repositories/core.py
  - tests/test_postgres_repositories_core.py
  - tests/test_postgres_state_store_integration.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-04: 本计划只处理现有 query-count benchmark 已证明的 import_batch_rows 逐行 round-trip 热点。"
    - "批量写复用 PostgresTransaction.execute_many_values 的 1000 rows/60000 parameters 边界。"
    - "owner guard、ON CONFLICT、幂等、审计与事务原子失败保持不变；删除逐行兼容 fallback。"
    - "Bank/Pending DTO、Pending options、Turnover lots、Cost ordering、App Health 本 Phase 不授权修改。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/core.py"
      provides: "import_batch_rows bounded multi-value upsert only"
    - path: "tests/test_postgres_repositories_core.py"
      provides: "query-count/chunk/owner-conflict contract"
    - path: "tests/test_postgres_state_store_integration.py"
      provides: "real transaction atomicity and persistence regression"
  key_links:
    - from: "CorePostgresRepository._save_batch_rows"
      to: "PostgresTransaction.execute_many_values"
      via: "one bounded batched owner-guard upsert"
      pattern: "execute_many_values"
---

<objective>
只闭合已测量的 import batch-row 应用/数据库 round-trip 热点，明确排除所有未被现有 benchmark 证明的候选优化。

Purpose: 用已有 transaction capability 替代旧逐行 fallback，保持最小 diff 和原子业务合同。
Output: 一个 repository 根修与两层测试；无 DTO、页面、App Health、Cost 或 Turnover 变更。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@backend/src/fin_ops_platform/services/postgres_connection.py
@docs/modules/imports-bank-transactions/boundary-io.md
@docs/modules/imports-invoices/boundary-io.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 强制 import batch rows 走既有 bounded multi-value owner-guard upsert</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/core.py, tests/test_postgres_repositories_core.py, tests/test_postgres_state_store_integration.py</files>
  <behavior>
    - "N rows 使用 ceil(N/1000) 次 bounded multi-value execution，而不是 N 次 execute。"
    - "60000 parameter 上限仍由既有 PostgresTransaction 合同保护。"
    - "任一 row 已归属其它 batch 时整个事务失败且无半写；幂等重复保持同 owner。"
    - "connection 缺少 execute_many_values 时 fail fast，不回退逐行路径。"
  </behavior>
  <action>先用 `tests/test_postgres_repositories_core.py` 锁定 query count、chunk、owner conflict 与 no-per-row-fallback，再把 `_save_batch_rows` 收敛到既有 `PostgresTransaction.execute_many_values(...)`。删除 `getattr(...)/sum(connection.execute(...))` 兼容分支，不修改 `postgres_connection.py` 的既有 1000/60000 实现。用 integration test 证明冲突 rollback、成功 persistence 与 audit/canonical delta 不变。明确记录以下未被命名 benchmark 证明的候选为未授权：Bank/Pending DTO、Pending options、Turnover lots、Cost ordering、App Health；不得修改相关文件。</action>
  <verify><automated>test -n "${FIN_OPS_TEST_DATABASE_URL:-}" &amp;&amp; PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_postgres_repositories_core.py tests/test_postgres_state_store_integration.py</automated></verify>
  <done>import rows 只有 bounded batch path，逐行 fallback 被删除，owner/idempotency/rollback 合同由真实 PostgreSQL 回归保护。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-03-01 | Tampering | import row ownership | mitigate | owner-guard affected-count + whole-transaction rollback tests。 |
| T-40-03-02 | Denial of Service | parameter/query amplification | mitigate | existing 1000 rows/60000 params bounded helper；query-count assertion。 |
| T-40-03-03 | Repudiation | speculative optimization | mitigate | exact named benchmark/file scope；all other candidates explicitly unauthorized。 |
</threat_model>

<verification>
Repository unit/query-count 与 PostgreSQL integration；API/DTO/frontend categories 不适用，因为其生产文件禁止变化。
</verification>

<success_criteria>
- import batch-row 热点无逐行 round-trip 或 compatibility fallback。
- 只有三个命名文件可修改；未测量候选零生产 diff。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-03-SUMMARY.md` when done.
</output>
