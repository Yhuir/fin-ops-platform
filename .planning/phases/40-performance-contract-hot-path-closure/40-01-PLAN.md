---
phase: 40-performance-contract-hot-path-closure
plan: "01"
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - backend/src/fin_ops_platform/tools/http_slo_probe.py
  - backend/src/fin_ops_platform/tools/sync_slo_baseline.py
  - tests/test_http_slo_probe.py
  - tests/test_sync_slo_baseline.py
  - web/src/components/common/FinanceTable.tsx
  - web/src/test/FinanceTable.test.tsx
  - docs/operations/performance-contract.md
  - docs/operations/monitoring.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-04/D-05: current-production 与 target-scale evidence 分开；缺少隔离目标规模数据时必须写 not_measured。"
    - "FinanceTable 在数万页仍只渲染常数级页码，同时保留精确 total、首尾页、键盘与 aria 合同。"
    - "现有 probe 只增加有界并发、错误与响应体证据，不增加压测框架或运行时入口。"
  artifacts:
    - path: "backend/src/fin_ops_platform/tools/http_slo_probe.py"
      provides: "有界认证并发与响应体/错误分布"
    - path: "web/src/components/common/FinanceTable.tsx"
      provides: "常数级页码窗口"
    - path: "docs/operations/performance-contract.md"
      provides: "measured/not_measured 与目标规模证据合同"
  key_links:
    - from: "http_slo_probe"
      to: "performance contract"
      via: "bounded samples + named environment/evidence window"
      pattern: "not_measured"
    - from: "FinanceTable total/page/pageSize"
      to: "pagination buttons"
      via: "first/last/current-neighbor token window"
      pattern: "ellipsis"
---

<objective>
先建立可复现的性能证据合同并修复确定性的公共分页热点；不触碰业务 API、read model、worker 或写链。

Purpose: 为后续 SQL、导入与 Workbench 容量门禁提供同一 measured/not_measured 语义。
Output: 现有 probe 的有界并发能力、FinanceTable 常数级页码和对应测试/运维合同。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@.planning/phases/40-performance-contract-hot-path-closure/40-RESEARCH.md
@docs/operations/performance-contract.md
@docs/operations/monitoring.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化有界并发性能探针与证据语义</name>
  <files>backend/src/fin_ops_platform/tools/http_slo_probe.py, backend/src/fin_ops_platform/tools/sync_slo_baseline.py, tests/test_http_slo_probe.py, tests/test_sync_slo_baseline.py, docs/operations/performance-contract.md, docs/operations/monitoring.md</files>
  <behavior>
    - "串行默认行为和既有 probe names 保持兼容。"
    - "并发 4/8 使用有界 worker 数，报告 request_count/error_count/p50/p95/p99/response_bytes。"
    - "无隔离 target-scale 数据时报告 not_measured，不用生产小样本冒充目标规模。"
  </behavior>
  <action>先扩展现有 tests，再在既有 probe/baseline owner 内增加有界并发聚合与响应体/错误统计；不新增 CLI family、依赖、daemon 或通用 load framework。把 D-04/D-05/D-07 的 current-production、target-scale、`not_measured` 与禁止生产业务数据写入现有 performance/monitoring 文档。保留认证、timeout、redaction 和既有输出字段。</action>
  <verify><automated>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_http_slo_probe.py tests/test_sync_slo_baseline.py</automated></verify>
  <done>串行兼容且并发 4/8 可重复；缺目标规模数据时 fail closed 为 not_measured，零新依赖/运行时。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 FinanceTable 页码渲染收敛为常数级窗口</name>
  <files>web/src/components/common/FinanceTable.tsx, web/src/test/FinanceTable.test.tsx</files>
  <behavior>
    - "50,000 页仅渲染首、尾、当前邻域与省略号的常数级 token。"
    - "精确 total、上一页/下一页、首尾当前可达、disabled/aria/keyboard 行为不变。"
  </behavior>
  <action>在现有 FinanceTable 模块内直接计算首/尾/当前前后窗口；不抽 pagination library/helper package，不改变组件 public props 或页面 API。用大 total 用例锁定 token 数上限与可访问性。</action>
  <verify><automated>npm --prefix web test -- --run src/test/FinanceTable.test.tsx</automated></verify>
  <done>页数增长不再线性增加 DOM 节点，精确分页与可访问性回归全绿。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-01-01 | Denial of Service | concurrent probe | mitigate | bounded concurrency/timeout；production 只读；tests 锁定上限。 |
| T-40-01-02 | Information Disclosure | probe report | mitigate | 保留认证与 redaction，不记录 token/业务 payload。 |
| T-40-01-03 | Tampering | pagination exactness | mitigate | exact total/page contract 与大页数交互测试。 |
</threat_model>

<verification>
Backend probe tests、frontend component tests、docs gate；不涉及业务 core、API shape、worker 或 E2E mutation。
</verification>

<success_criteria>
- 性能报告区分 measured 与 not_measured，probe 并发有界且无敏感数据。
- FinanceTable 页码 DOM 常数级且精确分页/aria 合同不变。
- 无新增依赖、runtime component、read model、worker、cache 或页面 I/O。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-01-SUMMARY.md` when done.
</output>
