---
phase: 40-performance-contract-hot-path-closure
plan: "01"
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - backend/src/fin_ops_platform/tools/http_slo_probe.py
  - backend/src/fin_ops_platform/tools/sync_slo_baseline.py
  - web/src/components/common/FinanceTable.tsx
  - backend/src/fin_ops_platform/services/pending_invoice_canonical_query.py
  - backend/src/fin_ops_platform/services/postgres_repositories/invoice_usage_collection_query.py
  - backend/src/fin_ops_platform/services/postgres_repositories/read_models.py
  - backend/src/fin_ops_platform/services/postgres_repositories/core.py
  - tests/
  - web/src/test/
  - docs/architecture/persistence-and-read-models.md
  - docs/modules/
  - docs/operations/performance-contract.md
  - docs/operations/monitoring.md
  - .planning/phases/40-performance-contract-hot-path-closure/40-01-SUMMARY.md
  - .planning/phases/40-performance-contract-hot-path-closure/40-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The performance contract distinguishes current-production evidence from target-scale evidence and must report target scale as not_measured when no isolated dataset exists."
    - "FinanceTable pagination renders a bounded page window even when total pages are tens of thousands, preserving exact totals and accessibility."
    - "Pending/invoice/Workbench hot queries reduce repeated scans or unbounded ID aggregation while preserving exact financial and generation contracts."
    - "Only proven application/import hotspots change; existing repository batch capabilities are reused without a new runtime abstraction."
    - "Retired Search/read-model current-fact residue is removed; the canonical no-OA API remains because route, command and Workbench consumers still exist."
    - "Full local gates, origin/main, deploy and authenticated production evidence close correctness, isolation and performance."
---

<objective>
建立可复现性能合同，修复确定性分页和经证据确认的核心热路径，删除旧链污染，并完成 main 发布与生产验证；不引入新的异步/缓存/投影架构。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立有界性能基准并修复公共分页</name>
  <read_first>http_slo_probe.py, sync_slo_baseline.py, FinanceTable.tsx, tests/test_http_slo_probe.py, web/src/test/FinanceTable.test.tsx, monitoring.md</read_first>
  <action>复用现有 probe/metrics 增加有界并发采样和响应体/错误统计；更新过期的 read-model/worker SLO 描述。FinanceTable 页码只生成首/尾/当前前后窗口和省略号，保留精确 total、上一页/下一页、键盘和 aria 合同。不得新增压测依赖或改变页面 API。</action>
  <acceptance_criteria>现有串行 probe 默认兼容；并发 4/8 有测试；一百万行/20 page size 时数字页码常数级且首尾当前可达；lint/frontend tests 通过。</acceptance_criteria>
  <verify>tests/test_http_slo_probe.py, tests/test_sync_slo_baseline.py, web/src/test/FinanceTable.test.tsx</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修复三个已确认查询热点</name>
  <read_first>pending_invoice_canonical_query.py, invoice_usage_collection_query.py, read_models.py, 对应 API/repository tests 与三模块 boundary-io</read_first>
  <action>以现有 SQL owner 为唯一实现：待找发票用一次 scope aggregate 计算三项摘要；销项收款在同一 canonical CTE 执行中读取当前页 supporting groups，删除第二次完整事实 CTE；Workbench all-scope 删除全量 matching_group_ids 数组，让数据库用同一 active-member/filter contract 直接计数和分页。保留 exact total/statistics/filter options、repeatable-read、active generation/version/freshness、响应结构和排序稳定性。进项 relation seed、索引/JSONB 列/cursor 没有安全的生产 EXPLAIN 证据，不修改。</action>
  <acceptance_criteria>旧新 fixture 业务结果等价；SQL/Python 不构造无界 Workbench ID 参数；API/query/workbench generation tests 通过；无新增 worker/read model/cache。</acceptance_criteria>
  <verify>pending invoice, invoice usage/collection, Workbench SQL/facade/API integration tests; target-scale EXPLAIN in isolated PostgreSQL when available</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 优化经测量的应用层、DTO 与导入热路径</name>
  <read_first>turnover_ledger_service.py, cost canonical repository/service, core.py, postgres_connection.py, import/turnover/cost tests 与 boundary-io</read_first>
  <action>先对 query-count 明确证明的 import batch-row 热点实施根修：复用 PostgresTransaction.execute_many_values 的 1,000 行/60,000 参数上限，保持 ON CONFLICT owner guard、幂等、审计、rollback 和 canonical delta。首次生产并发基准如证明其它首屏超限，只在现有 owner 内删除未消费 DTO、拆分非阻塞辅助聚合和修正伪当前状态：Bank/Pending 只输出展示/canonical 字段，Pending options 使用既有专用 endpoint，Turnover 页面不输出导出专用 lots，Cost scoped 内容先于全局 statistics，App Health 过滤 completed matching history 并 single-flight runtime snapshot。不得创建新层、cache 产品、route family 或 worker。</action>
  <acceptance_criteria>未测量路径不改；import batch rows 按 chunk 而非行 round-trip；冲突仍使整个事务失败且无半写；页面 DTO 不丢用户消费字段、导出仍保留 lots、canonical totals/statistics 正确；service/repository boundaries 和 tests 通过。</acceptance_criteria>
  <verify>turnover/cost API+service+PostgreSQL tests; postgres connection/core/state-store/import worker tests</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 删除旧链污染并更新长期事实源</name>
  <read_first>runtime/read-model/no-OA/import boundary docs, monitoring.md, server routes, no_oa_bank_batch_application_service.py, whole-repo callers/tests</read_first>
  <action>删除长期事实源中仍把 Search/四 read models 当当前 runtime 的描述；审计 no-OA API 的 route、写命令、Workbench decorators、测试与外部合同。因为仍有正式消费者，不删除 canonical API/service/DTO；不引入 alias/fallback/double-read。保留 canonical facts 和当前 rollback 所需历史记录。</action>
  <acceptance_criteria>当前事实源精确描述 6 workers/2 read models，Search runtime 保持删除；no-OA 保留理由有 caller/contract 证据；所有受影响 docs/architecture guards 通过。</acceptance_criteria>
  <verify>architecture guards, route/API negative tests, docs, registry/manifest/deploy parity</verify>
</task>

<task type="auto">
  <name>Task 5: 全量回归、main 发布与生产闭环</name>
  <read_first>scripts/verify.sh, scripts/deploy-oa.sh, deploy/oa/README.md, monitoring.md, Phase 39 production evidence</read_first>
  <action>运行 lint/backend/frontend/e2e/infra-smoke/docs/diff gates和独立代码审阅；原子提交所有授权改动、push origin/main，使用 scripts/with-production-admin-token.sh ./scripts/deploy-oa.sh 发布。验证 exact runtime release、six-worker/two-read-model topology、queue drain、canonical/System Audit、authenticated concurrent core API SLO、HTTP/DB resources、Workbench/invoice/turnover/import read chains；不在生产生成规模数据或执行无界 EXPLAIN ANALYZE。</action>
  <acceptance_criteria>所有本地门禁通过；origin/main 等于本地 main；active release 等于提交 SHA；核心 API p95 <=1000ms，无错误/资源/队列/跨页隔离回归；previous release 保留回滚锚点。</acceptance_criteria>
  <verify>full verify scripts; deploy release gate; authenticated production probes and T+0/T+60/T+300 evidence</verify>
</task>

</tasks>

<threat_model>
- 生产性能 fixture 误写真实数据：工具/执行流程必须拒绝 production database，只在隔离环境写入。
- 大查询或 EXPLAIN ANALYZE 压垮生产：生产只允许有界只读探针；目标规模执行只在 disposable PostgreSQL。
- SQL 优化改变财务口径：before/after fixture equality、API contract 和业务核心测试为 blocking gate。
- 旧链过早删除破坏消费者/回滚：whole-repo + 生产访问证据 + protected release reader/writer/backlog gate。
</threat_model>

<verification>
- 七类测试均适用；任何业务、权限、freshness、导出、抽屉或跨页回归均阻断发布。
- 性能优化只有在正确性等价后判定；单次最快样本不能代替 p95/p99。
</verification>

<success_criteria>
- 当前生产并发性能可重复测量；目标规模在无隔离数据源时明确为 `not_measured`，生产验证不写业务数据。
- 公共分页、三个确定性查询热点和 import batch-row 热点按证据闭环，未增加运行时架构。
- 旧事实描述被删除；仍有消费者的 canonical no-OA API 明确保留，必要 rollback 数据边界不变。
- main、production release、正确性、性能和运行资源证据全部通过。
</success_criteria>

## Artifacts this phase produces

- Phase 40 context/plan/summary/verification artifacts.
- Bounded pagination token helper inside the existing FinanceTable module.
- Optional bounded-concurrency fields/CLI arguments in the existing HTTP SLO probe; no new framework.
- No new runtime worker, read model, cache, route family or dependency.
