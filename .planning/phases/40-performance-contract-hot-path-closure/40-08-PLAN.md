---
phase: 40-performance-contract-hot-path-closure
plan: "08"
type: execute
wave: 3
depends_on:
  - 40-04
  - 40-07
files_modified:
  - docs/architecture/module-boundaries/read-model-contracts.md
  - docs/app-architecture/runtime-and-ownership.md
  - docs/modules/reconciliation-workbench/boundary-io.md
  - docs/modules/reconciliation-workbench/state-machine.md
  - docs/modules/reconciliation-workbench/tests.md
  - backend/src/fin_ops_platform/tools/http_slo_probe.py
  - tests/test_http_slo_probe.py
  - web/e2e/fixtures/operationLatency.ts
  - web/e2e/bank-flow-rule-batches-flow.spec.ts
  - tests/test_playwright_e2e_strict_diagnostics.py
  - docs/operations/performance-contract.md
  - docs/operations/monitoring.md
  - .planning/phases/40-performance-contract-hot-path-closure/40-workbench-visibility-p99.json
  - .planning/phases/40-performance-contract-hot-path-closure/40-08-SUMMARY.md
  - .planning/phases/40-performance-contract-hot-path-closure/40-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-13: C_normal/C_peak 来自命名生产 evidence window 或批准 capacity contract；4/8 只是 floors。"
    - "refresh-status 在 N_normal/N_peak 下 p95<=1000ms、error=0，pool/DB/queue/worker 无饱和。"
    - "D-15: existing Playwright/Node monotonic clock 独占 t0..t4；每样本四段之和严格等于 total，browser-inclusive total p99<=3000ms。"
    - "t4 包含 t3 generation 被 status 观察、existing 300ms debounce、combined payload install 与 business identity DOM render。"
    - "D-07/D-09: full/capacity/p99 全绿后 Phase 40 只 push/deploy 一次；previous release 是无 migration 回滚锚点。"
  artifacts:
    - path: "web/e2e/fixtures/operationLatency.ts"
      provides: "single Node monotonic-microsecond t0..t4 recorder and report writer"
    - path: "web/e2e/bank-flow-rule-batches-flow.spec.ts"
      provides: "real bank-flow POST -> status/generation -> Workbench DOM timing scenario"
    - path: ".planning/phases/40-performance-contract-hot-path-closure/40-workbench-visibility-p99.json"
      provides: "per-sample marks, segments, sum checks, distributions and p99"
    - path: ".planning/phases/40-performance-contract-hot-path-closure/40-VERIFICATION.md"
      provides: "capacity, browser p99, SHA, deploy, T+300 and rollback evidence"
  key_links:
    - from: "production session/access evidence"
      to: "N_normal/N_peak"
      via: "named window and rolling-60s unique visible-client derivation"
      pattern: "C_normal"
    - from: "bank-flow submit POST"
      to: "Workbench DOM business identity"
      via: "one Playwright process; operationLatency Node monotonic microseconds; raw refresh-status + generation + combined payload"
      pattern: "createWorkbenchVisibilitySegmentRecorder"
    - from: "t3 generation version"
      to: "t4 DOM"
      via: "same response version, 300ms reload debounce, payload contains submitted transaction id, unique identity locator visible"
      pattern: "active_generation_id"
    - from: "green full/capacity/p99 gates"
      to: "official deploy"
      via: "one origin/main push and ./scripts/deploy-oa.sh"
      pattern: "deploy-oa.sh"
---

<objective>
固化 Workbench self-convergence 合同，以目标并发和可执行的 browser-inclusive same-clock t0..t4 证明性能，并执行 Phase 40 唯一发布/回滚闭环。

Purpose: 把“每秒检查高性能”变成可审计容量与 commit-to-visible p99，而不是 mock、混合时钟或最快样本。
Output: 最小长期文档、derived capacity、Playwright same-clock report、full gates、one release/T+300/rollback evidence。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@.planning/phases/40-performance-contract-hot-path-closure/40-RESEARCH.md
@.planning/phases/40-performance-contract-hot-path-closure/40-04-SUMMARY.md
@.planning/phases/40-performance-contract-hot-path-closure/40-05-SUMMARY.md
@.planning/phases/40-performance-contract-hot-path-closure/40-06-SUMMARY.md
@.planning/phases/40-performance-contract-hot-path-closure/40-07-SUMMARY.md
@web/playwright.config.ts
@scripts/with-production-admin-token.sh
@scripts/deploy-oa.sh
@deploy/oa/README.md

<browser_timing_contract>

| Mark | Exact owner / observation |
| --- | --- |
| t0 | `operationLatency.ts` Node monotonic microseconds immediately before `workbenchPage.context().request.post(existing bank-flow submit path)` |
| t1 | same clock immediately after that POST response resolves and canonical 2xx receipt is parsed |
| t2 | same clock in the Workbench page response listener when existing `/api/workbench/refresh-status?month=all` first reports the manifest exact scope as stale+`refresh_scope_keys` or refreshing+matching `dirty_scopes`; `all` is rejected |
| t3 | same listener when status reports `fresh` and active/read-model generation `g1 != pre-submit g0` |
| t4 | same clock after a combined Workbench payload for `g1` contains submitted transaction/relation identity and the unique business identity is rendered visible in the Workbench DOM |

Sample binding is `{sample_id,batch_id,transaction_ids,business_identity,exact_scope,g0,g1}` from a root-owned test fixture manifest. No runtime correlation field or endpoint is added.

</browser_timing_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 query-owned self-convergence 长期合同</name>
  <files>docs/architecture/module-boundaries/read-model-contracts.md, docs/app-architecture/runtime-and-ownership.md, docs/modules/reconciliation-workbench/boundary-io.md, docs/modules/reconciliation-workbench/state-machine.md, docs/modules/reconciliation-workbench/tests.md</files>
  <action>只记录已由 40-05..07 改变的事实：ordinary writer 只推进 canonical proof/zero notification；existing refresh-status stale exact enqueue；existing gateway/queue/worker atomic generation；visible entry/focus immediate、all-status settle+1000ms、hidden pause、single-flight、changed fresh version 300ms reload once。Bank-flow API/transaction/current-page reload、worker topology、maintenance/repair/rehydrate/domain jobs 未改变，不重写其它模块。</action>
  <verify><automated>bash scripts/verify.sh docs &amp;&amp; PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_read_model_architecture_guards.py tests/test_runtime_worker_registry.py</automated></verify>
  <done>五个事实源一致描述 D-10..D-17，没有把 writer、transport 或未改变模块写成新职责。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 推导目标并发并生成 executable Playwright same-clock t0..t4 p99</name>
  <files>backend/src/fin_ops_platform/tools/http_slo_probe.py, tests/test_http_slo_probe.py, web/e2e/fixtures/operationLatency.ts, web/e2e/bank-flow-rule-batches-flow.spec.ts, tests/test_playwright_e2e_strict_diagnostics.py, docs/operations/performance-contract.md, docs/operations/monitoring.md, .planning/phases/40-performance-contract-hot-path-closure/40-workbench-visibility-p99.json</files>
  <behavior>
    - "Named 14-day source/window/method yields C_normal=p95 and C_peak=max rolling-60s unique clients; unavailable evidence means NOT_MEASURED/release blocked."
    - "N_normal=max(4,C_normal), N_peak=max(8,C_peak); both refresh-status loads p95<=1000ms/error=0/no resource starvation."
    - "Each Playwright sample has one monotonic integer-microsecond t0<=...<=t4 and exact (t1-t0)+(t2-t1)+(t3-t2)+(t4-t3)=t4-t0."
    - "t2 exact scope, t3 new generation and t4 payload/DOM identity all bind to the same submitted batch/relation; missing/mismatch fails."
    - ">=100 isolated prod-equivalent samples compute p99; production smoke is exactly one approved test-owned reversible sample and cannot claim p99."
  </behavior>
  <action>先从最近 14 个完整自然日的 authenticated refresh-status access/session evidence 推导 C_normal/C_peak；只保存匿名 aggregate。若日志不能区分 client/session，只接受注明 source/version/approver 的 capacity contract；否则 NOT_MEASURED 并停止。把 existing `workbench_refresh_status_all` probe 加入 40-01 的 bounded probe set，用 N_normal/N_peak 记录 latency/error/bytes/source-proof SQL/pool/DB/queue/worker 指标。

  浏览器证据只复用现有 Playwright harness，不使用 Python runner 时钟。最小扩展 `operationLatency.ts`：新增 `createWorkbenchVisibilitySegmentRecorder`，以同一 Node `performance.now()` 转整数 microseconds 拥有 baseline g0、t0..t4、逐段和、聚合分布与 redacted JSON report；现有 recorder 字段/调用者保持兼容。Report path 由该 fixture 相对自身 `import.meta.url` 解析到仓库唯一 `.planning/phases/40-performance-contract-hot-path-closure/40-workbench-visibility-p99.json`，不依赖 npm cwd。隔离 run 写 `isolated` >=100 samples/p99；后续 production run 只合并一个 `production_smoke` row，不覆盖 isolated rows、也不把单样本重算成 p99。

  扩展现有 `bank-flow-rule-batches-flow.spec.ts` 的 opt-in real-backend case：Workbench page 保持 visible；t0 后用 `workbenchPage.context().request.post(...)`（与 browser context 共享 cookie storage 的既有 Playwright `APIRequestContext`）调用现有 batch submit POST；raw response listener观察现有 refresh-status取得 t2/t3；等待 `/api/workbench` payload generation=g1 且含 manifest transaction/relation identity，再等待唯一 business label DOM visible 取得 t4。每 sample 随后调用 manifest 中现有 exact withdraw/recovery POST并验证回到 inactive，禁止 reset-all。Manifest 必须 root-owned、`fixture_ownership=test_owned`、每 sample 唯一 id/batch/transaction/business label/scope/submit+withdraw payload；生产最多一项，隔离 prod-equivalent 至少100项。`tests/test_playwright_e2e_strict_diagnostics.py` 锁定 explicit opt-in/admin token/manifest/recovery/redaction/max-production-one-sample；不新增 endpoint/runtime correlation/package。对应 D-04/D-05/D-07/D-13/D-15。</action>
  <verify>
    <automated>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_http_slo_probe.py tests/test_playwright_e2e_strict_diagnostics.py &amp;&amp; test -n "${FIN_OPS_E2E_WORKBENCH_SLO_FIXTURE_MANIFEST:-}" &amp;&amp; FIN_OPS_E2E_WORKBENCH_VISIBILITY_SLO=1 FIN_OPS_E2E_WORKBENCH_VISIBILITY_SLO_SAMPLES=100 npm --prefix web run e2e -- e2e/bank-flow-rule-batches-flow.spec.ts --project=chromium --grep "commit-to-visible same-clock" &amp;&amp; test -s .planning/phases/40-performance-contract-hot-path-closure/40-workbench-visibility-p99.json</automated>
    <evidence>Attach named concurrency source/window, N_normal/N_peak reports and the >=100-sample report. Run one separate bounded production sample through `scripts/with-production-admin-token.sh` with samples=1; it is smoke evidence only.</evidence>
  </verify>
  <done>Target capacity passes both derived tiers；Playwright same clock records complete t0..t4 with exact identity/scope/generation binding and total p99<=3000ms；production remains one bounded reversible smoke。</done>
</task>

<task type="auto">
  <name>Task 3: 运行 full gates 并执行 Phase 40 唯一 push/deploy/rollback closure</name>
  <files>.planning/phases/40-performance-contract-hot-path-closure/40-08-SUMMARY.md, .planning/phases/40-performance-contract-hot-path-closure/40-VERIFICATION.md</files>
  <action>按 40-VALIDATION 运行全部 targeted/PostgreSQL/Chromium、lint/all/runtime-check/infra-smoke/docs/diff；汇总七类测试、40-04/07 exact candidates、capacity、same-clock report 和 previous release。任何 NOT_MEASURED、skip、sample不足、identity/scope/generation mismatch、p95/p99/resource/queue/worker失败均阻断。全部绿色后才冻结单一 combined candidate，执行本 Phase 唯一 `git push origin main` 和 `./scripts/deploy-oa.sh`，验证 local=origin/main=active SHA、6 workers/2 read models、queue drain、fresh generation、Audit 与 T+0/T+60/T+300。失败只回滚 previous application release并复验；无 migration/cache/fallback/writer notification/second candidate。</action>
  <verify>
    <automated>bash scripts/verify.sh lint &amp;&amp; bash scripts/verify.sh all &amp;&amp; bash scripts/verify.sh runtime-check &amp;&amp; bash scripts/verify.sh infra-smoke &amp;&amp; test -s .planning/phases/40-performance-contract-hot-path-closure/40-workbench-visibility-p99.json</automated>
    <evidence>Attach SHA equality, official deploy output, T+0/T+60/T+300 topology/queue/Audit/page evidence and rollback anchor. Only this task may push/deploy.</evidence>
  </verify>
  <done>一个 candidate 通过 correctness/capacity/browser p99 后只发布一次并闭合 T+300；或失败时恢复 previous release 且 Phase 未完成。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-08-01 | Information Disclosure | access/fixture/report evidence | mitigate | anonymous counts, root-owned manifest, report redaction, no token/payload。 |
| T-40-08-02 | Denial of Service | 1 QPS/client capacity | mitigate | derived bounded tiers + API/SQL/pool/DB/queue/worker release gates。 |
| T-40-08-03 | Repudiation | browser p99 | mitigate | one Node monotonic clock, identity/scope/generation binding, per-sample equality, >=100 samples。 |
| T-40-08-04 | Tampering | production fixture/recovery | mitigate | explicit opt-in, test-owned exact manifest, per-sample withdraw, production max one。 |
| T-40-08-05 | Tampering | release fingerprint | mitigate | local/origin/main/active SHA equality, official deploy only。 |
| T-40-08-06 | Denial of Service | failed release | mitigate | T+0/T+60/T+300 and previous application release rollback。 |
</threat_model>

<verification>
## Seven-category assessment

1. Business core: regression-only; business rules unchanged.
2. Service: status/gateway/writer-proof covered.
3. API: auth/month/status shape and bank-flow receipt/recovery covered.
4. Read model/worker: exact dirty/outbox/dedupe/atomic publish/fresh covered.
5. Frontend: lifecycle/single-flight/reload-once covered.
6. E2E: real POST -> exact status -> generation -> DOM identity with same clock.
7. Existing regression: all writer proofs, other pages, maintenance/domain jobs, 6/2 topology.
</verification>

<source_coverage>
| Source | ID | Feature / Constraint | Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 40 | performance evidence/hotspots/legacy/Workbench self-convergence/one release | 40-01..40-08 | COVERED |
| REQ | — | no new product requirement IDs | 40-01..40-08 | COVERED |
| CONTEXT | D-01..D-09 | canonical architecture, measure-first, correctness, isolated data, cleanup, one release | 40-01..04,40-08 | COVERED |
| CONTEXT | D-10..D-11 | writer zero notification, status exact self-heal | 40-05,40-07 | COVERED |
| CONTEXT | D-12..D-13 | visible completion+1s, single-flight, derived capacity | 40-06,40-08 | COVERED |
| CONTEXT | D-14 | evidence-based local legacy removal/retained jobs | 40-07 | COVERED |
| CONTEXT | D-15 | browser-inclusive same-clock total p99<=3000ms | 40-08 | COVERED |
| CONTEXT | D-16..D-17 | Workbench-only boundary/all writer proof | 40-05..07 | COVERED |
| RESEARCH | confirmed hot paths and root/browser/proof/capacity gaps | existing owners/helpers/Playwright | 40-01..08 | COVERED |
| CONTEXT | Deferred | physical projection DROP/unproven JSONB/index/cursor | none | EXCLUDED |
</source_coverage>

<success_criteria>
- ordinary writers zero-notify；status/gateway/queue/worker/browser 完整 self-convergence。
- 1-second visible polling passes named target concurrency without API/DB/queue saturation。
- one Playwright/Node clock covers POST through DOM identity；per-sample sum exact and total p99<=3000ms。
- full gates、one release、T+300、rollback closure，zero new runtime architecture。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-08-SUMMARY.md` and finalize `.planning/phases/40-performance-contract-hot-path-closure/40-VERIFICATION.md`.
</output>
