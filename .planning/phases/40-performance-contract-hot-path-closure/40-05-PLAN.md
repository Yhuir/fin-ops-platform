---
phase: 40-performance-contract-hot-path-closure
plan: "05"
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - backend/src/fin_ops_platform/services/workbench_query_facade.py
  - tests/test_workbench_query_facade.py
  - tests/test_workbench_routes.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-10/D-16: 所有普通 writer 保持零 Workbench notification/target/barrier/queue I/O。"
    - "D-11: public refresh-status 同一次 source proof 发现 stale 后只经既有 gateway enqueue exact mismatch scopes。"
    - "fresh/active refreshing 不重复 enqueue；failed 只重试 exact failed scope；普通 stale 不退化为 all。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/workbench_query_facade.py"
      provides: "public refresh-status reuse of existing exact self-heal helper"
    - path: "tests/test_workbench_query_facade.py"
      provides: "stale/fresh/refreshing/failed/cold-start service contract"
    - path: "tests/test_workbench_routes.py"
      provides: "unchanged auth/month/status/response API contract"
  key_links:
    - from: "WorkbenchQueryFacade.refresh_status"
      to: "_groups_refresh_status_payload"
      via: "enqueue_non_fresh=True and one proof"
      pattern: "_groups_refresh_status_payload"
    - from: "_groups_refresh_status_payload"
      to: "ReadModelRefreshGateway"
      via: "existing injected enqueue_refresh port"
      pattern: "_enqueue_refresh"
---

<objective>
让现有 Workbench refresh-status 成为完整 exact-scope 自愈入口，同时保持所有普通 writer 完全解耦。

Purpose: 修复持续打开的关联台只看到 stale 却未必启动 refresh 的根缺口。
Output: 一个 facade 根修与 service/API tests；零新 endpoint/gateway/worker/read model/table/dependency。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/40-performance-contract-hot-path-closure/40-CONTEXT.md
@.planning/phases/40-performance-contract-hot-path-closure/40-RESEARCH.md
@backend/src/fin_ops_platform/services/workbench_query_freshness_service.py
@backend/src/fin_ops_platform/services/read_model_refresh_gateway.py
@docs/architecture/module-boundaries/read-model-contracts.md
@docs/modules/reconciliation-workbench/boundary-io.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 public refresh-status 复用既有 exact self-heal seam</name>
  <files>backend/src/fin_ops_platform/services/workbench_query_facade.py, tests/test_workbench_query_facade.py, tests/test_workbench_routes.py</files>
  <behavior>
    - "refresh_status('all') stale + two mismatch months 时各 exact month enqueue 一次且无 all。"
    - "fresh 与 pending/processing refreshing 零 enqueue；failed exact 只重试该 scope。"
    - "只有缺 active generation 的冷启动/显式恢复保留既有 all 语义。"
    - "endpoint auth、month validation、status/error/payload shape 和 timeout mapping 不变。"
  </behavior>
  <action>先增加 public `refresh_status` 表驱动 service/API tests，再仅让 `WorkbenchQueryFacade.refresh_status` 在现有 transient-error 边界内调用同文件 `_groups_refresh_status_payload(scope_key, enqueue_non_fresh=True)` 并继续 `_normalize_refresh_status(...)`。删除 duplicate repository/proof path；不新增 helper/enqueue loop/repository port/reason registry，也不为 enqueue 再读一次 proof。现有 composition 继续经 `ReadModelRefreshGateway` normalize/validate/dedupe/coalesce。不得修改 bank-flow 或任何普通 writer，对应 D-10/D-11/D-13/D-16。</action>
  <verify><automated>PYTHONPATH=backend/src:. python3 -m pytest -q tests/test_workbench_query_facade.py tests/test_workbench_routes.py tests/test_read_model_refresh_gateway.py</automated></verify>
  <done>public status 一次 proof 即可报告并推进 exact stale scopes；fresh/refreshing 无 queue 放大，HTTP/writer 合同不变。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-40-05-01 | Tampering | refresh scopes | mitigate | existing scope policy + exact/all tests。 |
| T-40-05-02 | Denial of Service | duplicate enqueue | mitigate | one proof + gateway active coalescing + no-op tests。 |
| T-40-05-03 | Elevation of Privilege | public status | mitigate | unchanged session/read permission and month validation。 |
</threat_model>

<verification>
Service/API/gateway tests cover stale/fresh/refreshing/failed/cold-start/timeout/permission/invalid month/enqueue error。
</verification>

<success_criteria>
- status proof 自行推进 exact durable refresh，普通 writers 零 Workbench dependency。
- 无 broad all fallback、第二次 proof 或新 runtime component。
</success_criteria>

<output>
Create `.planning/phases/40-performance-contract-hot-path-closure/40-05-SUMMARY.md` when done.
</output>
