---
phase: 02-bank-details-improvements
plan: 01
type: execute
wave: 1
autonomous: true
requirements:
  - BANK-01
  - BANK-02
  - BANK-03
  - BANK-04
  - BANK-05
  - BANK-06
  - BANK-07
files_modified:
  - backend/src/fin_ops_platform/services/bankdetail_write_uow.py
  - tests/test_bankdetail_write_uow_contract.py
  - tests/test_platform_runtime_boundary_guards.py
  - docs/modules/bank-details/boundary-io.md
  - docs/modules/bank-details/tests.md
  - docs/modules/bank-details/implementation-notes.md
  - docs/modules/permissions-and-audit/tests.md
  - docs/modules/permissions-and-audit/e2e-coverage.md
  - docs/dev/testing-closure-state.md
  - docs/dev/testing-closure-dependency-map.md
---

<objective>
删除没有任何 production caller、且注释明确“disconnected from production write paths”的 `BankdetailWriteUnitOfWork` 试验链，修正当前文档中把它误写为生产事务/audit/dirty/outbox owner 的内容，并在现有 bank-details architecture guard 中阻止该 module/class/并行 owner 回归。保持银行明细 API、业务规则、read model、worker、UI 和已经达标的热路径完全不变；定向本地验证后部署，并用既有 fan-out evidence 完成生产性能、Audit 和隔离闭环。
</objective>

<grill_gate>

- 生产 owner 必须继续是 `BankDetailsApplicationService`、明确 category/settings/no-OA owner、category side-effect port、read model refresh gateway；不得新建 replacement UoW。
- 删除前必须再次 whole-repo 扫描 runtime import/caller；如果非测试 runtime caller 不为零，停止而不删除。
- 历史 discovery/refactor prompt/migration state log 只记录当时事实，不改写；当前 durable docs 不得继续引用 skeleton 作为现行 coverage。
- `_legacy_bank_detail_text_display_fields`、`BankDetailsService`、410 tombstone、`no_oa_bank_batch` 不是本次可删除对象。
- 不修改 migration、SQL schema、API DTO、read model scope/schema、worker、cache、frontend 页面或其他页面代码。
- 不运行与本次删除无关的全量 CI；只运行足以证明 owner、边界、read model、权限审计和银行页面回归的定向检查。
- production apply 只使用既有 `fanout_evidence` / standing ticket，token 不打印、不入库；失败立即停止，不进入下一页面。

</grill_gate>

<tasks>

<task type="tdd">
  <name>Task 1：先锁定唯一生产 owner 与旧 skeleton 禁止回归</name>
  <files>tests/test_platform_runtime_boundary_guards.py</files>
  <action>在现有 bank-details application boundary guard 附近增加一个小而明确的断言：`services/bankdetail_write_uow.py` 必须不存在；`BankdetailWriteUnitOfWork` 和 `fin_ops_platform.services.bankdetail_write_uow` 不得出现在 production Python source；当前 `routes_bank_details.py`、`bank_details_application_service.py`、`bank_detail_category_side_effects.py` 的既有 owner markers 必须继续存在。只复用现有 `SERVICES_ROOT`/source scan pattern，不创建新 guard framework，不扫描历史 docs。</action>
  <verify>PYTHONPATH=backend/src python3 -m unittest tests.test_platform_runtime_boundary_guards.PlatformRuntimeBoundaryGuardTests.test_bank_details_disconnected_write_uow_stays_deleted -v</verify>
  <acceptance_criteria>删除实现前 guard 应因文件仍存在而失败；删除后通过，并且现有 bank-details boundary guards 不放宽。</acceptance_criteria>
</task>

<task type="implementation">
  <name>Task 2：删除 disconnected UoW 与孤立测试</name>
  <files>backend/src/fin_ops_platform/services/bankdetail_write_uow.py, tests/test_bankdetail_write_uow_contract.py</files>
  <action>删除两个文件，不保留 import shim、compatibility module、fallback、deprecated alias 或 replacement abstraction。删除后运行 whole-repo production source scan，确认 runtime import/caller 为零，测试和当前 docs 引用将在 Task 3 收口。</action>
  <verify>test ! -e backend/src/fin_ops_platform/services/bankdetail_write_uow.py && test ! -e tests/test_bankdetail_write_uow_contract.py && ! rg -n "BankdetailWriteUnitOfWork|fin_ops_platform\.services\.bankdetail_write_uow" backend/src scripts deploy web</verify>
  <acceptance_criteria>旧 module 和 test consumer 物理删除；生产代码、脚本、部署和前端没有引用；没有并行旧路径。</acceptance_criteria>
</task>

<task type="documentation">
  <name>Task 3：修正当前边界、测试和权限审计事实</name>
  <files>docs/modules/bank-details/boundary-io.md, docs/modules/bank-details/tests.md, docs/modules/bank-details/implementation-notes.md, docs/modules/permissions-and-audit/tests.md, docs/modules/permissions-and-audit/e2e-coverage.md, docs/dev/testing-closure-state.md, docs/dev/testing-closure-dependency-map.md</files>
  <action>删除当前文档对 `bankdetail_write_uow.py`/其测试的 active coverage 引用；把 bank category、auto-tag、no-OA 的真实 owner 和测试替换为当前 application service/category side-effect port、真实 no-OA/Workbench owner、refresh gateway、相应 API/service/read-model tests。`implementation-notes.md` 追加本次影响范围、关键决策、测试分类与验证结果。不要改写历史 backend-refactor discovery/state log；必要时在当前记录中注明它们是历史材料而非当前事实源。</action>
  <verify>当前边界/测试矩阵不再把旧 skeleton 列为 active owner；implementation-notes 只保留本次删除记录；bash scripts/verify.sh docs</verify>
  <acceptance_criteria>所有当前事实文档只指向真实 production owner；历史记录仍保留；docs gate 通过。</acceptance_criteria>
</task>

<task type="verification">
  <name>Task 4：运行与删除范围成比例的本地验证并判定 READY</name>
  <files>tests/test_platform_runtime_boundary_guards.py, docs/modules/bank-details/implementation-notes.md</files>
  <action>依次运行：git diff/check；backend lint；bank-details route/application/category/auto-tag；SQL read model/account balance/refresh producer；architecture guard；permissions/audit 受影响测试；BankDetails frontend API/Page component tests；docs。只在失败与本次变更相关时修复；不得通过 skip、relaxed assertion、ignore_errors 或 compatibility branch 隐藏失败。记录七类测试：business core 无新规则所以不新增；service/API/read-model/frontend/integration/regression 运行现有定向覆盖。确认无 migration、API/read-model/worker/frontend behavior diff，worktree 只含本 phase 文件，然后标记 `READY_FOR_UNIFIED_DEPLOYMENT`。</action>
  <verify>git diff --check；bash scripts/verify.sh lint；PYTHONPATH=backend/src python3 -m unittest tests.test_bank_details_routes tests.test_bank_auto_tag_rules_api tests.test_bank_details_sql_runtime tests.test_bank_account_balance_read_model tests.test_bank_detail_read_model_refresh_producer tests.test_platform_runtime_boundary_guards tests.test_audit_service tests.test_permissions_write_entry_inventory -v；cd web && npm test -- --run src/test/BankDetailsApi.test.ts src/test/BankDetailsPage.test.tsx；bash scripts/verify.sh docs</verify>
  <acceptance_criteria>所有定向门禁通过且没有无关改动；旧 skeleton 全仓当前引用为零；银行页面既有行为和 I/O 合同未变；形成 READY 证据。</acceptance_criteria>
</task>

<task type="release">
  <name>Task 5：单页面提交、push main、部署和生产闭环</name>
  <files>.planning/phases/02-bank-details-improvements/*, docs/modules/bank-details/implementation-notes.md</files>
  <action>确认 main 与 origin/main 无冲突后，把本 phase release 文件作为一个可回退提交推送 main；使用 `./scripts/deploy-oa.sh` 正式入口部署精确 SHA。部署后验证 release SHA/schema/service/worker/queue health。运行 authenticated accounts/transactions/rules/Page Audit 探针和 warm UI；再通过 root-owned deploy control 使用标准 scenario 与 `FINOPS-WRITE-SMOKE-STANDING-20260702` 执行受控可逆 fan-out write apply，从其 affected scopes 验证 bank-detail enqueue-to-fresh、页面读取新事实、Page Audit、queue drain，同时 smoke Workbench/no-OA/turnover 和至少一个不相关页面。不得打印 admin token，不得直接写数据库/read model/queue。任一门槛失败则停止、保留报告并回退/修复本页，不进入下一页面。</action>
  <verify>生产 release SHA = 提交 SHA；warm UI p95 ≤1000ms；accounts/transactions/rules/Page Audit p95 ≤1000ms；write enqueue-to-fresh p95 ≤1000ms 且 p99 ≤3000ms；Page Audit pass；dirty/outbox/failed queue=0；受影响与不相关页面 smoke 无回归</verify>
  <acceptance_criteria>代码、部署 SHA、生产性能、写后 freshness、Audit、queue 和隔离证据全部闭合，且报告无敏感数据；只有此时本页面完成并允许进入下一页面。</acceptance_criteria>
</task>

</tasks>

<verification>

- `bankdetail_write_uow.py` 和孤立测试不存在，当前 runtime/docs 引用为零；
- architecture guard 机械阻止旧 module/class/production import 回归；
- 当前真实 application/category/no-OA/read-model owners 仍有测试保护；
- 没有 migration、API、DTO、read model、worker、cache、frontend 或其他页面实现变化；
- warm read、write-to-fresh、Audit、queue drain 和隔离门槛均有生产报告；
- 七类测试适用性与未测风险记录完整。

</verification>

<success_criteria>

- 删除旧链减少一个虚假跨模块 owner，没有新增替代层；
- 银行明细 warm UI/API 继续满足 1 秒门槛；
- 受控写后银行明细在门槛内读到 fresh 新事实；
- Audit 通过且操作后仍通过；
- Workbench/no-OA/turnover 与不相关页面没有回归；
- 页面达到完整 `READY_FOR_UNIFIED_DEPLOYMENT -> DEPLOYED -> PRODUCTION_VERIFIED` 闭环。

</success_criteria>
