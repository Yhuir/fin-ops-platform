---
phase: 18-canonical-invoice-etc-link-closure
status: phase_c_implementation_complete_apply_gated
created: 2026-06-23
---

# 18-PHASE-C-20260623：历史 Backfill 与 Runbook 闭环

## 已完成

- 新增 `backfill_etc_batch_invoice_links` dry-run/apply 工具：
  - 默认只读，输出 JSON。
  - strict 匹配候选进入 `auto_backfill_candidates`。
  - 日期、金额、购销方等不一致进入 `manual_review_candidates`。
  - 已存在 active link 进入 `already_linked`。
  - apply 必须提供 `reason` 和 `operator`，并输出 rollback plan 与 Workbench affected scopes。
  - apply 前必须让输出包含完整候选 row set；如果 `--limit` 小于候选总数，CLI 拒绝写入。
- `WorkbenchSqlProjectionBuilder._etc_invoice_summary_rows`：
  - 优先读取 active `app.etc_batch_invoice_links` + canonical `app.invoices`。
  - 旧 `app.etc_submission_batches` / `app.etc_business_batches` / `app.etc_invoices` 路径作为未 backfill 历史 fallback。
  - 同一 identity 去重时 link table 行优先。
- Reset/runbook 边界：
  - `reset_invoices` 不得留下指向已删除 invoice id 的 active ETC batch invoice link。
  - 清空发票池后必须先重导 canonical invoice，再 dry-run/backfill ETC links，最后刷新 Workbench。
  - 备份范围加入 `app.etc_business_batches` 和 `app.etc_batch_invoice_links`。
- 模块文档已更新：
  - `docs/operations/invoice-pool-cleanup.md`
  - `docs/modules/data-safety-reset/README.md`
  - `docs/modules/etc-tickets/implementation-notes.md`
  - `docs/modules/etc-tickets/tests.md`
  - `docs/modules/reconciliation-workbench/implementation-notes.md`
  - `docs/modules/reconciliation-workbench/tests.md`

## 本轮未做

- 未执行生产 migration。
- 未执行生产 backfill `--apply`。
- 未删除 `app.etc_invoices` 或旧 summary fallback。
- 未重建生产 Workbench active generation。

## 测试与验证

新增/更新测试：

- `tests/test_backfill_etc_batch_invoice_links_tool.py`
- `tests/test_workbench_sql_runtime.py::WorkbenchSqlProjectionRelationPayloadTests::test_etc_invoice_summary_rows_prefer_link_table_source`
- `tests/test_repair_submitted_etc_invoice_overlaps_tool.py` 的 apply row-set 完整性 guard

已通过的 Phase C 窄验证：

```bash
PYTHONPATH=backend/src python3 -m pytest tests/test_backfill_etc_batch_invoice_links_tool.py tests/test_workbench_sql_runtime.py::WorkbenchSqlProjectionRelationPayloadTests::test_etc_invoice_summary_rows_prefer_link_table_source -q
```

## 生产 Apply Gate

任何真实库写入前必须停下来等待用户确认以下内容：

- dry-run 输出的 exact row set。
- `auto_backfill_candidates`、`manual_review_candidates`、`already_linked` 数量和示例。
- apply reason 与 operator。
- rollback plan 中的 link ids 或 candidate keys。
- 受影响 Workbench scopes 和刷新方式。

推荐生产前只读命令：

```bash
PYTHONPATH=backend/src python3 -m fin_ops_platform.tools.backfill_etc_batch_invoice_links --json --limit 0
PYTHONPATH=backend/src python3 -m fin_ops_platform.tools.repair_submitted_etc_invoice_overlaps --json --limit 0
```

只有用户确认后，才允许执行带 `--apply --reason --operator` 的 backfill/repair。

## 下一轮 bounded execution prompt

```text
读取 Phase 18 所有执行记录，连接目标生产/镜像库只运行 read-only dry-run，不做 --apply。目标：生成生产执行前最终审批包，包含 repair_submitted_etc_invoice_overlaps 与 backfill_etc_batch_invoice_links 的 exact row set、manual_review 清单、rollback plan、affected Workbench scopes、建议执行顺序和验证命令。停止条件：输出审批包并等待用户确认；不得执行任何生产写入。
```
