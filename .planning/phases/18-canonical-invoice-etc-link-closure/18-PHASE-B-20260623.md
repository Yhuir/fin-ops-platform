---
phase: 18-canonical-invoice-etc-link-closure
status: phase_b_minimal_boundary
created: 2026-06-23
---

# 18-PHASE-B-20260623：Link Table 最小事实源边界

## 已完成

- 新增 migration `0074_etc_batch_invoice_links.sql`：
  - `app.etc_batch_invoice_links`
  - active partial unique：`tenant_id + business_batch_id + identity_key`
  - active partial unique：`tenant_id + business_batch_id + invoice_id`
  - business batch、invoice、ETC invoice 查询索引
- 新增 `EtcBatchInvoiceLinkService`：
  - 接收明确 repository 依赖。
  - 校验 canonical invoice id、business batch id、invoice identity。
  - 幂等委托 repository upsert。
- 新增 `PostgresCoreRepository.upsert_etc_batch_invoice_link(...)`：
  - 先解析 canonical `app.invoices.id`。
  - 以 active batch identity 做 upsert。
  - 保留 raw payload 审计上下文。
- `ImportNormalizationService` submitted ETC 反向链接路径：
  - 严格匹配后继续写兼容 ETC metadata。
  - 同时写入 `app.etc_batch_invoice_links`。
- `WorkbenchSqlProjectionBuilder` open invoice 排除：
  - 先排除 active `app.etc_batch_invoice_links`。
  - 保留旧 `app.etc_invoices` fallback，支持尚未 backfill 的历史数据。

## 本轮未做

- 未执行生产 migration。
- 未 backfill 历史 `app.etc_invoices`、`app.invoices.etc_invoice_id`、`app.workbench_pair_relations.special_metadata` 到 link table。
- 未把 `_etc_invoice_summary_rows` 完全切到 link table。
- 未改 reset/runbook。
- 未执行任何真实库 `--apply`。

## 测试与验证

新增/更新测试：

- `tests/test_etc_batch_invoice_link_service.py`
- `tests/test_postgres_repositories_core.py::test_upsert_etc_batch_invoice_link_is_idempotent_by_batch_identity`
- `tests/test_import_service.py::ImportNormalizationServiceTests::test_input_invoice_import_links_existing_submitted_etc_metadata_when_formal_invoice_arrives_later`
- `tests/test_workbench_sql_runtime.py::WorkbenchSqlProjectionRelationPayloadTests::test_invoice_rows_excludes_visible_formal_invoices_already_bound_to_submitted_etc_batches`
- `tests/test_postgres_migrations.py`

已通过验证：

```bash
PYTHONPATH=backend/src python3 -m pytest tests/test_import_service.py tests/test_etc_batch_invoice_link_service.py tests/test_postgres_repositories_core.py::test_find_submitted_etc_invoice_by_identity_returns_active_batch_metadata tests/test_postgres_repositories_core.py::test_upsert_etc_batch_invoice_link_is_idempotent_by_batch_identity tests/test_workbench_sql_runtime.py::WorkbenchSqlProjectionRelationPayloadTests tests/test_repair_submitted_etc_invoice_overlaps_tool.py tests/test_postgres_migrations.py -q
PYTHONPATH=backend/src python3 -m compileall -q backend/src/fin_ops_platform/services/imports.py backend/src/fin_ops_platform/services/etc_batch_invoice_link_service.py backend/src/fin_ops_platform/services/postgres_repositories/core.py backend/src/fin_ops_platform/services/workbench_sql_projection.py backend/src/fin_ops_platform/tools/repair_submitted_etc_invoice_overlaps.py
bash scripts/verify.sh docs
git diff --check -- backend/src/fin_ops_platform/postgres/migrations/0074_etc_batch_invoice_links.sql backend/src/fin_ops_platform/services/imports.py backend/src/fin_ops_platform/services/etc_batch_invoice_link_service.py backend/src/fin_ops_platform/services/postgres_repositories/core.py backend/src/fin_ops_platform/services/workbench_sql_projection.py backend/src/fin_ops_platform/tools/repair_submitted_etc_invoice_overlaps.py tests/test_import_service.py tests/test_etc_batch_invoice_link_service.py tests/test_postgres_repositories_core.py tests/test_workbench_sql_runtime.py tests/test_repair_submitted_etc_invoice_overlaps_tool.py tests/test_postgres_migrations.py
```

## 下一轮 bounded execution prompt

```text
读取 Phase 18 的 18-CONTEXT.md、18-PLAN.md、18-AUDIT-20260623.md、18-PHASE-B-20260623.md，以及 settings/reset、ETC business batch delete/reset、Workbench `_etc_invoice_summary_rows`、repair/backfill 工具模式。目标：执行 Phase C 最小历史闭环，不执行生产写入；先写失败测试并实现 link table backfill dry-run 工具，覆盖从 app.etc_invoices + app.invoices.etc_invoice_id + submitted business batches 推导 active links、严格匹配自动 backfill、日期/金额/购销方 mismatch 进入 manual_review、rollback plan 输出、reset_invoices runbook 边界说明；然后让 Workbench ETC summary 读取在 link table 有数据时优先使用 link table，旧 `app.etc_invoices` 作为 fallback。允许改动范围：backfill/repair tool、repository/service read helpers、Workbench summary read path、settings reset 文档/必要测试、Phase 18 和模块文档。停止条件：dry-run/backfill 分类测试、Workbench summary link-table 优先测试、docs check 和 diff check 通过；不得执行生产 --apply/backfill/delete。
```
