---
phase: 18-canonical-invoice-etc-link-closure
status: phase_a_audit
created: 2026-06-23
---

# 18-AUDIT-20260623：Phase A 只读审计与 dry-run 结果

## 范围

- 数据库：当前本地 PostgreSQL `fin_ops` 镜像，只执行只读审计和 dry-run。
- Excel 镜像：`/Users/yu/Desktop/sy/财务运营平台/发票/进项全量发票查询导出结果1-6.22(1).xlsx`。
- 写入边界：本轮未执行任何真实数据库 `--apply`、删除、隐藏或 backfill。

## 发票池与 Excel 镜像

- `app.invoices` 总数：762。
- 进项：742；销项：20。
- 进项 visible：708；`hidden_after_etc_submission`：34。
- Excel `发票基础信息`：701 行含表头，699 个有效发票身份，699 个唯一身份，0 个重复身份；第 701 行为非空但无有效身份的尾部/异常行。
- Excel `信息汇总表`：950 行含表头，949 条明细行，按发票身份聚合后为 699 个唯一身份；与 `发票基础信息` 的发票级聚合差异数为 0。
- Excel 有效身份缺失于 DB：0。
- DB 多于 Excel 的进项：43。当前分类为 OA 附件/旧期间/非本 Excel 镜像覆盖范围候选，不能按“多余”直接删除。
- DB 与 Excel 字段 mismatch 发票数：37；字段计数为 seller_name 6、amount 32、tax_amount 32、total_with_tax 6、seller_tax_no 25。金额/税额 mismatch 需要后续单独按发票基础信息与导入来源分类，不能混入 ETC overlap 自动修复。

## ETC overlap 审计

- `app.etc_batch_invoice_links`：当前不存在。
- `app.etc_invoices`：submitted 149，unsubmitted 144。
- `app.etc_business_batches`：deleted 12，manually_marked_submitted 5。
- active ETC relation count：4。
- active ETC 发票唯一身份：149。
- `app.invoices` 与 submitted/manual-submitted/closed ETC 批次 overlap pairs：147。
- visible overlap pairs：113。
- 自动修复候选：112。
- 人工判定候选：1。
- 禁止自动处理/无需处理：34。

人工判定候选：

- 发票号码：`26537912330300001801`
- 原因：正式发票日期 `2026-03-13` 与 ETC metadata 日期 `2026-03-14` 不一致。

截图发票 `26537912570200055449` 当前事实：

- `app.invoices` id：`a6181d79-c3eb-4e20-bbd2-719215ed161d`
- 状态：visible，`etc_invoice_id` 为空。
- 日期/金额：`2026-02-28`，不含税 `18.63`，税额 `0.56`，含税 `19.19`。
- ETC metadata：`etc_invoice_0028`，业务批次 `etc_business_batch_hist_20260413_241125`，业务状态 `manually_marked_submitted`，日期/金额一致。

## Phase A 已完成的代码闭环

- 正式发票 import/upsert 后新增 submitted ETC metadata 反向链接：严格匹配发票号、日期、金额/税额/含税金额和购销方字段后，把 ETC metadata 合并到同一 canonical invoice，避免创建第二个发票事实。
- PostgreSQL repository 新增 submitted ETC identity 查询，查询 `app.etc_invoices` + `app.etc_business_batches` 的 submitted/manual-submitted/closed 范围。
- Workbench open invoice SQL 增加 submitted ETC overlap 防线，防止 visible canonical invoice 在迁移期继续作为普通 open invoice row 发布。
- 新增 dry-run-first 工具 `fin_ops_platform.tools.repair_submitted_etc_invoice_overlaps`：
  - 默认只 dry-run。
  - `--apply` 必须提供 `--reason` 和 `--operator`。
  - apply 只处理严格匹配 auto candidates。
  - apply 后 enqueue 受影响 Workbench month scopes 与 `all`。

真实库 dry-run 命令：

```bash
PYTHONPATH=backend/src python3 -m fin_ops_platform.tools.repair_submitted_etc_invoice_overlaps --json --limit 0
```

真实库 dry-run 结果：

- status：`attention`
- overlap_pair_count：147
- auto_fix_candidate_count：112
- manual_review_candidate_count：1
- no_action_candidate_count：34
- affected_workbench_scopes：`2026-01`、`2026-02`、`2026-03`、`2026-04`、`all`
- `apply_required_confirmation`：true

## 测试与验证

新增/更新测试：

- `tests/test_import_service.py::ImportNormalizationServiceTests::test_input_invoice_import_links_existing_submitted_etc_metadata_when_formal_invoice_arrives_later`
- `tests/test_postgres_repositories_core.py::test_find_submitted_etc_invoice_by_identity_returns_active_batch_metadata`
- `tests/test_workbench_sql_runtime.py::WorkbenchSqlProjectionRelationPayloadTests::test_invoice_rows_excludes_visible_formal_invoices_already_bound_to_submitted_etc_batches`
- `tests/test_repair_submitted_etc_invoice_overlaps_tool.py`

已通过验证：

```bash
PYTHONPATH=backend/src python3 -m pytest tests/test_import_service.py tests/test_postgres_repositories_core.py::test_find_submitted_etc_invoice_by_identity_returns_active_batch_metadata tests/test_workbench_sql_runtime.py::WorkbenchSqlProjectionRelationPayloadTests -q
PYTHONPATH=backend/src python3 -m pytest tests/test_repair_submitted_etc_invoice_overlaps_tool.py -q
PYTHONPATH=backend/src python3 -m fin_ops_platform.tools.repair_submitted_etc_invoice_overlaps --help
```

## 下一轮 bounded execution prompt

```text
读取 Phase 18 的 18-CONTEXT.md、18-PLAN.md、18-AUDIT-20260623.md，以及 migrations、PostgresCoreRepository、ImportNormalizationService、WorkbenchSqlProjectionBuilder 和 ETC business batch 写入路径。目标：执行 Phase B 的最小事实源边界切片，新增 app.etc_batch_invoice_links migration、repository 和幂等 service；先写失败测试覆盖同一 business batch + invoice identity 只能有一个 active link、重复 upsert 幂等、removed 后可重建、正式发票反向链接写入 link table 而不是只写 raw metadata、Workbench open invoice 排除优先读 link table。允许改动范围：migration、repository/service、相关测试、Phase 18 文档。停止条件：Phase B schema/repository/service 最小闭环测试通过，旧 app.etc_invoices 读取路径仍保留兼容但不新增业务行为；不执行生产 backfill/apply。验证命令：针对新增 migration/repository/service/Workbench SQL/import service 的 pytest，compileall，git diff --check。
```
