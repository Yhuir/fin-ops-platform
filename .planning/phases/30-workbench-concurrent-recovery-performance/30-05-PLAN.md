---
phase: 30-workbench-concurrent-recovery-performance
plan: "05"
type: execute
wave: 4
depends_on:
  - 30-04
files_modified:
  - backend/src/fin_ops_platform/services/postgres_repositories/oa_pending_payment_source_snapshot.py
  - backend/src/fin_ops_platform/services/oa_pending_payment_sql_projection.py
  - backend/src/fin_ops_platform/tools/write_operation_e2e_smoke.py
  - tests/test_oa_pending_payment_source_snapshot_repository.py
  - tests/test_oa_pending_payment_read_model_refresh.py
  - tests/test_oa_pending_payment_postgres_integration.py
  - tests/test_write_operation_e2e_smoke.py
  - docs/modules/oa-pending-payments/boundary-io.md
  - docs/modules/oa-pending-payments/tests.md
  - docs/modules/oa-pending-payments/implementation-notes.md
autonomous: true
requirements:
  - AUDIT-04
  - RELCL-01
  - RELCL-02
  - RELCL-03
  - RELCL-05
  - RMF-02
  - RMF-03
  - RMF-08
must_haves:
  truths:
    - "OA 待付款只消费结构化 SQL status=active 的 Workbench relation；撤回 relation 的历史/raw payload 不能继续产生 bank/invoice relationCount。"
    - "OA freshness proof 对 active relation membership 敏感；撤回较旧 relation 时，即使同月仍有更新时间更晚的 active relation，也必须判 stale。"
    - "Workbench relation 生产 fixture 的 Cost probe 必须访问 Workbench-dependent active profile；time/bank_tag 不能冒充 relation allocation proof。"
    - "确认与撤回仍然 zero write-after fan-out；页面只在访问时按 exact scope 收敛。"
    - "Candidate C 只修已证明的 OA 根因与验证合同，不臆测修改 Cost runtime。"
  artifacts:
    - path: "backend/src/fin_ops_platform/services/postgres_repositories/oa_pending_payment_source_snapshot.py"
      provides: "set-based、membership-sensitive 的 active Workbench relation source proof"
    - path: "backend/src/fin_ops_platform/services/oa_pending_payment_sql_projection.py"
      provides: "结构化 status 过滤后的 active-only canonical relation reader"
    - path: "backend/src/fin_ops_platform/tools/write_operation_e2e_smoke.py"
      provides: "relation-impact Cost consumer 的 profile/semantic assertion fail-closed gate"
  key_links:
    - from: "backend/src/fin_ops_platform/services/postgres_repositories/oa_pending_payment_source_snapshot.py"
      to: "backend/src/fin_ops_platform/services/postgres_repositories/read_models.py"
      via: "expected/actual source vector equality"
      pattern: "oa_pending_payment_workbench_pair_relations"
    - from: "backend/src/fin_ops_platform/services/oa_pending_payment_sql_projection.py"
      to: "backend/src/fin_ops_platform/services/postgres_repositories/workbench_relation.py"
      via: "load_active_workbench_pair_relations_for_row_ids"
      pattern: "load_active_workbench_pair_relations_for_row_ids"
    - from: "backend/src/fin_ops_platform/tools/write_operation_e2e_smoke.py"
      to: "/api/cost-statistics/explorer"
      via: "active Workbench-dependent relation consumer contract"
      pattern: "cost-statistics"
---

<objective>
一次性修复生产撤回后 OA 待付款仍显示旧 bank/invoice relation 的共享根因，并修正不能证明 Cost relation allocation 的生产 fixture 合同。

Purpose: 让 confirm/withdraw、OA source proof、active-only projection、Cost relation-impact probe、System Audit 和 inverse recovery 形成同一条可验证闭环。
Output: 最小运行时代码、回归测试、必要模块文档、Candidate C 单次部署与生产证据。
</objective>

<execution_context>
@/Users/yu/.codex/gsd-core/workflows/execute-plan.md
@/Users/yu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/30-workbench-concurrent-recovery-performance/30-04-PLAN.md
@docs/architecture/module-boundaries/read-model-contracts.md
@docs/modules/oa-pending-payments/README.md
@docs/modules/oa-pending-payments/boundary-io.md
@docs/modules/oa-pending-payments/state-machine.md
@docs/modules/oa-pending-payments/tests.md
@docs/modules/cost-statistics/boundary-io.md
@docs/modules/cost-statistics/state-machine.md
@docs/modules/runtime-workers/boundary-io.md
@docs/operations/runtime-worker-governance.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 OA active relation source proof 与 projection authority</name>
  <files>backend/src/fin_ops_platform/services/postgres_repositories/oa_pending_payment_source_snapshot.py, backend/src/fin_ops_platform/services/oa_pending_payment_sql_projection.py, tests/test_oa_pending_payment_source_snapshot_repository.py, tests/test_oa_pending_payment_read_model_refresh.py, tests/test_oa_pending_payment_postgres_integration.py</files>
  <behavior>
    - active relation proof 同时包含 count 与稳定 digest；active set、typed members、mode、版本或被消费 payload 变化必须改变 proof。
    - 撤回较旧 relation，即使同月仍有更晚 active relation，也改变 proof。
    - projector 只调用 repository 已有 active-only overlap reader，不再从 raw payload 推断结构化 activity。
    - inactive SQL row 即使 raw payload 仍写 active，也投影为 OA 单行且 bank/invoice relationCount=0。
    - legitimate active relation 的现有聚合、Turnover closure 排除和 in-progress pending relation保持不变。
  </behavior>
  <action>
先写失败测试，再复用现有 `load_active_workbench_pair_relations_for_row_ids()`；删除 `_active_relations` 旧过滤 owner。source proof 保持一个 set-based SQL，不加 migration、表、cache、worker、queue 或新 abstraction；bump OA projector/source contract，使已发布旧 scope 在下一次访问时必然判 stale。
  </action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_oa_pending_payment_source_snapshot_repository tests.test_oa_pending_payment_read_model_refresh tests.test_oa_pending_payment_postgres_integration</automated>
  </verify>
  <done>OA fresh gate 与 projector 都以同一个结构化 active canonical set 为事实，旧 relation 无法继续显示或伪装 fresh。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收紧 relation-impact Cost production fixture 合同</name>
  <files>backend/src/fin_ops_platform/tools/write_operation_e2e_smoke.py, tests/test_write_operation_e2e_smoke.py</files>
  <behavior>
    - bank_oa_invoice confirm/withdraw 的 Cost affected consumer 拒绝 time/bank_tag profile。
    - 必须使用 project_scope=active 的 project/bank/expense_type profile。
    - 仅按数组位置断言 transaction_id 不能作为 relation-derived semantic proof。
    - 非 relation-impact scenario 和 isolation consumers 的既有合同不改变。
  </behavior>
  <action>
复用 runner 现有 scenario validation；只增加 bank_oa_invoice relation-impact Cost probe 的最小 fail-closed 条件，不创建第二 runner或通用 schema framework。候选生产 scenario 使用临时副本，不回写历史 Phase 20 fixture。
  </action>
  <verify>
    <automated>PYTHONPATH=backend/src python3 -m unittest tests.test_write_operation_e2e_smoke</automated>
  </verify>
  <done>生产 smoke 不能再用 Bank Detail-only view 证明 Cost relation allocation。</done>
</task>

<task type="auto">
  <name>Task 3: 集中验证、单次 Candidate C、确认与撤回生产闭环</name>
  <files>docs/modules/oa-pending-payments/boundary-io.md, docs/modules/oa-pending-payments/tests.md, docs/modules/oa-pending-payments/implementation-notes.md</files>
  <action>
先一次性运行 OA source/projection、runner、Workbench relation、Cost profile、Page/System Audit、read-model/queue boundary定向测试和 lint，收集完整失败后集中修复并只复跑同组；不运行 full CI、pytest 或 183 Browser tests。只更新 OA 已变化的 projector/source proof 事实与测试记录；Cost runtime合同未变。

全部本地门通过后一次 push remote main、一次官方部署 Candidate C。使用 admin-token wrapper、一个 test-owned 可逆 bank_oa_invoice 临时 scenario、fresh idempotency keys。先 dry-run，再执行 inactive preimage→confirm→affected consumers→withdraw→affected consumers；Cost 同时访问 active Workbench-dependent allocation profile和 time Bank Detail profile。要求 canonical paired→unpaired、OA relationCount 1→0、Cost active exact scope收敛、其它页面完整/正确、isolation页面不变、mutation immediate fan-out为零、同 scope原子去重、queue/worker drained、System Audit 16/16。记录实际 write/preview/各页面 access-to-fresh耗时；本轮不以3秒为 correctness硬门，但不允许无界、失败或 falsely-fresh。最后删除 exact remote temp scenario并验证不存在。
  </action>
  <verify>
    <automated>bash scripts/verify.sh lint</automated>
  </verify>
  <done>同一 Candidate C 上完成 confirm→fresh/correct→withdraw→fresh/correct→inverse/Audit/queue闭环，并留下真实性能证据。</done>
</task>

</tasks>

<verification>
1. 后端只跑受影响的 unittest/service/read-model/API/Audit/runner矩阵与 repository lint。
2. 不改变 API response shape，不新增前端代码；只保留既有关键 Workbench Chromium证据，不运行183项套件。
3. 生产 mutation只允许一个 test-owned confirm/withdraw inverse scenario，所有写走正式 API。
4. 3秒只记录性能结果，不作为本轮 correctness门；fresh、完整、正确、zero fan-out、inverse和Audit仍是硬门。
</verification>

<success_criteria>
- inactive/cancelled Workbench relation不能进入 OA projection。
- membership变化必使OA scope stale，访问后exact scope重建。
- Cost生产probe访问active Workbench-dependent profile，不再误用time view。
- confirm/withdraw后所有受影响页面fresh且数据/配对正确，isolation页面不污染。
- 生产队列/worker收敛、System Audit 16/16、fixture恢复inactive、临时文件删除。
</success_criteria>

<output>
Create `.planning/phases/30-workbench-concurrent-recovery-performance/30-05-SUMMARY.md` when done.
</output>
