---
phase: 36-right-drawer-motion-production-closure
plan: "01"
type: execute
wave: 1
depends_on:
  - 35-01
files_modified:
  - web/src/components/common/AppDrawer.tsx
  - web/src/components/reconciliation/DetailDrawer.tsx
  - web/src/pages/OaPendingPaymentsPage.tsx
  - web/src/pages/BankFlowRuleBatchPage.tsx
  - web/src/components/tax/CertifiedResultsDrawer.tsx
  - web/src/app/styles.css
  - web/src/test/CommonPlatformComponents.test.tsx
  - web/src/test/OaPendingPaymentsPage.test.tsx
  - web/src/test/BankFlowRuleBatchPage.test.tsx
  - web/src/test/TaxOffsetPage.test.tsx
  - web/e2e/drawer-motion.spec.ts
  - docs/refactor-ui/interaction_smoothness.md
  - docs/modules/finance-table-system/implementation-notes.md
  - docs/modules/finance-table-system/tests.md
  - docs/modules/oa-pending-payments/implementation-notes.md
  - docs/modules/oa-pending-payments/tests.md
  - docs/modules/bank-flow-rule-batches/implementation-notes.md
  - docs/modules/bank-flow-rule-batches/tests.md
  - docs/modules/tax-offset/implementation-notes.md
  - docs/modules/tax-offset/tests.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "所有模态右抽屉复用 HeroUI Drawer 的 right placement 和原生 translate 状态，不再被自定义 transform/keyframes 覆盖。"
    - "OA 关联支出流水与流水规则标签管理只迁移展示壳，原 API、权限、表单、忙碌态、错误态和写入时机不变。"
    - "税金抵扣认证结果栏保持 complementary rail 语义，通过 transform/opacity 展开收起，并对隐藏内容应用 inert、aria-hidden 和 pointer-events。"
    - "Workbench 详情只接受同一 active generation 的完整 row detail；版本冲突最多重试一次，其它不可用状态不盲重试。"
    - "删除冲突旧动画、旧自定义抽屉壳和重复 Escape 监听；不新增第三方动画依赖、fallback 或第二套 drawer abstraction。"
  artifacts:
    - path: "web/src/components/common/AppDrawer.tsx"
      provides: "全站模态/持久右抽屉的唯一生命周期与可访问性壳"
    - path: "web/src/app/styles.css"
      provides: "HeroUI native translate、240ms enter/180ms exit、reduced-motion 和 rail motion 合同"
    - path: "web/e2e/drawer-motion.spec.ts"
      provides: "真实浏览器 enter/exit 中间帧、布局稳定、reduced-motion 与网络隔离证据"
  key_links:
    - from: "web/src/components/common/AppDrawer.tsx"
      to: "@heroui/react Drawer"
      via: "Drawer.Backdrop, Drawer.Content placement=right, Drawer.Dialog"
      pattern: "Drawer.Content"
    - from: "web/src/pages/OaPendingPaymentsPage.tsx"
      to: "web/src/components/common/AppDrawer.tsx"
      via: "display-shell reuse only"
      pattern: "AppDrawer"
    - from: "web/src/pages/BankFlowRuleBatchPage.tsx"
      to: "web/src/components/common/AppDrawer.tsx"
      via: "display-shell reuse only"
      pattern: "AppDrawer"
---

<objective>
修复 HeroUI 原生 right Drawer 动画被项目 CSS 覆盖而瞬时出现的根因，把两处遗留自定义抽屉迁移到共享壳，
以最少的 transform/opacity CSS 完成税务结果栏动效，并将已落地的 Workbench 同代详情修复一起发布和生产验证。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 恢复共享 AppDrawer 原生 right translate 合同</name>
  <files>web/src/components/common/AppDrawer.tsx, web/src/components/reconciliation/DetailDrawer.tsx, web/src/app/styles.css, web/src/test/CommonPlatformComponents.test.tsx</files>
  <action>保留现有 HeroUI `Drawer.Backdrop`、`Drawer.Content placement="right"`、`Drawer.Dialog` 和非模态 persistent 生命周期；删除 `.finance-drawer` 上覆盖 HeroUI individual `translate` 的 transform transition、28px/22px keyframes和永久 will-change。让 modal content 使用 `translate 240ms cubic-bezier(0.25,1,0.5,1)` enter 与 180ms exit，backdrop 只过渡 opacity，pointer-events 在退出期 fail closed；persistent branch 使用同一 100%→0→100% 视觉路径并保留 180ms 卸载计时。覆盖 rapid open-close-open 与 `prefers-reduced-motion`/`data-reduce-motion`。删除 Workbench `DetailDrawer` 自己的 window Escape listener，由 HeroUI/AppDrawer 独占关闭语义。只有真实组件测试复现退出内容闪烁时，才在 AppDrawer 中保留最后一次 open children 到退出完成；禁止预防性页面 state。</action>
  <verify>npm --prefix web test -- --run src/test/CommonPlatformComponents.test.tsx src/test/ReconciliationWorkbenchPage.test.tsx</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 迁移遗留自定义抽屉并实现 tax rail 动效</name>
  <files>web/src/pages/OaPendingPaymentsPage.tsx, web/src/pages/BankFlowRuleBatchPage.tsx, web/src/components/tax/CertifiedResultsDrawer.tsx, web/src/app/styles.css, web/src/test/OaPendingPaymentsPage.test.tsx, web/src/test/BankFlowRuleBatchPage.test.tsx, web/src/test/TaxOffsetPage.test.tsx</files>
  <action>将 `OaBankLinkDrawer` 的自定义 backdrop/aside/header/close shell 替换为 `AppDrawer`，保留 560px、搜索、筛选、分页、选择、提交、加载、错误、权限和 AbortController 时机，删除 `if (!open) return null` 对退出生命周期的阻断。将流水规则标签管理自定义 shell 替换为 `AppDrawer`，保留 `min(960px,92vw)`、表格、footer、权限、dirty/busy 和现有 nested dialog 行为。删除对应旧 shell/header/close CSS，只保留业务内容样式。认证结果 `aside` 保持 mounted/complementary，在内容容器上用 100% translateX 与 opacity collapse/expand，隐藏态设置 inert/aria-hidden/pointer-events，完成切换后把焦点留在 toggle；不动画 grid/width。所有新增动画仅 transform/opacity。</action>
  <verify>npm --prefix web test -- --run src/test/OaPendingPaymentsPage.test.tsx src/test/BankFlowRuleBatchPage.test.tsx src/test/TaxOffsetPage.test.tsx</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 全站清理门禁、浏览器动效与文档</name>
  <files>web/e2e/drawer-motion.spec.ts, web/src/test/CommonPlatformComponents.test.tsx, web/src/test/OaPendingPaymentsPage.test.tsx, web/src/test/BankFlowRuleBatchPage.test.tsx, web/src/test/TaxOffsetPage.test.tsx, docs/refactor-ui/interaction_smoothness.md, docs/modules/finance-table-system/implementation-notes.md, docs/modules/finance-table-system/tests.md, docs/modules/oa-pending-payments/implementation-notes.md, docs/modules/oa-pending-payments/tests.md, docs/modules/bank-flow-rule-batches/implementation-notes.md, docs/modules/bank-flow-rule-batches/tests.md, docs/modules/tax-offset/implementation-notes.md, docs/modules/tax-offset/tests.md</files>
  <action>whole-repo 扫描所有 right drawer 入口，确认 modal 路径都落到 AppDrawer/HeroUI、税务结果栏是唯一明确的 non-modal rail、设置标签入口仍是 popover/menu 而非 drawer。加入源码负向门禁阻止旧 OA/bank-flow shell、旧 keyframes、重复 Escape 和动画库回归。浏览器用 requestAnimationFrame/DOMMatrix 采样 enter/exit 中间帧，断言右→左进入、左→右退出、CLS=0、无额外业务网络请求、reduced-motion 近即时；不要写脆弱的逐像素截图测试。文档只记录 motion/test/legacy cleanup；业务 I/O、模块职责和 API shape 不变，因此不修改 boundary-io。</action>
  <verify>npm --prefix web test -- --run src/test/CommonPlatformComponents.test.tsx src/test/OaPendingPaymentsPage.test.tsx src/test/BankFlowRuleBatchPage.test.tsx src/test/TaxOffsetPage.test.tsx && npm --prefix web run build && bash scripts/verify.sh lint</verify>
</task>

<task type="auto">
  <name>Task 4: 发布并完成生产正确性/性能闭环</name>
  <files>.planning/phases/36-right-drawer-motion-production-closure/36-01-SUMMARY.md, .planning/phases/36-right-drawer-motion-production-closure/36-VERIFICATION.md</files>
  <action>实现阶段只完成本地提交和验证，最终 push/deploy 由主编排器在审查通过后执行。发布时把当前 main 的 `825d34011` Workbench 修复与本计划抽屉修复作为同一候选 push 到 `origin/main`，按 `./scripts/deploy-oa.sh` 部署。生产用 token wrapper 验证 Workbench visible group 的同代 detail 成功、版本冲突只重试一次、不可用状态明确失败且不触发旧 stale-source recheck；浏览器覆盖共享 AppDrawer、OA bank-link、bank-flow tag drawer 和 tax rail，采样 enter/exit 中间帧、长任务、CLS、网络增量和详情 latency。目标：详情暖读 p95&lt;1s，drawer frame interval p95&lt;=25ms，CLS=0，动画开关本身新增业务请求=0；检查队列/worker/System Audit 健康。生产探针只读，除非使用仓库既有可逆且明示授权的 fixture；不得操作真实业务关系。</action>
  <verify>bash scripts/verify.sh lint</verify>
</task>

</tasks>

<success_criteria>
- 所有模态右抽屉从视口右侧完整滑入并滑出，enter 240ms、exit 180ms，reduced-motion 生效。
- OA bank-link 和 bank-flow tag-management 不再拥有自定义 backdrop/aside/close shell，业务行为不变。
- tax certified-results rail 只动画 transform/opacity，隐藏内容不可交互且无 layout shift。
- 旧 28px/22px keyframes、冲突 transform transition、永久 will-change、重复 Escape 和旧 shell whole-repo 不可达。
- Workbench 同代详情、页面隔离、frontend/backend regression、build、lint、部署与生产健康验证通过。
- 生产详情暖读 p95 小于 1 秒，drawer frame interval p95 不高于 25ms，CLS 为 0，动画不增加业务 API 请求。
</success_criteria>

<output>
Create `.planning/phases/36-right-drawer-motion-production-closure/36-01-SUMMARY.md` when done.
</output>
