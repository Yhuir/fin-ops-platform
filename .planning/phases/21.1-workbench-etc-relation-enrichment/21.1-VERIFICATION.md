# Phase 21.1 Verification

## 发布前结论

Phase 21.1 的代码、migration、模块边界、旧链删除和定向回归已闭合，可以进入精确 SHA 的生产部署与验证。生产通过前不宣称最终完成。

## 已验证事实

- exact candidate 只使用 completed OA `normalized_payload.etc_batch_id` 与 submitted ETC business batch external identity；没有金额、名称、OCR、附件或模糊匹配。
- external batch advisory lock、OA/batch row lock、typed OA member、唯一 OA/批次/active relation owner、invoice count 与 total amount 均在关系写事务内复验；任一冲突整批失败。
- 同轮新建/扩展 relation 在首次保存前合并 ETC metadata；已有 relation 只执行一次 changed-case enrichment save。
- paired attachment 与 unpaired exclusion 使用同一 fail-closed marker resolver；冲突 marker 不静默选择 winner。
- 关联台 Page Audit 独占两条 ETC owner proof，不增加成本统计或其他页面 Audit 查询预算。
- ordinary matching 事件保留原 60 秒 debounce I/O；只有 completed OA 和 ETC submitted 状态事件显式使用零 debounce。
- processing scope 收到新事件时保留 lease，当前 run 完成后立即重新 dirty；没有丢更新或双 worker claim。
- UI 折叠数量优先 canonical collapsed detail count；aggregate summary row 不计入发票张数。
- 两个 operator-only service、两个 CLI、三个专属测试与当前文档入口已删除；architecture guard 固化文件不得恢复。`HistoricalEtcRepairService` 保留。

## 发布前验证

- `bash scripts/verify.sh lint`：通过。
- `bash scripts/verify.sh docs`：通过。
- Phase 21.1 最终后端定向矩阵：`598 passed + 73 subtests`，1 个条件性测试 skipped。
- 隔离回归（成本统计 Audit budget、settings reset、WorkBench write UoW、dirty queue、Page Audit）：`115 passed + 6 subtests`，1 个条件性 PostgreSQL测试 skipped。
- disposable PostgreSQL：实际应用 `0001–0116`；ETC exact candidate/事务复验/Audit owner proof/urgent queue/index `3 passed`，临时库自动删除。
- 前端全量：`72 files / 854 tests passed`；production build 通过，仅保留既有依赖 CSS/chunk warning。
- 整仓后端：运行 `4217` 项；本轮涉及链路通过。仍有 4 个 bank-flow-rule-batch local read repository 夹具错误和 1 个既有 bank+invoice→cost statistics impact-matrix 合同错误；涉及文件均未被 Phase 21.1 修改，不作为本轮扩 scope 的理由。
- whole-repo 生产调用方扫描：已删除 service/tool 在 backend、web、scripts、deploy、tests 中零 runtime caller；当前文档只保留明确历史记录。
- `git diff --check`：通过。

## 生产门

部署后必须全部满足：

1. migration 0116 applied，workbench-matching env 为 `0.25s`，worker ready/idle 且 matching/read-model queue drained。
2. `etc_business_batch_0014` / `etc_20260622_001` canonical 发票数为 34、金额为 1584.35。
3. OA 与流水所在 active relation 拥有唯一一致的 `etc_batch_link`；ETC summary 在 paired 中出现且 unpaired 中为零。
4. 关联台 Page Audit 通过；其它页面 Audit/query budget 没有因 ETC proof 增量受影响。
5. Workbench fresh API p95 `<= 1s`；canonical change 到 fresh p99 `<= 3s`。记录 matching candidate SQL、queue wait、worker run、projection publish 的分段耗时。
6. 0.25 秒 idle poll 没有造成异常 DB CPU、连接池或 backlog。
