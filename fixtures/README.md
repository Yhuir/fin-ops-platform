# Fixtures

这个目录只放不会影响运行逻辑的样例数据和手工验收文件。

当前约定：

- `测试数据/`：功能开发和自动化测试共同依赖的固定样例
- `测试用银行流水下载/`：银行流水原始样例文件
- `发票信息导出1-3月/`：发票导出原始样例文件
- `过路费发票/`：专项票据样例
- 根目录下散落的 Excel 已统一归档到这里，避免污染仓库顶层

使用建议：

- 会被自动化测试直接引用的样例，尽量保持文件名和内容稳定
- 只用于手工核对的大文件，优先放在这里而不是仓库根目录
- 运行时产物、下载缓存、临时导出结果不要提交到这个目录
